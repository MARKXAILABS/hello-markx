node_modules/
node_modules
out/
dist/
.DS_Store
*.log
.vite/
.env
.env.local
.env.signing
.gstack/

# signing / notarization secrets — NEVER commit these
*.p8
*.p12
*.cer
*.mobileprovision
build/AuthKey_*.p8


# git worktree mounts for hive worker agents — never commit (each is a separate branch checkout)
.worktrees/

# local CodeFlow session snapshots
.codeflow/

# a 333 KB Public Suffix List dump left by the discuss phase — never tracked
# (git ls-files psl.dat is empty); named narrowly so a future *.dat fixture
# (e.g. test/fixtures/sample.dat) is never silently swallowed
/psl.dat
