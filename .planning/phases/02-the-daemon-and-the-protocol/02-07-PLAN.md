---
phase: 02-the-daemon-and-the-protocol
plan: 07
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/shared/agentProvider.ts
  - src/shared/providerAutomation.ts
  - src/main/hive.ts
  - src/main/hiveProvisioning.ts
  - src/main/hiveTemplates.ts
  - test/engine-parity.test.cjs
  - test/agent-provider.test.cjs
  - test/provider-automation.test.cjs
  - test/repo-claims.test.cjs
autonomous: true
requirements: [PARITY-01a, PARITY-02, PARITY-03]

must_haves:
  truths:
    - "D-33: PARITY-01a's work is kimi's BRIDGE, not a label — but the flag it rides on is checked FIRST. `kimi --help` runs before a line of bridge code is written, because Moonshot documents `--yolo`/`-y`, `--yes`, `--auto-approve` and `--afk` while the preset ships `--auto`, and an inbox delivered into a CLI that will not start is worse than the bounce it replaces"
    - "D-33: the kimi bridge is the CODEX case, not the grok case — `HOOK_SHIM` is reused verbatim, `installKimiConfig` seeds the operator's `~/.kimi/config.toml` as a STRING and appends `[[hooks]]`, and no TOML parser is introduced because none exists in Node core or in this repo's dependencies (D-06 forbids adding one)"
    - "D-33: the second consequence is RULED ON, not discovered — flipping `kimi.canReceiveInbox` to true makes kimi god-eligible at `CommandCenterPanel.tsx` and `OnboardingWizard.tsx` (both filter `AGENT_PROVIDER_PRESETS` by `canReceiveInbox`) while kimi has no interactive initial-prompt form. This plan flips it, does NOT give kimi `seedDelivery:'type-into-tui'`, makes `ensureAgent`'s silent bare-spawn fall-through record itself, and hands the two pickers to plan 02-06 by name"
    - "D-33 / D-35: the kimi bridge ships `LIVE-UNVERIFIED` in this plan's own diff at every site it touches. It closes a CAPABILITY gap (engines that can receive mail: 8 to 9) and OPENS a verification gap (unverified bridges: 4 to 5). The SUMMARY states both, in that order, with the sign the right way round"
    - "D-34: PARITY-02's buildable reading is adopted in plan text and in source. MEASURED CORRECTION the executor re-verifies: `bridgeOf` returns ONE descriptor (`preset.bridge` first, else `{kind:'hooks'}` derived from `hookBridge`) and `hive.ts`'s dispatch is `if (desc.kind === 'hooks') ... else if (desc.kind === 'proxy')`. So converting grok / kimi / opencode to `bridge:{kind:'proxy'}` for cost would DELETE their hooks bridge, trading PARITY-01a's mail for PARITY-02's number. Zero engines are converted, and the reason is written into `BridgeDescriptor`'s own doc comment"
    - "D-34: copilot and custom get no number because they have none, and antigravity's `gemini` sidecar api mode is named as NOT delivered. The requirement's wording is never quietly narrowed — the SUMMARY says which engines report cost, which do not, and why each one does not"
    - "D-32: `costTracking` is a DECLARATION, not a wire — measured: its only consumer anywhere in `src/` is `providerCapabilities` in `providerAutomation.ts`. PARITY-02's automated half is therefore a test that the declaration matches the wiring (`costTracking === 'proxy'` iff `bridgeOf(p)?.kind === 'proxy'`), which is the only assertion that can go red when the two drift"
    - "D-35: PARITY-03's pin is written against a ledger the executor MEASURES, and it binds BOTH directions — unmarking a bridge and silently adding one both go red. It is deliberately tighter than D-35's one-directional rule, per D-40"
    - "D-40: the marker pin is the one clause in `test/repo-claims.test.cjs` that must NOT use `stripComments` — `LIVE-UNVERIFIED` lives inside comments, and a comment-stripped grep for it would count zero in every file forever. The clause reads the raw file, says in its own body why it is the exception, and carries an exact upper AND lower bound plus a repo-wide total equal to the sum of the per-file map"
    - "D-40: `test/agent-provider.test.cjs` is NOT a `node:test` file — it is a self-contained script with its own `test()` helper and `process.exit(1)`. Measured: `node --test --test-reporter=tap test/agent-provider.test.cjs` reports `# tests 1 / # pass 1` for the WHOLE file regardless of how many cases it holds, so a TAP-counter criterion against it passes on an empty file. Its acceptance is a direct run: exit code plus a counted `✓` total"
    - "UI-SPEC Rule C-1b: `supportsMcp` is a REQUIRED preset field surfaced as `ProviderCapabilities.mcp`, and it carries NO behavioural claim about the MCP channel — D-25 live-verified that today's `--settings` bundle is a complete no-op for every engine, and plan 02-11 proves the `--mcp-config` channel before anything renders it. `capabilityLine`'s STRING is NOT extended with an MCP clause"
    - "UI-SPEC Rule C-1a step 1 ONLY: `providerCapabilities(provider, platform?)` forwards as `remoteControlAvailability(provider, platform ?? process.platform)`, and the contradicting ADR-0002 JSDoc is amended in the SAME change. Steps 2 (the preload `platform` field) and 3 (renderer call sites plus the no-one-argument-call clause) are NOT written here — at wave 4 there are zero renderer call sites, so that clause would pass vacuously (D-40). Both are handed to plan 02-06"
    - "D-30 honesty carry: the new `mcp` bit lands with ZERO production consumers, exactly like `capabilityLine` does today. The SUMMARY records that as a stated fact and does NOT count it as PARITY-01b delivered; plan 02-06 lands the consumer and the clause that proves it"
    - "D-01: every count in this plan — marker totals, per-file splits, suite counters, line numbers — is re-measured by the executor in its own session. Plan 02-03 edits `src/main/hive.ts` in wave 3, so every `hive.ts` line reference below is stale by construction"
    - "D-06: `package.json` and `package-lock.json` are untouched; zero npm packages are added or removed; no TOML parser is introduced"
  artifacts:
    - path: "src/main/hiveProvisioning.ts"
      provides: "installKimiConfig — the kimi hook bridge, modelled on installCodexHooks, returning the per-agent CONFIG FILE path for `--config-file`"
      contains: "installKimiConfig"
    - path: "src/shared/agentProvider.ts"
      provides: "the eleven-preset ledger: kimi's bridge and flipped inbox, the `hookBridge` union widened by one string, and a REQUIRED `supportsMcp` on every preset"
      contains: "supportsMcp"
    - path: "src/shared/providerAutomation.ts"
      provides: "ProviderCapabilities.mcp and providerCapabilities(provider, platform?), with the ADR-0002 JSDoc amended in the same change"
      contains: "platform ?? process.platform"
    - path: "test/repo-claims.test.cjs"
      provides: "PARITY-03's marker pin — an exact per-file ledger plus a repo-wide total, over RAW (not comment-stripped) source"
      contains: "LIVE-UNVERIFIED"
  key_links:
    - from: "hive.ts ensureAgent, the `desc.kind === 'hooks'` arm"
      to: "installKimiConfig"
      via: "preArgs.push('--config-file', installKimiConfig({…})) — the same shape codex uses for --dangerously-bypass-hook-trust"
      pattern: "'--config-file'"
    - from: "ProviderCapabilities.mcp"
      to: "AgentProviderPreset.supportsMcp"
      via: "one field read inside providerCapabilities; no second derivation path (UI-SPEC Rule C-1b)"
      pattern: "mcp: preset\\.supportsMcp"
    - from: "test/repo-claims.test.cjs marker pin"
      to: "every LIVE-UNVERIFIED site in src/"
      via: "an exact per-file map whose sum is asserted equal to the repo-wide total"
      pattern: "LIVE-UNVERIFIED"
---

<objective>
Own the eleven-preset engine ledger and every spawn-time bridge derived from it, so that no other plan
in this phase edits `src/shared/agentProvider.ts` or `src/shared/providerAutomation.ts`.

Three requirements land here and they do not point the same way. **PARITY-01a** builds kimi's hook
bridge, which moves the count of engines that can receive mail from 8 to 9. **PARITY-03** pins the
`LIVE-UNVERIFIED` ledger — and the kimi bridge, built without a Moonshot account on a machine where
`kimi` is not installed, makes it the FIFTH unverified bridge, not one fewer (D-33). **PARITY-02**
cannot be met as written (D-34), and the buildable reading turns out narrower than D-34 assumed:
`bridgeOf` returns exactly one descriptor per engine and the spawn dispatch is `hooks` XOR `proxy`, so
every remaining cost candidate — grok, kimi, opencode — would have to give up its mail bridge to gain a
cost number. This plan converts none of them, says so in source, and delivers PARITY-02's automated
half as the one assertion that can actually fail: the declaration matching the wiring.

Output: kimi's bridge shipping marked; a capability ledger the card can later read (`supportsMcp` to
`mcp`, plus C-1a's platform forwarding); a costTracking-to-wiring parity test; and a marker pin that
goes red in both directions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-the-daemon-and-the-protocol/02-CONTEXT.md
@.planning/phases/02-the-daemon-and-the-protocol/02-RESEARCH.md
@.planning/phases/02-the-daemon-and-the-protocol/02-PATTERNS.md
@.planning/phases/02-the-daemon-and-the-protocol/02-UI-SPEC.md
@.planning/phases/02-the-daemon-and-the-protocol/02-VALIDATION.md
@.planning/phases/02-the-daemon-and-the-protocol/02-01-SUMMARY.md
@.planning/phases/02-the-daemon-and-the-protocol/02-02-SUMMARY.md
@.planning/phases/02-the-daemon-and-the-protocol/02-03-SUMMARY.md
@docs/adr/0002-prompt-cache-invariant.md
@docs/adr/0005-cumulative-cost-ledger.md

<interfaces>
**Measured at `37f43b2` this session. Three plans edit these files before this one lands — 02-01 splits
`hive.ts`, 02-02 moves the composition root, 02-03 rewrites the mail paths in `hive.ts` — so every
`hive.ts` line number below is stale by construction. D-01: re-measure, never quote.**

**⚠ LANDMINE 1 — 02-01's `B-markers-hive` / `B-markers-templates` are a MID-PLAN snapshot, and reading
them as the current state is wrong by four.** Measured now: `grep -rc 'LIVE-UNVERIFIED' src/` returns
**8**, **all eight in `src/main/hive.ts`** (`:1014`, `:1024`, `:2422`, `:2449`, `:2458`, `:2483`,
`:3747`, `:3788`), and nowhere else in `src/`. 02-01 records its two numbers after its **task 1** (the
template move), when the split is `hive.ts` 6 / `hiveTemplates.ts` 2. Its **task 3** then moves the
four installer markers — `:2422` is `installPiHooks`' JSDoc, `:2449` and `:2458` are
`installOpenCodePlugin`, `:2483` is `installCrushConfig`'s — into `src/main/hiveProvisioning.ts`, and
02-01 does not re-assert the split afterwards. **The real post-02-01 ledger is three files, not two.**
This plan therefore measures its own baseline across the whole of `src/`, uses 02-01's two numbers only
as a cross-check on the total, and pins a per-file map *plus* a repo-wide total, so a marker that moves
file cannot hide inside a sum.

**⚠ LANDMINE 2 — `BridgeDescriptor` is mutually exclusive, and D-34's "zero new code" reading does not
survive it.** `agentProvider.ts`'s `bridgeOf`:

| Step | Behaviour |
|---|---|
| 1 | `if (preset.bridge) return preset.bridge;` |
| 2 | `if (preset.hookBridge) return { kind: 'hooks', shim: preset.hookBridge };` |
| 3 | `return undefined;` |

and `hive.ts`'s `ensureAgent` dispatch is `if (desc.kind === 'hooks') { … } else if (desc.kind ===
'proxy') { … }`. **One bridge per engine, and `bridge` wins over `hookBridge`.** So setting
`bridge:{kind:'proxy', …}` on grok (hooks), opencode (hooks) or kimi (hooks, after this plan) silently
deletes that engine's mail bridge. Every engine with `costTracking:'none'` that could plausibly be
pointed at a base URL already has a hooks bridge, so after PARITY-01a there is **no zero-code proxy
candidate left**. Re-verify both facts with a command before acting on this paragraph.

**⚠ LANDMINE 3 — `costTracking` has exactly ONE consumer in the whole tree.** Measured:
`grep -rn 'costTracking' src/ | grep -v agentProvider.ts` returns a single line —
`providerAutomation.ts`'s `spend: preset.costTracking`. Nothing in the spend path reads it. It is a
label in the same class as `capabilityLine` (D-30), and PARITY-02's only failable automated assertion
is that the label agrees with `bridgeOf`.

**⚠ LANDMINE 4 — the kimi TOML shape is NOT codex's.** `installCodexHooks` appends nested
`[[hooks.<Event>]]` / `[[hooks.<Event>.hooks]]` tables with `type = "command"`. Kimi's documented
surface (RESEARCH §6.1) is a **flat `[[hooks]]` array-of-tables** whose keys are `event`, `command`,
optional `matcher`, and `timeout` **in seconds**. Copying codex's nesting verbatim produces a file kimi
cannot read, and the failure is silent: hooks never fire, and mail is delivered into a CLI with no
drain — precisely the state PARITY-01a exists to end. Copy `installCodexHooks`' *structure* — read the
operator's file as a string, append an auto-generated banner, single-quoted TOML literal for the
command path, `timeout = 30`, try/catch-and-log, return the per-agent path — and NOT its table shape.

**⚠ LANDMINE 5 — the seeded kimi config carries the operator's OAuth credentials into the hive git
repo.** `kimi login` stores credentials **inside `~/.kimi/config.toml`** (RESEARCH §6.3 item 4), which
is the file `installKimiConfig` copies. Codex sidesteps this — it **symlinks `auth.json`** and copies
only `config.toml`, so the credential never enters the copy. Kimi has no such split. And the hive root
is a real git repo that a debounced committer stages with `git add -A`, so `<agentDir>/kimi-config.toml`
would be committed. **Do not rely on `scrubStagedSecrets` for this**: it commits UNSCANNED above
`SECRET_SCAN_MAX_LINES`, commits UNSCANNED when the staged diff cannot be read, matches on a
`redactSecrets` regex battery rather than on knowing what the file is, and carries a `harnessAuthored`
bypass. The fail-closed answer already exists in this file: `MINE_IGNORE_LINES` in `hive.ts`
(`['settings.json', 'cursor.json', 'inbox/', 'outbox/']`) is written into `<agentDir>/.gitignore` by
`ensureMineIgnore(dir)` at agent birth, **before** any spawn writes the config. One line. Note the known
divergence: `src/main/memory.ts` holds a **second, larger copy** of `MINE_IGNORE_LINES` refreshed by the
mine loop; it is append-only and is not this plan's file — record the divergence in the SUMMARY rather
than editing `memory.ts`.

**⚠ LANDMINE 6 — `test/agent-provider.test.cjs` cannot be graded on TAP counters.** Measured:
`TAP=$(mktemp); node --test --test-reporter=tap test/agent-provider.test.cjs > "$TAP"; grep -E '^# (tests|pass|fail) ' "$TAP"`
returns `# tests 1 / # pass 1 / # fail 0` for the entire file, because it is a self-contained script
with its own `test()` helper and `process.exit(1)`, not a `node:test` file. A `# pass >= N` criterion
against it is satisfied by an empty file. Grade it with a direct run: exit code plus
`node test/agent-provider.test.cjs | grep -c '✓'`.

**⚠ LANDMINE 7 — the marker pin is the one repo-claims clause that must NOT strip comments.**
`test/repo-claims.test.cjs`' header makes comment-stripping mandatory (`stripComments`, and
`readStripped` for every clause). `LIVE-UNVERIFIED` **is** a comment. A clause built on `readStripped`
would count zero in every file forever and pass green against any ledger. The clause reads the raw
file, and its own body says why it is the exception, so the next reader does not "fix" it back into the
house style and silently disarm the pin.

**What this plan does NOT touch, and who owns it.**

| Wanted | Owner | How this plan designs around it |
|---|---|---|
| `src/preload/index.ts` (C-1a step 2: `platform: process.platform` on the `api` object) | 02-04 (wave 4), 02-11, 02-05 | Land step 1 only — the optional parameter on `providerCapabilities`. It is backward-compatible: every existing caller omits it and gets today's behaviour. |
| `CommandCenterPanel.tsx`, `AddAgentModal.tsx`, `AgentCard.tsx`, `store/config.ts` (C-1a step 3, C-2's chips, the god pickers) | 02-06 (wave 6); also inside 01-21's `src/renderer/src/**` wildcard | Hand forward by name in the SUMMARY: the two `canReceiveInbox` god-picker filters, the `NO MCP` / `NO SPEND` chips, and the no-one-argument-`providerCapabilities` clause. |
| `src/main/memory.ts`'s duplicate `MINE_IGNORE_LINES` | nobody in this phase | Add the line to `hive.ts`'s list only — it is the birth-time write, and therefore the one that runs before the credential exists. Record the divergence. |
| `README.md`'s per-engine limitation table (a channel that renders nowhere) | 02-12 (wave 9, the honesty ledger) | Do not edit docs here. The SUMMARY states which README claims this plan makes true and which it leaves for 02-12. |
| `test/capability-surface.test.cjs` (the ≥1-production-consumer clause for `capabilityLine` / `mcp`) | 02-06 | Not written here — at wave 4 it would pass vacuously (D-40). |

**Analogs, per 02-PATTERNS.md.** `src/shared/agentProvider.ts` maps to *"the `grok` / `codex` presets in
the same file"*, match **exact**. `installKimiConfig` maps to `installCodexHooks` (PATTERNS § "The
seed-and-append TOML pattern the kimi bridge copies", which reproduces the body to copy and the block
saying **do NOT use `GROK_HOOK_SHIM` as the analog**). `test/engine-parity.test.cjs` maps to *"its own
`capabilityLine` block"*, match **exact**. `test/repo-claims.test.cjs` maps to its own both-directions
clause pattern. Per-agent isolation under `agentDir` is PATTERNS § 7: `<agentDir>/kimi-config.toml`
joins `settings.json`, `.codex/config.toml`, `.pi-agent/extensions/`, `.opencode/plugin/` and the
per-agent `CRUSH_GLOBAL_CONFIG` — an established pattern, not a new one.

**The `supportsMcp` values, from D-26's corrected scoreboard.** `true`: claude, codex, grok, kimi,
antigravity, qwen, opencode, crush, copilot. `false`: **pi** (no native MCP; needs a third-party
adapter) and **custom** (arbitrary binary). antigravity is `true` with its per-agent surface recorded as
UNVERIFIED in the preset comment — the field answers "can this ENGINE take MCP servers", and the
per-agent delivery question belongs to plan 02-11.

**The `capabilityLine` assertion this plan BREAKS, deliberately.** `test/engine-parity.test.cjs`
asserts the kimi line verbatim under the name *"the capability line SHOUTS about a mail-incapable
engine"*, with a comment reading *"Kimi has no hook bridge, so routed mail bounces to the god"*. Both
the assertion and the comment become false when the bridge lands. **Rewrite them; never delete the
shouting case** — re-point it at `copilot`, which is still genuinely mail-incapable, and keep kimi
covered by a positive assertion in the same block. Deleting the case would remove the only test of the
gap vocabulary.

**Repo-fact clause style.** `test/repo-claims.test.cjs`' header states the rule that governs wave
planning: *"Clauses are added by later waves — one owner per wave."* This plan is **wave 4's sole
repo-claims owner** (02-04 does not declare the file). 02-12 asserts the whole file at the end of the
phase.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run `kimi --help`, rule on the bridge, and measure the marker ledger this plan will pin</name>
  <files>(measurement and the STOP ruling only — no file is edited in this task)</files>
  <read_first>
    - `02-RESEARCH.md` §6 in full — §6.1 the hook surface and its thirteen events, §6.2 why kimi is the codex case and not the grok case, §6.3 `--config-file` and its four consequences including the credential caveat, §6.4 the cost table, and **§6.5, which is this task's whole subject**: `autoModeFlag: '--auto'` is `[UNVERIFIED]` while Moonshot documents `--yolo`/`-y`, `--yes`, `--auto-approve` and `--afk`.
    - `src/shared/agentProvider.ts`, the kimi preset — `autoModeFlag: '--auto'`, `autoFlag: '--auto'`, `canReceiveInbox: false`, and the comment claiming Hello MarkX installs no kimi hook bridge.
    - `02-VALIDATION.md` § "Cannot be verified without an operator-supplied device or account", the PARITY-01a and PARITY-03 rows — the honest-outcome wording this plan's SUMMARY has to match.
    - `02-CONTEXT.md` D-32 (the engine ledger table), D-33, D-34, D-35.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-01-SUMMARY.md`'s `B-markers-hive` / `B-markers-templates` heading — read it, then read LANDMINE 1 above before using either number.
  </read_first>
  <action>
    **This task is a gate, not a narrative. It runs before any bridge code is written, because it is
    cheaper than the bridge (RESEARCH §6.5) and because it can make the bridge the wrong thing to
    build.**

    Run, and paste the full output of each: `command -v kimi`, `kimi --version`, `kimi --help`.

    Then rule, and the ruling is one of exactly three — write which one fired into the task notes:

    1. **`kimi` present, an auto-approval flag confirmed in `--help`.** Record the exact flag string.
       If it is not `--auto`, correct `autoModeFlag` and `autoFlag` on the kimi preset in task 3 to the
       flag `--help` actually printed, and record the correction as a measured fix. Proceed.
    2. **`kimi` present, and `--help` shows NO auto-approval flag of any kind.** **STOP. Report. Build
       no bridge.** Tasks 3 and 4's kimi work do not run; task 2 still runs (it is independent of kimi)
       and task 4's PARITY-02 ruling and marker pin still run against the unchanged ledger. Delivering
       an inbox into a CLI that stops for approval on every tool call is worse than the bounce it
       replaces.
    3. **`kimi` absent from this machine** — the state RESEARCH measured, and the expected one. The flag
       can be neither confirmed nor refuted. **Do not swap `--auto` for `--yolo` on documentation
       alone**: that trades one unverified claim for another and risks breaking a value that may well be
       a working alias. Leave both flag fields byte-identical, and in task 3 mark the kimi preset's flag
       comment `LIVE-UNVERIFIED`, naming the four documented alternates so whoever next has the CLI can
       settle it in one command. Proceed with the bridge — D-33 and VALIDATION both rule that an
       unverified inbox beats a bounce.

    Then record the baselines, each with pasted command output and each labelled, because task 4's
    marker pin is written against them and a pin written against a stale number is decoration:

    - `git rev-parse HEAD` → **B-sha**. Every containment criterion in this plan is range-bound to it.
    - `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` → the **C-marker-map**: the exact
      per-file ledger as it stands after 02-01, 02-02 and 02-03. Expect three files (`hive.ts`,
      `hiveTemplates.ts`, `hiveProvisioning.ts`), not two. Paste it verbatim.
      **Counted per OCCURRENCE (`grep -o`), never per line (`grep -c`) — and that is not a style choice.**
      `grep -c` counts matching LINES, so two markers on one line score 1. Measured this session:
      `printf '// LIVE-UNVERIFIED and LIVE-UNVERIFIED\n' > x.ts` gives `grep -c` → `1` while
      `grep -o … | wc -l` → `2`. Task 4's `MARKER_LEDGER` counts occurrences with a global regex over
      the whole text, so a line-counted map makes the reconciliation compare two different quantities
      and go red for a reason no reviewer can act on. At HEAD (`02dea58`) both forms happen to agree —
      `8 src/main/hive.ts` — which is precisely why the difference has to be pinned before a marker
      doubles up on a line.
    - `grep -ro 'LIVE-UNVERIFIED' src/ | wc -l` → **C-markers**, the repo-wide total. Cross-check it
      against 02-01's recorded `B-markers-hive + B-markers-templates`; state in the task notes whether
      they agree and, if they do not, that LANDMINE 1 is the reason (02-01's numbers predate its own
      task 3) — not a lost marker.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped|todo) ' "$TAP"; rm -f "$TAP"` → **C-suite**.
    - `node test/agent-provider.test.cjs | grep -c '✓'` with the script's own exit code captured → **C-agentprov**, the direct-run case count. LANDMINE 6: this file's TAP counters are meaningless.
    - `ls eslint.config.js` → 01-21's ruleset. Absent means 02-02's STOP-check was not honoured and this
      plan's dependency chain is broken: **STOP and report** rather than landing new `src/` code under a
      ruleset that does not exist yet (D-06).

    No file is edited in this task. Its entire product is a ruling and a set of numbers.
  </action>
  <acceptance_criteria>
    - The full output of `command -v kimi`, `kimi --version` and `kimi --help` is pasted — including the exact shell error when the binary is absent. "Not installed" written as prose, with no command output beneath it, does not satisfy this.
    - **Exactly one of the three rulings is named in the task notes**, with the evidence line that selected it quoted. If ruling 2 fired, the notes record STOP and tasks 3–4's kimi work does not run.
    - **B-sha, C-marker-map, C-markers, C-suite, C-agentprov and the `eslint.config.js` answer are all pasted with the commands that produced them.** A number without its command fails this criterion (D-01).
    - `C-markers` equals the sum of `C-marker-map`'s per-file counts, **both measured as occurrences**. Produce the arithmetic with one command so the two cannot drift: `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c | awk '{s+=$1; print} END {print "SUM="s}'`, pasted beside `grep -ro 'LIVE-UNVERIFIED' src/ | wc -l`. Measured at HEAD this session: the map is `8 src/main/hive.ts`, `SUM=8`, and the total is `8` — they agree. If they disagree, either a marker lives in a file the map dropped or two markers share a line; find which before continuing.
    - The task notes state, in one sentence, whether `C-markers` agrees with 02-01's `B-markers-hive + B-markers-templates`, and name LANDMINE 1 if it does not.
    - `grep -rn 'costTracking' src/ | grep -v 'src/shared/agentProvider.ts'` is run and pasted. It must return **at least 1** line, and the task notes must name every file it returns. LANDMINE 3 claims exactly one (`providerAutomation.ts`); if wave 3 added a second consumer, task 4's parity test has to cover it. A grep returning zero would mean `costTracking` is now read nowhere at all — a different and worse finding. Record it either way.
    - `grep -n 'preset.bridge' src/shared/agentProvider.ts` and `grep -n "desc.kind === 'proxy'" src/main/hive.ts` are run and pasted, confirming LANDMINE 2's precedence and the `else if` dispatch survived waves 1–3. Both must return **at least 1** line; if either returns zero, task 4's mutual-exclusivity ruling has to be re-derived before it is written into source.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs test/provider-automation.test.cjs</automated>
  </verify>
  <done>`kimi --help` has been run and its output pasted, one of three rulings is on the record with the evidence that selected it, and the marker ledger this plan pins has been measured in this session across all of `src/` rather than inherited from 02-01's mid-plan snapshot.</done>
</task>

<task type="auto">
  <name>Task 2: Land the capability ledger — `supportsMcp`, the `mcp` bit, and `providerCapabilities(provider, platform?)`</name>
  <files>src/shared/agentProvider.ts, src/shared/providerAutomation.ts, test/engine-parity.test.cjs, test/provider-automation.test.cjs, test/agent-provider.test.cjs</files>
  <read_first>
    - `02-UI-SPEC.md` § "Rule C-1a — how the renderer obtains the platform, named exactly" in full, especially its three prescribed mechanism steps and its two rejected alternatives; and § "Rule C-1b — where `NO MCP` gets its bit" in full, including its closing sentence that `capabilityLine`'s **string** is not extended.
    - `src/shared/providerAutomation.ts` — the `ProviderCapabilities` interface (`{provider, mail, spend, compact, remote}`, each field's `/** */`), `remoteControlAvailability` with its defaulted `platform`, `providerCapabilities`, and **the whole `capabilityLine` doc comment**, whose closing paragraph currently argues *against* a platform parameter. That paragraph is what this task amends.
    - `src/shared/agentProvider.ts` — the `costTracking` doc comment, ending *"Required, deliberately: a new provider must state its answer instead of inheriting a flattering default."* That sentence is `supportsMcp`'s register, verbatim.
    - `02-CONTEXT.md` D-26 (the per-engine MCP scoreboard: 8 clean, 1 unverified, 2 unsupported) and D-25 (the live-verified no-op — three runs against `claude` 2.1.236).
    - `test/engine-parity.test.cjs`' capability block in full — the four `capabilityLine` assertions, the per-provider `providerCapabilities` loop with its FLOOR-18 comment about typing rather than truthing a bit, the two-branch `remoteControlAvailability` test, and the `split(', ').length` clause counter.
    - `test/provider-automation.test.cjs:1-20` — its `loadTs` import list and `node:test` harness.
    - `test/agent-provider.test.cjs:1-35` — the transpile-four-files harness, its own `test()`/`failures` counter and `process.exit(1)`. LANDMINE 6.
    - `docs/adr/0002-prompt-cache-invariant.md` in full — the invariant the amendment must not weaken.
  </read_first>
  <action>
    **Rule C-1b — the `mcp` bit.** Add a **required** `supportsMcp: boolean` to `AgentProviderPreset`,
    positioned next to `costTracking` and carrying a `/** */` in `costTracking`'s register. That doc
    comment must say three things, and it is load-bearing that it says all three: what the field means
    (this ENGINE accepts MCP servers), what it does **not** mean (that Hello MarkX delivers any to it —
    D-25 live-verified across three runs that `mcpServers` inside a `--settings` file is ignored
    unconditionally, so today's bundle is a complete no-op for every engine, and plan 02-11 owns the
    `--mcp-config` fix), and that it is required rather than optional for the same reason `costTracking`
    is.

    Set it on all eleven presets, each with a one-line comment naming D-26's evidence: `true` for
    claude, codex, grok, kimi, antigravity, qwen, opencode, crush, copilot; `false` for pi (no native
    MCP) and custom (arbitrary binary). antigravity's comment records that its per-agent surface is
    documented but UNVERIFIED — the fact, not the marker, because the marker ledger is a *bridge* ledger
    and plan 02-11 owns MCP verification.

    In `providerAutomation.ts`, add `mcp: boolean` to `ProviderCapabilities` with a `/** */` in the
    existing fields' register, and `mcp: preset.supportsMcp` to `providerCapabilities`. **Do not add an
    MCP clause to `capabilityLine`'s string** — Rule C-1b forbids it, and the string is ADR-0002
    territory.

    **Rule C-1a step 1 — the platform parameter, and only step 1.** Change the signature to
    `providerCapabilities(provider: AgentProvider, platform?: string)` and the `remote` bit to
    `remoteControlAvailability(provider, platform ?? process.platform) === 'ok'`. The `??` is the whole
    mechanism: when a platform is passed, the right-hand operand is never evaluated, so a renderer call
    never touches `process`. Do **not** add the preload field and do **not** edit any renderer file —
    02-04 owns `src/preload/index.ts` in this same wave, and 02-06 owns the call sites.

    **Amend the ADR-0002 JSDoc in the same change.** Its closing paragraph currently reads that
    *"`capabilityLine` itself grew a `remote` BIT and not a platform PARAMETER (D-40): a caller free to
    pass a varying platform is exactly how a stable prefix turns volatile."* That ruling **stands for
    `capabilityLine`**, and the amendment must keep it standing: `capabilityLine` remains one argument
    and forwards no platform, so the roster line it produces is byte-identical on every turn of every
    agent on a given host. What the amendment adds is that `providerCapabilities` — which is not a
    prompt string and never reaches a prompt — now takes an optional platform for the RENDERER's benefit
    (UI-SPEC Rule C-1a), and that the two arities are pinned by test so the parameter cannot creep from
    the record into the prompt. Leaving the paragraph as-is would make the file argue against the change
    sitting immediately above it.

    **Tests.** In `test/engine-parity.test.cjs`:
    - extend the per-provider loop from four capability questions to five, asserting
      `typeof c.mcp === 'boolean'` in the same FLOOR-18 register the `remote` assertion uses (a missing
      bit reads as `undefined`, which is falsy, which would silently shout `NO MCP` at every engine);
    - assert the eleven `mcp` values against an explicit committed map, so a preset that flips goes red
      rather than being absorbed by a `typeof` check;
    - assert `capabilityLine('claude').split(', ').length === 4` **still** holds, under a name that says
      what it pins: `capabilityLine` did not grow an MCP clause;
    - assert both arities — `providerCapabilities.length === 2` and `capabilityLine.length === 1`.

    In `test/provider-automation.test.cjs`, add the `??` short-circuit proof, which is the only
    mechanical evidence that a renderer call cannot read `process`: temporarily redefine
    `process.platform` with `Object.defineProperty` to a getter that **throws**, restore it in
    `t.after`, then assert that `providerCapabilities('codex', 'linux')` returns without throwing
    **and** that `providerCapabilities('codex')` throws. Both halves are required — the first alone
    passes if the parameter is accepted and ignored; the second alone passes if the function never reads
    the platform at all. Also assert forwarding end to end:
    `providerCapabilities('codex', 'win32').remote === false` and
    `providerCapabilities('codex', 'darwin').remote === true`.

    In `test/agent-provider.test.cjs`, add a preset-table completeness case — this is the file that
    transpiles `agentProvider.ts` standalone, so it is where a missing required field belongs: every one
    of the eleven presets has `typeof p.supportsMcp === 'boolean'`, pi and custom are `false`, and the
    other nine are `true`.
  </action>
  <acceptance_criteria>
    - `grep -c 'supportsMcp' src/shared/agentProvider.ts` returns **exactly 12** — the interface field plus one per preset — and `grep -c "^    id: '" src/shared/agentProvider.ts` returns **exactly 11**. Paste both. If the preset count is not 11, an engine was added or lost and every count in this plan is wrong.
    - Joined-text assertions, per D-40(a), so a formatter that wraps a signature cannot defeat either half. **The joiner must SQUEEZE runs of spaces — `tr '\n' ' ' | tr -s ' '` — because prettier indents continuation lines and a bare `tr '\n' ' '` leaves those runs in place.** Measured this session against a wrapped sample (`export function providerCapabilities(` / `  provider: AgentProvider,` / `  platform?: string` / `)…`, with `mcp:` and `platform ??` also broken across lines): the unsqueezed join scores **0, 0, 0** on the three patterns below and the squeezed join scores **1, 1, 1**. Bind it once — `J=$(tr -d '\r' < src/shared/providerAutomation.ts | tr '\n' ' ' | tr -s ' ')` — then assert
      `printf '%s' "$J" | grep -c 'platform ?? process\.platform'` ≥ **1**;
      `printf '%s' "$J" | grep -c 'mcp: preset\.supportsMcp'` ≥ **1**;
      `printf '%s' "$J" | grep -c 'export function providerCapabilities( *provider: AgentProvider, *platform?: string'` ≥ **1**.
      The ` *` runs make the third pattern match the wrapped and the unwrapped form alike — verified against both samples this session, each scoring `1`. All three are RED at HEAD, measured: `0`, `0`, `0`. Paste the joiner and all three counts.
    - **Both directions on the arity ruling**: `node -e "const m=require('./test/load-ts.cjs')('src/shared/providerAutomation.ts'); console.log(m.providerCapabilities.length, m.capabilityLine.length)"` prints exactly `2 1`. A `capabilityLine` that grew a parameter fails here, and so does a `providerCapabilities` that did not.
    - **The MCP clause did not enter the prompt string**: a `node -e` probe printing `capabilityLine('pi')` shows no `MCP` substring (case-insensitive), and the same probe for `claude` prints a line with **exactly 4** comma-separated clauses. Paste both lines. UI-SPEC Rule C-1b is explicit that the string is not extended.
    - **`supportsMcp` has no behavioural consumer**: `grep -rn 'supportsMcp' src/ | grep -v 'src/shared/agentProvider.ts' | grep -v 'src/shared/providerAutomation.ts'` returns **exactly nothing**, **and** `grep -c 'mcp: preset.supportsMcp' src/shared/providerAutomation.ts` returns **at least 1**. Both directions: the negative alone is satisfied by never adding the field. This is what pins D-25's honesty — the field declares, it does not enable.
    - **The ADR-0002 JSDoc was amended, not left contradicting the code**, proven by a grep scoped to the doc block itself rather than to the file. A file-wide `grep -c 'providerCapabilities' src/shared/providerAutomation.ts` is **already ≥ 1 at HEAD** (the declaration at `:293` and the call at `:333`), so it passes green with the amendment absent — measured, and exactly the F4 shape D-40 bans. Extract the block instead: `D=$(awk '/^\/\*\*/{b=""} {b=b" "$0} /^export function capabilityLine/{print b; exit}' src/shared/providerAutomation.ts | tr -s ' ')`, then assert **both directions**: `printf '%s' "$D" | grep -c 'providerCapabilities'` ≥ **1** (RED at HEAD, measured `0` — the amendment is what puts the name there), **and** `printf '%s' "$D" | grep -c 'platform PARAMETER'` ≥ **1** (measured `1` at HEAD — the survival pin, so the amendment adds to the D-40 ruling instead of deleting it). Paste the extracted block in full into the task notes as well; a reviewer must be able to read the amendment without opening the file.
    - `node --test --test-reporter=tap test/engine-parity.test.cjs test/provider-automation.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, and `# pass` at least those two files' pre-task total **+ 6**. Paste before and after.
    - `node test/agent-provider.test.cjs` exits `0`, and `node test/agent-provider.test.cjs | grep -c '✓'` returns at least **C-agentprov + 1**. Paste both. LANDMINE 6: do not grade this file on `# pass`.
    - `npm run typecheck` exit code `0`. Because `supportsMcp` is **required**, a preset missing it is a compile error — state in the task notes that the typecheck run, not the grep, is what proves all eleven were set.
    - `npx eslint src/shared/agentProvider.ts src/shared/providerAutomation.ts --max-warnings 0` exits `0`.
    - `npm test` exit code `0`, `# fail 0`, `# skipped` no higher than **C-suite**'s.
    - Containment, **per-commit and range-bound**: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD -- src/shared/agentProvider.ts src/shared/providerAutomation.ts test/engine-parity.test.cjs test/provider-automation.test.cjs test/agent-provider.test.cjs) && test -n "$SHAS" || { echo "UNBOUND BASE OR NO COMMITS — CRITERION FAILS"; false; }; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/shared/agentProvider\.ts|src/shared/providerAutomation\.ts|test/engine-parity\.test\.cjs|test/provider-automation\.test\.cjs|test/agent-provider\.test\.cjs)$"` must print **nothing**. Bind `$BASE` inside the criterion — an unbound variable makes `git log` fatal with empty stdout, which "must print nothing" reads as PASS.
    - `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; git diff --stat "$BASE"..HEAD -- package.json package-lock.json` prints **nothing** (D-06). The bind guard is load-bearing, not decoration: measured this session with `$BASE` unset, `git diff --stat "$BASE"..HEAD -- package.json package-lock.json` resolves `..HEAD` as `HEAD..HEAD`, prints nothing and exits `0` — so the unguarded form passes green even if the lockfile had been rewritten.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node --test test/engine-parity.test.cjs test/provider-automation.test.cjs && node test/agent-provider.test.cjs</automated>
  </verify>
  <done>`ProviderCapabilities` carries an `mcp` bit sourced from one required preset field, `providerCapabilities` forwards an optional platform through a `??` proven not to read `process` when it is passed, `capabilityLine` is byte-unchanged and still one argument, and the ADR-0002 paragraph no longer argues against the code beneath it.</done>
</task>

<task type="auto">
  <name>Task 3: Build kimi's bridge as the codex case, ship it marked, and stop the bare spawn from being silent</name>
  <files>src/main/hiveProvisioning.ts, src/main/hive.ts, src/main/hiveTemplates.ts, src/shared/agentProvider.ts, test/engine-parity.test.cjs</files>
  <read_first>
    - **Task 1's ruling.** If ruling 2 fired, this task does not run.
    - `src/main/hiveProvisioning.ts` — `installCodexHooks` in full as 02-01 left it, including its ~25-line comment on why `timeout` is seconds with the measured cold-start numbers, the `auth.json` symlink-with-copy-fallback, the string seed of the operator's `config.toml`, the auto-generated banner, the single-quoted TOML literal, and the try/catch-and-log. This is the analog, structurally.
    - `02-PATTERNS.md` § "The seed-and-append TOML pattern the kimi bridge copies" — the body reproduced verbatim, the sentence naming exactly what to copy, and the block saying **do NOT use `GROK_HOOK_SHIM` as the analog**.
    - `02-RESEARCH.md` §6.1 — the thirteen events, the flat `[[hooks]]` array-of-tables with `event`/`command`/`matcher`/`timeout`, the stdin JSON payload keys, and the exit-code contract (`0` permit, `2` block). §6.2's closing paragraph — the one adapter delta, where `HOOK_SHIM` expresses a deny through stdout JSON at exit 0 while kimi documents exit 2. §6.3 items 1–4.
    - `src/main/hiveTemplates.ts` — `HOOK_SHIM` in full: it parses stdin JSON, stamps `agent_id` from `AGENT_ID` and `sock_token` from `HIVE_SOCK_TOKEN`, writes to `HIVE_SOCK`, echoes the response to stdout and always exits 0. Note which of the eight constants carry `LIVE-UNVERIFIED` today.
    - `src/main/hive.ts` — `ensureAgent`'s `bridgeOf(meta.provider)` dispatch, the `preArgs` array, codex's `preArgs.push('--dangerously-bypass-hook-trust')` as the shape to copy, the pi and opencode arms with their `LIVE-UNVERIFIED` comments as the marking register, and the **fall-through at the end of the injection block** — `if (seedDelivery === 'type-into-tui') … if (flag) … if (positionalInitialPrompt) … return { args: preArgs, env }`. That last line is where the hive protocol is silently dropped.
    - `src/main/hive.ts` — `const MINE_IGNORE_LINES`, `ensureMineIgnore(agentDir)` and its call site inside `ensureAgent`, plus the surrounding comment explaining that `mempalace mine` honours `.gitignore`. LANDMINE 5.
    - `src/main/hive.ts` — `scrubStagedSecrets` and `harnessAuthored`, so the reason this plan does not rely on either is understood rather than asserted.
    - `src/shared/agentProvider.ts` — the kimi preset, the `hookBridge?: 'agy' | 'codex' | 'grok'` union with its per-value doc, and the **grok preset's comment**, which PATTERNS names as the register for kimi's replacement comment.
    - `02-RESEARCH.md` §4.7's warning paragraph — the god-picker side effect, verbatim, and its instruction to decide deliberately.
    - `test/engine-parity.test.cjs` — the `capabilityLine('kimi')` case and its comment, both of which this task falsifies.
  </read_first>
  <action>
    **In `src/main/hiveProvisioning.ts`, add `installKimiConfig`** as a named free-function export in the
    explicit-arguments style 02-01 established: `installKimiConfig({ dir, shim, nodeRun, userHome }):
    string`. It **returns the per-agent CONFIG FILE path**, `<dir>/kimi-config.toml`, not a directory —
    `--config-file` takes a file where `CODEX_HOME` takes a directory, and getting that wrong produces
    an argv kimi rejects.

    Its body: read `join(userHome, '.kimi', 'config.toml')` as a **string** when it exists, else the
    empty string; append the auto-generated banner in `installCodexHooks`' exact wording; append one
    **flat `[[hooks]]` table per event** — `event = "<Event>"`, `command = '<nodeRun(shim)>'` as a
    single-quoted TOML literal, `timeout = 30` — for the eight events codex already wires
    (`PreToolUse`, `PostToolUse`, `Stop`, `SubagentStop`, `SessionStart`, `UserPromptSubmit`,
    `PreCompact`, `PostCompact`), every one of which appears in kimi's documented thirteen.
    **LANDMINE 4: this is NOT codex's nested `[[hooks.<Event>]]` / `[[hooks.<Event>.hooks]]` shape.**
    Write the file with an explicit mode of `0o600` — the seed carries the operator's `kimi login`
    credentials — and wrap the whole body in the house
    `try { … } catch (e) { console.error('[hive] installKimiConfig failed:', e); }`.

    Its JSDoc carries, in `installCodexHooks`' register: why the seed is a string append and not a parse
    (no TOML parser exists in Node core or in this repo's dependencies, and D-06 forbids adding one);
    why the seed is mandatory rather than an optimisation (`kimi login` stores OAuth credentials
    **inside** this file, so an unseeded config would silently deauthenticate the operator's agent — the
    one place kimi differs from codex, which keeps its credential in a separately symlinked
    `auth.json`); that `timeout` is seconds, matching kimi's documented default of 30; and a
    **`LIVE-UNVERIFIED`** marker stating that no kimi session has ever run against this file.

    **Do not build a `KIMI_HOOK_SHIM`.** RESEARCH §6.2's adapter delta — kimi documents a block as exit
    code 2, while `HOOK_SHIM` expresses a deny through stdout JSON at exit 0 — is unresolvable without
    the CLI, and a wrapper written to a guess is a second unverified surface with no way to test it.
    `HOOK_SHIM` is reused **verbatim**, as codex does. Instead, extend `HOOK_SHIM`'s header comment in
    `src/main/hiveTemplates.ts` to name kimi as a consumer and to record the exit-code question as open,
    carrying a **`LIVE-UNVERIFIED`** marker. That comment is the only edit this task makes to
    `hiveTemplates.ts`; **no template body changes by one byte**.

    **In `src/main/hive.ts`**, three edits:

    1. In `ensureAgent`'s `desc.kind === 'hooks'` arm, add
       `else if (desc.shim === 'kimi') preArgs.push('--config-file', installKimiConfig({ … }))`, with a
       comment in the pi/opencode arms' register explaining the per-agent isolation (`--config-file` is
       a per-invocation flag, so the operator's global `~/.kimi/config.toml` is never mutated — the
       property D-26 originally disqualified kimi for) and carrying a **`LIVE-UNVERIFIED`** marker.
    2. Add `'kimi-config.toml'` to `MINE_IGNORE_LINES`, with a comment saying why it differs from its
       four neighbours: the others are churn, this one is a **credential**. `ensureMineIgnore(dir)` runs
       at agent birth, before any spawn writes the config, so the line lands first. Record in the
       comment that this is the fail-closed guard and that `scrubStagedSecrets` is explicitly not relied
       on — it commits UNSCANNED past its size cap, matches on a regex battery rather than on knowing
       what the file is, and has a `harnessAuthored` bypass.
    3. At `ensureAgent`'s final `return { args: preArgs, env }` fall-through — the branch reached when a
       provider has neither `seedDelivery`, nor an `initialPromptFlag`, nor `positionalInitialPrompt` —
       append a log entry (`this.appendLog({ kind: 'protocol-not-seeded', agentId: meta.id, provider:
       meta.provider })`) with a comment naming what it records: the hive protocol was built and then
       dropped, so this agent starts with no identity, no protocol and no orientation. kimi is the
       engine this now fires for, and after this plan kimi can also be chosen as the **god**. A silent
       drop was survivable while kimi could only be a worker whose mail bounced; it is not survivable
       for an orchestrator.

    **Rejected, with the reason recorded in the SUMMARY:** exporting a `canSeedInitialPrompt(provider)`
    predicate from `agentProvider.ts`. The condition already exists as that branch, its only consumer
    would be the one call site, and UI-SPEC Rule C-2's gap vocabulary has no row for it — so it would
    ship as a second `capabilityLine`: a tested pure function that nothing renders (D-30). Add it when a
    UI surface actually needs it.

    **In `src/shared/agentProvider.ts`**, widen `hookBridge?: 'agy' | 'codex' | 'grok'` by one string to
    include `'kimi'`, extend that field's per-value doc with a `'kimi'` entry matching the existing
    entries' shape, and edit the kimi preset: `canReceiveInbox: true`, `hookBridge: 'kimi'`, and the
    current comment — which claims Hello MarkX installs no kimi hook bridge, and which this task makes
    false — **replaced**, in the grok preset's register, with a comment stating all four facts: the
    bridge is installed via a per-agent `--config-file`; `HOOK_SHIM` is reused verbatim because kimi's
    payload is Claude-shaped snake_case; the bridge is **`LIVE-UNVERIFIED`** because no Moonshot account
    exists; and kimi is now **god-eligible** in the two pickers that filter on `canReceiveInbox` while
    having no interactive initial-prompt form, so a kimi god spawns unoriented — named for plan 02-06,
    which owns those surfaces. Apply task 1's ruling to `autoModeFlag` / `autoFlag`: correct them under
    ruling 1, or leave them byte-identical under ruling 3 with a **`LIVE-UNVERIFIED`** comment naming
    `--yolo` / `-y` / `--yes` / `--auto-approve` / `--afk` as the documented alternates and
    `kimi --help` as the one command that settles it.

    **In `test/engine-parity.test.cjs`**, add the PARITY-01a cases and fix the two the bridge falsifies:
    - `bridgeOf('kimi')` deep-equals `{ kind: 'hooks', shim: 'kimi' }`, and `canReceiveInbox('kimi')` is
      `true`;
    - `installKimiConfig` against a **tmp `userHome`** seeded with a sentinel credential line: the output
      file contains the sentinel byte-for-byte (the auth survives — VALIDATION's row), contains a flat
      `[[hooks]]` table with `event = "Stop"`, and **does not** contain the string `[[hooks.Stop]]`
      (LANDMINE 4, both directions);
    - the tmp `userHome` file is **unmodified** after the call — compare its content and mtime before and
      after, so a future refactor that "helpfully" writes hooks into the operator's own config fails
      here;
    - on POSIX, the written file's mode is `0o600`; on win32 the case **announces its skip**
      (`console.error('[engine-parity] kimi config mode case skipped — <reason>')`, the
      `test/net-binding.test.cjs` pattern), never returning silently;
    - the spawn argv carries `--config-file` followed by a path ending `kimi-config.toml`;
    - **rewrite** the `capabilityLine('kimi')` assertion — kimi now reads `mail ok` — and rewrite its
      comment, which currently says kimi has no hook bridge. **Re-point the "SHOUTS about a
      mail-incapable engine" case at `copilot`**, which is still genuinely mail-incapable, so the gap
      vocabulary keeps its test. Do not delete either case.
  </action>
  <acceptance_criteria>
    - `grep -c 'export function installKimiConfig' src/main/hiveProvisioning.ts` returns **exactly 1**, and 02-01's electron-free contract is checked **over joined, space-squeezed text**: `tr -d '\r' < src/main/hiveProvisioning.ts | tr '\n' ' ' | tr -s ' ' | grep -cE "(import[^;]*from '(electron|electron/[^']*)')|(require\('electron'\))"` returns **exactly 0**. The line-oriented form is a defect rather than a style preference — measured this session, a prettier-wrapped `import {` / `  app,` / `  BrowserWindow` / `} from 'electron';` scores **0** under `grep -c "import .*from 'electron'"` and therefore **passes** the guard while importing electron, and scores **1** under the joined form above, which fails it as it must. A clean file (`import { readFileSync } from 'node:fs';`) scores `0` under the joined form, so it is not merely always-on.
    - **The TOML shape, both directions**, over a body extracted by command rather than by eye: `B=$(awk '/^export function installKimiConfig/{f=1} f{print} f&&/^}$/{exit}' src/main/hiveProvisioning.ts)`, then `printf '%s\n' "$B" | grep -cF '[[hooks]]'` returns **at least 1** and `printf '%s\n' "$B" | grep -cF '[[hooks.'` returns **exactly 0**. Paste `grep -n 'export function installKimiConfig' src/main/hiveProvisioning.ts`, the body's line count, and both counts. The negative alone is satisfied by writing no hooks at all. The extractor is proven against the analog at HEAD this session — run over `installCodexHooks` (`awk '/private installCodexHooks\(/{f=1} f{print} f&&/^  }$/{exit}' src/main/hive.ts`, 66 lines) it returns `[[hooks.` = **2** and `[[hooks]]` = **0**, which is LANDMINE 4's exact inversion of kimi's required counts and is what makes this pair falsifiable rather than decorative.
    - **No TOML parser was added** (D-06), by command rather than by prose: `grep -rniE "(require\(['\"][^'\"]*toml[^'\"]*['\"]\))|(from ['\"][^'\"]*toml[^'\"]*['\"])" src/` returns **exactly nothing**. It matches only a `require`/`from` **specifier**, so the literal path argument in `join(dir, 'kimi-config.toml')` is not a false positive and no exclusion clause is needed. Measured at HEAD: no output, exit `1`. Paired with the range-bound lockfile check: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; git diff --stat "$BASE"..HEAD -- package.json package-lock.json` prints **nothing**.
    - **Template bodies are byte-identical**, range-bound and with a positive companion — measured this session, with `$BASE` unset `git diff "$BASE"..HEAD -- src/main/hiveTemplates.ts` prints nothing and exits `0`, so "only comment lines changed" reads as PASS against an empty diff. Run: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; D=$(git diff "$BASE"..HEAD -- src/main/hiveTemplates.ts); test -n "$D" || { echo "EMPTY DIFF — the kimi consumer comment was never added, CRITERION FAILS"; false; }; printf '%s\n' "$D" | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -vE "^[+-] *(//|\*|/\*)"` must print **nothing** — every added or removed line is a comment line. Paste the full diff as well; a single changed character inside a backtick literal fails this criterion.
    - **The credential never enters git, and the guard is the ignore line, not the scrub.** `grep -c "kimi-config.toml" src/main/hive.ts` returns **at least 1** (RED at HEAD, measured `0`), and a test asserts that after `ensureAgent`, `<agentDir>/.gitignore` contains the line — run it and paste. Then the live proof: build a hive on a tmpdir, call `installKimiConfig` with a tmp `userHome` whose `config.toml` holds a sentinel token, drive the hive's own commit path, and assert **four** things — because the two negatives alone are satisfied by a hive that never committed and an installer that never wrote: (i) the sentinel **is** present in `<agentDir>/kimi-config.toml` on disk (`grep -c "<sentinel>" <agentDir>/kimi-config.toml` ≥ **1** — the credential really did get copied, which is what makes the containment mean anything); (ii) `git -C <root> log --oneline | wc -l` ≥ **1** (the commit path really ran); (iii) `git -C <root> ls-files | grep -c 'kimi-config\.toml$'` returns **exactly 0**; (iv) `git -C <root> log -p | grep -c "<sentinel>"` returns **exactly 0**. Paste all four. This is the one criterion that proves the mitigation rather than the intention, and (i)+(ii) are what stop it proving nothing at all.
    - **The operator's own config is never written.** The tmp-`userHome` before/after content-and-mtime comparison passes, and — reusing `$B`, the extracted body from the TOML-shape criterion above — `printf '%s\n' "$B" | grep -c 'writeFileSync'` returns **exactly 1** and `printf '%s\n' "$B" | grep -n 'writeFileSync'` shows its destination built from `dir`. Paste both. The extractor is proven: run over `installCodexHooks` at HEAD this session it returns `writeFileSync` = **1**, the same shape this asserts for kimi.
    - **File mode.** On POSIX, the written file's mode masked to `0o777` is `0o600`; on win32 the skip is announced and the announcement line is pasted. A silent skip fails.
    - **The union widened, proven by the compiler AND by a threshold** — a bare count with no comparison is not a verdict. `grep -o "'kimi'" src/shared/agentProvider.ts | wc -l` returns **at least 8**, and the task notes name what each occurrence is. Measured at HEAD this session: **6** — the `AgentProvider` union member (`:28`), `id: 'kimi'` (`:282`), `defaultCommand: 'kimi'` (`:285`), `isAgentProvider`'s `value === 'kimi'` (`:544`), and `inferAgentProvider`'s `if (bin === 'kimi') return 'kimi';` (`:594`, which holds two). The bound is therefore RED today, and this task adds exactly two: the new `hookBridge` union member and the preset's `hookBridge: 'kimi'`. `npm run typecheck` exit `0` is what proves the union widened *correctly* — a `hookBridge: 'kimi'` against an un-widened union is a compile error.
    - **The stale kimi comment is gone, in both directions**: `grep -c 'does not yet install a Kimi hook bridge' src/shared/agentProvider.ts` returns **exactly 0**, **and** `grep -c "hookBridge: 'kimi'" src/shared/agentProvider.ts` returns **at least 1**. The negative alone is satisfied by deleting the preset.
    - **The bare spawn is no longer silent**: `grep -c "protocol-not-seeded" src/main/hive.ts` returns **at least 1**, and a test drives `ensureAgent` for a preset with no seed path and asserts the entry reaches the hive log. Paste the log line.
    - **The falsified test was rewritten, not deleted.** The survival pins and the negative first: `grep -c "capabilityLine('kimi')" test/engine-parity.test.cjs` ≥ **1**, `grep -c 'NO MAIL' test/engine-parity.test.cjs` ≥ **1**, and `grep -c 'Kimi has no hook bridge' test/engine-parity.test.cjs` **exactly 0** (RED at HEAD, measured `1`). **Those three do not prove the rewrite on their own** — the first two are already green at HEAD, measured `1` and `3`. Add the two that are RED at HEAD:
      • `grep -c "kimi: mail ok" test/engine-parity.test.cjs` ≥ **1** — measured `0` at HEAD, because today the file asserts kimi's line as `NO MAIL (bounces to you)`. This is the assertion proving the bridge's effect was written into the test rather than the test loosened around it.
      • The SHOUTS case was **re-pointed**, not merely edited: `S=$(awk '/SHOUTS about a mail-incapable engine/,/^}\);/' test/engine-parity.test.cjs | tr -d '\r' | tr '\n' ' ' | tr -s ' ')`, then `printf '%s' "$S" | grep -c "capabilityLine('copilot')"` ≥ **1** and `printf '%s' "$S" | grep -c "capabilityLine('kimi')"` **exactly 0**. Measured at HEAD: `0` and `1` — both RED, both flip when the case moves to copilot. A file-wide `grep -c "capabilityLine('copilot')"` would NOT do: it already returns **2** at HEAD.
    - `node --test --test-reporter=tap test/engine-parity.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the pre-task count **+ 6**. Paste before and after.
    - `node --test test/hive-hook-node.test.cjs test/hive-cwd.test.cjs test/hive-durability.test.cjs test/hive-protocol-v2.test.cjs test/hive-router.test.cjs test/boot-floor.test.cjs` → `EXIT=0`, `# fail 0`, and each file's `# pass` **not lower** than its pre-task count. Paste before and after for all six. The phase gate stays green.
    - `npm run typecheck` exit `0`; `npx eslint src/main/hive.ts src/main/hiveProvisioning.ts src/main/hiveTemplates.ts src/shared/agentProvider.ts --max-warnings 0` exit `0`; `npm run build` exit `0`.
    - `npm test` exit code `0`, `# fail 0`, `# pass` at least **C-suite**'s + 6, `# skipped` no higher than C-suite's.
    - Containment, **per-commit and range-bound**, over `src/main/hiveProvisioning.ts src/main/hive.ts src/main/hiveTemplates.ts src/shared/agentProvider.ts test/engine-parity.test.cjs`, in the same `$BASE`-bound form as task 2, printing **nothing**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs test/hive-hook-node.test.cjs test/hive-durability.test.cjs test/boot-floor.test.cjs</automated>
  </verify>
  <done>Kimi has a per-agent hook bridge built as the codex case with `HOOK_SHIM` reused verbatim and kimi's own flat `[[hooks]]` shape, the seeded credential is kept out of git by an ignore line written at birth and proven by a real commit, the god-eligibility consequence is ruled on and the previously silent unoriented spawn now records itself, and every site the bridge touches ships `LIVE-UNVERIFIED`.</done>
</task>

<task type="auto">
  <name>Task 4: Rule on PARITY-02 in source, pin the cost ledger to the wiring, and pin PARITY-03's markers in both directions</name>
  <files>src/shared/agentProvider.ts, test/engine-parity.test.cjs, test/repo-claims.test.cjs</files>
  <read_first>
    - `02-CONTEXT.md` D-34 in full — the unachievable-as-written ruling, the buildable reading, the named candidates (grok, kimi, opencode, and antigravity-with-a-`gemini`-mode), and its closing instruction: *"The plan must state which it actually delivers and mark the rest, never silently."*
    - `02-CONTEXT.md` D-35 in full — the eight sites, the one-directional rule, the absence of any `live-unverified` field in the provider table, and *"a plan that schedules 'verify the four bridges' without an operator-supplied account is scheduling a lie."*
    - `02-CONTEXT.md` D-40 in full — the three vacuous gates this project already produced, and the two binding requirements (joined/parsed text; a positive lower bound).
    - `02-RESEARCH.md` §4.8 — the `else if` extension point, the `hookBridge` union note, and its two ⚠ paragraphs: `inboxDelivery:'terminal'` inheriting the headless-mail bug (fixed by 02-03, which this plan depends on), and ADR-0005's cumulative-sample hazard.
    - `src/shared/agentProvider.ts` — `BridgeDescriptor`'s doc comment, `bridgeOf` in full, and the `costTracking` doc comment listing the four modes.
    - `src/main/hive.ts` — the `if (desc.kind === 'hooks') … else if (desc.kind === 'proxy')` dispatch and `startProxyBridge`, including its GATE-01 paragraph on `mintProxyToken`.
    - `test/repo-claims.test.cjs:1-70` — the file header (the one-owner-per-wave rule and the mandatory comment-stripping), `stripComments`, `sourceFiles`, `readStripped`, and the `ONE_SHOT_READERS` clause as the model for an exact-count ledger with a per-entry failure message.
    - `test/repo-claims.test.cjs`' both-directions clause — a negative assertion satisfiable by deletion, paired with a positive one that fails if the feature is deleted. Every clause here copies it.
    - Task 1's **C-marker-map** and **C-markers**, and task 3's diff.
  </read_first>
  <action>
    **PARITY-02's ruling, written into source where the next planner will hit it.** Extend
    `BridgeDescriptor`'s doc comment in `src/shared/agentProvider.ts` with the measured constraint:
    `bridgeOf` returns **one** descriptor — `preset.bridge` wins over `hookBridge` — and `hive.ts`
    dispatches `hooks` XOR `proxy`, so an engine cannot hold both. Therefore adding a proxy bridge to a
    hooks engine to gain cost tracking **removes that engine's mail bridge**, trading PARITY-01a for
    PARITY-02. Name the three engines this rules out (grok, kimi, opencode — all hooks-bridged), name
    antigravity's separate blocker (it would additionally need a `gemini` api mode in the sidecar, which
    is **not delivered by this phase**), and name the two that genuinely have no number to report
    (copilot: spend sits on the user's Copilot plan; custom: unknown binary). Extend `costTracking`'s doc
    comment with LANDMINE 3's measured fact: its only consumer anywhere in `src/` is
    `providerCapabilities`, so it is a **declaration** that must be kept honest by a test, not a wire.

    **This plan converts zero engines to a proxy bridge, and the SUMMARY says so with the reason.** Two
    engines already report through the proxy (qwen, crush) and two through their own surfaces (claude
    via OTel, codex via rollout transcripts). That is four of eleven, unchanged by this plan, and the
    SUMMARY must state the number rather than the impression.

    **The parity test — PARITY-02's automated half**, in `test/engine-parity.test.cjs`, over all eleven
    presets. Four assertions, each able to fail:
    1. `costTracking === 'proxy'` **iff** `bridgeOf(p)?.kind === 'proxy'` — the declaration matching the
       wiring, in both directions, for every engine. This is the assertion that goes red the day someone
       flips a preset without changing the spawn path, or vice versa.
    2. **No preset sets both `bridge` and `hookBridge`.** This is the mutual-exclusivity pin: a future
       "just add proxy cost to grok" edit fails here, loudly, instead of silently deleting grok's mail.
    3. The set of engines with `costTracking === 'none'` equals an explicit committed list, so an engine
       gaining or losing a cost path forces a diff-visible edit.
    4. **Positive lower bound (D-40):** each of `'otel'`, `'proxy'`, `'transcript'` and `'none'` is held
       by **at least one** engine, and the preset count is **exactly 11**. Without this, an empty preset
       array satisfies assertions 1–3.

    **PARITY-03's pin**, in `test/repo-claims.test.cjs`, as this wave's single clause. Structure it on
    `ONE_SHOT_READERS`: a committed `const MARKER_LEDGER = { '<path>': <count>, … }` mapping every file
    in `src/` that carries `LIVE-UNVERIFIED` to its exact count, populated from a **re-measurement run in
    the executing session** — task 1's `C-marker-map` plus exactly the sites task 3 added — and never
    copied from this plan or from 02-01's SUMMARY.

    The clause asserts four things:
    - **each listed file's raw count equals its ledger entry exactly** — upper and lower bound, with a
      per-file failure message naming the file, the expected count and the found count, so the diff tells
      a reviewer which bridge changed;
    - **the repo-wide total across all of `src/` equals the sum of the ledger** — so a marker moved into
      an unlisted file, or a new marker in a new file, fails even though every listed file still
      balances;
    - **the total is `>= 1`**, and **every listed file exists and is non-empty** — so deleting the
      bridges, or deleting `src/`, fails instead of satisfying;
    - the failure message on the total says, in one sentence, what a reviewer must ask: *unmarking a
      bridge means a real session against a real account — which account?*

    **This clause is the file's one exception to comment-stripping, and it must say so in its own body.**
    `LIVE-UNVERIFIED` lives inside comments; `readStripped` deletes them; a stripped grep would count
    zero in every file forever and the gate could never fail. Read the raw file, count with a global
    regex over the whole text rather than per line, and write the two-sentence reason inline so the next
    reader does not restore the house style and silently disarm the pin.

    **Drive it RED three ways before trusting it**, pasting all three failures plus the green run after
    reverting each: (a) delete one `LIVE-UNVERIFIED` marker → the per-file count fails low; (b) add one
    marker in an already-listed file → the per-file count fails high; (c) move one marker into a file the
    ledger does not list → every per-file entry still balances and the **repo-wide total** fails. (c) is
    the one that proves the clause is not just a sum. A clause that has not been seen failing is
    decoration, and this project has already shipped three that could not fail.
  </action>
  <acceptance_criteria>
    - **The PARITY-02 ruling is in source, not only in the plan**, and the grep that proves it has to be RED at HEAD. "`BridgeDescriptor`'s doc block names `hookBridge`" is **already true at HEAD** — measured `1` — so it passes with the amendment absent. Extract both blocks and assert what only the amendment can supply:
      `BD=$(awk '/^\/\*\*/{b=""} {b=b" "$0} /^export type BridgeDescriptor/{print b; exit}' src/shared/agentProvider.ts | tr -s ' ')` — then `printf '%s' "$BD" | grep -c 'grok'`, `printf '%s' "$BD" | grep -c 'kimi'` and `printf '%s' "$BD" | grep -c 'opencode'` each return **at least 1**. Measured at HEAD: `0`, `0`, `0`. Those are the three engines the one-bridge-per-engine constraint rules out, so naming them **is** the ruling.
      `CT=$(awk '/\/\*\*/{b=""} {b=b" "$0} /^  costTracking: CostTracking;/{print b; exit}' src/shared/agentProvider.ts | tr -s ' ')` — then `printf '%s' "$CT" | grep -c 'providerCapabilities'` returns **at least 1**. Measured at HEAD: `0`. That is LANDMINE 3's measured fact landing in the field's own doc.
      Paste both extracted blocks in full into the task notes as well.
    - **Zero engines were converted**, range-bound so the negative cannot pass on an empty diff — measured this session, with `$BASE` unset `git diff "$BASE"..HEAD -- src/shared/agentProvider.ts | grep -c "^+.*kind: 'proxy'"` prints `0`, i.e. PASS, against a tree nobody looked at. Run: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; D=$(git diff "$BASE"..HEAD -- src/shared/agentProvider.ts); test -n "$D" || { echo "EMPTY DIFF — this task edits agentProvider.ts, CRITERION FAILS"; false; }; printf '%s\n' "$D" | grep -c "^+.*kind: 'proxy'"` returns **exactly 0**. **And** the positive companion, as an exact command: `node -e "const a=require('./test/load-ts.cjs')('src/shared/agentProvider.ts'); console.log(a.AGENT_PROVIDER_PRESETS.filter(p=>a.bridgeOf(p.id)&&a.bridgeOf(p.id).kind==='proxy').map(p=>p.id).join(','))"` prints exactly `qwen,crush` — verified runnable at HEAD this session, printing `qwen,crush`. Any `CRITERION FAILS` banner in that output is a failure regardless of the `0` printed after it — the guard's job is to make an unbound `$BASE` visible, not to change the count. The negative alone is satisfied by touching nothing; the positive alone is satisfied by a diff that swapped one proxy engine for another.
    - **The four parity assertions each fail when broken.** Drive assertion 2 RED by temporarily adding `bridge: { kind: 'proxy', api: 'openai', baseUrlEnv: 'X', inboxDelivery: 'terminal' }` to the grok preset; paste the failure, which must name grok and both bridge fields. Revert and paste the green run. Assertion 2 is what stops a future PARITY-02 attempt from deleting a mail bridge, so it is the one that must be seen failing.
    - `node --test --test-reporter=tap test/engine-parity.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the post-task-3 count **+ 4**.
    - **`MARKER_LEDGER` was measured, not copied.** Paste the command that produced it in this session and the resulting map side by side with **C-marker-map**, and show the arithmetic: `sum(MARKER_LEDGER)` equals `C-markers` plus the number of sites task 3 added. Name each added site by file. If the numbers do not reconcile, a marker was added or dropped outside task 3's declared work — find it before the clause is written.
    - **The clause reads raw source, not stripped**, asserted in both directions over a clause body extracted by command — "contains no `readStripped`" alone is satisfied by never writing the clause at all. `C=$(awk '/MARKER_LEDGER/{f=1} f{print} f&&/^}\);/{exit}' test/repo-claims.test.cjs)`, then: `printf '%s\n' "$C" | grep -c 'readStripped'` returns **exactly 0**; `printf '%s\n' "$C" | grep -c 'readFileSync'` returns **at least 1**; and `grep -c 'MARKER_LEDGER' test/repo-claims.test.cjs` returns **at least 2** (the `const` plus at least one use). All three are RED at HEAD — measured, `MARKER_LEDGER` and `LIVE-UNVERIFIED` each occur **0** times in `test/repo-claims.test.cjs` today, so the extractor returns an empty body and every count is zero. Also paste the two-sentence reason from the clause body. A marker pin built on `readStripped` is a gate that cannot fail (D-40), and this file's own header would otherwise push the next editor into building exactly that.
    - **All three RED runs pasted**, each with its failure message and each followed by the green run after revert: (a) a deleted marker fails low; (b) an added marker fails high; (c) a marker moved to an unlisted file passes every per-file entry and fails the repo-wide total. **The revert is proven by re-measurement, not by `git status`.** Every RED edit in this task lands in a file this plan already declares — `src/main/hive.ts` for (a)/(b)/(c), `src/shared/agentProvider.ts` for the planted grok proxy bridge — so `git status --porcelain -- src/ test/` "showing nothing beyond this plan's declared files" is satisfied with a planted proxy bridge still sitting in the tree. After each revert run BOTH: `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c | awk '{s+=$1; print} END {print "SUM="s}'` reproduces `MARKER_LEDGER` and its sum exactly, **and** the `bridgeOf` probe from the zero-conversions criterion above still prints exactly `qwen,crush`. Paste both after each revert.
    - **The positive lower bound is real**: with every `src/` marker removed in a scratch state, the clause fails on the `>= 1` assertion rather than passing on `0 == 0`. Paste that failure too — it is the cheapest possible demonstration that this pin is not this project's fourth vacuous gate.
    - `node --test --test-reporter=tap test/repo-claims.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the pre-task count **+ 1**.
    - `npm run typecheck` exit `0`; `npx eslint src/shared/agentProvider.ts --max-warnings 0` exit `0`; `npm test` exit code `0`, `# fail 0`, `# pass` at least **C-suite**'s + 11 across the whole plan, `# skipped` no higher than C-suite's.
    - Containment, **per-commit and range-bound**, over `src/shared/agentProvider.ts test/engine-parity.test.cjs test/repo-claims.test.cjs`, in the same `$BASE`-bound form as task 2, printing **nothing**.
    - `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; git diff --stat "$BASE"..HEAD -- package.json package-lock.json` prints **nothing** (D-06). The bind guard is load-bearing, not decoration: measured this session with `$BASE` unset, `git diff --stat "$BASE"..HEAD -- package.json package-lock.json` resolves `..HEAD` as `HEAD..HEAD`, prints nothing and exits `0` — so the unguarded form passes green even if the lockfile had been rewritten.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/engine-parity.test.cjs</automated>
  </verify>
  <done>The reason PARITY-02 cannot be extended without giving up a mail bridge is written where the next planner will read it, the cost declaration is pinned to the wiring by a test that goes red when they drift, and the `LIVE-UNVERIFIED` ledger has an exact per-file and repo-wide pin that has been seen failing low, high, sideways and empty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `~/.kimi/config.toml` → `<agentDir>/kimi-config.toml` → the hive git repo | `kimi login` stores OAuth credentials **inside** the config file the bridge copies. Unlike codex — which symlinks `auth.json` and copies only the credential-free `config.toml` — kimi has no such split, so the copy carries a live credential into a directory a debounced committer stages with `git add -A`. |
| `installKimiConfig` → the operator's own `~/.kimi` | The bridge reads the operator's credentialed config. A write on that path would corrupt the operator's personal CLI, not just a hive agent's. |
| `HOOK_SHIM` (written to `<hiveRoot>/bin/`) → every kimi tool call | The shim runs with the agent's `HIVE_SOCK_TOKEN` on every hook event. Adding an engine adds a caller of an existing GATE-01 surface. |
| `startProxyBridge` → a per-agent minted hook-socket token | Every proxy-tier engine gets a loopback listener holding its own GATE-01 token. Each conversion multiplies live listeners and live tokens. |
| The `LIVE-UNVERIFIED` ledger → what this project claims it has run | The only record separating "built" from "verified". It is comment-and-doc only — there is no field in the provider table (D-35) — so nothing but a test stops it drifting. |
| `supportsMcp: true` → what an operator believes an agent can reach | D-25 live-verified that today's MCP channel is a complete no-op. A capability field read as a delivery claim describes tools no agent has. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P02-07-01 | Information disclosure | the seeded `<agentDir>/kimi-config.toml` carrying `kimi login`'s OAuth credential into hive git history | mitigate | `'kimi-config.toml'` joins `MINE_IGNORE_LINES`, written into `<agentDir>/.gitignore` by `ensureMineIgnore` **at agent birth, before any spawn writes the file** — fail closed by ordering, not by detection. File mode `0o600`. Proven, not asserted: a real hive commit with a sentinel credential, then `git ls-files` and `git log -p` checked for both the path and the sentinel. `scrubStagedSecrets` is deliberately NOT the mitigation — it commits UNSCANNED past its size cap and when the diff cannot be read, matches on a regex battery rather than on knowing what the file is, and carries a `harnessAuthored` bypass. |
| T-P02-07-02 | Tampering | a hooks bridge writing into the operator's own credentialed `~/.kimi/config.toml` | mitigate | `installKimiConfig` takes `userHome` explicitly (02-01's style) and writes exactly once, under `dir`. Asserted two ways: exactly one `writeFileSync` in the function body, and a before/after content-and-mtime comparison of a tmp `userHome` across the call. This is also the property `--config-file` exists to give — a per-invocation flag, so the global config is never in the write path at all. |
| T-P02-07-03 | Elevation of privilege | a proxy bridge minting a per-agent GATE-01 token and opening a loopback listener for each converted engine | avoid | **Zero engines are converted to a proxy bridge in this plan** (task 4's ruling). No new `mintProxyToken` caller, no new loopback listener, no new token in an agent's env. Recorded as a deliberate avoidance with its reason in source, so a later plan re-opening it does so knowingly rather than by reading D-34's "zero new code" at face value. |
| T-P02-07-04 | Elevation of privilege | flipping `kimi.canReceiveInbox` makes kimi selectable as the GOD in two pickers, while kimi has no interactive initial-prompt form — an orchestrator that spawns with no hive protocol | mitigate | Ruled on rather than discovered (D-33 / RESEARCH §4.7): the flag flips because PARITY-01a requires delivery; kimi does **not** get `seedDelivery:'type-into-tui'` (unverifiable TUI behaviour, plus §4.1 gap 2's headless breakage); `ensureAgent`'s previously silent bare-spawn fall-through now records `protocol-not-seeded` with the agent id and provider; and the two picker surfaces are handed to plan 02-06 by name in the SUMMARY. |
| T-P02-07-05 | Tampering | a `[[hooks.<Event>]]` shape copied from codex producing a config kimi cannot parse — hooks never fire, and mail is delivered into an engine with no drain | mitigate | The flat-versus-nested distinction is called out as LANDMINE 4 with its source, and asserted in both directions: at least one `[[hooks]]` present, exactly zero `[[hooks.` in the function body. The failure mode is silent at runtime, so it has to be caught in the diff. |
| T-P02-07-06 | Repudiation | a bridge silently unmarked, or a new unverified bridge silently added | mitigate | An exact per-file `MARKER_LEDGER` plus a repo-wide total that must equal its sum, over **raw** source (the clause's stated exception to the file's comment-stripping rule), with a `>= 1` lower bound. Driven RED four ways: low, high, moved-to-an-unlisted-file, and all-markers-removed. |
| T-P02-07-07 | Repudiation | `supportsMcp: true` read as "this engine has MCP servers", when D-25 proved the channel is a no-op | mitigate | The field's doc states what it does and does not claim; a grep asserts it has **zero** consumers outside `providerCapabilities`, so no code path enables anything from it; and plan 02-11 proves the `--mcp-config` channel before plan 02-06 renders the bit. |
| T-P02-07-08 | Repudiation | PARITY-01a presented as improving verification, when it creates the fifth unverified bridge | mitigate | Every kimi site ships `LIVE-UNVERIFIED` in this plan's own diff; the marker pin's expected total is the pre-plan total plus the sites this plan added, reconciled arithmetically; and the SUMMARY states the capability gain (8 to 9) and the verification loss (4 to 5) in that order. |
| T-P02-07-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero package-manager operations occur in this plan.** No TOML parser is added — the bridge string-appends, which is why none is needed. `package.json` and `package-lock.json` are asserted unchanged in three separate tasks (D-06; npm here is 11.6.2 against an npm-10-only lockfile). `02-RESEARCH.md` § Package Legitimacy Audit records no install for this phase, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |
</threat_model>

<verification>
- `npm test` green locally with `# fail 0` and `# skipped` no higher than **C-suite**'s, plus TAP counters pasted for `engine-parity`, `provider-automation`, `repo-claims` and the six hive/gate files. `test/agent-provider.test.cjs` is verified by a **direct run** (exit code plus counted `✓` lines), never by TAP counters — LANDMINE 6.
- `npm run typecheck` exit `0` — and the task notes state that this, not a grep, is what proves all eleven presets carry the required `supportsMcp`.
- `npm run build` exit `0`.
- `npx eslint` clean at `--max-warnings 0` over all four `src/` files this plan touches. If `eslint.config.js` is absent, task 1 has already STOPped.
- **Cross-platform CI is read off the phase's draft PR, never off a phase-branch push** (`ci.yml` is `branches: [main]` only). Run `gh pr checks` and paste the rows for `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`, `Build`. An empty table means nothing was triggered — report that; do not substitute a local run. The `0o600` mode case is POSIX-only, so the macOS and ubuntu rows are the only places it actually executes, and the win32 skip announcement must appear in the Windows row's log.
- **`kimi --help`'s full output**, and which of task 1's three rulings fired.
- **The marker arithmetic, shown**: `C-markers` → the sites task 3 added, named per file → `sum(MARKER_LEDGER)`, with the repo-wide total agreeing.
- **All RED runs pasted**: parity assertion 2 against a planted grok proxy bridge, and the marker pin failing low, high, moved-to-an-unlisted-file, and empty.
- **The live credential proof pasted**: `git ls-files` and `git log -p` against a real hive commit, showing neither the path nor the sentinel.
- **Explicitly NOT claimed:** that PARITY-02 is met as written; that any bridge became verified; that the `mcp` bit or the `platform` parameter reaches a UI (both land here with zero production consumers, by design, for plan 02-06); that kimi's `--auto` flag is confirmed, unless ruling 1 fired; that `providerCapabilities`' renderer path exists (02-04 owns the preload field, 02-06 owns the call sites and the clause that pins them).
</verification>

<success_criteria>
- PARITY-01a: kimi has a per-agent hook bridge that seeds the operator's config without mutating it and without carrying its credential into git, so mail addressed to a kimi worker is delivered rather than bounced — and it ships marked, because no Moonshot account exists.
- PARITY-02 is restated to the reading that is actually buildable, with the measured reason (`bridgeOf` is one-descriptor-per-engine and the dispatch is `hooks` XOR `proxy`) written into source; the four engines that report cost and the seven that do not are both named; and the declaration is pinned to the wiring by a test that can fail.
- PARITY-03's `LIVE-UNVERIFIED` ledger is pinned exactly — per file and repo-wide — and the pin has been seen failing in four directions.
- The engine ledger has one owner: no other plan in this phase edits `src/shared/agentProvider.ts` or `src/shared/providerAutomation.ts`.
- Nothing in this plan claims a capability the phase cannot demonstrate: the `mcp` bit and the `platform` parameter land without consumers and are recorded as such, and PARITY-01b is explicitly not counted as delivered here.
</success_criteria>

<output>
Create `.planning/phases/02-the-daemon-and-the-protocol/02-07-SUMMARY.md` when done. It MUST record:

- the full `kimi --help` output and **which of the three rulings fired**;
- **C-markers, C-marker-map and the final `MARKER_LEDGER`**, with the arithmetic shown and every site
  this plan added named by file, under a heading plan 02-12 can grep for;
- the reconciliation sentence for LANDMINE 1 — whether 02-01's `B-markers-hive + B-markers-templates`
  agreed with the measured total, and why if not;
- **one sentence with the sign the right way round**: the kimi bridge moved engines that can receive
  mail from 8 to 9 and moved live-verified bridges not at all, taking unverified bridges from four to
  five. The capability gap closed; the verification gap widened;
- **PARITY-02's delivered scope, as a number**: four engines of eleven report cost (claude via OTel,
  codex via transcripts, qwen and crush via the proxy), **zero engines were converted by this plan**,
  and the reason — the one-bridge-per-engine constraint D-34 did not have — with copilot, custom and
  antigravity's undelivered `gemini` sidecar mode each named separately;
- the live credential proof (`git ls-files` + `git log -p` against a real hive commit) and the
  before/after tmp-`userHome` comparison;
- all RED runs;
- **what was handed forward, by plan number**: to 02-04, nothing (no preload edit was made); to 02-06,
  the two `canReceiveInbox` god pickers, the `NO MCP` / `NO SPEND` chips, the renderer call sites for
  `providerCapabilities(provider, platform)` and the ≥1-production-consumer clause that would have
  passed vacuously here; to 02-11, the `supportsMcp` field's explicit non-claim about the MCP channel;
  to 02-12, the marker ledger and the `README.md` per-engine table claims this plan did not touch;
- the known divergence between `hive.ts`'s and `memory.ts`'s two copies of `MINE_IGNORE_LINES`, and why
  only the former was edited;
- the rejected `canSeedInitialPrompt` predicate and the one-line reason (it would be a second
  `capabilityLine`);
- one plain sentence stating that PARITY-01b is **not** delivered by this plan, so no reader mistakes
  the `mcp` bit for the UI that renders it.
</output>
