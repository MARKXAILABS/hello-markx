---
phase: 02-the-daemon-and-the-protocol
plan: 12
type: execute
wave: 9
depends_on: ["02-02", "02-03", "02-04", "02-05", "02-06", "02-07", "02-08", "02-09", "02-10", "02-11"]
files_modified:
  - test/repo-claims.test.cjs
  - test/main-hardening.test.cjs
  - README.md
  - HIVE.md
  - docs/message-queue.md
  - SECURITY.md
  - .planning/codebase/TESTING.md
  - .planning/codebase/CONCERNS.md
  # The two documents that still assert what this phase disproved. Verified 2026-08-21 against every
  # 02-*-PLAN.md `files_modified` block: NO Phase-2 plan declares either. The plans carefully refuse to
  # INHERIT the false claims (02-01 "never quoted from ROADMAP.md", 02-02, 02-08) — nothing CORRECTS
  # them, so the phase would close green against its own acceptance surface. Line numbers below are
  # pointers; re-derive by content.
  - .planning/ROADMAP.md         # § Phase 2's two god-file figures and D-02's mechanism, and Success Criteria 1, 3, 4 and 5
  - .planning/REQUIREMENTS.md    # the PARITY-02 requirement text and its status row ONLY — see ownership note 4 in <interfaces>
autonomous: true
requirements: [PARITY-03]

must_haves:
  truths:
    - "D-35: the `LIVE-UNVERIFIED` set is re-measured across the WHOLE of `src/` after every other wave has landed, pinned to a committed number AND to a per-engine lower bound of `>= 1`, so unmarking a bridge and silently adding one both fail the same clause"
    - "D-35: pi, opencode, crush and qwen ship STILL MARKED. None of those four CLIs is installed on this machine and no operator account was supplied, so under the zero-recurring-cost rule that is the expected outcome and is recorded as such — not as a gap this plan failed to close"
    - "D-33: kimi's bridge made it the FIFTH marked bridge, not one fewer. The SUMMARY states the sign explicitly: PARITY-01a moved the count of engines that CAN receive mail from 8 to 9 and moved the count of live-verified bridges by zero"
    - "D-02: `test/main-hardening.test.cjs:5-7` no longer says `index.ts` cannot be loaded because it imports `electron`. `test/load-ts.cjs` has stubbed electron since #55; the blocker was module-scope side effects, and `test/boot-floor.test.cjs` is the proof"
    - "D-02: `.planning/codebase/TESTING.md` tells the same story as the corrected test header — one phase does not get to correct a claim in source and leave the codebase map asserting the old reason"
    - "D-12: all THREE copies of ADR-0001's one-gate sentence agree. `docs/adr/0001-one-gate-for-pty-writes.md` is amended by plan 02-03; this plan amends `docs/message-queue.md` §1 + §6 and `HIVE.md` §2, and asserts across all three — an amended ADR shipping beside two unamended copies is this phase's acceptance test failing"
    - "D-30: `README.md:51-61`'s engine-limitation table describes a channel that genuinely renders, pinned by a both-directions clause over the renderer consumer plan 02-06 delivered — the table was documenting `capabilityLine`, which had zero production consumers"
    - "D-34: PARITY-02's 'all eleven engines report cost' is restated in the docs to the engines actually delivered, naming copilot and custom as the two that cannot, and the restatement is pinned to `costTracking: 'none'` in `src/shared/agentProvider.ts` so a source change forces a doc change"
    - "D-41: DAEMON-05's 'URL always visible' is written down as met-in-purpose-not-literally at 800 px, in the docs and in the SUMMARY. It is never ticked verbatim and the requirement's wording is never quietly widened"
    - "D-40: every clause this plan adds asserts over joined or parsed text AND carries a positive lower bound, and every one of them was driven RED before being trusted. Three gates in this project already pass vacuously; this plan does not author a fourth"
    - "D-40 landmine: the marker clause reads RAW source, never `readStripped` — `LIVE-UNVERIFIED` lives in comments, so the file's house helper erases the very thing being counted and the clause would count zero against any tree"
    - "D-02 / D-05 / D-34 / D-38 / D-41: `.planning/ROADMAP.md`'s Phase-2 section is corrected in place. FOUR of its five Success Criteria assert something this phase disproved, and they are the phase's own acceptance surface — closing green against them is the failure this plan exists to prevent, one level above the source and docs it already sweeps. Every correction is marked so a reader sees what changed, and cites the D-id that falsified it"
    - "D-34: PARITY-02 is restated in `.planning/REQUIREMENTS.md` ITSELF, not only in source comments and `README.md`. The requirement keeps its id and is rewritten to the buildable reading — every engine that can be pointed at a base URL gets cost through the existing proxy bridge, the rest are declared through the capability channel. A checkbox and a status row flipped to Complete against 'all eleven engines report cost' is exactly the silent narrowing D-34's own clause forbids"
    - "D-06: `package.json` and `package-lock.json` are untouched; zero npm packages are added or removed, and the unused `tunnelmole` dependency stays"
  artifacts:
    - path: "test/repo-claims.test.cjs"
      provides: "wave 9's clauses — the re-measured marker ledger, the one-gate agreement across three copies, the engine-table channel, the PARITY-02 restatement, and the tunnel-close correction"
      contains: "LIVE_UNVERIFIED_TOTAL"
    - path: "SECURITY.md"
      provides: "the door's real caveats — the remotely-triggerable global lockout, the self-identifying `/phone/` shell, the Discord buffer-before-verify inversion — and the tunnel-close claim corrected in the direction the phase actually moved it"
      contains: "lockout"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "the phase's six stated limitations as tracked concerns rather than SUMMARY prose"
      contains: "LIVE-UNVERIFIED"
    - path: ".planning/ROADMAP.md"
      provides: "the phase's acceptance surface corrected to what it measured — the two god-file sizes and the handler count, D-02's real blocker, and Success Criteria 1, 3, 4 and 5, each marked with the D-id that falsified it"
      contains: "D-02"
    - path: ".planning/REQUIREMENTS.md"
      provides: "PARITY-02 restated to D-34's buildable reading, keeping the requirement id, with the status row left for the phase rollup to flip against text that is now true"
      contains: "D-34"
  key_links:
    - from: "the marker total committed in test/repo-claims.test.cjs"
      to: "every LIVE-UNVERIFIED site under src/"
      via: "a raw (un-stripped) read of every .ts the file's own sourceFiles() helper finds, counted and attributed per engine"
      pattern: "LIVE-UNVERIFIED"
    - from: "README.md's engine-limitation table"
      to: "the renderer surface plan 02-06 delivered"
      via: "a both-directions clause — the table exists AND at least one src/renderer file imports the capability channel"
      pattern: "providerCapabilities|capabilityLine"
    - from: "SECURITY.md's tunnel entry"
      to: "src/main/tunnel.ts"
      via: "the stale 'cannot be closed' limitation is replaced, and the replacement is pinned to the kill path plan 02-04 delivered"
      pattern: "hardKillTree"
---

<objective>
Close the phase by making every claim it touched true, and by pinning the one requirement whose entire
content is a claim: **PARITY-03**.

This plan runs last and depends on every other plan in the phase for a structural reason, not a
scheduling one. An honesty ledger that pins numbers a later wave can still move is precisely the
failure mode it exists to prevent — plan 02-01 moved two markers into a file that did not exist when
this phase was planned, plan 02-07 added kimi's, and a ledger written before wave 8 would be pinning a
number that three later diffs invalidate.

Two things happen here and they are not the same shape:

1. **The marker ledger (PARITY-03).** Re-measure `LIVE-UNVERIFIED` across the whole of `src/` and pin
   it in both directions with a per-engine lower bound, so that unmarking a bridge **or** silently
   adding one requires editing a committed number in the same diff — which forces a reviewer to ask
   *"verified against which account?"* mechanically rather than by remembering to. pi, opencode, crush
   and qwen stay marked; none of those four CLIs is installed on this machine and no operator supplied
   an account, so under the zero-recurring-cost rule **that is the expected outcome, not a failure**
   (D-35). Kimi joins them, which makes five (D-33).
2. **The corrections.** Every claim this phase falsified, in source and in docs: the wrong reason in
   `main-hardening.test.cjs:5-7` (D-02), the two unamended copies of ADR-0001's one-gate sentence
   (D-12), `README.md`'s engine table documenting a channel that did not render (D-30), PARITY-02's
   unachievable "all eleven" (D-34), DAEMON-05's 800 px caveat (D-41), and `SECURITY.md`'s tunnel
   entry — which this phase falsified **in the direction of understating what the app can now do**,
   which is the harder kind to notice, and which now has to state what the phase newly exposes instead.
3. **The phase's own acceptance surface.** `.planning/ROADMAP.md` and `.planning/REQUIREMENTS.md` still
   assert what this phase disproved, and no other Phase-2 plan declares either file. Every plan in the
   phase carefully refuses to *inherit* those claims (02-01 records that it "never quoted from
   ROADMAP.md"; 02-02; 02-08) — and refusing to inherit a false claim is not the same as correcting it.
   Four of the roadmap's five Success Criteria and PARITY-02's requirement text would be ticked green
   against sentences this phase's own measurements falsified. That is the same failure as an amended
   ADR shipping beside two unamended copies, one level up: the documents the phase is *graded against*.
   This plan is wave 9, it already sweeps every other claim surface, and it is the last plan that can.

Output: ten files whose claims match the source at the end of wave 8, and a set of repo-fact clauses
that have each been seen failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-the-daemon-and-the-protocol/02-CONTEXT.md
@.planning/phases/02-the-daemon-and-the-protocol/02-RESEARCH.md
@.planning/phases/02-the-daemon-and-the-protocol/02-PATTERNS.md
@.planning/phases/02-the-daemon-and-the-protocol/02-VALIDATION.md
@.planning/phases/02-the-daemon-and-the-protocol/02-01-SUMMARY.md
@.planning/phases/02-the-daemon-and-the-protocol/02-07-SUMMARY.md

<interfaces>
**Every line number below was measured at `37f43b2` this session and is a POINTER, not a fact. D-01 is
binding: re-derive each site by content in the executing session. Nine waves land between this plan and
its execution and at least four of them move these lines.**

### The claim sites, as measured

| Site | What it says today | Falsified by |
|---|---|---|
| `test/main-hardening.test.cjs:5-7` | "`src/main/index.ts` itself cannot be loaded here — it imports `'electron'`" | D-02 — the stub gets past the import; module-scope side effects are the blocker |
| `docs/message-queue.md:25-27` | "One place types automatic messages into a live agent's PTY: the **drain loop**, `useHive.ts` effect #4" | D-12 / FLOOR-02 — the drain is `delivery.ts` `drainQueue` in main |
| `docs/message-queue.md:154` | table row: "effect #4 drain (**the one writer**)" | same — the **second** copy in the same file |
| `HIVE.md:106-112` | "`useHive.ts` effect #4 — the one gate allowed to type into a live PTY" | same |
| `docs/adr/0001-one-gate-for-pty-writes.md:20` | the third copy | **owned by plan 02-03 — see the ownership note below** |
| `README.md:51-52` | "Every engine gets one capability line, and the orchestrator is handed **the same words you see here**" | D-30 — `capabilityLine` had zero production consumers; nothing rendered those words |
| `README.md:55-61` | the five-row engine-limitation table | D-30, and D-33 for kimi's `NO MAIL` row |
| `README.md:63-65` | "**Several** engine bridges are additionally marked `LIVE-UNVERIFIED` in `src/main/hive.ts`" | D-35 — the count is now committed and the file list changed at 02-01 |
| `SECURITY.md:24` | proxy-sidecar row: "`hive.ts` — `PROXY_BRIDGE_SHIM`" | plan 02-01 moved `PROXY_BRIDGE_SHIM` into `hiveTemplates.ts` |
| `SECURITY.md:57-62` | "**Three** of the six lifecycle shims send no token at all today" | PARITY-01a adds a seventh shim — re-measure **both** numbers |
| `SECURITY.md:66` | "binds loopback and opens a public tunnel (**tunnelmole**)" | D-14 / plan 02-04 — a cloudflared child process |
| `SECURITY.md:108-113` | "**Stopping a trigger does not close its tunnel** … no handle and no disposer" | D-13 / plan 02-04 — **false in the app's favour now** |
| `.planning/codebase/CONCERNS.md:70-75` | "`kimi`, `copilot`, and `custom` have no lifecycle hooks or cost accounting at all", plus the pointer `src/main/hive.ts:679-820` | D-33 (kimi has a bridge) and plan 02-01 (the pointer moved) |
| `.planning/codebase/TESTING.md:29, :153, :163` | "55 test files", "423 tests, 421 pass, 0 fail, 2 skipped", "The 55 `test/*.test.cjs` files" | every wave — **and 01-23 already corrected these once; re-measure, do not inherit** |
| `.planning/ROADMAP.md:166, :171-172, :185` | "`src/main/index.ts`, 5,620 lines, ~157 IPC handlers" and "`src/main/hive.ts`, 3,562 lines", repeated three times | measured 2026-08-21 on `main`: `wc -l` → **5,812** and **4,121**; `grep -c 'ipcMain.handle(' src/main/index.ts` → **153**. The 3,562 figure is contradicted inside this phase's own context — D-02 calls `hive.ts` "4,121 lines" |
| `.planning/ROADMAP.md:181-183` | "`index.ts` imports `electron`, so it cannot be loaded under `node --test`" | **D-02 — the wrong mechanism, and this is where the wrong mechanism came from.** `test/load-ts.cjs` has stubbed electron since #55; the blocker is module-scope side effects. Task 2 corrects this identical sentence in `test/main-hardening.test.cjs` and task 5 in `TESTING.md` — **correct it here identically, in the same words** |
| `.planning/ROADMAP.md:207` (Success Criterion 1) | "tests for agent lifecycle, shutdown, the mail router and the git committer **that could not be written before the split**" | **D-05 — false for two of the four.** Measured this session: `grep -c flushCommit test/hive-durability.test.cjs` → **6** and `test/engine-parity.test.cjs` → **1**, so the git committer already had tests; `routeOnce()` is a public method (`src/main/hive.ts:1711`, called from `index.ts`). Only shutdown and agent lifecycle are genuinely newly-possible |
| `.planning/ROADMAP.md:221` (Success Criterion 3) | "the answer arrives in **that worker's** terminal" | **D-38** — it arrives in that worker's **inbox**, enqueued as hive mail and delivered by main's drain. Nothing new types into a PTY and ADR-0001 is intact. A criterion phrased as "terminal" is satisfiable only by breaking the ADR this plan's task 2 is simultaneously defending |
| `.planning/ROADMAP.md:231` (Success Criterion 4) | "the live public URL is visible in the UI whenever the tunnel is up" | **D-41** — met in purpose, not literally. At 800 px, degradation step 2 renders `PUBLIC` alone with the untruncated URL one click away in the panel the chip opens |
| `.planning/ROADMAP.md:239` (Success Criterion 5) | "**All eleven** report cost to the ledger and to the breaker" | **D-34** — unachievable for copilot and custom by construction. Same sentence, same correction as task 3's README paragraph |
| `.planning/REQUIREMENTS.md:111` + its status row at `:418` | "- [ ] **PARITY-02**: All eleven engines report cost to the ledger and to the breaker" and `\| PARITY-02 \| Phase 2 \| Pending \|` | **D-34.** 02-07 and this plan's task 3 restate it into source comments and `README.md`; **the requirement file itself is restated nowhere.** The checkbox and the status row would flip to Complete against text this phase's own acceptance test proved false — the exact thing D-34's "never silently" clause exists to prevent |

### Measured at `37f43b2`, to be re-measured

- `grep -c 'LIVE-UNVERIFIED' src/main/hive.ts` → **8**, and
  `grep -rl 'LIVE-UNVERIFIED' src --include=*.ts` → **`src/main/hive.ts` only**. After 02-01 those
  eight split across `hive.ts` and `hiveTemplates.ts` (plan 02-01's SUMMARY records the split as
  **B-markers-hive** / **B-markers-templates**); after 02-07 kimi's marker joins them and 02-07's
  SUMMARY records the resulting number. **Read both SUMMARYs and then measure the tree yourself.
  Neither number is inherited on trust.**
- `grep -rn 'effect #4' HIVE.md docs/ src/` → 11 hits at plan time, of which **three are the one-gate
  claim** (`HIVE.md:107`, `docs/message-queue.md:26`, `docs/adr/0001-…:20`). The rest are ordinary
  in-source cross-references and are **not** in scope — a criterion demanding zero occurrences of the
  string would force deleting true history.

### ⚠ File ownership — four collisions, two of them hard preconditions

1. **`docs/adr/0001-one-gate-for-pty-writes.md` belongs to plan 02-03**, which amends it in wave 3.
   This plan **does not edit it** and does not declare it. It **asserts over it**, because the whole
   point of D-12's item is that the ADR and its two copies must agree. If 02-03's amendment is absent
   when this plan runs the clause goes red — which is correct, and is a STOP-and-report, not a licence
   to edit another plan's file.
2. **Phase 1 plan 01-23 owns `test/repo-claims.test.cjs`, `.planning/codebase/CONCERNS.md` AND
   `.planning/codebase/TESTING.md`** (verified against its `files_modified` block). It is `wave: 9` of
   Phase 1 with `depends_on: […, "01-21", "01-22"]`. **All three of this plan's shared files are its
   files.** That is a hard, checkable precondition (D-06), not an ordering note — task 1 answers it
   with a command before anything else runs.
3. **`test/capability-surface.test.cjs` belongs to plan 02-06** and is where PARITY-01b's
   "`capabilityLine` has ≥ 1 production consumer" assertion lives. This plan's README clause is **not
   a second copy of that**: it pins the *documentation-to-renderer* link — the table exists AND
   something under `src/renderer` imports the channel — so deleting the table and deleting the consumer
   each fail. Read 02-06's SUMMARY first; if it landed an identical assertion, say so in the task notes
   and keep this clause scoped to the README side.
4. **`.planning/REQUIREMENTS.md` is declared by Phase 1 plan 01-23 as well** (`01-23-PLAN.md:9`) — and
   the two edits **cannot** overlap, verified 2026-08-21 rather than assumed:
   - 01-23's task 2 (`:279`, `:284`, `:319`) licenses it to touch **FLOOR-01…18, GATE-01, RECORD-03/04
     and VERDICT-02/03**, "only where a requirement's own text is now factually wrong about source",
     and its three named corrections are FLOOR-02's "the **dead** Stop-drain", FLOOR-03's "supported
     major (38+)" and VERDICT-03's absent filter (`:560-564`). Its whole scope is FLOOR-*; it has no
     reason to touch a Phase-2 row.
   - `grep -c 'PARITY' .planning/phases/01-finish-the-floor/01-23-PLAN.md` returns **`0`**. Re-run it in
     the executing session — if it is non-zero, 01-23 grew a PARITY edit after this plan was written and
     that is a STOP-and-report, not a merge.
   - **This plan touches the `PARITY-02` bullet and the one `| PARITY-02 | Phase 2 | … |` status row and
     nothing else in the file.** Not another requirement's text, not another status row, and **not the
     checkbox state or the status value of PARITY-02 itself** — this plan corrects the *text* the phase
     rollup will later be graded against; flipping Pending→Complete is the rollup's job, and doing it
     here would be this plan certifying its own phase.
   `.planning/ROADMAP.md` has no collision at all: no `02-*-PLAN.md` declares it (verified against every
   `files_modified` block) and 01-23 does not declare it either. Re-verify both before task 6 writes.

### ⚠ THE LANDMINE THAT WOULD MAKE THIS PLAN'S OWN GATE VACUOUS

`test/repo-claims.test.cjs` reads everything through `readStripped(rel)` (`:53`), and its header calls
comment-stripping **mandatory, not tidiness** (`:33-37`) — for good reason, since this phase's honesty
work deliberately leaves comments quoting the claims it deletes.

**`LIVE-UNVERIFIED` is itself a comment.** A marker clause routed through `readStripped` counts
**zero**, every time, against any source tree. Pinned to a committed `8` it goes red immediately and
gets "fixed" to `0`; pinned as `>= 0` it becomes the fourth vacuous gate in this repo. The marker clause
therefore reads **raw** source and says so in its own comment, and its first proof of life is that
deleting one marker makes it fail.

**Plan 02-07 wrote the first version of this pin in wave 4.** Read it before writing anything. If it
routed through `readStripped` it has been green-because-empty for five waves — record that as a
finding, correct it here, and paste the RED run that proves the correction (D-40).

### Style the clauses copy

- **Both directions** — `test/repo-claims.test.cjs:245-269`, quoted in `02-PATTERNS.md:1117-1132`. A
  negative assertion a deletion would satisfy is always paired with a positive one a deletion breaks.
- **Structure-bounded parsing, never a character count** — `migrationsBody()` at the tail of
  `test/repo-claims.test.cjs` bounds an array literal by the `];` that closes it, with a comment
  explaining that a fixed-size slice of a growing file silently measures the wrong thing. The marker
  clause's per-engine attribution windows are bounded the same way: by the comment block the marker
  sits in, not by N characters either side.
- **One owner per wave** (`test/repo-claims.test.cjs:15-17`). This plan is wave 9's owner and the last
  one; the file is asserted whole at the end of the phase, which is this plan's `<verification>`.
- **`.planning/**` is NOT source.** No repo-fact clause is authored over `TESTING.md`, `CONCERNS.md`,
  `ROADMAP.md` or `REQUIREMENTS.md`. A grep-pin over a planning document asserts that a sentence exists
  — defeated by any paraphrase, satisfied by any copy. It is decoration, and tasks 5, 6 and 7 say so out
  loud rather than shipping one. **D-40 still binds those three tasks in full**, at the layer where it
  can actually bite: their *acceptance criteria* are joined-text greps, each paired with a **positive
  lower bound so that deleting the correction fails the criterion** rather than satisfying it.

  **The joiner is `tr -d '\r' < FILE | tr '\n' ' ' | tr -s ' '`, and BOTH halves are load-bearing.** Every one of
  these files hard-wraps its prose mid-sentence, so a line-oriented grep for a wrapped claim finds
  nothing and reads as PASS against the uncorrected file — `ROADMAP.md:181-182` splits "`index.ts`
  imports `electron`, so it / cannot be loaded", and measured 2026-08-21 the line-oriented grep returns
  `0` while the joined one returns `1`. And these are **indented** continuation lines, so joining alone
  leaves a run of spaces mid-phrase: `ROADMAP.md:207-208` becomes `could not be` + five spaces +
  `written before the split`, and a joined-but-unsqueezed grep for that claim **also** returns `0`
  today. Both traps were hit while writing these criteria; the squeeze is what makes them non-vacuous.
  **Every negative criterion in tasks 6 and 7 carries its measured pre-correction value** (`1` for the
  claim, `0` for the correction) so the executor can prove the criterion could fail before trusting it
  when it passes. Apply the same joiner to tasks 2-4's doc criteria if a wrapped claim there returns
  an unexpected `0`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Answer the two preconditions, re-measure the marker ledger across the whole tree, and pin it in both directions</name>
  <files>test/repo-claims.test.cjs</files>
  <read_first>
    - `test/repo-claims.test.cjs` in full — `:1-37` (the accumulator's purpose and the one-owner-per-wave rule), `:33-53` (`stripComments`, `sourceFiles`, `readStripped`), `:245-269` (the both-directions template), and the `migrationsBody()` helper at the tail (structure-bounded parsing, and the comment saying why a character count is wrong).
    - `.planning/phases/02-the-daemon-and-the-protocol/02-01-SUMMARY.md` § the heading carrying **B-markers-hive** and **B-markers-templates** — plan 02-01 was required to hand these forward for exactly this clause.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-07-SUMMARY.md` in full — the wave-4 pin, kimi's marker, and which file it landed in.
    - `.planning/phases/01-finish-the-floor/01-23-PLAN.md` frontmatter — the three-file collision.
    - `02-CONTEXT.md` D-33, D-35, D-40 in full.
    - `02-VALIDATION.md` § "Cannot be verified without an operator-supplied device or account" — the PARITY-03 row and the PARITY-01a row, which pull in opposite directions and both belong in the SUMMARY.
    - `02-PATTERNS.md:1098-1140` — the repo-claims analog section in full.
  </read_first>
  <action>
    **Precondition A — 01-23 has landed.** Run `git log --oneline main -- .planning/codebase/TESTING.md
    .planning/codebase/CONCERNS.md test/repo-claims.test.cjs` and
    `ls .planning/phases/01-finish-the-floor/01-23-SUMMARY.md`. Phase 1 plan 01-23 declares all three
    of this plan's shared files. If its SUMMARY is absent or its commits are not on `main`, **STOP and
    report** — two doc sweeps editing the same three files with `use_worktrees: false` is the collision
    D-06 exists to prevent, and this plan is the one that must yield.

    **Precondition B — every wave this plan pins has landed.** Run
    `ls .planning/phases/02-the-daemon-and-the-protocol/02-*-SUMMARY.md` and confirm 03 through 11 are
    all present. This plan's entire justification for being wave 9 is that a ledger written earlier
    pins numbers a later diff moves. A missing SUMMARY is a STOP, not a caveat.

    **Then record the D-01 baselines**, each with pasted command output (enumerated in the criteria).

    **Now the ledger.** Add a `readRaw(rel)` helper beside `readStripped`, carrying a `/** */` that
    states plainly why this one clause must not strip: the marker IS a comment, and the house helper
    would erase the thing being counted. Then add one clause, `PARITY-03 — the live-unverified ledger`:

    - Enumerate every `.ts` under `src/` with the file's own `sourceFiles()` helper — **not a hardcoded
      file list**. Plan 02-01 moved two markers into a file that did not exist when this phase was
      planned; a hardcoded list is how the next move goes unseen.
    - Count `LIVE-UNVERIFIED` across all of them, raw. Assert **exactly** a committed constant
      `LIVE_UNVERIFIED_TOTAL`, whose value you measure in this session and write into the file with a
      comment naming the date, the commit, and the only reason the number is allowed to change.
    - For each marker, take the comment block it sits in — **bounded by the block's own structure**
      (`/** … */`, or the run of consecutive `//` lines around it), never by a character count — and
      attribute it to whichever of `pi`, `opencode`, `crush`, `qwen`, `kimi` it names.
    - Assert **each of the five engines has `>= 1` marked site**. This is the positive half and it is
      the whole point: a total alone is satisfied by unmarking crush and adding a marker elsewhere.
    - Assert the **set of files carrying markers** equals a committed list, so a future split that
      moves them again shows up in a diff rather than in nobody's head.
    - The failure message is a sentence in the register of the file's existing messages, and it names
      the question the clause exists to force: *which account was this bridge verified against, by
      whom, on what date* — and that under the zero-recurring-cost rule the correct answer is normally
      "none, and it stays marked".

    **Drive it RED four ways** and paste all four, each followed by the green run after reverting:
    (1) delete one marker → the total fails **and** that engine's `>= 1` fails;
    (2) add a marker in a file that has none → the total and the file-set both fail;
    (3) rename the engine inside one marked block → that engine's `>= 1` fails while the total passes,
        which is exactly the case a count-only pin waves through;
    (4) route the clause through `readStripped` instead of `readRaw` → the count collapses to zero.
    Run (4) even if plan 02-07's pin already reads raw; it is the proof that this file's mandatory
    comment-stripping and this clause's mandatory raw read are both deliberate.

    **If plan 02-07's wave-4 pin routed through `readStripped`**, it has been passing on empty text
    since wave 4. Correct it in place, record it in the task notes as a fourth instance of D-40's
    pattern, and paste the RED run proving the correction.

    **Do not schedule verification of any bridge.** No plan in this phase may unmark one. pi, opencode,
    crush and qwen are not installed on this machine and no operator account exists; kimi's bridge is
    new and unverified by construction (D-33). The honest outcome is five marked bridges, and this plan
    says so in the clause, in the docs and in the SUMMARY.
  </action>
  <acceptance_criteria>
    - **B-sha recorded before any edit**: paste `git rev-parse HEAD`. Every containment criterion below is range-bound against it; an unrecorded B-sha makes them unrunnable.
    - **Precondition A answered with pasted output** of the `git log` and the `ls`. If 01-23 has not landed, the task notes record STOP and no further task in this plan runs.
    - **Precondition B answered with pasted output** listing SUMMARYs 02-03 … 02-11. Any missing file is a STOP.
    - **D-01 baselines recorded, each with pasted command output:**
      `grep -rc 'LIVE-UNVERIFIED' src --include=*.ts | grep -v ':0'` (**B-markers-by-file**);
      the same piped to `awk -F: '{s+=$2} END {print s}'` (**B-markers-total** — it was `8` in one file at plan time and is expected to be higher and spread across more files; re-measure, do not copy);
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped|todo) ' "$TAP"; rm -f "$TAP"` (**B-suite**);
      `ls test/*.test.cjs | wc -l` (**B-testfiles**).
      The task notes state that plan 02-01's and 02-07's SUMMARY numbers were **read and then re-measured**, and record any disagreement between them and the tree as a finding.
    - **The clause body is extracted structurally, and every criterion below that says "the clause body" uses this exact extractor** — bounded by the `});` that closes the `test(` block, in `migrationsBody()`'s style, never by a line count:
      `CLAUSE() { awk "/^test\('PARITY-03/{f=1} f{print} f && /^\}\);/{exit}" test/repo-claims.test.cjs; }`
      Measured at HEAD 2026-08-21: `CLAUSE | wc -l` → **`0`** (the clause does not exist yet), so every grep over it below is RED until it lands.
    - `grep -c 'readRaw' test/repo-claims.test.cjs` returns at least `2` (the definition and at least one use), **and** `CLAUSE | grep -c 'readStripped('` returns **exactly `0`**, **and** `CLAUSE | grep -c 'readRaw('` returns at least `1`. Both directions (D-40) — the zero alone is satisfied by the clause not existing, which is its state today. Measured at HEAD: `grep -c 'readRaw' test/repo-claims.test.cjs` → **`0`**, `CLAUSE | grep -c 'readRaw('` → **`0`**.
    - `grep -c 'LIVE_UNVERIFIED_TOTAL' test/repo-claims.test.cjs` returns at least `2`, and the committed value equals **B-markers-total**. Paste the constant's line and B-markers-total side by side.
    - **The file commits all five engine names**: `grep -oE "'(pi|opencode|crush|qwen|kimi)'" test/repo-claims.test.cjs | sort -u | wc -l` returns **exactly `5`**. Re-measured at HEAD 2026-08-23: **`0`** — RED, so it can fail today. **This one is deliberately file-wide and its sentence now says so.** A `CLAUSE`-scoped `== 5` would be the wrong assertion in the other direction: this file's own style puts committed constants at module scope — the criterion two lines up greps `LIVE_UNVERIFIED_TOTAL` file-wide for exactly that reason — so an engine list declared beside `LIVE_UNVERIFIED_TOTAL` and looped over inside the clause is a **correct** implementation that a `CLAUSE`-scoped `== 5` would fail (D-40(b)). What proves the clause *consumes* the five, rather than merely that the file names them, is **RED run (3)**: rename the engine inside one marked block, and that engine's `>= 1` must fire while the total still passes. That run is not optional, and the assertion text it printed must be pasted beside this number.
    - The clause enumerates via the shared helper, and does not read a hardcoded path. Three parts, all over `CLAUSE`:
      `CLAUSE | grep -c 'sourceFiles('` returns at least `1`; **and** `CLAUSE | grep -cE 'assert\.(deepEqual|deepStrictEqual)'` returns at least `1` — the committed marker-file set the action requires, so a future split that moves the markers again shows up in a diff; **and** `CLAUSE | grep -cE "(readRaw|readStripped|readFileSync)\([^)]*'src/main/"` returns **exactly `0`** — no file is *read* by a literal path, which is the failure plan 02-01 already demonstrated. All three are **`0`** over `CLAUSE` today, so the first two are RED and the third is trivially satisfied until the clause lands, which is why it is never the whole criterion.
      **The earlier form — `CLAUSE | grep -c "'src/main/hive.ts'"` returns exactly `0` — was unsatisfiable**, because the same task's action four bullets earlier *requires* the committed file-set list, and at HEAD every marker lives in `src/main/hive.ts` (`grep -rl 'LIVE-UNVERIFIED' src --include=*.ts` → `src/main/hive.ts`, `grep -rc` → `8`), so that literal is in the list. Measured 2026-08-23 against a clause implementing the action exactly, and against one that hardcodes the list as its read source:
      ```
      == correct clause: sourceFiles() enumerates, deepEqual pins the file set
      old criterion  CLAUSE | grep -c "'src/main/hive.ts'"          = 1     <- demanded 0; correct code FAILS
      new negative   hardcoded path as a READ source                = 0
      new positive   sourceFiles(                                   = 1
      new positive   committed file-set assertion                   = 1
      == hardcoded-list clause: iterates a literal array, reads a literal path
      old criterion  CLAUSE | grep -c "'src/main/hive.ts'"          = 2
      new negative   hardcoded path as a READ source                = 1     <- FAILS, correctly
      new positive   sourceFiles(                                   = 0     <- FAILS
      new positive   committed file-set assertion                   = 0     <- FAILS
      ```
      **File-wide is not the assertion for the `sourceFiles(` half**: re-measured at HEAD 2026-08-23, `grep -c 'sourceFiles(' test/repo-claims.test.cjs` → **`7`** (it was `4` when this plan was written — re-measure it yourself, D-01), so the file-wide form is already green before this task starts and asserts nothing. All three greps run over `CLAUSE`.
    - **All four RED runs pasted**, each with the assertion text that fired, each followed by the green run after revert. `git status --porcelain -- src/ test/` shows nothing beyond this plan's declared files afterwards.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/repo-claims.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E '^# (tests|pass|fail|skipped|todo) ' "$TAP"; rm -f "$TAP"` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the pre-task count + 1.
    - `npm run typecheck` exit code `0`.
    - **`$BASE` bound inline, or this criterion fails rather than passes**: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; git diff --stat "$BASE"..HEAD -- package.json package-lock.json` prints **nothing** (D-06), and `grep -c '"tunnelmole"' package.json` returns at least `1` — the unused dependency stays and this plan does not schedule its removal. **Measured 2026-08-21 with `$BASE` unset**: `git diff --stat ""..HEAD -- package.json package-lock.json` resolves to `HEAD..HEAD`, prints nothing and exits `0` — no error, no warning, and "prints nothing" reads as PASS against a diff that added a dependency. The bind guard is the whole criterion.
    - Containment, **per-commit and range-bound**: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD -- test/repo-claims.test.cjs) && test -n "$SHAS" || { echo "UNBOUND BASE OR NO COMMITS — CRITERION FAILS"; false; }; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^test/repo-claims\.test\.cjs$"` must print **nothing**. Bind `$BASE` inside the criterion — an unbound variable makes `git log` fatal with empty stdout, which "must print nothing" reads as PASS.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>Both preconditions are answered with commands, the marker ledger is measured across the whole tree rather than a remembered file list, it fails in four independent directions including the comment-stripping one, and no bridge was unmarked.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the wrong reason in `main-hardening.test.cjs` and make all three copies of ADR-0001 agree</name>
  <files>test/main-hardening.test.cjs, docs/message-queue.md, HIVE.md, test/repo-claims.test.cjs</files>
  <read_first>
    - `test/main-hardening.test.cjs:1-30` — the false header at `:3-8` in full, and `tempDir(t, prefix)` at `:22-26`. `02-PATTERNS.md:1030-1046` quotes this header verbatim as the claim this phase corrects.
    - `test/load-ts.cjs:10-26` — `requireElectron` and the comment explaining the stub and why an injected `require.cache` entry beats it. This is the authority for the corrected sentence.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-02-SUMMARY.md` — the pasted D-02 reproduction (the loader gets past the import, writes a real log file, and dies at `app.on is not a function`). Cite what it measured; do not re-derive a different story.
    - `test/boot-floor.test.cjs` as plan 02-02 wrote it — the existence proof the corrected header points at.
    - `docs/message-queue.md` §1 (`:23-33`) and §6's table (`:148-155`) — **two copies of the same claim in one file**.
    - `HIVE.md` §2 (`:95-118`) — the third in-scope copy, inside a blockquote that already says the drain works headless.
    - `docs/adr/0001-one-gate-for-pty-writes.md` as plan 02-03 amended it — **read only**. The corrected wording in the two files this task owns must match the ADR's, not paraphrase it.
    - `src/main/delivery.ts` around `drainQueue` and `quiesce` — the code all three copies must now describe.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-03-SUMMARY.md` — what DAEMON-01 actually changed about who types into a PTY headless; D-11's `emitTerminalHandoff` and crush-seed fixes both land in main and both bear on the one-gate wording.
  </read_first>
  <action>
    **`test/main-hardening.test.cjs:3-8`.** Replace the reason, keep the file's purpose sentence. The
    corrected header states three things and no more: `index.ts` is not loaded here because it runs
    **module-scope side effects** (an `app.on` at import time, and an `initFileLogging()` that opens a
    real write stream and cross-contaminates parallel test files through a shared stub path); the
    `electron` import itself has not been the blocker since #55, because `test/load-ts.cjs` stubs it;
    and the composed boot path now **is** tested, in `test/boot-floor.test.cjs`. Name both files. This
    is a comment fix — **no assertion changes and this file's `# pass` count must not move**.

    **`docs/message-queue.md` §1.** The one-gate paragraph names main's drain (`src/main/delivery.ts`
    `drainQueue`) as the single writer and describes `useHive.ts` effect #4 in the past tense as where
    the gate used to live, with the FLOOR-02 move named. Keep the existing parenthetical about the god
    agent's boot sequence and **re-derive whether it is still true** against `useHive.ts` — if plan
    02-03 moved it, say so; if it did not, leave it and record that you checked. Keep the ASCII flow
    diagram and update its right-hand side to the real path.

    **`docs/message-queue.md` §6.** The table row calling effect #4 "the one writer" is the second copy
    in the same file, and is exactly how a single-site fix ships incomplete. Correct it and add the
    `src/main/delivery.ts` row.

    **`HIVE.md` §2.** The blockquote already says the drain keeps working headless; the sentence below
    it still hands the one-gate title to effect #4. Correct that sentence only. Do not rewrite the
    surrounding decision — it is accurate.

    **Then one clause in `test/repo-claims.test.cjs`**, both directions, over the three doc files
    **joined into one string** so a reflow across lines cannot defeat it:
    - Negative: none of `HIVE.md`, `docs/message-queue.md`,
      `docs/adr/0001-one-gate-for-pty-writes.md` names `useHive.ts` effect #4 as the *current* one gate.
      Match the **claim**, not the bare string `effect #4` — that phrase legitimately survives as a
      historical reference, and matching it bare would fail the correct fix.
    - Positive, three halves, each `>= 1`: all three files name main's drain; `src/main/delivery.ts`
      still contains `drainQueue(`; and `docs/adr/0001-one-gate-for-pty-writes.md` still exists and is
      non-empty. Without the third half, deleting the ADR satisfies the negative — and an ADR is
      exactly the kind of file a tidy-up deletes.

    **If the ADR is unamended when this runs the clause is red, and that is a STOP-and-report.** Plan
    02-03 owns that file. Do not edit it, and do not narrow the clause to the two files you own — the
    whole item is that three copies must agree.

    Drive the clause RED three ways: restore the old sentence in `HIVE.md`; delete the ADR; delete
    `drainQueue(` from `delivery.ts`. Paste all three, then the green run.
  </action>
  <acceptance_criteria>
    - `grep -c "it imports 'electron'" test/main-hardening.test.cjs` returns **exactly `0`**, **and** `grep -cE 'module-scope|boot-floor' test/main-hardening.test.cjs` returns at least `1`. Both directions — the first alone is satisfied by deleting the header (D-40).
    - `node --test test/main-hardening.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, and `# pass` **exactly equal** to the pre-task count. Paste both. A comment fix that moves a test count changed behaviour.
    - **The three doc copies, joined+squeezed, with the pattern written out rather than described.** The claim wraps in all three files and `HIVE.md`'s copy wraps *inside a blockquote*, so the continuation carries `   > ` — plain `tr '\n' ' '` leaves a run of spaces and a `>` mid-phrase. The joiner is `tr -d '\r' < FILE | tr '\n' ' ' | tr -s ' '` and the pattern must tolerate the marker. For each of `HIVE.md`, `docs/message-queue.md`, `docs/adr/0001-one-gate-for-pty-writes.md`:
      **negative** — `tr -d '\r' < FILE | tr '\n' ' ' | tr -s ' ' | grep -coE '(one place types|the one gate|the one writer).{0,200}useHive\.ts|useHive\.ts.{0,200}(the one gate|the one writer)'` returns **exactly `0`**. Measured at HEAD 2026-08-21: **`1` / `1` / `1`** — RED in all three, so the criterion can fail.
      **positive** — `tr -d '\r' < FILE | tr '\n' ' ' | tr -s ' ' | grep -coE '(one place types|the one gate|the one writer|single writer).{0,200}(delivery\.ts|drainQueue)|(delivery\.ts|drainQueue).{0,200}(the one gate|the one writer|one place types)'` returns at least `1`. Measured at HEAD: **`0` / `0` / `0`** — RED in all three.
      **The bare positive `grep -ci 'drainQueue\|delivery\.ts'` is NOT the assertion**: measured at HEAD it already returns **`1`** for `HIVE.md`, from four pre-existing unrelated mentions (`HIVE.md:91`, `:114`, `:250`, `:295`). It is green before any work is done and cannot fail. Paste all six numbers and the six commands.
    - `grep -c 'effect #4' docs/message-queue.md` is **allowed** to be non-zero — the phrase legitimately survives as history — but **every surviving occurrence must carry historical framing on its own line**: `grep -n 'effect #4' docs/message-queue.md | grep -vcE 'used to|previously|historical|no longer|before FLOOR-02|until FLOOR-02|past'` returns **exactly `0`**. Measured at HEAD 2026-08-21: **`2`** — RED, so this is a check and not a narration. Paste both numbers.
    - `grep -c 'the one writer' docs/message-queue.md` returns **exactly `0`**. Measured at HEAD 2026-08-21: **`1`**, at `docs/message-queue.md:154`, and that occurrence does name effect #4 (`effect #4 drain (the one writer)`), so the conditional is already answered and the criterion is unconditional. **and** `grep -c 'delivery\.ts' docs/message-queue.md` returns at least `1` (measured at HEAD: **`0`**) — the table's replacement row. Both directions.
    - **All three RED runs pasted** for the new clause, plus the green run after revert.
    - `node --test test/repo-claims.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the post-task-1 count + 1.
    - `npm test` exit code `0`, `# fail 0`, `# pass` at least **B-suite**'s pass count + 1, `# skipped` no higher than B-suite's.
    - Containment, **per-commit and range-bound**, over `test/main-hardening.test.cjs docs/message-queue.md HIVE.md test/repo-claims.test.cjs` using the same `$BASE`-bound form as task 1, printing **nothing**. In particular `docs/adr/0001-one-gate-for-pty-writes.md` must **not** appear.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/main-hardening.test.cjs test/repo-claims.test.cjs</automated>
  </verify>
  <done>The test file states the real reason and points at the boot test that proves it, all three copies of ADR-0001's one-gate sentence describe main's drain, and a clause seen failing three ways stops any one of them drifting back alone.</done>
</task>

<task type="auto">
  <name>Task 3: `README.md` — an engine table that describes a channel which renders, and PARITY-02 restated to what shipped</name>
  <files>README.md, test/repo-claims.test.cjs</files>
  <read_first>
    - `README.md:33-66` — the "What it is" section, the capability-line sentence at `:51-52`, the five-row table at `:55-61`, and the `LIVE-UNVERIFIED` paragraph at `:63-65`.
    - `02-CONTEXT.md` D-30 (zero production consumers, and that this exact README table is named as the doc promising a channel that does not run), D-31 (the three surfaces), D-32 (the measured per-engine ledger — 8 of 11 mail, 4 of 11 cost), D-33 (kimi), D-34 (why "all eleven" is unachievable), D-41 (the 800 px caveat).
    - `.planning/phases/02-the-daemon-and-the-protocol/02-06-SUMMARY.md` — which of the three PARITY-01b surfaces actually rendered, and whether `test/capability-surface.test.cjs` already asserts the consumer.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-07-SUMMARY.md` — which engines actually got proxy cost under PARITY-02, and which did not.
    - `.planning/phases/02-the-daemon-and-the-protocol/02-10-SUMMARY.md` — what the `PUBLIC` chip degrades to at 800 px, in that plan's own words.
    - `src/shared/agentProvider.ts` — the eleven presets **after** wave 4: `canReceiveInbox`, `costTracking`, bridge kind, and the copilot/custom preset comments D-34 quotes.
    - `src/shared/providerAutomation.ts` — `providerCapabilities` and `capabilityLine` as plan 02-07 left them, including the `supportsMcp` field and the amended ADR-0002 JSDoc.
    - `test/repo-claims.test.cjs:245-269` again — the template.
  </read_first>
  <action>
    Re-derive the whole table from `src/shared/agentProvider.ts` **in this session**. Do not edit rows
    against D-32's plan-time table — kimi's `NO MAIL` row is false after PARITY-01a, and any engine plan
    02-07 pointed at the proxy has moved out of the `spend UNTRACKED` row.

    Four edits:

    1. **`:51-52`'s capability-line sentence.** It claimed the operator sees the same words the
       orchestrator gets. Until wave 6 nothing rendered them (D-30). Now say where they render — naming
       the surfaces plan 02-06 delivered, from its SUMMARY, not from D-31's plan-time list.
    2. **The table.** Re-derived per engine. Add a `NO MCP` row if plan 02-11's `supportsMcp` preset
       field produces one.
    3. **PARITY-02, restated (D-34).** One short paragraph under the table saying which engines report
       cost and by which mechanism, and naming **copilot** and **custom** as the two that report none —
       with their own preset comments as the reason ("spend sits on the user's Copilot plan"; "unknown
       binary — nothing to read"). **The words "all eleven" must not appear as a claim about cost
       anywhere in the file.** Say what was delivered, then say the requirement as written is
       unachievable, in that order. Never narrow the requirement's wording silently.
    4. **The `LIVE-UNVERIFIED` paragraph.** Replace "Several … in `src/main/hive.ts`" with the number
       and the file list task 1 measured, and name the five engines. Add the sentence that makes the
       marking honest: they stay marked because no account exists to run them against, and unmarking
       one requires editing a committed number in `test/repo-claims.test.cjs` — the reviewer prompt,
       stated where a reader actually meets it.

    Also add **one sentence** recording DAEMON-05's 800 px caveat (D-41) where the public URL is
    described: met in purpose, because presence is the signal and the tunnel can never be up without
    the operator seeing the chip; not met literally at the narrowest width, where the untruncated URL is
    one click away in the panel the chip opens. **Do not tick the clause and do not reword the
    requirement.** The full statement of the limitation lives in `CONCERNS.md` (task 5); this is the
    user-facing pointer to it.

    Then **one clause** in `test/repo-claims.test.cjs`, both directions, over joined text:
    - Positive (`>= 1` each): `README.md` still contains the engine table — count its rows by the
      table's own `|`-delimited structure, never a fixed slice — **and** at least one file under
      `src/renderer` imports the capability channel from `@shared/providerAutomation`.
    - Negative: `README.md` contains no claim that all eleven engines report cost, **and**
      `src/shared/agentProvider.ts` still declares `costTracking: 'none'` for copilot and custom — so
      if a future change gives either one a cost path, this clause goes red and forces the README
      paragraph to be rewritten in the same diff.

    That second negative half fails on a **source improvement**, on purpose. Say so in the clause's
    comment, so the next reader does not delete it as a false positive.

    Drive it RED three ways: delete the README table; delete the renderer import; change copilot's
    `costTracking` to anything else. Paste all three and the green run.
  </action>
  <acceptance_criteria>
    - **The table was re-derived, not edited from memory.** Paste the command that read the eleven presets out of `src/shared/agentProvider.ts` (`canReceiveInbox` / `costTracking` / bridge kind per preset) and the resulting table, beside the README rows as written. Any row that disagrees is a defect in the README, not in the source.
    - `tr -d '\r' < README.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'all eleven[^.]*cost|eleven engines[^.]*(cost|ledger|breaker)' | wc -l` returns **exactly `0`** (the `tr -s ' '` is the canonical joiner — the unsqueezed form is the trap D-40 names), **and** the restatement is asserted as a *claim* rather than as a word: `tr -d '\r' < README.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'copilot.{0,200}custom.{0,200}(no cost|report none|cannot report|nothing to read)|(no cost|report none|cannot report|nothing to read).{0,200}copilot.{0,200}custom' | wc -l` returns at least `1`. Measured at HEAD 2026-08-21: negative **`0`**, positive **`0`** — the positive is RED, so the bullet can fail. **The bare `grep -c 'copilot' README.md` is NOT the assertion: it already returns `4` at HEAD** (`README.md:40`, `:59`, `:60`, `:61`) and cannot fail.
    - **kimi's two moves, asserted rather than eyeballed** (D-33): `grep -E '^\|.*NO MAIL' README.md | grep -c 'kimi'` returns **exactly `0`** (measured at HEAD: **`1`**, `README.md:59`), **and** the marker paragraph names all five engines — ``awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' README.md | grep -oE '`(pi|opencode|crush|qwen|kimi)`' | sort -u | wc -l`` returns **exactly `5`** (measured at HEAD: **`0`**). Both RED. **The backticks in that pattern are PLAIN, and the double-backtick code span is what lets them be** — in GNU grep `\`` is the buffer-start anchor, not a literal backtick, so the escaped form `grep -oE '\`(pi|…)\`'` matches nothing on any input and returns `0` unconditionally, which would make this criterion unpassable and its HEAD `0` meaningless. Both forms measured 2026-08-23 (GNU grep 3.0) against a paragraph that names all five engines correctly:
      ```
      $ cat /tmp/rm-sim.md          # a PERFECT marker paragraph: all five engines, correctly quoted
      Five engine bridges are marked `LIVE-UNVERIFIED`: `pi`, `opencode`, `crush`, `qwen` and `kimi`.

      $ awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' /tmp/rm-sim.md | grep -oE '\`(pi|opencode|crush|qwen|kimi)\`' | sort -u | wc -l
      0        # ESCAPED form: 0 against a PERFECT paragraph -- can never pass, on any input
      $ awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' /tmp/rm-sim.md | grep -oE '`(pi|opencode|crush|qwen|kimi)`' | sort -u | wc -l
      5        # PLAIN form: GREEN on a correct paragraph
      $ awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' README.md | grep -oE '`(pi|opencode|crush|qwen|kimi)`' | sort -u | wc -l
      0        # PLAIN form at HEAD 2026-08-23: RED, and RED because the paragraph names no engine
      ```
      **The bare `grep -c 'kimi' README.md` is NOT the assertion: it already returns `3` at HEAD**, two of those hits being the very rows this task rewrites.
    - **The three-places-one-number check is a command, not a comparison by eye.** Run it and paste its single line of output:
      `R=$(awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' README.md | grep -oE '[0-9]+' | head -1); T=$(grep -oE 'LIVE_UNVERIFIED_TOTAL[^0-9]*[0-9]+' test/repo-claims.test.cjs | grep -oE '[0-9]+$' | head -1); test -n "$R" && test -n "$T" && test "$R" = "$T" && echo "SAME=$R" || echo "MISMATCH README=$R TEST=$T — CRITERION FAILS"`
      Measured at HEAD 2026-08-21: `MISMATCH README= TEST= — CRITERION FAILS` (the README says "Several" and the constant does not exist). A README saying "several" while the test pins a number is the gap this plan closes, and an empty variable must fail rather than compare equal to another empty one.
    - `grep -ciE '800 ?px' README.md` returns at least `1` (measured at HEAD: **`0`**), **and** the file makes no unqualified always-visible claim: `tr -d '\r' < README.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'URL.{0,60}always visible|always visible.{0,60}URL' | wc -l` returns **exactly `0`** (measured at HEAD: **`0`** — the negative is a regression invariant here; the positive is the half that is RED). Quote the sentence in the task notes and state which half of D-41 it claims.
    - `grep -rl 'providerCapabilities\|capabilityLine' src/renderer --include=*.ts --include=*.tsx | wc -l` returns at least `1`. Paste the file list — this is D-30's headline, and the number was `0` before this phase.
    - **All three RED runs pasted**, plus the green run after revert.
    - The task notes state whether `test/capability-surface.test.cjs` (plan 02-06) already asserts the renderer consumer, and if so that this clause is scoped to the README-to-renderer link rather than duplicating it.
    - `node --test test/repo-claims.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0`, `# pass` at least the post-task-2 count + 1.
    - `npm test` exit code `0`, `# fail 0`.
    - Containment, **per-commit and range-bound**, over `README.md test/repo-claims.test.cjs` using the same `$BASE`-bound guarded form as task 1 — the guard is what makes it fail rather than pass on an unbound variable — printing **nothing**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/engine-parity.test.cjs</automated>
  </verify>
  <done>Every row of the README's engine table was re-derived from the presets, the channel it documents demonstrably renders, PARITY-02 is stated as what shipped with the two impossible engines named, and DAEMON-05's 800 px caveat is written down instead of ticked.</done>
</task>

<task type="auto">
  <name>Task 4: `SECURITY.md` — the door's real caveats, and the tunnel claim corrected in the direction the phase moved it</name>
  <files>SECURITY.md, test/repo-claims.test.cjs</files>
  <read_first>
    - `SECURITY.md` in full. Specifically `:11-31` (the network-surface table and its "symbols rather than line numbers on purpose — stale line numbers are how this document went wrong the first time" note), `:33-62` (the hook-server section and the "three of the six lifecycle shims" gap), `:63-70` (the Slack/webhook trigger paragraph naming tunnelmole), `:76-140` (Known limitations, including `:108-113`'s now-false tunnel entry).
    - `02-CONTEXT.md` D-13 (the library-mode blocker and the child-process answer), D-14 (no publisher checksum; the #57 precedent), D-17 (no $0 stable URL; 200 concurrent; no SSE; "testing and development only"; ~30 s verified), D-18/D-19 (why the tunnel is mandatory and what origin churn kills), D-23 (the global lockout is remotely DoS-able behind a tunnel; per-IP is meaningless), D-24 (per-endpoint verifiers).
    - `02-RESEARCH.md:539` (the `/phone/` shell makes the origin self-identifying and weakens no-enumeration — "unavoidable for an installable PWA and must be **stated**"), `:542` (the global rate-limit bucket versus a polling phone, and the TryCloudflare 429 collision), `:556` (Discord must buffer before it can verify, inverting `handleCreate`'s deliberate authenticate-before-buffer property), `:1167-1180` (the ASVS V7 row and the two register entries).
    - `.planning/phases/02-the-daemon-and-the-protocol/02-04-SUMMARY.md`, `02-05-SUMMARY.md`, `02-09-SUMMARY.md` — what the tunnel, the door and the phone actually shipped, including which live checks ran and which were announced-skipped.
    - `src/main/tunnel.ts`, `src/main/webhook.ts`, `src/main/slack.ts`, `src/main/push.ts` as they stand — **read only**; every one belongs to another plan.
    - `src/main/hooks.ts:29` and `:418` — the two comments that already state the hook boundary's limits, so this file's wording stays consistent with source.
  </read_first>
  <action>
    Re-derive the network-surface table **by symbol grep, in this session** — the file's own stated rule.
    At least three rows moved: `PROXY_BRIDGE_SHIM` now lives in `hiveTemplates.ts` (plan 02-01), the
    tunnel is a cloudflared child process rather than a `tunnelmole()` call (plan 02-04), and
    `WebhookServer` now serves a static `/phone/` route and holds per-endpoint verifiers (plans 02-05,
    02-09). Find each symbol with `grep -rln` and write the file it is actually in.

    Then five edits to the prose:

    1. **`:63-70`.** Replace `tunnelmole` with what ships. State the vendor, how the binary is acquired
       the way plan 02-04 chose, and how its integrity is established — D-14: there is **no publisher
       checksum** in the cloudflared release, and this repo already shipped and fixed exactly that
       defect as #57, so say which of the two honest options was taken. Replace "Requests are verified
       before anything is dispatched: Slack request signing, and a per-endpoint shared secret for
       webhooks" with the real per-endpoint verifier set, Telegram and Discord included.
    2. **`:108-113` — delete the false limitation and replace it; do not merely soften it.** "Stopping a
       trigger does not close its tunnel … the package exposes no handle and no disposer" is now false;
       plan 02-04 made `stop()` kill a child process. Write the **narrower true caveat** in its place:
       what the close test actually proved (D-16's poll), what it did **not** — no multi-hour soak,
       ~30 s verified, Cloudflare's own "testing and development only" framing — and D-17's stable-URL
       limitation, because a reader deciding whether to expose this needs the churn story.
    3. **Add: the lockout is global and remotely triggerable.** Behind a tunnel every client presents
       the tunnel's IP (`webhook.ts` already says so in source), so per-IP limiting is meaningless and
       anyone who can reach the public URL can lock the operator out of their own floor. **Do not claim
       per-client lockout anywhere in the file** (D-23). Include RESEARCH's measured rate-limit
       interaction: the global bucket is shared with every webhook caller, and a TryCloudflare 429 is
       indistinguishable from the app's own.
    4. **Add: the `/phone/` shell is self-identifying.** Every other unknown path answers identically by
       design — the no-enumeration property. A public static shell answers 200 with no token, so the
       origin identifies itself as a Hello MarkX floor. Unavoidable for an installable PWA; state that
       the *auth* endpoints keep the uniform-failure discipline even though the shell does not.
    5. **Add: Discord inverts authenticate-before-buffer.** `handleCreate` deliberately authenticates
       before buffering so an unauthenticated peer cannot make the process allocate; Ed25519 over
       `timestamp + body` requires the raw bytes first. A real, deliberate weakening of one property for
       one endpoint — named, capped by `MAX_BODY_BYTES`, and endpoint-scoped.

    Also re-measure the hook-server gap sentence: "**Three** of the six lifecycle shims send no token at
    all today". PARITY-01a adds a shim. Count both numbers with a command and rewrite the sentence
    around what you counted, or record that it did not change.

    **Add the phone's honest status where a reader will use it**, per DAEMON-02's own fallback: the auth
    path is verified on localhost and, absent a physical Android device, **that is what is claimed** —
    never installation, never Web Push while asleep. `02-VALIDATION.md` says "never as completion";
    `SECURITY.md` is where a person makes the trust decision, so it belongs here as well as in the
    SUMMARY.

    Then **one clause** in `test/repo-claims.test.cjs`, both directions:
    - Negative: `SECURITY.md` no longer claims the tunnel cannot be closed (match the **claim** over
      joined text, not the word "tunnel"), and contains no "per-client"/"per-IP" lockout claim.
    - Positive (`>= 1` each): `SECURITY.md` names the global-lockout caveat; `src/main/tunnel.ts`
      contains the kill path plan 02-04 delivered (`hardKillTree`, or the symbol its SUMMARY records);
      and neither `src/main/webhook.ts` nor `src/main/slack.ts` retains an `openTunnel` body while both
      still reference the shared helper — asserted over comment-stripped, joined source, with the helper
      reference as the lower bound so deleting the tunnel outright fails.

    Drive it RED three ways: restore the old limitation paragraph; delete the kill path from
    `tunnel.ts`; delete the shared-helper reference from `webhook.ts`. Paste all three and the green run.
  </action>
  <acceptance_criteria>
    - **Every network-surface row re-derived by command.** Paste one `grep -rln '<symbol>' src` per row (`PROXY_BRIDGE_SHIM`, `startProxyBridge`, `WebhookServer`, `SlackWebhookServer`, `SlackReplyServer`, `TelemetryCollector`, `IntegrationBroker`, `HookServer`, and the tunnel's own symbol) beside the file each row now names. Any mismatch is a defect in this table.
    - **`tunnelmole` asserted as a claim, not as a word.** ``tr -d '\r' < SECURITY.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'opens a public tunnel \(tunnelmole\)|via tunnelmole|using tunnelmole|`?tunnelmole\(\)`? resolves' | wc -l`` returns **exactly `0`**, **and** `grep -ci 'cloudflared' SECURITY.md` returns at least `1`. Re-measured at HEAD 2026-08-23: **`2`** (`SECURITY.md:66` and `:108`) and **`0`** — both RED. The bare `grep -ci 'tunnelmole' SECURITY.md` (**`2`** at HEAD) is **not** the criterion: it has no verdict, because the word may legitimately survive as past tense, and "paste the sentence and state it is past tense" is an agent's assertion rather than a command's output. If the word does survive, paste the line and say so in the task notes — but the two greps above are what passes or fails.
      **The backticks in the fourth alternative are PLAIN, and the double-backtick code span is what lets them be.** In GNU grep a backslash-escaped backtick is the buffer-start anchor, not a literal backtick, so the escaped form of that alternative was dead and the criterion saw only `:66`. Correcting `:66` alone would then have taken it to `0` and PASSED with `:108`'s `` `tunnelmole()` resolves `` claim — the second of the two sites this criterion exists to cover — still in the file. Both forms run 2026-08-23 (GNU grep 3.0):
      ```
      $ grep -n 'tunnelmole' SECURITY.md
      66:  loopback and opens a public tunnel (tunnelmole) to that port. Requests are verified
      108:- **Stopping a trigger does not close its tunnel.** `tunnelmole()` resolves a URL and
      $ tr -d '\r' < SECURITY.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'opens a public tunnel \(tunnelmole\)|via tunnelmole|using tunnelmole|\`?tunnelmole\(\)\`? resolves' | wc -l
      1        # ESCAPED form: sees :66 only — blind to :108, the sentence D-13 falsified
      $ tr -d '\r' < SECURITY.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'opens a public tunnel \(tunnelmole\)|via tunnelmole|using tunnelmole|`?tunnelmole\(\)`? resolves' | wc -l
      2        # PLAIN form: sees BOTH sites, so fixing one cannot close the criterion
      ```
    - `tr -d '\r' < SECURITY.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'does not close its tunnel|no handle and no disposer' | wc -l` returns **exactly `0`** (measured at HEAD: **`1`**), **and** `grep -ciE 'soak|testing and development only|30 ?s' SECURITY.md` returns at least `1` (measured at HEAD: **`0`**). The false limitation is gone and the true ceiling is stated. Both directions, and both RED.
    - **The lockout claim, in a form that can both fail and pass.** Negative: `grep -ciE 'per-(client|IP) lockout|lockout is per-(client|IP)|locks out (only )?that (client|IP)' SECURITY.md` returns **exactly `0`** (measured at HEAD: **`0`** — a regression invariant, D-23). Positive: `tr -d '\r' < SECURITY.md | tr '\n' ' ' | tr -s ' ' | grep -oiE '(lockout|locked out).{0,160}(global|globally|every client|remotely)|(global|globally|remotely).{0,160}(lockout|locked out)' | wc -l` returns at least `1` (measured at HEAD: **`0`** — RED). **A blanket `grep -ciE 'per-client|per-IP' SECURITY.md` == 0 is NOT usable and was removed**: this task's own action requires the sentence "per-IP limiting is meaningless", so a criterion forbidding the string could never pass while the action is followed — the mirror defect, an assertion that fails a correct fix.
    - `grep -ci 'enumerat' SECURITY.md` returns at least `1` and the pasted sentence names the `/phone/` shell as the thing that weakens it.
    - `grep -ciE 'ed25519|discord' SECURITY.md` returns at least `1` and the pasted sentence states the buffer-before-verify inversion, its cap, and that it is endpoint-scoped.
    - **The lifecycle-shim numbers were counted, not carried.** Paste the command that counted the shims and the ones sending no token, and the rewritten sentence beside the old one.
    - `grep -ci 'localhost' SECURITY.md` returns at least `1` in the phone paragraph, and the pasted sentence claims a **verified auth path** — never installation, never push-while-asleep.
    - **All three RED runs pasted**, plus the green run after revert.
    - `node --test test/repo-claims.test.cjs test/webhook-endpoints.test.cjs test/tunnel.test.cjs` → `EXIT=0`, `# fail 0`, `# skipped 0` for each — or, for a platform/network-conditional case, an **announced** skip with its `console.error` line pasted, per `test/net-binding.test.cjs:286`. A silent skip is a test that does not exist.
    - `npm test` exit code `0`, `# fail 0`.
    - Containment, **per-commit and range-bound**, over `SECURITY.md test/repo-claims.test.cjs` using the same `$BASE`-bound guarded form as task 1 — the guard is what makes it fail rather than pass on an unbound variable — printing **nothing**. In particular no `src/main/*.ts` may appear — every source file this task reads belongs to another plan.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/webhook-endpoints.test.cjs</automated>
  </verify>
  <done>The network-surface table points at the files the symbols are actually in, the tunnel limitation says what is true now rather than what was true before wave 4, and the three exposures this phase newly created — a remotely triggerable global lockout, a self-identifying shell, and one endpoint that must buffer before it verifies — are stated where a person makes a trust decision.</done>
</task>

<task type="auto">
  <name>Task 5: `.planning/codebase/TESTING.md` and `CONCERNS.md` — the new test surface and the phase's stated limitations</name>
  <files>.planning/codebase/TESTING.md, .planning/codebase/CONCERNS.md</files>
  <read_first>
    - `.planning/codebase/TESTING.md` in full — especially `:29` ("55 test files, ~466 cases"), `:31-76` (the `load-ts.cjs` section, the electron-stub explanation, the `require.cache` injection pattern), `:147-155` (Coverage and the "423 tests, 421 pass, 0 fail, 2 skipped" figure), `:163` (the no-renderer-component-tests gap, carrying a **second copy** of `:29`'s number), `:165-171` (Common Patterns, including structural/regression pinning).
    - `.planning/codebase/CONCERNS.md` — `:1-15` (the Method paragraph, which is the standard this task is held to: the bar is "the issue's stated done", not "some code exists"), `:60-67` (Known Bugs), `:68-75` (Fragile Areas — the engine-bridge entry with its `src/main/hive.ts:679-820` pointer and its "kimi, copilot, and custom have no lifecycle hooks or cost accounting at all" sentence), `:76-90` (Test Coverage Gaps).
    - `.planning/phases/01-finish-the-floor/01-23-SUMMARY.md` — which of these numbers 01-23 already corrected and to what. Re-measure anyway (D-01); the point of reading it is to avoid re-reporting an old correction as new.
    - Every `02-*-SUMMARY.md` from 03 to 11 — the stated limitations each wave recorded. This task is where they stop being SUMMARY prose and become tracked concerns.
    - `02-VALIDATION.md` § "Manual-Only Verifications" and § "Cannot be verified without an operator-supplied device or account" — the canonical list, and the exact wording each row is allowed to be recorded with.
    - `02-CONTEXT.md` D-10 items 4 and 5 (Linux `setLoginItemSettings` is a no-op; macOS `setActivationPolicy('accessory')` is unverified, no Mac available), D-17, D-33, D-35, D-41.
  </read_first>
  <action>
    **`TESTING.md` — re-measure everything numeric, then describe what is new.**

    Re-measure with commands: the test-file count, the case count, and the full TAP counters. `:29`,
    `:153` and `:163` each carry a number and all three go stale together; `:163`'s "55" is a second
    copy of `:29`'s and is precisely the duplicate-claim pattern this phase exists to catch.

    Then add what the phase built:
    - The Wave-0 files — `boot-floor`, `agent-lifecycle`, `hive-router`, `tunnel`, `mcp-per-agent`,
      `capability-surface`, `qr-vendor`, `push-vapid`, `build-assets` — one line each, named by the
      **behaviour** each proves, not by the source file it covers.
    - **The `require.cache` injection is now load-bearing, not a curiosity.** `:43-76` describes it as
      the pattern `config-secrets` happens to use; `test/boot-floor.test.cjs` makes it the route by
      which the whole composition root is testable. Correct the framing, and correct the reason
      `index.ts` is not loaded directly so this file and `test/main-hardening.test.cjs` (task 2) tell
      the same story (D-02).
    - **The announced-skip convention** as a named house rule, with the `net-binding.test.cjs:286`
      exemplar — "never `return` silently" — because this phase adds live-network and platform-gated
      branches, and a silent skip is a test that does not exist.
    - **Tests deliberately outside `npm test`**: the tunnel live-check script and any other live probe,
      with the reason (no test in this repo makes an outbound network call) and where it lives.
    - **Structural pinning** (`:169`) now has a much larger consumer: name `test/repo-claims.test.cjs`
      and state D-40's rule — both directions, a positive lower bound, joined text — as the standard for
      any new clause. Add the comment-stripping exception the marker ledger needed, since it is a
      genuine counter-example to the file's own rule and the next author will hit it.

    **`CONCERNS.md` — the phase's stated limitations become tracked concerns.**

    Rewrite the Fragile Areas engine-bridge entry against source: its file pointer moved at 02-01, and
    "kimi … ha[s] no lifecycle hooks" is false after 02-07. Re-derive the whole per-engine sentence from
    `src/shared/agentProvider.ts` rather than editing the old one. Check whether "`CostSample` from the
    proxy tier reaches the ledger but not the breaker" survived PARITY-02 and say what you found either
    way.

    Then add the phase's limitations as entries, each with the four fields the file already uses (what
    is not done / files / risk / priority) and each in the wording `02-VALIDATION.md` allows:
    - **No stable public URL at $0** (D-17), with the churn consequence for an installed PWA (D-19).
    - **`setLoginItemSettings` is a no-op on Linux** (D-10.4) — stated in source, docs and UI; this is
      the docs half.
    - **`setActivationPolicy('accessory')` is UNVERIFIED — needs a macOS machine** (D-10.5).
    - **DAEMON-02 is a localhost-verified auth path, not a device-verified install** — recorded as such,
      never as completion.
    - **Five `LIVE-UNVERIFIED` bridges** (D-33, D-35), with the count, the file list, and a pointer to
      the clause in `test/repo-claims.test.cjs` that pins it.
    - **DAEMON-05's "always visible" is met in purpose, not literally at 800 px** (D-41) — the full
      statement, with task 3's README sentence as its user-facing pointer.

    Finally re-measure the Test Coverage Gaps entry: it counts renderer files touched by tests, and
    plans 02-06 and 02-10 both added renderer test surface.

    **Author no repo-fact clause over either file, and say why in the SUMMARY.** `.planning/codebase/**`
    is a planning artefact, not source; a grep-pin asserting a sentence exists is defeated by any
    paraphrase and satisfied by any copy. It would be the fifth vacuous gate, and this plan's whole
    subject is not authoring one (D-40).
  </action>
  <acceptance_criteria>
    - **Every number in `TESTING.md` re-measured, and the agreement asserted rather than eyeballed.** Run and paste:
      ``N=$(ls test/*.test.cjs | wc -l | tr -d ' '); S=$(grep -oE '[0-9]+ (test files|`test/\*\.test\.cjs` files)' .planning/codebase/TESTING.md | grep -oE '^[0-9]+' | sort -u | tr -d '\n'); test "$S" = "$N" && echo "AGREE=$N" || echo "STALE OR SPLIT: file says [$S], tree has $N — CRITERION FAILS"``
      Re-measured at HEAD 2026-08-23: `STALE OR SPLIT: file says [5560], tree has 64 — CRITERION FAILS`. **"The two copies carry the same number" is NOT the criterion** — the `sort -u` collapse is what makes one stale copy *and* two disagreeing copies both fail, and today they disagree: `:29` says `60` and `:163` says `55`, so `$S` is the concatenation `5560`.
      **Both backticks in that pattern are PLAIN, and the double-backtick code span is what lets them be.** In GNU grep a backslash-escaped backtick is the buffer-start anchor, not a literal backtick, so the escaped alternative that stood here was dead and only `:29` was ever read — an executor who fixed `:29` to `64` and left `:163` at `55` would have got `AGREE=64` and passed, which is precisely the second copy this task exists to catch. Both forms run 2026-08-23 (GNU grep 3.0):
      ```
      $ ls test/*.test.cjs | wc -l | tr -d ' '
      64
      $ grep -oE '[0-9]+ (test files|\`test/\*\.test\.cjs\` files)' .planning/codebase/TESTING.md | grep -oE '^[0-9]+' | sort -u | tr -d '\n'; echo
      60              # ESCAPED form: reads :29 only, blind to :163's stale "The 55 `test/*.test.cjs` files"
      $ grep -oE '[0-9]+ (test files|`test/\*\.test\.cjs` files)' .planning/codebase/TESTING.md | grep -oE '^[0-9]+' | sort -u | tr -d '\n'; echo
      5560            # PLAIN form: sees BOTH copies, so the split cannot be papered over
      ```
      Also paste the full-suite TAP counters beside `:153`'s `423 tests, 421 pass` figure, before and after.
    - `grep -c "it imports 'electron'" .planning/codebase/TESTING.md` returns **exactly `0`** in any sentence giving that as the reason `index.ts` is untestable, **and** `grep -ciE 'module-scope|boot-floor' .planning/codebase/TESTING.md` returns at least `1`. The task notes state in one sentence that this file and `test/main-hardening.test.cjs` now agree, and quote both.
    - **Every Wave-0 file is described — asserted per file, not by a total.** `for n in boot-floor agent-lifecycle hive-router tunnel mcp-per-agent capability-surface qr-vendor push-vapid build-assets; do printf '%-20s %s\n' "$n" "$(grep -c -- "$n" .planning/codebase/TESTING.md)"; done` — **every one of the nine returns at least `1`**. Measured at HEAD 2026-08-21: all nine return **`0`**. The single alternation `grep -cE 'boot-floor|…|mcp-per-agent' … >= 5` was removed: it counts *lines matching any alternative*, so five lines mentioning `tunnel` alone satisfy it with eight of the nine files undescribed. Paste all nine numbers and the lines.
    - `grep -ci 'announce' .planning/codebase/TESTING.md` returns at least `1` and the pasted text names `test/net-binding.test.cjs` as the exemplar.
    - `grep -ci 'repo-claims' .planning/codebase/TESTING.md` returns at least `1` and the pasted text states the both-directions + positive-lower-bound rule **and** the comment-stripping exception.
    - **`CONCERNS.md`'s engine-bridge entry re-derived, not edited.** Paste the command that read the presets and the resulting sentence. Three assertions, all RED at HEAD 2026-08-21:
      `grep -c 'hive.ts:679-820' .planning/codebase/CONCERNS.md` returns **exactly `0`** (HEAD: **`1`**);
      `tr -d '\r' < .planning/codebase/CONCERNS.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'no lifecycle hooks or cost accounting at all' | wc -l` returns **exactly `0`** (HEAD: **`1`**, the false sentence naming kimi);
      `tr -d '\r' < .planning/codebase/CONCERNS.md | tr '\n' ' ' | tr -s ' ' | grep -oE 'kimi.{0,200}LIVE-UNVERIFIED|LIVE-UNVERIFIED.{0,200}kimi' | wc -l` returns at least `1` (HEAD: **`0`**).
      **The bare `grep -c 'kimi' … >= 1` is NOT the assertion: it already returns `1` at HEAD** — and the line satisfying it is `CONCERNS.md:72`, the very sentence claiming kimi has no lifecycle hooks. A positive bound satisfied by the text it is supposed to force out asserts nothing.
    - **All six limitations present**, each greppable and each pasted with its surrounding entry, against `.planning/codebase/CONCERNS.md`, **each measured RED at HEAD 2026-08-21 so none is already-satisfied**: `grep -ciE 'stable (public )?URL'` (HEAD **`0`**), `grep -c 'setLoginItemSettings'` (HEAD **`0`**), `grep -ciE 'setActivationPolicy|accessory'` (HEAD **`0`**), `grep -ci 'localhost'` (HEAD **`0`**), `grep -c 'LIVE-UNVERIFIED'` (HEAD **`0`**), `grep -ciE '800 ?px'` (HEAD **`0`**) — each returns at least `1`. **`grep -ci 'Linux'` was replaced by `grep -c 'setLoginItemSettings'`: the former already returns `1` at HEAD** from `CONCERNS.md:81`'s unrelated note that the e2e workflow runs on Linux, so it can never fail and never proves the D-10.4 entry landed.
    - **The three-places-one-number check is a command.** Run it and paste its single line:
      `R=$(awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' README.md | grep -oE '[0-9]+' | head -1); C=$(awk '/LIVE-UNVERIFIED/{f=1} f{print; if(NR>1 && $0 ~ /^[[:space:]]*$/) exit}' .planning/codebase/CONCERNS.md | grep -oE '[0-9]+' | head -1); T=$(grep -oE 'LIVE_UNVERIFIED_TOTAL[^0-9]*[0-9]+' test/repo-claims.test.cjs | grep -oE '[0-9]+$' | head -1); test -n "$R" && test -n "$C" && test -n "$T" && test "$R" = "$T" && test "$C" = "$T" && echo "SAME=$T" || echo "MISMATCH README=$R CONCERNS=$C TEST=$T — CRITERION FAILS"`
      Measured at HEAD 2026-08-21: `MISMATCH README= CONCERNS= TEST= — CRITERION FAILS`. **Three documents carrying the same number is the test; three carrying three numbers is this phase's subject matter** — and three carrying *no* number must fail rather than compare equal, which is why each variable is tested non-empty before the comparison.
    - **No repo-fact clause was authored over `.planning/codebase/**` — asserted against `$BASE`, not narrated.** `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null || { echo "UNBOUND BASE — CRITERION FAILS"; false; }; A=$(git show "$BASE":test/repo-claims.test.cjs | grep -c '\.planning/'); B=$(grep -c '\.planning/' test/repo-claims.test.cjs); test "$A" = "$B" && echo "UNCHANGED=$A" || echo "GREW $A -> $B — CRITERION FAILS"`. Equality, not an upper bound: **the count must not move in either direction**. Measured at HEAD 2026-08-21 the file already carries **`2`** such references (`test/repo-claims.test.cjs:8`, a header comment, and `:237`, an assertion message naming `ARCHITECTURE.md`), so a criterion demanding `0` would be unsatisfiable without deleting an existing clause. The task notes still state in prose *why* no clause was authored.
    - `npm test` exit code `0`, `# fail 0`, **and `# tests` and `# pass` exactly equal to B-suite's** — these are documentation edits and must move nothing, so the assertion is equality. `# fail 0` alone is satisfied by deleting a test file.
    - Containment, **per-commit and range-bound**, over `.planning/codebase/TESTING.md .planning/codebase/CONCERNS.md` using the same `$BASE`-bound guarded form as task 1 — the guard is what makes it fail rather than pass on an unbound variable — printing **nothing**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>Both planning documents describe the tree as it is at the end of wave 8, the phase's six limitations are tracked concerns rather than SUMMARY prose, the same marker count appears in all three places that state it, and no decorative gate was authored over a planning file.</done>
</task>

<task type="auto">
  <name>Task 6: `.planning/ROADMAP.md` — the six claims the phase disproved in the document it is graded against</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - `.planning/ROADMAP.md` § "Phase 2: The Daemon and the Protocol" **in full** — the ⚠️ risk paragraph, the three "Why the god-file extractions live here" bullets, and Success Criteria 1 through 5. Six claims in this one section are false; four of them are Success Criteria.
    - **`.planning/ROADMAP.md:83-91` — the file's OWN correction convention, used twice already**: `— **corrected 2026-08-20 (D-37): …**` on FLOOR-02's criterion and `— **restated 2026-08-20 (D-02): …**` on FLOOR-03's. Both leave the original wording standing and append the correction inline with its date and its D-id, so a reader sees what the criterion said, what it says now, and which decision moved it. **Reuse that shape exactly. Do not invent a marker, do not add a footnote section, and do not silently overwrite a sentence** — an unmarked correction to an acceptance criterion is indistinguishable from moving the goalposts.
    - `02-CONTEXT.md` D-02 in full (both consequences), D-05's "**CORRECTED by research (2026-08-21): only TWO of the four tests criterion 1 names are actually blocked by the split**" paragraph, D-34, D-38, D-41.
    - `02-01-SUMMARY.md` (STRUCT-02) and `02-02-SUMMARY.md` (STRUCT-01) — the **pre-split baselines** each recorded for `hive.ts` and `index.ts`. Criterion 1's figures describe the files *before* the extraction; today's post-split `wc -l` is a different quantity and writing it where a pre-split number belongs would replace one wrong number with another.
    - `02-03-SUMMARY.md` and `test/boot-floor.test.cjs` — what actually flips criterion 1's gate (D-05: a green boot test, never a line count).
    - `02-08-SUMMARY.md` (GSD-06) — where the human's answer actually lands, in that plan's own words.
    - `02-10-SUMMARY.md` — the 800 px degradation step, in that plan's own words.
    - This plan's own tasks 2, 3 and 5. **The same D-02 sentence and the same D-34 sentence are corrected in four places by this one plan** (`test/main-hardening.test.cjs`, `TESTING.md`, `README.md`, here). They must tell the same story in the same words; four corrections that disagree is the failure mode of task 2 reproduced at a larger scale.
  </read_first>
  <action>
    Re-derive every site by content (D-01). The `<interfaces>` line numbers are pointers and the file is
    edited by other plans between now and execution.

    Six corrections, each marked in the file's own convention with **today's date and the D-id**:

    1. **The god-file figures.** "5,620 lines" appears three times and "~157 IPC handlers" and "3,562
       lines" once each. Measured on `main` 2026-08-21: `wc -l src/main/index.ts` → **5,812**,
       `wc -l src/main/hive.ts` → **4,121**, `grep -c 'ipcMain.handle(' src/main/index.ts` → **153**.
       Note that the roadmap disagreed with this phase's own context before a line of code moved — D-02
       already calls `hive.ts` 4,121 lines. Re-run all three commands yourself and paste them. Then
       write the correction as **two quantities, not one**: the pre-split size the criterion is actually
       talking about (from 02-01's and 02-02's recorded baselines) and what the files measure now that
       the extraction has landed. Replacing a stale pre-split figure with a post-split figure is a
       second false claim wearing the first one's clothes.
    2. **"`index.ts` imports `electron`, so it cannot be loaded under `node --test`" (D-02).** This is
       the origin of the sentence task 2 deletes from `test/main-hardening.test.cjs` and task 5 deletes
       from `TESTING.md`. Correct it here **in the same words those two tasks used**: the stub in
       `test/load-ts.cjs` has got past the `electron` import since #55; the blocker was module-scope
       side effects (`app.on` at import time, a real `initFileLogging()` write stream); and the
       consequence D-02 draws is that the extraction's success test is "no module-scope side effects
       plus an injectable boot function", never "fewer lines". Keep the bullet's argument — it is still
       the right argument — and correct only its mechanism.
    3. **Success Criterion 1 — "tests … that could not be written before the split" (D-05).** False for
       two of the four. Paste `grep -c 'flushCommit' test/hive-durability.test.cjs` (**6** at plan time)
       and `test/engine-parity.test.cjs` (**1**), and `grep -n 'routeOnce' src/main/hive.ts` showing it
       is a public method. The corrected criterion still requires all four tests; it stops claiming all
       four were impossible, and names **shutdown and agent lifecycle** as the two the split genuinely
       unblocked. Add D-05's other half while you are in the sentence: the gate is flipped by a green
       `test/boot-floor.test.cjs`, not by a line count.
    4. **Success Criterion 3 — "the answer arrives in **that worker's** terminal" (D-38).** It arrives
       in that worker's **inbox**, enqueued as hive mail and delivered by main's drain when that agent
       is idle, exactly like every other automatic message. **ADR-0001 is intact and nothing new types
       into a PTY.** Correct the word. As written, this criterion is satisfiable only by breaking the
       one-gate decision that task 2 of this same plan spends its entire length defending across three
       files — the sharpest available demonstration of why an uncorrected criterion is not harmless.
    5. **Success Criterion 4 — "the live public URL is visible in the UI whenever the tunnel is up"
       (D-41).** Met in purpose, not literally: presence is the signal and the tunnel cannot be up
       without the operator seeing the chip, but at 800 px degradation step 2 renders `PUBLIC` alone
       with the untruncated URL one click away in the panel the chip opens. Write it as met-in-purpose-
       not-literally, in the same words as task 3's README sentence and `CONCERNS.md`'s full statement
       from task 5. **Do not widen the criterion's wording to something the app meets** — that is the
       quiet-widening D-41 forbids, and doing it in the acceptance document is the worst place to do it.
    6. **Success Criterion 5 — "All eleven report cost to the ledger and to the breaker" (D-34).**
       Unachievable for copilot and custom by construction. Same correction as task 3's README paragraph
       and task 7's requirement restatement, in the same words: what was delivered, then that the
       criterion as written is unachievable, in that order. Cross-reference PARITY-02's restated text so
       the roadmap and `REQUIREMENTS.md` cannot drift apart again.

    **Nothing else in this file is touched.** Not Phase 1's section, not Phases 3-6, not the plan
    checklist at `:140-156`, and **no checkbox anywhere** — including Phase 2's own. This plan corrects
    the text the phase will be graded against; flipping the phase to done is the rollup's job, and a
    plan that both writes and ticks its own criteria has certified itself.

    **Author no repo-fact clause over this file** — see the `.planning/**` rule in `<interfaces>`. The
    criteria below are the check, and each one is joined-text and both-directions (D-40).
  </action>
  <acceptance_criteria>
    - **The three figures re-measured with pasted output**: `wc -l src/main/index.ts`, `wc -l src/main/hive.ts`, `grep -c 'ipcMain.handle(' src/main/index.ts`, beside 02-01's and 02-02's recorded pre-split baselines. State which number in the corrected text is the pre-split quantity and which is today's.
    - **The stale figures, asserted in a form the file's own correction convention can satisfy.** This file's convention *leaves the original wording standing* and appends the correction inline (`:83-91`), so a blanket `grep -c '5,620\|3,562\|~157 IPC' == 0` is a gate that **cannot pass while this task's action is followed** — and its "if a stale figure legitimately survives, paste the line and state which case applies" escape hatch turns any value into a pass. Both are removed. The runnable form asserts the figures survive **only inside a marked correction**, and the unit of exemption is **the list item / labelled paragraph the claim sits in, inside the Phase 2 section only** — which is exactly where `:83-91`'s two existing markers sit:
      `UNMARKED() { awk -v pat="$1" -v mk='[*][*](corrected|restated) [0-9]{4}-[0-9]{2}-[0-9]{2} [(]D-[0-9]+[)]' 'function flush(){ if(u!=""){gsub(/[ \t]+/," ",u); if(u~pat && u!~mk) n++} u="" } /^### Phase 2:/{inp=1} inp && /^### Phase 3:/{inp=0} !inp{next} /^[[:space:]]*$/ || /^[[:space:]]*[0-9]+\. / || /^- / || /^\*\*/ {flush()} {u=u " " $0} END{flush(); print n+0}' .planning/ROADMAP.md; }`
      **Two properties of that definition are load-bearing, and replace an earlier `OUTSIDE()` that had neither.** (a) It is **bounded to the Phase 2 section** (`^### Phase 2:` … `^### Phase 3:`). The earlier whole-file form counted `2` for D-38 because a *second* `worker's terminal` paragraph lives in **Phase 6** (`.planning/ROADMAP.md:493`, under `### Phase 6` at `:445`) — and this task's own action forbids touching Phases 3-6, as does its scope criterion below. `== 0` was therefore unreachable without violating a sibling criterion. (b) The exemption unit is a **list item**, not an `RS=""` paragraph. `**Depends on**` through `**UI hint**` is a single blank-line-free block, so under the old form one marker on Success Criterion 5 exempted Criteria 1, 3 and 4 as well, and one marker on the D-02 bullet exempted bullet 3's `5,620-line refactor`. Both simulated on a scratch copy 2026-08-23, repo untouched:
      ```
      $ cp .planning/ROADMAP.md R.md
      $ # BEFORE — old whole-file RS="" form vs new list-item Phase-2 form
      OLD: D-05=1 D-38=2 D-34=1 figs=3 D-02=1
      NEW: D-05=2 D-38=2 D-34=1 figs=3 D-02=1
      $ # add ONE marker to Success Criterion 5 and ONE to the D-02 bullet; touch nothing else
      $ sed -i 's/marked — never silently unmarked\./… — **corrected 2026-08-23 (D-34): …**/' R.md
      $ sed -i 's/a test — which is the whole point/a test — **corrected 2026-08-23 (D-02): …** which is the whole point/' R.md
      $ # AFTER
      OLD: D-05=0 D-38=1 D-34=0 figs=2 D-02=0     <- D-05 and figs went GREEN with Criterion 1 and bullet 3 uncorrected
      NEW: D-05=2 D-38=2 D-34=0 figs=3 D-02=0     <- only the two claims actually marked went green
      $ grep -c 'could not be' R.md
      2
      $ grep -c '5,620' R.md
      3                                            <- both claims still verbatim in the file
      ```
      And the D-38 gate is **reachable** under the new form using Phase-2 edits alone: marking Success Criterion 3 and the closing `**Note:**` sentence on a scratch copy took `UNMARKED 'that worker.s[*]* terminal'` to `0` while `.planning/ROADMAP.md:493`'s Phase-6 paragraph was never touched.
      `UNMARKED '5,620|3,562|~157 IPC'` returns **exactly `0`**. Re-measured at HEAD 2026-08-23: **`3`** — RED (the ⚠️ risk paragraph, the STRUCT-01/STRUCT-02 sentence, and bullet 3's `5,620-line refactor`; each needs its OWN marker).
      **and** `grep -cE '5,812|4,121' .planning/ROADMAP.md` returns at least `1` (**`0`** today), **and** the handler count is asserted rather than described: `tr -d '\r' < .planning/ROADMAP.md | tr '\n' ' ' | tr -s ' ' | grep -oiE '153[^0-9]{0,20}(IPC )?handler' | wc -l` returns at least `1` (**`0`** today). Three directions, all RED (D-40).
    - **D-02, marker-scoped** (`UNMARKED` as defined above — the convention leaves the original sentence standing, so a file-wide `== 0` is the mirror defect): `UNMARKED 'imports .electron., so it cannot be loaded'` returns **exactly `0`**. Re-measured at HEAD 2026-08-23: **`1`** — RED. **and** `grep -ciE 'module-scope|boot-floor' .planning/ROADMAP.md` returns at least `1` (**`0`** today — measured, so it only passes once the correction lands). Measured 2026-08-21: joined+squeezed → `1`, **line-oriented → `0`**. The claim wraps mid-sentence across `:181-182`; the line-oriented form finds nothing today and reads as PASS against the uncorrected file.
    - **D-05, marker-scoped**: `UNMARKED 'could not be written before the split'` returns **exactly `0`**. Re-measured at HEAD 2026-08-23: **`2`** — RED, and **`2` is the honest count, not `1`**: Success Criterion 1 carries the claim, and the section's closing `**Note:**` sentence quotes it again in its list of six. Both are inside Phase 2 and both must be marked; the old whole-file `RS=""` form scored this `1` because the `**Note:**` sentence shares no blank line with its neighbours and was folded into the same record as an already-marked one. **and** `tr -d '\r' < .planning/ROADMAP.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'flushCommit|routeOnce|shutdown and agent lifecycle' | wc -l` returns at least `1` (**`0`** today). Paste the corrected criterion and the two `grep -c 'flushCommit'` counts it rests on. **The `tr -s ' '` is load-bearing here and was found the hard way**: the claim wraps as `could not be` + five spaces of indentation + `written before the split`, so the un-squeezed joiner returns `0` against the uncorrected file — a vacuous criterion that passes before any work is done.
    - **D-38, marker-scoped**: `UNMARKED 'that worker.s[*]* terminal'` returns **exactly `0`** — note `[*][*]` / `[*]*` rather than `\*\*`, because POSIX awk warns on and discards that backslash escape. Re-measured at HEAD 2026-08-23: **`2`** — RED, and **both sites are inside Phase 2**: Success Criterion 3 and the closing `**Note:**` sentence. `.planning/ROADMAP.md:493`'s Phase-6 `worker's terminal` paragraph is **outside the bound and must stay untouched** — that is why the extractor is section-bounded, and a whole-file `grep -c` for that phrase holding steady at its pre-task value across this task is the proof it was not edited. **and** `tr -d '\r' < .planning/ROADMAP.md | tr '\n' ' ' | tr -s ' ' | grep -oiE "worker.s\** inbox" | wc -l` returns at least `1` (**`0`** today). The pasted sentence must also state that ADR-0001 is intact — the same claim task 2 asserts across three files.
    - **D-41**: `grep -ciE '800 ?px' .planning/ROADMAP.md` returns at least `1` (**`0`** today) and the pasted criterion says met-in-purpose-not-literally. Quote it beside task 3's README sentence and task 5's `CONCERNS.md` entry and state that the three agree. **The criterion's own wording must not have been widened** — paste it before and after.
    - **D-34, marker-scoped**: `UNMARKED 'All eleven report cost|all eleven[^.]*(ledger|breaker)'` returns **exactly `0`**. Re-measured at HEAD 2026-08-23: **`1`** — RED. **and** `grep -ci 'copilot' .planning/ROADMAP.md` returns at least `1` (**`0`** today) with the pasted text naming copilot and custom.
    - **Every correction is marked and carries a D-id — counted by occurrence, not by line**: `grep -oE '\*\*(corrected|restated) [0-9]{4}-[0-9]{2}-[0-9]{2} \(D-[0-9]+\)' .planning/ROADMAP.md | wc -l` returns **exactly `8`**. Measured at HEAD 2026-08-21: **`2`** (`ROADMAP.md:83`, `:89`) — RED, and the delta is therefore exactly `6`. **Equality, not a lower bound**, and `grep -o` rather than `grep -c`: `grep -c` counts *lines*, so two markers landing on one wrapped line would score `1` and a seventh unmarked correction would still pass an "at least 8". Each new marker must sit in the file's existing convention rather than a new one. Paste the count before and after.
    - **Scope, mechanically — no checkbox moved, range-bound against `$BASE`**: `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && test -n "$(git diff --name-only "$BASE"..HEAD -- .planning/ROADMAP.md)" || { echo "UNBOUND BASE OR NO COMMITTED CHANGE — CRITERION FAILS"; false; }; git diff -U0 "$BASE"..HEAD -- .planning/ROADMAP.md | grep -cE '^[+-].*- \[[ x]\]'` returns **exactly `0`**. **The working-tree form `git diff -U0 -- .planning/ROADMAP.md` is NOT usable**: measured on the committed tree at HEAD 2026-08-21 it produces no output at all, so the grep returns `0` and the criterion passes for a task that flipped every checkbox in the file and committed it. The range bind and the non-empty-diff guard are the criterion.
    - **Scope, mechanically — Phase 2 only, asserted rather than compared by eye**: with `$BASE` bound and guarded as above,
      `S=$(grep -n '^### Phase 2:' .planning/ROADMAP.md | cut -d: -f1); E=$(awk -v s="$S" 'NR>s && /^### /{print NR; exit}' .planning/ROADMAP.md); git diff -U0 "$BASE"..HEAD -- .planning/ROADMAP.md | grep -oE '^@@ -[0-9]+(,[0-9]+)? \+[0-9]+' | sed 's/.*+//' | awk -v s="$S" -v e="$E" '$1<s || $1>=e {n++} END{print n+0}'`
      returns **exactly `0`** — every hunk's post-image start line falls inside the Phase 2 section. Measured at HEAD 2026-08-21 the section is `S=159`, `E=247` (`### Phase 2: The Daemon and the Protocol` → `### Phase 3: Scale and Observability`). Paste `S`, `E`, every hunk header, and the count. Nothing in Phase 1's section, Phases 3-6, or the plan checklist is touched.
    - `npm test` exit code `0`, `# fail 0`, **and `# tests` and `# pass` exactly equal to B-suite's** — this is a planning-document edit and must move nothing, so the assertion is equality; `# fail 0` alone is satisfied by deleting a test file.
    - Containment, **per-commit and range-bound**, over `.planning/ROADMAP.md` using the same `$BASE`-bound form as task 1, printing **nothing**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>The four Success Criteria this phase disproved say what the phase measured, each marked in the roadmap's own correction convention with the D-id that moved it, the god-file figures distinguish the pre-split quantity from today's, and the phase can no longer close green against its own acceptance surface.</done>
</task>

<task type="auto">
  <name>Task 7: `.planning/REQUIREMENTS.md` — PARITY-02 restated in the requirement itself, not only downstream of it</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/REQUIREMENTS.md` § "PARITY — Every engine a first-class citizen" in full — PARITY-01a, **PARITY-01b's italic `*(Split deliberately. …)*` note**, PARITY-02 and PARITY-03. PARITY-01b's note is this file's **own** convention for recording why a requirement's wording changed and what a skeptic could no longer do afterwards. Reuse that shape; it is the sibling row, two lines up.
    - The status table at the tail — the single `| PARITY-02 | Phase 2 | … |` row.
    - `02-CONTEXT.md` D-34 in full, especially the buildable reading it names verbatim: *"every engine that can be pointed at a base URL gets cost through the proxy bridge that already exists; the rest are declared through the channel D-30/D-31 makes real"*, and the mechanism it points at (`hive.ts` `bridge: {kind:'proxy', api, baseUrlEnv}` → `startProxyBridge`). D-34's last sentence is binding: *"The plan must state which it actually delivers and mark the rest, never silently."*
    - `02-07-SUMMARY.md` — which engines PARITY-02 **actually** pointed at the proxy, and which it did not. The restated requirement is written against what shipped, not against D-34's plan-time candidate list (grok / kimi / opencode / antigravity were candidates, not deliveries).
    - `src/shared/agentProvider.ts` — the eleven presets after wave 4, and copilot's and custom's own `costTracking: 'none'` preset comments, which are the reason the restatement gives.
    - This plan's task 3 (the README paragraph) and task 6 (Success Criterion 5). **All three restate the same requirement and must say the same thing in the same words.** Write this one last and diff it against the other two.
    - `.planning/phases/01-finish-the-floor/01-23-PLAN.md:279-284, :319, :560-564` — the other declarer of this file, and the proof its rows are disjoint from this one.
  </read_first>
  <action>
    **First, re-answer ownership note 4 with a command, before editing.** Run
    `grep -c 'PARITY' .planning/phases/01-finish-the-floor/01-23-PLAN.md` (expected `0`) and
    `git log --oneline main -- .planning/REQUIREMENTS.md`. 01-23 owns FLOOR-*, GATE-01, RECORD-03/04 and
    VERDICT-02/03 in this file. **If it has grown a PARITY edit, STOP and report** — that is the
    two-sweeps-one-file collision D-06 exists to prevent, and this plan yields.

    **Restate PARITY-02, keeping the requirement id.** The id is load-bearing: it is referenced from
    `02-07-PLAN.md`, from this file's status table, and from the roadmap's criterion 5. A renamed or
    deleted requirement breaks the trace and hides the correction; a restated one carries it.

    The restatement says, in this order and no other:
    1. **What is buildable** — every engine that can be pointed at a base URL reports cost through the
       proxy bridge that already exists, and the rest **declare** the absence through the capability
       channel D-30/D-31 makes real, so the operator meets the fact in the UI rather than in a doc.
    2. **Which engines that actually is**, from `02-07-SUMMARY.md` and re-derived from
       `src/shared/agentProvider.ts` in this session — never from D-34's plan-time candidate list.
    3. **That the original wording is unachievable, and why**, naming **copilot** and **custom** with
       their own preset comments as the reason ("spend sits on the user's Copilot plan; nothing
       per-agent reaches us"; "unknown binary — nothing to read"). No amount of work makes an engine
       emit a number it does not have, and inventing one locally would be fabrication.

    Mark it in this file's own convention — the italic parenthetical PARITY-01b already uses — carrying
    today's date, **D-34**, and the one sentence that makes the restatement honest rather than
    convenient: *what a skeptic can no longer do is confuse "we delivered cost for the engines that can
    report it" with "all eleven report cost"*. **Never let the restated text read as though the original
    requirement had always said this.**

    **Leave the checkbox `[ ]` and leave the status row's value as it stands.** Correct the row only if
    its *text* is wrong; its **state** belongs to the phase rollup. This plan's job is to make sure that
    when the rollup does flip it, it flips against a sentence that is true. Ticking it here would be
    this plan certifying the phase it is the last plan of.

    **Touch no other requirement and no other status row.** Not PARITY-01a, not PARITY-01b, not
    PARITY-03 — PARITY-03 is this plan's own requirement and its text is already accurate (D-35: marked
    or verified, never silently unmarked); task 1 satisfies it and does not restate it.

    **Author no repo-fact clause over this file** — the `.planning/**` rule in `<interfaces>`.
  </action>
  <acceptance_criteria>
    - **Ownership note 4 answered with pasted output** of `grep -c 'PARITY' .planning/phases/01-finish-the-floor/01-23-PLAN.md` (expected `0`) and the `git log`. Non-zero is a STOP recorded in the task notes, and this task does not run.
    - **The negative, joined+squeezed**: `tr -d '\r' < .planning/REQUIREMENTS.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'All eleven engines report cost' | wc -l` returns **exactly `0`** as the requirement's own statement (**`1`** today — measured, so the criterion is not vacuous). PARITY-02 fits on one line today but every other requirement in this file wraps with indented continuations, and the restatement will too — use the joined+squeezed form so the criterion survives the reflow it is about to cause. If the phrase survives *inside* the marked note quoting what PARITY-02 used to say, paste that line and state that it is quoted history, not a live requirement.
    - **The positive lower bound, joined+squeezed** (D-40 — deleting the restatement must fail this): `tr -d '\r' < .planning/REQUIREMENTS.md | tr '\n' ' ' | tr -s ' ' | grep -oiE 'base URL|proxy bridge' | wc -l` returns at least `1`, **and** `grep -ci 'copilot' .planning/REQUIREMENTS.md` returns at least `1`, **and** `grep -c 'D-34' .planning/REQUIREMENTS.md` returns at least `1`. All three return **`0`** today — measured, so all three only pass once the restatement lands. Paste all three with the surrounding requirement.
    - **The id survived**: `grep -c 'PARITY-02' .planning/REQUIREMENTS.md` returns at least `2` — the requirement and its status row. Paste both lines.
    - **The state was not flipped**: `grep -n 'PARITY-02' .planning/REQUIREMENTS.md` shows the checkbox still `[ ]` and the status row's value unchanged from the pre-task value. Paste before and after. A flipped state here is this plan grading its own phase.
    - **Nothing else moved — range-bound, and asserted rather than read.** With `$BASE` bound and guarded exactly as in task 1:
      `BASE=<B-sha>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && test -n "$(git diff --name-only "$BASE"..HEAD -- .planning/REQUIREMENTS.md)" || { echo "UNBOUND BASE OR NO COMMITTED CHANGE — CRITERION FAILS"; false; }; git diff -U0 "$BASE"..HEAD -- .planning/REQUIREMENTS.md | grep -E '^[-+]' | grep -vE '^(\+\+\+ |--- )' | grep -vc 'PARITY-02'`
      returns **exactly `0`** — every added or removed content line names `PARITY-02`. **and** the same pipeline ending `| grep -c 'PARITY-02'` returns at least `2` (the requirement bullet's `-`/`+` pair), so an empty diff fails instead of passing. Paste the surviving lines; there should be two, and they should be the before and after of the one bullet.
      **The earlier form of this criterion — `git diff -U0 -- .planning/REQUIREMENTS.md | grep -E '^[+-][^+-]'` — was defective twice over, and both defects are reproduced below rather than asserted.** (a) It was a **working-tree** diff, empty the moment the task commits — the same form task 6 identifies and rejects one task earlier. (b) Its filter `^[+-][^+-]` requires the second character not to be `+` or `-`, and every requirement in this file is the markdown bullet `- [ ] **PARITY-02**: …`, so its diff lines are `+- [x] …` / `-- [ ] …` and were **all discarded** — the filter hid precisely the requirement text and checkbox changes it existed to display. Measured 2026-08-23 in a scratch repo that rewrote PARITY-02 **and** FLOOR-01, ticked both, flipped both status rows, and committed:
      ```
      $ git diff -U0 -- .planning/REQUIREMENTS.md | grep -E '^[+-][^+-]'
      $ echo "exit=$?"
      exit=1                      # OLD: empty — the out-of-scope rewrite PASSES

      $ git diff -U0 "$BASE"..HEAD -- .planning/REQUIREMENTS.md | grep -E '^[-+]' | grep -vE '^(\+\+\+ |--- )' | grep -vc 'PARITY-02'
      4                           # NEW: FAILS, and names how many lines strayed
      ```
      and against an in-scope-only edit (PARITY-02's bullet restated, nothing else, committed):
      ```
      $ git diff -U0 "$BASE"..HEAD -- .planning/REQUIREMENTS.md | grep -E '^[-+]' | grep -vE '^(\+\+\+ |--- )' | grep -vc 'PARITY-02'
      0                           # NEW: negative half green
      $ git diff -U0 "$BASE"..HEAD -- .planning/REQUIREMENTS.md | grep -E '^[-+]' | grep -vE '^(\+\+\+ |--- )' | grep -c 'PARITY-02'
      2                           # NEW: positive half green — the bullet's before and after
      ```
      The `grep -vE '^(\+\+\+ |--- )'` drops git's two file headers and **not** a `-- [ ] **…**` bullet line, which carries two dashes and a space, never three.
    - **The three restatements agree**: paste PARITY-02's restated text, task 3's README paragraph and task 6's Success Criterion 5 side by side, and state that all three name copilot and custom as the two engines with no cost path and give the same mechanism for the rest. Three documents restating one requirement three different ways is this phase's subject matter.
    - `npm test` exit code `0`, `# fail 0`.
    - Containment, **per-commit and range-bound**, over `.planning/REQUIREMENTS.md` using the same `$BASE`-bound form as task 1, printing **nothing**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/engine-parity.test.cjs</automated>
  </verify>
  <done>PARITY-02 keeps its id and states what is actually buildable, the two engines that cannot report cost are named in the requirement itself with their own reasons, the restatement is marked with D-34 in the file's own convention, and the checkbox the rollup will flip now sits above a sentence the phase's acceptance test can support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `SECURITY.md` → the person deciding whether to expose this floor | This phase puts an authenticated door in front of agent CLIs running with bypassed permissions, on the public internet. The reader's threat model is built from this file. A documentation set that understates that exposure is itself a security defect — it is the control by which every other control is discovered. |
| the `LIVE-UNVERIFIED` ledger → the operator's belief about which bridges work | The markers are the only record that four (now five) engine bridges have never run against a real account. Nothing else in the app carries that fact — there is no `live-unverified` field in the provider table (D-35). If the count can drift, the claim is unbacked. |
| a repo-fact clause → the phase's own acceptance test | A clause that cannot fail certifies nothing while reading as coverage. This project has already produced three; the marker clause is one `readStripped` call away from being the fourth. |
| `README.md` / `HIVE.md` / `docs/**` → a contributor's model of where PTY writes come from | ADR-0001 defines the single write gate. Three copies of its claim exist. One amended copy beside two stale ones is worse than three stale ones, because the disagreement makes all three untrustworthy. |
| `.planning/ROADMAP.md` + `.planning/REQUIREMENTS.md` → whether this phase is done | These two documents are what the phase is **graded against**. Every other correction in this plan is downstream of them. Four of Phase 2's five Success Criteria and PARITY-02's requirement text assert things the phase's own measurements falsified; if they stand, the phase closes green against false criteria and the rollup's ✅ certifies nothing. Correcting source and docs while leaving the acceptance surface uncorrected is the same defect as an amended ADR beside two unamended copies — one level up, where it decides the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P02-12-01 | Repudiation | a bridge unmarked with no live session behind it, or a new unverified bridge added silently | mitigate | The ledger pins an exact total **and** a per-engine `>= 1` **and** the file set, driven RED four ways. Unmarking or adding requires editing a committed number in the same diff, which puts "verified against which account?" in front of a reviewer mechanically (D-35). |
| T-P02-12-02 | Tampering | the marker clause routed through `readStripped`, counting zero and passing on empty text | mitigate | A dedicated `readRaw` helper with a comment stating why; an acceptance criterion asserting the clause body contains no `readStripped(`; and a RED run proving the stripped variant collapses to zero (D-40). Plan 02-07's wave-4 pin is re-checked for the same defect. |
| T-P02-12-03 | Information disclosure | `SECURITY.md` omitting the exposures this phase created — a globally DoS-able lockout, a self-identifying `/phone/` origin, one endpoint that buffers before it authenticates | mitigate | All three written as named limitations, each greppable and each pasted in the acceptance criteria; the no-enumeration property's weakening is stated rather than silently accepted (RESEARCH §4.4, § V7). |
| T-P02-12-04 | Repudiation | `SECURITY.md` keeping a limitation that is no longer true, in the app's favour | mitigate | The tunnel-close entry is **replaced**, not softened, and the replacement is pinned to the kill path plan 02-04 delivered — negative half asserting the old claim is gone, positive half failing if the tunnel code is deleted. A file that over-states its limitations trains readers to discount all of them. |
| T-P02-12-05 | Spoofing | docs pointing at symbols in files the wave-1 split moved them out of | mitigate | Every network-surface row is re-derived by `grep -rln '<symbol>' src` in the executing session; `SECURITY.md`'s own header states that stale pointers are how it went wrong the first time. |
| T-P02-12-06 | Repudiation | PARITY-02 quietly narrowed, or DAEMON-05's clause ticked verbatim at a width where it is not met | mitigate | "All eleven … cost" is asserted absent from `README.md` while copilot and custom are asserted present with their reasons; the negative half is additionally pinned to `costTracking: 'none'` in source, so a future cost path forces the doc to change. The 800 px caveat is written as met-in-purpose-not-literally, in `README.md` and in full in `CONCERNS.md` (D-34, D-41). |
| T-P02-12-07 | Tampering | one of the three ADR-0001 copies corrected alone | mitigate | One clause asserts across all three files, with the ADR's continued existence as a positive lower bound so deleting it does not satisfy the negative. The ADR belongs to plan 02-03 and is asserted, never edited, here. |
| T-P02-12-08 | Denial of service | this plan running before wave 8, pinning numbers a later diff moves | mitigate | Precondition B enumerates SUMMARYs 02-03 … 02-11 and STOPs on any absence; `depends_on` carries all ten plans; 01-23's three-file collision is answered by Precondition A before any edit. |
| T-P02-12-09 | Repudiation | the phase closing green against `ROADMAP.md`'s Success Criteria 1, 3, 4 and 5 and `REQUIREMENTS.md`'s PARITY-02, all of which assert what the phase disproved | mitigate | Tasks 6 and 7 correct both documents in their own existing correction conventions, each marked with its date and D-id so a reader can tell a correction from a moved goalpost. Every criterion is joined-text (all four claims wrap mid-sentence) with a positive lower bound, and the marker count is asserted to grow by exactly six. Refusing to *inherit* a false claim — which every other plan in this phase did correctly — does not correct it, and this is the last plan that can. |
| T-P02-12-10 | Tampering | a criterion quietly widened, or a checkbox flipped, in the act of "correcting" it | mitigate | D-41's clause is never reworded to something the app meets and D-34's restatement states what shipped *before* stating the original is unachievable, never the reverse. No checkbox and no status value is touched by either task — asserted by a diff criterion — because a plan that both writes and ticks its own acceptance criteria has certified itself. |
| T-P02-12-SC | Tampering | npm/pip/cargo installs | mitigate | **Zero package-manager operations occur in this plan.** `package.json` and `package-lock.json` are asserted unchanged and the unused `tunnelmole` dependency is asserted still present (D-06 — this machine has npm 11.6.2 against an npm-10 lockfile). `02-RESEARCH.md` § Package Legitimacy Audit records no install for this phase, so no `[ASSUMED]`/`[SUS]` legitimacy checkpoint applies. |
</threat_model>

<verification>
- `npm test` green locally with `# fail 0` and `# skipped` no higher than **B-suite**'s, and the whole
  of `test/repo-claims.test.cjs` asserted at once — this plan is the last wave, and the file's own
  header says the accumulator is asserted whole at the end of the phase. Paste its TAP counters.
- `npm run typecheck` exit `0`. `npm run build` exit `0`.
- **Every clause added by this plan was driven RED, and the failing output is pasted** — four runs for
  the ledger, three each for the one-gate, README and SECURITY clauses, thirteen in total. A clause
  nobody has seen fail is decoration, and this plan's subject is that three already shipped in this
  repo.
- **Cross-platform CI read off the phase's draft PR, never a phase-branch push** (`ci.yml` and `e2e.yml`
  are `branches: [main]` only). Run `gh pr checks` and paste the rows for `Typecheck`,
  `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`, `Build` and
  `Electron smoke (ubuntu-latest)`. An empty table means nothing was triggered — report that; do not
  substitute a local run.
- **The three-places-one-number check**: the marker total in `test/repo-claims.test.cjs`, in `README.md`
  and in `.planning/codebase/CONCERNS.md` are pasted side by side and are equal.
- **The one-story checks.** D-02's corrected mechanism is pasted from all four places this plan writes it
  (`test/main-hardening.test.cjs`, `.planning/codebase/TESTING.md`, `.planning/ROADMAP.md`, and the
  boot-test pointer) and they agree. D-34's restatement is pasted from all three (`README.md`,
  `.planning/ROADMAP.md` criterion 5, `.planning/REQUIREMENTS.md` PARITY-02) and they name the same two
  engines and the same mechanism. D-41's caveat is pasted from all three (`README.md`,
  `.planning/codebase/CONCERNS.md`, `.planning/ROADMAP.md` criterion 4). Four documents telling one story
  is the test; four telling four is what this plan exists to end.
- **No checkbox and no status value moved.** `git diff -- .planning/ROADMAP.md .planning/REQUIREMENTS.md`
  is pasted whole and contains no `- [ ]`→`- [x]` transition and no changed `| … | Pending |` cell. The
  phase rollup flips those, against text these two tasks made true — this plan does not grade itself.
- **Explicitly NOT claimed. Each of these belongs in the SUMMARY as a stated limitation:**
  - PARITY-03 is **not** closed by verifying bridges. It is closed by pinning the ledger. pi, opencode,
    crush and qwen remain `LIVE-UNVERIFIED` and kimi joins them; none of those CLIs is installed here
    and no account was supplied (D-35).
  - PARITY-01a **raised** the unverified count from four to five while raising the can-receive-mail
    count from 8 to 9. The sign is stated, not implied (D-33).
  - PARITY-02 delivered less than "all eleven", and the requirement as written is unachievable (D-34).
  - DAEMON-02 is a **localhost-verified auth path**, never a device-verified install.
  - DAEMON-05's "always visible" is met in purpose and not literally at 800 px (D-41).
  - `setActivationPolicy('accessory')` is **UNVERIFIED — needs a macOS machine**;
    `setLoginItemSettings` is a **no-op on Linux**.
</verification>

<success_criteria>
- PARITY-03 is delivered as a ledger that can fail: an exact total, a per-engine lower bound and a
  committed file set, all measured after wave 8, all seen failing, none of it routed through a helper
  that erases comments.
- No claim this phase falsified survives in the ten files this plan owns, and the three copies of
  ADR-0001's one-gate sentence agree with each other and with `src/main/delivery.ts`.
- **The phase cannot close green against a false criterion.** Phase 2's four disproved Success Criteria
  and PARITY-02's requirement text say what the phase measured, each marked with its date and its D-id
  in the convention its own file already uses — and no checkbox or status value was moved by the plan
  that wrote them.
- `SECURITY.md` describes the door this phase actually built — including the two properties it
  deliberately weakened and the one limitation it removed.
- Every limitation the phase could not close is written in source, docs **and** UI by the plan that owns
  each surface, and is recorded here as a tracked concern in `02-VALIDATION.md`'s wording — never
  quietly narrowed into something achievable.
- No fifth vacuous gate was authored, and the one place a repo-fact clause would have been decorative
  (`.planning/codebase/**`) is named in the SUMMARY as deliberately left un-gated.
</success_criteria>

<output>
Create `.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md` when done. It MUST record:
both preconditions with their commands and answers; **B-markers-by-file** and **B-markers-total** with
the per-engine attribution, beside plan 02-01's and 02-07's SUMMARY numbers and any disagreement found;
whether plan 02-07's wave-4 pin routed through `readStripped` and what was done about it; all thirteen
RED runs; the three-places-one-number paste; the six stated limitations in `02-VALIDATION.md`'s wording;
and one plain sentence stating that PARITY-01a moved the count of engines that can receive mail from 8
to 9 and moved the count of live-verified bridges by zero (D-33).

It MUST also record **the acceptance-surface corrections**: the six `ROADMAP.md` claims with the
before/after text of each and the D-id marking it; the god-file figures as two distinct quantities
(pre-split, from 02-01's and 02-02's baselines, and today's measured `5,812` / `4,121` / `153`);
PARITY-02's restated text beside `README.md`'s paragraph and criterion 5, shown to agree; the answer to
ownership note 4 with its `grep -c 'PARITY'` output; and one plain sentence stating that **no checkbox
and no status value was flipped by this plan** — the rollup flips them, against text this plan made
true. Say plainly that the other Phase-2 plans were right to refuse to inherit these claims and that
refusing to inherit is not correcting, which is why the correction is here in wave 9 and nowhere else.
</output>
