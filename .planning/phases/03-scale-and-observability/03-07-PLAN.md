---
phase: 03-scale-and-observability
plan: 7
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/renderer/src/components/DayBandTab.tsx
  - src/renderer/src/components/CommandCenterPanel.tsx
  - src/renderer/src/design/global.css
  - test/renderer-components.test.cjs
autonomous: false
requirements_addressed: [SCALE-03]

must_haves:
  truths:
    - "The band is one <svg role=img aria-label> of <rect>s — no <canvas>, no Pixi Application, no <text> element inside the SVG"
    - "The scrubber is a native <input type=range> with min/max/step/aria-label/aria-valuetext all present in first-pass static markup"
    - "A bucket the store cannot show in full renders truncated:true plus the real count, never a silent slice"
    - "The detail list renders exactly the rows main returned and filters none of them — 03-03 drops zero-delta cost rows BEFORE its 200-row cap and defines total as the displayable count, so the {shown} and {total} halves of the truncation line can never disagree"
    - "A day before the stored record's first timestamp renders a named missing-not-quiet declaration, distinct from a genuinely-quiet day — including the fresh-install (firstTs===null) case and the day-entirely-before-the-record case, which are their own distinct copy, never the plain 'was quiet' string"
    - "A costTracking:'none' OR costTracking:'transcript' agent active on the floor never lets the day band's cost track read as silently zero — a declared gap sentence names it, with its OWN variant for the transcript tier (whose spend reaches the display join but never the cost ledger this track is drawn from), reusing the app's existing gap-sentence vocabulary, per D-35 on this new surface"
    - "An {ok:false} answer from hive:timeline or hive:timelineBucket renders UI-SPEC's BINDING error copy (03-UI-SPEC.md:247, `Could not read the timeline: {reason}. Pick the day again to retry.`) — not an invented sentence — with the returned reason in the VISIBLE text AND in the SVG's aria-label, never in a `title` alone, and never the quiet-day, fresh-install, empty-bucket or blank-band treatment"
    - "A day the events table cannot speak to but the cost ledger CAN (eventsAgedOut) renders its own sentence instead of the no-record copy — the band never claims 'Nothing was recorded on {date}' while drawing that same day's real cost bars"
    - "The whole day's raw rows are never held in the renderer at once — only the 96 bucket summaries and, on demand, one bucket's capped detail list"
    - "The new 'timeline' tab is appended LAST in TABS, after 'workers', and reuses the existing clock icon"
    - "DayBandTab accepts an optional data prop the production mount leaves undefined and a test can supply directly — the data-dependent assertions (aria-label counts, the truncation <li>) are genuinely first-pass markup, not values a renderToStaticMarkup pass can never produce"
    - "The scrubber CSS lands in the file the app actually imports (src/renderer/src/design/global.css), not a same-named file elsewhere that nothing bundles"
  artifacts:
    - path: src/renderer/src/components/DayBandTab.tsx
      provides: "the day band SVG, scrubber, day picker, and detail list per UI-SPEC S1, with an injectable data prop for tests"
  key_links:
    - from: "src/renderer/src/components/DayBandTab.tsx"
      to: "src/main/index.ts (hive:timeline, hive:timelineBucket)"
      via: "window.cth.hiveTimeline / window.cth.hiveTimelineBucket"
      pattern: "hiveTimeline"
    - from: "src/renderer/src/components/CommandCenterPanel.tsx (TABS)"
      to: "src/renderer/src/components/DayBandTab.tsx"
      via: "a new 'timeline' tab entry, label 'day', rendered when tab === 'timeline'"
      pattern: "'timeline'"
---

> **ANCHOR RULE — binding on every `file:line` citation in this plan.** This phase was planned
> before PR #80 (Phase 2) merged and was then merged with `origin/main`, which shifted `hive.ts`,
> `index.ts`, `REQUIREMENTS.md` and `ROADMAP.md` by tens of lines. A mechanical audit of all nine
> plans on 2026-08-25 found **52 of 534 resolvable citations pointing at a blank or unrelated line**.
> Load-bearing anchors have been corrected, but **assume any line number you read here may be stale.**
>
> **The quoted CONTENT is authoritative; the line number is a hint.** Before editing at any cited
> line, confirm the line actually contains what the citation claims. If it does not, find the content
> by symbol (`grep -n`) and edit there. Never edit a line because its number matched.
>
> This is not defensive boilerplate — it is the defect class that produced findings across four
> separate red-team rounds of this phase, including one that would have sent an edit into a
> `MUST NOT EDIT` block owned by another plan. Every acceptance criterion in this plan is already
> symbol- or content-based and therefore drift-proof; only the prose carries numbers.

> **Requirements-ledger note — read before running the completion step.** This plan's
> frontmatter key is `requirements_addressed`, not `requirements`, and that is deliberate. D-07
> forbids ticking SCALE-03 in Phase 3. `execute-plan.md`'s `update_requirements` step only acts on
> a literal `requirements:` frontmatter key ("If no requirements field, skip") — this plan carries
> no such key, so `requirements.mark-complete` never runs for it. `requirements_addressed` exists
> only so `plan-phase.md`'s coverage gate still counts SCALE-03 as planned. If something tries to
> auto-tick SCALE-03 in `.planning/REQUIREMENTS.md` while finishing this plan, STOP — that is
> red-team finding #1, which named this exact plan as a fourth offender (`03-07-PLAN.md:13
> requirements: [SCALE-03]` in the original). 03-09 (the last plan in this phase) asserts SCALE-03
> is still `Pending` after every other plan lands.

<objective>
Build the day-band CCTab: a fixed-height density band over a wall-clock 24h axis, a native range
scrubber, a day picker, and one merged detail list of the selected bucket's rows — reading from
03-03's `hive:timeline`/`hive:timelineBucket` IPC (which reports `firstTs`, the earliest event
timestamp still stored). Pixi.js and any second WebGL context are refused (D-28); no windowing
library is used or needed, because both lists are structurally bounded (96 columns, one bucket deep,
capped at 200).

`test/renderer-components.test.cjs`'s `renderToStaticMarkup` harness runs with no effects and no state
commits — a component that fetches its data via `useEffect` renders with EMPTY state on the one pass
the test can observe, so any assertion depending on real counts, the gap sentence, or the truncation
`<li>` would silently pass on empty output, or force the executor to downgrade to grepping the source
file (which proves the string exists in the code, not that the branch renders it). This plan gives
`DayBandTab` an explicit, optional data prop the production mount leaves undefined (so real usage is
unaffected — it still fetches via IPC) and the test supplies directly, mirroring 03-08's
`agentView.ts` seeding-seam requirement, so the data-dependent assertions are genuine first-pass
markup.

Purpose: "yesterday is replayable" needs a surface, not only a store. This is that surface, built
entirely from primitives already in the app (a native range input, an SVG of rects, the `Modal`-free
tab-body pattern every other CCTab already uses).
Output: `DayBandTab.tsx` (with its test-injectable data prop), the `TABS` entry, and the one new CSS
block for the scrubber thumb, landed in the CSS file the app actually bundles.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-UI-SPEC.md

<interfaces>
Full UI-SPEC.md §S1 (S1a-S1f) is the binding visual/interaction contract for this plan — every pixel
value, token, colour and accessibility rule below is specified there in full and is NOT re-derived
here. Read it in full before writing any markup.

**Copy: UI-SPEC binds nearly all of it, and an earlier draft of this plan got that badly wrong.**
That draft claimed the additional sentences were "this plan's own text … deliberately absent" from
UI-SPEC.md. That claim was false for FIVE of them, including the error sentence, and it sent the
executor away from a binding table (round-3 finding: the invented error copy). §S1e (`:533-548`) is
only ONE of THREE places UI-SPEC.md specifies this surface's copy. The other two are in the
Copywriting Contract: **Empty states** (`:224-236`, whose day-band rows are `:228-231`) and
**Error states — problem, then what to do next** (`:243-247`). Re-verified this session, every line
number below re-read from the live file.

Sentences this plan renders that UI-SPEC.md ALREADY SPECIFIES — use them VERBATIM from UI-SPEC,
never a paraphrase:

| Sentence | UI-SPEC source |
|---|---|
| `No record before {HH:mm} — missing, not quiet.` | §S1e table, `:540` |
| `The rest of today has not happened yet.` | §S1e table, `:541` |
| `The stored record for {date} starts at {HH:mm}. Hours before that were never written or have since been rotated out — the floor cannot tell you which.` | §S1e `title` text, `:546` |
| `Nothing was recorded on {date}. The stored record starts {firstDate}.` | Empty states, `:228` |
| `{date} was quiet. The floor recorded nothing that day.` | Empty states, `:229` |
| `Nothing in this fifteen minutes.` | Empty states, `:230` |
| `No timeline yet — the record starts the first time the floor logs an event.` | Empty states, `:231` |
| `Could not read the timeline: {reason}. Pick the day again to retry.` | **Error states, `:247`** — the copy for BOTH `ok:false` states (the day summary's and the bucket's; the row is titled "Timeline query failed" and names no channel) |
| `Showing {shown} of {total} rows in this bucket.` | §S1f, `:556` |
| `Envelopes show their subject. The body was never recorded.` | §S1f, `:612` |

Only THREE sentences are genuinely this plan's own, because no UI-SPEC row can express their state
without fabricating — they are quoted verbatim in Task 1's `<behavior>` and nowhere else: the
`eventsAgedOut` sentence (round-3 #9) and the TWO cost-track tier sentences (round-2 #19,
round-3 #2, the latter reusing UI-SPEC `:233`'s existing `no cost meter` vocabulary). Use those
three exactly as written; invent no further wording. If a state seems to need a sentence not listed
above and not one of those three, STOP — the UI-SPEC row must be edited by a plan that owns that
file (today none does), not worked around here.

Task 1's `<behavior>` is the complete enumeration for the band (the four firstTs-derived causes, the
rest-of-today cause, the two cost-track tiers, the `eventsAgedOut` sentence and the unreadable-day
sentence); Task 2's adds the truncation line, the empty-bucket line and the unreadable-bucket
sentence. Each is pinned by its own acceptance criterion; none is optional.

From `src/renderer/src/components/CommandCenterPanel.tsx` (current, verified this session):
```
const TABS: { key: CCTab; label: string; icon: Parameters<typeof Icon>[0]['name'] }[] = [
  { key: 'terminal', label: 'terminal', icon: 'terminal' },
  { key: 'floor', label: 'monitor', icon: 'mcp' },
  { key: 'tasks', label: 'tasks', icon: 'check' },
  { key: 'human', label: 'ask me', icon: 'bell' },
  { key: 'triggers', label: 'triggers', icon: 'clock' },
  { key: 'trigger-history', label: 'history', icon: 'ledger' },
  { key: 'memory', label: 'memory', icon: 'sparkle' },
  { key: 'graph', label: 'graph', icon: 'web' },
  { key: 'activity', label: 'activity', icon: 'bell' },
  { key: 'skills', label: 'skills', icon: 'sparkle' },
  { key: 'workers', label: 'workers', icon: 'gear' }
];
```
Append `{ key: 'timeline', label: 'day', icon: 'clock' }` LAST (per UI-SPEC S1a — the canonical order
is append-only). `CCTab` is a type alias somewhere near the top of this file — widen it to include
`'timeline'`. The tab body is rendered by a conditional block keyed on `tab === '<key>'` — follow the
exact existing pattern for the sibling `'activity'`/`'workers'` bodies (locate and match, do not
invent a new rendering convention).

`ActivityTab`'s `fmt()` switch (current, verified this session, in the SAME file):
```
const fmt = (e: LogEntry): string => {
  switch (e.kind) {
    case 'spawn': return `spawned ${e.name ?? e.agentId}`;
    case 'message': return `${e.from} → ${e.to}: ${e.subject || e.act}`;
    case 'drain': return `${e.agentId} drained ${e.count} msg(s)`;
    case 'escalate': return `escalated to human: ${e.subject ?? ''}`;
    case 'approval': return `approval ${e.approve ? 'granted' : 'denied'}`;
    default: return JSON.stringify(e);
  }
};
```
UI-SPEC S1f reuses these FIVE cases' wording verbatim and adds a SIXTH, `cost`, which does not exist
in `ActivityTab` — do not "restore" `cost` into `ActivityTab` itself (that tab is a live tail; adding
a cumulative-vs-diff cost line there risks ADR-0005's bug in a second place, out of scope here).

Preload contract, landed in 03-03 (re-derive the exact field names from the live `index.ts`/
`preload/index.ts` this plan's `depends_on` produced, do not assume without reading them first — but
the shape is PINNED in 03-03-PLAN.md's `<interfaces>` "RETURN-SHAPE CONTRACT" and restated here
VERBATIM, because a mismatch between the two is exactly the defect this restatement exists to close
(round-3 findings #8 and #26: 03-03's success path returned a bare payload while its failure path
returned `{ok:false, error}` — two different discriminants — and this plan had no error branch at
all, so a REJECTED day rendered as an empty timeline, i.e. as a quiet one):
```ts
hiveTimeline: (day: string) => Promise<
  | { ok: true; buckets: Array<{events:number; envelopes:number; usd:number}>; firstTs: number | null; eventsAgedOut: boolean }
  | { ok: false; error: string }
>
hiveTimelineBucket: (day: string, bucketIndex: number) => Promise<
  | { ok: true; rows: Array<{ts:number; kind:string; /* + the per-kind fields S1f formats */}>; truncated: boolean; total: number }
  | { ok: false; error: string }
>
```
Three consequences this plan MUST honour, none of them optional:
- `ok === false` is the answer for a rejected `day`, a rejected `bucketIndex`, AND main's
  store-unavailable guard. There is no zeroed-success path to fall back on, so `ok` is the FIRST
  thing `DayBandTab` branches on — before any gap-cause logic, before the band renders.
- `eventsAgedOut` is 03-03's flag for "the `events` table cannot speak to this day at all, but the
  never-rotated `cost-ledger.jsonl` still has its spend" — the mixed case created by the events
  table's `EVENTS_RETENTION_DAYS` prune and, more commonly, by a just-migrated install whose events
  table is empty while its ledger is months deep. When it is `true` the no-record copy is FORBIDDEN
  (round-3 finding #9).
- `truncated`/`total` count DISPLAYABLE rows: 03-03 drops zero-delta cost rows before its 200-row
  cap, so this plan's "never emit a cost row for a zero-delta row" rule (Task 2) and the
  `Showing {shown} of {total}` line now agree on what a row is (round-3 finding #10). This plan does
  no filtering of its own.

`day` is ALWAYS a `'YYYY-MM-DD'` LOCAL-date string, per 03-03's settled contract — exactly what the
native `<input type="date">` (S1d) already produces; never an epoch-range form.

**The test-injection seam this plan requires** (closes the `renderToStaticMarkup` gap above): give
`DayBandTab` an explicit optional prop typed as the FULL discriminated result above, not just its
success half — `summary?: TimelineResult | null; bucket?: { index: number; detail: BucketDetailResult
} | null;` — so a test can inject `{ok:false, error:'...'}` as easily as a success payload, which is what
makes the error branch first-pass-assertable at all. When `summary` is `undefined`
(the production mount's default — it never passes this prop), the component fetches via
`window.cth.hiveTimeline` in a `useEffect`, exactly as it would otherwise. When a test supplies
`summary` (and optionally `bucket`) directly as props, the component's FIRST render already reflects
that data — no effect, no state commit, no IPC call needed for the assertion to see real counts, the
gap sentence, or the truncation `<li>`. This mirrors 03-08's `agentView.ts` seeding-seam requirement
(a different mechanism there — a module-singleton seed — same PURPOSE: make a data-dependent
first-pass render testable under this file's no-effects ceiling).

`QrCode.tsx`'s SVG-as-accessible-image precedent (`role="img"` + `aria-label`, no upstream SVG helper,
`shapeRendering="crispEdges"`) is the direct structural precedent for the band — read it before
writing `DayBandTab.tsx`'s SVG.

**CSS file correction:** the app's ONE bundled stylesheet is `src/renderer/src/design/global.css`,
imported at `src/renderer/src/main.tsx:5` (`import './design/global.css';`). There is NO
`src/renderer/src/global.css` — verified this session, `git ls-files "*.css"` returns exactly
`src/renderer/src/design/global.css` and `src/renderer/src/design/tokens.css`. Every reference to a
top-level `global.css` in this plan's earlier drafts was wrong; this version uses the real path
throughout.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: the band SVG — tracks, gap markers, aria-label (S1b, S1e), driven by the injectable data prop</name>
  <files>src/renderer/src/components/DayBandTab.tsx, test/renderer-components.test.cjs</files>
  <read_first>.planning/phases/03-scale-and-observability/03-UI-SPEC.md §S1b and §S1e (full) AND the Copywriting Contract's Empty states table (:224-233) and Error states table (:243-247) — the error sentence this task renders is a BINDING row there (:247), not this plan's invention, src/renderer/src/components/QrCode.tsx:50-67 (the SVG-as-accessible-image precedent), test/renderer-components.test.cjs:24-40 (the renderToStaticMarkup ceiling this component must render correctly under — no effects, no state commits), src/renderer/src/components/AgentDetailPanel.tsx or wherever 03-08's seeding-seam pattern lands, if already present, for a sibling injectable-prop-for-testing example</read_first>
  <behavior>
    - `DayBandTab` accepts an optional `summary?: TimelineResult | null` prop — the FULL
      discriminated shape from `<interfaces>`, `{ok:true, buckets, firstTs, eventsAgedOut}` or
      `{ok:false, error}`. When absent (the production default), the component fetches via
      `window.cth.hiveTimeline` in a `useEffect`. When present (test usage), the FIRST render
      reflects it directly — no effect required for the assertions below to see real data.
    - THE ERROR BRANCH IS CHECKED FIRST (round-3 finding #8). A `summary` of `{ok: false, error}` —
      which is what main returns for a rejected `day`, a rejected bucket index, or a closed/absent
      store — renders UI-SPEC's OWN binding error copy beneath a fully ink-100 band, verbatim from
      the Error states table (`03-UI-SPEC.md:247`, "Timeline query failed"), with the returned
      `error` interpolated as `{reason}`:
      `Could not read the timeline: {reason}. Pick the day again to retry.`
      THE REASON IS PART OF THE VISIBLE SENTENCE — it is NOT relegated to a `title` attribute. §S1e's
      terse-visible / full-`title` pattern applies to the missing-record sentence, whose visible half
      is already a complete claim; it does NOT apply here, because this row's whole point is
      "problem, then what to do next" and the problem is the reason. A `title` on a non-interactive
      element is not reliably exposed by Chromium's accessibility tree, so a reason living only there
      is a reason the operator never receives. It must NEVER render `{date} was quiet…`,
      `No timeline yet…`, or `Nothing was recorded on {date}…` — those three assert a fact about the
      record, and an unread record supports no such fact. A `summary` of `null`/`undefined` is NOT
      this case: that is the not-yet-loaded production state and renders the band's ordinary
      pre-fetch state.
    - `eventsAgedOut === true` OVERRIDES the two no-record causes below (round-3 finding #9). 03-03
      sets it when the `events` table cannot speak to the viewed day at all while the never-rotated
      `cost-ledger.jsonl` still has that day's spend — the 30-day retention case, and much more
      commonly the just-migrated install whose events table is empty and whose ledger is months deep.
      In that state the band is drawing REAL cost bars, so `Nothing was recorded on {date}.` and
      `No timeline yet…` are both false, and the day renders instead:
      `No events are stored for {date} — they were never written or have aged out of the record. The
      cost track below still has that day's spend.` (The "never written or aged out" phrasing is
      §S1e's own load-bearing clause, reused deliberately: the band cannot distinguish the two, and
      claiming either would be a fabrication.) The gap fill for this case covers the events and
      envelopes tracks only — the cost track is real data and is drawn normally.
    - `summarizeDay`'s gap logic (called by `DayBandTab` indirectly, through the `summary` it is
      given, and only ever on the `ok === true` branch) branches on FOUR distinct causes, per UI-SPEC
      §S1e's copy table — never collapsed into
      one string, and computed by comparing `firstTs` against the VIEWED day's own boundaries, never
      formatting a whole-store timestamp as a time-of-day on the wrong day (round-2 finding #33):
      - `firstTs === null` (no events table rows exist at all — a fresh install / pre-migration
        store): `No timeline yet — the record starts the first time the floor logs an event.`
      - `firstTs !== null && firstTs >= dayStartMs + 86400000` (the record's earliest data is on a
        LATER calendar day than the one being viewed — the whole viewed day predates the record):
        `Nothing was recorded on {date}. The stored record starts {firstDate}.` (round-2 #22/#36 —
        this branch did not exist before; a fresh install or any day older than the record's start
        rendered the "was quiet" copy, which is a fabricated claim of genuine silence)
      - `firstTs !== null && firstTs >= dayStartMs && firstTs < dayStartMs + 86400000` (the record's
        earliest data falls INSIDE the viewed day): the `{HH:mm}` form applies, computed as
        `firstTs`'s time-of-day WITHIN this specific day (`new Date(firstTs)`'s local hours/minutes,
        never a raw offset) — `No record before {HH:mm} — missing, not quiet.` for every bucket
        before that time.
      - `firstTs !== null && firstTs < dayStartMs` (the record already started before the viewed day
        — no gap-before-record exists on THIS day at all): no gap marker from this cause; a
        genuinely-zero day under this condition renders `{date} was quiet. The floor recorded
        nothing that day.` instead.
    - Given an injected `summary` for TODAY with buckets after the current time, those trailing
      buckets render the `ink-100` gap fill with the sentence `The rest of today has not happened
      yet.`
    - THE COST-TRACK GAP DECLARATION COVERS TWO TIERS, NOT ONE (round-3 finding #2). The cost track
      is drawn from `cost-ledger.jsonl` (03-03's `dailyCostRows`), and TWO of the four `CostTracking`
      values never reach that file — verified this session in `src/shared/agentProvider.ts`
      (`export type CostTracking = 'otel' | 'proxy' | 'transcript' | 'none';` at :81; SEVEN presets
      declare `costTracking: 'none'` and ONE declares `costTracking: 'transcript'`, so both counts
      are `>= 1` and neither is hypothetical):
      - `'none'` — the engine reports no spend at all.
      - `'transcript'` — the engine's spend IS measured, and 03-02's display join shows it, but it
        never reaches the ledger. This is 03-CONTEXT.md's first Accepted Residual, stated there in
        exactly these terms: `boot.ts`'s append gate (`if (sample?.sessionId)
        hive.appendCostLedger(sample)`, live at `src/main/floor/boot.ts:429`, verified this session)
        stays as it is, so a transcript-fallback sample never lands. Counting only `'none'` here —
        which is what the previous draft did — makes the Accepted Residual render as exactly the
        silent zero D-35 forbids, on the one surface built after the residual was accepted.
      So: when ANY agent currently in the renderer's own roster (`useStore`'s `agents`, already
      available to every CCTab — no new IPC call) has a provider whose `costTracking` (from
      `src/shared/agentProvider.ts`'s `AGENT_PROVIDER_PRESETS`) is `'none'` OR `'transcript'`, the
      band's cost track carries the `--cth-ink-100` treatment (matching the existing gap-fill token)
      and the DOM gains a declared-gap sentence, reusing `src/renderer/src/store/config.ts:488-493`'s existing
      `caps.spend === 'none'` vocabulary pattern (verified this session). The two tiers get their own
      sentence because their gaps are not the same gap, and each is emitted only when its own count
      is `>= 1` (so a floor of only-`'otel'` agents emits neither):
      - `'none'`: `` `{n} agent(s) on this floor report no cost meter — spend for those agents never
        reaches this track.` ``
      - `'transcript'`: `` `{n} agent(s) report spend only from their own transcripts — that spend
        never reaches the cost ledger this track is drawn from.` `` — the sentence names the LEDGER,
        not the meter, because the meter works for these engines and the agent card shows it; it is
        the ledger hop that is missing.
      Both are independent of the firstTs-derived gap causes above and of each other, and any
      combination can render together (round-2 finding #19 + round-3 finding #2 — D-35 forbids an
      engine's spend reading as zero/absent without a declared reason on ANY surface, and the day
      band is a new surface).
    - The SVG's `aria-label` is built as one string: `` `Activity for {date}: {n} events, {m}
      envelopes, ${cost} across 96 fifteen-minute buckets.` ``, with whichever gap sentence(s) apply
      (firstTs-derived or the `eventsAgedOut` sentence, plus either cost-track declaration when
      applicable) appended verbatim — assertable in `renderToStaticMarkup` output via the injected
      prop. On the `ok:false` branch the counts are unknowable, so the label is the prefix plus the
      SAME error sentence the DOM shows, appended verbatim exactly as UI-SPEC `:462` mandates for the
      gap sentences: `` `Activity for {date}: Could not read the timeline: {reason}. Pick the day
      again to retry.` `` — never a label reporting `0 events, 0 envelopes, $0.00`, which is the same
      fabrication in ARIA form, and never a reason-stripped variant, which strands the screen-reader
      user with a problem and no cause. `{reason}` reaches the ARIA string and the visible string
      from the SAME single-line constant, so the two cannot drift.
    - Each bucket's bar height is an integer 1..8 viewBox units via `Math.ceil(8 * value / trackMax)`,
      never an opacity value.
  </behavior>
  <action>
    Create `DayBandTab.tsx` accepting the optional `summary`/`bucket` props from `<interfaces>` above
    (production mount passes neither — it fetches internally; leave the fetch/`useEffect` wiring for
    Task 2, this task focuses on the band given whatever `summary` it has, injected or fetched).
    Implement the band per UI-SPEC S1b exactly (the `<svg role="img" aria-label={...} viewBox="0 0 96
    26" preserveAspectRatio="none" shapeRendering="crispEdges">`, the plate `<rect>`, three 8-unit
    tracks with 1-unit gaps, 96 one-unit-wide columns, the events/envelopes/cost track colours and
    sources per the table in S1b) and the FOUR-branch gap-marker logic per S1e and `<behavior>` above
    (compare `firstTs` against `dayStartMs`/`dayStartMs + 86400000`, never format a whole-store
    timestamp as a bare time-of-day without first checking it falls inside the viewed day; the
    `title` attribute on the missing-record sentence carries UI-SPEC `:546`'s full explanation).
    Branch on `summary.ok` FIRST, before any of that: an `ok:false` summary renders UI-SPEC `:247`'s
    BINDING sentence — `Could not read the timeline: {reason}. Pick the day again to retry.` with the
    returned `error` interpolated as `{reason}` IN THE VISIBLE TEXT (a `title` is NOT a substitute
    here; see `<behavior>`) and that same composed string appended to the SVG's `aria-label` — over
    an all-ink-100 band, and returns before any gap-cause or count logic runs. Then
    handle `eventsAgedOut` BEFORE the two no-record causes, per `<behavior>`: when it is true,
    neither `Nothing was recorded on` nor `No timeline yet` may be emitted, and the cost track is
    drawn normally because it holds real data. Add BOTH cost-track
    gap declarations from `<behavior>`, reading the roster's agents (already available via the
    existing `useStore` the sibling CCTabs already import — no new fetch) and cross-referencing
    `AGENT_PROVIDER_PRESETS` for `costTracking === 'none'` and, separately, for `=== 'transcript'` —
    two counts, two sentences, each emitted only when its own count is `>= 1`.
    Add the two DOM legend/axis rows beneath
    the band (S1b: the `00 · 06 · 12 · 18 · 24` axis row and the three-swatch track legend row) as
    plain DOM, outside the SVG. Extend `test/renderer-components.test.cjs` with `renderToStaticMarkup`
    assertions — using the injected `summary` prop, NOT a live fetch — for the `aria-label` string
    shape, ALL FOUR gap-cause sentences (including the `firstTs === null` and
    day-entirely-before-record cases), an injected `{ok:false, error}` summary whose `error` is the
    SENTINEL string `sentinel-store-unreadable` (a token that appears nowhere in this repo today —
    `grep -rF "sentinel-store-unreadable" src/ test/` returns 0, verified this session — so the
    assertions below cannot be satisfied by unrelated text), asserting that (a) the exact rendered
    sentence `Could not read the timeline: sentinel-store-unreadable. Pick the day again to retry.`
    IS present in the markup — the REASON in the VISIBLE text, which is the half a `title`-only
    implementation drops — (b) the `aria-label` attribute's value also contains
    `sentinel-store-unreadable`, so the reason survives into ARIA too, and (c) `was quiet`,
    `No timeline yet` and `Nothing was recorded on` are ALL absent from that same markup — that one
    case's `test(...)` title MUST begin with the literal
    `round-3 #8: an ok:false timeline`, because the last acceptance criterion below anchors on it and
    is deliberately vacuous-proof rather than trusting that the case exists — an injected `ok:true`
    summary with `eventsAgedOut: true`
    (asserting the aged-out sentence is present and `Nothing was recorded on` is absent), BOTH
    cost-track declarations — seed the store fixture with a `costTracking:'none'` agent for one and a
    `costTracking:'transcript'` agent (codex is the one preset that carries it, `agentProvider.ts:257`)
    for the other, and assert each sentence renders for its own tier and NOT for the other's — and
    the absence of any `<text>` element inside the SVG.
    Write each declared sentence as a SINGLE-LINE string constant or template literal (never as JSX
    text children broken across lines by the formatter) and compose the `aria-label` from those same
    constants — the sentences are asserted by literal `grep -F` above and by `renderToStaticMarkup`
    below, and a formatter-wrapped JSX text node would defeat the former while the component still
    behaved correctly. One definition per sentence, used by both the DOM and the label.
  </action>
  <acceptance_criteria>
    - `grep -n "<canvas\|new Application(" src/renderer/src/components/DayBandTab.tsx` matches exactly `0`
    - `grep -n "role=\"img\"" src/renderer/src/components/DayBandTab.tsx` matches `>= 1`
    - `grep -n "summary?:" src/renderer/src/components/DayBandTab.tsx` matches `>= 1` (the injectable
      data prop exists and is optional)
    - `grep -n "No record before\|has not happened yet\|was quiet\|No timeline yet\|Nothing was recorded on" src/renderer/src/components/DayBandTab.tsx` matches `>= 5` (all FIVE distinct strings present — round-2 #22/#33/#36's fix)
    - THE ERROR SENTENCE IS UI-SPEC `:247`'s, NOT AN INVENTED ONE, AND IT CARRIES ITS REASON:
      `grep -cF "Could not read the timeline: " src/renderer/src/components/DayBandTab.tsx` is
      `>= 1` AND `grep -cF "Pick the day again to retry." src/renderer/src/components/DayBandTab.tsx`
      is `>= 1` (both halves of the binding row — a component that keeps only the first half has
      dropped the "what to do next" clause the row's own heading names) AND
      `grep -cF "could not read the record" src/renderer/src/components/DayBandTab.tsx` is exactly
      `0` (the earlier draft's INVENTED wording, which contradicted the binding row and dropped the
      reason — its presence is the defect, so this negative is load-bearing; it is paired with the
      two positives immediately above it over the SAME file, so it can never be the vacuous `0` of a
      file that was never written). Baselines re-verified this session:
      `grep -rniF "Could not read the timeline" src/` and `grep -rniF "Pick the day again" src/` each
      return 0 across the whole tree, and `DayBandTab.tsx` does not exist yet.
    - `grep -cF "aged out of the record" src/renderer/src/components/DayBandTab.tsx` is `>= 1`
      (round-3 #9's two-stores sentence — one of the three sentences that genuinely IS this plan's
      own text). Baseline 0.
    - `grep -cF "no cost meter" src/renderer/src/components/DayBandTab.tsx` is `>= 1` (round-2 #19's
      `'none'`-tier sentence — the existing app-wide vocabulary, not a re-invented string) AND
      `grep -cF "never reaches the cost ledger" src/renderer/src/components/DayBandTab.tsx` is `>= 1`
      (round-3 #2's SEPARATE `'transcript'`-tier sentence). Both baselines are 0. A single merged
      sentence covering both tiers fails the second grep, which is the point: the two gaps have
      different causes and different remedies.
    - `grep -cE "[\"']transcript[\"']" src/renderer/src/components/DayBandTab.tsx` is `>= 1` — the
      component actually TESTS the `'transcript'` tier in code, not merely mentions it in prose
      (quote-style tolerant; this repo writes `'transcript'` — `src/shared/agentProvider.ts:257`).
      Baseline 0.
    - `node -e` proving the error branch's EXCLUSIVITY is actually ASSERTED, not merely intended —
      anchored on the test case's MANDATED title (see `<action>`), which is what makes it both
      vacuous-proof and immune to how the component happens to be factored. Read
      `test/renderer-components.test.cjs` into `s`; set
      `const i = s.indexOf('round-3 #8: an ok:false timeline');` and
      `if (i < 0) { console.error('FAIL: the round-3 #8 ok:false case is missing — this check is
      vacuous'); process.exit(1); }`; then `let j = s.indexOf('\ntest(', i); if (j < 0) j = s.length;`
      — this slice bound is correct ONLY because every `test(` in this file starts at column 0 today
      (verified this session: `grep -c "^test(" test/renderer-components.test.cjs` is `>= 7` and
      `grep -c "describe(" test/renderer-components.test.cjs` is `0`). Do NOT wrap the new cases in a
      `describe(` block or indent them — that would push the next `test(` off column 0, widen the
      slice to end-of-file, and let strings from LATER cases satisfy this check vacuously.
      and FAIL unless `s.slice(i, j)` contains ALL SIX of
      `Could not read the timeline: sentinel-store-unreadable. Pick the day again to retry.` (the
      binding UI-SPEC `:247` sentence with its reason interpolated — asserted as one literal, so a
      reason-stripped or paraphrased implementation cannot satisfy it), `aria-label` and
      `sentinel-store-unreadable` (together: the reason is asserted to survive into ARIA too, not
      only into the visible text), and `was quiet`, `No timeline yet` and `Nothing was recorded on`
      (the three exclusions). Print which are missing. In a case whose
      only fixture is an injected `{ok: false, error}` summary, the last three can be there for
      exactly one reason — the absence assertions this finding requires — so a case that checks only
      the positive sentence goes red, and a plan-did-nothing executor goes red at the anchor guard.
      **Do not re-express this as a slice of `DayBandTab.tsx` between the `ok`-branch anchor and its
      `return`:** `<action>` mandates the error branch as an EARLY RETURN, so that slice is empty by
      construction and the check would fail every correct implementation. Assert it where it is
      observable — the rendered markup — not in the source's shape.
    - `node --test test/renderer-components.test.cjs` passes with 0 failures, and the new assertions
      drive the component via the injected `summary` prop AND cover the `firstTs === null` case, the
      day-entirely-before-record case, the `{ok:false}` case, the `eventsAgedOut: true` case, and
      BOTH cost-track gap tiers
    - **The two `firstTs` boundary cases need their own pins** — the bullet below already pins
      `ok: false`, `eventsAgedOut` and the transcript tier, but `firstTs === null` (fresh install) and
      day-entirely-before-record have only a SOURCE-side sentence grep, which proves `DayBandTab.tsx`
      *can* render those strings, not that any fixture drives the boundary. Add
      `grep -cE "firstTs: *null" test/renderer-components.test.cjs` is `>= 1` and
      `grep -cE "firstTs: *[0-9]" test/renderer-components.test.cjs` is `>= 1` (a real timestamp
      later than the requested day). Both measured 0 today; that file passes 7/7 before any work, so
      the test-run bullet alone cannot fail.
    - The seeded cases are pinned in the test file this plan owns and modifies (spacing-tolerant
      patterns, so a formatter pass cannot fail a correct test) —
    - **THE PRODUCTION IPC CALLS ARE PINNED (round-9 finding #2).** Without these, 03-03's entire
      timeline IPC — two handlers, a preload bridge and a migration — can ship with **zero renderer
      callers** and every criterion in both plans still passes. That is the `push.ts` shape D-38
      exists to prevent, one wave apart. Both measured **0** today (the file does not exist yet):
      `grep -c -F "window.cth.hiveTimeline(" src/renderer/src/components/DayBandTab.tsx` is `>= 1`
      AND `grep -c -F "window.cth.hiveTimelineBucket(" src/renderer/src/components/DayBandTab.tsx`
      is `>= 1`. Keep the trailing `(` so the type-only mention in `<interfaces>` cannot satisfy it.
      `grep -cE "ok: *false" test/renderer-components.test.cjs` is `>= 1`,
      `grep -cF "eventsAgedOut" test/renderer-components.test.cjs` is `>= 2` (the `true` case and its
      control), and `grep -cE "costTracking: *.transcript." test/renderer-components.test.cjs` is
      `>= 1`. All three baselines are 0 today — `grep -nE "transcript|costTracking|ok: *false|eventsAgedOut"
      test/renderer-components.test.cjs` returns NO matches, verified this session — so none of these
      can be satisfied by pre-existing text.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-components.test.cjs</automated>
  </verify>
  <done>The band renders as one accessible SVG with integer bar heights, no text inside it; the ok:false branch, the eventsAgedOut branch, all four firstTs-derived gap causes and BOTH cost-track declarations ('none' and 'transcript') are distinct sentences that are never collapsed into one string; and every one of those assertions is genuinely first-pass markup via the injectable summary prop, not values a no-effects render pass could never produce.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the scrubber, day picker, IPC fetch wiring, and merged detail list (S1c, S1d, S1f)</name>
  <files>src/renderer/src/components/DayBandTab.tsx, test/renderer-components.test.cjs</files>
  <read_first>.planning/phases/03-scale-and-observability/03-UI-SPEC.md §S1c, §S1d, §S1f (full), src/renderer/src/components/CommandCenterPanel.tsx's ActivityTab fmt() (the five cases to reuse verbatim)</read_first>
  <behavior>
    - The scrubber renders `<input type="range" min="0" max="95" step="1" aria-label="Time of day"
      aria-valuetext="{HH:mm}–{HH:mm} · {n} events · {m} envelopes · ${cost}">` with all five
      attributes present in first-pass static markup (assertable, no effects needed).
    - The day picker renders `<input type="date" aria-label="Day to replay" max={today}>` with NO
      `min` attribute at all.
    - `DayBandTab` accepts an optional `bucket?: { index: number; detail: BucketDetailResult } | null`
      prop (per `<interfaces>`) — when a test injects it directly, selecting that bucket's detail
      renders WITHOUT needing `window.cth.hiveTimelineBucket` to resolve first. Given an injected
      bucket detail with `ok: true, truncated: true, total: 312`, the list renders a LAST `<li>`
      reading `Showing {shown} of {total} rows in this bucket.` — never a silent slice with no count
      shown — assertable on the FIRST render pass.
    - `detail.ok === false` is checked before the list renders, the same way Task 1 checks
      `summary.ok`: the `<ol>` is replaced by the SAME binding sentence from UI-SPEC's Error states
      table (`03-UI-SPEC.md:247`) Task 1 uses — `Could not read the timeline: {reason}. Pick the day
      again to retry.`, `{reason}` being this call's returned `error`, IN THE VISIBLE TEXT. That row
      is titled "Timeline query failed" and names no channel, so it binds the bucket query as much as
      the day query; an earlier draft invented `This bucket could not be read.` here, which is the
      same defect as Task 1's invented sentence and drops the reason the same way. Reuse Task 1's
      single sentence constant rather than declaring a second one. An unreadable bucket must never
      render as an empty bucket (`Nothing in this fifteen minutes.`, UI-SPEC `:230`), for the same
      reason a rejected day must never render as a quiet one.
    - THE EMPTY BUCKET IS ITS OWN RENDERED BRANCH, and it must actually exist for the sentence above
      to mean anything. `detail.ok === true` with `rows.length === 0` renders UI-SPEC `:230`'s
      `Nothing in this fifteen minutes.` verbatim, in place of the `<ol>`. Without this branch the
      unreadable-bucket rule is unfalsifiable — the string would appear nowhere in the component, so
      "the empty-bucket copy is absent from the unreadable-bucket markup" would be true by
      construction rather than by correct branching, and a genuinely empty bucket would render as
      blank space, which is the silent-nothing D-27 forbids. `<interfaces>`'s copy table already
      assigns `:230` to this task; this bullet is where it is actually rendered.
    - `truncated` and `total` are taken from main VERBATIM and never recomputed here. 03-03 drops
      zero-delta cost rows before its 200-row cap and defines `total` as the DISPLAYABLE row count
      (round-3 finding #10), so `shown === rows.length` and `total` already agree with this list's
      own "no cost row for a zero-delta row" rule. This plan filters nothing and counts nothing.
    - In PRODUCTION (no injected props), selecting a bucket calls `window.cth.hiveTimelineBucket` and
      updates local state with the result on resolve — the injected-prop path and the live-fetch path
      share the same rendering code, differing only in where the data comes from.
    - A `cost` row renders `{name} +$0.0123` and a message row with a blank subject renders using the
      `|| e.act` fallback (`{from} → {to}: {act}`), matching `ActivityTab`'s existing fallback
      behavior exactly.
    - `kind` renders in `--cth-ink-500` (NOT `ActivityTab`'s `--cth-ink-300`) — this divergence is
      deliberate per S1f's contrast measurement, not a copy-paste miss.
  </behavior>
  <action>
    Implement the scrubber input and day picker per S1c/S1d exactly (attributes, no `min` on the date
    input, arrow-key native stepping — no glyph buttons). Wire the component's internal state (selected
    day, selected bucket index) and its `useEffect`-driven `window.cth.hiveTimeline`/`hiveTimelineBucket`
    calls — but ONLY when the corresponding `summary`/`bucket` prop was not supplied by the caller
    (Task 1's seam); the production mount always takes this live-fetch path since it never passes
    those props. Implement the merged detail list per S1f: an `<ol>` of `<li>` with time/kind/text
    columns at the specified tokens and colours, reusing `ActivityTab`'s five `fmt` cases verbatim (do
    not re-derive their wording) plus the new `cost` case (`{name} +$0.0123`, never emitted for a
    zero-delta row — 03-03 already drops those before its cap, so this is a restatement of the
    contract, not a second filter), the truncation `<li>` rendered from main's `truncated`/`total`
    verbatim, the `detail.ok === false` branch from `<behavior>` (the `<ol>` replaced by UI-SPEC
    `:247`'s binding `Could not read the timeline: {reason}. Pick the day again to retry.`, reusing
    Task 1's sentence constant), the zero-row branch from `<behavior>` (`ok: true` with
    `rows.length === 0` renders UI-SPEC `:230`'s `Nothing in this fifteen minutes.` verbatim in place
    of the `<ol>` — write it as a single-line constant like every other declared sentence), and the
    standing "Envelopes show their subject. The body
    was never recorded." section note. The component never holds more than one bucket's rows in state
    at once. Extend `test/renderer-components.test.cjs` with the scrubber/date-input attribute
    assertions and the truncation-`<li>` assertion from `<behavior>`, using the injected `bucket` prop
    for the data-dependent case, PLUS one more case whose `test(...)` title MUST begin with the
    literal `round-3 #8: an ok:false bucket detail` (the last acceptance criterion anchors on it):
    inject `bucket: { index: 0, detail: { ok: false, error: 'sentinel-bucket-unreadable' } }` and
    assert the markup contains the literal
    `Could not read the timeline: sentinel-bucket-unreadable. Pick the day again to retry.` and does
    NOT contain `Nothing in this fifteen minutes`. `sentinel-bucket-unreadable` appears nowhere in
    this repo today (`grep -rF "sentinel-bucket-unreadable" src/ test/` returns 0, verified this
    session).
  </action>
  <acceptance_criteria>
    - `grep -n "type=\"range\"" src/renderer/src/components/DayBandTab.tsx` matches `>= 1`
    - `grep -n "aria-valuetext" src/renderer/src/components/DayBandTab.tsx` matches `>= 1`
    - `grep -n "type=\"date\"" src/renderer/src/components/DayBandTab.tsx` matches `>= 1`, and
      `grep -n "min=" src/renderer/src/components/DayBandTab.tsx` shows no `min` on that same input
    - `grep -n "bucket?:" src/renderer/src/components/DayBandTab.tsx` matches `>= 1` (the second
      injectable data prop)
    - `grep -n "Showing {shown} of {total} rows in this bucket\|Showing .* of .* rows in this bucket" src/renderer/src/components/DayBandTab.tsx` matches `>= 1`
    - THE EMPTY-BUCKET BRANCH EXISTS, which is what makes the next criterion's absence half
      falsifiable instead of true-by-construction:
      `grep -cF "Nothing in this fifteen minutes." src/renderer/src/components/DayBandTab.tsx` is
      `>= 1` (UI-SPEC `:230`'s verbatim copy, rendered when `detail.ok === true &&
      rows.length === 0`). Baseline 0 — `grep -rniF "Nothing in this fifteen minutes" src/` returns
      0 across the whole tree today and `DayBandTab.tsx` does not exist, verified this session. If
      this string were absent from the component, the `node -e` check below could never fail on its
      `Nothing in this fifteen minutes` absence assertion, and a genuinely empty bucket would render
      as blank space.
    - THE BUCKET'S `ok:false` BRANCH USES THE SAME BINDING SENTENCE, AND IS PROVEN TO RENDER:
      `grep -cF "This bucket could not be read" src/renderer/src/components/DayBandTab.tsx` is
      exactly `0` (the earlier draft's invented bucket copy is gone — UI-SPEC `:247`'s
      "Timeline query failed" row names no channel, so it binds this state too). Since Task 2 reuses
      Task 1's sentence constant, a grep for the sentence would already pass from Task 1 alone and
      would prove nothing about this branch — so the real check is a `node -e` anchored on the
      mandated case title, exactly like Task 1's: read `test/renderer-components.test.cjs` into `s`;
      `const i = s.indexOf('round-3 #8: an ok:false bucket detail');` and
      `if (i < 0) { console.error('FAIL: the ok:false bucket-detail case is missing — this check is
      vacuous'); process.exit(1); }`; then `let j = s.indexOf('\ntest(', i); if (j < 0) j = s.length;`
      — this slice bound is correct ONLY because every `test(` in this file starts at column 0 today
      (verified this session: `grep -c "^test(" test/renderer-components.test.cjs` is `>= 7` and
      `grep -c "describe(" test/renderer-components.test.cjs` is `0`). Do NOT wrap the new cases in a
      `describe(` block or indent them — that would push the next `test(` off column 0, widen the
      slice to end-of-file, and let strings from LATER cases satisfy this check vacuously.
      and FAIL unless `s.slice(i, j)` contains BOTH
      `Could not read the timeline: sentinel-bucket-unreadable. Pick the day again to retry.` and
      `Nothing in this fifteen minutes` (the latter can only be present as the absence assertion, the
      injected fixture having no rows to render it from). Print which is missing. A component that
      renders an unreadable bucket as an empty one fails the `node --test` run this case is part of.
    - NO SECOND ZERO-DELTA FILTER (round-3 finding #10, consumer half):
      `grep -cE "usd === 0|tokens === 0|usd !== 0|tokens !== 0" src/renderer/src/components/DayBandTab.tsx`
      is exactly `0`. 03-03's `bucketDetail` drops zero-delta cost rows BEFORE its 200-row cap and
      defines `total` as the displayable count; a second drop here would make the `{shown}` half of
      `Showing {shown} of {total} rows in this bucket.` disagree with the `{total}` main computed —
      the misreporting this finding is about, moved one process across. The positive lower bound this
      negative is paired with is Task 1's `grep -n "role=\"img\"" …DayBandTab.tsx` matches `>= 1`
      over the SAME file: a missing or empty component goes red there first, so this `0` can never be
      the vacuous `0` of a file that does not exist.
    - `node --test test/renderer-components.test.cjs` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-components.test.cjs</automated>
  </verify>
  <done>The scrubber and day picker are fully attributed native inputs; the detail list is one bucket deep, never silently truncated, reuses ActivityTab's wording where it applies, and its data-dependent rendering is testable via the injected bucket prop.</done>
</task>

<task type="auto">
  <name>Task 3: wire the 'timeline' tab and the scrubber CSS — into the file the app actually bundles</name>
  <files>src/renderer/src/components/CommandCenterPanel.tsx, src/renderer/src/design/global.css</files>
  <read_first>src/renderer/src/components/CommandCenterPanel.tsx:75-115 (TABS array and CCTab type), src/renderer/src/design/global.css (the existing token/focus-ring rules near the file's top — NOT src/renderer/src/global.css, which does not exist), src/renderer/src/main.tsx:5 (confirms design/global.css is the one imported file), .planning/phases/03-scale-and-observability/03-UI-SPEC.md §S1c's exact CSS block</read_first>
  <action>
    Append `{ key: 'timeline', label: 'day', icon: 'clock' }` to `TABS`, after `'workers'`; widen the
    `CCTab` type union to include `'timeline'`; add the render branch mounting `DayBandTab` when `tab
    === 'timeline'` (with no `summary`/`bucket` props passed, so it takes the live-fetch path),
    matching the existing sibling tab-body pattern in this file exactly. Add the ONE CSS addition
    UI-SPEC S1c specifies verbatim to `src/renderer/src/design/global.css` — the file `main.tsx`
    actually imports (the `.cth-scrub` block — track, thumb at 24x24, no border-radius, `--cth-ink-300`
    track colour) — do not add any other new class, and do not create a new CSS file.
  </action>
  <acceptance_criteria>
    - `grep -n "key: 'timeline', label: 'day'" src/renderer/src/components/CommandCenterPanel.tsx` matches `>= 1`
    - `grep -n "\.cth-scrub" src/renderer/src/design/global.css` matches `>= 1`
    - `grep -n "\.cth-scrub" src/renderer/src/global.css 2>/dev/null` matches exactly `0` (or the
      file simply does not exist — confirms the fix did not create the wrong, unbundled file)
    - `grep -n "design/global.css" src/renderer/src/main.tsx` matches `>= 1` (the bundled import site,
      confirming this is the file the app actually loads)
    - `grep -n "'timeline'" src/renderer/src/components/CommandCenterPanel.tsx | wc -l` shows both the
      TABS entry and the CCTab type widening (at least 2 occurrences)
    - `node --test test/renderer-components.test.cjs` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-components.test.cjs</automated>
  </verify>
  <done>The day tab is reachable, appended last, and the scrubber renders with the one new CSS block landed in the actual bundled stylesheet.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: containment check — the 12th tab, and the day tab body at its narrowest</name>
  <what-built>A 12th `CommandCenterPanel` tab ('day'), appended last to `TABS`.</what-built>
  <how-to-verify>
    1. Build the BASE sha (before this plan) and the HEAD sha (after) of the app.
    2. Run the dev app at both, open Michael's Command Center, and measure the docked tab strip's
       rendered height at 1280, 1024 and 800 logical widths (Electron devtools -> toggle device
       toolbar, or a CDP `Emulation.setDeviceMetricsOverride` probe if available) — confirm the
       strip's height at HEAD is AT MOST one 31px row taller than at BASE, at all three widths,
       docked.
    3. Confirm `scrollWidth === clientWidth` on every tab `<button>` (no clipped label) at all three
       widths.
    4. Confirm the strip stays ONE row in fullscreen mode at 1280.
    5. Open the `day` tab at the narrowest docked width and confirm no content overflows the panel
       horizontally.
  </how-to-verify>
  <files>(none -- operator verification of the running app)</files>
  <action>Pause execution and present the numbered verification steps above to the operator verbatim. Do not write the SUMMARY until the operator answer is returned.</action>
  <verify><human-check>Operator confirms the docked tab strip grows by at most one 31px row across 1280/1024/800, no tab label clips, fullscreen stays one row, and the day tab body does not overflow at its narrowest.</human-check></verify>
  <done>The operator has exercised the 12th-tab containment check and the result is recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" or report the exact width/row-count that broke, per UI-SPEC's stop-and-report clause (never shorten a label or shrink the tab font to compensate).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer <-> main (`hive:timeline`/`hive:timelineBucket`) | Read-only; day and bucket-index parameters are the only renderer-supplied input, both validated in main per 03-03's `timeline.ts` validators (`parseDayParam`/`validateBucketIndex`) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-07a | Denial of Service | rendering an unbounded detail list | mitigate | `BUCKET_DETAIL_ROW_CAP` (200, set in 03-03's `timeline.ts`) is enforced in main; this plan's UI only ever renders what main returns, plus the truncation `<li>` |
| T-03-07b | Information Disclosure | envelope subject/body on the timeline | accept | Only the subject was ever recorded (`hive.ts:1668`); this plan states that limitation in the UI rather than implying more detail exists |
| T-03-07c | Spoofing | a second WebGL context regression (Pixi reintroduced here) | mitigate | This plan's own acceptance criteria pin `<canvas>`/`new Application(` absence in `DayBandTab.tsx` |
</threat_model>

<verification>
- `node --test test/renderer-components.test.cjs` — 0 failures
- `npm run typecheck` — 0 errors
- Task 4 checkpoint approved
</verification>

<success_criteria>
- The band renders as one accessible SVG with no text inside it and integer bar heights
- The scrubber and day picker are fully-attributed native inputs, assertable in static markup
- The detail list is one bucket deep, capped, and never silently truncates
- Data-dependent assertions (aria-label counts, the truncation `<li>`, all four firstTs-derived gap
  sentences, the `{ok:false}` error sentence, the `eventsAgedOut` sentence, and BOTH cost-track gap
  declarations) are genuinely first-pass markup via the injectable
  `summary`/`bucket` props, not values a no-effects render pass could never produce
- A rejected, unreadable or store-unavailable day — and equally an unreadable bucket — renders
  UI-SPEC `:247`'s binding `Could not read the timeline: {reason}. Pick the day again to retry.`,
  with the reason visible AND in ARIA; never an invented sentence, never the quiet-day copy, never
  the fresh-install copy, never the empty-bucket copy, never a silently empty band
- Every sentence this plan renders is either quoted from UI-SPEC.md with its line number (the table
  in `<interfaces>`) or is one of the THREE sentences named there as this plan's own — no copy is
  invented for a state UI-SPEC already binds
- A day whose events aged out but whose ledger still holds spend says so, instead of asserting
  "Nothing was recorded" above its own cost bars
- Both cost-gap tiers are declared: `costTracking:'none'` (no meter at all) and
  `costTracking:'transcript'` (metered, but never reaching the ledger this track is drawn from) —
  the Accepted Residual's engines can no longer render as a silent zero here
- The 'timeline' tab is appended last and does not regress the tab strip's containment
- The scrubber CSS lands in the file the app actually bundles (`src/renderer/src/design/global.css`)
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-07-SUMMARY.md` when done
</output>
