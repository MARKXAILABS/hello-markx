---
phase: 03-scale-and-observability
plan: 6
type: execute
wave: 5
depends_on: ["03-04"]
files_modified:
  - src/renderer/src/store/config.ts
  - src/renderer/src/hooks/useRestoreTeam.ts
  - src/renderer/src/hooks/bulkSpawn.ts
  - src/renderer/src/components/AddAgentModal.tsx
  - src/renderer/src/components/TeamReviewModal.tsx
  - test/bulk-spawn.test.cjs
  - test/team-review.test.cjs
autonomous: false
requirements: [SCALE-02]

must_haves:
  truths:
    - "The bulk-spawn concurrency/roster-order/failure-isolation shape is EXTRACTED from useRestoreTeam.ts and shared, never re-implemented — useRestoreTeam.ts calls the same shared function it always ran, and this is now proven by a real test file that loads and drives spawnBatch directly, not merely claimed"
    - "A team file's members spawn CONCURRENTLY, land on the roster in the file's original member order (not completion order), and one member's failure never aborts the rest"
    - "Two members with the same name hired in the SAME team-import batch get DIFFERENT agent ids, and this is proven against the PRODUCTION id path — id generation lives in an EXPORTED, loadable `batchAgentIds(names, now)` in bulkSpawn.ts that BOTH the team-hire batch and AddAgentModal's single-hire path call, so the test drives the same code the app runs, never a module-private copy the test cannot reach (round-3 #7/#35). The fix never touches useRestoreTeam.ts, which generates no ids and reuses persisted ones by design"
    - "Every untrusted manifest field that reaches a spawned agent is READABLE in the review sheet before the operator hires. `description` becomes `hive.role` (AddAgentModal.tsx:363, useRestoreTeam.ts:152) and is interpolated into identity.md on every spawn (hive.ts:1397 -> hive.ts:899); `goal` is capped at 4000 chars (hire.ts:182) and becomes the agent's standing directive. 03-UI-SPEC.md:263 declares this sheet IS the confirmation for a bulk hire, so both fields render in full and wrapping — never nowrap-ellipsized out of sight — and this is proven by a REAL renderToStaticMarkup assertion in test/team-review.test.cjs, not by a grep for a variable name"
    - "A team file's member count is bounded by TEAM_MAX_MEMBERS (03-04), not by the byte cap alone, and this plan's threat register cites that constant"
    - "Duplicate-name rows in the review sheet default UNCHECKED, with a visible reason"
    - "Import continues to prefill the single-agent form for a hire@1 file exactly as before; only a team@1 file opens the new review sheet"
    - "The import button's label no longer promises only 'hire' once it also accepts a team file"
  artifacts:
    - path: src/renderer/src/hooks/bulkSpawn.ts
      provides: "the shared, order-preserving, failure-isolated bulk-spawn function `spawnBatch`, AND the exported `batchAgentIds(names, now)` that is the single production id-generating path for both the team-hire batch and AddAgentModal's single hire"
    - path: src/renderer/src/components/TeamReviewModal.tsx
      provides: "the import team review sheet — which mints its member ids by CALLING the exported `batchAgentIds` and spawns by CALLING `spawnBatch`, never by re-deriving either rule inline, so the sheet ships the exact code test/bulk-spawn.test.cjs drives"
    - path: test/bulk-spawn.test.cjs
      provides: "real node --test coverage of spawnBatch's concurrency, order-preservation, failure-isolation, AND of `batchAgentIds` under a FIXED clock — the collision case asserted against the exported production function rather than a module-private copy (round-3 #7). No prior test in this repo touches useRestoreTeam.ts, spawnBatch or batchAgentIds"
  key_links:
    - from: "src/renderer/src/components/AddAgentModal.tsx (importHire, res.team branch)"
      to: "src/renderer/src/components/TeamReviewModal.tsx"
      via: "a new modal opened instead of applyManifest for a team result"
      pattern: "res.team"
    - from: "src/renderer/src/hooks/useRestoreTeam.ts"
      to: "src/renderer/src/hooks/bulkSpawn.ts"
      via: "the extracted shared function, no re-implementation"
      pattern: "spawnBatch"
    - from: "src/renderer/src/components/AddAgentModal.tsx (uniqueId / the single-hire submit path)"
      to: "src/renderer/src/hooks/bulkSpawn.ts (batchAgentIds)"
      via: "the single-hire path delegates to the same exported id generator the team batch uses, so the test reaches the production path"
      pattern: "batchAgentIds"
    - from: "src/renderer/src/components/TeamReviewModal.tsx (the hire {n} handler)"
      to: "src/renderer/src/hooks/bulkSpawn.ts (batchAgentIds, spawnBatch)"
      via: "the team batch mints every member id through the SAME exported generator the collision test drives, and spawns through the SAME shared batch — otherwise the tested path and the shipped path diverge and round-3 #7 reopens on the other side"
      pattern: "batchAgentIds"
---

<objective>
Build SCALE-02's renderer half: extract the bulk-spawn concurrency shape `useRestoreTeam.ts` already
paid for (three real defects fixed in it — serial cost, completion-order roster corruption, one
rejected IPC call silently aborting every subsequent agent) into a shared function, then use it for a
NEW review sheet that opens when `importHire()` receives a team@1 result. Close the id-collision
hazard explicitly, per D-18/the UI-SPEC's STOP-AND-REPORT clause, by MOVING id generation out of
`AddAgentModal.tsx` — where `uniqueId` is module-private and no test can reach it — into an exported
`batchAgentIds(names, now)` in `bulkSpawn.ts` that BOTH the new team-hire batch AND the existing
single-hire submit path call. The collision test then drives the production id path instead of a copy
of it (round-3 #7/#35: a test that loads only `bulkSpawn.ts` proves nothing about a rule that lives
inside a component). `useRestoreTeam.ts` stays untouched by this fix — it generates no ids at all and
reuses persisted ones by design.

`useRestoreTeam.ts`, `spawnBatch` and `batchAgentIds` have NO existing test coverage today — `grep -rEn
"restoreTeam|RestoreTeam|useRestoreTeam|spawnBatch|batchAgentIds" test/` returns zero hits before this
plan (note `-E`: without it those `|`s are BRE literals and the command returns zero for the wrong
reason, which would make this an evidence claim that cannot fail — re-run with `-E`). This
plan's own claim that the extraction is "behavior-preserving" is therefore backed by a NEW test file
this plan writes, not by pre-existing coverage that "still passes unmodified."

Purpose: `useRestoreTeam.ts`'s loop is the ONLY concurrent-spawn shape in this codebase whose three
defects are already fixed in its comments; re-implementing it for team import is exactly how those
three fixed defects come back. Output: `bulkSpawn.ts` (with real test coverage), `TeamReviewModal.tsx`,
and the wired `AddAgentModal.tsx` import/export footer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-UI-SPEC.md

<interfaces>
From `src/renderer/src/hooks/useRestoreTeam.ts` (current, full function already read this session —
the shape to extract, not reimplement):
```
const restoredInOrder = await Promise.all([...restorableAgents].map(async (a): Promise<Agent | null> => {
  try {
    // build spawn args from `a`, call window.cth.spawnPty(...)
    if (res.ok) { restored++; return {...a, ...}; }
    else if ((res.error ?? '').includes('already exists')) { alreadyLive++; useStore.getState().removeRestorableAgent(a.id); }
    else { failures.push(`${a.name}: ${res.error ?? 'spawn failed'}`); }
  } catch (e) { failures.push(...); }
  return null;
}));
for (const restoredAgent of restoredInOrder) {
  if (restoredAgent) useStore.getState().addAgent(restoredAgent);
}
```
The shape to extract as a shared function: `Promise.all` over the input array with a per-item
try/catch returning `T | null`, then a SEPARATE sequential loop adding successes IN INPUT ORDER
(never completion order). Parameterize on the per-item async spawn function so `useRestoreTeam.ts`
(spawning `Agent`-shaped restorable entries) and the new team-import path (spawning
`HireManifest`-shaped members) share the identical concurrency/ordering/failure-isolation shape
without sharing the (very different) per-item spawn-argument construction.

**`useRestoreTeam.ts` generates no ids — verified this session, `grep -n "uniqueId"
src/renderer/src/hooks/useRestoreTeam.ts` returns zero hits.** It reuses each restored agent's
PERSISTED identity throughout: `const ptyId = a.ptyId ?? \`pty-${a.id}\`` and `id: ptyId`, `hive: {
id: a.id, ... }`. `uniqueId` exists ONLY at `AddAgentModal.tsx:131` (definition) and `:337` (its one
call site, the single-hire form) — re-verified this session: `grep -c "uniqueId"
src/renderer/src/components/AddAgentModal.tsx` is `2`, and `grep -c "toString(36)"` on that same file
is exactly `1` (line 132, inside `uniqueId`). The team-hire batch path this plan builds is the ONLY
place besides that single call site where a fresh id must be minted for several same-named members at
once. Both therefore move to ONE exported generator in `bulkSpawn.ts`; leaving the rule
module-private in the component is precisely what made the previously-mandated collision test
unwritable.

`test/load-ts.cjs`'s `resolveTs` handles `@shared/` and NOT `@/` (verified this session;
`test/renderer-components.test.cjs:59-67` documents why that must stay true). So `bulkSpawn.ts` may
reference `@/store/store`'s `Agent` type only through `import type` — a value import of `@/…` makes
the module unloadable by `loadTs`, and `test/bulk-spawn.test.cjs` installs no `Module._load` shim.

From `src/renderer/src/components/AddAgentModal.tsx` (current, verified this session):
```
function uniqueId(name: string): string {
  return `${name.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-${Date.now().toString(36)}`;
}
const hireCommand = (m: HireManifest): string => {   // closes over the component's `config` prop
  const prov: AgentProvider = m.provider ?? inferAgentProvider(config.defaultCommand);
  const base = buildSpawnCommand(config, m.model, prov);
  return m.commandFlags?.length ? `${base} ${m.commandFlags.join(' ')}` : base;
};
const importHire = async () => {
  setError(undefined);
  const res = await window.cth.importHireFile();   // now returns {ok, manifest?, team?, error?} after 03-04
  if (res.ok && res.manifest) applyManifest(res.manifest);
  else if (res.error && res.error !== 'cancelled') setError(res.error);
};
```
`buildSpawnCommand` (`config.ts:405`) and `tokenizeCommand` (`config.ts:347`) are DEFINED in
`src/renderer/src/store/config.ts`; `inferAgentProvider` is defined in `src/shared/agentProvider.ts:661`
and RE-EXPORTED from `config.ts:33` (all three re-verified this session, which is why
`useRestoreTeam.ts:3` imports all of them from `@/store/config`) — extracting
`hireCommand` there, alongside them, keeps it next to the functions it actually depends on (a
deliberate deviation from RESEARCH.md's suggestion of `src/shared/hire.ts`, which cannot import
renderer-only `buildSpawnCommand` without creating a new cross-boundary dependency).

`window.cth.spawnPty(...)`'s `hive` payload (from `useRestoreTeam.ts`'s own call at
`useRestoreTeam.ts:152`, re-read this session) carries `{id, name, provider, cwd, role, account,
accountPolicy}` — a team member's spawn descriptor needs the SAME shape, built from the manifest's
`name`/`provider`/`description` plus an operator-picked cwd (D-19: team members share ONE
operator-picked root, never per-member cwd).

**`role` comes from `description`, NOT from `goal`.** Both existing spawn paths agree, re-verified
this session: `AddAgentModal.tsx:363` is `role: description.trim() || undefined` and
`useRestoreTeam.ts:152` is `role: a.description`. The manifest's `goal` rides along as the ROSTER
agent's own `goal` field (`src/renderer/src/store/store.ts:43` declares `goal?: string`; the
single-hire path sets it at `AddAgentModal.tsx:397`), never as `role`. An earlier revision of this
plan said the descriptor was built from "`goal` (as `role`)" — that was wrong about this repo and is
corrected here; the executor must not reintroduce it.

**Both fields are untrusted, near-unbounded, and both reach the spawned agent — so the sheet must show
both** (round-3 finding, the goal half). `src/shared/hire.ts:182` is
`const goal = capped(o.goal, 4000, 'goal', errors)`, so a member's `goal` may legally carry 4,000
characters of attacker-authored instruction. `role` is interpolated into `identityText()`
(`src/main/hive.ts:1397`, `` `- Role: ${meta.role ?? ...}` ``) and that text is written to
`<agentDir>/identity.md` on EVERY spawn (`src/main/hive.ts:899`,
`writeFileSync(identity, this.identityText(meta), 'utf8'); // refresh on each spawn`); `goal` becomes
the hired agent's standing directive on the roster. `03-UI-SPEC.md:263`'s destructive-actions table
declares this sheet IS the confirmation for a bulk hire ("**None.** ... The review sheet **is** the
confirmation — it lists every member, every checkbox is inspectable"). But §S3a's row table
(`03-UI-SPEC.md:768-772`) ellipsizes `description` to one `nowrap` line and does not mention `goal` at
all — which would let a 3,900-character instruction ride into `identity.md` and into the standing goal
behind a tidy 200-character description that looks fine in the row. This plan therefore ADDS a
per-row review region to §S3a's row shape. That is an ADDITION to the contract, not a contradiction of
it: the ellipsized one-line `description` stays exactly as specified as the row's summary line, and
the new region sits beneath it. §S3a's own STOP-AND-REPORT clause covers the id half of this hazard;
this is its sibling, and it is closed here rather than accepted.

From 03-04 (wave 4, landed before this wave-5 plan): `TEAM_MAX_MEMBERS = 16` in
`src/shared/hire.ts`, enforced by `validateTeamManifest` — a team@1 file's member count is ALREADY
bounded there, independent of `TEAM_MAX_BYTES`. This plan's threat register cites that constant by
name (not the byte cap) for the "large team file renders hundreds of rows" concern, since the byte cap
alone does not bound member count (a worst-case minimal-member file could otherwise fit thousands of
rows under 256KB).

From `.planning/phases/03-scale-and-observability/03-UI-SPEC.md` §S3a (the review sheet contract —
header line, per-member row shape, duplicate-name default-unchecked with a `name taken` note, no
confirmation dialog, per-member failure copy) and §Copywriting (button labels: `hire {n}`, `hiring
{done}/{n}...`, `cancel`; the import button relabels to `import...` with `title="Import a hire or a
team file (.json)"`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extract the shared bulk-spawn shape into a REAL test file, and move id generation into an exported, loadable batchAgentIds</name>
  <files>src/renderer/src/hooks/bulkSpawn.ts, src/renderer/src/hooks/useRestoreTeam.ts, src/renderer/src/store/config.ts, src/renderer/src/components/AddAgentModal.tsx, test/bulk-spawn.test.cjs</files>
  <read_first>src/renderer/src/hooks/useRestoreTeam.ts (the full restoreTeam function), src/renderer/src/components/AddAgentModal.tsx:131-133 (uniqueId — the ONLY id-generating site in this whole flow), src/renderer/src/components/AddAgentModal.tsx:156-160 (hireCommand — re-verified this session), test/load-ts.cjs (the loader this new test file uses — note `resolveTs` resolves `@shared/` and NOT `@/`), test/renderer-components.test.cjs:55-70 (READ-ONLY, owned by 03-08 this same wave — the documented reason `@/` stays unresolvable in loadTs)</read_first>
  <behavior>
    - A shared function `spawnBatch<T>(items: readonly T[], spawnOne: (item: T, index: number) =>
      Promise<Agent | null>): Promise<{ ok: Agent[]; failures: string[] }>` runs every item's
      `spawnOne` CONCURRENTLY (`Promise.all`), catches a per-item throw so one rejection never aborts
      the batch, and returns successes in the ORIGINAL array order (never completion order) alongside
      a flat failure-message list.
    - A REAL test — `test/bulk-spawn.test.cjs`, loading `bulkSpawn.ts` via `test/load-ts.cjs` and
      driving `spawnBatch` directly with a fake `spawnOne` under controlled, INVERTED completion
      timing (the last item resolves first) — proves: (a) all items' `spawnOne` calls start before
      any resolves (concurrency, not serial), (b) successes come back in INPUT order regardless of
      completion order, (c) one item's `spawnOne` throwing leaves the other N-1 in `ok`, with the
      thrown item's message in `failures`. This is the coverage `useRestoreTeam.ts`'s own behavior
      claims already existed for — it did not; this file is what makes the claim true.
    - `useRestoreTeam.ts`'s `restoreTeam()` is refactored to CALL `spawnBatch(restorableAgents, ...)`
      with its existing per-item spawn closure passed as the callback — the same concurrency/
      ordering/failure-isolation shape it always ran, now shared rather than duplicated. This is a
      refactor verified by `test/bulk-spawn.test.cjs`'s new coverage of the extracted shape, not by
      any prior test of `useRestoreTeam.ts` itself (there was none).
    - `export function batchAgentIds(names: readonly string[], now: number): string[]`, in
      `bulkSpawn.ts`, is THE production id generator. `now` is a PARAMETER, never a `Date.now()` call
      inside the function, so a test pins the clock without mocking a global.
      `batchAgentIds(['Jim','Jim','Pam'], 1700000000000)` returns THREE ids, each slugged from its
      name (`jim-…`, `jim-…`, `pam-…`) and all three DISTINCT — a running per-slug counter
      disambiguates repeats WITHIN the one call. Identical milliseconds are the normal case here, not
      an edge case: every id in a batch is minted from the same `now`.
    - That counter keys on the SLUG, never the raw name, so two DISTINCT names that slug to the same
      string also get distinct ids: `batchAgentIds(['Jim B','jim-b'], 1700000000000)` returns two
      different ids. This is UI-SPEC §S3a's explicitly flagged half of the hazard ("not collision-safe
      under a bulk spawn even for *distinct* names, if two slugs happen to match"), which a
      name-keyed counter would leave open.
    - BOTH id-minting callers route through `batchAgentIds`, which is what makes the test above a
      test of production code rather than of a duplicate: the team-hire batch (Task 2) calls it ONCE,
      synchronously, before any spawn fires, and `AddAgentModal.tsx`'s single-hire submit path takes
      its id from the same function. After this task no slug/timestamp arithmetic remains in
      `AddAgentModal.tsx` — round-3 #7/#35: `uniqueId` was module-private, so the previously-mandated
      collision test could not reach the code it claimed to cover.
    - `bulkSpawn.ts` is loadable by `test/load-ts.cjs` with no shim: any `@/store/…` reference in it
      is `import type` only (erased at transpile), never a value import, because `resolveTs` does not
      resolve `@/`.
    - `useRestoreTeam.ts` is UNTOUCHED by the id fix — it generates no ids and is not part of this
      hazard.
    - `hireCommandFor(manifest, config)` (moved into `store/config.ts`, exported) produces the
      identical command string `AddAgentModal.tsx`'s current inline `hireCommand` closure would have,
      for the same inputs.
  </behavior>
  <action>
    Create `src/renderer/src/hooks/bulkSpawn.ts` exporting `spawnBatch<T>` per `<behavior>` above,
    extracting the exact concurrency/ordering/failure-isolation shape from `useRestoreTeam.ts`'s
    `restoreTeam()` (the `Promise.all` + per-item try/catch + input-order re-assembly loop) — do not
    change the semantics, only lift the shape out. Refactor `useRestoreTeam.ts` to call `spawnBatch`
    with its existing per-item spawn closure (unchanged logic, now just the callback argument). In
    `store/config.ts`, add an exported `hireCommandFor(m: HireManifest, config: HarnessConfig): string`
    with the SAME body as `AddAgentModal.tsx`'s current `hireCommand` closure (provider inference +
    `buildSpawnCommand` + flag-join), since `buildSpawnCommand`/`inferAgentProvider` already live in
    this file. Close the id-collision hazard (the STOP-AND-REPORT hazard, resolved explicitly rather than
    inherited) by MOVING generation out of the component, not by widening a private helper. Add to
    `bulkSpawn.ts`: `export function batchAgentIds(names: readonly string[], now: number): string[]`
    — slug each name exactly as `AddAgentModal.tsx:132` does today
    (`name.toLowerCase().replace(/[^a-z0-9]+/g, '-')`), append `now.toString(36)`, and append a
    further `-2`, `-3`, … from a running per-slug counter for each REPEAT within the same call, so
    `['Jim','Jim']` yields `jim-<t>` and `jim-<t>-2`. Then DELETE the slug/timestamp arithmetic from
    `AddAgentModal.tsx`: `uniqueId` becomes a one-line delegate — `return batchAgentIds([name],
    Date.now())[0];` — keeping the single-hire call site at `:337` unchanged in shape while routing
    it through the same exported path the team batch uses. Keep `bulkSpawn.ts` free of value-level
    `@/…` imports (`import type` only) so `test/bulk-spawn.test.cjs` can load it with plain `loadTs`.
    Put a comment at `batchAgentIds` naming the hazard it closes and stating why `useRestoreTeam.ts`
    is out of scope for it (no `uniqueId` call, no id generation, reuses persisted ids). Do NOT touch
    `useRestoreTeam.ts` for this fix. Write
    `test/bulk-spawn.test.cjs` per `<behavior>` — this is a NEW file, not an extension of an existing
    one, since none covers this code today.
  </action>
  <acceptance_criteria>
    - `grep -n "export function spawnBatch" src/renderer/src/hooks/bulkSpawn.ts` matches `>= 1`
    - `grep -n "spawnBatch(" src/renderer/src/hooks/useRestoreTeam.ts` matches `>= 1`
    - `grep -n "export function hireCommandFor" src/renderer/src/store/config.ts` matches `>= 1`
    - `grep -n "export function batchAgentIds" src/renderer/src/hooks/bulkSpawn.ts` matches `>= 1`
    - `grep -c -F "batchAgentIds(" src/renderer/src/components/AddAgentModal.tsx` is `>= 1` (baseline
      today: `0`) — note the trailing `(` and `grep -F`: this matches a CALL, so an executor that adds
      the import line and then mints ids some other way does not satisfy it. Paired with the
      `toString(36)` count below, the two together are what make the single-hire path demonstrably the
      SAME path `test/bulk-spawn.test.cjs` drives (round-3 #7)
    - `grep -c "toString(36)" src/renderer/src/components/AddAgentModal.tsx` is exactly `0` (baseline
      today: `1`, at `:132` inside `uniqueId` — this is the ONLY occurrence in the file, re-verified
      this session, so the count dropping to 0 detects the arithmetic actually leaving the component
      and cannot be satisfied by an unrelated edit)
    - `grep -n "uniqueId" src/renderer/src/hooks/useRestoreTeam.ts` matches exactly `0` (confirms the
      fix was NOT mis-applied to the one file that has no id-generation hazard), AND
      `grep -c "spawnBatch(" src/renderer/src/hooks/useRestoreTeam.ts` is `>= 1` (the positive half:
      the file really was rewired, not merely left alone)
    - The production id path is reachable from a test and collision-free — run verbatim:
      `node -e "const loadTs=require('./test/load-ts.cjs');const m=loadTs('src/renderer/src/hooks/bulkSpawn.ts');if(typeof m.batchAgentIds!=='function'){console.error('FAIL: batchAgentIds is not exported from bulkSpawn.ts');process.exit(1);}const ids=m.batchAgentIds(['Jim','Jim','Pam'],1700000000000);if(!Array.isArray(ids)||ids.length!==3){console.error('FAIL: expected 3 ids, got '+JSON.stringify(ids));process.exit(1);}if(new Set(ids).size!==3){console.error('FAIL: same-name collision — '+ids.join(','));process.exit(1);}if(!ids[0].startsWith('jim-')||!ids[1].startsWith('jim-')||!ids[2].startsWith('pam-')){console.error('FAIL: ids are not name-slugged — '+ids.join(','));process.exit(1);}const slug=m.batchAgentIds(['Jim B','jim-b'],1700000000000);if(new Set(slug).size!==2){console.error('FAIL: distinct names sharing one slug collided — '+slug.join(','));process.exit(1);}console.log('PASS '+ids.join(',')+' '+slug.join(','));"`
      — must print `PASS` and exit 0. It fails today for the only reason that matters: `bulkSpawn.ts`
      does not exist and nothing exports `batchAgentIds`. (`loadTs`'s signature re-verified this
      session — `test/load-ts.cjs` does `module.exports = function loadTs(relativePath)` and resolves
      that path against the REPO ROOT, so this line works from the repo root with no wrapper.)
    - `node --test test/bulk-spawn.test.cjs` passes with 0 failures, AND all four required cases are
      present as separately-titled tests, checked mechanically rather than by eye:
      `grep -c "^test(" test/bulk-spawn.test.cjs` is `>= 4` (the repo's top-level test-title
      convention — `grep -c "^test(" test/roster.test.cjs` is `11`, `test/breaker.test.cjs` is `25`,
      re-verified this session), and each of these four tokens appears at least once, one per case
      title: `grep -c -F "concurrent" test/bulk-spawn.test.cjs`, `grep -c -F "input order" …` and
      `grep -c -F "failure isolation" …` are each `>= 1` (grep -F, so no BRE alternation can silently
      match), and the fourth is anchored to a TITLE rather than to anywhere in the file —
      `grep -c "^test(.*batchAgentIds" test/bulk-spawn.test.cjs` is `>= 1` — because a bare
      `batchAgentIds` token would be satisfied by the `loadTs` line at the top of the file and would
      therefore pass even with the collision case deleted, which is exactly the vacuity round-3 #7 is
      about. (`^test(` is the repo's real title shape: `test/roster.test.cjs:40` is
      `test('a roster round-trips through the file', () => {`, re-verified this session.) Dropping any
      one of the four — concurrency (every `spawnOne` call starts before any resolves), input-order
      preservation under inverted completion, per-item failure isolation, or `batchAgentIds` under a
      fixed clock — fails a named check rather than merely lowering a count
  </acceptance_criteria>
  <verify>
    <automated>node --test test/bulk-spawn.test.cjs test/hire-manifest.test.cjs</automated>
  </verify>
  <done>spawnBatch is extracted and proven, by a real new test file, to preserve useRestoreTeam.ts's concurrency/order/failure-isolation behavior; id generation now lives in an exported batchAgentIds that BOTH the single-hire path and the team batch call, with test/bulk-spawn.test.cjs driving that exact function under a fixed clock to prove two same-batch same-name members get distinct ids; useRestoreTeam.ts is explicitly left untouched by the id fix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TeamReviewModal — the import review sheet, wired via bulk-spawn, with a real test that can see it</name>
  <files>src/renderer/src/components/TeamReviewModal.tsx, src/renderer/src/components/AddAgentModal.tsx, test/team-review.test.cjs</files>
  <read_first>.planning/phases/03-scale-and-observability/03-UI-SPEC.md §S3a (full contract), 03-UI-SPEC.md:768-772 (the row table this plan ADDS a review region beneath — the summary line's ellipsis stays), 03-UI-SPEC.md:263 (the destructive-actions row that makes this sheet the ONLY confirmation), src/renderer/src/components/AddAgentModal.tsx:363 and src/renderer/src/hooks/useRestoreTeam.ts:152 (READ-ONLY evidence that `role` comes from `description`, never from `goal`), src/renderer/src/components/Modal.tsx (the primitive — role=dialog, focus trap, Escape-closes-top-only, already owned), src/renderer/src/hooks/bulkSpawn.ts (Task 1), src/renderer/src/store/config.ts (hireCommandFor, Task 1), test/add-agent-export.test.cjs (03-04's dedicated-new-test-file precedent for a component this large — mirror its ownership pattern, not its stub content)</read_first>
  <behavior>
    - Given a validated `{members: HireManifest[]}` (already bounded to `TEAM_MAX_MEMBERS` by 03-04's
      validator before this component ever sees it) with two members sharing a name, the sheet
      renders both rows but the SECOND one defaults its checkbox UNCHECKED with a `name taken` note
      beside it — the FIRST occurrence of a repeated name stays checked by default (only later
      duplicates are flagged).
    - This duplicate-detection rule is a PURE, EXPORTED function — `export function
      markDuplicates(members: HireManifest[]): Array<{member: HireManifest; checked: boolean; note:
      string | null}>` — callable and assertable WITHOUT mounting React (round-2 findings #26/#41:
      the prior verification pointed at `test/renderer-components.test.cjs`, a file this plan neither
      owns nor modifies and that cannot see `TeamReviewModal` at all — its Module._load shim loads
      only `PixelBadge.tsx`/`BlockedBanner.tsx`/`AgentCard.tsx`/`store.ts`/`agentProvider.ts`). The
      component calls `markDuplicates(members)` to derive each row's initial checked state and note;
      it does not re-implement the rule inline in JSX.
    - **The sheet has a FOLDER REGION, and `hire {n}` is DISABLED until a root is chosen (round-7
      finding #7 — this is the plan-checker's original blocker, which earlier rounds addressed in prose
      without ever giving the sheet a cwd).** ONE operator-picked root for the whole team (D-19: never
      per-member), rendered as literal text above the member rows so the operator can SEE which
      directory up to `TEAM_MAX_MEMBERS` agents from an untrusted file are about to get read/write
      access to. It defaults to `AddAgentModal`'s existing `cwd` state, passed in as a prop. With no
      root set, the hire control is disabled and carries the same "Pick a folder first" copy
      `AddAgentModal`'s single-hire submit guard already uses — reuse that string, do not invent one.
      **`cwd` is the field that decides the blast radius of a team import; a sheet that never shows or
      passes it is not a review sheet.**
    - Clicking `hire {n}` (n = currently-checked count) calls `spawnBatch` over exactly the checked
      members, threading the operator-picked root into EVERY member's spawn payload as `cwd`, using
      `hireCommandFor` to build each member's command and Task 1's exported
      `batchAgentIds(checkedMembers.map(m => m.name), Date.now())` — called ONCE, synchronously,
      before the batch fires — to mint every member's id, so the ids the sheet spawns with are the
      ids `test/bulk-spawn.test.cjs` already proved collision-free. Without the `cwd` thread-through
      the spawn is rejected by `useRestoreTeam.ts`'s own `if (!command || !a.cwd)` guard shape.
    - One member's spawn failure renders that row's OWN error copy in place and leaves every other
      member's spawn to complete — the sheet never surfaces one sheet-level error that hides which
      member failed. The copy is UI-SPEC §Copywriting:250 verbatim: `{name} did not start: {reason}.
      The rest of the team is hired — you can add {name} on their own.`
    - The sheet is a `Modal` (inherits `role="dialog"`, focus trap, Escape) titled `Import team`, with
      the header line `{n} agents in this file. Uncheck anyone you do not want.` and a `cancel` ghost
      button beside `hire {n}`/`hiring {done}/{n}…` while running.
    - EVERY row renders that member's FULL `goal` and FULL `description` in a review region beneath
      the row's one-line summary — wrapping text, bounded by a `maxHeight` with `overflow: auto` so a
      4,000-character goal scrolls INSIDE its own row instead of growing the sheet, and NEVER the
      summary line's `nowrap` + `textOverflow: ellipsis`, which hides exactly the text this region
      exists to show. A member carrying no `goal` renders an explicit `no goal` note rather than an
      empty gap, so "there is nothing to read" stays distinguishable from "the field was dropped".
      This is the sheet's confirmation obligation, not decoration: `03-UI-SPEC.md:263` makes this
      sheet the ONLY confirmation a bulk hire gets, `description` becomes `hive.role` and lands in
      `<agentDir>/identity.md` on every spawn (`src/main/hive.ts:1397` -> `src/main/hive.ts:899`),
      `goal` becomes the hired agent's standing directive, and `src/shared/hire.ts:182` caps `goal` at
      4,000 characters of untrusted, colleague-or-gallery-supplied text. The summary line stays
      exactly as §S3a specifies; this region is an addition beneath it.
    - `TeamReviewModal.tsx` is LOADABLE by plain `test/load-ts.cjs` and renderable by
      `react-dom/server`'s `renderToStaticMarkup` with NO `Module._load` shim: every cross-directory
      import is RELATIVE (`./Modal`, `../store/config`, `../hooks/bulkSpawn`, `../store/store`) or an
      `import type`, never a value-level `@/…` — `resolveTs` does not resolve `@/`. Verified
      achievable this session, with existing precedent: `loadTs` loads
      `src/renderer/src/components/Modal.tsx`, `src/renderer/src/store/config.ts` and
      `src/renderer/src/store/store.ts` clean today; rendering `Modal` with a child through
      `renderToStaticMarkup` returned 1,091 characters of real markup containing the child's text; and
      `SidebarSplitter.tsx:2` already imports `../store/sidebarLayout` relatively. `react` and
      `react-dom` are existing production dependencies — this adds no dependency.
  </behavior>
  <action>
    Create `TeamReviewModal.tsx` per UI-SPEC §S3a's full region table (header line, per-member
    `<label>`-wrapped checkbox row with name/engine/model/description, duplicate-name unchecked
    default with the `name taken` note, footer `cancel`/`hire {n}`), reading its members prop from
    the caller and deriving each row's initial state via the exported `markDuplicates` function
    described in `<behavior>` (define it either in this same file, exported, or in
    `store/config.ts` beside `hireCommandFor` — the executor's call, but it must be a plain export,
    never inline JSX logic a test cannot reach). Wire it into `AddAgentModal.tsx`'s `importHire()`:
    add a THIRD branch — `res.ok && res.team` — that opens `TeamReviewModal` (state-controlled, e.g.
    a new `teamToReview` state) instead of calling `applyManifest` (which stays exclusively for the
    single-agent `res.manifest` branch, unchanged). Relabel the import button from `import hire…` to
    `import…`, with `title="Import a hire or a team file (.json)"` (per D-17: the same button now
    accepts both formats, so the old label would be false). Create a NEW, DEDICATED test file —
    `test/team-review.test.cjs` — that loads `markDuplicates` directly (via `loadTs`, no React
    needed since it is a plain function) and asserts: two same-named members → second is `checked:
    false` with a non-null `note`, first stays `checked: true`; three members all distinct names →
    all `checked: true`, all `note: null`. Add a per-member-failure-isolation case driving
    `spawnBatch` (from Task 1, already tested there for the general shape) with a fixture where the
    SECOND of three `spawnOne` calls rejects, asserting the first and third still land in `ok` and the
    second's message lands in `failures` — reusing Task 1's fixture style, not re-deriving it.
    Then add the REVIEW-REGION render case, the one assertion in this plan that can tell "the goal is
    on screen" from "the goal was silently dropped": in the same test file, `require('react')` and
    `require('react-dom/server')` (both already production dependencies), `loadTs`
    `src/renderer/src/components/TeamReviewModal.tsx`, render it through `renderToStaticMarkup` with a
    ONE-member fixture whose `goal` is the sentinel string `ZZ-GOAL-SENTINEL-ZZ` and whose every other
    field carries that sentinel nowhere, and assert the sentinel IS present in the returned markup.
    Keep the sentinel out of a `title=` attribute — assert on text the operator can read. A component
    that renders nothing, that drops `goal`, or that only stores it in a prop fails this case. Do NOT
    reach for a jsdom/RTL harness or extend another file's `Module._load` shim: the relative-import
    rule stated above is what makes the plain `loadTs` render work, and D-27 already rejected the
    heavier option.
  </action>
  <acceptance_criteria>
    - `grep -n "export function TeamReviewModal" src/renderer/src/components/TeamReviewModal.tsx` matches `>= 1`
    - `grep -n "export function markDuplicates" src/renderer/src/components/TeamReviewModal.tsx src/renderer/src/store/config.ts` matches `>= 1` total (round-2 #26/#41's fix — a pure, exported, directly-testable duplicate rule)
    - `grep -n "res.team" src/renderer/src/components/AddAgentModal.tsx` matches `>= 1`
    - **The folder region exists and gates the hire (round-7 #7), as a positive/negative pair:**
      `grep -c -F "cwd" src/renderer/src/components/TeamReviewModal.tsx` is `>= 1` (measured **0**
      today — the file does not yet exist, so any hit proves the wiring was written) AND
      `grep -c -F "disabled" src/renderer/src/components/TeamReviewModal.tsx` is `>= 1` (the hire
      control's guard). A `TeamReviewModal` that renders member rows but never mentions `cwd` fails
      this pair, which is exactly the state three earlier rounds left it in.
    - `grep -c -F "cwd" test/team-review.test.cjs` is `>= 1` — measured **0** today. The existence pin:
      the test must actually drive the no-root-chosen case and assert the hire control is disabled,
      not merely load the component.
    - **The gate itself is pinned, not just the tokens** (round-8 finding #11 — three token-presence
      greps are each satisfiable without the gate existing): `grep -c -F "Pick a folder first"
      src/renderer/src/components/TeamReviewModal.tsx` is `>= 1` (measured **0** in that file today —
      the string exists only in `AddAgentModal.tsx`, so this cannot be satisfied by the existing
      single-hire guard), AND a `renderToStaticMarkup` case in `test/team-review.test.cjs` that
      renders with `cwd: undefined` and asserts the markup matches `/disabled/` at the hire control.
      Token presence proves the word is in the file; only the render proves the button is off.
    - The import button's label really changed, checked as a pair of literal counts (both re-verified
      against today's source this session, so neither half is vacuous):
      `grep -c -F "import hire…" src/renderer/src/components/AddAgentModal.tsx` is exactly `0`
      (baseline today: `1`, the button's JSX child at `:1102`) AND
      `grep -c -F "import…" src/renderer/src/components/AddAgentModal.tsx` is `>= 1` (baseline today:
      `0` — `import hire…` does not contain `import…` as a substring, so the positive half cannot be
      satisfied by the old label). `grep -F`, not a quoted alternation, because the label is bare JSX
      text and carries no surrounding quotes to match on
    - The team batch spawns through the SAME two functions Task 1 tested, not through a second copy
      of either rule — otherwise round-3 #7 simply reopens on the team side, where the collision it
      names actually happens: `grep -c -F "batchAgentIds(" src/renderer/src/components/TeamReviewModal.tsx`
      is `>= 1` AND `grep -c -F "spawnBatch(" src/renderer/src/components/TeamReviewModal.tsx` is
      `>= 1` (both baselines today: the file does not exist, so both are `0`; the trailing `(` and
      `grep -F` mean an import line alone does not satisfy either). Guarding the negative half:
      `grep -c -F "toString(36)" src/renderer/src/components/TeamReviewModal.tsx` is exactly `0` and
      `grep -c -F "replace(/[^a-z0-9]+/g" src/renderer/src/components/TeamReviewModal.tsx` is exactly
      `0` — no slug or timestamp arithmetic re-derived inside the sheet. These two negatives cannot
      fail on their own and are not the detector; the two positive counts above are
    - `grep -n "name taken" src/renderer/src/components/TeamReviewModal.tsx` matches `>= 1`
    - `grep -c -F "did not start" src/renderer/src/components/TeamReviewModal.tsx` is `>= 1` — the
      per-member failure copy UI-SPEC:250 mandates is actually in the component (round-3 #32; the
      string appears nowhere in the repo's renderer components today, re-verified this session)
    - The untrusted `goal` really reaches the SCREEN, asserted on rendered markup rather than by
      grepping the component for a variable name: `grep -c -F "renderToStaticMarkup"
      test/team-review.test.cjs` is `>= 1` AND `grep -c -F "ZZ-GOAL-SENTINEL-ZZ"
      test/team-review.test.cjs` is `>= 2` (the sentinel appears once as the fixture member's `goal`
      and once in the assertion, so neither half can be dropped) AND `node --test
      test/team-review.test.cjs` is green. A sheet that omits the goal region, renders an empty
      element, or fails to load under plain `loadTs` turns that case red. Baselines today, re-verified
      this session: `test/team-review.test.cjs` does not exist and
      `src/renderer/src/components/TeamReviewModal.tsx` does not exist, so every count above is `0`
      before the executor does anything
    - `grep -c -F "no goal" src/renderer/src/components/TeamReviewModal.tsx` is `>= 1` — the
      declared-gap branch for a member that carries no goal at all (baseline `0`: the file does not
      exist)
    - `test -f test/team-review.test.cjs` (the new, dedicated, plan-owned test file exists)
    - `node --test test/team-review.test.cjs` passes with 0 failures, by test title covering BOTH
      "second same-named member defaults unchecked" and "distinct names all default checked" (round-2
      #26 — name the two cases so their absence is detectable, not only a pass/fail count)
    - **EXISTENCE PINS for those two titles**, since "by test title" is only detectable if something
      actually greps the titles: `grep -c -F "second same-named member defaults unchecked"
      test/team-review.test.cjs` is `>= 1` AND `grep -c -F "distinct names all default checked"
      test/team-review.test.cjs` is `>= 1`. Both are red today by construction: the file does not exist yet, so the grep exits 2 (No such file) rather than printing 0 — either way the criterion is not satisfied.
      Without these, the duplicate-name defaulting — the UI-SPEC §S3a control that stops a
      double-import silently doubling the floor — could be replaced by unrelated passing assertions
      and this bullet would still read green.
    - This plan touched neither file its WAVE-5 SIBLING 03-08 owns. `test/renderer-components.test.cjs`
      and `test/renderer-runstate.test.cjs` are BOTH in 03-08-PLAN.md's `files_modified` — re-verified
      this session: `03-08-PLAN.md:5` is `wave: 5`, `:14` is `test/renderer-runstate.test.cjs` and
      `:16` is `test/renderer-components.test.cjs` (03-04 does NOT own either one, contrary to this
      criterion's older rationale — round-3 #16/#39). The check must be scoped to THIS PLAN'S OWN
      COMMITS, never to a sha range: this phase runs `use_worktrees: false`, so 03-08 commits onto the
      same branch during this same wave and lands INSIDE any `$BASE..HEAD` range 03-06 records — the
      previous range-based form would have gone red for a sibling's work while 03-06 touched nothing,
      and an executor's only ways out were force-passing it or reverting a sibling. Use instead the
      plan-id-scoped pair 03-04 Task 3 already uses (`03-04-PLAN.md:496-499`, re-read this session),
      positive half first so it cannot pass by matching nothing at all:
      `git log --oneline --grep="03-06" -- test/team-review.test.cjs | wc -l` is `>= 1` (this plan's
      own commits DO touch the test file it owns) AND
      `git log --oneline --grep="03-06" -- test/renderer-components.test.cjs test/renderer-runstate.test.cjs | wc -l`
      is exactly `0` (not one of them touches 03-08's two files). Do NOT run either 03-08 file as a
      gate: 03-08 is rewriting both in this same wave, so their green/red is not this plan's to
      control
  </acceptance_criteria>
  <verify>
    <automated>node --test test/team-review.test.cjs</automated>
  </verify>
  <done>TeamReviewModal exists, wired to a real team@1 import, duplicate names default unchecked via a pure exported markDuplicates function with real test coverage that can actually see it, per-member failures are isolated and visible, and every member's full untrusted goal and description are readable in the sheet before hire — proven by a renderToStaticMarkup assertion, with the sibling-ownership pin scoped to this plan's own commits rather than to a sha range a wave-5 sibling also lands in.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: containment check — the review sheet at 3, 8 and 16 members</name>
  <what-built>TeamReviewModal.tsx, a Modal-based review sheet whose row count varies with the
  imported file's member count (bounded to 16 by 03-04's TEAM_MAX_MEMBERS).</what-built>
  <how-to-verify>
    1. Run the dev app (`npm run dev`).
    2. Build three test team@1 files (or use `team:export` from a roster with 3/8/16 agents hired,
       then re-import each) with 3, 8 and 16 members respectively.
    3. Open Add Agent -> `import…`, pick each file in turn.
    4. At each member count, confirm: no row's name/engine/model/description text is clipped in a way
       that hides information, the sheet scrolls internally rather than growing off-screen, the
       footer (`cancel`/`hire {n}`) stays visible and reachable without the page needing to scroll
       past it, and duplicate names (construct at least one deliberately) show `name taken` and start
       unchecked.
    5. Give ONE member in the 16-member file a deliberately long goal (paste ~3,000 characters ending
       in a sentence you will recognise) and confirm you can actually READ that whole goal in its row
       — the region wraps and scrolls inside the row, the sheet still fits, and no part of the goal is
       reachable only by hovering a tooltip. This is the security half of the check: the sheet is the
       only confirmation a bulk hire gets, and a goal you cannot read is a goal you cannot refuse.
  </how-to-verify>
  <files>(none -- operator verification of the running app)</files>
  <action>Pause execution and present the numbered verification steps above to the operator verbatim. Do not write the SUMMARY until the operator answer is returned.</action>
  <verify><human-check>Operator confirms no clipped text, internal scroll, a reachable footer, and correct duplicate-name handling at 3, 8 and 16 members.</human-check></verify>
  <done>The operator has exercised the review sheet at 3, 8 and 16 members and the result is recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" or describe the specific row/width that broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Imported team@1 file <-> the review sheet | Already validated by `validateTeamManifest`/`validateHireManifest` (main process, per member) before this UI ever sees it — the sheet renders trusted, validated data only |
| Operator checkbox selection <-> `spawnBatch` | Only checked members are ever passed to the spawn batch; the sheet is the human-in-the-loop gate this format's security model requires (manifests never auto-spawn) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-06a | Elevation of Privilege | a manipulated member row spawning with an unreviewed command | accept | Every member's command is rebuilt LOCALLY via `hireCommandFor` from the provider preset — the manifest can never inject the spawn binary, matching the existing single-hire security model unchanged |
| T-03-06b | Denial of Service | a large team file rendering hundreds of rows | mitigate | The member cap lives in the validator (03-04's `TEAM_MAX_MEMBERS = 16`, `src/shared/hire.ts`) — NOT the byte cap, which alone would not bound member count; this sheet's own containment is verified live in Task 3 at 16 members, the actual ceiling |
| T-03-06c | Repudiation | a failed member's error being lost/overwritten by a later success | mitigate | Per-member failure copy renders in place on that row per `<behavior>`, never collapsed into one sheet-level toast |
| T-03-06d | Elevation of Privilege | a shared team file smuggling attacker-authored instructions into the hired agent behind a benign-looking row — `goal` is untrusted and capped only at 4,000 chars (`src/shared/hire.ts:182`), `description` becomes `hive.role` and is written into `<agentDir>/identity.md` on every spawn (`src/main/hive.ts:1397` -> `:899`), and `03-UI-SPEC.md:263` makes this sheet the ONLY confirmation a bulk hire gets | mitigate | The sheet renders BOTH fields in full per member — wrapping, height-bounded, internally scrollable, never nowrap-ellipsized — so nothing that reaches the agent is invisible at review time. `validateTeamManifest` (03-04) already strips `commandFlags`, `skills` and `mcpServers` precisely because no review surface exists for them; this plan builds the review surface for the two fields it does pass through rather than stripping them, since a team file without goals is a roster of blank agents (D-16 exports goals on purpose, `03-UI-SPEC.md:819`). Verified by a `renderToStaticMarkup` assertion, not by inspection |
</threat_model>

<verification>
- `node --test test/bulk-spawn.test.cjs test/team-review.test.cjs test/hire-manifest.test.cjs` — 0
  failures (the two renderer-* files 03-08 is rewriting in this same wave are deliberately NOT gated
  here — round-3 #16/#39; `test/hire-manifest.test.cjs` stays because 03-04 lands in wave 4, before
  this one)
- `npm run typecheck` — 0 errors
- Task 3 checkpoint approved
</verification>

<success_criteria>
- `spawnBatch` is the ONE shared bulk-spawn shape, used by both `useRestoreTeam.ts` and the new team
  import path, and proven by a REAL new test file rather than a claim of pre-existing coverage
- Agent-id generation is ONE exported function (`batchAgentIds` in `bulkSpawn.ts`) called by both the
  single-hire path and the team batch, so `test/bulk-spawn.test.cjs`'s collision case exercises the
  production path and not a module-private copy of the rule — `useRestoreTeam.ts` is explicitly,
  correctly left untouched by that fix
- The review sheet renders, defaults duplicate rows unchecked, and isolates per-member failure
- The sheet shows everything it is about to hand the agents: each member's FULL `goal` and FULL
  `description` are readable in it before `hire {n}` is clickable — a confirmation that hid the
  4,000-character field it was confirming (`src/shared/hire.ts:182`) would not be one — and that is
  proven by a `renderToStaticMarkup` assertion in `test/team-review.test.cjs`, not by grep
- The `hive` descriptor maps `description` -> `role` (matching `AddAgentModal.tsx:363` and
  `useRestoreTeam.ts:152`) and carries `goal` as the roster agent's `goal`; `goal` is never written
  into `role`
- The threat register cites the real member-count bound (`TEAM_MAX_MEMBERS`), not the byte cap
- The import button's label is honest about accepting both formats
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-06-SUMMARY.md` when done
</output>
