---
phase: 03-scale-and-observability
plan: 3
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/main/db.ts
  - src/main/hive.ts
  - src/main/timeline.ts
  - src/main/floor/boot.ts
  - src/main/index.ts
  - src/preload/index.ts
  - test/db-fts.test.cjs
  - test/timeline.test.cjs
  - test/boot-floor.test.cjs
  - test/hive-durability.test.cjs
  - test/hive-protocol-v2.test.cjs
autonomous: true
requirements_addressed: [SCALE-03]

must_haves:
  truths:
    - "Every hive event (spawn, message, drain, escalate, approval) is queryable by time range from SQLite, not only from a 64KB tail read of log.jsonl"
    - "A synthetic driver that pushes past the old 64KB tail-read cap still gets back the earliest hour's events from the events table, while logTail() no longer sees that same early data"
    - "Cost data for a bucket is computed via the clamped consecutive diff, never a sum, including across a mid-day session_id rollover"
    - "hive.ts still has zero `from 'electron'` imports after this plan lands"
    - "The events table never grows without bound — a retention delete runs on open, PROVEN by a test that inserts an expired row and asserts it is actually gone after reopen, not merely that the DELETE statement's text is present"
    - "A day's bucket query never ships the whole day's raw rows to the renderer — only 96 bucket summaries, plus one bucket's detail rows on demand"
    - "hive:timeline reports firstTs — the earliest event timestamp STILL STORED (not 'first event ever', since this plan's own retention delete moves MIN(ts) over time) — so a day the events table cannot speak to renders as missing, not as quiet"
    - "hive:timeline and hive:timelineBucket validate their renderer-supplied day/bucket-index arguments and return a discriminated error rather than throwing or querying with an unbounded/malformed value"
    - "BOTH channels answer in ONE discriminated shape on every path — success, validation failure, and store-unavailable alike — so no consumer can mistake a rejected day for an empty one: `{ok:true, ...}` or `{ok:false, error}`, never a bare payload and never a zeroed success"
    - "The record's start covers BOTH stores or neither: a day the events table cannot speak to but the cost ledger CAN is reported as `eventsAgedOut: true`, so 03-07 can never assert 'Nothing was recorded on {date}' over a day it is simultaneously drawing real cost bars for (the events table is retained for EVENTS_RETENTION_DAYS; cost-ledger.jsonl is never rotated — verified this session, hive.ts's only renameSync-based rotation, :2503, is log.jsonl's)"
    - "Zero-delta cost rows are dropped BEFORE the 200-row detail cap, and `total` counts DISPLAYABLE rows only — an idle floor's ~one-cost-sample-per-agent-per-30s beat can never crowd real events out of a bucket, nor inflate the truncation count with rows the UI would not have shown"
    - "The bucket-aggregation and detail-cap logic lives in a plain module test/db-fts.test.cjs's sibling test/timeline.test.cjs can load and call directly with node --test — NOT only inside index.ts, which no test in this repo can load"
  artifacts:
    - path: src/main/db.ts
      provides: "MIGRATIONS[2] — the events table, its two indexes, retention on open, recordEvent, eventsInRange, earliestEventTs"
    - path: src/main/timeline.ts
      provides: "the PURE, loadable bucket-aggregation module — summarizeDay (returning the pinned {ok:true, buckets, firstTs, eventsAgedOut} shape), bucketDetail (returning {ok:true, rows, truncated, total} after dropping zero-delta cost rows), parseDayParam, validateBucketIndex, and the BUCKET_MINUTES/BUCKET_DETAIL_ROW_CAP constants"
    - path: src/main/hive.ts
      provides: "a fourth constructor parameter (the injected events sink, fired from appendLog ONLY — never from appendCostLedger), applyCostRow hoisted to a module-level export with its diff logic unchanged and its return channel widened at BOTH exits (the null-task early exit and the final statement), wired at both its call sites, and dailyCostRows(dayStart, dayEnd)"
    - path: src/main/index.ts
      provides: "hive:timeline (day summary, 96 buckets, firstTs, eventsAgedOut) and hive:timelineBucket (one bucket's merged detail rows) as THIN handlers delegating to timeline.ts, each returning the ONE pinned discriminated shape on every path including the store-unavailable one"
  key_links:
    - from: "src/main/hive.ts (appendLog ONLY — never appendCostLedger)"
      to: "the injected events sink"
      via: "a constructor-injected callback, never a PersistStore import"
      pattern: "eventsSink"
    - from: "src/main/floor/boot.ts (new HiveManager(...))"
      to: "src/main/db.ts (persist)"
      via: "a fourth constructor argument closing over the module-level persist variable"
      pattern: "new HiveManager"
    - from: "src/main/index.ts (hive:timeline, hive:timelineBucket)"
      to: "src/main/timeline.ts (summarizeDay, bucketDetail, parseDayParam, validateBucketIndex)"
      via: "a direct function call — index.ts only fetches rows from persist/hive and hands them to the pure module"
      pattern: "timeline.ts"
    - from: "src/renderer (window.cth.hiveTimeline)"
      to: "src/main/index.ts (hive:timeline)"
      via: "ipcRenderer.invoke"
      pattern: "hive:timeline"
---

> **Requirements-ledger note — read before running the completion step.** This plan's
> frontmatter key is `requirements_addressed`, not `requirements`, and that is deliberate. D-07
> forbids ticking SCALE-03 in Phase 3. `execute-plan.md`'s `update_requirements` step only acts
> on a literal `requirements:` frontmatter key ("If no requirements field, skip") — this plan
> carries no such key, so `requirements.mark-complete` never runs for it. `requirements_addressed`
> exists only so `plan-phase.md`'s coverage gate still counts SCALE-03 as planned. If something
> tries to auto-tick SCALE-03 in `.planning/REQUIREMENTS.md` while finishing this plan, STOP —
> that is red-team finding #1. 03-09 (the last plan in this phase) asserts SCALE-03 is still
> `Pending` after every other plan lands.

<objective>
Land SCALE-03's storage layer: an `events` SQLite table as migration #3, fed by an injected sink
beside `appendLog`/`appendCostLedger` (never a `PersistStore` import into `hive.ts`), a NEW pure,
loadable aggregation module (`timeline.ts`) that computes day/bucket summaries, and the two
main-process IPC handlers a day-band UI will read from. `log.jsonl` stays as the agent-readable tail;
SQLite becomes the queryable-by-time-range authority. This plan does NOT build the timeline UI —
that is 03-07, which depends on this plan's IPC contract, including `firstTs`.

`index.ts` cannot be loaded by any test in this repo (`electron_1.app.on is not a function` under the
test harness's Electron stub) — three existing test files already document this limit in prose. That
means aggregation logic written directly inside `index.ts`'s handlers is exercised only by grep
assertions an empty stub implementation would also satisfy. This plan puts the actual bucket math,
the detail-row cap, `firstTs`, and argument validation into `src/main/timeline.ts` — a plain,
Electron-free module — so a new `test/timeline.test.cjs` can call it directly and assert real
behavior (96 buckets, envelope filtering, the 312-row truncation case, a malformed-argument
rejection). `index.ts`'s handlers become thin: fetch rows from `persist`/`hive`, hand them to
`timeline.ts`, return the result.

Purpose: nothing in this app can answer "what happened between 9am and 10am yesterday" today —
`logTail()`'s 64KB read is a window, not a time-range query, and cost-ledger.jsonl and log.jsonl are
never joined. This plan makes that answerable without adding a new dependency, and makes the answer
itself testable, not merely grep-shaped.
Output: migration #3, `timeline.ts`, the injected sink, `applyCostRow` as an exported shared function,
and `hive:timeline`/`hive:timelineBucket`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-RESEARCH.md
@.planning/phases/03-scale-and-observability/03-UI-SPEC.md
@docs/adr/0005-cumulative-cost-ledger.md

<interfaces>
From `src/main/db.ts`'s `MIGRATIONS` array (append-only — add a THIRD entry, never edit the first
two):
```
const MIGRATIONS: Array<(db: Database.Database) => void> = [
  (db) => { /* kv, command_history */ },
  (db) => { /* memory_fts */ }
  // <- append migration #3 here
];
```
Schema, locked by D-20:
```sql
CREATE TABLE IF NOT EXISTS events (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  floor_id TEXT NOT NULL,
  ts INTEGER NOT NULL,
  kind TEXT NOT NULL,
  agent_id TEXT,
  task_id TEXT,
  session_id TEXT,
  payload TEXT
);
CREATE INDEX IF NOT EXISTS idx_events_ts ON events(ts);
CREATE INDEX IF NOT EXISTS idx_events_floor_ts ON events(floor_id, ts);
```
`floor_id` is a constant (e.g. `'default'`) until SCALE-01's project key exists elsewhere — verified
this session: `floorId|floor_id|windowId` returns 0 hits across `src/main`/`src/shared` today. Per
D-40, this column's NAME is `floor_id` (matching the locked schema literally) but every plan's PROSE
must say "project", never "floor" — the column name is the one place D-40's rule does not apply,
because the schema text itself is locked verbatim in D-20.

From `src/main/hive.ts` (current, verified this session — **`applyCostRow` is UNCHANGED by 03-02**
in this revision round: the ledger-gate-widening/fallback-baseline/monotonic-floor work an earlier
draft assigned to 03-02 was reverted after two red-team rounds found 13 confirmed defects of the
same class in it — see 03-CONTEXT.md's Accepted Residuals. Read the LIVE file before editing it, but
do not expect any `isFallback` branch to be present):
```
constructor(
  private getHome: () => string | null,
  private emit?: (channel: string, payload: unknown) => boolean | void,
  private handoff?: (order: TerminalWorkOrder) => boolean
) {}
appendLog(event: Record<string, unknown>): void { ... writes log.jsonl, rotates at LOG_ROTATE_BYTES ... }
appendCostLedger(sample: AgentUsageSample): void { ... writes cost-ledger.jsonl ...
  if (this.costByTask) this.applyCostRow(this.costByTask, this.costCumulative, row);
}
private applyCostRow(                          // hive.ts:2608, verified live this session
  byTask: Map<string, {tokens:number; usd:number}>,
  cumulative: Map<string, {tokens:number; usd:number}>,
  row: {agent_id?: string|null; session_id?: string|null; task_id?: string|null; input?: number; output?: number; usd?: number}
): void {
  const key = `${row.agent_id ?? ''}\u0000${row.session_id ?? ''}`;   // a NUL separator, not a space
  const now = { tokens: (row.input ?? 0) + (row.output ?? 0), usd: row.usd ?? 0 };
  const previous = cumulative.get(key);
  const delta = previous
    ? { tokens: Math.max(0, now.tokens - previous.tokens), usd: Math.max(0, now.usd - previous.usd) }
    : now;
  cumulative.set(key, now);
  const taskId = row.task_id;
  if (!taskId) return; // hive.ts:2621 — advanced the baseline, credits no card
  const acc = byTask.get(taskId) ?? { tokens: 0, usd: 0 };
  acc.tokens += delta.tokens; acc.usd += delta.usd;
  byTask.set(taskId, acc);
}
private rescanCostLedger(): Map<string, {tokens:number; usd:number}> { /* scans the WHOLE cost-ledger.jsonl — TWO call sites, hive.ts:2079/:2695 (taskSpend, lazily on first call) and hive.ts:2642 (its own JSON.parse loop feeding applyCostRow directly) — see round-1 finding #47 */ }
```
This method's body references NOTHING on `this` except the parameters it is called with — it is
already a pure function of its three arguments, which is exactly what makes it safe to hoist to a
module-level EXPORTED function. The DIFF LOGIC — the key construction, the `now`/`previous`/`delta`
computation, the `cumulative.set`, the `byTask` accumulation, and the `if (!taskId)` early exit's
CONDITION — is untouched by this plan. What DOES change, and is the ONLY thing that changes, is the
function's RETURN CHANNEL, which round-2 finding #34 requires: the widened signature is
`{tokens: number; usd: number}`, so BOTH of the body's exits must produce that value. That is TWO
mechanical edits, not one:

1. `hive.ts:2621`'s `if (!taskId) return;` becomes `if (!taskId) return delta;` — the early exit
   still credits no card, it now merely hands the caller the delta it already computed.
2. `return delta;` is appended as the function's final statement.

An earlier draft of this plan said "exactly ONE addition" and called the result BYTE-IDENTICAL. Both
were wrong and self-defeating (round-3 findings #23/#40, round-4 finding #1): `tsconfig.node.json`
sets `"strict": true` (verified live this session, `tsconfig.node.json:6`), so a declared
`{tokens:number; usd:number}` return type with a bare `return;` still in the body does not compile,
and "silently fixing" it by editing 2621 would have made this plan's own BYTE-IDENTICAL claim a
false statement in the SUMMARY. Edit 1 is mandatory for a second, behavioral reason: `dailyCostRows`
must include `task_id: null` rows in the DAY total (see Task 2's `<behavior>`), and it can only do
that if the null-task exit returns the delta rather than `undefined`.

**Do not touch the two OTHER `if (!taskId) return...` guards in this file.** `hive.ts:2016` is
`applyReviewVerdict` (returns `void`) and `hive.ts:2726` is `budgetForAgent` (returns `null`) — both
verified live this session, both unrelated to cost, both unchanged by this plan. Only the guard
INSIDE `applyCostRow` moves.

From `src/main/floor/boot.ts` (current, verified this session):
```
hive = new HiveManager(
  () => readConfig().harnessHome,
  (channel, payload) => deps.send(channel, payload),
  handoff
);   // line ~1004
...
persist = new PersistStore(undefined, () => readConfig().harnessHome);   // line ~1148, AFTER 03-01 lands
```
`persist` is constructed 140+ lines AFTER `hive`, so any closure passed into `HiveManager`'s
constructor that reads the module-level `persist` variable must do so LAZILY (inside the callback
body, never at construction time) — matching the existing `breakerLevel: (agentId) =>
breaker.levelFor(agentId)` closure shape already used a few lines above the `persist` assignment in
this same function.

From `src/main/index.ts`'s existing hive IPC neighborhood (~line 2983 per RESEARCH.md, and
`hive:agentDirectory` at ~3450) and `src/preload/index.ts`'s `hiveLog`/`hiveBoard` pattern — new
handlers/preload entries follow the SAME `hive:xxx` naming and `ipcRenderer.invoke('hive:xxx', ...)`
shape already used by every sibling channel.

From `test/load-ts.cjs` (current, the loader `test/db-fts.test.cjs` already uses via `const
{ PersistStore } = loadTs('src/main/db.ts');`) — `src/main/timeline.ts` has ZERO imports of its own
(no `db.ts`, no `hive.ts`, no Electron), so `loadTs('src/main/timeline.ts')` loads it with no stub
setup beyond what `load-ts.cjs` already does for every main-process module.

**Day-parameter contract, settled here to remove an ambiguity 03-07 otherwise inherits**: `day` is
ALWAYS a `'YYYY-MM-DD'` LOCAL-date string — the exact format a native `<input type="date">` (03-07's
day picker) already produces. `hive:timeline`/`hive:timelineBucket` never accept an epoch-range form.

**RETURN-SHAPE CONTRACT — pinned here, restated verbatim in 03-07's `<interfaces>`, and the ONLY
shape either channel is allowed to answer with (round-3 findings #8 and #26).** An earlier draft let
the success path return a bare payload while the failure path returned `{ok:false, error}`: two
different discriminants, so 03-07 — which had no error branch at all — rendered a REJECTED day as an
empty timeline, i.e. as a quiet day. One shape, on every path:

```ts
// hive:timeline
type TimelineResult =
  | { ok: true; buckets: BucketSummary[]; firstTs: number | null; eventsAgedOut: boolean }
  | { ok: false; error: string };

// hive:timelineBucket
type BucketDetailResult =
  | { ok: true; rows: DetailRow[]; truncated: boolean; total: number }
  | { ok: false; error: string };
```

Three rules that follow from it, all of them testable:
- The `ok:false` branch is the answer for a rejected `day`, a rejected `bucketIndex`, AND the
  store-unavailable guard (`hive.enabled()`/`persist.isOpen` false). There is NO zeroed-success
  path — a zeroed success is indistinguishable from a genuinely empty day, which is the exact
  fabrication D-27 forbids.
- `summarizeDay`/`bucketDetail` return these shapes THEMSELVES (including the `ok: true` literal),
  so the handlers stay one-line thin AND `test/timeline.test.cjs` can assert the discriminant
  directly against the pure module rather than against unloadable `index.ts`.
- 03-07 is the ONLY consumer, and consuming BOTH discriminants is the other half of this contract:
  03-07 Task 1 carries a dedicated `ok === false` branch rendering 03-UI-SPEC.md:247's binding error
  string, `Could not read the timeline: {reason}. Pick the day again to retry.`, with this channel's
  `error` substituted for `{reason}` — never the quiet-day or fresh-install copy — and a dedicated `eventsAgedOut === true`
  branch that OUTRANKS every `firstTs`-derived no-record sentence. If either branch is missing over
  there this contract is only half-landed — the shape is pinned here, and restated verbatim in
  03-07's `<interfaces>`, so the two halves cannot drift.
- `eventsAgedOut` exists because the two stores behind this channel have DIFFERENT lifetimes: the
  `events` table is pruned at `EVENTS_RETENTION_DAYS` (Task 1) while `cost-ledger.jsonl` is never
  rotated (verified this session — `hive.ts:2503`'s `renameSync(path, ${path}.1)` is `log.jsonl`'s
  rotation, guarded by `LOG_ROTATE_BYTES`; `grep -n "cost-ledger.jsonl" src/main/hive.ts` returns
  FOUR hits — :783 an ignore-list entry, :2513 a comment, :2550 `appendFileSync`, :2639
  `readFileSync` — and no rotate, truncate or unlink among them). Without it, 03-07 asserts
  "Nothing was recorded on {date}" over a
  day it is drawing real cost bars for. Its definition is fixed in Task 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migration #3 — the events table, its indexes, retention (positively proven), and the range/earliest readers</name>
  <files>src/main/db.ts, test/db-fts.test.cjs</files>
  <read_first>src/main/db.ts (the full MIGRATIONS array and the migrate() transaction loop), test/db-fts.test.cjs (the existing real-SQLite, second-independent-handle test pattern)</read_first>
  <behavior>
    - Opening a `PersistStore` whose DB is at `user_version 2` (the current shipped schema) runs
      migration #3 and lands at `user_version 3`, without altering anything migrations 0/1 already
      created — a second independent `PersistStore` handle against the same file, opened after the
      first closes, sees the `events` table with all 8 columns and both indexes.
    - Inserting one row via `recordEvent(...)` and reading it back through a SECOND, independently
      opened `PersistStore` against the same file returns the identical row — proving the schema
      round-trips across process-boundary-shaped access, the same discipline `test/db-fts.test.cjs`'s
      existing tests already apply to `memory_fts`.
    - PROVEN retention, not merely a present statement: insert one row with `ts = Date.now() -
      (EVENTS_RETENTION_DAYS + 1) * 86400000` (already expired) and one fresh row with `ts =
      Date.now()`, close the store, reopen it (retention runs again on `open()`), and assert the
      expired row is GONE (`eventsInRange` over a wide window returns `0` rows at that old
      timestamp) while the fresh row SURVIVES (`>= 1` row at its timestamp) — a no-op retention
      implementation (wrong units, inverted comparison, or a statement that is built but never
      `.run()`) fails this case, unlike a bare "the DELETE text exists" grep.
    - `eventsInRange(startMs, endMs)` returns only rows with `ts` in `[startMs, endMs)`, ordered by
      `ts`, each shaped `{ts, kind, agentId, taskId, sessionId, payload}` (camelCase — the SQL
      column/JS boundary is inside `db.ts`, never leaked as snake_case to a caller).
    - `earliestEventTs()` returns `null` on an empty `events` table and the `MIN(ts)` of whatever
      rows are CURRENTLY stored otherwise — after this same test's retention case runs, it reflects
      the surviving fresh row's timestamp, not the expired one's (proving the "earliest ts STILL
      STORED" semantic, not "first event ever").
  </behavior>
  <action>
    Append a THIRD entry to `MIGRATIONS` in `db.ts` creating the `events` table exactly as specified
    in `<interfaces>` above (do not add, rename or drop any column from that schema). Add a small,
    exported retention constant `EVENTS_RETENTION_DAYS` (a reasoned default — 30 is a reasonable
    starting point, comment it as reasoned not measured, matching the day-band's own `BUCKET_MINUTES`
    comment style in `timeline.ts`, Task 3) and a retention call — either inside `open()` right after
    `migrate()` runs, or as a small private method called from there; either way it must actually
    `.run()` a bound `DELETE FROM events WHERE ts < ?` statement, not merely construct one. Add three
    public methods on `PersistStore`: `recordEvent(row: {ts:number; kind:string; agentId?:string|null;
    taskId?:string|null; sessionId?:string|null; payload:unknown}): void` (best-effort, never throws
    — mirrors `setKv`'s guard-on-`this.db` shape, JSON.stringifies `payload` for storage and
    JSON.parses it back out on read), `eventsInRange(startMs: number, endMs: number): Array<{...}>` per
    `<behavior>`, and `earliestEventTs(): number | null`. Extend `test/db-fts.test.cjs` with the
    migration/round-trip/PROVEN-retention/eventsInRange/earliestEventTs cases from `<behavior>`.
  </action>
  <acceptance_criteria>
    - `grep -n "CREATE TABLE IF NOT EXISTS events" src/main/db.ts` matches exactly `1`
    - `grep -n "idx_events_ts\|idx_events_floor_ts" src/main/db.ts` matches `>= 2`
    - `grep -n "recordEvent\|eventsInRange\|earliestEventTs" src/main/db.ts` matches `>= 3`
    - `grep -n "DELETE FROM events WHERE ts" src/main/db.ts` matches `>= 1`
    - `node --test test/db-fts.test.cjs` passes with 0 failures, INCLUDING the proven-retention case
      (an expired row inserted, reopened, and asserted actually gone — not only that the DELETE
      statement's text is present in the source)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/db-fts.test.cjs</automated>
  </verify>
  <done>Migration #3 lands, is round-trip-verified through a second independent handle, retention is proven to actually delete an expired row while sparing a fresh one, and eventsInRange/earliestEventTs exist for Task 3 to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: the injected events sink in hive.ts (log events only, never cost), applyCostRow hoisted unchanged, and the SQL-survives-the-tail-cap proof</name>
  <files>src/main/hive.ts, src/main/floor/boot.ts, test/hive-durability.test.cjs, test/hive-protocol-v2.test.cjs</files>
  <read_first>src/main/hive.ts (the constructor, appendLog, appendCostLedger, the CURRENT applyCostRow method body — read it live; it is UNCHANGED by 03-02 in this revision round — tailLines/logTail), src/main/floor/boot.ts (the `hive = new HiveManager(...)` construction call and the persist variable's later assignment), test/hive-protocol-v2.test.cjs (its existing cost-row fixture style, `#34 — per-card cost attribution`, the real owner of this repo's cost-arithmetic test style per 03-VALIDATION.md), docs/adr/0005-cumulative-cost-ledger.md</read_first>
  <behavior>
    - `HiveManager`'s events sink is a FOURTH constructor parameter, optional, never invoked when
      absent (existing tests that construct `HiveManager` with 3 args keep passing unmodified).
    - Calling `appendLog({kind: 'spawn', agentId: 'a1', name: 'Jim'})` on a `HiveManager` constructed
      with an events-sink callback invokes that callback with a row shaped for the `events` table
      (`ts`, `kind`, `agentId`, `taskId`, `sessionId`, `payload`) — `log.jsonl` is STILL written
      identically to before (the sink is additive, never a replacement).
    - Calling `appendCostLedger(sample)` does NOT invoke the events sink at all — cost has exactly
      ONE destination, `cost-ledger.jsonl`/`dailyCostRows` (D-26: cost-ledger.jsonl is the one
      genuinely SECOND read; the events table is not a third copy of it). Writing `kind:'cost'` rows
      into `events` would (a) double-represent cost — once via the bucket's own cost total, once via
      the bucket's all-kinds events count — and (b) pollute that count with roughly one row per agent
      per ~30s breaker beat (~2,880/agent/day), burying real activity (round-2 findings #14, #18).
    - The exported `applyCostRow` function keeps its current DIFF LOGIC unchanged (verified live in
      `hive.ts` — this task does not touch the key construction, the delta math, the
      `cumulative.set`, the `byTask` accumulation, or the `if (!taskId)` condition, and there is no
      `isFallback` branch to preserve or lose, per 03-CONTEXT.md's Accepted Residuals). A NEW fixture in
      `test/hive-protocol-v2.test.cjs`, driving a real `HiveManager.appendCostLedger` twice for the
      SAME `agent_id` across a mid-day `session_id` ROLLOVER (a genuine new OTel session starting
      mid-sequence, not a transcript fallback), asserts the first post-rollover row credits its OWN
      whole value — no predecessor exists for the new `(agent_id, session_id)` key — never a negative
      and never an over-count against the OLD session's cumulative total.
    - `applyCostRow` now RETURNS the delta it just computed (`{tokens, usd}`) on EVERY exit, in
      addition to its existing side effects on `byTask`/`cumulative` — a signature widening
      (`void` -> `{tokens: number; usd: number}`) that the two existing callers ignore, so their
      behavior is unchanged. This closes round-2 finding #34: `dailyCostRows` needs a PER-ROW delta
      (bucket aggregation is time-based, at 15-minute granularity, not task-based), and re-deriving
      that diff a second way inside `dailyCostRows` would be exactly the parallel-implementation risk
      D-22 warns against.
    - EVERY exit means BOTH exits, and the null-task one is the load-bearing half (round-3 #23/#40,
      round-4 #1). `applyCostRow({}, {}, {agent_id:'a', session_id:'s', usd: 2, input: 1, output: 1})`
      — a row with NO `task_id` — returns `{tokens: 2, usd: 2}`, not `undefined`. That is the exact
      call shape `dailyCostRows` makes for a cost sample recorded while an agent was between cards,
      and the day total would silently lose it if the early exit still returned nothing. Under
      `"strict": true` (`tsconfig.node.json:6`) a bare `return;` in a function declared to return
      `{tokens:number; usd:number}` does not compile at all, so this is a typecheck failure AND a
      wrong-total defect, not a style question.
    - `HiveManager.dailyCostRows(dayStartMs, dayEndMs)` returns diffed `{ts, agentId, taskId, usd,
      tokens}` rows for that range — each row's `usd`/`tokens` is the delta `applyCostRow` returned
      for it, not re-computed — seeded by scanning the WHOLE `cost-ledger.jsonl` (mirroring
      `rescanCostLedger`'s existing whole-file scan, never `tailLines`) so a diff at the day boundary
      is correctly seeded against pre-boundary state, not treated as a false "first row" — including
      rows whose `task_id` is `null` (any row not tied to a task — e.g. a cost sample recorded while
      an agent was between cards — still counts toward the day's total spend even though it credits
      no specific card).
    - A driver in `test/hive-durability.test.cjs`, constructing a real `HiveManager` AND a real
      `PersistStore` (temp dir) wired together via the events-sink closure exactly as `boot.ts` wires
      them, calls `appendLog` enough times to push `log.jsonl` past `LOG_TAIL_BYTES` (64KB) for the
      EARLIEST entries — at that point `hive.logTail()` (the old 64KB-capped read) no longer includes
      the earliest entries, while `persist.eventsInRange(<the earliest entries' time window>)` still
      returns them, proving the SQL path survives what the JSONL tail read does not.
  </behavior>
  <action>
    In `hive.ts`: add a fourth, optional constructor parameter `private eventsSink?: (row: {ts: number;
    kind: string; agentId?: string|null; taskId?: string|null; sessionId?: string|null; payload:
    unknown}) => void`. In `appendLog` ONLY, after the existing `log.jsonl` write, call
    `this.eventsSink?.({ts: Date.now(), kind: String(event.kind ?? 'unknown'), agentId: typeof
    event.agentId === 'string' ? event.agentId : null, taskId: typeof event.taskId === 'string' ?
    event.taskId : null, sessionId: typeof event.sessionId === 'string' ? event.sessionId : null,
    payload: event})` — best-effort, wrapped so a sink failure never breaks the existing log write.
    Do NOT add any `eventsSink` call inside `appendCostLedger` — cost never lands in the `events`
    table (see `<behavior>`'s double-count rationale). Change the PRIVATE `applyCostRow` method
    (`hive.ts:2608`, verified live this session) to a module-level EXPORTED function
    `applyCostRow(byTask, cumulative, row): {tokens: number; usd: number}`, hoisting its DIFF LOGIC
    unchanged (read the live file — it is unchanged by 03-02 in this revision round; do not add or
    invent any DIFF-LOGIC branch that is not already there: no `isFallback`, no monotonic floor, no
    widened ledger gate) and making EXACTLY TWO edits, both to the return channel and neither to the
    math, per round-2 finding #34 and round-3 findings #23/#40. Edit one: change `hive.ts:2621`'s
    `if (!taskId) return;` to `if (!taskId) return delta;` — the guard's CONDITION and its
    credits-no-card semantics are unchanged, it now returns the delta the body already computed.
    Edit two: append `return delta;` as the function's final statement. Both are required: under
    `"strict": true` (`tsconfig.node.json:6`) the widened return type does not typecheck with a bare
    `return;` still in the body, and `dailyCostRows` below needs the null-`task_id` exit to yield a
    real delta so those rows still reach the DAY total. Do NOT touch the two unrelated
    `if (!taskId) return...` guards elsewhere in this file — `hive.ts:2016` (`applyReviewVerdict`,
    returns `void`) and `hive.ts:2726` (`budgetForAgent`, returns `null`), both verified live this
    session; only the guard inside `applyCostRow` moves. Update BOTH of
    `applyCostRow`'s current call sites to call the exported function the same way —
    `appendCostLedger` (hive.ts:2554) AND `rescanCostLedger`'s own `JSON.parse` loop (hive.ts:2642) —
    per round-1 finding #47, this method has two callers, not one. Add `dailyCostRows(dayStartMs:
    number, dayEndMs: number): Array<{...}>` as a THIN loop calling the exported `applyCostRow` per
    row and collecting its RETURNED delta directly (never re-deriving the diff a second way) following
    `rescanCostLedger`'s whole-file-scan shape, filtering to the requested
    range after diffing, INCLUDING rows whose `task_id` is `null` in the day total (only the
    per-TASK accumulator skips a null `task_id`, per D-22's own "advanced the baseline, credits no
    card" comment — the DAY total is not task-scoped) — this repeats `rescanCostLedger`'s existing
    whole-file-read cost profile (see this plan's threat register T-03-03e for why that is an
    accepted, not fixed, residual here). In `boot.ts`: add the fourth argument to the `new
    HiveManager(...)` call — a closure `(row) => { try { persist.recordEvent(row); } catch { /*
    best-effort */ } }` (lazy reference to the module-level `persist`, safe despite `persist` being
    assigned later in the same function, per the interface note above). Extend
    `test/hive-protocol-v2.test.cjs` (already this repo's real owner of cost-arithmetic-style tests,
    per 03-VALIDATION.md's SCALE-03 row — 03-VALIDATION.md's own text misattributing this style to
    `hive-durability.test.cjs` is corrected by this plan landing the case in the right file) with the
    mid-day session-rollover fixture from `<behavior>`, and with a `dailyCostRows` case covering a
    `task_id: null` row. Extend `test/hive-durability.test.cjs` with the SQL-survives-the-tail-cap
    driver from `<behavior>`, constructing both `HiveManager` and a real `PersistStore` (via
    `loadTs('src/main/db.ts')`, temp dir) wired through the SAME events-sink shape `boot.ts` uses.
  </action>
  <acceptance_criteria>
    - `grep -n "eventsSink" src/main/hive.ts` matches `>= 2` (constructor param + the ONE appendLog
      call — never a second call inside appendCostLedger)
    - `node -e` check that `appendCostLedger`'s body contains no `eventsSink` call: `node -e "const
      s=require('fs').readFileSync('src/main/hive.ts','utf8');const i=s.indexOf('appendCostLedger(sample');const j=s.indexOf('\n  }',i);const body=s.slice(i,j);if(/eventsSink/.test(body)){console.error('FAIL: appendCostLedger still writes to the events sink — this double-counts cost against the events-count track');process.exit(1);}console.log('PASS');"`
    - `grep -n "export function applyCostRow" src/main/hive.ts` matches `>= 1`
    - `grep -c "return delta" src/main/hive.ts` matches `>= 2` (round-2 #34 + round-3 #23/#40 —
      applyCostRow returns its computed delta on BOTH exits: the widened `if (!taskId) return delta;`
      at what is `hive.ts:2621` today, and the appended final statement. `>= 1` was the previous,
      defective bound: it passed with the null-task exit still returning `undefined`, which is both a
      `"strict": true` typecheck failure and a silent hole in `dailyCostRows`' day total)
    - `node -e` proving BOTH return-channel edits landed, driven through the real module rather than
      a grep — hive.ts is already loadable this way (`test/engine-parity.test.cjs:30` does
      `loadTs('src/main/hive.ts')`, verified live this session): `node -e "const
      loadTs=require('./test/load-ts.cjs');const {applyCostRow}=loadTs('src/main/hive.ts');const
      a=applyCostRow(new Map(),new Map(),{agent_id:'a',session_id:'s',usd:2,input:1,output:1});const
      b=applyCostRow(new Map(),new
      Map(),{agent_id:'a',session_id:'s',task_id:'t',usd:5,input:2,output:2});if(!a||a.tokens!==2||a.usd!==2){console.error('FAIL:
      the task_id-less exit returned',a,'- hive.ts:2621 was not widened to return delta, so
      dailyCostRows silently drops every between-cards cost sample from the day
      total');process.exit(1);}if(!b||b.tokens!==4||b.usd!==5){console.error('FAIL: the task-credited
      exit returned',b,'- the final return delta; statement is
      missing');process.exit(1);}console.log('PASS');"`. A plan-did-nothing executor goes red here
      first (`applyCostRow` is still private, so the destructure yields `undefined` and the call
      throws); an executor who lands only the appended final statement goes red on the first assert.
    - `grep -n "private applyCostRow" src/main/hive.ts` matches exactly `0` (fully hoisted, not
      duplicated)
    - `grep -n "isFallback" src/main/hive.ts` matches exactly `0` (there is no fallback-baseline
      branch to preserve — its absence here is correct, not a regression, per 03-CONTEXT.md's
      Accepted Residuals)
    - `grep -c "this.applyCostRow" src/main/hive.ts` equals exactly `0` (no lingering reference to
      the now-removed private method — both call sites use the hoisted export)
    - `grep -n "dailyCostRows" src/main/hive.ts` matches `>= 1`
    - `grep -n "from 'electron'" src/main/hive.ts` matches exactly `0`
    - `grep -n "new HiveManager(" src/main/floor/boot.ts -A 5` shows a 4th argument
    - `node --test test/hive-durability.test.cjs test/hive-protocol-v2.test.cjs` passes with 0
      failures, including the mid-rollover fixture, the `task_id: null` `dailyCostRows` case, and the
      SQL-survives-the-tail-cap driver
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-durability.test.cjs test/hive-protocol-v2.test.cjs</automated>
  </verify>
  <done>The events sink is constructor-injected, wired at boot.ts, and fires ONLY from appendLog — never from appendCostLedger, so cost is never double-represented in the events table; hive.ts stays Electron-free; applyCostRow is one shared, exported diff function wired at both its call sites, its math untouched and its return channel widened at both exits; a real driver proves SQL answers a time range the 64KB JSONL tail read cannot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: timeline.ts — the pure, loadable aggregation module — and hive:timeline / hive:timelineBucket as thin handlers</name>
  <files>src/main/timeline.ts, src/main/index.ts, src/preload/index.ts, test/timeline.test.cjs, test/boot-floor.test.cjs</files>
  <read_first>src/main/index.ts (hive:agentDirectory, for the response-shaping pattern; the existing hive IPC neighborhood), src/preload/index.ts (hiveLog/hiveBoard pattern), src/main/hive.ts (dailyCostRows from Task 2), src/main/db.ts (eventsInRange/earliestEventTs from Task 1), test/load-ts.cjs (the loader test/timeline.test.cjs uses — timeline.ts has no imports of its own, so no extra stubbing is needed)</read_first>
  <behavior>
    - `summarizeDay(events, costRows, dayStartMs, firstTs)` (pure — no I/O) given a day's event/cost
      rows returns `{ok: true, buckets, firstTs, eventsAgedOut}` per the pinned RETURN-SHAPE CONTRACT
      in `<interfaces>` — `buckets` is EXACTLY `BUCKETS_PER_DAY` (96) bucket summaries
      (`BUCKET_MINUTES = 15` as a commented-reasoned constant), each with an events count, an
      envelopes count (events filtered to `kind === 'message'`), and a diffed cost total for that
      bucket — never raw rows — plus the `firstTs` value it was given, passed through unchanged (this
      function does not compute `firstTs` itself; the caller supplies it from `earliestEventTs()`).
    - `eventsAgedOut` is `true` when, and only when, the events store cannot speak to this day AT ALL
      while the cost ledger CAN: `(firstTs === null || firstTs >= dayStartMs + 86400000) &&` at least
      one displayable (non-zero-delta) cost row falls inside the day. This is the two-stores-one-record
      fix (round-3 finding #9): the `events` table is pruned at `EVENTS_RETENTION_DAYS` while
      `cost-ledger.jsonl` is never rotated, so a 60-day-old day, and equally the just-migrated install
      whose events table is empty but whose ledger is months deep, both have real spend and zero
      events. Without this flag 03-07 renders "Nothing was recorded on {date}." / "No timeline yet"
      directly above real cost bars. The record's start therefore covers BOTH stores or neither: the
      no-record declarations are reserved for the day where NEITHER store has anything, and
      `eventsAgedOut` names the mixed case for 03-07 to declare in its own words.
      A day whose `firstTs` falls INSIDE it is untouched by this flag — the existing
      `No record before {HH:mm} — missing, not quiet.` sentence already tells that truth.
    - `bucketDetail(events, costRows, bucketIndex, dayStartMs)` (pure) returns `{ok: true, rows,
      truncated, total}` per the pinned contract: that bucket's MERGED detail rows (events + the
      matching cost deltas for that window, sorted by `ts`), capped at `BUCKET_DETAIL_ROW_CAP = 200`
      with `truncated: true` and the real `total` when exceeded — never a silent slice. A fixture of
      312 displayable rows in one bucket returns `rows.length === 200`, `truncated: true`,
      `total: 312`.
    - ZERO-DELTA COST ROWS ARE DROPPED BEFORE THE CAP, and `total` counts DISPLAYABLE rows only
      (round-3 finding #10). A cost row whose diffed `usd` and `tokens` are both `0` is never
      rendered by 03-07 (its detail list "never emits a cost row for a zero-delta row" — 03-07 Task
      2), so counting it against the 200-row cap both crowds real events out of the bucket and makes
      the `Showing {shown} of {total}` line report rows nothing would have shown. The drop happens
      at the TOP of `bucketDetail`, before merging and before the cap — not in the caller, and not in
      03-07 — so `test/timeline.test.cjs` can drive it directly against the pure module. An idle
      floor produces roughly one cost sample per agent per ~30s breaker beat (~120 per agent per
      15-minute bucket), which is what makes this the common case, not the corner one.
      A bucket seeded with 150 zero-delta cost rows and 60 events returns all 60 events,
      `truncated: false`, and `total: 60`.
    - `parseDayParam(raw: unknown)` returns `{ok: true, dayStartMs}` for a well-formed `'YYYY-MM-DD'`
      string (interpreted as LOCAL midnight) and `{ok: false, error}` for anything else — a
      non-string, an out-of-range calendar date (e.g. `'2024-13-45'`), or a string more than one day
      in the future relative to an injected `now` parameter.
    - `validateBucketIndex(raw: unknown)` returns `{ok: true, index}` only for `Number.isInteger(raw)
      && raw >= 0 && raw < BUCKETS_PER_DAY`, and `{ok: false, error}` for `-1`, `96`, `NaN`, `1e9`, or
      a non-number — every case a hostile/malformed renderer call could send.
  </behavior>
  <action>
    Create `src/main/timeline.ts` with ZERO imports beyond plain TypeScript types — no `db.ts`, no
    `hive.ts`, no Electron. Export the constants `BUCKET_MINUTES = 15`, `BUCKETS_PER_DAY = 96`,
    `BUCKET_DETAIL_ROW_CAP = 200` (each commented as a reasoned default, not a measured one, per
    D-25's discretion clause), the two result types from the RETURN-SHAPE CONTRACT in `<interfaces>`
    (`TimelineResult`, `BucketDetailResult`) and the four functions from `<behavior>` —
    `summarizeDay`, `bucketDetail`, `parseDayParam`, `validateBucketIndex`. `summarizeDay` and
    `bucketDetail` each build and return the `{ok: true, ...}` literal themselves, so the handlers
    below add nothing to the payload. Compute `eventsAgedOut` inside `summarizeDay` exactly as
    `<behavior>` defines it, and drop zero-delta cost rows (`usd === 0 && tokens === 0`) as
    `bucketDetail`'s FIRST step — before the merge, before the sort, before the cap — with `total`
    taken from the merged DISPLAYABLE row count. Write `test/timeline.test.cjs` loading
    this module via `loadTs('src/main/timeline.ts')` and asserting every case in `<behavior>`
    directly against the pure functions — this is the test that proves the aggregation MATH, not a
    grep over `index.ts`. It must include: the 96-bucket case; the 312-row truncation case; the
    150-zero-delta-cost-rows-plus-60-events case (`rows` contains all 60 events, `truncated === false`,
    `total === 60`); an `eventsAgedOut === true` case (`firstTs` on a LATER day than the one viewed,
    plus at least one non-zero cost row in the day) AND its `false` control (same `firstTs`, zero cost
    rows in the day); and one rejected malformed `day` and one rejected `bucketIndex`, each asserted
    as `ok === false`. In `index.ts`, add two THIN IPC handlers near the existing hive
    neighborhood: `hive:timeline` calls `parseDayParam` on its argument, returns
    `{ok:false, error}` on failure, otherwise fetches `persist.eventsInRange(dayStartMs, dayStartMs +
    86400000)`, `hive.dailyCostRows(dayStartMs, dayStartMs + 86400000)` and `persist.earliestEventTs()`,
    and returns `summarizeDay(events, costRows, dayStartMs, firstTs)`; `hive:timelineBucket` calls
    `parseDayParam` and `validateBucketIndex` on its two arguments, fetches the same day's rows, and
    returns `bucketDetail(events, costRows, bucketIndex, dayStartMs)`. Both handlers read
    `hive.enabled()`/`persist.isOpen` guards consistent with every other hive handler in this file
    and return `{ok:false, error: 'timeline store unavailable'}` when either is false — NEVER an
    empty-but-successful result, which a consumer cannot tell apart from a genuinely empty day (this
    is the store-unavailable rule of the pinned contract). index.ts itself contains NO
    bucket math, no cap logic, no zero-delta filtering, and no argument-shape assumptions beyond
    calling `timeline.ts`'s validators. Add the two matching preload entries (`hiveTimeline`, `hiveTimelineBucket`) following
    the exact `ipcRenderer.invoke('hive:xxx', ...)` shape every sibling hive preload entry already
    uses. `test/boot-floor.test.cjs`'s IPC-name pin needs the same two-place edit
    (`B_IPC_JOINED` count + sorted `B_IPC_NAMES` insertion) — insert both new names at their correct
    sorted positions, re-deriving the CURRENT count from the live file (03-02's Task 1 may already
    have bumped it).
  </action>
  <acceptance_criteria>
    - `grep -n "export function summarizeDay\|export function bucketDetail\|export function parseDayParam\|export function validateBucketIndex" src/main/timeline.ts` matches `>= 4`
    - `grep -n "electron\|from '\./db'\|from '\./hive'" src/main/timeline.ts` matches exactly `0`
      (timeline.ts lives in `src/main/` beside `db.ts`/`hive.ts`, so a same-directory relative import
      would read `from './db'`/`from './hive'`, not `'../db'`/`'../hive'` — round-2 finding #44/#46
      corrected the pattern to one that can actually match) AND `grep -c "^import " src/main/timeline.ts` matches exactly `0` (a stronger, direct positive-lower-bound-paired check: the file has zero top-level import statements at all, not merely zero matches for two guessed paths)
    - `grep -c "eventsAgedOut" src/main/timeline.ts` is `>= 1` AND `grep -c "eventsAgedOut" test/timeline.test.cjs` is `>= 2` (the flag is computed in the pure module and both its `true` case and its `false` control are asserted; baseline for both files today is 0 — neither file exists yet, verified this session)
    - `node -e` proving the zero-delta drop precedes the cap, driven through the real module rather
      than a grep — load it the way every other test in this repo loads a main-process module,
      `const loadTs = require('./test/load-ts.cjs'); const { bucketDetail } =
      loadTs('src/main/timeline.ts');` — then build 150 cost rows with `usd: 0, tokens: 0` and 60
      events, all inside bucket 0 of
      one day, call `bucketDetail`, and FAIL unless `res.ok === true && res.rows.length === 60 &&
      res.truncated === false && res.total === 60` — print the observed `{rows: n, truncated, total}`
      on failure so a regression names itself. (Round-3 #10: with the drop applied AFTER the cap this
      returns 200 rows / `truncated: true` / `total: 210`, so the check genuinely fails on the
      defect, and it also fails against a `return {}` stub.)
    - `node --test test/timeline.test.cjs` passes with 0 failures, covering 96-bucket summarization,
      the 312-row truncation case, the 150-zero-delta-plus-60-events case, both `eventsAgedOut`
      cases, and at least one rejected malformed day/bucket-index argument each — each rejection
      asserted as `ok === false` with a non-empty `error`
    - `node -e` proving BOTH pure entry points carry the `ok` discriminant on their SUCCESS path (the
      round-3 #8/#26 fix — two different discriminants is what let a rejected day render as a quiet
      one): via the same `loadTs('src/main/timeline.ts')`, call `summarizeDay([], [], <a day start>,
      null)` and `bucketDetail([], [], 0, <the same
      day start>)` and FAIL unless BOTH returned objects have `ok === true`. A success path that
      returns a bare `{buckets, firstTs}` payload fails this check.
    - `grep -n "ipcMain.handle('hive:timeline'" src/main/index.ts` matches `>= 1`
    - `grep -n "ipcMain.handle('hive:timelineBucket'" src/main/index.ts` matches `>= 1`
    - `grep -n "parseDayParam\|validateBucketIndex" src/main/index.ts` matches `>= 2` (index.ts
      actually calls the validators, not merely a grep for the word "truncated")
    - `grep -c "timeline store unavailable" src/main/index.ts` matches `>= 1` — the baseline in the
      live file today is exactly `0` (verified this session), so this cannot pass on unchanged
      source. `index.ts` is unloadable under this repo's test harness, so this grep is the only
      reachable check that the store-unavailable guard answers with the pinned `{ok:false, error}`
      shape rather than a zeroed success; the SUCCESS-path discriminant is proven for real by the
      `loadTs('src/main/timeline.ts')` success-discriminant check above, which a `return {}` stub
      also fails
    - `grep -n "hiveTimeline\|hiveTimelineBucket" src/preload/index.ts` matches `>= 2`
    - `node --test test/boot-floor.test.cjs` passes with 0 failures (IPC-name pin green)
  </acceptance_criteria>
  <verify>
    <automated>node --test test/timeline.test.cjs test/boot-floor.test.cjs test/db-fts.test.cjs</automated>
  </verify>
  <done>timeline.ts is a pure, loadable module with real test coverage of the bucket math, the truncation cap, the zero-delta-before-cap rule, eventsAgedOut, and argument validation; hive:timeline/hive:timelineBucket are thin, IPC-pinned handlers that delegate to it, report firstTs, and answer in ONE discriminated shape on every path — success, rejection and store-unavailable alike.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| `hive.ts` <-> `db.ts` | Deliberately NOT a direct import boundary — the events sink crosses it only via an injected closure, never a module import |
| Renderer <-> main (`hive:timeline`/`hive:timelineBucket`) | Read-only queries; day and bucket-index parameters are the only renderer-supplied input, validated by `timeline.ts`'s pure validators before any query runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-03a | Tampering | `hive.ts`<->electron boundary regression | mitigate | `grep -n "from 'electron'" src/main/hive.ts` asserted at exactly 0 in this plan's own acceptance criteria — a regression here breaks `node --test`-loadability, caught immediately by the existing test harness |
| T-03-03b | Denial of Service | unbounded `events` table growth | mitigate | Retention delete on open (Task 1), PROVEN by a test that inserts an expired row and asserts it is actually gone, not only that the DELETE statement's text exists |
| T-03-03c | Denial of Service | a malformed day/bucket-index IPC argument driving an unbounded SQL range or array index | mitigate | `parseDayParam`/`validateBucketIndex` (Task 3, `timeline.ts`) validate both before any query runs, returning a discriminated error rather than throwing or returning an unbounded result — tested directly, not only claimed in this table |
| T-03-03d | Information Disclosure | `payload` column storing full event bodies | accept | Mirrors `appendLog`'s existing PII-conscious event shape (no raw OTel record, no credentials) — the events table stores the SAME data `log.jsonl` already persists on disk today, not a new data class |
| T-03-03e | Denial of Service | `dailyCostRows` performs a synchronous whole-file scan of `cost-ledger.jsonl` on every `hive:timeline` call, and that file is never rotated | accept | This mirrors `rescanCostLedger`'s EXISTING whole-file-scan cost profile (unchanged by this plan) rather than introducing a new one; `hive:timeline` is invoked on explicit day-change, not per-keystroke, in 03-07's day picker. Real memoization (by ledger size+mtime, mirroring `usage.ts:138-139`) or moving cost reads to the reserved `cost_ledger` SQLite columns (`db.ts:44-59`) is the follow-up if this is ever measured to matter — out of scope here since ledger rotation is not one of this phase's five requirements |
</threat_model>

<verification>
- `node --test test/db-fts.test.cjs test/hive-durability.test.cjs test/timeline.test.cjs test/boot-floor.test.cjs` — 0 failures
- `npm run typecheck` — 0 errors
</verification>

<success_criteria>
- Migration #3 lands, round-trips through a second independent handle, and PROVEN retention actually
  deletes an expired row while sparing a fresh one
- The events sink is constructor-injected, wired at `boot.ts`, and `hive.ts` stays Electron-free
- `applyCostRow` is one shared, exported, diff-never-sum function, wired at BOTH its call sites,
  with its diff logic unchanged from its pre-Phase-3 body and its return channel widened at BOTH
  exits — so a `task_id: null` row still yields its delta to the day total instead of `undefined`
- Cost never lands in the events table, so the day band's events-count track is never
  double-represented against its own cost track
- `timeline.ts` is a pure, loadable module with real `node --test` coverage of the bucket math, the
  truncation cap, and argument validation — not only grep assertions over unloadable `index.ts` code
- `hive:timeline`/`hive:timelineBucket` exist, are IPC-pinned, report `firstTs`, bound their response
  size, validate their arguments, and never silently truncate
- Both channels answer in ONE discriminated shape — `{ok:true, ...}` / `{ok:false, error}` — on every
  path including store-unavailable, so 03-07 can never render a rejected day as a quiet one
- `eventsAgedOut` distinguishes "neither store has this day" from "the events table lost this day but
  the never-rotated ledger still has its spend", so the no-record declaration and the cost bars can
  never contradict each other on the same day
- Zero-delta cost rows are dropped BEFORE the 200-row cap and `total` counts displayable rows only —
  proven by a `bucketDetail` case where 150 idle cost rows do not displace 60 real events
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-03-SUMMARY.md` when done
</output>
