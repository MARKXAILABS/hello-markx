---
phase: 03-scale-and-observability
plan: 9
type: execute
wave: 6
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [SCALE-01, SCALE-03]

must_haves:
  truths:
    - "REQUIREMENTS.md:164's STRUCT-02 messaging-split claim is corrected to reflect that messaging is still in hive.ts, with Phase 3 adopting the orphan correction explicitly"
    - "REQUIREMENTS.md:206 and :208's stale file:line anchors (telemetry.ts:126, hive.ts:267) are corrected to the measured lines (:161, :323)"
    - "SCALE-01 and SCALE-03 remain Pending in REQUIREMENTS.md, with an owner column naming the post-Phase-5 re-verification phase, never the word 'later'"
    - "ROADMAP.md's Phase 3 section no longer claims STRUCT-02's registry/router split as SCALE-01's blocking dependency"
    - "ROADMAP.md no longer claims re-ordering makes both SCALE-01 and SCALE-03's criteria hold"
    - "ROADMAP.md's SCALE-03 justification no longer cites the 8MB rotation as the mechanism that holes a replayable day"
    - "The post-Phase-5 re-verification phase is inserted with a real phase id, carrying exactly the two skeptic tests already at ROADMAP.md:297-310, and this edit lands ONLY if 02-12 has already landed"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "the corrected STRUCT-02/SCALE-01/SCALE-03 lines and anchors"
    - path: .planning/ROADMAP.md
      provides: "the corrected Phase 3 dependency reasoning and the new post-Phase-5 phase entry"
  key_links:
    - from: ".planning/ROADMAP.md (new phase entry)"
      to: ".planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md"
      via: "an existence check gating whether this plan's ROADMAP edit lands at all"
      pattern: "02-12-SUMMARY"
---

<objective>
Close the honesty work D-01, D-02, D-05, D-06, D-07, D-21 and D-39 assign to Phase 3: correct the
stale claims this project's own planning documents make about STRUCT-02, SCALE-01's dependency
reasoning, the "both criteria hold" overstatement, the 8MB-rotation justification, and three stale
file:line anchors — then insert a real phase id for the post-Phase-5 re-verification, gated on 02-12
having already landed. This is the phase's own acceptance-honesty test, not a deviation from the
phase's work.

Purpose: this repo's own evidence is that unowned prose residuals rot — several are still open from
earlier phases, and the counter-example (#18) was caught precisely because it had an id. SCALE-01 and
SCALE-03 stay Pending; this plan makes that Pending state accurately explained rather than silently
inherited.
Output: corrected `REQUIREMENTS.md` and `ROADMAP.md`, and a `STATE.md` sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md

<interfaces>
This plan does not touch source code — every task edits planning documents only. The full text of
every correction this plan makes is already written out, decision by decision, in
`03-CONTEXT.md`'s `<decisions>` section (D-01, D-02, D-05, D-06, D-07, D-21, D-39) and in its
corrections table at the top of the file. Read that section in full before editing either document —
do not paraphrase from this plan's summary of it.

Corrections table (from `03-CONTEXT.md`, reproduced here for the exact target lines only — the
CONTENT of each correction is in the source document, not repeated here):
| Document | Line(s) | What is wrong today |
|---|---|---|
| REQUIREMENTS.md | 164 | STRUCT-02 lists "messaging" among split concerns; messaging is still in hive.ts (D-01) |
| REQUIREMENTS.md | 206 | span ring cited at telemetry.ts:126; actually :161 (D-39) |
| REQUIREMENTS.md | 208 | LOG_ROTATE_BYTES cited at hive.ts:267; actually :323 (D-39, D-21) |
| ROADMAP.md | 281 | Same hive.ts:267 -> :323 anchor (D-39) |
| ROADMAP.md | 283 | "the 8MB rotation is what holes a replayable day" — refuted, it has never fired; the real cause is the absence of any time-range query mechanism (D-21) |
| ROADMAP.md | 293-294 | "SCALE-01's per-project isolation needs the registry/router split from STRUCT-02" — not supported by code; the isolation seam is hive.root() (D-02) |
| ROADMAP.md | 286 | "Both criteria then hold" under the reorder option — false for SCALE-01, since RECALL-02 is memory-only, not task-ledger (D-06) |
| ROADMAP.md | 297-310 | the two skeptic tests to carry verbatim into the new inserted phase (D-05) |

**MUST NOT edit**: `REQUIREMENTS.md:145` and `:452` (PARITY-02's text and status row) — owned
exclusively by plan `02-12`. If `02-12-SUMMARY.md` does not yet exist on disk when this plan runs,
that is fine and expected — this plan's exclusion still holds regardless of whether 02-12 has landed.

`REQUIREMENTS.md`'s status table (the `| SCALE-01 | Phase 3 | Pending |` / `| SCALE-03 | Phase 3 |
Pending |` rows, near the bottom of the file) currently name only "Phase 3" as owner — this plan
widens that to name the NEW post-Phase-5 phase as the re-verification owner, per D-07 ("never
'later'"), once that phase exists (Task 2 creates its id; Task 1 can reference it by the same name
Task 2 assigns, since both land in one plan).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md corrections — the orphan adoption, the two stale anchors, and the owner column</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (the STRUCT-02 line at :164, the SCALE-01/SCALE-03 status rows near the bottom, the RECORD-02 line naming hive.ts:267, the RECORD-01 line naming telemetry.ts:126), .planning/phases/03-scale-and-observability/03-CONTEXT.md (D-01, D-07, D-39 full text)</read_first>
  <action>
    Edit `REQUIREMENTS.md:164` (STRUCT-02's requirement text) to remove "messaging" from the list of
    split concerns, or to explicitly annotate it as an adopted-but-still-open orphan per D-01 — match
    whichever phrasing keeps the line a single, accurate sentence rather than a hedge; state that
    Phase 3 adopts this as a named open item rather than silently absorbing it. Correct the
    `telemetry.ts:126` anchor (near RECORD-01's text, `:206`) to `telemetry.ts:161`. Correct the
    `hive.ts:267` anchor (near RECORD-02's text, `:208`) to `hive.ts:323`. In the status table, update
    the `SCALE-01` and `SCALE-03` rows' owner column from "Phase 3" to name the new post-Phase-5
    phase Task 2 of this plan creates (use the exact phase name/number Task 2 assigns — coordinate
    the two tasks' wording, e.g. "Phase 7 — Close the forward dependencies", so the two documents
    agree). Do NOT tick either checkbox — both stay `[ ]` Pending. Do NOT edit lines 145 or 452.
  </action>
  <acceptance_criteria>
    - `grep -n "telemetry.ts:126" .planning/REQUIREMENTS.md` matches exactly `0`
    - `grep -n "hive.ts:267" .planning/REQUIREMENTS.md` matches exactly `0`
    - `grep -n "telemetry.ts:161" .planning/REQUIREMENTS.md` matches `>= 1`
    - `grep -n "hive.ts:323" .planning/REQUIREMENTS.md` matches `>= 1`
    - `sed -n '145p;452p' .planning/REQUIREMENTS.md` is BYTE-IDENTICAL to its pre-edit content (confirm via `git diff -- .planning/REQUIREMENTS.md` showing no hunk touching those two lines)
    - `grep -n "SCALE-01" .planning/REQUIREMENTS.md | grep -i "pending"` matches `>= 1` (still unticked)
    - `grep -n "SCALE-03" .planning/REQUIREMENTS.md | grep -i "pending"` matches `>= 1` (still unticked)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "hive.ts:267\|telemetry.ts:126" .planning/REQUIREMENTS.md</automated>
  </verify>
  <done>REQUIREMENTS.md's two stale anchors are corrected, the STRUCT-02 orphan is adopted explicitly, and SCALE-01/SCALE-03's owner column names a real phase id instead of "Phase 3"/"later".</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md corrections and the gated post-Phase-5 phase insertion</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md (Phase 3's full section, lines ~272-315, and lines 281/283/286/293-294/297-310 specifically), .planning/phases/03-scale-and-observability/03-CONTEXT.md (D-02, D-05, D-06, D-21, D-39 full text), .planning/STATE.md (the Pending Todos section)</read_first>
  <action>
    Correct `ROADMAP.md:281`'s anchor (`hive.ts:267` -> `:323`). Correct `ROADMAP.md:283`'s
    justification: remove or rewrite the claim that the 8MB rotation is what holes a replayable day —
    state instead that the rotation has never fired and the real gap is the absence of any
    time-range query mechanism (D-21). Correct `ROADMAP.md:293-294`'s stated dependency reasoning:
    remove the claim that SCALE-01 needs STRUCT-02's registry/router split, and state instead that
    the isolation seam is `hive.root()`, already lazy, unaffected by whether messaging is extracted
    (D-02). Correct `ROADMAP.md:286`'s "Both criteria then hold" under the reorder option: state
    instead that re-ordering does NOT make SCALE-01 hold, since RECALL-02 is memory-only and the
    task-ledger half is Phase 3's own net-new work under either ordering (D-06). THEN — and only
    after re-reading the check below — insert a new phase entry after Phase 5, titled "Close the
    forward dependencies", carrying EXACTLY the two skeptic tests already written at
    `ROADMAP.md:297-310` verbatim (do not rewrite their wording, only relocate/reference them), and
    renumber GSD to Phase 7 (a doc-only renumber, since no `07-*`/GSD phase directory exists yet).
    **Gate check, run BEFORE making this one insertion**: verify
    `.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md` exists on disk. If it does
    NOT exist, do NOT insert the new phase or renumber GSD — leave `ROADMAP.md`'s phase list and
    numbering exactly as they are today, complete every OTHER correction in this task normally, and
    record in this plan's SUMMARY.md that the phase-insertion edit was deliberately withheld pending
    02-12, per D-05's own explicit instruction ("do not land this roadmap edit until 02-12 lands").
    Sync `STATE.md`'s Pending Todos section to reflect whichever outcome actually occurred (inserted,
    or withheld-pending-02-12).
  </action>
  <acceptance_criteria>
    - `grep -n "hive.ts:267" .planning/ROADMAP.md` matches exactly `0`
    - `grep -n "hive.ts:323" .planning/ROADMAP.md` matches `>= 1`
    - `grep -n "registry/router split from STRUCT-02" .planning/ROADMAP.md` matches exactly `0`
    - `grep -n "Both criteria then hold" .planning/ROADMAP.md` matches exactly `0`
    - `test -f .planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md && grep -n "Close the forward dependencies" .planning/ROADMAP.md | wc -l || echo "gated — 02-12 not landed, insertion correctly withheld"` — either the phase entry exists (02-12 landed) or the gate correctly withheld it (02-12 not landed); both are passing outcomes, a silent skip with no SUMMARY note is not
  </acceptance_criteria>
  <verify>
    <automated>grep -c "hive.ts:267\|registry/router split from STRUCT-02\|Both criteria then hold" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP.md's four Phase-3 corrections land unconditionally; the post-Phase-5 phase insertion lands ONLY if 02-12 has already landed, and the outcome either way is recorded, never silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan <-> `.planning/` documents | Docs-only edit; no runtime trust boundary is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-09a | Tampering | accidental edit to `REQUIREMENTS.md:145`/`:452` (02-12's owned lines) | mitigate | This plan's own acceptance criteria diff those two exact lines against pre-edit content and fail if either changed |
| T-03-09b | Repudiation | the phase-insertion edit landing before 02-12, silently | mitigate | The gate check is a literal file-existence test run before the edit, with a recorded outcome either way — never a silent skip |
</threat_model>

<verification>
- `grep -c "hive.ts:267\|telemetry.ts:126" .planning/REQUIREMENTS.md .planning/ROADMAP.md` — 0 in both files
- `git diff -- .planning/REQUIREMENTS.md` shows no hunk touching lines 145/452
- `npm test` (full suite) — 0 failures, skip count unchanged (this plan touches no test-covered source)
</verification>

<success_criteria>
- REQUIREMENTS.md's STRUCT-02/anchor/owner-column corrections land; lines 145/452 are untouched
- ROADMAP.md's four Phase-3 corrections land unconditionally
- The post-Phase-5 phase insertion lands if and only if 02-12 has already landed, with the outcome
  stated in this plan's own SUMMARY.md either way
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-09-SUMMARY.md` when done
</output>
