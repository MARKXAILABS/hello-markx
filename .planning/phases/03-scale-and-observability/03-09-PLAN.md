---
phase: 03-scale-and-observability
plan: 9
type: execute
wave: 7
depends_on: ["03-01", "03-02", "03-03", "03-04", "03-05", "03-06", "03-07", "03-08"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements_addressed: [SCALE-01, SCALE-03]

must_haves:
  truths:
    - "REQUIREMENTS.md:164's STRUCT-02 messaging-split claim is corrected to reflect that messaging is still in hive.ts, with Phase 3 adopting the orphan correction explicitly"
    - "REQUIREMENTS.md:206 and :208's stale file:line anchors (telemetry.ts:126, hive.ts:267) are corrected to the measured lines (:161, :323)"
    - "SCALE-01 and SCALE-03 remain Pending in REQUIREMENTS.md — BOTH the checkbox AND the status-table row — after every other plan in this phase has already landed, proven by a criterion that would fail if any earlier plan's frontmatter had auto-ticked them"
    - "ROADMAP.md's Phase 3 section no longer claims STRUCT-02's registry/router split as SCALE-01's blocking dependency, verified with a check that is not satisfied by the phrase's pre-existing line-wrap"
    - "ROADMAP.md no longer claims re-ordering makes both SCALE-01 and SCALE-03's criteria hold, verified the same multiline-safe way, paired with the actual corrected sentence being present"
    - "ROADMAP.md's SCALE-03 justification no longer cites the 8MB rotation as the mechanism that holes a replayable day, and states the real gap (no time-range query mechanism) instead"
    - "The post-Phase-5 re-verification phase is inserted with a real phase id, carrying exactly the two skeptic tests already at ROADMAP.md:297-310, and this edit lands ONLY if 02-12 has already landed"
    - "If the post-Phase-5 phase is inserted and GSD is renumbered Phase 6 -> Phase 7, every one of REQUIREMENTS.md's 14 stale 'Phase 6' mentions, STATE.md's 2 mentions, AND ROADMAP.md's own 4 remaining mentions (round-2 finding #43) are swept to 'Phase 7' in the SAME gated edit — never left half-renumbered"
    - "The new inserted phase is named 'Phase 6' and GSD is named 'Phase 7' consistently everywhere in this plan's own text and edits — Task 1's owner-column example never contradicts Task 2's insertion, per round-2 findings #3/#8"
    - "Every acceptance criterion and <verify><automated> command in this plan is phrased so a correct fix EXITS 0 and a no-op or reverted fix EXITS NON-ZERO — never the inverted grep -c polarity where a landed fix exits 1"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "the corrected STRUCT-02/SCALE-01/SCALE-03 lines and anchors, and (if gated open) the Phase 6->7 sweep"
    - path: .planning/ROADMAP.md
      provides: "the corrected Phase 3 dependency reasoning and the new post-Phase-5 phase entry"
  key_links:
    - from: ".planning/ROADMAP.md (new phase entry)"
      to: ".planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md"
      via: "an existence check gating whether this plan's ROADMAP/REQUIREMENTS/STATE renumber edits land at all — checked independently, identically, by both tasks"
      pattern: "02-12-SUMMARY"
---

<objective>
Close the honesty work D-01, D-02, D-05, D-06, D-07, D-21 and D-39 assign to Phase 3: correct the
stale claims this project's own planning documents make about STRUCT-02, SCALE-01's dependency
reasoning, the "both criteria hold" overstatement, the 8MB-rotation justification, and three stale
file:line anchors — then insert a real phase id for the post-Phase-5 re-verification, gated on 02-12
having already landed, sweeping every stale "Phase 6" reference if the renumber actually happens. This
is the phase's own acceptance-honesty test, not a deviation from the phase's work.

Two of this plan's own draft acceptance criteria were themselves an instance of the systemic defect
this phase exists to remove: `grep -n "Both criteria then hold" .planning/ROADMAP.md` and `grep -n
"registry/router split from STRUCT-02" .planning/ROADMAP.md` both return `0` TODAY, unedited, because
both phrases straddle a hard line wrap in the source file — so "matches exactly 0" was satisfied
whether or not the correction was ever made. Every check in this plan is now multiline-safe (a
whitespace-squeezed scan via `node -e`, mirroring `test/boot-floor.test.cjs`'s own `joinedStripped()`
idiom) and paired with a POSITIVE lower bound on the replacement text, per D-38.

Purpose: this repo's own evidence is that unowned prose residuals rot — several are still open from
earlier phases, and the counter-example (#18) was caught precisely because it had an id. SCALE-01 and
SCALE-03 stay Pending; this plan makes that Pending state accurately explained rather than silently
inherited, and PROVEN to still be true after every other plan in this phase has already run — three
of those plans (01, 03, 07) would otherwise auto-tick these exact requirements via
`execute-plan.md`'s `update_requirements` step, which is why they carry `requirements_addressed`
instead of `requirements` in their own frontmatter (see each plan's own note).
Output: corrected `REQUIREMENTS.md` and `ROADMAP.md`, a `STATE.md` sync, and a blocking, non-inverted
proof that SCALE-01/SCALE-03 are still Pending.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md

<interfaces>
**Phase numbering, pinned ONCE here — every other mention in this plan must agree with this
paragraph, and round-2 red-team findings #3/#8 confirmed an earlier draft did not** (Task 1's own
example named "Phase 7" for the inserted phase while Task 2's insertion logic implies "Phase 6"):
IF the phase-insertion gate is open (02-12 has landed), the NEW phase inserted after Phase 5 is
**Phase 6, "Close the forward dependencies."** The EXISTING Phase 6, "The Office Runs the Process"
(the GSD phase), is renumbered to **Phase 7** in the SAME edit. Every reference in this plan's own
text, Task 1's owner-column example, and every acceptance criterion MUST use these two numbers
consistently — "Phase 6" for the NEW phase, "Phase 7" for GSD post-renumber. If the gate is closed,
neither number is assigned and both tasks leave the existing numbering untouched (recorded as
withheld, per D-05).

This plan does not touch source code — every task edits planning documents only. The full text of
every correction this plan makes is already written out, decision by decision, in
`03-CONTEXT.md`'s `<decisions>` section (D-01, D-02, D-05, D-06, D-07, D-21, D-39) and in its
corrections table at the top of the file. Read that section in full before editing either document —
do not paraphrase from this plan's summary of it.

**The line-wrap trap, verified this session, byte-for-byte** — read this before writing any grep or
`<verify>` command in this plan:
```
.planning/ROADMAP.md:285-286:
  1. **Run Phase 3 after Phases 4 and 5** (execution order `1 → 2 → 4 → 5 → 3 → 6`). Both
     criteria then hold under a skeptic's test on the day they are marked green.
.planning/ROADMAP.md:293-294:
  **Depends on**: Phase 2 — SCALE-01's per-project isolation needs the registry/router split
  from STRUCT-02, and SCALE-05's single card needs PARITY-02's all-engine cost. **Forward:**
```
`grep -c "Both criteria then hold" .planning/ROADMAP.md` and `grep -c "registry/router split from
STRUCT-02" .planning/ROADMAP.md` BOTH return `0` against the UNEDITED file today — single-line grep
can never match either phrase, wrapped or not. Every check in this plan therefore reads the whole
file, squeezes whitespace (`.replace(/\s+/g, ' ')`), THEN checks — the same technique
`test/boot-floor.test.cjs:422`'s `joinedStripped()` already uses for exactly this reason.

**Corrections table** (from `03-CONTEXT.md`, reproduced here for the exact target lines only — the
CONTENT of each correction is in the source document, not repeated here):
| Document | Line(s) | What is wrong today |
|---|---|---|
| REQUIREMENTS.md | 164 | STRUCT-02 lists "messaging" among split concerns; messaging is still in hive.ts (D-01) |
| REQUIREMENTS.md | 206 | span ring cited at telemetry.ts:126; actually :161 (D-39) |
| REQUIREMENTS.md | 208 | LOG_ROTATE_BYTES cited at hive.ts:267; actually :323 (D-39, D-21) |
| ROADMAP.md | 281 | Same hive.ts:267 -> :323 anchor (D-39) |
| ROADMAP.md | 283 | "the 8MB rotation is what holes a replayable day" — refuted, it has never fired; the real cause is the absence of any time-range query mechanism (D-21) |
| ROADMAP.md | 293-294 | "SCALE-01's per-project isolation needs the registry/router split from STRUCT-02" — not supported by code; the isolation seam is hive.root() (D-02) |
| ROADMAP.md | 286 | "Both criteria then hold" under the reorder option — false for SCALE-01, since RECALL-02 is memory-only, not task-ledger (D-06) |
| ROADMAP.md | 297-310 | the two skeptic tests to carry verbatim into the new inserted phase (D-05) |

**MUST NOT edit**: `REQUIREMENTS.md:145` and `:452` (PARITY-02's text and status row) — owned
exclusively by plan `02-12`. If `02-12-SUMMARY.md` does not yet exist on disk when this plan runs,
that is fine and expected — this plan's exclusion still holds regardless of whether 02-12 has landed.

`REQUIREMENTS.md`'s status table (the `| SCALE-01 | Phase 3 | Pending |` / `| SCALE-03 | Phase 3 |
Pending |` rows, near the bottom of the file, and the `- [ ] **SCALE-01**:` / `- [ ] **SCALE-03**:`
checkbox lines near :152/:156) currently name only "Phase 3" as owner — this plan widens that to name
the NEW post-Phase-5 phase as the re-verification owner, per D-07 ("never 'later'"), IF AND ONLY IF
that phase actually gets created this run (the same `02-12-SUMMARY.md` gate both tasks check
independently). If the gate is closed, the owner column stays "Phase 3" WITH an inline note that the
re-verification phase id is pending 02-12 — never silently left to read "Phase 3" with no explanation
once a reader knows a renumber was supposed to happen.

**The Phase 6 -> 7 sweep, if the renumber lands** (verified this session): `grep -c "Phase 6"
.planning/REQUIREMENTS.md` returns `14` — 12 status-table rows (`GSD-01..05`, `DESK-01..04`,
`REACH-01..03`, lines ~485-497), the mapped-count line (:501, `Phase 6: 12`), and the split-category
line (:519, "01-05 in Phase 6"). `grep -n "Phase 6" .planning/STATE.md` returns 2 hits (:386, :586).
**`grep -n "Phase 6" .planning/ROADMAP.md` returns 4 hits of its own** (round-2 finding #43 — an
earlier draft's sweep list omitted ROADMAP.md's own remaining mentions): the phase-list prose line
(:21, "...Phase 6 runs GSD..."), the checkbox summary line (:45, "**Phase 6: The Office Runs the
Process**"), the section heading itself (:456), and the forward-dependency prose (:470, "Phase 6
carries..."). If the phase-insertion gate is open and GSD is renumbered to Phase 7, ALL 20 of these
mentions across the three documents sweep in the SAME gated edit — a partial sweep would ship
03-09's own plan (the one whose purpose is removing rotted cross-document anchors) with fresh ones.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md corrections — the orphan adoption, the two stale anchors, the owner column, and the SCALE-01/SCALE-03 Pending proof</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md (the STRUCT-02 line at :164, the SCALE-01/SCALE-03 checkbox lines at :152/:156, the status rows near the bottom, the RECORD-02 line naming hive.ts:267, the RECORD-01 line naming telemetry.ts:126, all 14 "Phase 6" mentions), .planning/phases/03-scale-and-observability/03-CONTEXT.md (D-01, D-07, D-39 full text), .planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md (check existence only — do not assume; this task runs its OWN gate check, identical to Task 2's, rather than trusting Task 2's outcome)</read_first>
  <action>
    Edit `REQUIREMENTS.md:164` (STRUCT-02's requirement text) to remove "messaging" from the list of
    split concerns, or to explicitly annotate it as an adopted-but-still-open orphan per D-01 — match
    whichever phrasing keeps the line a single, accurate sentence rather than a hedge; state that
    Phase 3 adopts this as a named open item rather than silently absorbing it. Correct the
    `telemetry.ts:126` anchor (near RECORD-01's text, `:206`) to `telemetry.ts:161`. Correct the
    `hive.ts:267` anchor (near RECORD-02's text, `:208`) to `hive.ts:323`. **Gate check, run before
    the owner-column edit**: check whether
    `.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md` exists on disk (this is the
    SAME check Task 2 runs independently — both tasks check the identical, static condition rather
    than one trusting the other's outcome, since the two tasks may not run in a fixed order). If it
    EXISTS: update the `SCALE-01`/`SCALE-03` status-table owner column from "Phase 3" to name the new
    post-Phase-5 phase — "Phase 6 — Close the forward dependencies", per this plan's `<interfaces>`
    pin (round-2 findings #3/#8: an earlier draft named "Phase 7" here, contradicting Task 2's own
    insertion, which places the new phase BEFORE the renumbered GSD phase) — AND sweep all 14
    of this file's "Phase 6" mentions (the 12 GSD/DESK/REACH status rows, the mapped-count line, the
    split-category line) to "Phase 7". If it does NOT exist: leave the owner column as "Phase 3" but
    add an inline note next to both rows stating the re-verification phase id is pending 02-12 landing
    — never leave it reading bare "Phase 3" with no explanation once this plan has run. Do NOT tick
    either SCALE-01 or SCALE-03's checkbox — both stay `- [ ]`. Do NOT edit lines 145 or 452.
  </action>
  <acceptance_criteria>
    - `grep -n "telemetry.ts:126" .planning/REQUIREMENTS.md` matches exactly `0`
    - `grep -n "hive.ts:267" .planning/REQUIREMENTS.md` matches exactly `0`
    - `grep -n "telemetry.ts:161" .planning/REQUIREMENTS.md` matches `>= 1`
    - `grep -n "hive.ts:323" .planning/REQUIREMENTS.md` matches `>= 1`
    - `sed -n '145p;452p' .planning/REQUIREMENTS.md` is BYTE-IDENTICAL to its pre-edit content (confirm via `git diff -- .planning/REQUIREMENTS.md` showing no hunk touching those two lines)
    - **The blocking, non-vacuous Pending proof** — run AFTER every other plan in this phase has
      already landed (this task is wave 6, last): `grep -c '^- \[ \] \*\*SCALE-01\*\*' .planning/REQUIREMENTS.md` equals exactly `1`, AND `grep -c '^- \[ \] \*\*SCALE-03\*\*' .planning/REQUIREMENTS.md` equals exactly `1`, AND `grep -c '^- \[x\] \*\*SCALE-01\*\*\|^- \[x\] \*\*SCALE-03\*\*' .planning/REQUIREMENTS.md` equals exactly `0` — if any of the three plans that carry `requirements_addressed` instead of `requirements` (01, 03, 07) had auto-ticked either checkbox, this criterion fails
    - `grep -n "SCALE-01" .planning/REQUIREMENTS.md | grep -i "pending"` matches `>= 1` AND `grep -n "SCALE-03" .planning/REQUIREMENTS.md | grep -i "pending"` matches `>= 1` (the status-table row is also still Pending, not only the checkbox)
    - IF `.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md` exists: `grep -c "Phase 6" .planning/REQUIREMENTS.md` equals exactly `0` AND `grep -c "Phase 7" .planning/REQUIREMENTS.md` is `>= 14`; IF it does NOT exist: `grep -c "Phase 6" .planning/REQUIREMENTS.md` is unchanged from this task's own pre-edit baseline (14) — either outcome is passing, a partial sweep is not
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('.planning/REQUIREMENTS.md','utf8');const stale=(s.match(/hive\.ts:267|telemetry\.ts:126/g)||[]).length;const ticked=(s.match(/^- \[x\] \*\*SCALE-0[13]\*\*/gm)||[]).length;if(stale>0){console.error('FAIL: stale anchor(s) still present:',stale);process.exit(1);}if(ticked>0){console.error('FAIL: SCALE-01/SCALE-03 was auto-ticked:',ticked);process.exit(1);}console.log('PASS: anchors corrected, SCALE-01/SCALE-03 still Pending');"</automated>
  </verify>
  <done>REQUIREMENTS.md's two stale anchors are corrected, the STRUCT-02 orphan is adopted explicitly, the owner column names a real phase id (or an explicit pending-02-12 note) instead of bare "Phase 3"/"later", and SCALE-01/SCALE-03 are PROVEN still Pending after every other plan in this phase has run — the check exits non-zero if it is not true, never the inverted polarity.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md corrections and the gated post-Phase-5 phase insertion, with STATE.md and the Phase 6 sweep</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>.planning/ROADMAP.md (Phase 3's full section, lines ~272-315, and lines 281/283/285-286/293-294/297-310 specifically — read the line-wrap warning in this plan's <interfaces> block first), .planning/phases/03-scale-and-observability/03-CONTEXT.md (D-02, D-05, D-06, D-21, D-39 full text), .planning/STATE.md (the Pending Todos section and its 2 "Phase 6" mentions), .planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md (check existence only)</read_first>
  <action>
    Correct `ROADMAP.md:281`'s anchor (`hive.ts:267` -> `:323`). Correct `ROADMAP.md:283`'s
    justification: rewrite the SCALE-03 table row so it no longer claims the 8MB rotation is what
    holes a replayable day — state instead that the rotation has never fired in measured use and the
    real gap is the absence of any time-range query mechanism, and use the phrase "time-range query"
    somewhere in the corrected sentence (this becomes this plan's own positive-lower-bound check
    below). Correct `ROADMAP.md:293-294`'s stated dependency reasoning: remove the claim that SCALE-01
    needs STRUCT-02's registry/router split, and state instead that the isolation seam is
    `hive.root()` (use that exact token — it becomes the positive-lower-bound check below), already
    lazy, unaffected by whether messaging is extracted. Correct `ROADMAP.md:285-286`'s "Both criteria
    then hold" under the reorder option: state instead that re-ordering does NOT make SCALE-01 hold
    (use that exact phrase), since RECALL-02 is memory-only (use "RECALL-02 is memory-only" verbatim —
    both become positive-lower-bound checks below) and the task-ledger half is Phase 3's own net-new
    work under either ordering. THEN — and only after re-reading the check below — insert a new phase
    entry after Phase 5, headed EXACTLY `### Phase 6: Close the forward dependencies` (per this plan's
    `<interfaces>` pin — the NEW phase is Phase 6, never Phase 7), carrying EXACTLY the two skeptic
    tests already written at `ROADMAP.md:297-310` verbatim (do not rewrite their wording, only
    relocate/reference them), and renumber the EXISTING `### Phase 6: The Office Runs the Process`
    (the GSD phase) to `### Phase 7: The Office Runs the Process` (a doc-only renumber, since no
    `07-*`/GSD phase directory exists yet) IN THE SAME EDIT. Also sweep ROADMAP.md's OWN remaining
    "Phase 6" mentions that refer to the GSD phase — verified this session, 4 total: the phase-list
    line (`:21`, "...Phase 6 runs GSD..."), the checkbox summary line (`:45`, "**Phase 6: The Office
    Runs the Process**"), the section heading itself (`:456`), and the forward-dependency prose
    (`:470`, "Phase 6 carries...") — to "Phase 7", so the renumber is not partial WITHIN this same
    file (round-2 finding #43: a prior draft's sweep only covered REQUIREMENTS.md/STATE.md and missed
    ROADMAP.md's own remaining mentions). **Gate check, run BEFORE making this one insertion — the
    SAME independent check Task 1 also runs**: verify
    `.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md` exists on disk. If it does NOT
    exist, do NOT insert the new phase or renumber GSD — leave `ROADMAP.md`'s phase list and numbering
    exactly as they are today, complete every OTHER correction in this task normally, and record in
    this plan's SUMMARY.md that the phase-insertion edit was deliberately withheld pending 02-12, per
    D-05's own explicit instruction ("do not land this roadmap edit until 02-12 lands"). If it DOES
    exist and the phase is inserted: sweep `STATE.md`'s 2 "Phase 6" mentions (:386, :586) to "Phase 7"
    in the SAME edit (mirroring Task 1's REQUIREMENTS.md sweep — do not leave one document
    half-renumbered relative to the other). Sync `STATE.md`'s Pending Todos section to reflect whichever
    outcome actually occurred (inserted, or withheld-pending-02-12) either way.
  </action>
  <acceptance_criteria>
    - `grep -n "hive.ts:267" .planning/ROADMAP.md` matches exactly `0`
    - `grep -n "hive.ts:323" .planning/ROADMAP.md` matches `>= 1`
    - The four multiline-aware corrections, checked over WHITESPACE-SQUEEZED text (never a bare
      single-line grep, per this plan's own <interfaces> warning) — run:
      `node -e "const fs=require('fs');const j=fs.readFileSync('.planning/ROADMAP.md','utf8').replace(/\s+/g,' ');const checks=[['registry/router split from STRUCT-02',0],['Both criteria then hold',0],['hive.root()',1],['RECALL-02 is memory-only',1],['does NOT make SCALE-01 hold',1],['time-range query',1]];let fail=0;for(const [needle,minOrMax] of checks){const count=j.split(needle).length-1;if(minOrMax===0&&count>0){console.error('FAIL: stale phrase still present:',needle);fail=1;}if(minOrMax===1&&count<1){console.error('FAIL: replacement phrase missing:',needle);fail=1;}}process.exit(fail);"`
      exits `0`
    - **Non-vacuous insertion proof** (round-2 finding #25: a prior draft's `test -f ... && grep | wc
      -l || echo ...` pipeline ALWAYS exits `0` regardless of outcome, since `wc -l` itself always
      succeeds — a forgotten insertion passed silently). Replace it with:
      `node -e "const fs=require('fs');const gate=fs.existsSync('.planning/phases/02-the-daemon-and-the-protocol/02-12-SUMMARY.md');const roadmap=fs.readFileSync('.planning/ROADMAP.md','utf8');const inserted=/### Phase 6: Close the forward dependencies/.test(roadmap);if(gate!==inserted){console.error('FAIL: gate state ('+gate+') disagrees with insertion state ('+inserted+')');process.exit(1);}if(inserted&&/### Phase 6: The Office Runs the Process/.test(roadmap)){console.error('FAIL: old Phase 6 heading still present after insertion — half-renumbered');process.exit(1);}console.log('PASS:', inserted?'phase inserted and gate open':'gate closed, insertion correctly withheld');"`
      exits `0` in BOTH outcomes (gate open+inserted, or gate closed+withheld) and exits `1` if the
      two states ever disagree
    - IF the phase was inserted: `grep -c "Phase 6" .planning/STATE.md` equals exactly `0` AND `grep -c "Phase 7" .planning/STATE.md` is `>= 2`; IF withheld: `grep -c "Phase 6" .planning/STATE.md` is unchanged from this task's pre-edit baseline (2)
    - IF the phase was inserted: `grep -c "### Phase 6: The Office Runs the Process" .planning/ROADMAP.md` equals exactly `0` (the OLD GSD heading is gone, renumbered — never left half-renumbered) AND `grep -c "Phase 6" .planning/ROADMAP.md` is `>= 1` (the NEW phase's own heading survives, naming itself correctly) AND `grep -c "Phase 7" .planning/ROADMAP.md` is `>= 4` (round-2 finding #43 — GSD's four former "Phase 6" mentions are now "Phase 7," not only the two-phrase spot-fixes above); IF withheld: `grep -c "Phase 6" .planning/ROADMAP.md` is unchanged from this task's pre-edit baseline (4, verified this session — the phase-list line :21, the checkbox summary :45, the section heading :456, the forward-dependency prose :470)
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const j=fs.readFileSync('.planning/ROADMAP.md','utf8').replace(/\s+/g,' ');const stale=['hive.ts:267','registry/router split from STRUCT-02','Both criteria then hold'];let bad=0;for(const s of stale){if(j.includes(s)){console.error('FAIL: stale phrase present:',s);bad=1;}}if(bad){process.exit(1);}console.log('PASS: all three stale ROADMAP phrases are gone');"</automated>
  </verify>
  <done>ROADMAP.md's four Phase-3 corrections land unconditionally, verified against whitespace-squeezed text rather than a single-line grep the pre-existing line-wrap would satisfy for free; the post-Phase-5 phase insertion (and the matching REQUIREMENTS.md/STATE.md Phase 6->7 sweep) lands ONLY if 02-12 has already landed, and the outcome either way is recorded, never silent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| This plan <-> `.planning/` documents | Docs-only edit; no runtime trust boundary is touched |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-09a | Tampering | accidental edit to `REQUIREMENTS.md:145`/`:452` (02-12's owned lines) | mitigate | This plan's own acceptance criteria diff those two exact lines against pre-edit content and fail if either changed |
| T-03-09b | Repudiation | the phase-insertion edit landing before 02-12, silently | mitigate | The gate check is a literal file-existence test run INDEPENDENTLY by both tasks (never one trusting the other's outcome), with a recorded outcome either way — never a silent skip |
| T-03-09c | Repudiation | this plan's own acceptance criteria being vacuous (satisfied by the pre-existing line-wrap, or inverted exit-code polarity) — the exact defect class this plan exists to remove, found in its own earlier draft by red-team findings #10-12, #21, #26 | mitigate | Every multiline-sensitive check in this plan now squeezes whitespace before matching (mirroring `test/boot-floor.test.cjs`'s `joinedStripped()`), pairs every negative with a positive lower bound on the replacement text, and every `<verify><automated>` exits 0 on success / non-zero on failure — never the inverted `grep -c` form where a correct fix returns a non-zero exit |
| T-03-09d | Repudiation | a partial Phase 6->7 renumber sweep leaving REQUIREMENTS.md and STATE.md disagreeing with the renumbered ROADMAP.md | mitigate | Both documents' sweeps are gated on the SAME condition and land in the same task run (Task 1 for REQUIREMENTS.md, Task 2 for STATE.md — Task 2 also performs the ROADMAP.md renumber itself, so the renumber and its STATE.md echo are atomic within one task) |
</threat_model>

<verification>
- `node -e "const fs=require('fs');['.planning/REQUIREMENTS.md','.planning/ROADMAP.md'].forEach(f=>{const s=fs.readFileSync(f,'utf8');if(/hive\.ts:267|telemetry\.ts:126/.test(s)){console.error('FAIL',f);process.exit(1);}});console.log('PASS: no stale anchors in either file');"` exits 0
- `git diff -- .planning/REQUIREMENTS.md` shows no hunk touching lines 145/452
- `npm test` (full suite) — 0 failures, skip count unchanged (this plan touches no test-covered source)
</verification>

<success_criteria>
- REQUIREMENTS.md's STRUCT-02/anchor/owner-column corrections land; lines 145/452 are untouched
- ROADMAP.md's four Phase-3 corrections land unconditionally, verified against whitespace-squeezed
  text so the pre-existing line-wrap cannot satisfy the check for free
- SCALE-01 and SCALE-03 are PROVEN Pending (checkbox AND status row) after every other plan in this
  phase has already run, via a criterion that fails if any earlier plan auto-ticked either one
- The post-Phase-5 phase insertion (named "Phase 6" consistently, never contradicting Task 1's own
  owner-column reference), and the REQUIREMENTS.md/STATE.md/ROADMAP.md Phase 6->7 sweep it implies
  (including ROADMAP.md's own 4 remaining mentions, round-2 #43), land if and only if 02-12 has
  already landed, with the outcome stated in this plan's own SUMMARY.md either way
- Every acceptance criterion and `<verify><automated>` command in this plan exits 0 on a correct fix
  and non-zero on a no-op — never the inverted polarity
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-09-SUMMARY.md` when done
</output>
