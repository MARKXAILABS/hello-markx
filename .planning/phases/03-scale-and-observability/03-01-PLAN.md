---
phase: 03-scale-and-observability
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/db.ts
  - src/main/knowledge.ts
  - src/main/config.ts
  - src/main/floor/boot.ts
  - src/main/index.ts
  - README.md
  - src/renderer/src/components/MemoryPanel.tsx
  - src/renderer/src/components/HivePicker.tsx
  - CHANGELOG.md
  - test/db-fts.test.cjs
autonomous: true
requirements_addressed: [SCALE-01]

must_haves:
  truths:
    - "A PersistStore constructed AFTER harnessHome is already set defaults its DB file under that home, not under userData"
    - "A PersistStore constructed BEFORE harnessHome is set (the fresh-install case) and then repointed at the null->set transition ends up reading and writing under the NEW home — it does not stay stranded at the pre-onboarding path"
    - "KnowledgeManager.root() returns a harnessHome-relative path once harnessHome is set, with the default-path expression as the only code change (no repoint plumbing — it recomputes on every call)"
    - "The task ledger's per-project isolation required ZERO new code (D-11) and no task in this plan claims otherwise"
    - "config.ts's second PersistStore handle (firedStore) repoints in the SAME transition as boot.ts's persist handle, so a mission does not fire off the previous project's missionLastFiredAt"
    - "config:changeHome's 'move' mode actually moves harness.db and the knowledge-graph store, not only hive/palace/roster — and closes the live persist handle before copying so no WAL is open across cpSync"
    - "README.md and MemoryPanel.tsx no longer instruct the operator to switch to a per-agent memory scope control that does not exist in the shipped app, using the ACTUAL wording each file carries today, not a paraphrase"
    - "HivePicker.tsx no longer tells the operator they can run different setups side by side — the exact sentence D-13 exists to remove is gone, replaced by the honest one-project-at-a-time statement"
    - "The existing-install data question for command_history/kv/knowledge-graph documents is resolved ONE way and stated in CHANGELOG.md, never silently dropped"
    - "This plan does NOT deliver simultaneous side-by-side projects, and README.md and HivePicker.tsx both say so"
  artifacts:
    - path: src/main/db.ts
      provides: "PersistStore.repoint() and a harnessHome-aware default DB path via an injected getHome getter"
    - path: src/main/knowledge.ts
      provides: "harnessHome-aware default path for KG_ROOT, no repoint needed"
    - path: src/main/config.ts
      provides: "repointFiredStore() export forcing the mission-stamps handle to reopen at its new default path"
  key_links:
    - from: "src/main/index.ts (config:update handler)"
      to: "src/main/db.ts (persist.repoint()) and src/main/config.ts (repointFiredStore())"
      via: "the existing !hiveWasEnabled && hive.enabled() hook"
      pattern: "repoint"
---

> **Requirements-ledger note — read before running the completion step.** This plan's
> frontmatter key is `requirements_addressed`, not `requirements`, and that is deliberate.
> D-07 forbids ticking SCALE-01 in Phase 3. `execute-plan.md`'s `update_requirements` step only
> acts on a literal `requirements:` frontmatter key ("If no requirements field, skip") — this
> plan carries no such key, so `requirements.mark-complete` never runs for it.
> `requirements_addressed` exists only so `plan-phase.md`'s coverage gate (which greps
> `requirements_addressed\|requirements:`) still counts SCALE-01 as planned. If you are an
> executor and something tries to auto-tick SCALE-01 in `.planning/REQUIREMENTS.md` as part of
> finishing this plan, STOP — that is the exact defect (red-team finding #1) this note exists to
> prevent. 03-09 (the last plan in this phase) asserts SCALE-01 is still `Pending` after every
> other plan lands.

<objective>
Make the two stores that escaped `harnessHome` (`harness.db`, `KG_ROOT`) follow it, so two projects
run side by side without sharing one database or one knowledge-graph store — SCALE-01's whole code
change, per D-08/D-09. The task ledger half is already free by construction (`hive.root()`); this
plan does not touch it and does not claim to deliver it.

Purpose: close the two literal path escapes that make "an agent in project X cannot see project Y's
data" false today for exactly two stores, while leaving the task ledger's already-true isolation
alone.
Output: `PersistStore.repoint()`, both harnessHome-aware default paths, the one repoint call site in
`config:update`, `config:changeHome`'s move-list closing the same two escapes, and the doc/UI
corrections D-13 and D-14 require.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-RESEARCH.md

<interfaces>
From `src/main/db.ts` (current, verified this session):
```
export class PersistStore {
  private db: Database.Database | null = null;
  constructor(private dbPath?: string) {}
  open(): void {
    if (this.db) return;
    const path = this.dbPath ?? join(app.getPath('userData'), 'harness.db');
    ...
  }
  close(): void { try { this.db?.close(); } catch {} this.db = null; }
  get isOpen(): boolean { return this.db !== null; }
  getKv<T>(key: string): T | undefined
  setKv(key: string, value: unknown): void
}
```
`MIGRATIONS` is an append-only array at the top of the file — do not touch it in this plan (SCALE-03
owns migration #3, a different plan).

From `src/main/knowledge.ts` (current):
```
export class KnowledgeManager {
  active(): boolean
  root(): string {   // computed FRESH on every call, no caching
    const override = readConfig().knowledgeGraph?.rootPath;
    if (override && override.trim()) return override;
    return join(app.getPath('userData'), 'knowledge');
  }
  env(): Record<string, string>   // injects KG_ROOT into every agent spawn
}
```
`knowledge.ts` already `import { readConfig } from './config'` — no circular-import problem here,
unlike `db.ts` (which `config.ts` itself imports `PersistStore` FROM, so `db.ts` must never import
`config.ts`).

From `src/main/config.ts` (current):
```
let firedDb: PersistStore | null | undefined;   // undefined = not opened yet
function firedStore(): PersistStore | null {
  if (firedDb !== undefined) return firedDb;
  firedDb = null;
  try { const s = new PersistStore(); s.open(); firedDb = s; }
  catch (e) { console.warn(...); }
  return firedDb;
}
```
`config.ts` already imports `PersistStore` from `./db` at its top.

From `src/main/floor/boot.ts` (current, the ONLY production construction site besides config.ts's):
```
persist = new PersistStore();   // ~line 1148, inside bootFloor()
```
`export let persist: PersistStore;` is declared at `boot.ts:163` and already imported directly
into `index.ts` (used at 15+ call sites there, e.g. `persist.getKv(...)`, `persist.setKv(...)`).

From `src/main/index.ts`'s `config:update` handler (current, the ONLY non-relaunching
`harnessHome: null -> set` transition):
```
ipcMain.handle('config:update', (_evt, patch) => {
  const hiveWasEnabled = hive.enabled();
  const next = writeConfig(patch);
  if (typeof patch?.telemetryEnabled === 'boolean') analytics.setEnabled(patch.telemetryEnabled);
  if (!hiveWasEnabled && hive.enabled()) {
    console.log('[hive] harnessHome configured — bootstrapping hive services');
    try { startHiveServices(); } catch (e) { console.error(...); }
  }
  return next;
});
```

From `src/main/index.ts`'s `config:changeHome` handler (current, verified this session — the
'move' branch this plan's Task 2 must widen, AND its copy-failure recovery path, which does NOT
relaunch — round-2 red-team finding #2 turns on this exact detail):
```
if (mode === 'move' && oldHome) {
  try {
    for (const sub of ['hive', 'palace', 'roster.json', 'roster-backups']) {
      const src = join(oldHome, sub);
      if (!existsSync(src)) continue;
      cpSync(src, join(newHome, sub), { recursive: true, force: true, dereference: false });
    }
  } catch (e) {
    // Copy failed: recover IN PLACE against the unchanged old home (config never
    // repointed) so the user loses nothing, and surface the error — no relaunch.
    startHiveServices();
    const cfg = readConfig();
    if (cfg.slackEnabled && cfg.slackSigningSecret) void startSlackServer();
    reconcileWebhookServer();
    return { ok: false, error: `Could not copy data: ${e instanceof Error ? e.message : String(e)}` };
  }
}
// Repoint config and relaunch so every service re-bootstraps against newHome.
allowQuit = true;
writeConfig({ harnessHome: newHome });
try { ptyManager.killAll(); } catch (e) { ... }
app.relaunch();
app.exit(0);
```
This list runs AFTER a teardown block of ~11 `try { stopX(); } catch {}` calls (clearMissionTimers,
clearContextTimers, stopWebhookDoneObserver, stopEphemeralWorkerWatcher, integrationBroker.stop,
hive.stopRouter, hookServer.stop, stopSlackServer, stopWebhookServer, memory.stop, reflector.stop,
delivery.stop) but that teardown block never closes `persist` — the module-level `PersistStore`
handle this plan's Task 1/2 point at `<home>/harness.db` — so a WAL file can be open across the
`cpSync` above.

**`config:changeHome` does NOT always end in `app.relaunch()`** — verified this session, the
copy-failure `catch` block (index.ts:2779-2787) calls `startHiveServices()` and `return`s an error
WITHOUT relaunching. `startHiveServices()` (`boot.ts:1184`) never touches `persist` — it only
re-arms hive/router/mission/context/webhook subsystems (verified this session: it constructs
nothing, only calls `hive.ensureHive()`, `loadPreservedWorktrees()`, `hive.startRouter()`, and a
handful of sync/start calls). So if Task 2 closes `persist` before the copy runs (the WAL-safety
fix below requires this) and the copy then fails, `persist` is stranded CLOSED for the rest of the
process's life unless this SAME recovery path explicitly reopens it — this is round-2 red-team
finding #2, and the fix below (`persist.repoint()` inside the `catch` block, immediately beside
`startHiveServices()`) closes it. The OLD home is still the config's active `harnessHome` on the
failure path (the copy fails BEFORE `writeConfig({harnessHome: newHome})` ever runs), so
`repoint()` correctly reopens `persist` against the unchanged old path.

`app:resetAll` (a DIFFERENT handler) still always ends in `app.relaunch(); app.exit(0)` — only
`config:changeHome`'s copy-failure path is the exception this plan's Task 2 must handle; the
non-relaunching `config:update` transition (Task 2, below) is the other one.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersistStore.repoint() and the two harnessHome-aware default paths</name>
  <files>src/main/db.ts, src/main/knowledge.ts, test/db-fts.test.cjs</files>
  <read_first>src/main/db.ts (full — open/close/constructor), src/main/knowledge.ts (root()/env()), test/db-fts.test.cjs (the existing real-SQLite-driver pattern this test file already uses)</read_first>
  <behavior>
    - `new PersistStore(undefined, () => '/tmp/home-a').open()` writes its DB file under
      `/tmp/home-a/harness.db`, never under `app.getPath('userData')`.
    - `new PersistStore(undefined, () => null).open()` (harnessHome not yet set) falls back to the
      existing `app.getPath('userData')` path unchanged — zero behavior change for a fresh install
      before onboarding.
    - A `PersistStore` opened while `getHome()` still returns `null`, then `repoint()`ed after the
      injected getter starts returning a real path, closes its stale handle and reopens under the
      new path — `db.isOpen` stays `true` throughout and a `setKv`/`getKv` round-trip after
      `repoint()` reads back the value written after the repoint, not before.
    - `new PersistStore('/explicit/test/path.db').open()` (the existing test-override constructor
      form) is UNCHANGED — the injected `getHome` getter is never consulted when `dbPath` is set.
    - `KnowledgeManager.root()` returns `join('/tmp/home-a', 'knowledge')` once `readConfig().harnessHome`
      is `/tmp/home-a`, with no caching — two consecutive calls after `harnessHome` changes both
      reflect the new value immediately (no repoint method needed on `KnowledgeManager`).
  </behavior>
  <action>
    In `db.ts`: widen `PersistStore`'s constructor to `constructor(private dbPath?: string, private
    getHome?: () => string | null) {}`. In `open()`, change the path expression to prefer
    `this.dbPath`, then `this.getHome?.()` joined with `'harness.db'`, then the existing
    `app.getPath('userData')` fallback — `this.getHome` is an injected closure (matching `hive.ts`'s
    `getHome: () => string | null` pattern), never a direct `readConfig()` call, because `config.ts`
    already imports `PersistStore` from this file and a reverse import would cycle. Add a `repoint():
    void` method that calls `this.close()` then `this.open()` — do NOT clear or touch `this.dbPath`;
    since the default-path branch is already re-evaluated fresh inside `open()`, closing and
    reopening is sufficient and this can never disturb a genuine test override (production callers
    never pass `dbPath`). In `knowledge.ts`: change `root()`'s fallback branch from
    `join(app.getPath('userData'), 'knowledge')` to `join(readConfig().harnessHome ??
    app.getPath('userData'), 'knowledge')` — one line, no new method, no caching to invalidate.
    Extend `test/db-fts.test.cjs` with the repoint round-trip case described in `<behavior>`, using a
    second real tmpdir the way the file's existing tests already do (grep the file for its current
    tmpdir-per-test pattern before adding a new one).
  </action>
  <acceptance_criteria>
    - `grep -n "getHome" src/main/db.ts` matches `>= 1` (the injected getter is present)
    - `grep -n "repoint(): void" src/main/db.ts` matches `>= 1`
    - `grep -n "readConfig" src/main/db.ts` matches exactly `0` (no forward import of config.ts —
      the circular-import constraint is never violated)
    - `grep -n "readConfig().harnessHome" src/main/knowledge.ts` matches `>= 1`
    - `node --test test/db-fts.test.cjs` passes with 0 failures, including the new repoint case
  </acceptance_criteria>
  <verify>
    <automated>node --test test/db-fts.test.cjs</automated>
  </verify>
  <done>PersistStore.repoint() exists and is exercised by a real-SQLite test; KnowledgeManager.root() is harnessHome-aware; db.ts still has zero imports from config.ts.</done>
</task>

<task type="auto">
  <name>Task 2: wire the one repoint call site, both handles, and close the escape in config:changeHome's move</name>
  <files>src/main/config.ts, src/main/floor/boot.ts, src/main/index.ts</files>
  <read_first>src/main/config.ts (firedStore()/firedDb, the withMissionStamps/stripMissionStamps pair around it), src/main/floor/boot.ts (the `persist = new PersistStore();` line inside bootFloor(), and export let persist declaration near the top), src/main/index.ts (the config:update handler and its existing import list from './config'; the config:changeHome handler's teardown block and 'move' sub-array at ~line 2752-2778)</read_first>
  <behavior>
    - The `config:update` null->set transition repoints both `persist` and the fired-store handle,
      exactly as before.
    - `config:changeHome` in `'move'` mode, given an old home containing `harness.db` (plus its
      `-wal`/`-shm` siblings, if present) and a `knowledge/` directory, copies BOTH into the new home
      alongside the existing `hive`/`palace`/`roster.json`/`roster-backups` — a post-move read of
      `harness.db`'s kv store and the knowledge-graph index at the NEW home returns the same data
      that was at the old one.
    - The live `persist` handle is closed BEFORE the copy runs, so no WAL file is open across
      `cpSync`. On SUCCESS, `config:changeHome` relaunches right after (`app.relaunch();
      app.exit(0)`), reconstructing `persist` fresh — no repoint needed on that path. On COPY
      FAILURE, the handler recovers IN PLACE against the unchanged OLD home and returns WITHOUT
      relaunching (index.ts:2779-2787) — `persist` is explicitly `repoint()`ed back open on THIS
      SAME path, never left stranded closed for the rest of the process's life.
  </behavior>
  <action>
    In `config.ts`: change `firedStore()`'s `new PersistStore()` call to `new PersistStore(undefined,
    () => readConfig().harnessHome)` (safe here — `readConfig` is a local function in the same file,
    not a reverse import). Add an exported `repointFiredStore(): void` that does `firedDb?.close();
    firedDb = undefined;` — clearing the module-level cache forces the NEXT `firedStore()` call to
    reopen against the freshly-resolved default path, mirroring `PersistStore.repoint()`'s
    close-then-reopen shape at this file's own memoization layer. In `boot.ts`: change the one
    `persist = new PersistStore();` call to `persist = new PersistStore(undefined, () =>
    readConfig().harnessHome);` — no other line in this file changes. In `index.ts`'s `config:update`
    handler: inside the existing `if (!hiveWasEnabled && hive.enabled())` block, add `persist.repoint();
    repointFiredStore();` BEFORE the `startHiveServices()` call — this order is MANDATORY, never "or
    after" (round-2 finding #24: running `startHiveServices()` first reproduces the exact D-12
    mission-stamp bug this plan exists to fix, since `startHiveServices()` reads `missionLastFiredAt`
    off whichever handle is open at that moment — the stale pre-onboarding one, if the repoint calls
    come later) (import `repointFiredStore` alongside the file's existing named imports from
    `./config`) — this is the ONE non-relaunching `harnessHome: null -> set` transition (D-12), and
    both live handles must repoint together, BEFORE anything reads through them, or missions fire off
    the previous project's `missionLastFiredAt`. Do NOT add any repoint call to `config:changeHome` or
    `app:resetAll` — both already end in `app.relaunch(); app.exit(0)`, which destroys and
    reconstructs `persist`/`firedDb` for free.
    Separately, in `index.ts`'s `config:changeHome` handler's `if (mode === 'move' && oldHome) { try
    {...} catch (e) {...} }` block (index.ts:2765-2788 — the catch branch recovers IN PLACE and
    returns WITHOUT relaunching, per round-2 finding #2, so `persist` must never be left closed on
    that path): add `try { persist.close(); } catch (e) { console.error('[changeHome] persist.close:',
    e); }` as the FIRST statement inside the `try` block, immediately before the `for (const sub of
    [...])` loop — this closes the WAL that Task 1's harnessHome-aware `persist` now holds open under
    the OLD home, scoped to 'move' mode only (never touched for 'fresh' mode, which needs no close or
    repoint). In the SAME handler's copy-failure `catch (e)` block, immediately BEFORE the existing
    `startHiveServices();` call, add `try { persist.repoint(); } catch (err) {
    console.error('[changeHome] persist.repoint after copy failure:', err); }` —
    `startHiveServices()` never touches `persist` (verified this session), so without this the
    recovery-in-place path strands the module-level `persist` handle CLOSED for the rest of the
    process; `PersistStore.repoint()`'s close-then-reopen shape (Task 1) is the right primitive here,
    and the OLD home is still `harnessHome` on this path (the copy fails before `writeConfig` runs),
    so `repoint()` correctly reopens against the unchanged old path. Widen the `'move'` mode's `sub`
    array from `['hive', 'palace',
    'roster.json', 'roster-backups']` to `['hive', 'palace', 'roster.json', 'roster-backups',
    'harness.db', 'harness.db-wal', 'harness.db-shm', 'knowledge']` — the two stores this plan just
    made project-local (`harness.db` and `KnowledgeManager`'s `knowledge/` directory) must move with
    everything else, or an operator who picks 'move' loses `command_history`, the `kv` store
    (including `missionLastFiredAt`), the FTS memory index and the whole knowledge graph, silently.
    `existsSync(src)`'s existing per-entry guard already makes the two new sidecar-file entries
    (`-wal`/`-shm`, which may not exist if the DB is not in WAL mode or was cleanly closed) a no-op
    when absent — no extra guard code needed.
    For the existing-install data question (D-9's discretion item): do NOT write a copy-forward
    migration for the DEFAULT boot path (`config:update`'s null->set transition) — declare the
    one-time loss instead; Task 3 of this plan adds the CHANGELOG.md entry. This is the
    ponytail-correct call there: a copy-forward is untested migration code for a question this repo
    has never needed to answer before. `config:changeHome`'s 'move' mode is a DIFFERENT, existing,
    operator-invoked action whose whole promise is "the data comes along" — that one must actually
    move the two new stores, which is why it is handled explicitly above rather than declared as a
    release-note loss.
  </action>
  <acceptance_criteria>
    - `grep -n "persist.repoint()" src/main/index.ts` matches `>= 2` (the `config:update` transition
      call PLUS the copy-failure recovery call inside `config:changeHome` — proving the stranded-
      handle fix landed, not only the original repoint site)
    - `grep -n "repointFiredStore()" src/main/index.ts` matches `>= 1`
    - `grep -n "export function repointFiredStore" src/main/config.ts` matches `>= 1`
    - `grep -n "app.relaunch" src/main/index.ts | wc -l` is unchanged from before this task (no new
      relaunch call was added — confirms `changeHome`/`resetAll` were left alone)
    - A `node -e` check confirms `persist.close()` sits INSIDE the `if (mode === 'move' && oldHome)`
      block's `try` clause, before the `for (const sub of` loop — not in the general ~11-entry
      teardown block that also runs for 'fresh' mode:
      `node -e "const s=require('fs').readFileSync('src/main/index.ts','utf8');const i=s.indexOf(\"mode === 'move' && oldHome\");const j=s.indexOf('persist.close()',i);const k=s.indexOf('for (const sub of',i);if(i<0||j<0||k<0||!(j<k)){console.error('FAIL: persist.close() is not positioned before the move-mode copy loop');process.exit(1);}console.log('PASS');"`
    - `grep -n "persist.repoint()" src/main/index.ts -B 3` shows one occurrence inside the
      `config:changeHome` copy-failure `catch` block, immediately before its `startHiveServices();`
      call (confirm by reading the matched context, not only the count)
    - `grep -n "'harness.db'" src/main/index.ts` matches `>= 1` AND `grep -n "'knowledge'" src/main/index.ts` matches `>= 1` (both new entries in the 'move' sub array)
    - **Ordering pin** (round-2 finding #24): inside `config:update`'s `if (!hiveWasEnabled &&
      hive.enabled())` block, `persist.repoint()` runs strictly BEFORE `startHiveServices()` — never
      after: `node -e "const s=require('fs').readFileSync('src/main/index.ts','utf8');const i=s.indexOf('!hiveWasEnabled && hive.enabled()');const j=s.indexOf('persist.repoint()',i);const k=s.indexOf('startHiveServices()',i);if(i<0||j<0||k<0||!(j<k)){console.error('FAIL: persist.repoint() does not run before startHiveServices() in the config:update transition');process.exit(1);}console.log('PASS');"`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/boot-floor.test.cjs</automated>
  </verify>
  <done>Both PersistStore handles repoint together at the one first-run transition; config:changeHome's 'move' mode actually moves harness.db and the knowledge store, with persist closed before the copy AND reopened via repoint() on the copy-failure recovery path (never left stranded closed); resetAll is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: doc/UI honesty — the dead memory-scope control and the no-simultaneous-projects limitation</name>
  <files>README.md, src/renderer/src/components/MemoryPanel.tsx, src/renderer/src/components/HivePicker.tsx, CHANGELOG.md</files>
  <read_first>README.md:104-125 (the "Memory & coordination" feature bullets, including the exact wrapped sentence "For real isolation today, set memory scope to\nper-agent..." at :122-123), src/renderer/src/components/MemoryPanel.tsx:118-127 (the "switch memory scope to per-agent" sentence — a DIFFERENT verb than README's, do not assume they match), src/renderer/src/components/HivePicker.tsx:75-90 (the dialog's intro copy, and line 82's exact "...so you can run different setups side by side." sentence)</read_first>
  <behavior>
    - `HivePicker.tsx`'s dialog body no longer contains the literal substring "side by side"
      anywhere; it instead states plainly that only one project runs at a time and switching
      relaunches into it.
    - Neither README.md nor MemoryPanel.tsx contains a dead-control instruction to reach a per-agent
      memory scope — checked as WHITESPACE-JOINED text (both files wrap this sentence across a line
      break today, at different points, with different verbs — "set" in README, "switch" in
      MemoryPanel — so a single-line grep for either exact phrase is not sufficient proof either file
      was actually edited).
    - Both README.md and MemoryPanel.tsx name RECALL-02 as where real, server-side enforcement lands
      — the honest replacement, not merely an absence of the old sentence.
    - README.md gains a new, previously-absent statement that this app does not run two projects
      side by side simultaneously (verified this session: README.md has ZERO existing mentions of
      "side by side" or of `harnessHome`/project switching at all — this is new content, not an edit
      to something already there).
  </behavior>
  <action>
    D-14: `HarnessConfig` has zero `scope` fields and `floor/boot.ts`'s memory construction passes
    `{ enabled, model }` only, so `scope()` always returns `'shared'` — the `'agent'` branch is
    unreachable in the shipped app. In `MemoryPanel.tsx`, remove or rewrite the sentence "For real
    isolation today, switch memory scope to per-agent" — it names a control that does not exist.
    Replace with an honest statement: recall narrowing is agent-supplied only, and server-side
    enforcement lands with RECALL-02 (Phase 5); do not imply a working workaround exists today. Apply
    the identical correction to README.md's matching paragraph at README.md:118-123 (the actual
    wording there is "For real isolation today, set memory scope to\nper-agent, which puts each agent
    in its own index rather than trusting a flag." — note the different verb and the line wrap; do
    not assume it reads the same as MemoryPanel's sentence). D-13: edit `HivePicker.tsx:82` — replace
    "Each config is separate and self-contained, so you can run different setups side by side." with
    an honest sentence, e.g. "Each config is separate and self-contained. Only one project runs at a
    time — switching relaunches the app into it." (near the dialog's existing "Open the one you were
    working in, switch to another, or start a new one." line). Add a NEW bullet to README.md's
    Features > "The floor" list (README.md:106-110) stating the same limitation — README currently
    has no mention of project switching at all, so this is new content, e.g. "One project per running
    app — switching relaunches into it; two projects never run side by side simultaneously." For the
    existing-install data decision from Task 2: add a `CHANGELOG.md` `[Unreleased]` entry stating that
    `harness.db` and the knowledge-graph store now default under the active project's own home instead
    of one shared app-data location, and that any `command_history`/`kv`/knowledge-graph data already
    written to the OLD location is NOT copied forward automatically on upgrade (`config:changeHome`'s
    'move' mode DOES move it, per Task 2) — the default boot path simply stays at the prior location,
    unused after the upgrade, until the operator explicitly moves it.
  </action>
  <acceptance_criteria>
    - `grep -c "side by side" src/renderer/src/components/HivePicker.tsx` matches exactly `0`
    - `grep -c "Only one project runs at a time" src/renderer/src/components/HivePicker.tsx` matches `>= 1`
    - **Non-vacuous compound check** (round-2 audit found the RECALL-02 half of the old two-bullet
      form was already green before any edit — both files already mentioned RECALL-02 adjacent to
      the dead-control sentence this task removes, so a bare `grep RECALL-02 >= 1` proved nothing by
      itself). This single script requires BOTH conditions in the SAME pass — the dead phrase gone
      AND RECALL-02 still present — so it is red before the edit (dead phrase present) and can only
      go green once the sentence is genuinely rewritten:
      `node -e "const fs=require('fs');const files=['README.md','src/renderer/src/components/MemoryPanel.tsx'];let bad=0;for(const f of files){const j=fs.readFileSync(f,'utf8').replace(/\s+/g,' ');if(/switch memory scope to per-agent|set memory scope to per-agent/.test(j)){console.error('FAIL: dead-control phrase still in',f);bad++;}if(!/RECALL-02/.test(j)){console.error('FAIL: RECALL-02 missing from',f);bad++;}}if(bad>0)process.exit(1);console.log('PASS');"`
      exits 0
    - `grep -c "side by side" README.md` matches `>= 1` (the new limitation sentence — verified `0`
      today, so this is genuinely new content, not a pre-satisfied check)
    - `grep -c "harness.db\|knowledge-graph" CHANGELOG.md` matches `>= 1` in the `[Unreleased]`
      section — verified `0` today (measured this session), so this is a real, non-vacuous gate
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-components.test.cjs</automated>
  </verify>
  <done>Neither shipped doc nor UI text points the operator at a per-agent memory-scope control that cannot be reached; HivePicker no longer claims side-by-side operation is possible; the no-simultaneous-projects limitation and the existing-install data policy are both stated, using the files' real wording, never a paraphrase that happens to already read zero.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Electron main <-> local filesystem | `harnessHome`-derived paths for `harness.db` and `KG_ROOT` are operator-configured local directory paths |
| Onboarding IPC (`config:update`) <-> main | Renderer-supplied config patch, already validated/normalized by `writeConfig` before this plan's hook fires |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-01a | Information Disclosure | `db.ts` default-path change | accept | The DB still lives under an operator-chosen `harnessHome`, a path the operator already fully controls and already trusts with the hive's memory/task data; no new secret or credential is newly exposed by relocating the DB file itself |
| T-03-01b | Tampering | `repoint()` mid-session | mitigate | `repoint()` only fires inside the single, already-guarded `!hiveWasEnabled && hive.enabled()` transition — it is never reachable from an ordinary `config:update` patch, so a malicious/buggy renderer patch cannot trigger a repeated close/reopen storm |
| T-03-01c | Denial of Service | existing-install data left at the old path (default boot transition) | accept | Declared in CHANGELOG.md per Task 3; the data is not deleted, only unused post-upgrade — recoverable by hand, or via `config:changeHome`'s 'move' mode which this plan makes actually move it |
| T-03-01d | Tampering | a WAL file open across `config:changeHome`'s `cpSync` | mitigate | Task 2 closes `persist` before the copy runs, so the moved `harness.db` is never copied mid-write |
</threat_model>

<verification>
- `node --test test/db-fts.test.cjs test/boot-floor.test.cjs test/hive-runtime-path.test.cjs test/hive-cwd.test.cjs test/renderer-components.test.cjs` — 0 failures
- `npm run typecheck` — 0 errors
- `npm test` (full suite) after this plan lands — 0 failures, skip count unchanged from the 7 baseline
</verification>

<success_criteria>
- `PersistStore` and `KnowledgeManager` both default under `harnessHome` once it is set, with zero
  behavior change before onboarding
- The one non-relaunching transition repoints both live `PersistStore` handles
- `config:changeHome`'s 'move' mode moves `harness.db` and the knowledge-graph store, with `persist`
  closed before the copy
- SCALE-01's task-ledger half remains explicitly zero-code, stated as such, not budgeted
- The dead memory-scope UI/doc instruction is corrected using each file's real wording; the
  no-simultaneous-projects limitation (HivePicker AND a new README statement) and the existing-install
  data policy are all stated, never silent, and never satisfied by text that was already there
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-01-SUMMARY.md` when done
</output>
