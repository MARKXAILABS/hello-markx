---
phase: 03-scale-and-observability
plan: 2
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/main/breaker.ts
  - src/main/floor/boot.ts
  - src/main/index.ts
  - src/main/hive.ts
  - src/preload/index.ts
  - test/boot-floor.test.cjs
  - test/breaker.test.cjs
  - test/telemetry-auth.test.cjs
autonomous: true
requirements_addressed: [SCALE-05]

must_haves:
  truths:
    - "A card sourcing block state on window reload can read the CURRENT breaker level immediately, not wait for the next ~30s breaker beat — the pull IPC lands here; its one production caller is wired in 03-08 (same-phase, later wave), not left callerless"
    - "A registry entry records when the current PTY was spawned, so a card can show duration since this spawn rather than nothing"
    - "resolveCodexHome is actually wired into the production TelemetryCollector — a codex agent's transcript fallback reads the per-agent CODEX_HOME this app derives for it (`agentDir(id)/.codex`), never a cwd-sharing Claude agent's transcripts — and this now protects the DISPLAY join (writeFleetSnapshot, hive:agentDirectory), which is the only thing this plan changes about transcript-fallback cost. The ONE codex agent for whom the derived home is not the effective one — a resumed agent whose CODEX_HOME index.ts repointed to the sibling that owns its rollout (index.ts:2234-2245, measured this session) — reads an empty home and shows zero: an UNDER-report, never a neighbour's dollars, declared as T-03-02h rather than claimed away"
    - "The DISPLAY join never shows a measured dollar figure that is not that agent's money. A transcript sample is read ONLY for an agent with a per-agent transcript ROOT — that is provider codex and nothing else, gated by hasOwnCostSource(reg.agents, id), with its CODEX_HOME wired by resolveCodexHome in this plan. EVERY other agent, claude included, renders the declared gap: usd 0 / tokens 0 with costUnattributed:true. A cwd is not a per-agent root — a whole-directory read can include another PROJECT's agent, a deleted agent, or the operator's own Claude Code CLI sessions — so no cwd-uniqueness arm exists and none may be added"
    - "snapshotAll()'s three specified behaviours are actually covered — test/breaker.test.cjs drives a real CircuitBreaker for the fresh-instance and post-tick cases, so a `return {}` stub fails the suite instead of passing every grep"
    - "The cost-ledger write gate (`if (sample?.sessionId) hive.appendCostLedger(sample)` at boot.ts) is left BYTE-UNCHANGED — this plan does not widen it. Transcript-tier spend still does not reach cost-ledger.jsonl. This is a stated, accepted residual (03-CONTEXT.md's Accepted Residuals section), not a silent gap: closing it is PARITY-02 work, and the per-agent session filter it needs (ReadUsageOptions) already exists at transcript.ts:299"
    - "The per-agent joins in writeFleetSnapshot and hive:agentDirectory include transcript-fallback-only agents' cost, not only agents with a live OTel session — but their lastActiveSecAgo reads null, never a permanently-fresh 0s, and their cost is marked costLifetime:true so it is never silently conflated with a per-session number"
    - "AgentDirectoryEntry.spawnedAt is a TYPED field on the preload interface (not merely a raw IPC payload widening) — its one consumer is 03-08's agentView.ts module singleton, which polls hive:agentDirectory itself; no src/renderer/src/store/store.ts Agent-type change is needed or made"
  artifacts:
    - path: src/main/breaker.ts
      provides: "CircuitBreaker.snapshotAll(), returning level+reason+ts per known agent"
    - path: src/main/index.ts
      provides: "the control:breakerSnapshot IPC handler"
    - path: src/main/hive.ts
      provides: "codexHomeFor(agentId), the exported pure predicate hasOwnCostSource(agents, id), and the spawnedAt registry field — applyCostRow is NOT touched by this plan"
    - path: src/preload/index.ts
      provides: "getBreakerSnapshot() and AgentDirectoryEntry.spawnedAt / .costLifetime / .costUnattributed"
    - path: test/breaker.test.cjs
      provides: "the two snapshotAll() cases (fresh instance deep-equals {}; after one tripping tick the agent's level+reason are present) that a `return {}` stub cannot pass"
  key_links:
    - from: "src/preload/index.ts (getBreakerSnapshot)"
      to: "src/main/index.ts (control:breakerSnapshot)"
      via: "ipcRenderer.invoke"
      pattern: "control:breakerSnapshot"
    - from: "src/main/floor/boot.ts (new TelemetryCollector(...))"
      to: "src/main/hive.ts (codexHomeFor)"
      via: "the resolveCodexHome option, gated on the registry's recorded provider"
      pattern: "resolveCodexHome"
    - from: "src/main/floor/boot.ts (writeFleetSnapshot) and src/main/index.ts (hive:agentDirectory)"
      to: "src/main/telemetry.ts (getAgentUsage)"
      via: "a per-agent direct call GATED on hasOwnCostSource, replacing the live-OTel-only snapshot() join — the cost-ledger APPEND gate at boot.ts:429 is untouched"
      pattern: "telemetry.getAgentUsage(id)"
    - from: "src/main/floor/boot.ts (writeFleetSnapshot) and src/main/index.ts (hive:agentDirectory)"
      to: "src/main/hive.ts (hasOwnCostSource)"
      via: "a named import beside the existing HiveManager import, called once per agent in the join loop"
      pattern: "hasOwnCostSource"
---

> **Requirements-ledger note — read before running the completion step.** This plan's frontmatter
> key is `requirements_addressed`, not `requirements`, and that is deliberate. SCALE-05 is delivered
> by TWO plans — this one (main-process fixes, wave 2) and 03-08 (the renderer consumer, wave 5) —
> and `execute-plan.md`'s `update_requirements` step marks a requirement Complete from whichever
> plan carries a literal `requirements:` key. Ticking SCALE-05 here would mark it done in wave 2
> while half of it is still unbuilt. Only **03-08**, the last plan delivering SCALE-05, carries
> `requirements:`. `requirements_addressed` still satisfies `plan-phase.md`'s coverage gate, which
> greps both keys. (Round-2 finding #31.)

> **Scope note — read before touching `applyCostRow` or the cost-ledger gate.** Two red-team
> rounds confirmed that widening `boot.ts`'s `if (sample?.sessionId) hive.appendCostLedger(sample)`
> to `if (sample) hive.appendCostLedger(sample)` produced **thirteen** confirmed findings across
> both rounds (round 1 #3, #4, #15, #35; round 2 #1, #12, #15, #16, #17, #21, #23, #30, #40) — every
> fix spawned two more of the same class. The orchestrator's decision for this revision round is to
> **remove the widening entirely**, not patch it a third time. `applyCostRow` is untouched by this
> plan. D-36's actual requirement — transcript-derived cost stops being invisible to every UI — is
> satisfied by the DISPLAY-JOIN half only (`writeFleetSnapshot`/`hive:agentDirectory` reading
> `telemetry.getAgentUsage(id)`), which this plan keeps. The ledger gap is recorded as an Accepted
> Residual in `03-CONTEXT.md` and belongs to PARITY-02 (D-03), not to Phase 3.

> **D-36 check — the `hasOwnCostSource` gate does not re-open a LOCKED decision.** D-36 itself is
> `03-CONTEXT.md:360-372` (re-measured this session); the sentence that pins the DISPLAY join to
> `telemetry.getAgentUsage(id)` "with a `costLifetime` discriminator" is `03-CONTEXT.md:438-441`, in the
> Accepted Residuals block — an earlier draft of this note cited `:436-442` and labelled it D-36, which
> is the wrong block by two lines and the wrong decision id. Both still say the same thing, and the gate
> still satisfies it: a codex agent (own CODEX_HOME) and a claude
> agent that is the sole holder of its `cwd` — the ordinary case, since each hire gets its own working
> directory — are shown exactly as D-36 requires. The gate only withholds the ONE reading that is not
> that agent's money: a whole-cwd transcript sum for an agent that shares that cwd, or for an engine
> with no reader of its own. That case renders D-35's declared gap (`costUnattributed: true`, never a
> faked figure) instead, which is the same rule D-36 is an instance of. (Round-3 finding #1.)

<objective>
Land the three main-process fixes SCALE-05's consolidated card needs and the renderer cannot make
itself (D-36), scoped to what the renderer genuinely cannot do and nothing more: a pull getter for
breaker state (closing a fail-unsafe "healthy" window after reload — its production caller lands in
03-08), a spawn timestamp on the registry, and the fix that stops transcript-tier spend from being
silently invisible on the fleet/agent-directory DISPLAY (not the ledger — see the scope note above).

`resolveCodexHome` must actually be wired in this same plan: today it is declared on
`TelemetryCollectorOptions` but never passed at the one production construction site, so
`transcriptFallback` would otherwise read a cwd-sharing NEIGHBOUR agent's transcripts for every codex
worker — and since this plan's display join now calls `telemetry.getAgentUsage(id)` directly (rather
than only the live-OTel-scoped `snapshot()`), that neighbour-billing bug would show up on screen even
though it never touches the ledger. Wiring `resolveCodexHome` is what keeps the DISPLAY join honest.

Purpose: without these fixes, SCALE-05's card either lies (stale "healthy"), shows a false-empty or
permanently-fresh duration/freshness reading, or under-reports spend on screen for exactly the engines
that most need an honest number. This plan does NOT change what reaches `cost-ledger.jsonl` — that
gate stays exactly as it is today.
Output: `control:breakerSnapshot` IPC, `RegistryAgent.spawnedAt` (+ its typed preload counterpart),
`codexHomeFor` wired into `TelemetryCollector`, and a corrected DISPLAY join (never the ledger write).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-RESEARCH.md

<interfaces>
From `src/main/breaker.ts` (current, verified this session):
```
export type BreakerLevel = 'healthy' | 'steering' | 'constrained' | 'stopped';
export interface BreakerState { agentId: string; level: BreakerLevel; reason: string; ts: number; }
interface AgentBreakerState { level: BreakerLevel; reason: string; ... }  // private, keyed by agentId
export class CircuitBreaker {
  private agents = new Map<string, AgentBreakerState>();
  private get(agentId: string): AgentBreakerState   // creates a 'healthy' default if absent
  levelFor(agentId: string): BreakerLevel { return this.agents.get(agentId)?.level ?? 'healthy'; }
  tick(inputs: BreakerInput[], now: number): BreakerDecision[]
}
```
`boot.ts` exports `export let breaker: CircuitBreaker;` and `index.ts` already imports it directly
(used at `index.ts:3476` inside `hive:agentDirectory`).

From `src/main/floor/boot.ts`'s `runBreakerBeat` (current, full function already read this session):
```
function runBreakerBeat(progressWindowMs: number): void {
  ...
  for (const [id, a] of Object.entries(reg.agents)) {
    ...
    const sample = telemetry.getAgentUsage(id);
    if (sample?.sessionId) hive.appendCostLedger(sample);        // <- STAYS EXACTLY AS-IS, this plan does not touch it
    if (sample?.sessionId) { /* recordSession, sid-safety warning */ }   // <- also stays as-is
    ...
    inputs.push({ agentId: id, sample, budget: ..., progressing: ... });  // already ungated
  }
  ...
}
```
`writeFleetSnapshot()` (also in `boot.ts`, `boot.ts:470-499`, re-measured this session — `grep -n "function
writeFleetSnapshot" boot.ts` -> 470, `grep -n "hive.writeFleetSnapshot({" boot.ts` -> 499; an earlier draft
said `:470-497`, two lines short) pre-builds `usageById` from
`telemetry.snapshot()` and reads `usageById.get(id)`:
```
const snap = telemetry.snapshot();
const usageById = new Map(snap.usage.map((u) => [u.agentId, u]));
...
const u = usageById.get(id);
...
lastActiveSecAgo: u ? Math.round((now - u.ts) / 1000) : null,
```
`accountUuid: u?.accountUuid ?? null` also reads off `u` here — a fallback sample carries no
`accountUuid` field at all, so this stays `null` for a fallback-only agent either way; no fix needed
for that field.

From `src/main/telemetry.ts` (current, verified this session — the wiring gap and its own doc
comment naming the fix this plan makes real):
```
export interface TelemetryCollectorOptions {
  ...
  /** Resolve a CODEX agent's per-agent CODEX_HOME (`<hive>/agents/<id>/.codex`), or null for
   *  every other engine. Codex keeps its rollout transcripts there instead of `~/.claude/projects`,
   *  so without this its spend is invisible to the breaker's caps (#19). Wired in index.ts, next to
   *  `resolveCwd`. */
  resolveCodexHome?: (agentId: string) => string | null;
}
export class TelemetryCollector {
  private readonly resolveCwd?: (agentId: string) => string | null;
  private readonly resolveCodexHome?: (agentId: string) => string | null;
  constructor(opts: TelemetryCollectorOptions = {}) {
    this.resolveCwd = opts.resolveCwd;
    this.resolveCodexHome = opts.resolveCodexHome;
  }
  getAgentUsage(agentId: string): AgentUsageSample | null {   // includes transcript fallback
    const live = this.aggregateLive(agentId);
    if (live) return live;
    return this.transcriptFallback(agentId);   // ALWAYS returns sessionId: '' when present
  }
  private transcriptFallback(agentId: string): AgentUsageSample | null {
    // Codex first: its rollouts live under the agent's own CODEX_HOME, and a codex worker sharing
    // a cwd with a claude worker would otherwise be billed the NEIGHBOUR's Claude transcripts.
    const codexHome = this.resolveCodexHome?.(agentId);
    const cwd = codexHome ? null : this.resolveCwd?.(agentId);
    const u = codexHome ? readCodexUsage(codexHome) : cwd ? readAgentUsage(cwd) : null;
    if (!u) return null;
    if (!u.inputTokens && !u.outputTokens && !u.cacheReadTokens && !u.cacheWriteTokens) return null;
    return { agentId, sessionId: '', ts: Date.now(), input: u.inputTokens, output: u.outputTokens,
      cacheRead: u.cacheReadTokens, cacheCreation: u.cacheWriteTokens, model: u.model ?? '',
      usd: u.estimatedCostUsd };
  }
  snapshot(): TelemetrySnapshot {   // LIVE-OTel-ONLY — iterates this.agentSessions.keys(), never falls back
    const usage: AgentUsageSample[] = [];
    for (const agentId of this.agentSessions.keys()) {
      const s = this.aggregateLive(agentId);
      if (s) usage.push(s);
    }
    ...
  }
}
```
The doc comment's "Wired in index.ts, next to `resolveCwd`" is FALSE today — verified this session,
`grep -rn resolveCodexHome src/main/index.ts src/main/floor/boot.ts` (before this plan) returns
nothing. This is why `snapshot()`-sourced joins silently omit any agent whose ONLY usage source is
the transcript fallback (codex etc.) — they never appear in `this.agentSessions` at all — and why
`transcriptFallback` today reads a cwd-sharing Claude agent's transcripts for a codex worker. This
plan wires it for the DISPLAY join's sake; it changes nothing about the ledger append gate above.

From `src/main/floor/boot.ts` (current, the ONE production `TelemetryCollector` construction site,
~line 1010):
```
telemetry = new TelemetryCollector({
  emit: (channel: string, payload: unknown) => { deps.send(channel, payload); },
  resolveCwd: (agentId: string) => hive.registry().agents[agentId]?.cwd ?? null
});
```
`join` is already imported from `node:path` at the top of this file.

From `src/main/hive.ts` (current, verified this session):
```
private agentDir(id: string): string { ... }   // private — no public accessor for an agent's own dir today
```
`installCodexHooks(dir, ...)` (in `hiveProvisioning.ts`) already computes a codex agent's isolated
home as `join(dir, '.codex')`, where `dir` is that same `agentDir(id)` value — this plan's new
`codexHomeFor` mirrors that exact join, as a NEW public method (the private `agentDir` itself stays
private; only the derived path is exposed).

`RegistryAgent` (interface, `hive.ts:221-239`, `extends AgentMeta`) has `lastSeen: number` and
`provider?: AgentProvider` (inherited from `AgentMeta`, `hive.ts:200`) but no `spawnedAt` — the
`provider` field is what this plan's `resolveCodexHome` closure gates on (only a `'codex'`-provider
agent resolves a CODEX_HOME; every other engine still falls through to `resolveCwd`, unchanged).
`provider` is OPTIONAL and an absent value means claude — `hive.ts` reads it as `meta.provider ?? 'claude'`
in FOUR places (`hive.ts:973, 991, 1162, 2386` — re-measured this session with
`grep -n "provider ?? 'claude'" src/main/hive.ts`; an earlier draft of this line said "five places" and
cited `:1009`, which does not match at all — a rotted anchor, corrected here), so any new predicate must use the
same `?? 'claude'` default or it will silently classify every legacy registry entry as "not claude".

**New in this plan, and the fix for round-3 finding #1:** an exported PURE predicate in `hive.ts`,
`hasOwnCostSource(agents: Record<string, RegistryAgent>, id: string): boolean` — true for EXACTLY ONE
case: `(provider ?? 'claude') === 'codex'`, whose `CODEX_HOME` is per-agent by construction and is wired
by this plan via `resolveCodexHome`. False for everything else, including claude.

**The claude arm was removed, and the reason is recorded so nobody re-adds it.** An earlier draft also
returned true for a claude agent that was the sole registry holder of its `cwd`. Three review rounds
showed that test is unsound in a different way each time: the registry it consults is ONE project's, so
another project's agent working the same directory is invisible to it; it cannot exclude the OPERATOR'S
OWN Claude Code CLI sessions living in that directory; and it cannot see transcripts belonging to no
registry entry at all. A whole-directory sum is not "provably this agent's money" under any of those
conditions, and a fourth predicate would repeat the pattern. Only a per-agent transcript ROOT — which
is what `CODEX_HOME` is and what a cwd is not — makes the claim true. It is a free function, not a method, for two reasons: both join sites already hold
`reg.agents`, and a pure function is testable from `test/telemetry-auth.test.cjs` with an object literal —
no `HiveManager` instance, no filesystem. `hasOwnCostSource` appears 0 times in `src/main/` today (measured
this session: `grep -c hasOwnCostSource src/main/hive.ts src/main/floor/boot.ts src/main/index.ts` -> 0/0/0).
Both `boot.ts:31` and `index.ts:30` already import named symbols from hive (`HiveManager, redactSecrets, …`),
so wiring it is one added token per import list.

**`applyCostRow` is NOT reproduced here and is NOT touched by this plan** (see the scope note above
the objective). Do not modify `src/main/hive.ts`'s `applyCostRow`/`costCumulative` in this plan.

From `src/main/index.ts`'s `hive:agentDirectory` handler (current, ~line 3450):
```
ipcMain.handle('hive:agentDirectory', () => {
  const snap = telemetry.snapshot();
  const usageById = new Map(snap.usage.map((u) => [u.agentId, u]));
  ...
  const agents = Object.entries(reg.agents).map(([id, a]) => {
    const u = usageById.get(id);
    ...
    return { id, ..., model: u?.model ?? null, breaker: breaker.levelFor(id), tokens,
      usd: u ? Number(u.usd.toFixed(4)) : 0, lastActiveSecAgo: u ? Math.round((now - u.ts) / 1000) : null, ... };
  });
  return { godId: reg.godId, agents };
});
```

From `src/preload/index.ts` (current pattern, `hiveLog`/`onBreakerState`):
```
hiveLog: (n?: number): Promise<unknown[]> => ipcRenderer.invoke('hive:log', n ?? 200),
onBreakerState: (cb: (s: BreakerState) => void): (() => void) => { ... },
setBreakerState: (state: BreakerState): Promise<{ ok: boolean }> =>
  ipcRenderer.invoke('control:setBreakerState', state),
```
`AgentDirectoryEntry` (current, `preload/index.ts:135-162`) has no `spawnedAt` field — this plan adds
`spawnedAt: number | null;` to the interface itself, not only to the raw IPC payload, so the TYPE the
renderer imports actually carries it. The card consumer is 03-08's `agentView.ts`, which polls
`window.cth.hiveAgentDirectory()` directly — no change to `src/renderer/src/store/store.ts`'s `Agent`
type is needed for this. (Corrected repo-fact, round-3 finding #20: an earlier draft of this plan said
`hive:agentDirectory` has ZERO renderer callers. It has FOUR, measured this session —
`src/renderer/src/realtime/tools.ts:481, :526, :578, :630`, all voice-tool call sites that narrate fleet
spend. None of them is a card prop-threading path, so the conclusion below still holds, but those four
DO read `usd`/`tokens` and this plan gives them no new rendering; that is recorded as an explicit residual
in T-03-02f rather than silently assumed away.) A fresh module-singleton poll in `agentView.ts`
is the smaller diff, matching D-33's "one pure derivation module" pattern rather than adding a sixth
field to a type the store already has to keep in sync by hand.

`AgentDirectoryEntry` (`preload/index.ts:135-162`, re-read this session) today ends with
`contextTokens/contextLimit/contextPct` and carries `tokens: number` and `usd: number`; this plan appends
three fields to it: `spawnedAt: number | null`, `costLifetime: boolean`, `costUnattributed: boolean`.

From `src/main/breaker.ts` (the escalation ladder, re-read this session — Task 1's test depends on it):
`tick()` moves ONE rank per beat: `breaker.ts:290` is
`target = LEVELS[Math.min(rank(s.level) + 1, rank(ceiling))]`, inside the `if (trip.tripping)` opened at
`:289` and after the `ceiling`/`target` bindings at `:287-288` (re-measured this session; an earlier draft
cited `:286-289`, a range that stops one line short of the statement it quotes). So a first tripping beat
from `healthy` lands on `'steering'`, NOT `'constrained'` —
`'constrained'` is the CEILING (`hardStop:false`), reached on the second tripping beat. The cost-cap arm's
reason is not the bare string `'cost cap'` either: `breaker.ts:394` builds
`` `cost cap: floor total over $${costCapUsd} (top spender $${...})` ``. Task 1's test therefore asserts
against the tick's OWN returned decision plus a `/cost cap/` match, never a hard-coded level or sentence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CircuitBreaker.snapshotAll() and the control:breakerSnapshot IPC (new capability, caller lands in 03-08)</name>
  <files>src/main/breaker.ts, src/main/index.ts, src/preload/index.ts, test/boot-floor.test.cjs, test/breaker.test.cjs</files>
  <read_first>src/main/breaker.ts (levelFor and the private get()/agents map; also :287-290's one-rank-per-beat ladder and :394's cost-cap reason string, which the new test asserts against), src/main/index.ts (hive:agentDirectory and control:setBreakerState, for the response shape and channel-naming pattern), src/preload/index.ts (onBreakerState/setBreakerState, for the preload-export pattern), test/boot-floor.test.cjs (B_IPC_JOINED / B_IPC_NAMES — this task's new channel needs the same two-place edit D-17 requires for team:export), test/breaker.test.cjs (its makeBreaker/beatIn/spending/inBand helpers at :40-58 and :239-246, and the existing "card in the band AND the floor top spender" case at :248-258, which is the exact two-beat cost-cap escalation this task's new case reuses)</read_first>
  <behavior>
    - `breaker.snapshotAll()` on a fresh `CircuitBreaker` with no `tick()` calls yet returns `{}` (no
      agent has been seen) — `deepEqual({})`, not merely "falsy" or "no a1 key".
    - After the beats that escalate agent `a` over the floor cost cap, `snapshotAll().a` carries that
      agent's CURRENT `level` and `reason` — the SAME level `levelFor('a')` already reports, plus the
      reason `levelFor` does not expose, plus `agentId` and a numeric `ts`. The level is whatever the
      same tick's returned decision reports (the ladder moves one rank per beat, so one tripping beat
      from healthy is `'steering'` and the second reaches `'constrained'`) and the reason matches
      `/cost cap/` — never a hard-coded sentence. A `snapshotAll()` that returns `{}` (a stub) or a
      permanently-`'healthy'` row fails this case.
    - `ipcMain.handle('control:breakerSnapshot', ...)` returns `{}` when `hive.enabled()` is false,
      and otherwise returns one entry per agent currently in `hive.registry().agents` (including
      agents `breaker` has never ticked, defaulting to `'healthy'`/`''`).
  </behavior>
  <action>
    In `breaker.ts`, add a public method `snapshotAll(): Record<string, BreakerState>` that iterates
    `this.agents` (the private map already keyed by agentId) and builds one `BreakerState` per entry
    from its stored `level`/`reason`, stamping `ts: Date.now()` for entries that have no natural
    timestamp — do not add a public getter for the private `AgentBreakerState` type itself, only the
    already-public `BreakerState` shape. In `index.ts`, add `ipcMain.handle('control:breakerSnapshot',
    () => { if (!hive.enabled()) return {}; const reg = hive.registry(); const out: Record<string,
    BreakerState> = {}; for (const id of Object.keys(reg.agents)) { const s =
    breaker.snapshotAll()[id]; out[id] = s ?? { agentId: id, level: 'healthy', reason: '', ts:
    Date.now() }; } return out; });` — placed near the existing `control:setBreakerState` handler so
    the push and pull sit together. In `preload/index.ts`, add `getBreakerSnapshot: (): Promise<Record<string,
    BreakerState>> => ipcRenderer.invoke('control:breakerSnapshot'),` beside `onBreakerState`.
    `B_IPC_JOINED` and the sorted `B_IPC_NAMES` array in `test/boot-floor.test.cjs` need a two-place
    edit in this SAME commit (D-17's rule, reused here for a different new channel): bump the count
    by one and insert `'control:breakerSnapshot'` at its correct sorted position (immediately before
    `'control:gateTool'` in the existing alphabetized list, since `'breakerSnapshot' < 'gateTool'`
    lexically after the shared `'control:'` prefix — verify against the live sorted array rather than
    trusting this description). This IPC handler has NO caller in this plan — 03-08 (same phase, wave
    5) wires its one production caller inside `agentView.ts`'s module-singleton first-subscriber
    initialization; do not add a caller here, and do not treat the absence of one as an omission in
    this plan's own scope.

    This task is `tdd=true` and the method it adds is trivially stubbable, so the test is NOT optional
    (round-3 finding #3): append two cases to `test/breaker.test.cjs`, which drives the REAL
    `CircuitBreaker` through `loadTs`-free `ts.transpileModule` and already owns every helper needed.
    Case A — `makeBreaker()` with no `tick()` call, `assert.deepEqual(b.snapshotAll(), {})`, with a
    message saying a non-empty map means the private `agents` map is being pre-populated. Write that
    call and its `{}` on ONE physical line (`assert.deepEqual(b.snapshotAll(), {}, 'message')`): the
    acceptance criterion below is a fixed-string, single-line `grep -F`, so wrapping the arguments
    across lines reports RED against a correct test. Case B —
    reuse the existing cost-cap escalation exactly as `test/breaker.test.cjs:248-258` already drives it
    (`makeBreaker({ costCapUsd: 5 })`, `beatIn` twice with `spending('a', ts, 12)` and `inBand()`), then
    assert on `b.snapshotAll()`: `snap.a` exists, `snap.a.level` equals the second beat's own
    `d.state.level`, `snap.a.reason` equals `d.state.reason` and matches `/cost cap/`, `snap.a.agentId`
    is `'a'`, and `typeof snap.a.ts === 'number'`. Compare against the decision rather than a literal
    level so the case survives a future ladder change while still failing a `return {}` stub, a
    `{ a: { level: 'healthy' } }` stub, and a snapshot that drops `reason`. Do NOT weaken the assertion
    to `Object.keys(snap).length >= 0` or to a truthiness check. `test/breaker.test.cjs` uses its OWN tiny
    harness — a module-level `test(name, fn)` at `:34-38` that counts failures, NOT `node:test` — and the
    file ENDS with `process.exit(failures ? 1 : 0)` at `:358` (its last line, re-read this session).
    Insert both cases ABOVE that final line: a case appended after it never runs, and would report green
    forever. Use the file's existing `test()`/`makeBreaker()`/`beatIn()`/`spending()`/`inBand()` helpers
    rather than importing a framework. A failed case is then a non-zero exit and `node --test` reports
    the file red — no framework wiring needed. (Note for the phase orchestrator, not a task step: this
    raises `grep -c "^test(" test/breaker.test.cjs` from 25 to 27; 03-06-PLAN.md quotes the 25 as a
    parenthetical example of the repo's test-title convention — find it with
    `grep -n 'test/breaker.test.cjs. is .25' .planning/phases/03-scale-and-observability/03-06-PLAN.md`
    rather than by line number, which drifts while that plan is being revised: it was at :270, then
    :277, and is at :307 as re-measured this session. It is not an assertion about this file, so
    nothing there goes red — but the parenthetical becomes stale.)
  </action>
  <acceptance_criteria>
    - `grep -n "snapshotAll(): Record<string, BreakerState>" src/main/breaker.ts` matches `>= 1`
    - `grep -n "ipcMain.handle('control:breakerSnapshot'" src/main/index.ts` matches `>= 1`
    - `grep -n "getBreakerSnapshot" src/preload/index.ts` matches `>= 1`
    - `grep -c "snapshotAll" test/breaker.test.cjs` is `>= 2` — measured 0 in this file today
      (`grep -c snapshotAll test/breaker.test.cjs` -> 0), so the count cannot be satisfied by anything
      already there; 2 is one call per new case
    - `grep -cF "snapshotAll(), {}" test/breaker.test.cjs` is `>= 1` — the fresh-instance deep-equal
      specifically (measured 0 today). **Use `-F`**: the pattern contains `()` and `{}`, and a plain
      `grep -c` would be at the mercy of whichever regex dialect the executor's shell provides
    - `grep -cF "snap.a.reason" test/breaker.test.cjs` is `>= 1` — measured **0** today. This is the
      bullet that pins case B specifically: the two bullets above are both satisfiable by writing the
      fresh-instance deep-equal twice, which would leave the escalated-agent behaviour (the one a
      `{ a: { level: 'healthy' } }` stub passes) untested. `reason` is the field such a stub drops
    - `node --test test/breaker.test.cjs` passes — with a `snapshotAll()` that returns `{}` for a ticked
      agent it must FAIL; run it once against a deliberate `return {}` stub before writing the real
      method if there is any doubt the case bites
    - `node --test test/boot-floor.test.cjs` passes — the IPC-name pin (both `B_IPC_JOINED` and the
      sorted-name diff) is green, proving the two-place edit landed together
  </acceptance_criteria>
  <verify>
    <automated>node --test test/boot-floor.test.cjs test/breaker.test.cjs</automated>
  </verify>
  <done>control:breakerSnapshot exists, is IPC-name-pinned, and returns a full per-agent breaker snapshot including agents never yet ticked; its production caller is explicitly deferred to 03-08, not silently missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wire resolveCodexHome, spawnedAt (+ its typed preload field), and the DISPLAY join — the cost-ledger gate is untouched</name>
  <files>src/main/hive.ts, src/main/floor/boot.ts, src/main/index.ts, src/preload/index.ts, test/telemetry-auth.test.cjs</files>
  <read_first>src/main/hive.ts (RegistryAgent interface, ensureAgent's registry upsert, agentDir()), src/main/floor/boot.ts (the runBreakerBeat sample?.sessionId gate around appendCostLedger — READ, DO NOT EDIT; writeFleetSnapshot's usageById-from-snapshot() join, the new TelemetryCollector({...}) construction site), src/main/index.ts (hive:agentDirectory's identical usageById-from-snapshot() join), src/main/telemetry.ts:180-195 and :625-645 (resolveCodexHome's doc comment and transcriptFallback), src/preload/index.ts:135-162 (AgentDirectoryEntry), test/telemetry-auth.test.cjs (existing TelemetryCollector test coverage style via loadTs, the temp-HOME + projectDir fixture pattern at :622-643, and the `projectDir` import at :560), src/main/transcript.ts readAgentUsage (:299) and src/main/usage.ts readCodexUsage (:140-161) — the two parsers the new fixture must satisfy, read them before writing a line of fixture data</read_first>
  <behavior>
    - `hive.codexHomeFor('a1')` returns the same path `installCodexHooks` would have derived for that
      agent's isolated CODEX_HOME (`<agentDir>/.codex`) — a pure path join, no filesystem check.
      Known non-coverage, declared as T-03-02h rather than claimed away (round-3 finding #19): for a
      RESUMED codex agent, `index.ts:2234-2245` (re-measured this session) repoints the spawn's
      `CODEX_HOME` to the sibling agent home that OWNS the rollout, and this pure join does not know
      that. Such an agent reads an empty home and displays zero — an under-report, never a
      neighbour's dollars. Recording the effective home on the registry entry is the fix and it is
      NOT in this plan: it needs a new hive writer on the spawn path (`hive.ts` exposes no public
      registry patcher today — `recordSession` at `hive.ts:1217` is the only per-agent writer,
      measured this session), which is more surface than SCALE-05's card needs.
    - `boot.ts`'s `TelemetryCollector` construction now passes a `resolveCodexHome` closure that
      returns `hive.codexHomeFor(agentId)` ONLY when that agent's registered `provider` is
      `'codex'`, and `null` for every other provider — so a codex agent's transcript fallback reads
      the per-agent home this app derives for it (never a cwd-sharing neighbour's Claude
      transcripts), and every other engine's fallback behavior (reading its cwd's
      transcripts) is completely unchanged AT THE COLLECTOR. `resolveCwd` keeps today's semantics,
      so `runBreakerBeat`'s inputs and the untouched cost-ledger gate see exactly what they see
      today; the whole-cwd read is withheld only at the DISPLAY join, by `hasOwnCostSource`, and
      only from an agent that cannot prove the reading is its own.
    - A synthetic two-agent fixture in `test/telemetry-auth.test.cjs` — one `provider: 'codex'` agent
      and one `provider: 'claude'` agent sharing the SAME `cwd`, constructed via a real
      `TelemetryCollector` with a `resolveCodexHome` closure that mirrors the production gating —
      proves the codex agent's `getAgentUsage()` result no longer contains the claude agent's
      transcript totals. The same case carries its own NEGATIVE CONTROL: the identical fixture read
      through a second collector built WITHOUT `resolveCodexHome` DOES return the claude neighbour's
      totals for the codex agent. Without the control the case could pass on a fixture where both
      sides read zero, which is why round-3 finding #4 called the old "fixture" undetectable.
    - `hasOwnCostSource` answers the OTHER direction of the same bug, and is asserted as a pure
      function over object literals in the same test file: false for two claude agents sharing one
      `cwd` (neither can prove the whole-cwd total is its own), true for a claude agent that is the
      sole holder of its `cwd`, true for a `'codex'` agent regardless of cwd sharing, false for a
      `'crush'`/`'gemini'` agent (no transcript reader of its own — a whole-cwd read would be a claude
      neighbour's transcripts), false for an unknown id, and true for an agent whose `provider` is
      absent but is the sole holder of its cwd (the `?? 'claude'` default the rest of hive.ts uses).
    - After `ensureAgent` runs for a brand-new spawn, `hive.registry().agents[id].spawnedAt` is a
      number close to `Date.now()`; after a RESPAWN of the same agent id, `spawnedAt` advances to the
      new spawn's timestamp rather than preserving the old one (unlike `sessionId`, which the
      `...prev` spread already preserves) — duration is "since THIS spawn," never cumulative across
      respawns.
    - `writeFleetSnapshot` and `hive:agentDirectory`'s per-agent join now report a non-zero `usd`/
      `tokens` for an agent whose ONLY usage source is the transcript fallback (no live OTel session)
      AND whose fallback is provably its own money (`hasOwnCostSource`), where before they silently
      reported `0` — but that SAME agent's `lastActiveSecAgo` reads `null`,
      never a permanently-fresh `0`, because a fallback sample's `ts` is the READ time, not an
      activity time (`sessionId: ''` is reused as the exact discriminator: freshness is only computed
      when `u?.sessionId` is truthy). The same discriminator ALSO sets a new `costLifetime: true` flag
      on that agent's response row, so a consumer never silently conflates an all-time cumulative
      total with a per-session number.
    - An agent whose transcript fallback is NOT provably its own — two claude workers in one repo, or
      any engine with no transcript reader of its own — keeps reporting `usd: 0` / `tokens: 0` and
      carries `costUnattributed: true`, so the zero reads as a DECLARED GAP rather than a measurement,
      and the row never carries the neighbour's dollars. Its live-OTel figures, if it has a session,
      are unaffected: OTel spend is attributed per agent at ingest, so a live sample is always shown
      and `costUnattributed` is `false` whenever a sample exists.
    - The `if (sample?.sessionId) hive.appendCostLedger(sample);` line in `runBreakerBeat` is
      BYTE-IDENTICAL after this task — a diff of `boot.ts` shows zero changes to that line or to
      `applyCostRow` in `hive.ts`.
  </behavior>
  <action>
    In `hive.ts`: add `spawnedAt?: number;` to the `RegistryAgent` interface (beside `lastSeen`), and
    in `ensureAgent`'s registry upsert object literal add `spawnedAt: Date.now()` UNCONDITIONALLY
    (not spread from `...prev` — every (re)spawn is a new PTY, so the timestamp must reset, exactly
    like the adjacent `lastSeen: Date.now()` it sits beside). Add a new public method `codexHomeFor(id:
    string): string { return join(this.agentDir(id), '.codex'); }` (mirrors `installCodexHooks`'s own
    `join(dir, '.codex')` derivation exactly; the private `agentDir` stays private). Also EXPORT a pure
    top-level function from the same file — `export function hasOwnCostSource(agents: Record<string,
    RegistryAgent>, id: string): boolean` — returning `true` when and ONLY when `(a.provider ?? 'claude')
    === 'codex'`, and `false` for every other provider (claude included) and for an unknown id. There is
    no cwd-uniqueness arm and none may be added: a cwd is not a per-agent transcript root, so a
    whole-directory read can always include another project's agent, a deleted agent, or the operator's
    own Claude Code CLI sessions. Keep it a free
    function, not a method: both call sites already hold `reg.agents`, and a pure function is testable
    from `test/telemetry-auth.test.cjs` with an object literal, no HiveManager instance. Do NOT touch
    `applyCostRow`, `costCumulative`, or the `appendCostLedger` call site in `boot.ts` — those are
    out of scope for this plan (see the scope note above the objective). In `boot.ts`: change the
    `new TelemetryCollector({...})` construction to add `resolveCodexHome: (agentId: string) =>
    hive.registry().agents[agentId]?.provider === 'codex' ? hive.codexHomeFor(agentId) : null`
    alongside the existing `emit`/`resolveCwd` options. In the SAME file's `writeFleetSnapshot`, and in
    `index.ts`'s `hive:agentDirectory`: widen the `usageById.get(id)` lookup (sourced from
    `telemetry.snapshot()`, which only iterates live-OTel agent ids) to reach the transcript fallback
    ONLY for an agent that provably owns it. Inside the existing `Object.entries(reg.agents)` loop —
    both handlers already loop over the full registry, so this is a mechanical swap, not a new loop —
    bind `const own = hasOwnCostSource(reg.agents, id);` — pass the FULL `reg.agents` map, never the
    archived-filtered list `writeFleetSnapshot` builds (`boot.ts:478`, `.filter(([, a]) =>
    !a.archived)`, re-measured this session): an archived neighbour's transcripts are still sitting in
    that directory, so filtering them out of the predicate's input would declare a shared cwd
    exclusive and hand the survivor the archived agent's dollars — and then
    `const u = own ? telemetry.getAgentUsage(id) : (usageById.get(id) ?? null);`. KEEP the
    `usageById`/`snap.usage` map: it is what still delivers live-OTel spend to an agent that fails the
    predicate, and dropping it would replace fabricated spend with a fabricated zero (round-3 finding
    #1). `getAgentUsage` tries the live aggregate first, so the `own` branch loses nothing. Change BOTH
    files' `lastActiveSecAgo: u ? Math.round((now - u.ts) / 1000) : null` to
    `lastActiveSecAgo: u?.sessionId ? Math.round((now - u.ts) / 1000) : null` — reusing `sessionId`
    (already the exact marker `transcriptFallback` sets to `''`) as the freshness discriminator.
    ALSO add `costLifetime: u ? u.sessionId === '' : false` to both handlers' per-agent response object
    (beside `usd`/`tokens`) — the SAME discriminator, surfaced explicitly rather than left for a
    consumer to re-derive from `sessionId`, which neither preload response currently exposes. ALSO add
    `costUnattributed: !u && !own` to both handlers' per-agent response object, beside `costLifetime` —
    the row's `usd: 0` / `tokens: 0` is then explicitly a declared gap ("this agent's spend cannot be
    attributed from here") rather than a measured zero, and it is the field 03-08's card renders instead
    of a dollar figure. Surface `spawnedAt` in `hive:agentDirectory`'s per-agent response object
    (`spawnedAt: a.spawnedAt ?? null`). Import `hasOwnCostSource` in both files by adding it to the
    existing named hive import (`boot.ts:31`, `index.ts:30`) — no new import line.
    In `preload/index.ts`: add `spawnedAt: number | null;`, `costLifetime: boolean;` and
    `costUnattributed: boolean;` to the `AgentDirectoryEntry` interface (`:135-162`) — this is the typed
    producer 03-08's `agentView.ts` consumes; do not leave the widening as a raw untyped IPC payload
    change only.

    Write the T-03-02d fixture in this task, in `test/telemetry-auth.test.cjs` — round-3 finding #4 was
    that no action step wrote it and no criterion detected it, so the mitigation was prose. Add ONE
    `node:test` case that: makes two temp dirs (a SHARED `cwd` and the codex agent's own CODEX_HOME) with
    `fs.mkdtempSync`, points `process.env.HOME`/`USERPROFILE` at a third temp dir and restores them in
    `t.after` (copy the existing pattern at `test/telemetry-auth.test.cjs:622-643`, which already does
    exactly this and already imports the `projectDir(cwd)` helper at `:560`); seeds a usage-bearing
    Claude transcript under `projectDir(sharedCwd)` and a codex rollout under
    `<codexHome>/sessions/<...>.jsonl` with a DIFFERENT non-zero total; then builds two real
    `TelemetryCollector`s over the same fixture — one with `resolveCodexHome` gated on the fake
    registry's `provider`, one without — and asserts the gated collector's `getAgentUsage('cx')` matches
    the codex total while the ungated one matches the claude total (the negative control that makes the
    case bite). Name the codex agent `'cx'` and write that id as a LITERAL in both assertions
    (`getAgentUsage('cx')` twice, not `getAgentUsage(codexId)` and not a shared helper): the
    acceptance criterion counting those two calls is a fixed-string grep, and a variable defeats it. Read `src/main/transcript.ts`'s `readAgentUsage` parser and `src/main/usage.ts`'s
    `readCodexUsage`/`rolloutUsage` (`usage.ts:140-161`) for the exact line shapes each expects — do not
    guess them; a fixture both readers score as zero would pass a sloppy assertion and prove nothing.
    In the same file add the pure-predicate case: `const { hasOwnCostSource } = loadTs('src/main/hive.ts')`
    (other tests already `loadTs` this module — `test/hive-durability.test.cjs:27` — so it transpiles
    standalone under D-24's no-electron-imports rule) driven over object literals for the six cases listed in this task's behavior block.
  </action>
  <acceptance_criteria>
    - `grep -c "codexHomeFor" src/main/hive.ts` is `>= 1` AND `grep -c "codexHomeFor"
      src/main/floor/boot.ts` is `>= 1` — measured 0 and 0 today. Split per file deliberately: a
      combined "`>= 2` total" is satisfied by the method definition plus one doc-comment mention
      INSIDE hive.ts, i.e. by a method nothing calls, which is precisely the wiring gap this plan
      exists to close
    - `grep -n "resolveCodexHome" src/main/floor/boot.ts` matches `>= 1` (actually wired at the
      production construction site — the exact gap round-1 red-team finding #3 identified; measured 0
      in this file today)
    - `grep -c "resolveCodexHome" test/telemetry-auth.test.cjs` is `>= 1` — measured **0** today, so
      this is exactly the "the fixture is written by no action step and detected by no criterion"
      hole round-3 finding #4 opened. The only other test command in this task runs files that are
      already green, so without this bullet the mitigation is unfalsifiable
    - `grep -cF "getAgentUsage('cx')" test/telemetry-auth.test.cjs` is `>= 2` — measured **0** today.
      The bullet above is satisfiable by a comment that merely names `resolveCodexHome`; this one is
      not: the action names the codex agent `cx`, and the negative control reads that SAME id through
      BOTH collectors (gated and ungated), so a single-collector case — the exact shape that "passes
      on a fixture where both sides read zero" — cannot reach 2. **Use `-F`** (the pattern contains
      `()` and quotes)
    - `grep -c "hasOwnCostSource" test/telemetry-auth.test.cjs` is `>= 1` — measured 0 today; the pure
      predicate's six cases live here
    - `grep -n "export function hasOwnCostSource" src/main/hive.ts` matches `>= 1` — measured 0 today
      (exported, not a private method, so the test can drive it without a HiveManager)
    - `grep -c "hasOwnCostSource" src/main/floor/boot.ts` is `>= 2` AND `grep -c "hasOwnCostSource"
      src/main/index.ts` is `>= 2` — measured 0 and 0 today; per file the floor is the added named
      import plus the `const own = ...` call site, so a join that reaches `getAgentUsage` ungated
      cannot satisfy it. Split per file: a combined "`>= 4` total" is fully satisfied by wiring
      boot.ts twice over and leaving `hive:agentDirectory` — the card's own source — ungated
    - `grep -cF "own ? telemetry.getAgentUsage(id)" src/main/floor/boot.ts` is `>= 1` AND the same
      `grep -cF` on `src/main/index.ts` is `>= 1` — measured **0/0** today. This is the bullet that actually closes round-3 finding
      #1: the `hasOwnCostSource` count bullet above is satisfied by an import plus a call whose result
      is ignored, which would leave the join reaching `getAgentUsage(id)` UNGATED and still showing a
      cwd-neighbour's dollars. This pins the gate expression itself, in both joins. **Use `-F`** (the
      pattern contains `?` and `()`)
    - `grep -cF "costUnattributed: !u && !own" src/main/floor/boot.ts` is `>= 1` AND the same
      `grep -cF` on `src/main/index.ts` is `>= 1` — measured **0/0** today; the declared-gap flag is
      the OTHER half of the same gate (an agent that fails the predicate and has no live sample
      renders the gap, never a measured `$0` that reads as "cheap" — D-35). Per file, again: a
      "`>= 2` total" passes with both matches in boot.ts and the agent card's own handler bare.
      **Use `-F`**
    - `grep -c "costUnattributed" src/preload/index.ts` is `>= 1` — measured 0 today (the typed
      preload field; the two join sites are already pinned per file by the bullet above)
    - `grep -n "spawnedAt" src/main/hive.ts` matches `>= 2` (interface field + upsert assignment)
    - `grep -n "spawnedAt: a.spawnedAt ?? null" src/main/index.ts` matches `>= 1` (the specific new
      field assignment inside hive:agentDirectory — not merely the word "spawnedAt", which
      already appears 7 times in this file for the unrelated liveWorkers/workerSignaledDone worker
      feature; this criterion is anchored to the actual new shape, not the bare token count)
    - `grep -nF "spawnedAt: number | null" src/preload/index.ts` matches `>= 1` (the typed interface
      field, not only the raw IPC payload). **Use `-F`.** Measured in this file today: the `-F` form
      matches 0 (correct — the field does not exist yet), while the same pattern written with an
      escaped `\|` matches **54** pre-existing lines, because BRE reads it as alternation of
      `"spawnedAt: number "` OR `" null"` — green before any work is done. `-F` removes the question.
    - `grep -cF "costUnattributed: boolean" src/preload/index.ts` is `>= 1` — the typed field, same
      `-F` reasoning; measured 0 today
    - `grep -n "if (sample?.sessionId) hive.appendCostLedger" src/main/floor/boot.ts` matches `>= 1`
      (the gate is UNCHANGED — this is a positive assertion that the line still exists exactly as
      before, proving this plan did not touch it)
    - `grep -cF "cumulative.get(key)" src/main/hive.ts` matches `>= 1` — applyCostRow's DIFF LOGIC is
      untouched by THIS plan. **Deliberately not anchored to `private applyCostRow`:** 03-03 (wave 3)
      hoists that function out of `private` and widens its return channel, so a signature-shaped pin
      here would go red one wave later through no fault of any executor — round-7 finding #9. What
      must not change is the arithmetic, and `cumulative.get(key)` is the line that carries it.
    - `grep -c "costLifetime"` is `>= 1` in EACH of `src/main/floor/boot.ts`, `src/main/index.ts` and
      `src/preload/index.ts` — measured 0/0/0 today. Three separate per-file bounds, not a `>= 3`
      total: the total is reachable with all three matches in one file
    - `grep -cF "u?.sessionId ? Math.round" src/main/floor/boot.ts` is `>= 1` AND the same `grep -cF`
      on `src/main/index.ts` is `>= 1` — measured 0/0 today; the freshness fix must land in BOTH
      joins, and a `>= 2` total does not require that. **Use `-F`**, measured this session against a
      two-line probe holding the old and new `lastActiveSecAgo` lines: `grep -c` (BRE) matches the
      new line 1 time, `grep -Ec` (ERE) matches it **0** times — under ERE the `?`s are quantifiers,
      so the un-`-F`'d bullet reports RED against correct work. `-F` removes the dialect question
    - `grep -c "telemetry.getAgentUsage(id)" src/main/index.ts` is `>= 1` — measured **0** in this
      file today, so this bullet is the whole gate on `hive:agentDirectory`'s half of the swap
    - `grep -c "telemetry.getAgentUsage(id)" src/main/floor/boot.ts` is `>= 2` — measured **1** today
      (`boot.ts:428`, `const sample = telemetry.getAgentUsage(id);` inside `runBreakerBeat`, the line
      this plan must leave alone). Split per file deliberately: a combined "`>= 2` across both files"
      would have been HALF-satisfied by that pre-existing line before any work was done, and could be
      finished by wiring only one of the two joins. The second boot.ts match is `writeFleetSnapshot`'s
      new gated call
    - `grep -c "usageById" src/main/floor/boot.ts` is `>= 2` AND `grep -c "usageById" src/main/index.ts`
      is `>= 2` — measured 2 and 2 today. An unchanged-pin, like the appendCostLedger bullet above,
      not a progress check: the live-OTel map must SURVIVE the swap, because it is what still delivers
      live spend to an agent that fails `hasOwnCostSource`. Deleting it (as an earlier draft of this
      action said to) would replace a fabricated neighbour figure with a fabricated zero
    - `node --test test/telemetry-auth.test.cjs test/hive-durability.test.cjs test/boot-floor.test.cjs`
      passes, and `npm run typecheck` exits 0
  </acceptance_criteria>
  <verify>
    <automated>node --test test/telemetry-auth.test.cjs test/hive-durability.test.cjs test/boot-floor.test.cjs</automated>
  </verify>
  <done>resolveCodexHome is wired at the one production TelemetryCollector construction site so a codex agent's DISPLAYED cost never reads a cwd-sharing neighbour's transcripts, and hasOwnCostSource closes the other direction so no non-codex fallback agent is ever shown a neighbour's dollars — an unprovable reading renders as costUnattributed, never as a measured figure; both directions are covered by real cases in test/telemetry-auth.test.cjs, the codex one with a negative control; spawnedAt is recorded per (re)spawn and typed on the preload interface; the display join reports transcript-fallback-only agents' cost with a correct freshness reading and an explicit costLifetime discriminator; the cost-ledger append gate and applyCostRow are byte-unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Renderer <-> main (`control:breakerSnapshot`) | Read-only pull; no input accepted from the renderer beyond the invoke itself |
| main <-> `cost-ledger.jsonl` | Append-only, main-process-only write; UNCHANGED by this plan — the gate and its semantics are exactly as they were before |
| main <-> a codex agent's isolated CODEX_HOME | `resolveCodexHome` reads only the directory this app itself created for that specific agent (`agentDir(id)/.codex`), gated on the registry's own recorded provider — never an operator- or renderer-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-02a | Information Disclosure | `control:breakerSnapshot` response | accept | Returns only `agentId`/`level`/`reason`/`ts` — the same fields `control:setBreakerState` already pushes to every window; no new sensitive field is added |
| T-03-02c | Tampering | `spawnedAt` reset on respawn | accept | `spawnedAt` is derived server-side from `Date.now()` at spawn time only, never renderer-supplied — no injection surface |
| T-03-02d | Information Disclosure | a codex agent's transcript fallback reading a cwd-sharing Claude agent's transcripts, surfacing the neighbour's spend on the fleet DISPLAY | mitigate | `resolveCodexHome` is wired at the ONE production construction site, gated on the registry's own recorded `provider === 'codex'`. The two-agent same-cwd fixture is WRITTEN by Task 2 (an explicit action step, not merely described) and is detected by three criteria that are measured at 0 today: `grep -c "resolveCodexHome" test/telemetry-auth.test.cjs` `>= 1`, `grep -c "hasOwnCostSource" test/telemetry-auth.test.cjs` `>= 1`, and — the one a naming comment cannot satisfy — `grep -cF "getAgentUsage('cx')" test/telemetry-auth.test.cjs` `>= 2`. The case carries a negative control (the same fixture read by a collector built WITHOUT `resolveCodexHome` returns the neighbour's totals), so a fixture that stopped discriminating fails, and the `>= 2` count is exactly what a single-collector (control-free) case cannot reach. This never reaches the ledger (that gate is untouched), only the display join this plan also changes |
| T-03-02g | Information Disclosure | the OTHER direction of the same read: a NON-codex agent whose transcript fallback is a whole-cwd sum — two claude workers in one repo, or an engine with no reader of its own — shown as that agent's measured spend on the fleet snapshot and the agent card | mitigate | The display join reaches the transcript fallback only through `hasOwnCostSource(reg.agents, id)`: codex (per-agent CODEX_HOME), or claude AND sole registry holder of that `cwd`. Every other agent renders `usd: 0` with `costUnattributed: true` — a declared gap, never a neighbour's dollars. The gate is detected mechanically, not by prose: `grep -cF "own ? telemetry.getAgentUsage(id)"` and `grep -cF "costUnattributed: !u && !own"` are each `>= 2` across the two join files, both measured 0/0 today, so an ungated join cannot pass. The predicate is pure and asserted over object literals in `test/telemetry-auth.test.cjs`; live-OTel spend is unaffected because it is attributed per agent at ingest. Residual: an agent that genuinely spent money through a shared cwd reads as an unattributed zero on the display — an UNDER-report, which is the direction this project accepts (D-35's declared-gap rule), never an over-report |
| T-03-02h | Repudiation | a RESUMED codex agent whose `CODEX_HOME` was repointed to the sibling home owning its rollout (`index.ts:2234-2245`, measured this session) — `codexHomeFor(id)`'s pure `agentDir(id)/.codex` join reads that agent's own, empty home and the display shows `$0` with `costUnattributed: false` | accept | Round-3 finding #19. The error direction is an UNDER-report of that agent's own spend, never another agent's dollars, so it does not re-open #1/T-03-02d: the gate still refuses every reading it cannot attribute. Closing it means recording the effective `CODEX_HOME` on the registry entry at spawn and preferring it in `codexHomeFor`, which needs a new public per-agent registry writer in `hive.ts` (today the only one is `recordSession`, `hive.ts:1217`) plus an edit to the resume branch — more surface than SCALE-05's card requires, and out of this plan's scope. Stated here so the card's `$0` for a resumed codex agent is a known limit, not a surprise |
| T-03-02e | Repudiation | transcript-tier spend never reaches `cost-ledger.jsonl` — the breaker's inputs and the display join see it, the durable ledger and `taskSpend`/`budgetForAgent` do not | accept | Declared Accepted Residual (03-CONTEXT.md). Three rounds of red-team review on the widened-gate approach produced 13 confirmed findings of the same class (baseline-seed correctness, monotonic-floor correctness, cross-agent double-billing, zero test coverage) — the orchestrator's decision for this revision is to revert the widening rather than patch it a third time. Closing this gap is PARITY-02 work (D-03); the per-agent session filter it will need (`ReadUsageOptions`) already exists at `transcript.ts:299` |
| T-03-02f | Repudiation | a fallback's all-time cumulative spend displayed beside `up` = time since THIS spawn, without a discriminator | mitigate | `costLifetime: true` is surfaced explicitly in both display joins whenever the sample's `sessionId === ''`; 03-08's `agentView.ts` is required to render it with a distinct label, never silently conflated with a per-session number. Residual, stated rather than assumed away: the four EXISTING renderer callers of `hive:agentDirectory` (`src/renderer/src/realtime/tools.ts:481, :526, :578, :630`, measured this session) narrate `usd`/`tokens` by voice and this plan gives them no new phrasing, so a `costLifetime`/`costUnattributed` row is spoken as a plain number there until a later plan widens them. This plan's own change makes those four MORE honest, not less — the gated join removed the neighbour's dollars they would otherwise have read |
</threat_model>

<verification>
- `node --test test/boot-floor.test.cjs test/breaker.test.cjs test/hive-durability.test.cjs test/telemetry-auth.test.cjs` — 0 failures
- `npm run typecheck` — 0 errors
</verification>

<success_criteria>
- `control:breakerSnapshot` is IPC-pinned and returns a full per-agent snapshot including untouched
  agents; its caller is explicitly deferred to 03-08
- `snapshotAll()` is covered by two real cases in `test/breaker.test.cjs` — a `return {}` stub fails
  the suite
- `resolveCodexHome` is wired at the production `TelemetryCollector` construction site, gated on
  provider, with a same-cwd fixture and negative control in `test/telemetry-auth.test.cjs` — a codex
  agent's DISPLAYED spend never reads a neighbour's transcripts
- No non-codex agent's row ever carries a whole-cwd transcript total it cannot prove is its own:
  `hasOwnCostSource` gates the join and the unprovable case renders `costUnattributed: true` with
  `usd: 0`, per D-35
- `spawnedAt` is recorded per (re)spawn and typed on both `RegistryAgent` and `AgentDirectoryEntry`
- `writeFleetSnapshot` and `hive:agentDirectory` no longer silently report zero cost for a
  transcript-fallback-only agent, report a correct (never permanently-fresh) `lastActiveSecAgo`, and
  carry an explicit `costLifetime` discriminator
- The cost-ledger append gate and `applyCostRow` are byte-unchanged — the ledger gap is a stated
  Accepted Residual, not a silent one
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-02-SUMMARY.md` when done
</output>
