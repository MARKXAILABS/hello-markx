---
phase: 03-scale-and-observability
plan: 8
type: execute
wave: 5
depends_on: ["03-02", "03-07"]
files_modified:
  - src/renderer/src/store/agentView.ts
  - src/renderer/src/hooks/useTelemetry.ts
  - src/renderer/src/components/AgentCard.tsx
  - src/renderer/src/components/FullscreenTerminal.tsx
  - src/renderer/src/components/CommandCenterPanel.tsx
  - src/renderer/src/components/AgentDetailPanel.tsx
  - test/renderer-runstate.test.cjs
  - test/repo-claims.test.cjs
  - test/renderer-components.test.cjs
autonomous: false
requirements: [SCALE-05]

must_haves:
  truths:
    - "One pure, React-free derivation module owns cost/duration/context/account/state; AgentCard, FullscreenTerminal and CommandCenterPanel all call it instead of re-deriving locally"
    - "The context-pressure threshold is 85/65 everywhere, replacing the drifted 88/75 and 87.5/75 pairs"
    - "Cost is a discriminated {kind:'measured', usd, lifetime} or {kind:'unmeasured', reason} value and NEVER renders $0.00 for a costTracking:'none' engine, and a lifetime:true (transcript-fallback all-time total) value is never presented as this session's spend"
    - "spawnedAt and costLifetime actually reach the renderer — agentView.ts polls hive:agentDirectory itself, and control:breakerSnapshot has a real production caller, closing three IPC-built-but-uncalled gaps"
    - "State reads 'unknown' until control:breakerSnapshot resolves, never defaulting to 'healthy'"
    - "The four independent useFleetTelemetry mounts share ONE subscription, with zero caller-side changes to the four consuming files beyond what this plan's threshold fix already touches"
    - "The consolidated five-field card lands in AgentDetailPanel.tsx, which today shows none of the five fields, and AgentCard's 322x86 box does not grow"
    - "Every one of the five cells has a declared-gap branch, the context cell's `not reported` included — deriveContext is a real exported derivation, not a percentage computed inline in the component"
    - "deriveContextColor has ONE signature, deriveContextColor(pct, accent) — AgentCard feeds it the pct it already computes from the 0..8 gauge, so an agent with an unreported contextLimit keeps its compaction warning"
    - "Task 4's blocking containment gate probes sidebarWidth (320/420/900), the container width that actually drives this card, at base sha and head sha, and records the observed column count at each stop"
    - "The god's non-coverage by this card is a stated, explicit residual decision, not a silent omission"
    - "Task 3's test coverage of AgentDetailPanel.tsx is honest about what it proves — either a real renderToStaticMarkup load of the component (extending test/renderer-components.test.cjs's Module._load shim), or an explicit, written acknowledgment that the assertions are string-presence-only over the source file and do not prove the branch actually renders"
  artifacts:
    - path: src/renderer/src/store/agentView.ts
      provides: "the pure derivation module: deriveCost, deriveDuration, deriveContext (the measured/not-reported pair), deriveContextColor(pct, accent) at the locked 85/65 threshold, the account label resolver, and deriveState"
  key_links:
    - from: "src/renderer/src/components/AgentDetailPanel.tsx"
      to: "src/renderer/src/store/agentView.ts"
      via: "useSyncExternalStore(subscribe, getSnapshot, getSnapshot) with a 3rd getServerSnapshot arg"
      pattern: "useSyncExternalStore"
    - from: "src/renderer/src/hooks/useTelemetry.ts (useFleetTelemetry)"
      to: "the module-singleton subscription"
      via: "one shared subscription reused across all 4 existing mount points"
      pattern: "useSyncExternalStore"
---

<objective>
Build `agentView.ts` — one pure, React-free derivation module following `autoMode.ts`'s exact
module-singleton + `useSyncExternalStore` shape — that owns cost, duration, context-pressure
threshold, account label and breaker-derived block state, so the three renderings that currently
re-derive these values (and have already drifted: 85/65 vs 88/75 vs 87.5/75) read ONE shared answer.
Collapse the four independent `useFleetTelemetry` mounts into one shared subscription in the same
plan, since `agentView.ts`'s own derivation needs that data anyway. Land the consolidated five-field
card in `AgentDetailPanel.tsx`.

Purpose: FLOOR-13 already proved what happens without a shared module — the AUTO chip stayed correct
through `autoMode.ts`, while cost, copied into three files, drifted. This plan closes the same gap for
cost/context/duration/account/state before it drifts further.
Output: `agentView.ts`, the three rewired renderings, the collapsed telemetry singleton, and the
consolidated stat card.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-scale-and-observability/03-CONTEXT.md
@.planning/phases/03-scale-and-observability/03-UI-SPEC.md

<interfaces>
Full UI-SPEC.md §S2 (S2a-S2d) is the binding contract for the card's layout, fields, discriminated
values, and the god-coverage residual decision — read it in full before writing any markup.

`src/renderer/src/store/autoMode.ts` (full file, already read this session) is the EXACT pattern to
follow: pure functions with zero React/store imports, a module-level mutable singleton + listener
`Set`, `get*`/`set*`/`subscribe*` exported trio, published from exactly one place, consumed via
`useSyncExternalStore(subscribeX, getX, getX)` (three args — the third `getServerSnapshot` is
MANDATORY, per UI-SPEC S2a: `renderToStaticMarkup` throws "Missing getServerSnapshot" on a two-arg
call, which would make every SCALE-05 test assertion fail to run rather than fail red). Every shipped
call site already does this correctly (`AgentCard.tsx:231`, `CommandCenterPanel.tsx:425`,
`FullscreenTerminal.tsx:597`) — `agentView.ts` needs a SEEDING SEAM so a server-rendered test can set
the module singleton's initial snapshot before render (the suite already seeds zustand this way —
locate that pattern and match it).

From `src/renderer/src/hooks/useTelemetry.ts` (current, full file already read this session):
```
export function useFleetTelemetry(): FleetTelemetry {
  const [samples, setSamples] = useState(...); ... // 5 independent useState calls
  useEffect(() => {
    window.cth.telemetrySnapshot?.().then(...);      // cold-start backfill
    const offEvent = window.cth.onTelemetryEvent?.(...);
    const offBreaker = window.cth.onBreakerState?.(...);
    return () => { offEvent?.(); offBreaker?.(); };
  }, []);
  return { samples, spark, rate, lastTool, breakers };
}
```
Four existing call sites, unchanged signature required: `AgentStrip.tsx:34`,
`CommandCenterPanel.tsx:370`, `FullscreenTerminal.tsx:88`, `ToolWaterfall.tsx:12`.

The three drifted thresholds to replace (verified this session, exact current lines):
```
FullscreenTerminal.tsx:533-534: const color = pct >= 85 ? coral : pct >= 65 ? lemon : accent;   // KEEP this pair, LOCKED (85/65)
CommandCenterPanel.tsx:853: const ccolor = cpct >= 88 ? coral : cpct >= 75 ? lemon : accent;      // FIX to 85/65
AgentCard.tsx:165-166: gaugeColor = progress >= 7 ? coral : progress >= 6 ? lemon : accent;        // over a 0..8 int (87.5/75) — FIX the COLOUR STEP to read agentView's threshold; the 0..8 GAUGE GEOMETRY stays
```

From `src/renderer/src/components/AgentDetailPanel.tsx` (current, verified this session) — insertion
point in render order: header row (ends ~line 203) -> `openTerminalError` strip (205-212) -> **NEW
stat card here** -> `agent.blockReason && <BlockedBanner>` (220-232) -> `isReal &&
<AgentControlStrip>` (235) -> `<SidebarTabs>` (238). Do not reorder the banner/tabs relationship —
`:216-221`'s comment states the banner sits above the tabs deliberately.

Main-process data this plan reads (landed in 03-02, do not re-implement): `window.cth.getBreakerSnapshot()`
(`control:breakerSnapshot`), `hive:agentDirectory`'s now-includes `spawnedAt` field, and the corrected
per-agent cost join (transcript-tier engines no longer silently zero, and their `lastActiveSecAgo`
correctly reads `null` rather than a permanently-fresh `0` — 03-02's fix, do not re-derive freshness
here). `src/shared/agentProvider.ts`'s `costTracking` field (`'none' | 'otel' | 'proxy' | 'transcript'`)
is the discriminator for the cost field's measured/unmeasured branch.

Existing gap-sentence vocabulary to REUSE, never re-invent (verified this session):
`src/renderer/src/store/config.ts:491-492`:
`` `${engine} reports no cost — ${subject}'s spend is invisible to every budget and to the breaker.` ``

**`test/renderer-components.test.cjs`'s actual coverage ceiling** (relevant to Task 3): the file's
`Module._load` shim (`:97-124`) today loads exactly `PixelBadge.tsx`, `BlockedBanner.tsx`,
`AgentCard.tsx`, `store/store.ts` and `src/shared/agentProvider.ts` — `AgentDetailPanel.tsx` is NOT
among them. Task 3 either extends that shim to also load `AgentDetailPanel.tsx` (if its import graph
proves tractable) or states plainly, in a code comment at the new test case, that its assertions are
string-presence checks over the source file, not proof the card actually renders — see Task 3's
`<action>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: agentView.ts — the pure derivation module, and the useFleetTelemetry singleton collapse</name>
  <files>src/renderer/src/store/agentView.ts, src/renderer/src/hooks/useTelemetry.ts, test/renderer-runstate.test.cjs</files>
  <read_first>src/renderer/src/store/autoMode.ts (full file — the pattern to follow exactly), src/renderer/src/hooks/useTelemetry.ts (full file), src/shared/agentProvider.ts (costTracking field), test/renderer-runstate.test.cjs (autoMode.ts's existing sibling test coverage, and the fixed-input test style to extend)</read_first>
  <behavior>
    - `deriveCost(agent, sample, costTracking)` returns `{kind:'unmeasured', reason: '<engine> reports
      no cost — ...'}` for a `costTracking: 'none'` engine, using the EXACT existing gap-sentence
      vocabulary from `config.ts:491-492` (never a re-invented wording) — and returns `{kind:
      'measured', usd, lifetime: boolean}` (including `usd: 0` when a measured engine has genuinely
      spent nothing) for every other tracking kind.
    - **THIRD BRANCH, and it is load-bearing (round-7 findings #4 and #5).** When the agent's directory
      entry carries `costUnattributed: true`, `deriveCost` returns `{kind:'unmeasured', reason: '<engine>
      spend cannot be attributed to this agent — its transcripts share a directory with other sessions'}`
      — BEFORE any measured branch is considered. Without this branch `costUnattributed` is a
      producer-only field: 03-02 emits it and nothing reads it, so the card renders a measured `$0.00`
      for precisely the agents whose spend is unknown, which is the faked-zero D-35 forbids. This is the
      same producer-with-no-consumer shape as the `firstTs` blocker, caught before it shipped.
      **03-02 drops the claude arm of `hasOwnCostSource` this same round, so this branch is the common
      case, not an edge case.** Precisely: 03-02 sets `costUnattributed: !u && !own`, and `!own` is now
      true for every non-codex agent — so the field is true for any non-codex agent that ALSO has no
      live OTel/proxy sample (`!u`). That is all seven `costTracking:'none'` engines unconditionally,
      plus the `'otel'`/`'proxy'` engines whenever they are not currently reporting. Do not simplify
      this to "always true for non-codex" — the `!u` term is real, and an assertion written against the
      simplified claim would be wrong for a live qwen or crush agent. `lifetime` is `true` when `sample.costLifetime`
      is `true` (03-02's discriminator for a transcript-fallback-sourced all-time cumulative total,
      round-2 finding #23) and `false` otherwise — a consumer renders a `lifetime: true` value with a
      distinct label (Task 3), never silently presenting it as this session's spend.
    - **`deriveContextColor` has exactly ONE signature, and it is percentage-based**:
      `deriveContextColor(pct: number, accent: string): string`. It returns the coral/lemon/accent
      step at exactly 85/65, matching `FullscreenTerminal.tsx`'s existing written rationale — feeding
      it `pct: 87` returns `var(--cth-coral)`, `pct: 70` returns `var(--cth-lemon)`, `pct: 50`
      returns `` `var(--cth-${accent})` ``. (Round-3 finding #28: this plan previously stated the
      signature two incompatible ways — `(tokens, limit)` here and a pct-fed call in Task 2. A
      tokens/limit signature is also WRONG for `AgentCard.tsx`, which has no measured limit in its
      colour path at all: verified this session, `AgentCard.tsx:164-167` derives its colour from the
      `progress` 0..8 integer, and `contextLimit` is used only for the `gaugeTitle` tooltip at
      `:168-170`. Feeding a tokens/limit signature there would silently delete the compaction warning
      for every agent whose limit was never reported.)
    - `deriveContext(contextTokens, contextLimit)` is the FIFTH derivation and the one that owns the
      declared gap: it returns `{kind:'measured', tokens, limit, pct}` when BOTH inputs are present
      and `limit > 0`, and `{kind:'unmeasured'}` when either is absent or the limit is zero —
      mirroring `deriveCost`'s discriminated shape. UI-SPEC §S2b:694 mandates `not reported` (never
      `0%`) for that branch, and round-3 finding #27 measured that no task, action or criterion in
      this plan implemented it. `deriveContext` is a SEPARATE function from `deriveContextColor`:
      the colour step takes a pct its callers already have, and the card's cell takes the raw
      token/limit pair.
    - `deriveDuration(spawnedAt)` returns a formatted string for a present timestamp and the literal
      `'not recorded'` for `undefined` — never `'0s'`.
    - `deriveState(breakerSnapshotEntry)` returns `'unknown'` when no entry has resolved yet, and the
      verbatim `level` string otherwise — never defaulting to `'healthy'`.
    - The module singleton's first-subscriber initialization calls BOTH
      `window.cth.getBreakerSnapshot?.()` (03-02's `control:breakerSnapshot` pull — round-2 finding
      #11: that IPC had no caller anywhere in the plan set until this task) AND
      `window.cth.hiveAgentDirectory?.()` (round-2 findings #29/#32: `spawnedAt` and `costLifetime`
      never reached the renderer — `hive:agentDirectory` had zero renderer callers), merging both
      into the per-agent view cache BESIDE the existing `telemetrySnapshot()` backfill — this is the
      ONE place all three main-process reads converge, matching D-33's "one pure derivation module"
      rationale. `agentView.ts` owns `spawnedAt`/`costLifetime`/breaker-derived `state` per agent id
      internally; no field is added to `src/renderer/src/store/store.ts`'s `Agent` type for this.
    - `useFleetTelemetry()`'s PUBLIC SIGNATURE — `FleetTelemetry` shape and its 4 named return fields
      — is IDENTICAL before and after this task; a diff of the 4 existing call sites
      (`AgentStrip.tsx`, `CommandCenterPanel.tsx`, `FullscreenTerminal.tsx`, `ToolWaterfall.tsx`) shows
      ZERO changes, because the singleton conversion is entirely internal to `useTelemetry.ts`.
    - A test seeding `agentView.ts`'s module singleton BEFORE calling `renderToStaticMarkup` on a
      consumer sees the seeded values in the first-pass output (proving the 3-arg
      `useSyncExternalStore` call and the seeding seam both work under the server-render ceiling).
  </behavior>
  <action>
    Create `agentView.ts` following `autoMode.ts`'s shape exactly: pure, `@shared`-only-import
    functions for each derivation (`deriveCost` returning the `lifetime`-discriminated shape above,
    `deriveContextColor(pct, accent)` with the LOCKED 85/65 pair as named constants — ONE signature,
    pct-based, never `(tokens, limit)`, per round-3 finding #28 — `deriveContext(contextTokens,
    contextLimit)` returning the `{kind:'measured'|'unmeasured'}` pair that owns UI-SPEC S2b's
    `not reported` gap (round-3 finding #27), `deriveDuration`,
    `deriveState`, an account-label resolver reusing `LOGIN_ACCOUNT_LABEL`), a module-level mutable
    per-agent view cache + listener `Set`, and the `get*`/`set*`/`subscribe*` exported trio, PLUS a
    seeding function/seam a server-render test can call before mount (match whatever seam the
    existing zustand-seeding tests already use — locate it first). On first subscriber, ALSO fetch
    `window.cth.getBreakerSnapshot?.()` and `window.cth.hiveAgentDirectory?.()` and fold `spawnedAt`/
    `costLifetime`/breaker `level` per agent id into the same per-agent cache the derivation functions
    read from — poll `hiveAgentDirectory` on the same cadence as the existing breaker-beat interval
    (or on each incoming telemetry event, whichever is simpler; the executor's call, but it must not
    be a one-shot fetch that goes stale for the life of the process). In `useTelemetry.ts`, convert
    `useFleetTelemetry`'s internals to the module-singleton pattern: one mutable module-level `state:
    FleetTelemetry` object, ONE subscription (the existing `telemetrySnapshot()` backfill +
    `onTelemetryEvent`/`onBreakerState` listeners, now registered ONCE at first subscriber rather than
    once per mount — reference-count if teardown-on-zero-listeners matters, or leave it always-on for
    the app's lifetime if simpler; either is acceptable as long as the four call sites still work
    unmodified), publishing a NEW top-level `state` object (not mutated in place) on every incoming
    event so `useSyncExternalStore(subscribe, () => state, () => state)` has a stable-until-changed
    snapshot reference. Do not touch `useAgentSpans` in this same file — it is out of scope. Extend
    `test/renderer-runstate.test.cjs` with the derivation function cases from `<behavior>` (including
    the `lifetime` discriminator), the breaker-snapshot-pull and `hiveAgentDirectory`-poll seeding
    cases, and the seeding-seam server-render case. Two of those cases are new in this revision and
    are not optional: `deriveContextColor(87, 'lagoon')`/`(70, …)`/`(50, …)` returning coral/lemon/
    `var(--cth-lagoon)` respectively (round-3 #28 — the settled pct signature), and `deriveContext`
    returning `{kind:'unmeasured'}` for `(undefined, 200000)`, for `(50000, undefined)` and for
    `(50000, 0)`, and `{kind:'measured'}` with `pct` rounded from the pair for `(50000, 200000)`
    (round-3 #27 — the branch UI-SPEC S2b:694 mandates and nothing implemented).
  </action>
  <acceptance_criteria>
    - All FIVE derivations are exported (round-3 finding #27 bumped this from four —
      `deriveContext` is the new one that owns UI-SPEC S2b's `not reported` branch). `grep -F` is
      mandatory and the five patterns must be run as FIVE SEPARATE commands: a quoted `\|` in a BRE
      pattern is alternation, which silently turns a five-way check into "any one of them matched",
      and a single `grep -c` over five `-e` patterns counts matching LINES, so a stray doc-comment
      line would inflate it. Run, and require `>= 1` for each:
      `grep -c -F "export function deriveCost" src/renderer/src/store/agentView.ts`,
      then the same for `"export function deriveContext("` (the trailing `(` is what stops
      `deriveContextColor`'s own line from satisfying it),
      `"export function deriveContextColor"`, `"export function deriveDuration"` and
      `"export function deriveState"`.
    - `grep -c -F "deriveContextColor(pct" src/renderer/src/store/agentView.ts` matches `>= 1` — the
      ONE settled signature is the pct-based one (round-3 finding #28); `grep -c -F
      "deriveContextColor(tokens" src/renderer/src/store/agentView.ts` matches exactly `0`
    - `grep -n "85" src/renderer/src/store/agentView.ts` matches `>= 1` and `grep -n "65" src/renderer/src/store/agentView.ts` matches `>= 1` (the locked threshold pair, as named constants)
    - `grep -n "lifetime" src/renderer/src/store/agentView.ts` matches `>= 2` (round-2 #23's
      discriminator, both produced by `deriveCost` and read from `sample.costLifetime`)
    - `grep -c -F "costUnattributed" src/renderer/src/store/agentView.ts` matches `>= 1` — measured
      **0** today. This is the consumer half of 03-02's producer field (round-7 #4/#5); without it the
      card renders a measured `$0.00` for every agent whose spend cannot be attributed.
    - `grep -c -F "costUnattributed" test/renderer-runstate.test.cjs` matches `>= 1` — measured **0**
      today. The existence pin: the derivation test must actually drive the third branch, not merely
      have the branch present in source.
    - `grep -n "getBreakerSnapshot" src/renderer/src/store/agentView.ts` matches `>= 1` (round-2 #11
      — the pull IPC now has its one production caller)
    - `grep -n "hiveAgentDirectory" src/renderer/src/store/agentView.ts` matches `>= 1` (round-2
      #29/#32 — spawnedAt/costLifetime now actually reach the renderer)
    - `grep -c "spawnedAt" src/renderer/src/store/store.ts` matches exactly `0` (confirms the design
      choice: no Agent-type field was added — agentView.ts owns spawnedAt in its own cache)
    - **Singleton-collapse pin, with the measured numbers stated** (round-3 finding #36 — the old
      form named no pass/fail threshold). Measured this session: `useTelemetry.ts` contains `7`
      `useState` calls in total, `5` of them inside `useFleetTelemetry`'s body and `2` inside
      `useAgentSpans`. After this task the body count is `0` and the file total is `2`:
      `node -e "const s=require('fs').readFileSync('src/renderer/src/hooks/useTelemetry.ts','utf8');const i=s.indexOf('export function useFleetTelemetry');if(i<0){console.error('FAIL: useFleetTelemetry not found — this check would be vacuous');process.exit(1);}let j=s.indexOf('\nexport ',i+1);if(j<0)j=s.length;const n=(s.slice(i,j).match(/useState/g)||[]).length;if(n!==0){console.error('FAIL: useFleetTelemetry still has '+n+' useState call(s) — not converted to the module singleton');process.exit(1);}if((s.match(/useState/g)||[]).length!==2){console.error('FAIL: file-wide useState count is not 2 — useAgentSpans was touched, or the collapse removed the wrong hook');process.exit(1);}console.log('PASS');"`
    - **Caller-side no-op pin, as a runnable command, scoped to THIS PLAN's OWN commits** (round-3
      finding #36 gave this pin a threshold; round-4's same-wave-ownership finding gives it the
      right range). A bare `git show --name-only HEAD` is the WRONG range here: `use_worktrees:
      false` and 03-06 is also `wave: 5`, so by the time this criterion runs `HEAD` may be one of
      03-06's commits — the positive half would then read `0` and fail 03-08 for a sibling's work,
      and the negative half would read `0` for that same wrong reason. Use the plan-id-scoped form
      03-04 Task 3 already uses, which counts only commits whose message names this plan:
      `git log --oneline --grep="03-08" -- src/renderer/src/store/agentView.ts | wc -l` is `>= 1`
      (the positive half — a never-made or empty commit fails it), AND
      `git log --oneline --grep="03-08" -- src/renderer/src/components/AgentStrip.tsx src/renderer/src/components/ToolWaterfall.tsx | wc -l`
      is exactly `0`. Both caller paths verified to exist this session (`AgentStrip.tsx:34`,
      `ToolWaterfall.tsx:12`, each calling `useFleetTelemetry()`); neither needs an edit, because
      the singleton conversion is entirely internal to `useTelemetry.ts` and `useFleetTelemetry`'s
      public signature is unchanged.
    - `node --test test/renderer-runstate.test.cjs` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-runstate.test.cjs</automated>
  </verify>
  <done>agentView.ts exists, follows autoMode.ts's pattern exactly including the mandatory 3-arg useSyncExternalStore, and useFleetTelemetry is a single shared subscription with an unchanged public signature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: wire the three existing renderings to the shared threshold and derivation</name>
  <files>src/renderer/src/components/AgentCard.tsx, src/renderer/src/components/FullscreenTerminal.tsx, src/renderer/src/components/CommandCenterPanel.tsx, test/repo-claims.test.cjs, test/renderer-components.test.cjs</files>
  <read_first>src/renderer/src/components/AgentCard.tsx:160-232 (the gauge/colour derivation and the existing agentRowForCard/autoMode call pattern to mirror), src/renderer/src/components/FullscreenTerminal.tsx:527-534 (ContextBar), src/renderer/src/components/CommandCenterPanel.tsx:846-853 (the ctx colour block), src/renderer/src/store/agentView.ts (Task 1)</read_first>
  <behavior>
    - `FullscreenTerminal.tsx:533`'s color expression is replaced by a call into `agentView.ts`'s
      `deriveContextColor` — the RENDERED colour for any given pct is byte-identical to before
      (85/65 was already correct here, so this is a derivation-source change with zero visible
      change), proving the wiring is correct without needing a new visual baseline.
    - `CommandCenterPanel.tsx:853`'s `cpct >= 88`/`75` pair is REPLACED. `pct = 86` now renders coral
      where it used to render lemon; `pct = 80` renders lemon under both thresholds and is the
      control case. (Round-3 finding #41: the earlier wording claimed 80% flips to coral and then
      contradicted itself in the same sentence — an executor writing the mandated boundary test from
      the first half would have written a failing test. 80 is the control, 86 is the flip.)
    - **AgentCard's compaction warning survives the rewire** (round-3 finding #28). `AgentCard.tsx`
      calls `deriveContextColor` with the pct it ALREADY computes from the 0..8 gauge —
      `Math.min(8, Math.max(0, progress))/8*100` — never with `(contextTokens, contextLimit)`. An
      agent with `progress: 7` and `contextLimit: undefined` therefore still renders coral (87.5 >=
      85), exactly as it does today via `progress >= 7`; a tokens/limit-fed call would have rendered
      the neutral accent and silently deleted the warning for every inferred-limit agent.
    - `AgentCard.tsx`'s 0..8 gauge geometry is UNCHANGED (still `Math.min(8, Math.max(0,
      progress))/8*100` for the bar fill), but its COLOUR now derives from `agentView.ts`'s shared
      threshold applied to the equivalent percentage, not from the `progress >= 7`/`>= 6` integer
      steps.
    - `test/repo-claims.test.cjs` gains a positive-lower-bound pin: each of the three files calls the
      shared derivation `>= 1` time (count-based, comment-stripped source, matching this file's
      existing pin style) — so deleting the shared call and reverting to a local re-derivation fails
      this test rather than silently reintroducing drift.
  </behavior>
  <action>
    In each of the three files, replace the local threshold expression with a call to `agentView.ts`'s
    `deriveContextColor` (import it, do not re-declare 85/65 locally anywhere). `AgentCard.tsx` keeps
    its `pct`/bar-fill calculation exactly as-is (D-34: the card's geometry does not change) and only
    swaps the COLOUR step's source — concretely, `AgentCard.tsx:165-167`'s
    `const gaugeColor = progress >= 7 ? … : progress >= 6 ? … : …` becomes
    `const gaugeColor = deriveContextColor(pct, accent);` reusing the `pct` already computed at
    `:164`, NOT `(contextTokens, contextLimit)` — round-3 finding #28. Leave
    `gaugeTitle` (`:168-170`) alone; it is the only place `contextLimit` belongs in this file.
    Add the `test/repo-claims.test.cjs` pin per `<behavior>` — follow
    this file's existing pin construction (comment-stripped source, `>= 1` count per file, never a
    bare `=== 0`). Add ONE case to `test/renderer-components.test.cjs` (whose `Module._load` shim
    already loads `AgentCard.tsx`, verified this session at `:97-124`): `renderToStaticMarkup` an
    `AgentCard` with `progress: 7` and `contextLimit: undefined` and assert the gauge's inline style
    still carries `var(--cth-coral)` — the compaction-warning regression test for finding #28.
  </action>
  <acceptance_criteria>
    - `grep -c -F "deriveContextColor"` run SEPARATELY on each of
      `src/renderer/src/components/AgentCard.tsx`,
      `src/renderer/src/components/FullscreenTerminal.tsx` and
      `src/renderer/src/components/CommandCenterPanel.tsx` is `>= 1` for EACH — per file, not a
      `>= 3` total across all three, which three occurrences in one file would satisfy while two
      files still re-derived locally
    - `grep -c "cpct >= 88" src/renderer/src/components/CommandCenterPanel.tsx` matches exactly `0`
      (the drifted pair is gone; measured `1` today, so this is red before the work)
    - `grep -c "progress >= 7 ? 'var(--cth-coral)'" src/renderer/src/components/AgentCard.tsx`
      matches exactly `0` (measured `1` today)
    - **The three-file shared-derivation pin actually EXISTS in the test file** (round-3 finding #12
      — until now the pin was mandated only in `<behavior>`/`<action>` prose, and its sole criterion
      was `node --test test/repo-claims.test.cjs`, which is green today, before any work, so nothing
      failed if the pin was never written): `grep -c "deriveContextColor" test/repo-claims.test.cjs`
      matches `>= 1` — measured `0` this session, so this criterion is red until the pin lands. Read
      the added clause to confirm it counts the shared call per file rather than asserting the
      derivation module merely exists.
    - `node --test test/repo-claims.test.cjs test/renderer-components.test.cjs` passes with 0
      failures, INCLUDING the new `progress: 7` / `contextLimit: undefined` AgentCard case that
      asserts `var(--cth-coral)` still renders (round-3 #28's compaction-warning regression test)
    - **EXISTENCE PIN:** `grep -c -F "contextLimit: undefined" test/renderer-components.test.cjs` is
      `>= 1` — measured **0** today, and that file already passes 7/7 before any Phase-3 work, so the
      run above cannot prove the case was written. This is the regression round-3 #28 raised: an agent
      at 87.5% context pressure silently losing its coral compaction warning after the threshold
      rewire. The one test that would catch it must be provably present, not assumed.
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/renderer-components.test.cjs</automated>
  </verify>
  <done>All three renderings call the one shared 85/65 threshold; the drifted 88/75 and 87.5/75 pairs are gone and pinned against reintroduction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: the consolidated five-field card in AgentDetailPanel, and the god-coverage decision — with honest test coverage</name>
  <files>src/renderer/src/components/AgentDetailPanel.tsx, test/renderer-components.test.cjs</files>
  <read_first>.planning/phases/03-scale-and-observability/03-UI-SPEC.md §S2a, §S2b, §S2d (full), src/renderer/src/components/AgentDetailPanel.tsx (full — the exact insertion point between the openTerminalError strip and BlockedBanner), test/renderer-components.test.cjs:97-124 (the Module._load shim's CURRENT coverage — PixelBadge/BlockedBanner/AgentCard/store.ts/agentProvider.ts — AgentDetailPanel.tsx is NOT in it today)</read_first>
  <behavior>
    - The stat card renders 5 cells (`cost · up · context · account · state`) in a
      `grid-template-columns: repeat(auto-fit, minmax(120px, 1fr))` container, all 5 values sourced
      from `agentView.ts` — none computed inline in this component.
    - A `costTracking: 'none'` agent's cost cell renders `no cost meter` with NO `$` character
      anywhere in its rendered text, `title` set to the reused gap sentence.
    - A `deriveCost` result with `lifetime: true` (round-2 finding #23 — a transcript-fallback-sourced
      all-time cumulative total, never a per-session number) renders its dollar figure with a visibly
      distinct qualifier — e.g. `$1.23 (lifetime)` — and a `title` explaining it is the agent's
      all-time total, not spend since this spawn; a `lifetime: false` result renders the plain `$X`
      form unchanged.
    - An agent with no `spawnedAt` renders `not recorded` in the `up` cell, never `0s`.
    - **An agent missing `contextTokens` or `contextLimit` renders `not reported` in the `context`
      cell, never `0%` and never an empty rail** (round-3 finding #27 — UI-SPEC §S2b:694 mandates
      this branch and, before this revision, no task, action or criterion in this plan implemented
      it, so the `context` cell was the one of the five fields with no declared-gap path at all).
      The cell reads Task 1's `deriveContext(contextTokens, contextLimit)`: `{kind:'measured'}`
      renders `{k} / {k} ({pct}%)` plus the 4px rail, `{kind:'unmeasured'}` renders the literal
      `not reported` in `--cth-ink-500` with no rail. Note the two gap strings are DIFFERENT and both
      are load-bearing: `not recorded` is the `up` cell's (no `spawnedAt`), `not reported` is the
      `context` cell's (no token/limit pair) — UI-SPEC uses each exactly once and they are not
      interchangeable.
    - An agent whose breaker snapshot has not yet resolved renders `unknown` in the `state` cell,
      never `healthy`.
    - `AgentCard.tsx`'s `width`/`height` constants (322/86) are byte-unchanged by this task (grep
      confirms — this task touches ONLY `AgentDetailPanel.tsx`).
    - The new test case for this component states, in a code comment, exactly what its assertions
      prove — either a real `renderToStaticMarkup` pass over `AgentDetailPanel.tsx` (if the import
      graph is tractable under an extended `Module._load` shim) or an explicit acknowledgment that
      the checks are string-presence-only over the source file, so a future reader never mistakes a
      grep-backed check for render-proven coverage.
  </behavior>
  <action>
    Insert the stat card into `AgentDetailPanel.tsx` at the exact point specified in `<interfaces>`
    (between the `openTerminalError` strip and `BlockedBanner`), per UI-SPEC S2a/S2b's full layout
    and field table. For the god-coverage question (S2d): do NOT silently mount this card above
    `CommandCenterPanel`'s tab strip for the god (costing ~62px of the docked rail), and do NOT
    silently omit any mention of the gap. Instead, add a short code comment at the point where
    `AgentDetailPanel.tsx:69` returns `<CommandCenterPanel agent={agent} />` for `agent.isGod`,
    stating explicitly that the god does not receive this consolidated card and that
    `CommandCenterPanel`'s floor tab already shows cost/context/breaker per roster row but neither
    duration nor account — and record the SAME statement in this plan's own SUMMARY.md as a named,
    explicit residual (never implied as delivered). For test coverage: FIRST attempt to extend
    `test/renderer-components.test.cjs`'s existing `Module._load` shim to also load
    `AgentDetailPanel.tsx` (following the same stub pattern already used for `AgentCard.tsx`) and
    assert every declared-gap branch in `<behavior>` on real `renderToStaticMarkup` output — the
    `context` cell's `not reported` case (round-3 #27) is not optional and needs its own fixture: an
    agent with `contextTokens: undefined` and `contextLimit: undefined` renders `not reported` and
    contains no `%` character in that cell. If `AgentDetailPanel.tsx`'s
    import graph proves genuinely unloadable under that shim (materially larger than the four modules
    it currently covers), do NOT silently fall back to a bare grep with no acknowledgment — add the
    explicit code comment named in `<behavior>` stating the assertions below are string-presence
    checks over the source file only, and do NOT claim `renderToStaticMarkup` coverage in this plan's
    SUMMARY.md if that fallback was taken.
  </action>
  <acceptance_criteria>
    - **EXISTENCE PINS for the five declared-gap render cases** (round-8 finding #9 — "read the added
      test code" is not a criterion, and `test/renderer-components.test.cjs` is green today so the run
      alone proves nothing). All four measured **0** right now and must reach `>= 1`:
      `grep -c -F "AgentDetailPanel" test/renderer-components.test.cjs`,
      `grep -c -F "not reported" test/renderer-components.test.cjs`,
      `grep -c -F "not recorded" test/renderer-components.test.cjs`, and
      `grep -c -F "no cost meter" test/renderer-components.test.cjs`.
    - **T-03-08b's claimed mitigation, made real** (round-8 finding #10 — the threat row asserts "no
      `$` character in the unmeasured render" and no criterion implemented it): a
      `renderToStaticMarkup` case rendering an agent whose `deriveCost` returns
      `{kind:'unmeasured'}` and asserting the produced markup for that cell contains **no `$`**. Pin
      it with `grep -c -F "kind: 'unmeasured'" test/renderer-components.test.cjs` `>= 1` (measured 0).
      If the render fallback described in `<action>` is taken instead, T-03-08b must be downgraded to
      `accept` with the honest residual — matching the `T-03-05d` correction pattern.
    - `grep -n "no cost meter" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1`
    - `grep -n "lifetime" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1` (round-2
      #23 — the lifetime-cost qualifier renders, never a bare unlabeled dollar figure)
    - `grep -c "not recorded" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1`
      (the `up` cell's gap string; measured `0` today)
    - `grep -c "not reported" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1`
      (round-3 finding #27 — the `context` cell's gap string, a DIFFERENT string from `not recorded`
      above; measured `0` today, so both are real gates and neither can satisfy the other)
    - `grep -c -F "deriveContext(" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1`
      (the cell reads Task 1's fifth derivation rather than computing a percentage inline; the
      trailing `(` stops `deriveContextColor` from satisfying it). Measured `0` today.
    - `grep -c "'unknown'" src/renderer/src/components/AgentDetailPanel.tsx` matches `>= 1` —
      measured `0` today; the file's one existing `unknown` is `'unknown error'` at `:81`, which this
      quoted pattern does not match, so the criterion is genuinely red before the work
    - **AgentCard's box is untouched, as runnable counts** (round-3 finding #36 — the old form ended
      at "unchanged from before this task" with no number and no command, and used a quoted `\|`,
      which is BRE alternation, so one of the two halves matching was enough). Measured this session:
      `grep -c -F "const width = 322" src/renderer/src/components/AgentCard.tsx` is `2` (the prose
      reference at `:80` and the real constant at `:193`) and
      `grep -c -F "const height = 86" src/renderer/src/components/AgentCard.tsx` is `1` (`:198`).
      Both counts must still read `2` and `1` after this task. AND — recording `$T3BASE=$(git
      rev-parse HEAD)` BEFORE Task 3's first edit — the commit-scoped pair below, which is the
      plan-id-scoped form for the same reason Task 1's caller pin is (`use_worktrees: false`, 03-06
      is also `wave: 5`, so `HEAD` may be a sibling's commit and a bare `git show --name-only HEAD`
      would measure 03-06's diff, passing the negative half for the wrong reason):
      `git log --oneline --grep="03-08" $T3BASE..HEAD -- src/renderer/src/components/AgentDetailPanel.tsx | wc -l`
      is `>= 1` (the positive half — Task 3 really did commit the file it owns), AND
      `git log --oneline --grep="03-08" $T3BASE..HEAD -- src/renderer/src/components/AgentCard.tsx | wc -l`
      is exactly `0` (no Task-3-era commit of this plan touches `AgentCard.tsx`; Task 2 edited it in
      an EARLIER commit, which the `$T3BASE..HEAD` range correctly excludes, and `--grep="03-08"`
      keeps 03-06's concurrent wave-5 commits out of both halves)
    - `grep -n "isGod" src/renderer/src/components/AgentDetailPanel.tsx -A 3` shows the new comment naming the god-coverage gap
    - The new test case in `test/renderer-components.test.cjs` for `AgentDetailPanel.tsx` carries a
      code comment stating whether it is a real component load or a string-presence-only check (read
      the added test code to confirm — this is not a pure grep-count criterion)
    - `node --test test/renderer-components.test.cjs` passes with 0 failures
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-components.test.cjs</automated>
  </verify>
  <done>The consolidated card renders all 5 fields sourced from agentView.ts with correct declared-gap branches; AgentCard's box is untouched; the god's non-coverage is a stated, explicit residual; the new test's actual coverage (real render vs. string presence) is stated honestly, not implied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: live-measure the consolidated card at sidebarWidth 320 / 420 / 900</name>
  <what-built>The consolidated 5-field stat card mounted in AgentDetailPanel.tsx, using an
  `auto-fit`/`minmax(120px, 1fr)` grid intended to reflow without a fixed width.</what-built>
  <how-to-verify>
    **Why the probe widths are `sidebarWidth`, not viewport widths** (round-3 finding #29 — as
    written before this revision, this blocking gate could not fail). `AgentDetailPanel` is rendered
    inside the sidebar container, whose width is the store's `sidebarWidth`, not the window's:
    verified this session at `App.tsx:662` (`width={sidebarWidth}`) and `App.tsx:670`
    (`width: sidebarWidth, flexShrink: 0`). `sidebarWidth` is read from `localStorage` and clamped to
    `[320, 1200]` with a default of `420` (`store.ts:653-659`, `store.ts:960-964`) and does NOT track
    the viewport. So probing 1280/1024/800 logical widths measures ONE container width three times
    and reports a pass no matter what the grid does. The probe must move the splitter instead.

    1. Run the dev app and select a non-god agent with a live PTY.
    2. Drag the sidebar splitter to `sidebarWidth` = **320** (the clamp floor), **420** (the default)
       and **900** (a wide rail). Read the value back from
       `window.localStorage.getItem('cth.sidebarWidth')` at each stop and RECORD it beside the
       observed result — a step whose recorded width is not the requested one did not happen.
    3. At each of the three widths, record the OBSERVED COLUMN COUNT of the stat card and confirm all
       5 cells render with no clipped label and no cell collapsing to zero width. At 320 the
       `minmax(120px, 1fr)` grid must give 2 columns; at 900 it must give 5. A recorded column count
       identical at all three widths means the grid is not reflowing and this gate FAILS.
    4. Confirm the reflow described in UI-SPEC S2a (five-across when wide, three-then-two in the
       docked rail) actually happens rather than a fixed 5-column row overflowing.
    5. Confirm the `BlockedBanner` (trigger one if possible) still renders directly above
       `SidebarTabs`, unmoved by the new card's insertion.
    6. **Base-sha / head-sha, per 03-UI-SPEC.md:327-335's binding Containment Protocol.** Run the
       IDENTICAL probe against the base sha and the head sha and report both, using
       `Emulation.setDeviceMetricsOverride` for the window size (`page.setViewportSize`,
       `win.setBounds` and `win.setContentSize` all leave `window.innerWidth` pinned at 1280 in
       Electron, per 01-15) and printing the true `window.innerWidth` beside the requested one. A
       head-only measurement cannot distinguish "this phase caused it" from "it was already broken",
       and the card does not exist at the base sha, so the base-sha run's job is to record the
       CONTAINER's pre-existing behaviour (the panel's width at each `sidebarWidth`, and that
       `BlockedBanner` already sat directly above `SidebarTabs`) as the control.
    7. Report a positive control at every stop: the stat-card element was FOUND and MOUNTED. A clean
       scan of an unmounted component is worth nothing (UI-SPEC:333).
  </how-to-verify>
  <files>(none -- operator verification of the running app)</files>
  <action>Pause execution and present the numbered verification steps above to the operator verbatim. Do not write the SUMMARY until the operator answer is returned. Record the three measured `sidebarWidth` values, the three observed column counts, and both shas in the SUMMARY — a SUMMARY that reports "approved" with no numbers does not close this gate.</action>
  <verify><human-check>Operator confirms, with the recorded sidebarWidth read back from localStorage and the observed column count at each of 320 / 420 / 900, that all 5 stat-card cells render un-clipped and that the column count actually CHANGES across the three widths; that the positive control (element found + mounted) held at every stop; and that the BlockedBanner stays directly above SidebarTabs at both the base and head shas.</human-check></verify>
  <done>The operator has live-measured the consolidated card at three real container widths, base sha and head sha, and the recorded widths, column counts and shas are in the SUMMARY.</done>
  <resume-signal>Type "approved" plus the three width/column-count pairs, or report the exact width and cell that broke.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| main (`control:breakerSnapshot`, `hive:agentDirectory`) <-> `agentView.ts` | Read-only; all fields already PII-free per the existing `hive:agentDirectory` contract (landed 03-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-03-08a | Information Disclosure | cost/context/account fields rendered on the card | accept | Same fields already rendered elsewhere in the app (AgentCard's own gauge, the account `<select>`); no new data class is exposed |
| T-03-08b | Spoofing | a `$0.00` misread as "measured, cheap" for an unmeasured engine | mitigate | The discriminated `{kind:'unmeasured', reason}` branch is enforced by this plan's own acceptance criteria (no `$` character in the unmeasured render) |
| T-03-08c | Repudiation | a stale "healthy" breaker read after reload | mitigate | `state` reads `'unknown'` until the pull resolves, closing the fail-unsafe window D-36 named |
</threat_model>

<verification>
- `node --test test/renderer-runstate.test.cjs test/repo-claims.test.cjs test/renderer-components.test.cjs` — 0 failures
- `npm run typecheck` — 0 errors
- Task 4 checkpoint approved
</verification>

<success_criteria>
- `agentView.ts` is the ONE source of cost/duration/context/account/state for all consuming
  renderings, following `autoMode.ts`'s proven pattern
- The 85/65 threshold is shared everywhere; the two drifted pairs are gone and pinned
- The four `useFleetTelemetry` mounts share one subscription with zero required caller-side edits
- The consolidated card lands in `AgentDetailPanel.tsx` with a declared-gap branch on ALL FIVE cells
  — including the `context` cell's `not reported` (round-3 #27), which had no implementation before
  this revision; `AgentCard`'s box is untouched; the god's non-coverage is explicit
- Task 3's test coverage of `AgentDetailPanel.tsx` states honestly whether it is a real render pass
  or a string-presence check, never implying more than it proves
- Task 4's containment gate is measured against `sidebarWidth` (320 / 420 / 900) — the container that
  actually drives this card — at base sha and head sha, with the observed column count recorded at
  each stop, so the gate can fail (round-3 #29)
</success_criteria>

<output>
Create `.planning/phases/03-scale-and-observability/03-08-SUMMARY.md` when done
</output>
