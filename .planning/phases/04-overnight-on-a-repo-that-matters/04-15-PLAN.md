---
phase: 04-overnight-on-a-repo-that-matters
plan: 15
type: execute
wave: 4
depends_on: ["04-02", "04-06"]
files_modified:
  - src/main/approvals.ts
  - src/main/hooks.ts
  - src/main/hiveProvisioning.ts
  - test/control.test.cjs
  - test/record-persist.test.cjs
autonomous: true
requirements: [GATE-05, RECORD-01]
user_setup: []

must_haves:
  decisions:
    # `D-NN:` citations for GSD's blocking decision-coverage gate, which searches only the
    # frontmatter `must_haves` block. Each names the task or criterion in THIS plan.
    - "D-08: the two-phase wait is built here — main answers synchronously, and only an `ask` reply carries `hive_ask.deadlineMs`/`pollMs`; allow and deny keep today's 5 s budget untouched."
    - "D-10: pending tool approvals live in an in-memory `ApprovalRegistry` merged into `openAsks()` behind a `kind` discriminator; nothing is written to `tasks.json`."
    - "D-17: RECORD-01's writer stays off the telemetry path — `SPAN_RING_CAP = 200` and the span ring are untouched by this plan, asserted by an unchanged `writePty` count and an empty `control.ts` diff."
    - "D-34: GATE-05 ships its own ceiling as items (a)-(h), continuing `hooks.ts:801-859`'s lettering after plan 04-06's (j)-(v)."
    - "D-01: GATE-03 and GATE-05 are one seam — this plan owns `hooks.ts`'s PreToolUse arm in wave 5, plan 04-06 owned it in wave 2, and `control.ts`'s `toolDecision` is widened by neither (`git diff --stat src/main/control.ts` is empty)."
  truths:
    - "The third answer is authored by `commandShapeDenial` (plan 04-06), NOT by widening `toolDecision`. `handle` stays SYNCHRONOUS — making it async would move every side effect in a 30-return-point function from same-tick to microtask, on every hook event on the floor, to serve one branch."
    - "A real `git push origin +main` driven through the real `HookServer` produces a `hive_ask` — not a bare deny. Without that assertion every GATE-05 surface in the phase can ship green with zero commands ever routed to ask."
    - "The ask TTL is DERIVED from `min(every PreToolUse hook timeout this app writes)`, in the same file, so the two cannot drift and the SHORTEST bound governs. At 120 s against grok's 5 s event-aware default the shim is KILLED before it can answer, and `hiveProvisioning.ts:214-219` records what a killed hook is: logged as FAILED, no stdout — and no stdout is ALLOW."
    - "**Claude leaves the `unreconciled` row.** Claude Code is the only engine that certainly runs on this machine and the one every live GATE-03/GATE-05 observation in the phase is gathered on, and `hiveProvisioning.ts:456-459`'s `entry()` helper writes no `timeout` key at all — so on Claude, GATE-05's `times out to deny` is still `times out to allow`. Either `entry()` learns the parameter and PreToolUse gets the constant, or Claude's real default is measured into a ceiling item and `ASK_TTL_MS` is re-derived from the minimum including it. The word `unreconciled` does not survive into a shipped decision table."
    - "**The TTL assertion carries a LOWER bound as well as an upper one.** `ASK_TTL_MS <= MIN_PRETOOLUSE_SEC * 1000` alone is satisfied by EVERY value, because the TTL is derived from that bound — `SEC = 5` yields `TTL = -25000`, every ask expired at birth, and `-25000 <= 5000` passes. The assertion is three-part and the test cannot pass on a degenerate constant."
    - "**A shim may enter the poll loop only on an engine whose PreToolUse timeout this app itself writes and can therefore bound.** grok's is its own ~5 s event-aware default with an unverified unit; agy's is `timeout: 0` with unknown semantics; neither CLI is installed. Guessing either would build a fail-OPEN inside the feature that exists to fail closed — so for both, the ask reply's deny stands, which is what the dual-reading object already is, and both are named ceiling items."
    - "An `ApprovalPoll` is bound to the agent that OWNS the ask, not merely to a valid token. `authorized()` derives an id from any valid token, so without an owner check agent B can poll — and settle — agent A's ask."
    - "**`ASK_TTL_MS` reaches the production registry, and one place owns saying so.** `HookServer` constructs the `ApprovalRegistry` itself, so `boot.ts` never does — which means a `ttlMs` constructor option nobody passes defaults to whatever the class was written with. `HookServer` imports `ASK_TTL_MS` in this plan's task 2, in the same commit that declares it, and plan 04-20 asserts the EFFECT on a really-booted floor: the production entry's `expiresAt - openedAt` equals `ASK_TTL_MS` read from source."
    - "The ask reply is simultaneously a valid DENY for an old shim and a pending handle for a new one, so an un-upgraded shim fails closed with no version negotiation."
    - "Ask ids are unguessable (`node:crypto`), single-use, and an expired ask cannot be answered."
    - "The server-side TTL is <= the deadline handed to the shim, so an operator's late yes can never answer a question whose asker already denied and moved on."
    - "Every tool call is persisted with agent, timestamp, tool and target from the HOOK SOCKET — where `tool_name` and `tool_input` already arrive and `authorized()` has already derived the agent id from a per-agent token."
  artifacts:
    - path: "src/main/approvals.ts"
      provides: "the in-memory pending-approval registry with TTL, electron-free"
      exports: ["ApprovalRegistry"]
    - path: "src/main/hiveProvisioning.ts"
      provides: "one PRETOOLUSE_HOOK_TIMEOUT_SEC constant driving codex/kimi/grok's PreToolUse hook timeout, and the ASK_TTL_MS derived from it"
      exports: ["PRETOOLUSE_HOOK_TIMEOUT_SEC", "ASK_TTL_MS"]
    - path: "src/main/hooks.ts"
      provides: "the ask reply, the ApprovalPoll branch, the RECORD-01 writer, and the GATE-05 ceiling"
      contains: "hive_ask"
  key_links:
    - from: "HookServer.handle PreToolUse"
      to: "ApprovalRegistry.open"
      via: "toolDecision's third answer, replied synchronously as a deny+hive_ask object"
      pattern: "hive_ask"
    - from: "HookServer.handle"
      to: "PersistStore.recordToolCall"
      via: "a best-effort injected writer, agent+ts+tool+target+decision"
      pattern: "recordToolCall"
    - from: "ApprovalRegistry"
      to: "openAsks() / notify / push"
      via: "an injected publisher, consumed by plan 04-17"
      pattern: "publish|openApprovals"
---

<objective>
GATE-05's main-side half — the third answer, its registry, its bounded wait — plus RECORD-01's writer,
which belongs on this same seam and in this same file.

**Purpose — D-01, and why these are one plan.** `ControlRegistry.toolDecision(id, tool)`
(`src/main/control.ts:97`) is the only per-tool-call verdict in main. It is **synchronous**, it returns
`{ deny: boolean; reason?: string }`, and it inspects only `paused` and `gatedTools` — it never sees the
command string. GATE-03 (plan 04-06) taught main to judge the string; GATE-05 needs that judge to have a
third answer. S-1: *"GATE-05's third answer is a third return value from the judge GATE-03 builds; there
is nowhere for 'ask' to hang until 'deny for this shape' exists."*

**Pattern 3 is the design, and it is decided by a measurement, not by taste.** RESEARCH § L-02:
`GROK_HOOK_SHIM` runs `JSON.parse(resp || '{}')` over the **whole accumulated buffer**
(`hiveTemplates.ts:772`); on a parse failure it sets `out = null` and `process.exit(0)` **with no
stdout — which is allow**. `AGY_HOOK_SHIM` has the same shape. Two JSON objects concatenated on one
connection is a parse failure. **So a held-open connection with a "pending" preamble is a silent
fail-OPEN on two of the seven engines.** It would pass every Claude test. That rules out the streaming
form regardless of how attractive it looks, and it is why the shims **poll** (plan 04-16).

**The reply shape that makes this work without version negotiation.** One object, two readings: an old
shim sees only `hookSpecificOutput` and denies; a new shim sees `hive_ask` and polls instead. Old shim →
**deny**. Fail-closed on the correct side, for free.

**D-08's three clauses, all of which this plan must hold simultaneously:**
1. allow and deny behave exactly as today; the shim's 5 s budget is untouched for them;
2. only on **ask** does a longer wait begin, defaulting to **deny** on expiry;
3. **a socket that is gone, refuses, or answers nothing still exits 0 = allow.** Fail-open is preserved
   for *"the floor is not running"*; fail-closed is introduced only for *"the floor asked and the operator
   did not answer"*. Those are different failures and must not share a default. The connect-error path is
   reached **before** any `hive_ask` is ever seen, so clause 3 holds by construction.

**RECORD-01 rides here, and the reason is measured.** RESEARCH § Pattern 4 / L-05 killed D-17's literal
reading: `ToolSpan` has no `target` field, `ATTR_ALLOWLIST` admits no path or command key, and the whole
OTel path is `if (claudeProvider && …)`. The data lives on the hook socket — `HookPayload`
(`hooks.ts:86-116`) carries `tool_name` and `tool_input`, `protectedPathDenial` already reads `file_path`,
`path`, `notebook_path` and `command` out of it at `:865-889`, and `agentId` comes from `authorized()`
(`:626`), i.e. **GATE-01's guarantee is already applied before the writer sees anything.** `hooks.ts` has
one owner per wave and this plan is it; putting the writer anywhere else would take a fifth wave.

**Output.** `approvals.ts`, `control.ts`'s third answer, `hooks.ts`'s ask reply + poll branch + record
writer + GATE-05 ceiling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-UI-SPEC.md
@docs/adr/0001-one-gate-for-pty-writes.md
</context>

<interfaces>
The contracts plans 04-16 (the shim poll) and 04-17 (the operator surfaces) are written against. Ship
these names.

```ts
// src/main/approvals.ts — electron-free.

export interface PendingApproval {
  id: string;            // `ask-<hex>` from node:crypto. MUST satisfy PHONE_TASK_ID_RE
                         // (/^[A-Za-z0-9._-]{1,128}$/, webhook.ts:231, enforced at :670).
  agentId: string;
  tool: string;
  command: string;       // agent-authored, untrusted, capped
  reason: string;        // why an ask rather than an allow — main's sentence
  openedAt: number;
  expiresAt: number;     // server-side TTL. MUST be <= the deadlineMs handed to the shim.
}

export class ApprovalRegistry {
  open(a: Omit<PendingApproval,'id'|'openedAt'|'expiresAt'>): PendingApproval;
  /** 'pending' | 'allow' | 'deny'. Unknown or expired id => 'deny'. Single-use. */
  poll(id: string): 'pending' | 'allow' | 'deny';
  /** Returns false when the id is unknown, already settled, or expired. */
  answer(id: string, approved: boolean): boolean;
  list(): PendingApproval[];      // plan 04-17 merges this into openAsks()
  sweep(now: number): void;       // expire, settle to deny, notify
}
```

```ts
// The PreToolUse ask reply, returned SYNCHRONOUSLY from hooks.ts.
// One object, two readings — RESEARCH § Pattern 3.
{
  hookSpecificOutput: {
    hookEventName: 'PreToolUse',
    permissionDecision: 'deny',
    permissionDecisionReason:
      'Approval required and this shim cannot wait for one — denied. ' +
      'Re-run after the floor upgrades its shims (ensureHive rewrites them on boot).'
  },
  hive_ask: { id: '<askId>', deadlineMs: <epoch>, pollMs: 1000 }
}
```

```ts
// The poll event a new shim posts. A FRESH SHORT CONNECTION per poll — never a held one.
{ hook_event_name: 'ApprovalPoll', ask_id: '<askId>', sock_token: '<per-agent token>' }
// Server answers { status: 'pending' } (loop again) or the final allow/deny.
```
</interfaces>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. Ask ids come from `node:crypto`, which
  `push.ts` already uses for dependency-free VAPID. No install step.
- **D-35 — `src/main/hooks.ts` has ONE owner per wave and this plan is it for wave 4.** Plan 04-06 owned
  it in wave 2. The Collision Map scores it three-way and **not splittable**.
- **D-35** — this plan does **not** touch `src/main/hiveTemplates.ts` (plan 04-16, wave 5),
  `src/main/webhook.ts`, `src/main/index.ts`, `src/main/push.ts` or `resources/phone/**` (plan 04-17,
  wave 5), or any renderer file (plan 04-18, wave 6). **It does not touch `src/main/floor/boot.ts`
  either** — plan 04-20 wires this plan's three seams at the production composition root in wave 5, with
  a test that boots a real floor and asserts them.
- **D-35 — `src/main/control.ts` is NOT in this plan.** The first draft widened `toolDecision` to express
  `ask`; the revision removed that. `commandShapeDenial` (plan 04-06) is the only thing in main that sees
  a command string, it runs **before** the `toolDecision` guard at `hooks.ts:1387`, and it already returns
  a `{kind: 'deny'|'ask', reason}` verdict. Widening `toolDecision` as well would (a) be unreachable for
  every shape this gate cares about and (b) turn the operator's race-free immediate tool-gate deny into a
  120 s wait — a fail-direction regression for a feature nobody asked to change. `git diff --stat
  src/main/control.ts` is asserted empty.
- **D-35 — `src/main/hiveProvisioning.ts` is this plan's in wave 4.** It is in no other plan's
  `files_modified` in any wave (plans 04-05, 04-06 and 04-10 cite it in prose only). It is deliberately
  electron-free (its own header says so) and `test/engine-parity.test.cjs:36` already loads it through
  `test/load-ts.cjs`.
- **ADR-0001 — exactly one place types into a live PTY.** GATE-05's approval must **not** become a second
  typer. The answer rides the hook return, exactly as `control.ts`'s own header describes. If any part of
  this plan reaches for `writePty`, it is wrong.
- **`handle` stays synchronous.** It has ~30 return points across `hooks.ts:1188-1516`; making it `async`
  changes the ordering of every side effect in it (`emit`, `emitControl`, `recordSession`, breaker feeds)
  from same-tick to microtask, on every hook event on the floor, to serve one branch.
- **D-34** — GATE-05 ships its own ceiling list.
- **D-33 / D-40** — assert a positive lower bound alongside every negative.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<planner_decisions>
The two numbers CONTEXT.md explicitly left open (D-08: *"the two-phase shape is locked, the numbers are
not"*), decided here and reconciled against each other as D-08 requires:

**There is a FOURTH deadline, it is the outermost one, and the first draft did not name it.** D-08's own
words are *"a plan that changes one number without the other ships a gate that times out on the wrong
side."* There are four numbers, not three. The engine's own PreToolUse hook timeout is the outermost bound,
and every value in the repo today is shorter than a 120 s ask:

| Engine | PreToolUse hook timeout today | Source | vs a 120 s TTL | Round-3 disposition |
|---|---|---|---|---|
| **Claude** | **no `timeout` key is written at all** — `entry()` (`hiveProvisioning.ts:456-459`) emits `{ ...(matcher ? {matcher} : {}), hooks: [{ type:'command', command: cmd }] }`, with no `timeout` in any branch. `hookSettings` is Claude-only (`:457`) and `PreToolUse: [entry('*')]` is at `:486` | `hiveProvisioning.ts:456-459`, `:486` | Claude Code's documented default is **60 s**, i.e. **2x short** | **RECONCILED — path A or path B below. This row was `unreconciled` in the previous draft and that is the single most consequential omission round 2 found**: Claude is the one engine that certainly runs on this machine, every live GATE-03/GATE-05 observation in the phase is gathered on it, and the relationship test could not catch it because it reads a constant Claude never consumed |
| codex | **30 s** | `hiveProvisioning.ts:232` (`timeout = 30`, seconds — see the long note at `:210-223`) | 4x short | raised to the constant for PreToolUse only |
| kimi | **30 s** | `hiveProvisioning.ts:293` (`timeout = 30`) | 4x short | raised to the constant for PreToolUse only |
| grok | **~5 s** — no `timeout` key is written, so grok applies its event-aware default | `hiveProvisioning.ts:415-426` (`tool()` emits `hooks: [{ type:'command', command: nodeRun(shim) }]`, and the comment at `:424-425` says *"Let Grok apply its event-aware defaults (5s normally, 600s for Stop)"*) | 24x short | **UNCHANGED, and the shim does not poll on grok.** See "the two engines this plan refuses to guess at" below |
| agy | `timeout: 0` | `hiveProvisioning.ts:128` and `:131` | semantics unknown; agy is not installed | **UNCHANGED, and the shim does not poll on agy.** Same reason |

`hiveProvisioning.ts:214-219` records what happens when the bound is exceeded, in this repo's own words:
the hook is killed and logged as **failed**, not denied. **A killed shim writes no stdout, and no stdout is
ALLOW.** So GATE-05's "times out to deny" becomes "times out to allow" on the ordinary path, on every
engine, silently, with every test green.

**The fix, and it derives from the MINIMUM rather than from one engine, because the shortest bound is the
one that kills the shim first.** Introduce the constants in `hiveProvisioning.ts`, beside the values it
writes:

```ts
/** PreToolUse only. Every other event keeps its existing budget — widening them
 *  costs latency on paths that have no verdict to wait for. [ASSUMED]: no
 *  measurement in this session supports 150; it is the smallest round number that
 *  leaves ASK_TTL_MS a workable phone-approval window with margin. Verified in
 *  bounds for codex with no model spend — see the note at :222-223. */
export const PRETOOLUSE_HOOK_TIMEOUT_SEC = 150;

/** Claude Code's PreToolUse budget. Path A: this app writes it, and the value is
 *  PRETOOLUSE_HOOK_TIMEOUT_SEC. Path B: this app does not write it, and this is
 *  Claude's MEASURED default — see ceiling item (g), which names the version the
 *  measurement came from. Either way it is a NUMBER here, never a shrug. */
export const CLAUDE_PRETOOLUSE_TIMEOUT_SEC = <path A: PRETOOLUSE_HOOK_TIMEOUT_SEC | path B: the measured default>;

/** The smallest PreToolUse budget any POLLING engine runs under. grok and agy are
 *  absent from this minimum ON PURPOSE: their shims do not poll (see below), so
 *  their budgets cannot kill a waiting shim. */
export const MIN_PRETOOLUSE_SEC = Math.min(PRETOOLUSE_HOOK_TIMEOUT_SEC, CLAUDE_PRETOOLUSE_TIMEOUT_SEC);

/** The server-side ask TTL, DERIVED so the two cannot drift. The 30 s margin covers
 *  shim cold-start (hiveProvisioning.ts:216-218 measured 0.6-0.7 s under 8 concurrent
 *  spawns) plus the final poll round trip. */
export const ASK_TTL_MS = (MIN_PRETOOLUSE_SEC - 30) * 1000;
```

**Claude, path A vs path B — decide by a CHECK, not by a guess, and record which you took.**

- **Path A (preferred).** Teach `entry()` an optional seconds parameter and write
  `PreToolUse: [entry('*', PRETOOLUSE_HOOK_TIMEOUT_SEC)]` at `:486`; every other event keeps
  `entry()` with no timeout, exactly as today. **Take path A only after confirming the unit** against the
  installed `claude` — record the version from `claude --version` and where you read the unit (its own
  settings documentation or schema). This is the same discipline the plan applies to grok below; the
  difference is that Claude **is** installed here, so the check is a two-minute local one rather than an
  impossible one.
- **Path B (fallback, and it is not a failure).** If the unit cannot be confirmed from the installed
  binary, **write nothing into `hookSettings`**, record Claude's measured default in ceiling item (g) with
  the version it came from, and set `CLAUDE_PRETOOLUSE_TIMEOUT_SEC` to that measured number.
  `MIN_PRETOOLUSE_SEC` then collapses to it and `ASK_TTL_MS` shrinks accordingly — which is the correct
  outcome, because a TTL longer than Claude's budget is a TTL that fails open on Claude.

Under path B the TTL is smaller than 120 s. **That is fine and it must not be "fixed" by taking path A on
a guess.** The lower bound below is what keeps path B honest: if the measured default leaves less than
30 s to answer on a phone, that is a stop-and-report, not a number to shade.

**The three-part assertion, because the two-part one cannot fail (R2-H7).** The previous draft asserted
only `ASK_TTL_MS <= PRETOOLUSE_HOOK_TIMEOUT_SEC * 1000`. Since the TTL is *derived* from that bound, the
inequality holds for **every** value above 30: `SEC = 31` gives a 1-second window to answer on a phone, and
`SEC = 5` gives `TTL = -25000` — every ask expired at birth — and `-25000 <= 5000` passes. So the test
asserts all three, reading every value from `src/main/hiveProvisioning.ts`:

1. `MIN_PRETOOLUSE_SEC >= 60` — no engine in the minimum runs a budget shorter than a minute;
2. `ASK_TTL_MS >= 30_000` — a phone push plus a human tap needs at least thirty seconds;
3. `ASK_TTL_MS <= MIN_PRETOOLUSE_SEC * 1000` — the TTL fits inside the shortest engine budget.

Codex's own resolver defaults to 600 s when the key is omitted (`hiveProvisioning.ts:212`), so 150 is well
inside its bounds. **Verify it with the no-spend check this plan's own `read_first` already cites** — the
note at `:222-223`: `codex app-server` → `initialize` → `hooks/list` reports the normalized `timeoutSec`
per event. That is an acceptance criterion, not a suggestion; `150` is `[ASSUMED]` and the check costs
nothing.

**And the 150 s hazard the repo's own comment raises is real and is bounded, not dismissed (R2-H3).**
`hiveProvisioning.ts:210-223` chose 30 s because each hook cold-starts via hive-node — *"measured
0.08-0.16s idle but 0.6-0.7s under 8 concurrent spawns"* — and 30 s *"clears that by two orders of
magnitude while still capping a wedged shim."* At 150 s a shim wedged for a **non-ask** reason would stall
the agent five times longer per tool call. The bound that makes this safe is one this phase deliberately
does not touch: **the shim's own unconditional 5 s exit timer stays armed on the non-ask path** (plan
04-16, Pitfall 6), so a non-ask wedge still exits at ~5 s regardless of the engine budget. The engine
timeout only binds once a `hive_ask` has been seen and the boot timer has been cleared — which is exactly
the case it exists for. Write that reasoning into ceiling item (h) so a future reader does not read 150 as
a five-fold latency regression.

**Who passes `ttlMs`, settled in ONE place (R2-BL9).** `ApprovalRegistry` takes `ttlMs` as a constructor
option so it stays dependency-free and a test can drive a 50 ms ask. The previous draft said *"the
production value is `ASK_TTL_MS` and plan 04-20 passes it"* **while also** instructing that the registry be
constructed **inside `HookServer`** — so `boot.ts` never constructs it, plan 04-20 had no argument to pass,
and 04-20's must_have asserted a promise nothing in the phase kept. Settled: **`hooks.ts` imports
`ASK_TTL_MS` from `hiveProvisioning.ts` and passes it at the `new ApprovalRegistry(...)` site**, in task 2,
in the same commit that declares the constant — one file owns both halves in one wave, which is precisely
why the two live in one plan.

That import is a **constant**, not a collaborator, so it does not violate the injected-over-imported house
law; `hooks.ts` already imports `estimateCostUsd` (`pricing.ts`), `SPAWN_SAFE_SESSION_ID`
(`transcript.ts`) and `isClaudeProvider` (`agentProvider.ts`) the same way. No cycle: `hiveProvisioning.ts`
imports `hiveTemplates.ts` and nothing else in `src/main/`, and it imports `hooks.ts` not at all.

**Plan 04-20 asserts the EFFECT rather than the argument**: on a really-booted floor, a `PreToolUse` that
opens an ask produces an entry whose `expiresAt - openedAt === ASK_TTL_MS`, read from
`src/main/hiveProvisioning.ts` in the test. A constants-only comparison — which is all the relationship
test above does — says nothing about what the production registry was constructed with, and that gap is the
whole of R2-BL9.

| Bound | Today | Under GATE-05 | Why |
|---|---|---|---|
| **Engine PreToolUse hook timeout** | none written (Claude) / 5 s (grok) / 30 s (codex, kimi) / `0` (agy) | **`PRETOOLUSE_HOOK_TIMEOUT_SEC = 150` for codex's and kimi's PreToolUse, and Claude's under path A.** grok and agy unchanged — and their shims do not poll | The outermost bound. Below the TTL it kills the shim, which writes no stdout, which is ALLOW |
| **`MIN_PRETOOLUSE_SEC`** | — | `Math.min(PRETOOLUSE_HOOK_TIMEOUT_SEC, CLAUDE_PRETOOLUSE_TIMEOUT_SEC)` | The shortest budget a POLLING engine runs under. grok and agy are excluded because they do not poll, not because their budgets are comfortable |
| `HOOK_IDLE_MS` (`hooks.ts:446`) | 2 000 ms | **unchanged** | It bounds *framing*, not decisions; every poll is a fresh short connection well inside it |
| `HOOK_SHIM` exit timer (`hiveTemplates.ts:347`) | 5 000 ms → allow | **unchanged for the non-ask path**; armed per-poll-connection, and replaced by `deadlineMs` → **deny** once `hive_ask` is seen | The 3 s margin `hooks.ts:440-445` reasons about is preserved exactly |
| **Ask TTL (new)** | — | **`ASK_TTL_MS = (MIN_PRETOOLUSE_SEC - 30) * 1000`**, `[ASSUMED]`, **derived from the row above** | Long enough for a phone push to arrive and be tapped; short enough that a worker is not wedged for a coffee break. Derived rather than written as a literal so the two cannot drift — D-08's requirement, applied to the pair that actually binds. **Asserted with a lower bound as well as an upper one**, because a one-sided assertion on a derived value cannot fail. |
| **`deadlineMs` handed to the shim** | — | `openedAt + TTL` | **The server-side TTL must be ≤ this**, or an operator's late "yes" answers a question whose asker already denied and moved on |
| **`pollMs`** | — | **1 000 ms** | One connection per second per pending ask; there is at most a handful of those at a time |

Do not change one number without the other. A plan that adjusts the TTL and not the deadline ships a gate
that times out on the wrong side.

**The two engines this plan refuses to guess at — and the rule that makes refusing safe.**

The previous draft applied one standard to agy and the opposite standard to grok. It declined to touch
agy's `timeout: 0` because *"its semantics are unknown, agy is not installed, and a guess that made it
worse would be undetectable"* — and then wrote `timeout: PRETOOLUSE_HOOK_TIMEOUT_SEC` into grok's config,
where grok is **also** not installed, its unit (seconds vs milliseconds) is unverified, and
`hiveProvisioning.ts:424-425` deliberately omits the key. **If grok reads milliseconds, `150` means 150 ms:
every grok PreToolUse hook is killed before the shim can answer, GATE-03 and GATE-05 both fail open on
grok, and it is worse than today and undetectable.** Round 2 was right; agy's own reasoning is applied to
both.

**So: `hiveProvisioning.ts:415-426` (grok) and `:128`/`:131` (agy) are UNCHANGED by this plan.**
`git diff` is asserted to leave grok's `tool()` helper and `installAgyHooks` alone.

That leaves a question the previous draft never had to answer: **if grok's budget is ~5 s and the ask TTL
is two minutes, what happens when a grok agent hits an ask?** Under a poll loop the shim would be killed
mid-poll, write no stdout, and **allow** — a fail-open built inside the feature that exists to fail closed.
The rule that removes it costs no new code at all:

> **A shim may enter the poll loop only on an engine whose PreToolUse timeout this app itself writes.**

For every other engine the **ask reply's own deny stands** — the dual-reading object in `<interfaces>` is
already a valid `permissionDecision: 'deny'`, and `GROK_HOOK_SHIM`'s existing decoder at
`hiveTemplates.ts:772-784` already translates exactly that into `{decision:'deny'}`, with `AGY_HOOK_SHIM`
the same shape. **grok and agy therefore need no shim change whatsoever**, which is why plan 04-16 leaves
both byte-identical and drives grok's unchanged decoder through `test/gate-harness.cjs`'s `opts.shim`
selector as a **child process on this machine** — real local evidence where the previous draft had a marker
on unobserved new code.

Both engines go into GATE-05's ceiling as item (e), with what would settle each: an installed CLI plus one
hook fire. Do NOT add a `LIVE-UNVERIFIED` token in `src/` from this plan —
`test/repo-claims.test.cjs` is a four-way pin owned by plans 04-10 (wave 3), 04-13 (wave 4), 04-16
(wave 5) and 04-19 (wave 7), never this one, and the ceiling list is the D-34 deliverable where this
belongs.
</planner_decisions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: approvals.ts — the registry, its TTL, and its unguessable single-use ids</name>
  <files>src/main/approvals.ts, test/control.test.cjs</files>
  <behavior>
    - `open()` returns an id matching `/^ask-[0-9a-f]{16,}$/` which also satisfies `/^[A-Za-z0-9._-]{1,128}$/`.
    - Two `open()` calls never collide, and an id is not derivable from `openedAt` or the agent id.
    - `poll(unknownId)` → `'deny'`. `poll` after `answer(id,true)` → `'allow'`, then the id is settled and a second `poll` still reports the settled verdict but `answer` on it returns `false`.
    - `poll` past `expiresAt` → `'deny'`, even if `answer(id,true)` is called afterwards.
    - `sweep(now)` settles every expired entry to deny exactly once and publishes the change.
    - `list()` returns only unsettled, unexpired entries.
  </behavior>
  <read_first>
    - `src/main/webhook.ts:462-476` — `mintEnrollment` + `phoneArmed`: the in-repo primary analog for a
      minted, single-use, expiring credential. **Copy its discipline.**
    - `src/main/hooks.ts:526` and `:709-727` — the `tokens` Map and its mint/revoke, the secondary analog
    - `src/main/webhook.ts:231` — `PHONE_TASK_ID_RE = /^[A-Za-z0-9._-]{1,128}$/`, validated at **`:670`**
      **before** `answerAsk` is called. An ask id that fails this regex produces a 400 the operator reads
      as "the floor is broken". **Verify the id shape in a test, not by inspection** (04-UI-SPEC rule G-1).
    - `src/main/push.ts` — dependency-free `node:crypto` usage, and the injected-transport pattern
    - `src/main/floor/deps.ts:1-25` — injected dependencies over imports, house law
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Pattern 3, and § Security
      Domain's **V3 Session Management** row: *"Ask ids must be unguessable (`randomBytes`) and single-use;
      an expired ask must not be answerable."*
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § `src/main/approvals.ts`
    - `test/control.test.cjs` — the analog for testing a pure, electron-free main module with an injected
      clock; extend this file rather than adding a new one
  </read_first>
  <action>
    Create `src/main/approvals.ts` exporting `ApprovalRegistry` per the `<interfaces>` block. Electron-free.
    The clock and the publisher arrive injected (house law); nothing is imported from `webhook.ts`,
    `index.ts` or `push.ts`.

    Ids: `'ask-' + randomBytes(n).toString('hex')` from `node:crypto`. Choose `n` so the id is unguessable
    and still well inside `PHONE_TASK_ID_RE`'s 128-character bound; state the bit count in a comment.

    **Single-use and expiry are the security properties, not conveniences** (ASVS V3). `answer` on an
    already-settled or expired id returns `false` and changes nothing. `poll` on an unknown id returns
    `'deny'` — an unknown id is either an expired one or a forged one, and both must fail closed.

    `expiresAt = openedAt + ttlMs`, where `ttlMs` is a **constructor option** whose production value is
    `ASK_TTL_MS` from `hiveProvisioning.ts` (task 2 adds it). Injected rather than imported so
    `approvals.ts` keeps zero module dependencies and a test can drive a 50 ms ask instead of a two-minute
    one. The registry expires its own entries via `sweep`; it does not rely on a caller noticing — and
    **`sweep` needs a caller, which task 3's writer supplies** (see H-8 there): an entry that expires with
    nobody sweeping stays `pending` forever, so `tool_calls.decision` would hold `'ask'` for a call that
    was in fact denied, and the Repudiation mitigation RESEARCH names would not exist.

    `command` is agent-authored and untrusted: cap it at the same bound plan 04-02 chose for
    `tool_calls.target`, and say so.

    **The registry is in-memory and does NOT become cards.** D-10: `openPhoneAsks()`
    (`index.ts:1221-1236`) is card-derived and keyed on `taskId`; a tool approval has no card, is
    ephemeral, and expires to deny in seconds. Writing one into `tasks.json` would block a real card,
    survive its own timeout as a dead question, and put a high-frequency transient into the durable ledger
    that ADR-0004 has a single committer for. Write that reasoning into the module header so nobody
    "improves" it later.

    Extend `test/control.test.cjs` with an `ApprovalRegistry` block, RED first, covering every behaviour
    above with an injected clock. The id-shape test must assert against `PHONE_TASK_ID_RE`'s literal
    pattern, not a hand-rolled approximation.
  </action>
  <acceptance_criteria>
    - `node --test test/control.test.cjs` exits 0 with at least seven new cases.
    - A test asserts every generated id matches `/^[A-Za-z0-9._-]{1,128}$/` — the actual regex from `webhook.ts:231`, copied as a literal with a comment naming its source.
    - 1 000 generated ids are all distinct (a cheap collision check).
    - `answer(id, true)` after `expiresAt` returns `false` **and** `poll(id)` still returns `'deny'`.
    - **The TTL relationship is asserted THREE ways, all reading `src/main/hiveProvisioning.ts` rather than literals in the test:** `MIN_PRETOOLUSE_SEC >= 60`, **and** `ASK_TTL_MS >= 30_000`, **and** `ASK_TTL_MS <= MIN_PRETOOLUSE_SEC * 1000`. The upper bound alone cannot fail — the TTL is *derived* from that bound, so `SEC = 31` (a one-second window on a phone) and `SEC = 5` (`TTL = -25000`, every ask expired at birth, `-25000 <= 5000`) both pass it. The test's own comment names those two degenerate values so nobody re-weakens it.
    - A test asserts `MIN_PRETOOLUSE_SEC === Math.min(PRETOOLUSE_HOOK_TIMEOUT_SEC, CLAUDE_PRETOOLUSE_TIMEOUT_SEC)` and that **neither** input is `undefined` — a `Math.min` over an undefined is `NaN`, and `NaN <= anything` is false, so this would surface as a confusing failure rather than a silent one; assert it directly.
    - `sweep(now)` past `expiresAt` settles the entry to deny **exactly once** across five successive calls, and publishes once — asserted with the count, not with "it settled".
    - `poll('ask-deadbeef')` (never opened) returns `'deny'`.
    - `list()` excludes settled and expired entries — asserted with a positive lower bound (an unsettled entry IS listed).
    - `grep -c "from 'electron'" src/main/approvals.ts` returns `0`.
    - The module header states why approvals are not cards (D-10).
    - The RED run is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/control.test.cjs</automated>
  </verify>
  <done>An electron-free registry with unguessable, single-use, self-expiring ids that fail closed on every unknown, settled or expired path — and that provably satisfies the phone's id regex.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: The ask reply that is a deny to an old shim, the poll branch, and the engine timeout it has to fit inside</name>
  <files>src/main/hooks.ts, src/main/hiveProvisioning.ts</files>
  <behavior>
    - A PreToolUse whose `commandShapeDenial` verdict is `{kind:'ask'}` opens a registry entry and returns the dual-reading object: a valid deny **and** a `hive_ask` handle. `handle` stays **synchronous**.
    - `hive_ask.deadlineMs` equals the registry entry's `expiresAt` — the two numbers are the same number.
    - An `ApprovalPoll` event returns `{status:'pending'}`, or the final allow/deny, or a deny for an unknown id — **and a deny when the polling agent is not the agent that owns the ask**, even though its token is valid.
    - The PreToolUse hook timeout written for **codex and kimi** — and for **Claude** under path A — is `PRETOOLUSE_HOOK_TIMEOUT_SEC`, and every other event's timeout is unchanged. **grok's and agy's configs are byte-identical to HEAD.**
    - The `ApprovalRegistry` `HookServer` constructs is given `ttlMs: ASK_TTL_MS`, imported from `hiveProvisioning.ts` in this same commit.
    - Allow and deny paths are unchanged: same returns, same side-effect ordering, same tick. `control.ts` is not modified at all.
  </behavior>
  <read_first>
    - `src/main/control.ts` (118 lines, whole file) — `toolDecision` at **`:97`**, its
      `{ deny: boolean; reason?: string }` return, and the header describing how the answer rides the hook
      return rather than a PTY write. **Read it to confirm the decision, then change nothing in it.** It
      inspects only `paused` and `gatedTools` and never sees a command string; it also runs at `:1387`,
      AFTER plan 04-06's shape judge, so an `ask` expressed here could never be reached for any shape this
      gate cares about. Widening it would additionally convert the operator's race-free immediate tool-gate
      deny into a 120 s wait.
    - `src/main/commandShape.ts` (plan 04-06, wave 2) — `ShapeVerdict = { kind: 'deny'|'ask'; reason }`
      and `<planner_decisions>` in `04-06-PLAN.md`, which settles **which shapes ask**: force-push,
      pipe-to-interpreter and denied-host ask; `rm -rf` denies. That decision is binding here.
    - `src/main/hooks.ts` — plan 04-06's new PreToolUse shape arm and the optional trailing constructor
      argument `openAsk?` it declared. **This task supplies that handler; it does not re-key the arm.**
    - `src/main/hooks.ts:764-781` — `authorized(p)`. Read it closely: it maps a **valid token** to the
      agent it was minted for and returns that id. It cannot reject agent B holding B's own valid token
      while polling A's ask id, because from its point of view nothing is wrong. The owner check is the
      poll branch's job and it does not exist for free.
    - `src/main/hiveProvisioning.ts:210-232` (codex, the long `timeout` note and `timeout = 30` at `:232`),
      `:285-296` (kimi, `timeout = 30` at `:293`), `:415-426` (grok's `tool()` helper and its
      *"Let Grok apply its event-aware defaults (5s normally, 600s for Stop)"* comment), `:128`/`:131`
      (agy's `timeout: 0`) and **`:214-219`** — the recorded outcome of exceeding the bound: the hook is
      killed and *logged as failed*. A killed shim writes no stdout, and no stdout is ALLOW.
    - `src/main/hooks.ts:1188-1200` — `handle`'s entry; **`:1364-1400`** — the PreToolUse region, the
      GATE-01 comment, `protectedPathDenial` at `:1369`, plan 04-06's shape judge before it, the HITL gate
      and `toolDecision` at `:1388`
    - `src/main/hooks.ts:467` — `boundDeny(reason)`, declared *"so the exits cannot drift apart"*
      (04-PATTERNS.md re-measured it at `:467`; RESEARCH cited `:468-474` — re-measure before quoting)
    - `src/main/hooks.ts:440-446` — the 3 s margin reasoning and `HOOK_IDLE_MS` at `:446`
    - `src/main/hooks.ts:586` (`listenOn`), `:626` (`authorized()`), `:630`
      (`conn.end(JSON.stringify(res ?? {}))` — one write, then close)
    - `src/main/hooks.ts:1521-1523` — `emitControl` → `control:approvalRequest`. **This renderer channel
      already exists and is already named for this** — RESEARCH § Supporting lists it as a reuse.
    - `src/main/hooks.ts:801-859` — the ceiling list, now carrying plan 04-06's **(j)-(v)** (thirteen
      items; that plan's own criterion asserts 13 — **not** the stale `(j)-(o)` or `(j)-(r)` earlier
      drafts of this file carried)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Pattern 3 in full,
      **L-02** (the held-connection fail-open on grok and agy), and § Common Pitfalls **6** (why flipping
      the shim's 5 s timeout to deny is wrong)
  </read_first>
  <action>
    **Do not touch `control.ts`.** The ask verdict already exists: plan 04-06's `commandShapeDenial`
    returns `{kind:'ask', reason}` and its call site already delegates to an optional `openAsk` handler
    when one is present. This task **supplies that handler** and nothing more of the decision.

    Construct the `ApprovalRegistry` **inside `HookServer`**, not at the composition root. `HookServer` is
    the only thing that opens asks (from `handle`'s PreToolUse arm) and the only thing that answers polls,
    so it is the registry's natural owner — and expose `openApprovals(): PendingApproval[]` and
    `answerApproval(id, approved): boolean` as public methods. That is what lets plan 04-17 read
    `floor.hookServer.openApprovals()` from `index.ts` and plan 04-18 route the desktop IPC to
    `floor.hookServer.answerApproval(...)` **without either of them needing a `boot.ts` line or a `Floor`
    interface change** — `hookServer` is a declared `Floor` member at **`boot.ts:102`** (re-measured in
    the round-3 revision; the earlier `:1160` was the return-object region, not the interface). This is
    the phase's model for a working seam: a **named accessor** on `Floor`, and a consumer criterion that
    reads it through that accessor.

    Wire `openAsk` to: open a registry entry, publish it (an injected publisher — plan 04-17 wires it to
    `openAsks()`, the desktop `Notification` and Web Push; plan 04-20 wires it at the composition root;
    this plan declares the seam), fire `emitControl` on the **already-existing** `control:approvalRequest`
    channel (`:1521-1523`), and return the dual-reading object from `<interfaces>` **synchronously**.

    `hive_ask.deadlineMs` must be **the same value** as the registry entry's `expiresAt`. Not derived
    separately, not recomputed — read it off the entry. Two independently-computed deadlines is exactly
    the "times out on the wrong side" failure D-08 warns about.

    Add an `ApprovalPoll` branch to `handle`. Its **shape** is the PreToolUse arm's — that is its analog —
    but its **statefulness is new**: no existing hook event is a poll keyed on an id main minted
    (04-PATTERNS.md § No Analog Found says so).

    **`authorized()` is necessary and NOT sufficient here, and the difference is a real privilege
    escalation.** `authorized(p)` (`hooks.ts:764-781`) maps a valid token to the agent it was minted for.
    Agent B polling agent A's ask id sends **B's own valid token**, so `authorized()` returns `'B'` and
    nothing is rejected — B then reads, and can settle, A's approval. GATE-01 bound the token to an
    identity; it never bound an ask to an owner. So the poll branch must **compare the derived agent id
    against the ask entry's own `agentId` and return a deny when they differ**, treating an unowned id
    exactly like an unknown one (fail closed, and leak nothing about whether the id exists). ASVS V4.
    `PendingApproval.agentId` already carries the owner — this is one comparison, and without it the
    criterion claiming cross-agent polls are rejected cannot pass as written.

    **Construct the registry with the production TTL, in this task, because this is the one place that owns
    both files.** `new ApprovalRegistry({ ttlMs: ASK_TTL_MS, now, publish })`, with `ASK_TTL_MS` imported
    from `./hiveProvisioning`. Do **not** leave `ttlMs` to a default and do **not** leave it for plan 04-20
    to pass: `boot.ts` never constructs the registry (it is built inside `HookServer`, above), so there is
    no argument there to pass it through, and an option nobody supplies is an option that silently takes
    whatever the class was written with. `<planner_decisions>` reasons the import.

    **Then set the engine PreToolUse timeouts, in `hiveProvisioning.ts`, in this same commit.** Add
    `PRETOOLUSE_HOOK_TIMEOUT_SEC`, `CLAUDE_PRETOOLUSE_TIMEOUT_SEC`, `MIN_PRETOOLUSE_SEC` and the derived
    `ASK_TTL_MS` per `<planner_decisions>`, and use the constant for **PreToolUse only**:

    - **Claude (`:456-459` `entry()`, `:486` `PreToolUse: [entry('*')]`) — path A or path B, and the
      choice is recorded.** Path A: give `entry()` an optional seconds parameter
      (`const entry = (matcher?: string, timeoutSec?: number) => ({ ...(matcher ? { matcher } : {}),
      hooks: [{ type: 'command', command: cmd, ...(timeoutSec ? { timeout: timeoutSec } : {}) }] })`) and
      call `entry('*', PRETOOLUSE_HOOK_TIMEOUT_SEC)` at `:486` **only**; every other event keeps
      `entry()` with no second argument, so their emitted JSON is byte-identical to today. **Take path A
      only after confirming the unit against the installed `claude`** — record the version and where you
      read it. Path B: change `hookSettings` not at all, measure Claude's real default, and set
      `CLAUDE_PRETOOLUSE_TIMEOUT_SEC` to it with ceiling item (g) naming the version. **Either way, Claude
      stops being `unreconciled` and becomes a number.** Say which path in the SUMMARY.
    - codex (`:232`): the loop writes one `timeout = 30` per event — emit `PRETOOLUSE_HOOK_TIMEOUT_SEC`
      for `PreToolUse` and leave the other seven at 30. **Verify with the no-spend check the `read_first`
      already cites** (`codex app-server` → `initialize` → `hooks/list` reports the normalized
      `timeoutSec` per event) and paste the reported numbers into the SUMMARY. `150` is `[ASSUMED]`; the
      check costs nothing and this plan's own note at `:222-223` names it.
    - kimi (`:293`): the same shape, the same change. kimi is not installed, so no live check is possible;
      it is the same key and the same unit as codex, whose resolver was checked.
    - **grok (`:415-426`): UNCHANGED. Do not give `tool()` a seconds argument and do not touch the
      *"Let Grok apply its event-aware defaults"* comment.** grok is not installed, the unit of its
      `timeout` key is unverified, and `hiveProvisioning.ts:424-425` omits it deliberately. If grok reads
      milliseconds, `150` means 150 ms and every grok PreToolUse hook dies before the shim can answer —
      worse than today and undetectable. Ceiling item (e).
    - **agy (`:128`/`:131`): UNCHANGED**, same reasoning, same ceiling item. `:210-213` records that the
      same `0` sentinel meant *one second* on codex rather than "no timeout".

    grok's and agy's exclusion is safe **only because their shims do not poll** — plan 04-16 leaves both
    byte-identical and their existing decoders read the ask reply's `permissionDecision: 'deny'` as a deny.
    Write that sentence into ceiling item (e) so the two halves of the decision live together.

    Do NOT widen any non-PreToolUse event's timeout: they have no verdict to wait for, and a longer budget
    there only slows a wedged shim's detection.

    **Do not make `handle` async.** Do not touch `HOOK_IDLE_MS`. Do not touch the shim's 5 s exit timer
    (that is `hiveTemplates.ts`, plan 04-16's file, and Pitfall 6 explains why flipping it to deny would
    make every socket outage deny every tool call on every agent).

    **Ship GATE-05's ceiling list** (D-34), continuing the lettering in `hooks.ts:801-859` after plan
    04-06's **(j)-(v)** — so this block starts at (a) again and its own eight items are (a)-(h):
    (a) the deny-on-timeout only binds engines whose shim polls — **pi, OpenCode, grok and agy get the
        first reply's deny and never poll, so for all four "ask" degrades to "deny"**, which is the safe
        side but is not the third answer. **Spell out what that means in practice rather than leaving it
        as a category**, because plan 04-10 turns OpenCode's judging on for the first time and the change
        in behaviour is substantial: on those four engines **every force-push, every `curl … | sh`, every
        `rm` with a recursive flag and every fetch to a host outside a 30-entry `[ASSUMED]` list becomes an
        unconditional deny with no path through**, and two of the four (pi, OpenCode) are LIVE-UNVERIFIED.
        Safe direction, real cost, named here rather than discovered by an operator whose pi agent cannot
        install a toolchain. **This is the phase's one new deliberate fail-CLOSED and it must appear
        here.**
    (a-rider, M-7) **the ask is still OPENED for those four engines, so the operator is still paged, and
        their answer is inert.** The registry entry is created and merged into `openAsks()` before the
        reply goes back, so the phone push and the desktop banner fire exactly as they do for a polling
        engine — but the deny has already been written to that shim's stdout and the decision is made.
        Tapping `approve` settles a registry entry nobody will read. Zero practical impact today (none of
        the four is installed here), and the safe direction either way, but an operator woken at 3am to
        answer a question that was already answered is a real cost and it is named here rather than
        discovered. What would settle it: suppress the ask surface for engines whose shim cannot poll, or
        label it "already denied — for your information". Owner: the hive maintainer;
    (b) an app restart mid-ask loses the in-memory registry and the next poll denies — deliberate, and it
        is the "asked and nobody answered" case, not the "floor is down" case;
    (c) an agent that never runs a *hook-bridged* tool is not asked about anything;
    (d) `--permission-mode bypassPermissions` still suppresses Claude's own prompt, so this gate is the
        only thing between the model and the command — which is **why (a) must be written down rather than
        implied**;
    (e) **grok's and agy's PreToolUse hook timeouts are UNCHANGED, and their shims deliberately do not
        poll.** agy is `timeout: 0` (`hiveProvisioning.ts:128`/`:131`) and `:210-213` records that the same
        `0` sentinel meant *one second* on codex rather than "no timeout". grok has no key at all
        (`:415-426`) and applies its own ~5 s event-aware default, whose UNIT — seconds or milliseconds —
        is unverified; if it reads milliseconds, writing `150` would mean 150 ms and every grok PreToolUse
        hook would die before the shim could answer, which is **worse than today and undetectable**.
        Neither CLI is installed here, so neither resolver can be probed. **So this plan guesses at
        neither, and the rule that makes that safe is: a shim may enter the poll loop only on an engine
        whose PreToolUse timeout this app itself writes.** For grok and agy the ask reply's own
        `permissionDecision: 'deny'` stands — their existing decoders (`hiveTemplates.ts:772-784` and the
        agy equivalent) already translate it — so they take ceiling item (a)'s degradation rather than a
        fail-open. What would settle the timeouts: an installed CLI plus one hook fire. Owner: whoever
        installs them;
    (f) **the ask registry is in-memory and its `answer` is reachable from three places** — the hook socket
        (poll), `index.ts` (the phone), and `index.ts` (the desktop IPC). The ask id is the whole
        capability in the latter two: it is unguessable and single-use, but anyone who can call those IPC
        or HTTP surfaces with a live id can settle the ask. The phone's half sits behind Phase 2's finished
        bearer boundary; the desktop's sits behind Electron IPC. Named so the capability model is legible;
    (g) **Claude's PreToolUse budget — the path taken, and the number.** Under path A this app writes
        `PRETOOLUSE_HOOK_TIMEOUT_SEC` into `hookSettings`' PreToolUse entry and the row is closed by
        construction. Under path B this app writes nothing, and this item records Claude Code's **measured**
        default in seconds, the `claude --version` it was measured against, and where the unit was read.
        Either way `CLAUDE_PRETOOLUSE_TIMEOUT_SEC` is a number and it enters `MIN_PRETOOLUSE_SEC`. **The
        word `unreconciled` does not appear.** What would settle path B further: a released schema that
        pins the unit. Owner: the hive maintainer;
    (h) **`PRETOOLUSE_HOOK_TIMEOUT_SEC = 150` is five times codex's previous 30 s, and that is a real
        latency change on the non-ask path — bounded, not dismissed.** `hiveProvisioning.ts:210-223` chose
        30 s because each hook cold-starts via hive-node (*"measured 0.08-0.16s idle but 0.6-0.7s under 8
        concurrent spawns"*). What keeps 150 s from becoming a five-fold stall is a bound this phase
        deliberately does not touch: **the shim's unconditional 5 s exit timer stays armed on the non-ask
        path** (plan 04-16, Pitfall 6), so a shim wedged for a non-ask reason still exits at ~5 s whatever
        the engine budget is. The engine timeout binds only after a `hive_ask` has been seen and the boot
        timer cleared — which is the case it exists for. `150` remains `[ASSUMED]`; the codex resolver
        check (`codex app-server` → `hooks/list`) is an acceptance criterion and its output is in the
        SUMMARY.
  </action>
  <acceptance_criteria>
    - `git diff --stat src/main/control.ts` is **empty** — `toolDecision` was not widened.
    - `grep -c 'async handle' src/main/hooks.ts` returns `0` — `handle` is still synchronous.
    - `grep -c 'HOOK_IDLE_MS' src/main/hooks.ts` is unchanged and its value is still `2000`.
    - A test asserts the ask reply object is **simultaneously** a valid deny (`hookSpecificOutput.permissionDecision === 'deny'`) **and** carries `hive_ask.id`/`deadlineMs`/`pollMs`.
    - A test asserts `hive_ask.deadlineMs === registry.list()[0].expiresAt` — the same number, not two.
    - `ApprovalPoll` with an unknown id returns a deny; with a pending id returns `{status:'pending'}`; after `answer(id,true)` returns an allow.
    - **An `ApprovalPoll` for agent A's ask id, carrying agent B's OWN valid token, returns a deny** — asserted directly, with a positive control in the same test asserting that A's own token on the same id returns `{status:'pending'}`. Both halves, because "it was rejected" is also what a broken registry returns for everything.
    - An `ApprovalPoll` carrying no token at all is dropped by `authorized()` before the branch runs — asserted separately, so the two mechanisms are not confused for one another.
    - **The per-event timeouts are asserted BEHAVIOURALLY, not by counting a literal (M3).** `test/engine-parity.test.cjs` already loads `hiveProvisioning.ts` through `test/load-ts.cjs` at `:36`, so drive the codex hook installer against a temp `CODEX_HOME`, read the generated `config.toml` back, and assert: exactly one `[[hooks.PreToolUse]]` group carries `timeout = 150`, and each of the other seven event groups carries `timeout = 30`. Do the same for kimi. **A `grep -c 'timeout = 30'` "unchanged" gate measures 3 today and pushes the executor toward keeping a literal branch string where a natural interpolation would drop it to 1** — the generated file is what matters and it is cheap to read.
    - **grok and agy are byte-identical:** `git diff --stat src/main/hiveProvisioning.ts` shows the file changed, and `awk '/^export function installAgyHooks\(/,/^}$/' src/main/hiveProvisioning.ts | grep -c 'timeout: 0'` returns `2`, unchanged; `awk '/^export function installGrokHooks\(/,/^}$/' src/main/hiveProvisioning.ts | grep -c 'timeout'` returns `0`, unchanged. Both by **symbol boundary**, never by line window (R3 rule 1), because this task inserts constants above both functions and moves every line number in the file.
    - **Claude's row is closed, and which path was taken is machine-checkable.** Under path A: `awk '/const entry = \(/,/^  \}\);$/' src/main/hiveProvisioning.ts | grep -c 'timeout'` ≥ 1, **and** the generated Claude settings object (`hookSettings(...)` is exported and pure — call it in the test) has a `timeout` on its `PreToolUse` entry equal to `PRETOOLUSE_HOOK_TIMEOUT_SEC` and **no** `timeout` key on any other event's entry. Under path B: `hookSettings(...)`'s output is `deepEqual` to HEAD's, **and** `CLAUDE_PRETOOLUSE_TIMEOUT_SEC` is a finite number ≥ 60, **and** ceiling item (g) names the `claude --version` it was measured against. **`grep -ri 'unreconciled' src/main/` returns `0`**, and the word appears nowhere in this plan's SUMMARY's decision table, either way. (It appears in this PLAN only as narration of the defect being closed.)
    - The codex resolver check ran with no model spend and its `hooks/list` output — the normalized `timeoutSec` per event — is pasted into the SUMMARY.
    - `node --test test/engine-parity.test.cjs` is green — a broken export shows up there first.
    - **The ceiling list gained items (a)-(h), and every count states the value it must beat.** *(Round 3 measured the previous draft's three "phrases that occur only in this block" at HEAD and **two of them already occur**, so those two could not fail.)* Since plan 04-06's block also uses `(a)`-`(i)` lettering in the same file, a bare letter count is ambiguous — so assert on content, each against its measured HEAD baseline:
      - `grep -c 'MIN_PRETOOLUSE_SEC' src/main/hooks.ts` ≥ `1` — **HEAD: `0`.** Bites.
      - `grep -ci 'do not poll' src/main/hooks.ts` ≥ `1` — **HEAD: `0`.** Bites.
      - `grep -ci 'degrades to' src/main/hooks.ts` ≥ **`3`** — **HEAD: `2`** (`:366` and `:371`, both the unrelated *"a bound that degrades to allow is not a bound"* comment). The previous `≥ 1` passed before a line was written.
      - `grep -ci 'OpenCode' src/main/hooks.ts` ≥ **`3`** — **HEAD: `2`** (`:38`, `:60`). Same defect, same fix.
      State all four measured before/after numbers in the SUMMARY. *(R3 rule 1: the previous line-window form (`801,940p`) sat below plan 04-06's ~50-line wave-2 ceiling insertion and would have read the wrong region.)*
    - Item (a) names the degradation for **all four** non-polling engines, each asserted against its measured HEAD count so none of them passes before the item is written: `grep -ci 'OpenCode' src/main/hooks.ts` ≥ `3` (HEAD `2`), `grep -ci 'grok'` ≥ `5` (HEAD `4`), `grep -ci 'agy'` ≥ `4` (HEAD `3`), `grep -ci 'pi '` present in the block. It states the practical consequence (every force-push, every `curl | sh`, every recursive `rm`, every unlisted host becomes an unconditional deny on those four) **and the M-7 rider below.**
    - **The production registry gets the derived TTL:** `grep -c 'ASK_TTL_MS' src/main/hooks.ts` ≥ 1 and `grep -c 'ttlMs' src/main/hooks.ts` ≥ 1, paired with a behavioural assertion in the same task — a `PreToolUse` that opens an ask through the real `HookServer` produces an entry whose `expiresAt - openedAt === ASK_TTL_MS`, read from `hiveProvisioning.ts`. Plan 04-20 asserts the same fact on a really-booted floor; this is the unit-level half.
    - Allow and deny regression: existing `hooks.ts` tests still pass unchanged.
    - `grep -c 'writePty' src/main/hooks.ts` is unchanged (ADR-0001).
    - `npm run typecheck` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/control.test.cjs && npm test</automated>
  </verify>
  <done>A third answer exists, replies synchronously, denies an un-upgraded shim by construction, hands out a deadline that is literally the registry's own expiry, and ships a ceiling that names the phase's one new fail-open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RECORD-01's writer, on the socket where agent, tool and target all exist</name>
  <files>src/main/hooks.ts, test/record-persist.test.cjs</files>
  <behavior>
    - A real `PreToolUse` payload through the real `HookServer` writes one `tool_calls` row with a non-null `target` for a path-shaped or command-shaped call.
    - The row's `agent_id` is the one `authorized()` derived from the per-agent token, never one the payload claimed.
    - The row's `decision` records `allow`/`deny`/`ask` — which is what makes GATE-05 auditable rather than merely interactive.
    - A `PersistStore` failure does not throw out of `handle` and does not change any verdict.
    - An ask that expires unanswered ends with `decision = 'deny'` in `tool_calls` — not `'ask'` forever. `ApprovalRegistry.sweep` has a real caller and settling an entry rewrites its row.
    - `telemetry.ts` is not modified — the ring buffer stays exactly as it is.
  </behavior>
  <read_first>
    - `src/main/hooks.ts:86-116` — `HookPayload`: `tool_name`, `tool_input`
    - `src/main/hooks.ts:865-889` — how `protectedPathDenial` already extracts `file_path`, `path`,
      `notebook_path` and `command` out of `tool_input`. **Reuse that extraction shape; do not invent a
      second one.**
    - `src/main/hooks.ts:626` — `authorized()` deriving the agent id from the per-agent token (GATE-01)
    - `src/main/hooks.ts:1188-1200` — `handle`'s entry, where the writer goes
    - `src/main/db.ts` — `recordToolCall` as plan 04-02 shipped it, including its `target` byte cap
    - `src/main/telemetry.ts:97-107` (`ToolSpan` — **no `target` field**), `:161` (`SPAN_RING_CAP = 200`),
      `:676-680` (`ATTR_ALLOWLIST`), and `src/main/hive.ts:1122`
      (`if (claudeProvider && this._otelEndpoint)`). **Read these to understand why the writer is here and
      not there** — and then change none of them.
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Pattern 4 and **L-05**
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § `src/main/telemetry.ts` —
      **NO SOURCE CHANGE RECOMMENDED**
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Anti-Vacuous-Pass Rules,
      the RECORD-01 row: *"Asserting row count — passes with a null `target`, which is the half the
      requirement exists for."*
  </read_first>
  <action>
    **Placement first, because the first draft's was self-contradictory.** `handle`'s entry
    (`hooks.ts:1188-1200`) is where `agentId`, `tool` and `target` are all available — and it is where
    `decision` and `reason` do **not exist yet**: they are produced at roughly thirty return points spread
    over `:1188-1516`. A writer at entry can only ever record `decision: 'pending'`, which is the half of
    the requirement RECORD-01 exists for.

    So: a **single private `record(...)` helper** declared beside `boundDeny` (`hooks.ts:467`), for the
    same stated reason — *"so the exits cannot drift apart"* — called from the **PreToolUse exits**, which
    is the only event class with a verdict. Extract `{tool, target}` once at the top of the PreToolUse
    block (one extraction, reused), then call `record` immediately before each `return` in the shape-deny
    arm, the shape-ask arm, the `protectedPathDenial` arm, the `toolDecision` arm, and the fall-through
    allow. Five call sites, one helper, no drift. **Do not add a writer to non-PreToolUse events** — a Stop
    or a Status has no tool call to record and would fill the table with rows the requirement never asked
    for.

    The store arrives as an **injected** thunk — an optional trailing constructor argument, per
    `hooks.ts:554-561`'s house pattern (plan 04-20 wires it at the composition root, where `persist` is a
    module-scope `let` assigned ~29 lines AFTER the `new HookServer(...)` call (measure both with
    `grep -n`, never by a quoted line number) — so the
    argument must be a closure read at call time, exactly as `boot.ts:998-1000` already reasons about
    `delivery`). `hooks.ts` must not import `db.ts`.

    **`sweep` needs a caller and this is it.** An ask that expires with nobody sweeping stays `pending` in
    the registry forever, so its `tool_calls` row holds `decision = 'ask'` for a call that was in fact
    denied — and the Repudiation mitigation RESEARCH names would not exist. Drive `registry.sweep(now)`
    from the `ApprovalPoll` branch (every poll is already a tick, and a pending ask is polled once a
    second by definition) **and** on `openAsk` (so an ask opened after a long idle does not sit behind a
    stale one). No new timer, no new scheduler slot: the poll loop is the clock. When `sweep` settles an
    entry to deny, rewrite that ask's `tool_calls` row's `decision` and `reason` — say in a comment that
    this is the one place a row is updated rather than appended, and why.

    `target` uses the same extraction `protectedPathDenial` already performs at `:865-889`: `file_path`,
    `path`, `notebook_path`, or `command`. For a tool with none of those, `target` is legitimately `null` —
    the column is nullable on purpose, and plan 04-02's tests already distinguish "nullable by design"
    from "null because nothing was written".

    `agentId` is whatever `authorized()` derived (`:626`), **never** a value the payload claimed. That is
    the Phase 1 dependency ROADMAP.md names: without GATE-01, a persisted tool-call record attributes to
    an agent id any shell could forge.

    Record the `decision` (`allow`/`deny`/`ask`) and, on a deny, the operator-legible `reason`. That field
    is free here and it is what makes GATE-05 auditable rather than merely interactive (Security Domain's
    Repudiation row).

    Wrap the call so a store failure is logged and swallowed — **a recording failure must never change a
    verdict.** A gate that fails open because its audit log was unavailable is a worse bug than a missing
    row.

    **Change nothing in `src/main/telemetry.ts`.** The ring buffer at `:161` stays as the hot read path
    for the waterfall UI, so nothing on screen regresses (D-17's one surviving clause). Add a short comment
    at the writer site stating the three measured reasons the telemetry path could not carry this — no
    `target` field, an `ATTR_ALLOWLIST` that admits no path or command key, and a Claude-only OTel gate —
    so a future reader does not "fix" this by moving it.

    Add the roundtrip cases **to `test/record-persist.test.cjs`** — plan 04-02's file, this plan's to
    extend in wave 4, and the file `04-VALIDATION.md` already assigns row `04-15-T3` to. (The first draft
    named `test/control.test.cjs` in the task and `test/record-persist.test.cjs` in VALIDATION; they are
    reconciled here on VALIDATION's side, because this is a persistence assertion and that is the
    persistence file.) Drive a real `PreToolUse` payload through the real `HookServer` using
    `test/gate-harness.cjs` (plan 04-01) and assert the persisted row. Calling `recordToolCall` directly
    proves the store, which plan 04-02 already did; this must prove the **join**.

    **No `{ skip: … }`.** Plan 04-01's harness runs on win32 (the socket path comes from
    `hive.sockPath()`, a `\\.\pipe\` name there), and `04-02-PLAN.md` binds this file: *"Neither file
    may be POSIX-gated — both are pure SQLite and must run on all three CI platforms, so the 7-skip
    baseline is unchanged."* That binding still holds. `test/suite-integrity.test.cjs`'s clause-3 census
    and clause-3b floor must both come out unchanged.

    **And the case that proves the ask path is not decorative — three-way, because two-way no longer
    discriminates.** Drive all three through the real `HookServer` in one case:

    1. `git push origin +main` → the reply carries `hive_ask.id`. Without this, every GATE-05 surface in
       the phase — the registry, the dual-reading reply, the poll loop, `GET /phone/api/asks`, the PWA,
       `BlockedBanner` — can ship green with zero commands ever routed to ask.
    2. `curl https://evil.example/x` **with the host allowlist getter returning `[]`** → a **bare deny with
       no `hive_ask`**. Round 3 moved `rm -rf` from deny to ask (04-06 `<planner_decisions>`), so the
       emptied allowlist is now the only producer of `kind:'deny'` — and it is what makes assertion 1 mean
       something rather than being satisfied by a judge with one answer.
    3. `ls -la` → **neither** a `hive_ask` nor a deny. The third leg matters: without it, a judge that
       verdicts everything passes 1 and 2 together.

    Assert `rm -rf ./x` alongside as an **ask** (it moved), so the case also pins the round-3 decision
    rather than leaving it to prose.
  </action>
  <acceptance_criteria>
    - A test drives a real `PreToolUse` for a `Write` with a `file_path` through the real `HookServer` and asserts the persisted row has `target === <that path>` — **non-null**, asserted strictly.
    - A test asserts the persisted `agent_id` equals the token-derived id and **not** a different id placed in the payload body — the negative is what proves GATE-01 is load-bearing here.
    - A denied call persists `decision === 'deny'` and a non-empty `reason`; an ask persists `decision === 'ask'` **and, after its TTL expires and a sweep runs, the same row reads `decision === 'deny'`** — asserted by re-reading the row, so an ask that nobody answers is auditable as the denial it actually was.
    - **The three-way contrast in one case:** a real `git push origin +main` returns a reply carrying `hive_ask.id`; a real `curl https://evil.example/x` **under an emptied allowlist** returns a deny with **no** `hive_ask`; a real `ls -la` returns neither. All three, because any two of them are satisfiable by a judge with one answer. `rm -rf ./x` is asserted in the same case to return `hive_ask` — it moved from deny to ask in round 3 and the test is what holds that decision.
    - `grep -c 'skip' test/record-persist.test.cjs` returns `0`, and `node --test test/suite-integrity.test.cjs` is green with `FROZEN` unchanged.
    - **The five PreToolUse exits all record, and nothing records at `handle`'s entry — both asserted by symbol boundary, never by line window (R3 rule 1).** `grep -o 'this\.record(' src/main/hooks.ts | wc -l` returns **≥ 5** (occurrences, not lines), **and** `awk '/^  private handle\(p: HookPayload, agentId: string\): unknown \{/,/PreToolUse/' src/main/hooks.ts | grep -c 'this.record('` returns **`0`** — the range runs from `handle`'s own signature to the first mention of `PreToolUse`, so it moves with the function instead of with the file. *(The previous line-window form (`1188,1210p`) was the worst of the five round-2 found: after plan 04-06's wave-2 ceiling insertion it would have read ~50 lines of unrelated code and returned `0` — a **silently vacuous pass**, which is worse than a failure.)*
    - **And the same fact asserted behaviourally, because a grep over exits cannot prove a verdict was recorded:** for each of the five exit classes — shape-deny, shape-ask, path-deny, `toolDecision`-deny, fall-through allow — one `PreToolUse` is driven through the real `HookServer` and the resulting `tool_calls` row's `decision` is asserted. Five drives, five decisions, no `'pending'` among them.
    - `grep -c '.sweep(' src/main/hooks.ts` ≥ 1 — the sweeper has a caller in production code, not only in a test.
    - With the store forced to throw, `handle` returns the **same verdict** it returns with a healthy store — asserted by comparing both returns.
    - `git diff --stat src/main/telemetry.ts` is **empty**.
    - `grep -cE "from './db'|require\('./db'\)" src/main/hooks.ts` returns `0`.
    - The RED run is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/record-persist.test.cjs test/control.test.cjs && node --test test/suite-integrity.test.cjs && npm test</automated>
  </verify>
  <done>Every tool call on every hook-bridged engine is persisted with a real target, a token-derived agent id and its FINAL verdict; a real force-push produces a real ask; the telemetry ring is untouched; and a store failure can cost a row but never a verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent shim → hook socket → `handle` | Untrusted payloads, including `ApprovalPoll` events carrying an ask id. `authorized()` (`hooks.ts:626`) is the boundary; GATE-01's per-agent tokens are what make it mean anything. |
| ask id → the phone's `POST /phone/api/answer` | The id crosses to an operator's device and back. It must be unguessable and single-use (ASVS V3) and must satisfy `PHONE_TASK_ID_RE` or the operator sees a 400. |
| agent-authored command → `tool_calls.target` | Durable storage of untrusted text (ASVS V7). |
| the audit writer → the verdict | A recording failure must not change a security decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ASK-01 | Elevation of privilege | Guessing or forging an ask id to approve another agent's command | mitigate | `randomBytes` ids, single-use, plus `authorized()` on the `ApprovalPoll` branch so a poll must carry that agent's own token (ASVS V3, V4) |
| T-04-ASK-02 | Elevation of privilege | A held-open connection silently failing OPEN on grok and agy | mitigate | **Measured (L-02).** The reply is a single object on a single write; the wait is a poll from a fresh connection. This is the reason Pattern 3 exists and it must not be "simplified" back |
| T-04-ASK-03 | Elevation of privilege | An un-upgraded shim treating an ask as an allow | mitigate | The ask reply **is** a valid deny in the `hookSpecificOutput` field an old shim reads. Fail-closed with no version negotiation |
| T-04-ASK-04 | Race / Repudiation | An operator's late "yes" answering a question whose asker already denied and moved on | mitigate | Server-side TTL ≤ the `deadlineMs` handed to the shim, asserted as the **same value**; `answer` on an expired id returns `false` |
| T-04-ASK-42 | Elevation of privilege | **Claude's PreToolUse hook timeout never set, on the one engine that certainly runs on this machine.** `entry()` (`hiveProvisioning.ts:456-459`) writes no `timeout` key in any branch, `hookSettings` is Claude-only, and Claude Code's default is ~60 s against a 120 s TTL. The plan's own words: *"the hook is killed and logged as failed, not denied. A killed shim writes no stdout, and no stdout is ALLOW."* So GATE-05's "times out to deny" was still "times out to allow" on the engine every live GATE-03/GATE-05 observation in the phase is gathered on — and the relationship test could not catch it, because it reads a constant Claude never consumed | mitigate | Path A (`entry()` learns a seconds parameter, `PreToolUse: [entry('*', PRETOOLUSE_HOOK_TIMEOUT_SEC)]`, unit confirmed against the installed `claude`) or path B (measure Claude's default into ceiling item (g) and re-derive `ASK_TTL_MS` from `MIN_PRETOOLUSE_SEC`). Asserted by calling the pure `hookSettings(...)` and inspecting its output, plus `grep -ri 'unreconciled' src/main/` returning 0 |
| T-04-ASK-43 | Elevation of privilege | **A shim polling on an engine whose PreToolUse budget this app cannot bound.** Writing `timeout: 150` into grok's config on an unverified unit turns 150 s into 150 ms if grok reads milliseconds — every grok PreToolUse hook killed, no stdout, ALLOW. Worse than today and undetectable, on an engine nobody here can install | mitigate | grok and agy are left byte-identical and **their shims do not poll**; the ask reply's own `permissionDecision: 'deny'` stands, translated by decoders that already exist (`hiveTemplates.ts:772-784`). Ceiling item (e) records both, and plan 04-16 drives grok's unchanged decoder through a real child process as local evidence |
| T-04-ASK-44 | Repudiation | **`ASK_TTL_MS` never reaching the production registry.** The registry is built inside `HookServer`, so `boot.ts` never constructs it and plan 04-20 had no argument to pass; a `ttlMs` option nobody supplies takes whatever the class defaults to, while 04-20's must_have asserted the derived value was in force. The whole remedy for round 1's BL-3, disconnected from production | mitigate | `hooks.ts` imports `ASK_TTL_MS` and passes it at the one `new ApprovalRegistry(...)` site, in the same commit that declares it. Asserted by **effect**: an ask opened through the real `HookServer` has `expiresAt - openedAt === ASK_TTL_MS`, here at unit level and in plan 04-20 on a really-booted floor |
| T-04-ASK-45 | Repudiation | **A TTL relationship test that cannot fail for the reason it exists.** `ASK_TTL_MS <= BOUND * 1000` where the TTL is *derived* from `BOUND` holds for every value: `SEC = 31` yields a one-second window, `SEC = 5` yields `-25000` — every ask expired at birth — and `-25000 <= 5000` passes | mitigate | Three-part assertion: `MIN_PRETOOLUSE_SEC >= 60`, `ASK_TTL_MS >= 30_000`, `ASK_TTL_MS <= MIN_PRETOOLUSE_SEC * 1000`. The test's comment names both degenerate values so the bound is not re-weakened |
| T-04-ASK-28 | Elevation of privilege | **The ask TTL exceeding every engine's own PreToolUse hook timeout** — 24x grok's ~5 s default, 4x codex's and kimi's 30 s — so the shim is KILLED before it can answer. `hiveProvisioning.ts:214-219` records that a killed hook is logged as *failed*, writes no stdout, and no stdout is ALLOW. GATE-05's "times out to deny" silently becomes "times out to allow" on the ordinary path, on every engine, with every test green | mitigate | `PRETOOLUSE_HOOK_TIMEOUT_SEC = 150` written for codex, kimi and grok's PreToolUse only, and `ASK_TTL_MS` **derived** from it in the same file so the two cannot drift; a test asserts the relationship reading both values from source. agy's `timeout: 0` is left unchanged and named as ceiling item (e) rather than guessed at |
| T-04-ASK-29 | Elevation of privilege | **Agent B polling — and settling — agent A's ask**, using B's own perfectly valid token. `authorized()` maps a valid token to an identity; it has no reason to object | mitigate | The poll branch compares the derived agent id against `PendingApproval.agentId` and denies on mismatch, treating an unowned id exactly like an unknown one; asserted with a positive control so "rejects everything" cannot pass (ASVS V4) |
| T-04-ASK-30 | Repudiation | **`ApprovalRegistry.sweep()` declared with no caller**, so an expired ask stays `pending` and `tool_calls` holds `decision = 'ask'` forever for a call that was in fact denied — the exact Repudiation mitigation RESEARCH names, absent | mitigate | `sweep` is driven from the `ApprovalPoll` branch and from `openAsk` (the poll loop is the clock — no new timer); settling rewrites the row's `decision`/`reason`; a test re-reads the row after expiry and asserts `'deny'` |
| T-04-ASK-31 | Repudiation | **An audit writer at `handle`'s entry, where `decision` and `reason` do not exist** — they are produced at ~30 return points — so every row records `pending` and the requirement's second half is unmet | mitigate | One private `record(...)` helper beside `boundDeny` (`:467`, declared *"so the exits cannot drift apart"*), called from the five PreToolUse exits; an acceptance criterion asserts zero `record(` calls at `handle`'s entry |
| T-04-ASK-05 | Elevation of privilege | Approval-prompt fatigue leading to blanket approval | mitigate | TTL times out to **deny**, and every verdict is persisted to `tool_calls.decision` — which is what makes the gate auditable rather than merely interactive |
| T-04-ASK-06 | Elevation of privilege | **pi, OpenCode, grok and agy** receiving the ask reply's deny and being unable to poll, so "ask" degrades to "deny" — which after plan 04-10 turns OpenCode's judging on means **every force-push, every `curl … \| sh`, every recursive `rm` and every fetch to a host outside a 30-entry `[ASSUMED]` list becomes an unconditional deny with no path through** on four engines, two of them LIVE-UNVERIFIED | accept — **the phase's ONE new deliberate fail-CLOSED** | It is the safe side and it is substantial. Ceiling item (a) states the consequence in those terms rather than as a category, written into `hooks.ts:801-859`, not implied. For grok and agy it is also what removes T-04-ASK-43's fail-open |
| T-04-ASK-07 | Denial of service | An app restart mid-ask losing the in-memory registry | accept | Ceiling item (b). It is the "asked and nobody answered" case, not the "floor is down" case, and the next poll denies |
| T-04-ASK-08 | Elevation of privilege | The socket being gone, refusing, or answering nothing — and that denying every tool call on every agent | accept — **deliberate, inherited** | D-08 clause 3 / D-07 hole 2: the connect-error `exit(0)` = allow path is reached **before** any `hive_ask` exists. Preserving it is a requirement, not an oversight. Pitfall 6 |
| T-04-LOG-09 | Repudiation | A tool-call record attributing to an agent id any shell could forge | mitigate | `agentId` comes from `authorized()`'s token derivation (GATE-01), never from the payload body; asserted with a payload that claims a different id |
| T-04-LOG-10 | Denial of service | An audit-store failure changing a verdict | mitigate | The writer is best-effort and swallowed; a test compares `handle`'s return with a healthy and a throwing store |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step. Ask ids from `node:crypto`, which `push.ts` already uses. `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed |
</threat_model>

<verification>
- `node --test test/control.test.cjs` — green, ≥ 7 registry cases plus the reply-shape and poll cases.
- `npm test` — full suite, 0 failures, skipped count unchanged by this plan.
- `npm run typecheck` — 0 errors. `npm run lint` — exit 0.
- `git diff --stat src/main/telemetry.ts src/main/hiveTemplates.ts src/main/webhook.ts src/main/index.ts src/main/control.ts src/main/floor/boot.ts` — all empty.
- `awk '/^export function installGrokHooks\(/,/^}$/' src/main/hiveProvisioning.ts | grep -c 'timeout'` → `0`, and the same `awk` over `installAgyHooks` piped to `grep -c 'timeout: 0'` → `2`. Both unchanged, both by symbol boundary.
- `grep -ri 'unreconciled' src/main/` → `0`.
- `node --test test/engine-parity.test.cjs` — green; it already loads `hiveProvisioning.ts` through `load-ts`.
- `node --test test/suite-integrity.test.cjs` — green; `DECLARED_SKIPS` and `FROZEN` unchanged.
</verification>

<success_criteria>
- The third answer is `commandShapeDenial`'s verdict kind; `control.ts` is untouched; `handle` is still synchronous; `HOOK_IDLE_MS` and the shim's 5 s timer are untouched.
- The engine PreToolUse hook timeouts and the ask TTL move together, derived from `MIN_PRETOOLUSE_SEC`, and a test asserts the relationship from source **with a lower bound as well as an upper one** — so the degenerate values (`SEC = 31`, `SEC = 5`) that pass a one-sided inequality both fail.
- **Claude's row is a number, not `unreconciled`**, by path A (this app writes the timeout) or path B (this app measures the default into ceiling item (g)); the SUMMARY says which and why.
- **grok's and agy's configs are byte-identical**, and their shims do not poll — the ask reply's own deny stands for both, which is ceiling item (a)'s degradation rather than a guessed unit's fail-open.
- **`ASK_TTL_MS` reaches the production registry**: `hooks.ts` imports it and passes it as `ttlMs` at the one `new ApprovalRegistry(...)` site, asserted here at unit level and by effect on a really-booted floor in plan 04-20.
- A cross-agent `ApprovalPoll` is denied, with a positive control.
- A real `git push origin +main` through the real `HookServer` produces a `hive_ask`; a real `rm -rf` produces a bare deny.
- The ask reply denies an old shim and hands a new one a deadline that is literally the registry's `expiresAt`.
- Ask ids are unguessable, single-use, expiry-enforced, and provably satisfy `PHONE_TASK_ID_RE`.
- Every tool call persists with a token-derived agent id, a real target, and its verdict — and a store failure costs a row, never a decision.
- GATE-05's ceiling names the pi/OpenCode degradation as the phase's one new deliberate fail-open.
- The SUMMARY records the RED runs, the test-count delta, the engine-timeout/TTL/deadline/poll numbers with their `[ASSUMED]` markings and the derivation between them, agy's unchanged `timeout: 0` as ceiling item (e), and the three measured reasons the writer is not on the telemetry path.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-15-SUMMARY.md` when done.
</output>
