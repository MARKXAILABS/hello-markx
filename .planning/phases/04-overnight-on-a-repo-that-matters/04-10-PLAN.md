---
phase: 04-overnight-on-a-repo-that-matters
plan: 10
type: execute
wave: 3
depends_on: ["04-06"]
files_modified:
  - src/main/hiveTemplates.ts
  - test/engine-parity.test.cjs
  - test/repo-claims.test.cjs
autonomous: true
requirements: [GATE-03]
user_setup: []

must_haves:
  decisions:
    # `D-NN:` citations for GSD's blocking decision-coverage gate, which searches only the
    # frontmatter `must_haves` block. Each names the task or criterion in THIS plan.
    - "D-06: criterion 1's four engines do not have four equal paths — task 3 writes the honest per-engine claim (refused live for Claude and codex; built and unit-verified for grok/kimi/agy; built and LIVE-UNVERIFIED for pi and OpenCode) and pins it in both directions."
    - "D-03: `AGENT_DENY_RULES` stays as defence in depth and no clause of the honest claim is green on its behaviour."
  truths:
    - "GATE-03's honest claim, written into the SUMMARY and pinned by a test **in both directions** so it can be over-stated in neither: refused through the real `HookServer` for Claude-shaped and Codex-shaped payloads, and observed on a live codex agent by plan 04-13 task 4; built for grok and kimi against their shipped shims and unit-verified through the real `HookServer`; built for agy, exercised by nothing; built and LIVE-UNVERIFIED for pi and OpenCode."
    - "OpenCode's veto is not merely unverified — as shipped it is provably DEAD, and that is verifiable here without OpenCode. `OPENCODE_PLUGIN`'s `tool.execute.before` posts `tool_name` and NO `tool_input`, so `protectedPathDenial` exits at `hooks.ts:890` and there is nothing for `commandShapeDenial` to judge. The marker must distinguish 'cannot verify the mechanism' from 'have not sent it anything to judge'."
    - "GATE-03's main-side judging reaches SEVEN engines, not four — `BridgeDescriptor` enumerates six hook shims (agy, codex, pi, opencode, grok, kimi) plus Claude's native `--settings`. `kimi` and `antigravity` are engines CONTEXT.md never mentions."
    - "pi's `post()` becomes request/response for PreToolUse so a deny can be honoured, and its `HIVE_AUTO_APPROVE=1` auto-approve branch is what GATE-03 gates."
    - "OpenCode's `tool.execute.before` veto path is built against the documented API and marked LIVE-UNVERIFIED — the OpenCode clause of criterion 1 is NOT ticked."
    - "`MARKER_LEDGER` is reconciled in the SAME COMMIT as every marker added, across all four of its pins: per-file counts, repo total, committed file set, and the per-engine lower bound."
  artifacts:
    - path: "src/main/hiveTemplates.ts"
      provides: "pi's request/response PreToolUse post and OpenCode's veto path, both LIVE-UNVERIFIED marked"
      contains: "LIVE-UNVERIFIED"
    - path: "test/engine-parity.test.cjs"
      provides: "grok's translator maps a main-side deny to {decision:'deny'}"
    - path: "test/repo-claims.test.cjs"
      provides: "the reconciled marker ledger, all four pins. The honest-claim pin is NOT here: it lands in plan 04-19 task 2 (wave 7), which owns SECURITY.md and this file in one commit — a wave-3 pin over SECURITY.md would be red for four waves, and a wave-3 pin over a comment this plan just wrote would be self-certifying"
      contains: "MARKER_LEDGER"
  key_links:
    - from: "PI_EXTENSION post()"
      to: "the HookServer reply"
      via: "read the response for PreToolUse instead of c.end() fire-and-forget"
      pattern: "PI_EXTENSION"
    - from: "GROK_HOOK_SHIM's reply decoder"
      to: "{decision:'deny'}"
      via: "the existing permissionDecision translator at :779-781"
      pattern: "GROK_HOOK_SHIM|decision"
---

<objective>
GATE-03's reach across engines, and the honesty clause that keeps its SUMMARY true.

**Purpose.** Plan 04-06 built the judge in main. This plan settles which engines actually honour its
verdict, builds the two paths that do not exist yet, and writes the claim down so it cannot inflate.

**RESEARCH corrected CONTEXT.md here, in both directions, and the plan is built against the measurement.**

*Narrower than D-06 says, on live verification:* **only Codex of criterion 1's four engines is installed
on this machine** (`codex-cli 0.128.0`, logged in via ChatGPT). grok, pi, opencode, crush, kimi, agy and
qwen are all absent — `command -v <cli>` probed in the research session. D-06's binding SUMMARY wording
— *"refused for Codex and Grok (live-verified)"* — is **not achievable here** without installing grok and
buying an xAI key, which the zero-recurring-cost constraint forbids. Writing it anyway would be the exact
over-claim this project bans.

*Wider than D-06 says, on reach:* `BridgeDescriptor` (`src/shared/agentProvider.ts:71`) enumerates **six**
hook shims — `agy | codex | pi | opencode | grok | kimi` — plus Claude's native `--settings`. Main-side
judging therefore covers **seven** engines, and **`kimi` and `antigravity` (agy) are two CONTEXT.md never
mentions**. That is five more engines than `AGENT_DENY_RULES` reaches, and it is the truth.

*And D-06's own premise was partly wrong:* **grok does not reuse `HOOK_SHIM`.** It has its own translator,
`GROK_HOOK_SHIM` at `hiveTemplates.ts:730` (written at `hiveProvisioning.ts:420`), with its own 5 s exit at
`:795` and its own `JSON.parse(resp)` reply decoder at `:772-784`. That matters enormously for plan 04-16
(L-02) and it matters here because grok's deny path is a *translation*, not a passthrough, and must be
asserted as one.

**D-03 stands: `AGENT_DENY_RULES` is not deleted and not the answer.** `hiveProvisioning.ts:63` stays
exactly as it is. It is the only rule surface that survives `--permission-mode bypassPermissions` inside
Claude's own process, so it fires before a hook round trip — real defence in depth. But **no success
criterion may be marked green on its behaviour**, because it is written into exactly one engine's settings
file.

**Output.** pi's request/response path, OpenCode's veto path, both marked; grok's translator asserted; the
marker ledger reconciled across all four pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-01-SUMMARY.md
</context>

<the_honest_claim>
**This exact wording goes into the SUMMARY. It replaces D-06's binding wording, which the machine cannot
support, and the substitution is stated rather than performed silently. The revision tightened it twice —
once because the first draft's "live-verified" outran what any task actually runs, and once because "agy is
unit-verified" outran what any test actually exercises:**

> GATE-03 is refused **through the real `HookServer`** for Claude-shaped and Codex-shaped `PreToolUse`
> payloads, driven by the real shim as a child process (`test/gate03-roundtrip.test.cjs`), and **a live
> codex agent had a command refused on this machine** (plan 04-13 task 4). It is **built for grok and
> kimi** against their shipped shims, with grok's reply translator exercised through a real child process
> in `test/engine-parity.test.cjs`; **LIVE-UNVERIFIED** for both, for want of an installed CLI. It is
> **built for agy and exercised by no test at all** — `AGY_HOOK_SHIM`'s translator is not driven anywhere
> in this phase — and is LIVE-UNVERIFIED. It is **built and LIVE-UNVERIFIED for pi**. For **OpenCode** it
> is built, LIVE-UNVERIFIED, **and known to be inert as shipped**: `OPENCODE_PLUGIN` posts no `tool_input`,
> so until task 1 fixes that there is nothing for either judge to read. The qwen and crush proxy tiers have
> no tool-call boundary to gate at (`hooks.ts:60-62`) and are out of scope. copilot has no bridge at all.

**Criterion 1's OpenCode clause is NOT ticked. Criterion 1's grok and pi clauses are NOT ticked.** A
SUMMARY that claims all four verbatim is the failure mode this plan exists to prevent.

**The pin asserts both directions — and it lands in WAVE 7, not here (R2-BL3).**

The previous draft put a claim pin in this plan's task 3 and never named the file it pinned. Both readings
broke. SECURITY.md is the natural subject (`repo-claims.test.cjs:1606-1624` already pins SECURITY.md prose)
— but the claim only reaches SECURITY.md in **wave 7**, so a wave-3 pin would be red for four waves and
would fail this plan's own wave gate. The alternative subject is the comment task 2 writes into
`test/engine-parity.test.cjs`, which makes the pin **satisfied by a comment it just wrote** — self-
certifying, and it leaves SECURITY.md unpinned. The prescribed `grep -v '^#'` filter guards a Markdown
heading; a JavaScript `//` comment sails straight through it.

So the claim pin is **plan 04-19 task 2's**, in wave 7, which owns SECURITY.md and
`test/repo-claims.test.cjs` in the same commit. **This plan pins only what exists at wave 3** — the marker
ledger's four pins — and writes `<the_honest_claim>` into its SUMMARY and into
`test/engine-parity.test.cjs`'s block comment beside the assertions that support it. Say in the SUMMARY
that the pin lands in wave 7 and name plan 04-19 task 2, so the closing plan finds a handoff rather than a
hole.

**The riskiest clause was also unpinned, and it is now in the pinned set.** The three positive phrases the
previous draft chose — `through the real HookServer`, `LIVE-UNVERIFIED for pi`, `exercised by no test at
all` — cover none of *"a live codex agent had a command refused on this machine"*, which is the exact
sentence round 1's BL-11 was about. Inflation in the one named direction would still have passed. Plan
04-19's clause pins that sentence too, cross-checked against the machine-readable
`LIVE GATE-03 REFUSAL: yes|no` line plan 04-13 task 4 is required to write.

**And the correction path is unblocked.** `04-VALIDATION.md`'s Manual-Only table prescribes correcting the
claim *"in the same commit"* if the live refusal does not appear — but plan 04-13's `files_modified`
contained neither `test/engine-parity.test.cjs` nor SECURITY.md, so the correction was a D-35 violation by
construction. `test/engine-parity.test.cjs` is now in plan 04-13's `files_modified` for wave 4 (this plan
owns it in wave 3; no collision), so 04-13 can correct the comment in the same commit as its finding, and
plan 04-19 writes whichever wording survives into SECURITY.md and pins it.
</the_honest_claim>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step. **Do not install grok, pi,
  opencode, kimi or agy to make a claim tickable** — every one needs its own paid subscription, and
  PROJECT.md's zero-recurring-cost rule is absolute. "Verification needs an account we do not have" is a
  first-class outcome here, not a failure.
- **D-35 — `src/main/hiveTemplates.ts` line ranges.** This plan owns **`:291-800`** in wave 3.
  `TASK_CLI` (`:24-290`) was plan 04-04's in wave 1. Plan 04-16 owns `:291-800` in wave 5 for the poll
  loops. **This plan does not add a poll loop** — that is GATE-05's and S-2 sequences it after this.
- **D-35 — `test/repo-claims.test.cjs`.** This plan owns it in wave 3. Plan 04-13 owns it in wave 4 and
  plan 04-19 in wave 7. S-8: the ledger is a four-way pin and two plans editing it in one wave is a
  genuine shared-line conflict, not a distance problem. Never edit it out of turn.
- **This plan does not touch `src/main/hooks.ts`** — plan 04-06 owned it in wave 2 and plan 04-15 owns it
  in wave 4.
- **D-33 / D-40** — assert a positive lower bound alongside every negative.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pi's request/response, and OpenCode's veto — both built, both marked</name>
  <files>src/main/hiveTemplates.ts</files>
  <behavior>
    - `PI_EXTENSION`'s `post()` reads the server's reply for `PreToolUse` and returns the verdict through pi's `ev.approve()` contract; for every other event it stays fire-and-forget.
    - pi's `HIVE_AUTO_APPROVE === '1'` branch no longer auto-approves a call main denied.
    - `OPENCODE_PLUGIN`'s `tool.execute.before` awaits main's verdict and vetoes on a deny, against the documented API.
    - Both carry a `LIVE-UNVERIFIED` marker naming what could not be verified and why.
    - A connect error still `process.exit(0)` / resolves-as-allow — D-08 clause 3's deliberate fail-open is preserved verbatim.
  </behavior>
  <read_first>
    - `src/main/hiveTemplates.ts:425-465` — `PI_EXTENSION` in full: `post()` doing
      `c.end(JSON.stringify(payload))` with **no reply read** at `:436-437`, and the `tool_call` handler
      with `ev.approve()` at `:443-446` whose **return value is honoured**. Also the
      `HIVE_AUTO_APPROVE === '1'` branch — *"that branch is precisely what GATE-03 must gate"* (D-06).
    - `src/main/hiveTemplates.ts:466-492` — `OPENCODE_PLUGIN` in full. **Read `:484-486` character by
      character:** `'tool.execute.before': async (input) => { try { post({ hook_event_name: 'PreToolUse',
      tool_name: input && (input.tool || input.name) }); } catch (e) {} }`. It sends `tool_name` and
      **no `tool_input`**. On the server, `protectedPathDenial` builds `ti` from `p.tool_input` (`:863-865`),
      finds no `file_path`/`path`/`notebook_path`/`command`, and returns `null` at **`:890`** — and plan
      04-06's shape arm needs `ti.command` to enter at all. **So OpenCode's veto path is not merely
      unverified: as shipped it has nothing to judge, and that is provable here without OpenCode
      installed.** Also `:486` and `:489` — `tool.execute.after` and `event` have the same omission; only
      `before` matters for a verdict.
    - the existing `LIVE-UNVERIFIED (plugin auto-load + session.idle firing need BYOK keys)` marker
    - `src/main/hiveTemplates.ts:298-347` — `HOOK_SHIM`: the reply read at `:342-345`, the 5 s exit at
      `:347`, and **`c.on('error', () => process.exit(0))`** — the line that makes "the floor is down"
      allow. **This must be preserved verbatim in every shim this plan touches** (D-08 clause 3).
    - `src/main/hiveTemplates.ts:730-795` — `GROK_HOOK_SHIM`: the reply decoder at `:772-784` and the
      `permissionDecision:'deny'` → `{decision:'deny'}` translation at `:779-781`
    - `src/main/hooks.ts:54-62` — the two inherited holes (D-07), including the shim fail-open, which is
      **deliberate and Deferred** and must be restated rather than fixed here
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Cross-Engine Reality (the
      per-engine table), Assumptions **A6** (pi honouring an async return — unsettleable without a pi
      install) and **A7** (OpenCode's veto — already ruled LIVE-UNVERIFIED by D-06)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § `src/main/hiveTemplates.ts`
  </read_first>
  <action>
    **pi.** Make `PI_EXTENSION`'s `post()` a request/response **for `PreToolUse` only** — every other event
    keeps today's fire-and-forget shape, because widening them costs latency on paths that have no verdict
    to wait for. Feed the verdict into the `tool_call` handler's `ev.approve()` contract at `:443-446`,
    whose return value pi already honours. Gate the `HIVE_AUTO_APPROVE === '1'` branch so it cannot
    auto-approve a call main denied — that branch is exactly what GATE-03 exists to intercept, and leaving
    it means the built path is decorative.

    Assumption A6 (does pi honour an **async** return?) cannot be settled without a pi install and a key.
    Build against the documented contract and say so in the marker.

    **OpenCode, and the first half is not optional.** `tool.execute.before` currently posts
    `{ hook_event_name: 'PreToolUse', tool_name: … }` and **no `tool_input`** (`:484-486`). Send the tool
    input too — OpenCode's `input` object is what carries the arguments — so that `protectedPathDenial` has
    a target to resolve and `commandShapeDenial` has a command string to judge. Without this the veto path
    is decorative twice over: even a perfect await-and-throw would be awaiting a verdict on an empty
    payload, and the server would return `{}` (allow) every time. **This half is verifiable on this machine
    right now** — drive an OpenCode-shaped payload through the real `HookServer` and assert a deny — so it
    is a task-2 test case, not a marker.

    Then the veto itself: `tool.execute.before` is already `async` (`:489`). Await main's verdict and veto
    on a deny, against OpenCode's documented plugin API. Whether returning or throwing is the veto
    mechanism is what A7 marks unverified; implement the documented form and mark it.

    **Word the two markers so they say different things.** *"Cannot verify the veto MECHANISM without an
    OpenCode install"* is A7 and is honest. *"Sends nothing for main to judge"* was a defect, is fixed
    here, and must **not** be laundered into the marker as if it were an unverifiable — otherwise the
    marker reads as an excuse for a bug rather than a limit on evidence. Say which is which.

    **Both:** preserve the connect-error fail-open verbatim. A shim that fails closed would break every
    agent whenever the app is legitimately not running, which is why `hooks.ts:54-62` records it as
    deliberate and why CONTEXT.md's Deferred list explicitly rejects fixing it here.

    **Markers.** Each new path gets a `LIVE-UNVERIFIED` marker in the house register — naming the engine,
    what is unverified, and what would settle it (an installed CLI plus an account). Do not write a vague
    marker; `test/repo-claims.test.cjs` counts them and a future reader needs to know what to re-run.

    **Do not add a poll loop to any shim.** GATE-05's `hive_ask` reply does not exist yet (plan 04-15) and
    the poll is plan 04-16's. S-2 sequences GATE-03's shim changes **before** GATE-05's poll loop
    deliberately.
  </action>
  <acceptance_criteria>
    - **`grep -o 'LIVE-UNVERIFIED' src/main/hiveTemplates.ts | wc -l` increases by exactly the number of markers added**, with the before and after numbers stated in the SUMMARY before task 3 runs. Whole-file occurrence count, no line window (R3 rule 1) — `PI_EXTENSION` and `OPENCODE_PLUGIN` both move when anything above them changes, and plan 04-04 edits `TASK_CLI` in wave 1.
    - **OpenCode's payload builder carries a real `tool_input` PROPERTY, not a comment about one (R2-H6).** The previous gate — a grep for the token inside a line window — is satisfied by `// we now send tool_input`. Assert structurally instead: load `OPENCODE_PLUGIN` through `test/load-ts.cjs`, strip `//`-to-end-of-line and `/* … */` from the constant's text, and assert the remainder matches `/tool_input\s*:/` — a property assignment, which a comment cannot be. Paired with a positive lower bound over the same stripped text (`/hook_event_name\s*:/` still matches), so an empty strip cannot satisfy it.
    - **And the honest ceiling on that assertion, stated rather than implied:** `OPENCODE_PLUGIN` is **ESM**, so `test/gate-harness.cjs`'s `runShim` cannot drive it as a child process the way it drives `HOOK_SHIM` and `GROK_HOOK_SHIM`. **Nothing in this repo executes OpenCode's payload builder.** The structural assertion above proves the property exists; the server-side case below proves main judges that shape correctly; neither proves OpenCode sends it. That gap goes in the marker text and in the SUMMARY as a stated limit, not as silence. What would settle it: an installed OpenCode plus one tool call.
    - A test drives an **OpenCode-shaped** `PreToolUse` payload (`tool_name` from `input.tool`, plus `tool_input.command`) through the real `HookServer` and asserts a deny — the half that needs no OpenCode install. Positive control: the same shape with a benign command returns no deny.
    - **The behaviour change this turns on is named, not just enabled (R2-H9).** Adding `tool_input` makes `commandShapeDenial` fire on OpenCode for the first time, and **four of five shapes are `ask`** — which for a non-polling shim means **deny**. So every force-push, every `curl … \| sh`, every recursive `rm` and every fetch to a host outside a 30-entry `[ASSUMED]` list becomes an unconditional deny with no path through, on two LIVE-UNVERIFIED engines. Safe direction, substantial new behaviour. The marker text says it, the SUMMARY says it, and plan 04-15's GATE-05 ceiling item (a) carries the same sentence for all four non-polling engines. A one-line "pi/OpenCode cannot poll" does not convey it.
    - Each new marker states, in its own text, whether the thing it covers is an **unverifiable mechanism** or a **defect that was fixed** — the two are not interchangeable and the SUMMARY quotes both.
    - Every shim this task touches still contains `process.exit(0)` on the connect-error path — `grep -c "on('error'" src/main/hiveTemplates.ts` is unchanged or higher, never lower.
    - `grep -c 'HIVE_AUTO_APPROVE' src/main/hiveTemplates.ts` ≥ 1 and the branch is textually gated on the main-side verdict.
    - No shim gains a retry/poll loop: `grep -ciE 'ApprovalPoll|hive_ask' src/main/hiveTemplates.ts` returns `0`.
    - `npm run typecheck` → 0 errors.
    - `git diff src/main/hiveTemplates.ts` is confined to `:291-800`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <done>pi can honour a deny and OpenCode has a veto path, both built against documented APIs, both honestly marked, and neither has broken the deliberate connect-error fail-open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert grok's translator, and the seven-engine reach</name>
  <files>test/engine-parity.test.cjs</files>
  <behavior>
    - Running the **real** `GROK_HOOK_SHIM` as a child process against a real `HookServer` that returns a main-side deny yields grok's own `{decision:'deny'}` on stdout, carrying the reason.
    - The same shim against a server that allows yields no deny — the positive control.
    - A **codex-shaped** `PreToolUse` payload (codex's own shell tool name, not `Bash`, with a command string in `tool_input`) driven through the real `HookServer` is **denied** — the assertion that GATE-03's arm is keyed on the command rather than on Claude's tool name.
    - `bridgeOf` reports a hook bridge for all six shim engines plus Claude's native path: seven engines main-judges.
    - The two proxy-tier engines (qwen, crush) report no tool-call boundary, and copilot reports no bridge — asserted so the seven is a measured number, not a claim.
  </behavior>
  <read_first>
    - `src/main/hiveTemplates.ts:730-795` — `GROK_HOOK_SHIM`, and specifically the decoder at `:772-784`
      and the translation at `:779-781`
    - `test/gate-harness.cjs` (plan 04-01) — `runShim(ctx, payload, opts)` and its **`opts.shim` selector**,
      which writes a chosen shim constant to disk and spawns it. That is how this test drives grok's real
      decoder without `eval`, without a POSIX-gated bespoke spawn, and without re-implementing the decoder
      in the test — the three bad options a decoder-in-isolation test is otherwise forced to choose from.
    - `src/main/hooks.ts:871` — `p.tool_name === 'Bash'`, Claude's tool name, and plan 04-06's decision to
      key its own arm on the command string instead. This test is what proves that decision held.
    - `src/shared/agentProvider.ts:71` — `BridgeDescriptor` and its six-shim union
      `'agy' | 'codex' | 'pi' | 'opencode' | 'grok' | 'kimi'`; `:229-235` Claude's native `--settings`;
      `:287` codex; `:320` grok; `:329`/`:351` kimi; `:366`/`:377` antigravity/agy; `:404` qwen (proxy);
      `:440` opencode; `:509` crush (proxy); `:556` pi; `:574-599` copilot (no bridge); `:688-691` `bridgeOf`
    - `src/main/hooks.ts:60-62` — the record that pi, opencode and the qwen proxy tier have no PreToolUse
      hook, and that the proxy tier has **no tool-call boundary at all**
    - `test/engine-parity.test.cjs` — the existing per-engine assertions; extend them
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Cross-Engine Reality and
      Drift Report rows **12** and **13**
  </read_first>
  <action>
    Extend `test/engine-parity.test.cjs` with a GATE-03 block.

    The grok case is the important one, and it must exercise the **real translator**, not a
    reimplementation of it. `GROK_HOOK_SHIM` is a template-string constant with no in-repo precedent for
    being executed from a test, and the three obvious ways to reach it are all bad: `eval` on a source
    string, a bespoke POSIX-gated spawn, or copying the decoder into the test (where it would then be
    testing the copy). **Use plan 04-01's harness instead**: `runShim(ctx, payload, { shim: GROK_HOOK_SHIM })`
    writes the real constant to disk, spawns it as a child process against a real `HookServer`, and hands
    back its real stdout. Cross-platform, no `eval`, no second copy.

    Drive a command main will deny, and assert the shim's stdout is grok's own `{decision:'deny'}` shape
    carrying the reason string. D-06 asserted grok "reuses `HOOK_SHIM`'s response contract" and Drift row
    12 corrected it — this test is what stops that error recurring.

    **Then the codex-shaped case, which is the one that decides whether GATE-03 is a gate at all — and it
    asserts the SHAPE rather than defining it (R2-BL4).** Plan 04-06 changed **both** arms in `hooks.ts` to
    key on a `commandOf(ti)` helper rather than on `p.tool_name === 'Bash'`, and that helper accepts
    `tool_input.command` as a **string or an argv array**. Both facts need driving here, because the
    previous draft did neither: it defined "codex-shaped" as *a `command` **string***, i.e. as whatever
    satisfies the condition under test, and codex's `shell` tool takes `command` as an argv array
    (`["bash","-lc","rm -rf ./x"]`). `hiveProvisioning.ts:160` vouches only that the envelope is
    Claude-shaped; it says nothing about this field.

    So drive **two** payloads through the real `HookServer`, both with a `tool_name` that is demonstrably
    not `Bash`:

    - `tool_input.command` as the **string** `'rm -rf ./x'` → denied;
    - `tool_input.command` as the **argv array** `['bash','-lc','rm -rf ./x']` → denied.

    Plus one benign control of each shape returning no deny. Four assertions, two shapes, both directions.
    A test that drives only the string form is a test that would stay green while GATE-03 refused nothing
    on the one non-Claude engine installed on this machine.

    **On codex's actual tool NAME: it does not matter and the plan says so rather than guessing.** Neither
    arm reads the name any more, so the test's `tool_name` need only be *not `Bash`*. Say that in a comment.
    If plan 04-01's spike transcript recorded a real name, use it and cite the transcript; if it did not,
    use `'shell'` and state in the comment that the assertion is about the entry **condition**, not about a
    spelling. **Plan 04-13 task 4 rider 4 measures the real payload** — name, key and the JSON type of the
    command argument — from a live interactive codex agent in wave 4, and if it turns out codex uses a
    different KEY (`argv`, `cmd`, a nested field) that is a finding reported against plan 04-06, whose
    ceiling item (s) already names the residual.

    Add the positive control: an allow reply produces no deny. Without it the case passes against a
    decoder that denies everything.

    Add a reach assertion derived from `bridgeOf` and the preset table, **computed rather than hard-coded**:
    count the engines with a hook bridge plus Claude's native path and assert it equals the number of
    engines main can judge; separately assert that the two proxy engines and copilot are excluded, naming
    each. A hard-coded `7` would go stale the moment a preset changes; a derived count with named
    exclusions stays true and fails loudly when the roster moves.

    Add a comment in the block quoting `<the_honest_claim>` above, so the next reader of this file finds
    the claim beside the assertions that support it.

    **Say what is NOT covered here, in the same comment.** `AGY_HOOK_SHIM`'s translator is exercised by
    nothing in this phase — not by this file, not by `test/gate03-roundtrip.test.cjs`, not by
    `test/gate05-bounded-wait.test.cjs`. The honest claim above says "built for agy and exercised by no
    test at all" and this comment is where a reader of the test file learns the same thing. If you choose
    to add an agy case through the harness's `opts.shim` selector — which is cheap, since the mechanism is
    already there for grok — then change the honest claim in the same commit. Do not leave the two
    disagreeing.
  </action>
  <acceptance_criteria>
    - `node --test test/engine-parity.test.cjs` exits 0 with at least five new cases, **on win32**, with zero skipped.
    - The grok deny case runs the real `GROK_HOOK_SHIM` as a child process via `runShim(..., { shim: GROK_HOOK_SHIM })` — `grep -c 'GROK_HOOK_SHIM' test/engine-parity.test.cjs` ≥ 1 and `grep -ci 'eval(' test/engine-parity.test.cjs` returns `0`.
    - The grok deny case asserts the reason string survives the translation, not merely that a deny appeared.
    - **The codex-shaped case drives BOTH command shapes**, each with a `tool_name` that is not `Bash`: `tool_input.command` as the string `'rm -rf ./x'` **and** as the argv array `['bash','-lc','rm -rf ./x']`, both denied, each with a benign control of the same shape returning no deny. Four assertions. **A test that drives only the string form defines "codex-shaped" as whatever satisfies the condition under test** — codex's `shell` tool takes an argv array, and `hiveProvisioning.ts:160` vouches only for the envelope.
    - **The path arm reaches a non-Claude payload too:** the same non-`Bash` `tool_name`, with `tool_input.command` naming a path under `<hive>/bin/`, is denied by `protectedPathDenial`. Before plan 04-06's wave-2 widening that payload was allowed on six of seven engines, and this is the case that proves the widening landed. Positive control: a `<hive>/scratch/` path still allows.
    - The allow control case is present and asserts absence of a deny **and** a well-formed decode.
    - The reach count is derived from the preset table (`grep -c '=== 7' test/engine-parity.test.cjs` returns `0` — no magic number).
    - The excluded engines are named individually: `qwen`, `crush`, `copilot`.
    - The honest-claim comment is present in the file.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>Grok's deny translation is pinned, the seven-engine reach is a derived measurement with named exclusions, and the honest claim lives beside its evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the marker ledger — all four pins, same commit; the claim pin hands off to wave 7</name>
  <files>test/repo-claims.test.cjs</files>
  <read_first>
    - `test/repo-claims.test.cjs:1249-1262` — the block explaining why the marker clause reads **raw** text
      while most clauses read stripped-of-comments text. Read it before changing anything; it is the
      reason the ledger works at all.
    - `test/repo-claims.test.cjs:1287-1289` — the re-measure command the file itself prescribes:
      `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`
    - `test/repo-claims.test.cjs:1291-1298` — `MARKER_LEDGER`, currently
      `{hive.ts:3, hiveProvisioning.ts:5, hiveTemplates.ts:3, index.ts:1, webhook.ts:3, agentProvider.ts:3}`
    - `test/repo-claims.test.cjs:1307` — `LIVE_UNVERIFIED_TOTAL = 18`
    - `test/repo-claims.test.cjs:1419-1454` — the file-set assertion (`:1422`) and the per-engine lower
      bound (`:1453`)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` **S-8** and **L-10** — the pin
      is four-way and all four must be reconciled in the SAME commit as any marker change
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § Collision Map **C**
  </read_first>
  <action>
    **Re-measure first, never arithmetic.** Run the file's own prescribed command:
    `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`. Do not compute the new counts by
    adding task 1's markers to the old ledger — plan 02-12 found a real gap that way round, and the whole
    point of the ledger is that it is measured.

    Update all four pins in one commit:
    1. `MARKER_LEDGER` per-file counts (`:1291-1298`)
    2. `LIVE_UNVERIFIED_TOTAL` (`:1307`)
    3. the committed file-set assertion (`:1422`) — if a file gained its first marker, the set changes
    4. the per-engine lower bound (`:1453`) — pi and opencode now carry GATE-03 markers as well as their
       pre-existing bridge markers

    **Do NOT add the honest-claim pin here (R2-BL3).** The previous draft did, and it named no subject
    file; both possible readings broke. SECURITY.md — the natural subject, and one
    `repo-claims.test.cjs:1606-1624` already pins prose in — does not carry the claim until **wave 7**, so
    a wave-3 pin is red for four waves and fails this plan's own wave gate. The only other reading is the
    comment task 2 writes into `test/engine-parity.test.cjs`, which makes the pin **satisfied by a comment
    it just wrote**: self-certifying, and SECURITY.md stays unpinned. (The prescribed `grep -v '^#'` filter
    guards a Markdown heading; a JavaScript `//` comment sails through it.)

    **The pin is plan 04-19 task 2's, in wave 7**, which owns SECURITY.md and this file in the same
    commit — and its pinned positive set includes the live-refusal clause, which the previous draft's three
    phrases did not cover at all. Write `<the_honest_claim>` into this plan's SUMMARY verbatim, name plan
    04-19 task 2 as the pin's owner, and stop there. A handoff stated is not a gap; a pin that cannot be
    green for four waves is.

    In the SUMMARY, state the before and after ledger counts per file, and note that the count came from a
    live grep in this session rather than from arithmetic over a prior figure.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exits 0.
    - The ledger's per-file counts match a live `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` run in the same session — the SUMMARY shows both.
    - `LIVE_UNVERIFIED_TOTAL` equals the sum of the per-file counts (assert the file's own consistency clause still passes).
    - **No honest-claim clause is added by this plan.** `git diff test/repo-claims.test.cjs` touches the four ledger pins and nothing else. The claim pin is plan 04-19 task 2's, in wave 7, and this plan's SUMMARY names it as the owner. A wave-3 pin over SECURITY.md would be red until wave 7; a wave-3 pin over a comment task 2 just wrote would be self-certifying.
    - The SUMMARY carries `<the_honest_claim>` verbatim and states, in one line, that its pin lands in wave 7 with SECURITY.md.
    - `npm test` — full suite green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs && npm test</automated>
  </verify>
  <done>All four pins reconciled from a live measurement in the same commit as the markers, plus a new clause that makes over-claiming GATE-03 a test failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine shim/plugin → hook socket → main | Six shim engines plus Claude's native path post here. Each shim is a file **inside the agent's own reach**, which is why `gitCommitter.harnessAuthored` byte-compares two of them (`:238-248`). |
| main's verdict → the engine's own permission contract | Every engine expresses deny differently. grok translates, agy translates, kimi may use exit code 2, pi uses `ev.approve()`, OpenCode's mechanism is undocumented here. A mistranslation is a silent allow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CMD-07 | Elevation of privilege | pi's `HIVE_AUTO_APPROVE=1` branch auto-approving a call main denied | mitigate | The branch is gated on the main-side verdict; asserted as a task acceptance criterion |
| T-04-CMD-08 | Elevation of privilege | A mistranslated deny reading as allow on grok or agy | mitigate | `test/engine-parity.test.cjs` exercises grok's decoder with the exact reply main returns, plus an allow control (Drift row 12 corrected D-06's premise here) |
| T-04-CMD-09 | Repudiation | A SUMMARY claiming all four of criterion 1's engines refused | mitigate | `<the_honest_claim>` is the required wording, and task 3 pins it with a both-directions repo-fact clause so inflation is a test failure |
| T-04-CMD-23 | Repudiation | **A pin that asserts only the negative**, so inflation in a direction it does not enumerate — "live-verified on this machine" with no task that runs a live agent, "agy unit-verified" with no test that touches agy — passes. The Phase-1 failure shape with a green check on it | mitigate — **owner is plan 04-19 task 2, wave 7** | The clause asserts **four** literal positive phrases (`through the real HookServer`, `LIVE-UNVERIFIED for pi`, `exercised by no test at all`, and the live-refusal sentence that round 1's BL-11 was actually about) as well as the negatives, cross-checked against plan 04-13 task 4's machine-readable `LIVE GATE-03 REFUSAL: yes\|no` line, with a recorded mutation check. It lands in wave 7 because that is the first wave in which its subject file (SECURITY.md) carries the claim and its pin file is free in the same commit |
| T-04-CMD-30 | Repudiation | **A claim pin with no named subject file.** The previous draft said *"add one new clause to this file"* and never said what the clause read. SECURITY.md does not carry the claim until wave 7, so a wave-3 pin is red for four waves and fails this plan's own wave gate; the only other subject is the comment task 2 writes, which makes the pin self-certifying and leaves SECURITY.md unpinned. And the correction path `04-VALIDATION.md` prescribes — *"corrected downward in the same commit"* — was a D-35 violation by construction, because plan 04-13's `files_modified` held neither `test/engine-parity.test.cjs` nor SECURITY.md | mitigate | The pin moves to plan 04-19 task 2 (wave 7), whose `files_modified` already holds both SECURITY.md and `test/repo-claims.test.cjs`; this plan pins only the ledger, which exists at wave 3. `test/engine-parity.test.cjs` is added to plan 04-13's `files_modified` for wave 4 (this plan owns it in wave 3 — no collision), so the downward correction is a legal same-commit edit |
| T-04-CMD-31 | Elevation of privilege | **"Codex-shaped" defined as whatever satisfies the condition under test.** The previous draft's codex case built a `tool_input.command` **string** because that is what the arm tested for — while codex's `shell` tool takes `command` as an argv array. If it is an array, both arms exit and GATE-03 refuses nothing on the one non-Claude engine installed here, with every test green and the honest claim already saying "refused for Codex-shaped payloads" | mitigate | Plan 04-06's `commandOf` accepts string **or** array; task 2 drives **both** shapes with a non-`Bash` tool name plus a benign control of each, four assertions; and plan 04-13 task 4 rider 4 measures the real payload — name, key and JSON type — from a live interactive codex agent. Plan 04-06 ceiling item (s) names the residual (a different KEY is still unjudged) |
| T-04-CMD-24 | Elevation of privilege | **OpenCode's veto is provably dead as shipped** — `OPENCODE_PLUGIN:484-486` posts `tool_name` and no `tool_input`, so `protectedPathDenial` returns null at `hooks.ts:890` and `commandShapeDenial` never enters — while the marker reads as though the mechanism were merely unverified | mitigate | Task 1 sends `tool_input`, and a test drives an OpenCode-shaped payload through the real `HookServer` and asserts a deny — no OpenCode install needed. The marker is worded to distinguish "cannot verify the mechanism" from "sent nothing to judge" |
| T-04-CMD-10 | Elevation of privilege | kimi expressing BLOCK as exit code 2 where the shim expresses deny via stdout JSON at exit 0 | accept — **open, recorded** | `hiveTemplates.ts:293-297` already records this. kimi is not installed; it is built and LIVE-UNVERIFIED. Named so it is not read as verified |
| T-04-CMD-11 | Elevation of privilege | The shim's connect-error `exit(0)` = allow | accept — **inherited, Deferred** | D-07 hole 2, `hooks.ts:54-62`. Deliberately rejected today because an agent PTY outlives a quit and a fail-closed shim would break every agent whenever the app is legitimately not running. **Restated, never silently carried** |
| T-04-CMD-12 | Elevation of privilege | The user-global engine seeds outside the hive root | accept — **inherited, Deferred** | D-07 hole 1. Restated in plan 04-06's ceiling list; restated again in this SUMMARY |
| T-04-SC | Tampering | npm/pip/cargo installs, or installing an engine CLI to make a claim tickable | mitigate | No install step. Installing grok/pi/opencode/kimi/agy requires paid accounts and is forbidden by PROJECT.md's zero-recurring-cost rule. `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed |
</threat_model>

<verification>
- `node --test test/engine-parity.test.cjs test/repo-claims.test.cjs` — green.
- `npm test` — full suite, 0 failures, skipped count unchanged by this plan.
- `npm run typecheck` — 0 errors. `npm run lint` — exit 0.
- `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` — matches `MARKER_LEDGER` exactly.
</verification>

<success_criteria>
- pi can honour a deny; OpenCode **sends a payload there is something to judge in** — asserted structurally on the parsed constant, not by a grep a comment could satisfy — and has a documented-API veto path; both are marked LIVE-UNVERIFIED with what would settle them, worded so a fixed defect is not laundered as an unverifiable, and with the ESM ceiling (`runShim` cannot drive `OPENCODE_PLUGIN`) stated rather than implied.
- The new-deny behaviour turning OpenCode's judging on is spelled out — four of five shapes ask, which on a non-polling shim means deny — rather than left as a category.
- grok's deny translation is asserted by running the real shim as a child process, with an allow control; codex-shaped payloads are denied **in both command shapes — string and argv array** — proving the arms are keyed on the command's presence rather than on Claude's tool name or on a guessed JSON type; a non-`Bash` payload is denied by the path arm too; the seven-engine reach is derived, not hard-coded.
- The marker ledger is reconciled across all four pins from a live grep, in the same commit as the markers.
- **No claim pin is added in this wave.** The SUMMARY carries `<the_honest_claim>` verbatim, states that D-06's binding wording was substituted and why, names plan 04-19 task 2 (wave 7) as the pin's owner, and restates D-07's two inherited fail-opens.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-10-SUMMARY.md` when done.
</output>
