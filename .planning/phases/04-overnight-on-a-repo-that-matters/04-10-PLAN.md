---
phase: 04-overnight-on-a-repo-that-matters
plan: 10
type: execute
wave: 3
depends_on: ["04-06"]
files_modified:
  - src/main/hiveTemplates.ts
  - test/engine-parity.test.cjs
  - test/repo-claims.test.cjs
autonomous: true
requirements: [GATE-03]
user_setup: []

must_haves:
  truths:
    - "GATE-03's honest claim, written into the SUMMARY and pinned by a test so it cannot be over-stated later: refused for Claude and Codex (live-verified on this machine); built for grok, kimi and agy against their shipped shims and unit-verified through the real `HookServer`, LIVE-UNVERIFIED for want of an installed CLI; built and LIVE-UNVERIFIED for pi and OpenCode."
    - "GATE-03's main-side judging reaches SEVEN engines, not four — `BridgeDescriptor` enumerates six hook shims (agy, codex, pi, opencode, grok, kimi) plus Claude's native `--settings`. `kimi` and `antigravity` are engines CONTEXT.md never mentions."
    - "pi's `post()` becomes request/response for PreToolUse so a deny can be honoured, and its `HIVE_AUTO_APPROVE=1` auto-approve branch is what GATE-03 gates."
    - "OpenCode's `tool.execute.before` veto path is built against the documented API and marked LIVE-UNVERIFIED — the OpenCode clause of criterion 1 is NOT ticked."
    - "`MARKER_LEDGER` is reconciled in the SAME COMMIT as every marker added, across all four of its pins: per-file counts, repo total, committed file set, and the per-engine lower bound."
  artifacts:
    - path: "src/main/hiveTemplates.ts"
      provides: "pi's request/response PreToolUse post and OpenCode's veto path, both LIVE-UNVERIFIED marked"
      contains: "LIVE-UNVERIFIED"
    - path: "test/engine-parity.test.cjs"
      provides: "grok's translator maps a main-side deny to {decision:'deny'}"
    - path: "test/repo-claims.test.cjs"
      provides: "the reconciled marker ledger, all four pins"
      contains: "MARKER_LEDGER"
  key_links:
    - from: "PI_EXTENSION post()"
      to: "the HookServer reply"
      via: "read the response for PreToolUse instead of c.end() fire-and-forget"
      pattern: "PI_EXTENSION"
    - from: "GROK_HOOK_SHIM's reply decoder"
      to: "{decision:'deny'}"
      via: "the existing permissionDecision translator at :779-781"
      pattern: "GROK_HOOK_SHIM|decision"
---

<objective>
GATE-03's reach across engines, and the honesty clause that keeps its SUMMARY true.

**Purpose.** Plan 04-06 built the judge in main. This plan settles which engines actually honour its
verdict, builds the two paths that do not exist yet, and writes the claim down so it cannot inflate.

**RESEARCH corrected CONTEXT.md here, in both directions, and the plan is built against the measurement.**

*Narrower than D-06 says, on live verification:* **only Codex of criterion 1's four engines is installed
on this machine** (`codex-cli 0.128.0`, logged in via ChatGPT). grok, pi, opencode, crush, kimi, agy and
qwen are all absent — `command -v <cli>` probed in the research session. D-06's binding SUMMARY wording
— *"refused for Codex and Grok (live-verified)"* — is **not achievable here** without installing grok and
buying an xAI key, which the zero-recurring-cost constraint forbids. Writing it anyway would be the exact
over-claim this project bans.

*Wider than D-06 says, on reach:* `BridgeDescriptor` (`src/shared/agentProvider.ts:71`) enumerates **six**
hook shims — `agy | codex | pi | opencode | grok | kimi` — plus Claude's native `--settings`. Main-side
judging therefore covers **seven** engines, and **`kimi` and `antigravity` (agy) are two CONTEXT.md never
mentions**. That is five more engines than `AGENT_DENY_RULES` reaches, and it is the truth.

*And D-06's own premise was partly wrong:* **grok does not reuse `HOOK_SHIM`.** It has its own translator,
`GROK_HOOK_SHIM` at `hiveTemplates.ts:730` (written at `hiveProvisioning.ts:420`), with its own 5 s exit at
`:795` and its own `JSON.parse(resp)` reply decoder at `:772-784`. That matters enormously for plan 04-16
(L-02) and it matters here because grok's deny path is a *translation*, not a passthrough, and must be
asserted as one.

**D-03 stands: `AGENT_DENY_RULES` is not deleted and not the answer.** `hiveProvisioning.ts:63` stays
exactly as it is. It is the only rule surface that survives `--permission-mode bypassPermissions` inside
Claude's own process, so it fires before a hook round trip — real defence in depth. But **no success
criterion may be marked green on its behaviour**, because it is written into exactly one engine's settings
file.

**Output.** pi's request/response path, OpenCode's veto path, both marked; grok's translator asserted; the
marker ledger reconciled across all four pins.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-01-SUMMARY.md
</context>

<the_honest_claim>
**This exact wording goes into the SUMMARY. It replaces D-06's binding wording, which the machine cannot
support, and the substitution is stated rather than performed silently:**

> GATE-03 is refused for **Claude and Codex** (live-verified on this machine); **built for grok, kimi and
> agy** against their shipped shims and unit-verified through the real `HookServer`, **LIVE-UNVERIFIED**
> for want of an installed CLI; and **built and LIVE-UNVERIFIED for pi and OpenCode**. The qwen and crush
> proxy tiers have no tool-call boundary to gate at (`hooks.ts:60-62`) and are out of scope. copilot has
> no bridge at all.

**Criterion 1's OpenCode clause is NOT ticked. Criterion 1's grok and pi clauses are NOT ticked.** A
SUMMARY that claims all four verbatim is the failure mode this plan exists to prevent.
</the_honest_claim>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step. **Do not install grok, pi,
  opencode, kimi or agy to make a claim tickable** — every one needs its own paid subscription, and
  PROJECT.md's zero-recurring-cost rule is absolute. "Verification needs an account we do not have" is a
  first-class outcome here, not a failure.
- **D-35 — `src/main/hiveTemplates.ts` line ranges.** This plan owns **`:291-800`** in wave 3.
  `TASK_CLI` (`:24-290`) was plan 04-04's in wave 1. Plan 04-16 owns `:291-800` in wave 5 for the poll
  loops. **This plan does not add a poll loop** — that is GATE-05's and S-2 sequences it after this.
- **D-35 — `test/repo-claims.test.cjs`.** This plan owns it in wave 3. Plan 04-13 owns it in wave 4 and
  plan 04-19 in wave 7. S-8: the ledger is a four-way pin and two plans editing it in one wave is a
  genuine shared-line conflict, not a distance problem. Never edit it out of turn.
- **This plan does not touch `src/main/hooks.ts`** — plan 04-06 owned it in wave 2 and plan 04-15 owns it
  in wave 4.
- **D-33 / D-40** — assert a positive lower bound alongside every negative.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pi's request/response, and OpenCode's veto — both built, both marked</name>
  <files>src/main/hiveTemplates.ts</files>
  <behavior>
    - `PI_EXTENSION`'s `post()` reads the server's reply for `PreToolUse` and returns the verdict through pi's `ev.approve()` contract; for every other event it stays fire-and-forget.
    - pi's `HIVE_AUTO_APPROVE === '1'` branch no longer auto-approves a call main denied.
    - `OPENCODE_PLUGIN`'s `tool.execute.before` awaits main's verdict and vetoes on a deny, against the documented API.
    - Both carry a `LIVE-UNVERIFIED` marker naming what could not be verified and why.
    - A connect error still `process.exit(0)` / resolves-as-allow — D-08 clause 3's deliberate fail-open is preserved verbatim.
  </behavior>
  <read_first>
    - `src/main/hiveTemplates.ts:425-465` — `PI_EXTENSION` in full: `post()` doing
      `c.end(JSON.stringify(payload))` with **no reply read** at `:436-437`, and the `tool_call` handler
      with `ev.approve()` at `:443-446` whose **return value is honoured**. Also the
      `HIVE_AUTO_APPROVE === '1'` branch — *"that branch is precisely what GATE-03 must gate"* (D-06).
    - `src/main/hiveTemplates.ts:466-500` — `OPENCODE_PLUGIN`: `tool.execute.before` already `async` at
      `:490`, posting fire-and-forget and returning nothing; and the existing
      `LIVE-UNVERIFIED (plugin auto-load + session.idle firing need BYOK keys)` marker
    - `src/main/hiveTemplates.ts:298-347` — `HOOK_SHIM`: the reply read at `:342-345`, the 5 s exit at
      `:347`, and **`c.on('error', () => process.exit(0))`** — the line that makes "the floor is down"
      allow. **This must be preserved verbatim in every shim this plan touches** (D-08 clause 3).
    - `src/main/hiveTemplates.ts:730-795` — `GROK_HOOK_SHIM`: the reply decoder at `:772-784` and the
      `permissionDecision:'deny'` → `{decision:'deny'}` translation at `:779-781`
    - `src/main/hooks.ts:54-62` — the two inherited holes (D-07), including the shim fail-open, which is
      **deliberate and Deferred** and must be restated rather than fixed here
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Cross-Engine Reality (the
      per-engine table), Assumptions **A6** (pi honouring an async return — unsettleable without a pi
      install) and **A7** (OpenCode's veto — already ruled LIVE-UNVERIFIED by D-06)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § `src/main/hiveTemplates.ts`
  </read_first>
  <action>
    **pi.** Make `PI_EXTENSION`'s `post()` a request/response **for `PreToolUse` only** — every other event
    keeps today's fire-and-forget shape, because widening them costs latency on paths that have no verdict
    to wait for. Feed the verdict into the `tool_call` handler's `ev.approve()` contract at `:443-446`,
    whose return value pi already honours. Gate the `HIVE_AUTO_APPROVE === '1'` branch so it cannot
    auto-approve a call main denied — that branch is exactly what GATE-03 exists to intercept, and leaving
    it means the built path is decorative.

    Assumption A6 (does pi honour an **async** return?) cannot be settled without a pi install and a key.
    Build against the documented contract and say so in the marker.

    **OpenCode.** `tool.execute.before` is already `async` (`:490`). Await main's verdict and veto on a
    deny, against OpenCode's documented plugin API. Whether returning or throwing is the veto mechanism is
    what A7 marks unverified; implement the documented form and mark it.

    **Both:** preserve the connect-error fail-open verbatim. A shim that fails closed would break every
    agent whenever the app is legitimately not running, which is why `hooks.ts:54-62` records it as
    deliberate and why CONTEXT.md's Deferred list explicitly rejects fixing it here.

    **Markers.** Each new path gets a `LIVE-UNVERIFIED` marker in the house register — naming the engine,
    what is unverified, and what would settle it (an installed CLI plus an account). Do not write a vague
    marker; `test/repo-claims.test.cjs` counts them and a future reader needs to know what to re-run.

    **Do not add a poll loop to any shim.** GATE-05's `hive_ask` reply does not exist yet (plan 04-15) and
    the poll is plan 04-16's. S-2 sequences GATE-03's shim changes **before** GATE-05's poll loop
    deliberately.
  </action>
  <acceptance_criteria>
    - `sed -n '425,500p' src/main/hiveTemplates.ts | grep -c 'LIVE-UNVERIFIED'` increases by exactly the number of markers added, and the number is stated in the SUMMARY before task 3 runs.
    - Every shim this task touches still contains `process.exit(0)` on the connect-error path — `grep -c "on('error'" src/main/hiveTemplates.ts` is unchanged or higher, never lower.
    - `grep -c 'HIVE_AUTO_APPROVE' src/main/hiveTemplates.ts` ≥ 1 and the branch is textually gated on the main-side verdict.
    - No shim gains a retry/poll loop: `grep -ciE 'ApprovalPoll|hive_ask' src/main/hiveTemplates.ts` returns `0`.
    - `npm run typecheck` → 0 errors.
    - `git diff src/main/hiveTemplates.ts` is confined to `:291-800`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <done>pi can honour a deny and OpenCode has a veto path, both built against documented APIs, both honestly marked, and neither has broken the deliberate connect-error fail-open.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assert grok's translator, and the seven-engine reach</name>
  <files>test/engine-parity.test.cjs</files>
  <behavior>
    - Feeding `GROK_HOOK_SHIM`'s reply decoder a main-side `{hookSpecificOutput:{permissionDecision:'deny', permissionDecisionReason:'…'}}` yields `{decision:'deny'}` carrying the reason.
    - Feeding it an allow (`{}`) yields no deny — the positive control.
    - `bridgeOf` reports a hook bridge for all six shim engines plus Claude's native path: seven engines main-judges.
    - The two proxy-tier engines (qwen, crush) report no tool-call boundary, and copilot reports no bridge — asserted so the seven is a measured number, not a claim.
  </behavior>
  <read_first>
    - `src/main/hiveTemplates.ts:730-795` — `GROK_HOOK_SHIM`, and specifically the decoder at `:772-784`
      and the translation at `:779-781`
    - `src/shared/agentProvider.ts:71` — `BridgeDescriptor` and its six-shim union
      `'agy' | 'codex' | 'pi' | 'opencode' | 'grok' | 'kimi'`; `:229-235` Claude's native `--settings`;
      `:287` codex; `:320` grok; `:329`/`:351` kimi; `:366`/`:377` antigravity/agy; `:404` qwen (proxy);
      `:440` opencode; `:509` crush (proxy); `:556` pi; `:574-599` copilot (no bridge); `:688-691` `bridgeOf`
    - `src/main/hooks.ts:60-62` — the record that pi, opencode and the qwen proxy tier have no PreToolUse
      hook, and that the proxy tier has **no tool-call boundary at all**
    - `test/engine-parity.test.cjs` — the existing per-engine assertions; extend them
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Cross-Engine Reality and
      Drift Report rows **12** and **13**
  </read_first>
  <action>
    Extend `test/engine-parity.test.cjs` with a GATE-03 block.

    The grok case is the important one, and it must exercise the **translator**, not a passthrough: build
    the exact reply object main returns for a `commandShapeDenial` verdict, run it through
    `GROK_HOOK_SHIM`'s decoding logic, and assert the result is grok's own `{decision:'deny'}` shape
    carrying the reason string. D-06 asserted grok "reuses `HOOK_SHIM`'s response contract" and Drift row
    12 corrected it — this test is what stops that error recurring.

    Add the positive control: an allow reply produces no deny. Without it the case passes against a
    decoder that denies everything.

    Add a reach assertion derived from `bridgeOf` and the preset table, **computed rather than hard-coded**:
    count the engines with a hook bridge plus Claude's native path and assert it equals the number of
    engines main can judge; separately assert that the two proxy engines and copilot are excluded, naming
    each. A hard-coded `7` would go stale the moment a preset changes; a derived count with named
    exclusions stays true and fails loudly when the roster moves.

    Add a comment in the block quoting `<the_honest_claim>` above, so the next reader of this file finds
    the claim beside the assertions that support it.
  </action>
  <acceptance_criteria>
    - `node --test test/engine-parity.test.cjs` exits 0 with at least four new cases.
    - The grok deny case asserts the reason string survives the translation, not merely that a deny appeared.
    - The allow control case is present and asserts absence of a deny **and** a well-formed decode.
    - The reach count is derived from the preset table (`grep -c '=== 7' test/engine-parity.test.cjs` returns `0` — no magic number).
    - The excluded engines are named individually: `qwen`, `crush`, `copilot`.
    - The honest-claim comment is present in the file.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>Grok's deny translation is pinned, the seven-engine reach is a derived measurement with named exclusions, and the honest claim lives beside its evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the marker ledger — all four pins, same commit</name>
  <files>test/repo-claims.test.cjs</files>
  <read_first>
    - `test/repo-claims.test.cjs:1249-1262` — the block explaining why the marker clause reads **raw** text
      while most clauses read stripped-of-comments text. Read it before changing anything; it is the
      reason the ledger works at all.
    - `test/repo-claims.test.cjs:1287-1289` — the re-measure command the file itself prescribes:
      `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`
    - `test/repo-claims.test.cjs:1291-1298` — `MARKER_LEDGER`, currently
      `{hive.ts:3, hiveProvisioning.ts:5, hiveTemplates.ts:3, index.ts:1, webhook.ts:3, agentProvider.ts:3}`
    - `test/repo-claims.test.cjs:1307` — `LIVE_UNVERIFIED_TOTAL = 18`
    - `test/repo-claims.test.cjs:1419-1454` — the file-set assertion (`:1422`) and the per-engine lower
      bound (`:1453`)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` **S-8** and **L-10** — the pin
      is four-way and all four must be reconciled in the SAME commit as any marker change
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § Collision Map **C**
  </read_first>
  <action>
    **Re-measure first, never arithmetic.** Run the file's own prescribed command:
    `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`. Do not compute the new counts by
    adding task 1's markers to the old ledger — plan 02-12 found a real gap that way round, and the whole
    point of the ledger is that it is measured.

    Update all four pins in one commit:
    1. `MARKER_LEDGER` per-file counts (`:1291-1298`)
    2. `LIVE_UNVERIFIED_TOTAL` (`:1307`)
    3. the committed file-set assertion (`:1422`) — if a file gained its first marker, the set changes
    4. the per-engine lower bound (`:1453`) — pi and opencode now carry GATE-03 markers as well as their
       pre-existing bridge markers

    Add one new clause to this file, in its established both-directions style, pinning **the honest GATE-03
    claim itself** so a later SUMMARY cannot inflate it: assert that the phrase describing live
    verification names Claude and Codex and does **not** name grok, pi or OpenCode as live-verified.
    Follow `:1249-1262`'s guidance on raw-vs-stripped text and D-33's both-directions rule — assert a
    positive lower bound (the claim is present and non-empty) alongside the negative (the over-claim is
    absent), so deleting the claim fails the test instead of satisfying it.

    In the SUMMARY, state the before and after counts per file, and note that the count came from a live
    grep in this session rather than from arithmetic over a prior figure.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exits 0.
    - The ledger's per-file counts match a live `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` run in the same session — the SUMMARY shows both.
    - `LIVE_UNVERIFIED_TOTAL` equals the sum of the per-file counts (assert the file's own consistency clause still passes).
    - The new honest-claim clause asserts both directions: the claim's presence AND the absence of a live-verified assertion for grok / pi / OpenCode.
    - The clause uses `grep -v '^#'`-equivalent filtering or the raw-text discipline `:1249-1262` prescribes — never a bare `== 0` gate on unfiltered text.
    - `npm test` — full suite green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs && npm test</automated>
  </verify>
  <done>All four pins reconciled from a live measurement in the same commit as the markers, plus a new clause that makes over-claiming GATE-03 a test failure.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| engine shim/plugin → hook socket → main | Six shim engines plus Claude's native path post here. Each shim is a file **inside the agent's own reach**, which is why `gitCommitter.harnessAuthored` byte-compares two of them (`:238-248`). |
| main's verdict → the engine's own permission contract | Every engine expresses deny differently. grok translates, agy translates, kimi may use exit code 2, pi uses `ev.approve()`, OpenCode's mechanism is undocumented here. A mistranslation is a silent allow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CMD-07 | Elevation of privilege | pi's `HIVE_AUTO_APPROVE=1` branch auto-approving a call main denied | mitigate | The branch is gated on the main-side verdict; asserted as a task acceptance criterion |
| T-04-CMD-08 | Elevation of privilege | A mistranslated deny reading as allow on grok or agy | mitigate | `test/engine-parity.test.cjs` exercises grok's decoder with the exact reply main returns, plus an allow control (Drift row 12 corrected D-06's premise here) |
| T-04-CMD-09 | Repudiation | A SUMMARY claiming all four of criterion 1's engines refused | mitigate | `<the_honest_claim>` is the required wording, and task 3 pins it with a both-directions repo-fact clause so inflation is a test failure |
| T-04-CMD-10 | Elevation of privilege | kimi expressing BLOCK as exit code 2 where the shim expresses deny via stdout JSON at exit 0 | accept — **open, recorded** | `hiveTemplates.ts:293-297` already records this. kimi is not installed; it is built and LIVE-UNVERIFIED. Named so it is not read as verified |
| T-04-CMD-11 | Elevation of privilege | The shim's connect-error `exit(0)` = allow | accept — **inherited, Deferred** | D-07 hole 2, `hooks.ts:54-62`. Deliberately rejected today because an agent PTY outlives a quit and a fail-closed shim would break every agent whenever the app is legitimately not running. **Restated, never silently carried** |
| T-04-CMD-12 | Elevation of privilege | The user-global engine seeds outside the hive root | accept — **inherited, Deferred** | D-07 hole 1. Restated in plan 04-06's ceiling list; restated again in this SUMMARY |
| T-04-SC | Tampering | npm/pip/cargo installs, or installing an engine CLI to make a claim tickable | mitigate | No install step. Installing grok/pi/opencode/kimi/agy requires paid accounts and is forbidden by PROJECT.md's zero-recurring-cost rule. `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed |
</threat_model>

<verification>
- `node --test test/engine-parity.test.cjs test/repo-claims.test.cjs` — green.
- `npm test` — full suite, 0 failures, skipped count unchanged by this plan.
- `npm run typecheck` — 0 errors. `npm run lint` — exit 0.
- `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` — matches `MARKER_LEDGER` exactly.
</verification>

<success_criteria>
- pi can honour a deny; OpenCode has a documented-API veto path; both are marked LIVE-UNVERIFIED with what would settle them.
- grok's deny translation is asserted with an allow control; the seven-engine reach is derived, not hard-coded.
- The marker ledger is reconciled across all four pins from a live grep, in the same commit as the markers.
- The SUMMARY carries `<the_honest_claim>` verbatim, states that D-06's binding wording was substituted and why, and restates D-07's two inherited fail-opens.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-10-SUMMARY.md` when done.
</output>
