---
phase: 04-overnight-on-a-repo-that-matters
plan: 20
type: execute
wave: 5
depends_on: ["04-02", "04-06", "04-15"]
files_modified:
  - src/main/floor/boot.ts
  - test/boot-floor.test.cjs
autonomous: true
requirements: [GATE-03, GATE-05, RECORD-01]
user_setup: []

must_haves:
  truths:
    - "The PRODUCTION `HookServer` — the one `bootFloor` constructs at `boot.ts:1119`, not one a test built — has its host allowlist, its ask opener, its approval publisher and its tool-call recorder all supplied."
    - "That is proven by booting a real floor through `bootFloor(deps)` and observing each seam's EFFECT: a config-driven host denial, a real `hive_ask`, a real row in `floor.persist`, and a real publish — not by grepping the argument list."
    - "The ask TTL the production registry uses is `ASK_TTL_MS`, read from `hiveProvisioning.ts`, so it stays derived from the engine hook timeout rather than defaulting to whatever the class was constructed with."
    - "`boot.ts` still contains exactly ONE `new HookServer(` — `test/hive-durability.test.cjs:186-193` pins that, and this plan adds arguments rather than a second construction."
  artifacts:
    - path: "src/main/floor/boot.ts"
      provides: "the four wave-2/wave-4 seams supplied at the sole production HookServer construction"
      contains: "recordToolCall"
    - path: "test/boot-floor.test.cjs"
      provides: "the production-wiring cases — each seam observed through a real bootFloor, by its effect"
  key_links:
    - from: "boot.ts:1119-1124 new HookServer(...)"
      to: "readConfig().hostAllowlist"
      via: "an optional trailing getter argument declared by plan 04-06"
      pattern: "hostAllowlist"
    - from: "boot.ts:1119-1124 new HookServer(...)"
      to: "persist.recordToolCall"
      via: "a thunk read at call time — `persist` is assigned at :1148, AFTER this construction"
      pattern: "recordToolCall"
---

<objective>
The composition root. Four seams were declared in waves 2 and 4 as optional trailing constructor arguments
and nothing has supplied them. This plan supplies them, and proves it by booting a real floor.

**Purpose — this is the phase's one systemic defect, given its own plan because it deserved one.** The
red-team round found that `files_modified` had been verified for collisions but never for **completeness
against what each plan's own prose said it must change**, and `src/main/floor/boot.ts` — the sole
production construction site for every seam this phase adds — was in no plan's scope for any of them. The
consequence is specific and it is the shape D-28 already burned this project once: plan 04-06's host
allowlist, plan 04-15's `ApprovalRegistry` and its `recordToolCall` writer can all ship with green unit
tests, driven by `HookServer` instances the tests constructed themselves, while the floor an operator
actually runs has none of them. **Green and dead.**

**Why the seams are optional and trailing, and why that makes this plan necessary rather than redundant.**
`hooks.ts:554-561` states the house rule for a new `HookServer` dependency in its own words: *"Optional so
the server still runs without it, exactly like `breaker` and `control` above — and LAST, so no existing
call site or test has to move an argument."* There are **eight** `new HookServer(` sites across `src/` and
`test/`, most passing three to five positional arguments, and `test/hive-durability.test.cjs:186-193` pins
that `boot.ts` holds exactly one of them. Converting the constructor to a dep object would touch all eight
in the repo's most security-sensitive file for no requirement. So the seams are appended, optional — and
optional means **nothing fails if they are never wired**. That is exactly why the wiring needs an owner, a
wave, and a test that fails when it is missing.

**Why wave 5 and not earlier.** A declaration and its sole consumer cannot sit in two parallel plans: under
`use_worktrees: false` both edit one tree and whichever lands first sees the other's absence as a typecheck
error, and every plan in this phase carries `typecheck → 0 errors` as a hard criterion. Plan 04-06 declares
`hostAllowlist` and `openAsk` in wave 2; plan 04-15 declares `recordToolCall` and `publishApproval` and
supplies `openAsk`'s implementation in wave 4. Wave 5 is the first wave in which all four exist and
`boot.ts` has no other owner (plan 04-03 held it in wave 1, plan 04-09 in wave 2, plan 04-11 in wave 3).

**What is NOT here, and why.** `blocked` was wired at wave 1 by plan 04-03 — it is a **required**
`DeliveryDeps` member, so `boot.ts` fails typecheck until it is supplied, and typecheck pressure is a better
enforcement mechanism than a plan. `envPassThrough` rides `SpawnOptions` from `index.ts`'s
`spawnAgentCore` (plan 04-05, wave 1) rather than through a `PtyManager` setter, so it needs no
composition-root line at all. `src/main/floor/deps.ts` needs **no change this phase**: plan 04-08's ledger
writer reaches the hive through `deps.hive`, which `agentTeardownDeps()` (`boot.ts:254-261`) has always
carried. Plan 04-17 and plan 04-18 reach the approval registry through `floor.hookServer.openApprovals()`
and `.answerApproval()`, and `hookServer` has been on `Floor` since Phase 2 (`boot.ts:1160`) — so neither
needs a `Floor` interface change either. All four of those are stated here so the next reader knows they
were considered rather than missed.

**Output.** Four arguments at `boot.ts:1119-1124`, and the boot test that fails if any of them is dropped.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-06-SUMMARY.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-15-SUMMARY.md
</context>

<interfaces>
The four optional trailing arguments plans 04-06 and 04-15 declared on `HookServer`'s constructor, in
declaration order after `recordCost`. Read the shipped signatures rather than trusting this block — it is
a map, not the territory.

```ts
// declared by plan 04-06 (wave 2)
hostAllowlist?: () => readonly string[];
openAsk?: (a: { agentId: string; tool: string; command: string; reason: string }) => unknown;

// declared by plan 04-15 (wave 4)
recordToolCall?: (row: {
  agentId: string; ts: number; tool: string;
  target: string | null; decision: 'allow' | 'deny' | 'ask'; reason?: string;
}) => void;
publishApproval?: (open: PendingApproval[]) => void;
```
</interfaces>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step.
- **D-35 — this plan owns `src/main/floor/boot.ts` `:1119-1124`** (the `new HookServer(...)` argument list)
  and `test/boot-floor.test.cjs` in wave 5. It owns **no other region of `boot.ts`**: `:150-180` and
  `:928-956` (the timer block) belong to plans 04-09 and 04-11, and `:1056-1117` (the `DeliveryService`
  dep object) to plan 04-03. `git diff` must show the argument list and nothing else.
- **`test/hive-durability.test.cjs:186-193` pins that `boot.ts` contains exactly ONE `new HookServer(`.**
  This plan adds arguments to that one construction. It does not add a second, and it does not move the
  existing one.
- **This plan touches no other source file.** If a seam turns out to need a change in `hooks.ts`,
  `approvals.ts`, `config.ts` or anywhere else, that is a finding to report against the plan that declared
  it — not a drive-by fix in the wiring plan. `hooks.ts` had one owner per wave for a reason.
- **D-33 / D-40** — assert a positive lower bound alongside every negative. For this plan specifically: a
  test that greps the argument list proves the text, not the wiring. Every assertion here must observe an
  **effect** through a really-booted floor.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Supply the four arguments at the one production construction site</name>
  <files>src/main/floor/boot.ts</files>
  <read_first>
    - `src/main/floor/boot.ts:1119-1124` — `hookServer = new HookServer(hive, () => fakeWebContents(),
      () => readConfig(), control, breaker, (agentId) => delivery.drainAtStop(agentId),
      (agentId) => deps.focus?.(agentId), (s) => telemetry.recordCostSample(s));` — eight positional
      arguments today. Count them yourself before adding.
    - `src/main/hooks.ts:542-562` — the constructor's parameter list and the doc block on `recordCost` at
      `:554-561`, which states the append-optional-LAST rule and why. Read the four new parameters as they
      actually shipped in waves 2 and 4; the `<interfaces>` block above is a map, not the territory.
    - `src/main/floor/boot.ts:1148` — `persist = new PersistStore()`, and `:1155` — `persist.open()`.
      **Both are AFTER `:1119`.** So the `recordToolCall` argument must be a closure that reads the
      module-scope `let` at CALL time, not a value captured at construction time.
    - `src/main/floor/boot.ts:985-1000` — the `handoff` closure's comment, which is the in-file precedent
      for exactly that: *"`delivery` is a module-scope `let`, not yet assigned this early in bootFloor's
      body, but this closure only ever RUNS during message routing (well after boot completes), so it
      always sees the real instance by the time it fires."* Copy that reasoning and cite it.
    - `src/main/floor/boot.ts:411-413` — the `readConfig().notifications` gate every other toast honours.
      `publishApproval`'s notification half must honour it too; its data half (what plan 04-17's
      `openAsks()` reads) must not, because a pull is not a notification.
    - `src/main/floor/boot.ts:1160` — the `Floor` return object. Confirm for yourself that `hookServer` is
      already on it: that is why plans 04-17 and 04-18 need no change here.
    - `src/main/hiveProvisioning.ts` — `ASK_TTL_MS` and `PRETOOLUSE_HOOK_TIMEOUT_SEC` as plan 04-15
      shipped them, and the reason the TTL is derived rather than written.
    - `src/main/config.ts` — the host-allowlist key plan 04-06 added and its default.
    - `test/hive-durability.test.cjs:186-193` — the clause pinning the single `new HookServer(` call.
  </read_first>
  <action>
    Append the four arguments to the existing `new HookServer(...)` call at `boot.ts:1119-1124`, in the
    order `hooks.ts`'s constructor declares them. Do not reorder anything, do not add a second
    construction, and do not convert the call to a dep object.

    1. **`hostAllowlist`** → `() => readConfig().hostAllowlist ?? DEFAULT_HOST_ALLOWLIST`. Read it through
       `readConfig()` at call time, the way `:1121` already reads config, so an operator's Settings edit
       takes effect without a restart. Plan 04-06's judge already falls back to the default when the getter
       is absent; supplying it is what makes the operator's list reachable at all.

    2. **`openAsk`** → the handler plan 04-15 built on `HookServer`. If plan 04-15 made it a method on the
       server itself, this argument may be unnecessary — **read `04-15-SUMMARY.md` and the shipped
       signature before writing a line**, and if the seam turned out not to need a composition-root
       argument, say so in the SUMMARY and assert its absence rather than inventing a use for it. A wiring
       plan that wires something already wired is a diff with a comment attached.

    3. **`recordToolCall`** → `(row) => { try { persist.recordToolCall(row); } catch (e) {
       console.error('[db] recordToolCall failed:', e); } }`. **A closure, not `persist.recordToolCall`
       bound eagerly**: `persist` is a module-scope `let` assigned at `:1148`, twenty-nine lines *after*
       this construction, so an eager reference captures `undefined`. `boot.ts:995-1000`'s `handoff`
       comment is the in-file precedent for this exact hazard — cite it in a comment here so the next
       reader does not "simplify" the closure away. Swallow and log: plan 04-15's contract is that a
       recording failure costs a row and never a verdict, and this is the outermost place that promise can
       be broken.

    4. **`publishApproval`** → a function that (a) hands the open list to whatever plan 04-17 reads and
       (b) raises the operator's attention through the already-wired `deps.notify`, **gated on
       `readConfig().notifications` exactly as `boot.ts:411-413` gates every other toast**. An operator
       who turned notifications off did not ask for this one either. The data half is not gated: plan
       04-17's `GET /phone/api/asks` is a pull, and a pull the operator asked for is not a notification.

    Add one short comment above the argument list naming the four seams, the plans that declared them, and
    the reason they are optional (`hooks.ts:554-561`). A reader arriving at twelve positional arguments
    deserves to know which four are new and why they were not made a dep object.

    **Change nothing else in `boot.ts`.** Not the timer block, not `SHUTDOWN_STEPS`, not the
    `DeliveryService` dep object, not the `Floor` return.
  </action>
  <acceptance_criteria>
    - `grep -c 'new HookServer(' src/main/floor/boot.ts` returns `1`, unchanged — `test/hive-durability.test.cjs` stays green.
    - `sed -n '1115,1140p' src/main/floor/boot.ts | grep -c 'hostAllowlist'` returns `1`, and the same for `recordToolCall` and `publishApproval`. (`openAsk` is asserted only if plan 04-15 left it as a constructor argument — see the action.)
    - The `recordToolCall` argument is a **closure**, not a bound method reference: `sed -n '1115,1140p' src/main/floor/boot.ts | grep -c 'persist.recordToolCall'` returns `1` and it appears inside an arrow function body, with the `:995-1000` precedent cited in a comment.
    - The toast half of `publishApproval` is gated on `readConfig().notifications` — the same expression `boot.ts:411-413` uses.
    - `git diff src/main/floor/boot.ts` touches only lines between the start and end of the `new HookServer(` call. `:150-180`, `:928-956`, `:1056-1117` and the `Floor` return are all untouched.
    - `git diff --stat src/main/hooks.ts src/main/approvals.ts src/main/config.ts src/main/hiveProvisioning.ts src/main/floor/deps.ts` — all **empty**.
    - `npm run typecheck` → 0 errors in both projects.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node --test test/hive-durability.test.cjs</automated>
  </verify>
  <done>The one production `HookServer` is constructed with every seam this phase added, through a closure that survives boot ordering and a notification gate that respects the operator's setting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove it on a real floor — each seam observed by its effect, not by its argument</name>
  <files>test/boot-floor.test.cjs</files>
  <behavior>
    - A floor booted through `bootFloor(deps)` with a config whose `hostAllowlist` excludes `evil.example` denies a `PreToolUse` for `curl https://evil.example/x`; the same floor with the host added allows it. The verdict changes with the CONFIG, which is what proves the getter is wired rather than that a default happened to deny.
    - The same floor, given a `PreToolUse` for `git push origin +main`, returns a reply carrying `hive_ask.id`.
    - After a `PreToolUse` for a `Write` with a `file_path`, `floor.persist` holds a `tool_calls` row with that `target` non-null and the token-derived `agent_id`.
    - Opening an ask calls the publisher; with `notifications: false` in config the toast half does not fire and the data half still does.
    - Every one of the four assertions fails if its argument is removed from the construction.
  </behavior>
  <read_first>
    - `test/boot-floor.test.cjs:71-85` — the per-test isolated `(userData, harnessHome)` pattern and
      `fakeDeps(env)`; `:178-190` — the case that connects to `floor.hive.sockPath()` **on win32**, which
      is the proof that a real floor's socket is reachable here without a platform gate.
    - The `restorePointTimer` by-name pin plan 04-09 added in wave 2 and the `watchdogTimer` pin plan 04-11
      added in wave 3 — the in-file precedent for asserting something a boot-internal `let` hides.
    - `src/main/floor/boot.ts:1160` — the `Floor` return object: `hookServer`, `persist` and `hive` are all
      on it, which is how this test reaches each seam's effect without touching `boot.ts`'s internals.
    - `test/gate-harness.cjs` (plan 04-01) — **read it, then decide.** This test does not need a child
      process: it holds the real `HookServer` directly off `floor.hookServer` and can call `handle()` on a
      payload `authorized()` has accepted. Driving a shim would prove the loop, which
      `test/gate03-roundtrip.test.cjs` already does; **this file's job is the composition**, which is a
      different fact. Say which you chose and why.
    - `src/main/db.ts` — `toolCalls({agentId})` as plan 04-02 shipped it, for reading the row back.
    - `src/main/commandShape.ts` — `DEFAULT_HOST_ALLOWLIST`, so the config-driven case can pick a host that
      is genuinely outside it.
  </read_first>
  <action>
    Extend `test/boot-floor.test.cjs` with a `composition root` block, RED first — run it before task 1's
    edit and confirm every case fails for the right reason (a missing seam, not a missing import), and
    paste that output into the SUMMARY.

    **Every assertion observes an effect through a really-booted floor.** A grep over `boot.ts`'s argument
    list proves the text is present; it does not prove the argument reaches the judge, survives boot
    ordering, or reads the operator's config. That distinction is the whole reason this plan exists, so do
    not weaken it into a source-text assertion.

    Four cases:

    1. **The host allowlist is the OPERATOR's, not the default.** Boot a floor whose `readConfig()` returns
       a `hostAllowlist` **containing** a synthetic host, drive a `PreToolUse` for a `curl` at that host
       through `floor.hookServer`, and assert **allow**. Then boot a second floor whose config omits it and
       assert **deny**. The verdict must change with the config — that is what distinguishes "the getter is
       wired" from "`DEFAULT_HOST_ALLOWLIST` happened to answer". Pick a host that is not in
       `DEFAULT_HOST_ALLOWLIST` so the two runs genuinely differ.

    2. **A real ask, from the production server.** Drive a `PreToolUse` for `git push origin +main` and
       assert the reply carries `hive_ask.id` and a `deadlineMs` in the future. Assert in the same case
       that `rm -rf ./x` comes back a **bare deny with no `hive_ask`** — otherwise a server that answered
       everything with an ask would pass.

    3. **A real row, in the floor's own store.** Drive a `PreToolUse` for a `Write` with a `file_path`,
       then read `floor.persist` back and assert one `tool_calls` row whose `target` is that path
       (non-null, strictly) and whose `agent_id` is the token-derived id — **not** an id placed in the
       payload body. Include the forged-id negative: a payload claiming a different `agent_id` still
       records the derived one. This is the assertion that proves the closure at `:1119` saw a `persist`
       that was assigned twenty-nine lines later.

    4. **The publisher, and the notification gate.** Open an ask on a floor with `notifications: true` and
       assert the fake `deps.notify` was called; open one on a floor with `notifications: false` and assert
       it was **not**, while `floor.hookServer.openApprovals()` still returns the entry. Both halves,
       because a publisher that never fires and a publisher that ignores the gate both pass a one-sided
       test.

    Add a fifth, cheap assertion in the same block: `floor.hookServer` is the same object the `Floor`
    return exposes, and `floor.persist.isOpen` is true — a positive lower bound, so a `bootFloor` that
    threw halfway cannot satisfy the four negatives above.

    Not POSIX-gated, no `{ skip: … }`. This file already runs on win32 (`:178-190` connects to a named pipe
    with no gate), so `test/suite-integrity.test.cjs`'s clause-3 census and clause-3b floor both stay
    unchanged.
  </action>
  <acceptance_criteria>
    - `node --test test/boot-floor.test.cjs` exits 0 with at least five new cases, on win32, zero skipped.
    - The host case asserts **two different verdicts from two different configs** — not one deny.
    - The ask case asserts `hive_ask.id` present for a force-push AND absent for `rm -rf`, in the same case.
    - The row case asserts `target` non-null strictly, and that a forged `agent_id` in the payload does not reach the row.
    - The publisher case asserts both the gated and the ungated half.
    - **Every case was seen RED** against the tree before task 1's edit, and the RED output is in the SUMMARY. A composition test that has never been seen failing is a test that would not notice a dropped argument.
    - `grep -c 'skip' test/boot-floor.test.cjs` is unchanged from before this plan, and `node --test test/suite-integrity.test.cjs` is green with `FROZEN` unchanged.
    - `npm test` → 0 failures; skipped count unchanged at the re-measured baseline. `npm run lint` → exit 0.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/boot-floor.test.cjs && node --test test/suite-integrity.test.cjs && npm test && npm run lint</automated>
  </verify>
  <done>Each of this phase's four `HookServer` seams is proven live on a floor booted the way the operator boots it — by the verdict it changes, the ask it opens, the row it writes and the toast it does or does not raise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the composition root → every gate this phase added | `bootFloor` is the only place the production `HookServer` is constructed. A seam missing here is a gate that exists in the type system and in the test suite and nowhere the operator's agents can reach. |
| operator config → the host allowlist, at call time | Read through `readConfig()` per call so a Settings edit takes effect without a restart. That is also why an agent that can rewrite `userData/config.json` can widen it — GATE-03 ceiling item (q), accepted there, restated here so the wiring choice is legible. |
| `persist` (assigned at `:1148`) → an argument passed at `:1119` | A boot-ordering hazard with an in-file precedent (`boot.ts:995-1000`). An eager reference captures `undefined` and silently records nothing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-CMD-26 | Repudiation | **The phase's systemic defect: three gates with green unit tests, driven by `HookServer` instances the tests constructed, while the floor the operator runs has none of them wired** — the D-28 "shipped half is unreachable" shape rebuilt in three new files at once | mitigate | This plan owns the one production construction site, and its test observes each seam's **effect** through a really-booted `bootFloor(deps)` — a changed verdict, a real `hive_ask`, a real row, a real toast — never the argument text |
| T-04-LOG-11 | Repudiation | `recordToolCall` bound eagerly to `persist`, which is `undefined` at `:1119` and assigned at `:1148` — so every tool call is silently unrecorded and RECORD-01's audit trail is empty on a floor whose tests are green | mitigate | The argument is a closure read at call time, with `boot.ts:995-1000`'s precedent cited beside it; the test reads a real row back out of `floor.persist` |
| T-04-NET-02 | Repudiation | The host arm answering from `DEFAULT_HOST_ALLOWLIST` forever because the getter was never supplied, so the operator's Settings list is inert and nobody notices — the default is non-empty, so nothing visibly breaks | mitigate | The test asserts **two different verdicts from two different configs**, which a default-only implementation cannot produce |
| T-04-ASK-39 | Denial of service | An approval toast firing for an operator who turned notifications off | mitigate | The toast half is gated on `readConfig().notifications`, the same expression `boot.ts:411-413` uses; the data half is deliberately ungated, because plan 04-17's `GET /phone/api/asks` is a pull and asserted separately |
| T-04-CMD-27 | Tampering | A wiring plan quietly editing `hooks.ts`, `approvals.ts` or `config.ts` to make a seam fit, in a wave where those files have no owner | mitigate | `git diff --stat` on all four declaring files is asserted **empty**; a seam that does not fit is a finding reported against the plan that declared it |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step; `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed |
</threat_model>

<verification>
- `node --test test/boot-floor.test.cjs` — green, five new cases, each seen RED first.
- `node --test test/hive-durability.test.cjs` — green; `boot.ts` still holds exactly one `new HookServer(`.
- `node --test test/suite-integrity.test.cjs` — green; `DECLARED_SKIPS` and `FROZEN` unchanged.
- `npm test` — full suite, 0 failures, skipped count unchanged.
- `npm run typecheck` — 0 errors in both projects. `npm run lint` — exit 0.
- `git diff --stat` touches only `src/main/floor/boot.ts` and `test/boot-floor.test.cjs`.
</verification>

<success_criteria>
- All four seams are supplied at the sole production `new HookServer(...)`, and `boot.ts` still holds exactly one of them.
- Each seam is proven by an effect on a really-booted floor: a config-driven verdict change, a real `hive_ask`, a real persisted row with a non-null target and a token-derived agent id, and a notification gate honoured in both directions.
- Every case was seen RED before the wiring landed, and that output is in the SUMMARY.
- `hooks.ts`, `approvals.ts`, `config.ts`, `hiveProvisioning.ts` and `deps.ts` are all untouched.
- The SUMMARY states whether `openAsk` needed a constructor argument at all, and records the boot-ordering reason the `recordToolCall` argument is a closure.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-20-SUMMARY.md` when done.
</output>
