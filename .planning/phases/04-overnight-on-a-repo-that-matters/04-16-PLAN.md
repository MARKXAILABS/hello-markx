---
phase: 04-overnight-on-a-repo-that-matters
plan: 16
type: execute
wave: 5
depends_on: ["04-01", "04-10", "04-15"]
files_modified:
  - src/main/hiveTemplates.ts
  - test/gate05-bounded-wait.test.cjs
  - test/repo-claims.test.cjs
  - test/suite-integrity.test.cjs
  - .planning/phases/01-finish-the-floor/01-VALIDATION.md
autonomous: true
requirements: [GATE-05]
user_setup: []

must_haves:
  truths:
    - "An unanswered ask DENIES at the deadline, observed on the real shim's stdout — not inferred from the registry."
    - "An explicit yes ALLOWS, observed the same way."
    - "A dead socket on the FIRST connection still ALLOWS. Fail-open is preserved for 'the floor is not running'; fail-closed is introduced only for 'the floor asked and the operator did not answer'. Those are different failures and they do not share a default."
    - "A dead socket on a POLL connection, once a `hive_ask` has been seen, DENIES. The ask is known to exist, so this is the second failure wearing the first's clothes — and leaving it fail-open makes killing the floor an agent-triggerable way to convert its own pending deny into an allow."
    - "The poll loops for agy and grok are built against shims for engines that are not installed here, so they carry `LIVE-UNVERIFIED` markers and the four-way marker ledger is reconciled in the SAME commit."
    - "The shim writes the deny JSON to stdout and exits 0 on timeout — it NEVER exits silently, because a silent exit is allow."
    - "The poll is a fresh short connection each time. A held-open connection is a silent fail-OPEN on grok and agy, measured, and this plan must not reintroduce it."
  artifacts:
    - path: "src/main/hiveTemplates.ts"
      provides: "the poll loop in HOOK_SHIM, AGY_HOOK_SHIM and GROK_HOOK_SHIM"
      contains: "hive_ask"
    - path: "test/gate05-bounded-wait.test.cjs"
      provides: "deadline-denies, yes-allows, dead-socket-allows — all through the real shim as a child process"
  key_links:
    - from: "HOOK_SHIM's reply reader (:342-345)"
      to: "the ApprovalPoll loop"
      via: "on seeing hive_ask, discard resp and loop with a FRESH connection per poll"
      pattern: "hive_ask"
    - from: "the shim's deadline expiry"
      to: "stdout"
      via: "write the deny JSON, then exit 0 — never a silent exit"
      pattern: "permissionDecision"
---

<objective>
GATE-05's shim half: the bounded wait that times out to **deny**, built as a poll because a measurement
says the obvious alternative is a silent fail-open.

**Purpose.** Plan 04-15 made main answer an ask with a reply that is simultaneously a deny for an old shim
and a `hive_ask` handle for a new one. This plan teaches the three reply-reading shims to see the handle
and poll.

**L-02 is why this is a poll and not a stream, and it is not a preference.** Measured:
- `HOOK_SHIM` (`hiveTemplates.ts:341-345`): `c.on('data', d => resp += d)`, `c.on('end', () => done(0))`,
  and `done` writes `resp` to stdout verbatim.
- `GROK_HOOK_SHIM` (`:770-784`): `done` runs `JSON.parse(resp || '{}')` over the **whole accumulated
  buffer**; on a parse failure it sets `out = null` and `process.exit(0)` **with no stdout — which is
  allow**.
- `AGY_HOOK_SHIM` (`:364-412`): the same shape.

A "pending" preamble written on the live connection followed by a verdict produces
`{"pending":…}{"deny":…}` in `resp`. `JSON.parse` throws. **grok and agy then exit 0 silently, which is
allow.** A streaming implementation would pass every Claude test and silently allow on two of the seven
engines. Do not let this plan be "simplified" into holding the connection.

**S-2 sequenced this after plan 04-10 for a reason, and there is a commit hazard to carry.**
`gitCommitter.ts:238-248`'s `harnessAuthored` byte-compares `bin/cth-hook.cjs` against the `HOOK_SHIM`
constant — *against the INDEX blob (`git show :path`), not the working file.* Changing the constant is
fine: `hive.ts:801` rewrites the file every bootstrap. **But the constant change and any regenerated
committed shim must land in the SAME commit, or the secret-scrub sees a mismatch and the false positives
it exists to suppress come back.**

**Pitfall 6 — the trap.** Flipping the shim's existing 5 s `setTimeout(() => process.exit(0), 5000)`
(`hiveTemplates.ts:347`) to a deny is the obvious way to make the wait fail closed. It would make **every
socket outage, every app restart, and every legitimately-not-running floor deny every tool call on every
agent.** The 5 s timer is untouched for the non-ask path; the deadline replaces it only once `hive_ask` is
seen.

**Output.** A poll loop in three shims, and the integration test that observes all three outcomes on real
stdout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-15-SUMMARY.md
@docs/adr/0001-one-gate-for-pty-writes.md
</context>

<interfaces>
The wire contract plan 04-15 shipped. The shim reads these names; it does not negotiate them.

```js
// What main returns for an ask. An OLD shim reads only hookSpecificOutput and denies.
{
  hookSpecificOutput: { hookEventName: 'PreToolUse', permissionDecision: 'deny', permissionDecisionReason: '…' },
  hive_ask: { id: 'ask-<hex>', deadlineMs: <epoch>, pollMs: 1000 }
}

// What a NEW shim posts, on a FRESH SHORT CONNECTION, every pollMs:
{ hook_event_name: 'ApprovalPoll', ask_id: '<askId>', sock_token: '<per-agent token>' }

// What main answers a poll with:
{ status: 'pending' }            // loop again
// …or the final PreToolUse-shaped allow ({}) or deny.
```
</interfaces>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. Shims use `node:net`, which they already do.
  No install step.
- **D-35 — `src/main/hiveTemplates.ts` `:291-800` is this plan's in wave 5.** `TASK_CLI` (`:24-290`) was
  plan 04-04's in wave 1; the pi/OpenCode veto paths were plan 04-10's in wave 3. **Do not revisit either.**
- **D-35** — this plan touches no other source file. `hooks.ts` was plan 04-15's in wave 4; `webhook.ts`,
  `index.ts`, `push.ts` and the phone are plan 04-17's in this same wave.
- **ADR-0001 — exactly one place types into a live PTY.** The shim writes to **its own stdout**, which is
  how a hook expresses a decision. It must never write to a terminal.
- **D-08 clause 3 is verbatim-preserved:** `c.on('error', () => process.exit(0))` must survive untouched in
  every shim this plan edits. It is what makes "the floor is down" allow, and it fires **before** any
  `hive_ask` can exist.
- **The 5 s exit timer (`hiveTemplates.ts:347`, and agy's at `:412`, and grok's at `:795`) is unchanged for
  the non-ask path.** Pitfall 6.
- **D-33 / D-40** — assert a positive lower bound alongside every negative.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The poll loop, in all three reply-reading shims</name>
  <files>src/main/hiveTemplates.ts</files>
  <behavior>
    - A reply with no `hive_ask` behaves exactly as today: write `resp` (or the translated form) to stdout, exit 0, within the existing 5 s budget.
    - A reply with `hive_ask`: discard `resp`, then loop — one fresh `net.createConnection` per `pollMs`, posting `ApprovalPoll` with the ask id and the per-agent token.
    - `{status:'pending'}` → loop again. A final allow/deny → emit it in that shim's own contract and exit 0.
    - `Date.now() > deadlineMs`, or an unknown ask id → **write the deny JSON to stdout** and exit 0. Never a silent exit.
    - A connect error on the FIRST connection → `process.exit(0)` with no stdout, unchanged, verbatim.
    - A connect error on a POLL connection, after `hive_ask` has been received → **write the deny JSON to stdout** and exit 0.
  </behavior>
  <read_first>
    - `src/main/hiveTemplates.ts:298-347` — `HOOK_SHIM` in full: the connect at `:340`, the reply read at
      **`:342-345`**, the 5 s exit at **`:347`**, and the `c.on('error', () => process.exit(0))` line
    - `src/main/hiveTemplates.ts:364-412` — `AGY_HOOK_SHIM`, its translator, and its own 5 s exit at `:412`
    - `src/main/hiveTemplates.ts:730-795` — `GROK_HOOK_SHIM`: the reply decoder at **`:772-784`**, the
      `permissionDecision:'deny'` → `{decision:'deny'}` translation at `:779-781`, and its 5 s exit at
      `:795`. **This is the shim L-02 measured; read its `JSON.parse(resp || '{}')` line and understand
      exactly why the poll exists.**
    - `src/main/hiveTemplates.ts:293-297` — the recorded note that Moonshot documents a BLOCK as **exit
      code 2** where this shim expresses deny via stdout JSON at exit 0 (kimi, open, LIVE-UNVERIFIED)
    - `src/main/hiveTemplates.ts:314-322` — the status-mode fire-and-forget connect, the other in-file
      connect shape
    - `src/main/gitCommitter.ts:238-248` — `harnessAuthored`'s byte-identity map:
      `{'bin/cth-hook.cjs': HOOK_SHIM, 'bin/hive-proxy.cjs': PROXY_BRIDGE_SHIM}`, compared against the
      **index blob** (`git show :path`), *"the only form immune to core.autocrlf, which is true by default
      on Git for Windows"*
    - `src/main/hive.ts:801` — `writeIfChanged(join(root,'bin','cth-hook.cjs'), HOOK_SHIM)`: the constant is
      rewritten to disk every bootstrap
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Pattern 3 (the design and its
      sketch), **L-02**, § Common Pitfalls **6**
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § No Analog Found, the shim-poll
      row: *"NO ANALOG — no shim in this repo has ever re-connected. Every one of the seven is one
      connection, one reply, exit. The retry/deadline loop is genuinely new code with no in-repo
      precedent."*
  </read_first>
  <action>
    Add the poll loop to `HOOK_SHIM` (`:298-347`), `AGY_HOOK_SHIM` (`:364-412`) and `GROK_HOOK_SHIM`
    (`:730-795`). These are shim **source strings** written to disk, so they run on the agent's node with
    no bundler — plain ES5-safe CommonJS, `node:net` only, no imports the shim does not already make.

    **Two of the three cannot be observed live from this machine, and that goes in the ledger, not in a
    footnote.** `HOOK_SHIM` is Claude's and codex's and is exercised end to end by task 2. `AGY_HOOK_SHIM`
    and `GROK_HOOK_SHIM` are built against shipped shims for engines that are **not installed here** —
    grok needs an xAI key, agy needs an antigravity install — so their poll loops are built, unit-driven
    through `test/gate-harness.cjs`'s `opts.shim` selector, and **live-unverified**. Add one
    `LIVE-UNVERIFIED` marker per shim in the house register (naming the engine, what is unverified, and
    that an installed CLI plus an account would settle it), then **reconcile all four pins of the marker
    ledger in the SAME commit** — see task 3. Plan 04-19's closing sweep goes looking for exactly this
    shape of gap and it must find a decision, not an omission.

    The loop, per RESEARCH § Pattern 3:
    1. On the first reply, check for `hive_ask`. **Absent → today's behaviour exactly**, including the 5 s
       budget and each shim's own output contract. Present → **discard `resp`** and enter the loop.
    2. Each poll opens a **fresh short connection**, posts
       `{hook_event_name:'ApprovalPoll', ask_id, sock_token}`, reads one reply, and closes. **Never hold
       the connection open across polls** — L-02 is why, and a comment at the loop must say so, naming
       grok and agy explicitly, so a future reader does not "optimise" it back into a stream.
    3. `{status:'pending'}` → wait `pollMs` and repeat. A final verdict → translate it into that shim's own
       contract (`HOOK_SHIM` writes it verbatim; grok and agy translate as they already do) and exit 0.
    4. On `Date.now() > deadlineMs`, or an unknown ask id, or a malformed poll reply: **write the deny JSON
       to stdout, then exit 0.** A silent exit is allow — that is the single most important line in this
       plan and it deserves a comment saying so.
    5. Arm the existing 5 s `setTimeout(...).unref()` **per poll connection** so a hung individual poll
       cannot wedge the shim, and let `deadlineMs` bound the loop as a whole.

    **Preserve `c.on('error', () => process.exit(0))` verbatim on the FIRST connection** in all three. It
    is D-08 clause 3 and it fires before any `hive_ask` can exist, which is what makes clause 3 hold by
    construction rather than by care.

    **But a poll connection is not the first connection, and copying the handler onto it is a fail-open
    the reasoning does not cover.** RESEARCH's justification for the exit-0 is precise: *"the error path is
    reached before any `hive_ask` is ever seen"* — that is true of the first connect and false of every
    poll. Once a `hive_ask` has been received the shim **knows** an ask exists and knows main opened it. A
    floor that dies, restarts, or has its socket briefly unavailable **while that ask is pending** is
    D-08's second case, not its first: *"the floor asked and the operator did not answer."* Leaving the
    handler as-is on poll connections means an agent that receives a `hive_ask` and then kills the Electron
    main process converts its own pending deny into an **allow** — and agents here run permissions-bypassed
    by design, so that is agent-triggerable, not hypothetical.

    So: **the poll loop installs its own error handler**, which writes the deny JSON to stdout and exits 0,
    exactly like a deadline expiry. Put a comment on it naming D-08 clause 3 and saying which half of the
    clause each handler serves, so a future reader who sees two different error handlers in one file
    understands they are two different facts rather than a copy-paste slip. The same reasoning covers a
    poll connection that times out with no reply, and a poll reply that fails to parse.

    **The commit hazard (S-2):** `gitCommitter.ts:238-248` byte-compares `bin/cth-hook.cjs` against the
    `HOOK_SHIM` constant. Changing the constant is fine — `hive.ts:801` rewrites the file every bootstrap —
    **but the constant change and any regenerated committed shim must land in the SAME commit**, or the
    secret-scrub sees a mismatch and the false positives it exists to suppress come back. Check whether a
    generated shim is committed anywhere in this repo before committing, and say what you found.

    Do **not** touch `PI_EXTENSION` or `OPENCODE_PLUGIN`. Plan 04-10 built their veto paths; neither can
    poll, and GATE-05's ceiling item (a) already records that "ask" degrades to "deny" for them. Adding a
    poll there would be building an unverifiable path on top of an unverifiable path.
  </action>
  <acceptance_criteria>
    - `grep -c 'hive_ask' src/main/hiveTemplates.ts` ≥ 3 (one per reply-reading shim). Count OCCURRENCES where a line may hold two — `grep -o 'hive_ask' src/main/hiveTemplates.ts | wc -l` — because `grep -c` counts lines and would under-report.
    - `grep -c "on('error'" src/main/hiveTemplates.ts` is **strictly higher** than before this plan: each shim gains a second, poll-scoped handler alongside its untouched first-connection one. State the before and after numbers in the SUMMARY.
    - The first-connection handler survives verbatim in all three: `grep -c "c.on('error', () => process.exit(0))" src/main/hiveTemplates.ts` is unchanged from before this plan.
    - The poll-scoped handler writes before exiting: for each of the three shims, the poll error path contains a stdout write. Asserted behaviourally by task 2 case 5, not by grep alone.
    - `grep -c '5000' src/main/hiveTemplates.ts` is unchanged — the non-ask budget is untouched (Pitfall 6).
    - Every deadline-expiry path is followed by a stdout write before `exit(0)`: assert by reading each of the three shims and by the task-2 integration test, which observes the bytes.
    - `grep -ci 'grok' src/main/hiveTemplates.ts` includes at least one occurrence inside the new poll comment naming L-02.
    - `sed -n '291,800p' src/main/hiveTemplates.ts | grep -c 'LIVE-UNVERIFIED'` increases by exactly 2 (agy and grok), and the number is stated in the SUMMARY **before** task 3 runs.
    - No shim holds a connection across polls: `grep -c 'createConnection' src/main/hiveTemplates.ts` increases (a fresh connect per poll), and no `setInterval` writes to an existing socket.
    - `git diff src/main/hiveTemplates.ts` is confined to `:291-800` and does not touch `PI_EXTENSION` or `OPENCODE_PLUGIN`.
    - `npm run typecheck` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && npm test</automated>
  </verify>
  <done>Three shims can wait for an operator's answer without holding a connection, deny loudly on expiry, and allow silently when the floor is simply not running.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gate05-bounded-wait — three outcomes, observed on real stdout</name>
  <files>test/gate05-bounded-wait.test.cjs</files>
  <behavior>
    - Unanswered past the deadline → the shim's stdout carries a deny, and the shim exited 0.
    - `answer(id, true)` before the deadline → the shim's stdout carries an allow, and the shim exited 0.
    - Server never started / socket removed → the shim exited 0 with **empty stdout** (allow), and never entered the poll loop.
    - Server killed AFTER the `hive_ask` reply and BEFORE the ask is answered → the shim's stdout carries a **deny**, and the shim exited 0. The mid-ask socket death fails CLOSED.
    - A poll carrying another agent's token is rejected.
    - The whole ask cycle completes in bounded time under an injected clock — the test does not sleep for 120 seconds.
  </behavior>
  <read_first>
    - `test/gate-harness.cjs` (plan 04-01) — `withHookServer` and `runShim`. **Use it.**
    - `test/hook-auth-roundtrip.test.cjs:65-125` — the model the harness came from
    - `test/boot-floor.test.cjs:178-190` — the case that **runs on win32 with no skip** and connects to
      `floor.hive.sockPath()`. Its `:183` *"Connect, never stat"* comment argues against `stat`-ing, not
      for skipping. **This file carries NO `{ skip: !POSIX }`.**
    - `test/suite-integrity.test.cjs:195-203` (`DECLARED_SKIPS`), `:235` (the per-file `deepEqual`) and
      `:256` (`FROZEN = { win32: 6, other: 1 }`) — and the assertion message at `:238-245`, which demands
      that `.planning/phases/01-finish-the-floor/01-VALIDATION.md`'s ceiling move in the same change.
      Read all of it: it is why this file is not gated, and why both files are in this plan's
      `files_modified` as an escape hatch rather than an intention.
    - `src/main/approvals.ts` (plan 04-15) — `open`/`poll`/`answer`/`sweep` and the TTL
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` — the three GATE-05 rows, and
      § Anti-Vacuous-Pass Rules: *"Calling `handle()` directly proves the judge, not the loop. **Drive the
      real shim as a child process into the real `HookServer`.**"*
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Common Pitfalls **2** — two
      tests both passed while the whole floor was dead-hooked, because each half is easier to test than the
      join
  </read_first>
  <action>
    Write `test/gate05-bounded-wait.test.cjs` RED first, using the harness. **Add no `{ skip: … }` of any
    kind** — plan 04-01's harness runs on win32 because the socket path comes from `hive.sockPath()`, a
    `\\.\pipe\` name there that both `listen` and `net.createConnection` accept.

    This is not a portability nicety. `04-VALIDATION.md:104` makes the real-shim roundtrip the **only**
    admissible evidence for criterion 2, explicitly disqualifying plan 04-15's registry unit test as a
    substitute. A POSIX gate would make every case here pass **vacuously**, with all of them skipped, on
    the only machine the phase runs on — and a skipped case has no RED, so this task's own
    "the RED run is recorded in the SUMMARY" criterion would be unproducible.

    **If, and only if, the harness cannot be made to work on win32:** that is a stop-and-report at plan
    04-01, not a licence to gate here. Should the operator direct a gate anyway, this plan owns
    `test/suite-integrity.test.cjs` and `01-VALIDATION.md` in wave 5 for exactly that contingency, and the
    re-derivation is a **named step**: one `DECLARED_SKIPS` row per gated case with `winSkips: true`,
    `FROZEN.win32` re-derived from the table, and `01-VALIDATION.md`'s published `# skipped <= 7` ceiling
    moved in the same commit.

    **Every assertion is on the shim's real stdout and real exit code.** Reading the registry to decide
    whether the wait worked proves the registry, which plan 04-15 already did. This file's whole reason to
    exist is the join.

    Five cases:
    1. **Deadline denies.** Open an ask with a short TTL (inject the clock or configure a small TTL for the
       test — **do not sleep 120 s**), let it expire, assert the shim's stdout parses to a deny and the exit
       code is 0. Assert stdout is **non-empty** — a silent exit is allow and would pass a naive "no allow
       seen" check.
    2. **Yes allows.** Answer the ask before the deadline; assert stdout carries an allow and exit 0.
    3. **Dead socket allows.** Do not start the server (or remove the socket). Assert the shim exited 0 with
       **empty stdout**, and assert it never posted an `ApprovalPoll` — the connect-error path fires before
       any `hive_ask` can exist, and that ordering is the whole of D-08 clause 3.
    4. **Cross-agent poll rejected.** Poll a valid ask id with a different agent's token; assert the server
       rejects it and the shim does not receive an allow. Positive control in the same case: the owning
       agent's token on the same id gets `{status:'pending'}`.
    5. **Mid-ask socket death denies.** Let the shim receive `hive_ask`, then **stop the server** (or close
       the socket) before the ask is answered, and assert the shim's stdout is **non-empty and parses to a
       deny**, exit 0. Contrast it in the same file with case 3, which kills the socket *before* any reply
       and asserts **empty** stdout. Those two cases are the whole of D-08 clause 3's boundary: the floor
       being down is an allow, the floor having asked and then gone away is a deny. A shim that treats them
       alike is a floor an agent can kill to get a yes.

    Case 1 needs a bounded runtime. Use the smallest TTL the implementation accepts and say in a comment
    that the production default is 120 s — a test that takes two minutes is a test that gets skipped.

    Add one assertion that no case took longer than a stated ceiling, so a future change that reintroduces a
    real 120 s wait fails loudly instead of slowly.
  </action>
  <acceptance_criteria>
    - `node --test test/gate05-bounded-wait.test.cjs` exits 0 **on this win32 machine** with five cases that **ran** — the runner's summary shows `pass 5` / `skipped 0`, not `skipped 5`. A vacuous pass is a failed task.
    - `grep -c 'skip' test/gate05-bounded-wait.test.cjs` returns `0`, and `node --test test/suite-integrity.test.cjs` is green with `DECLARED_SKIPS` and `FROZEN` unchanged.
    - The mid-ask case (5) asserts non-empty stdout parsing to a deny, and the pre-ask case (3) asserts empty stdout — **both in the same file**, so the two halves of D-08 clause 3 cannot collapse into one.
    - The deadline case asserts stdout is non-empty **and** parses to a deny — both, because either alone is satisfiable by the wrong behaviour.
    - The dead-socket case asserts stdout is **empty** and exit code is 0, **and** that zero `ApprovalPoll` events reached the server.
    - No case sleeps longer than the stated ceiling; the whole file runs in under 10 s.
    - `grep -c 'existsSync' test/gate05-bounded-wait.test.cjs` returns `0`.
    - Every verdict assertion reads the shim's stdout, not the registry: `grep -c 'registry.poll' test/gate05-bounded-wait.test.cjs` returns `0`.
    - The RED run is recorded in the SUMMARY, and the skipped count is stated with its delta from 7 confirmed **zero**.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/gate05-bounded-wait.test.cjs && node --test test/suite-integrity.test.cjs && npm test</automated>
  </verify>
  <done>All three D-08 outcomes are observed on the real shim's real stdout, in under ten seconds, with the fail-open and the fail-closed proven to be different code paths.</done>
</task>

<task type="auto">
  <name>Task 3: Two markers, and all four pins reconciled in the same commit</name>
  <files>test/repo-claims.test.cjs</files>
  <read_first>
    - `test/repo-claims.test.cjs:1249-1262` — why the marker clause reads **raw** text where most clauses
      read stripped-of-comments text. Read it before changing anything.
    - `test/repo-claims.test.cjs:1287-1289` — the file's own prescribed re-measure command:
      `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`
    - `test/repo-claims.test.cjs:1291-1298` `MARKER_LEDGER`; `:1307` `LIVE_UNVERIFIED_TOTAL`;
      `:1419-1454` the committed file-set assertion (`:1422`) and the per-engine lower bound (`:1453`)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` **S-8** and **L-10** — the pin
      is four-way and all four must move in the SAME commit as any marker change
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-10-SUMMARY.md` and `04-13-SUMMARY.md` — the
      ledger's state after waves 3 and 4. **Read them for context, then re-measure rather than adding to
      their numbers.**
  </read_first>
  <action>
    Task 1 added two `LIVE-UNVERIFIED` markers to `src/main/hiveTemplates.ts` — one for `AGY_HOOK_SHIM`'s
    poll loop, one for `GROK_HOOK_SHIM`'s. Neither engine is installed here, so both loops are built
    against a shipped shim and observed by nothing.

    **Re-measure first, never arithmetic.** Run the file's own prescribed command:
    `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`. Do not compute the new counts by
    adding 2 to plan 04-13's figures — plan 02-12 found a real gap that way round, and the whole point of
    the ledger is that it is measured.

    Reconcile all four pins in one commit: `MARKER_LEDGER`'s per-file counts (`:1291-1298`),
    `LIVE_UNVERIFIED_TOTAL` (`:1307`), the committed file-set assertion (`:1422`), and the per-engine lower
    bound (`:1453`) — grok and agy now carry GATE-05 poll markers as well as their pre-existing bridge
    markers.

    In the SUMMARY, state the before and after counts per file and note that the count came from a live
    grep in this session rather than from arithmetic over a prior figure. **This is also the answer to
    plan 04-19's zero-marker sweep for GATE-05**: say so explicitly, so the closing plan finds a decision
    rather than a hole.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exits 0.
    - The ledger's per-file counts match a live `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c` run in the same session — the SUMMARY shows both.
    - `LIVE_UNVERIFIED_TOTAL` equals the sum of the per-file counts (the file's own consistency clause still passes).
    - The SUMMARY names which engines the two new markers cover and what would settle each.
    - `npm test` — full suite green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs && npm test</automated>
  </verify>
  <done>The two poll loops nobody can observe are declared rather than implied, and all four pins were reconciled from a live measurement in the same commit as the markers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the shim (running on the agent's node, in the agent's reach) → the hook socket | The shim is a file inside the agent's own reach. `gitCommitter.harnessAuthored` byte-compares it against the compiled-in constant precisely because *"an agent that edits a shim to smuggle a key changes the bytes and the scrub fires on it like any other file."* |
| the poll → main's registry | Each poll carries an ask id and the per-agent token. `authorized()` is the check. |
| the shim's stdout → the engine's permission decision | A silent exit is allow. Every non-allow outcome must be a written one. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ASK-09 | Elevation of privilege | A held-open connection producing two concatenated JSON objects, so grok and agy `JSON.parse` throw and `exit(0)` silently — **which is allow** | mitigate | **Measured (L-02).** Fresh short connection per poll; a comment at the loop names grok and agy so it is not optimised back into a stream; the integration test observes real stdout on the real shims |
| T-04-ASK-10 | Elevation of privilege | The shim exiting silently on deadline expiry, which reads as allow | mitigate | Write the deny JSON to stdout **then** exit 0; the test asserts stdout is non-empty as well as parsing to a deny |
| T-04-ASK-11 | Denial of service | Flipping the existing 5 s timer to deny, so every socket outage denies every tool call on every agent | mitigate | Pitfall 6: the 5 s budget is unchanged for the non-ask path and armed per poll connection; `grep -c '5000'` is an acceptance criterion |
| T-04-ASK-12 | Spoofing | An agent polling another agent's ask id to approve a command it did not request | mitigate | Every poll carries the per-agent token and goes through `authorized()`; a dedicated cross-agent test case |
| T-04-ASK-13 | Elevation of privilege | A connect error on the FIRST connection being reinterpreted as deny, breaking every agent whenever the app is legitimately not running | accept — **deliberate, D-08 clause 3** | `c.on('error', () => process.exit(0))` preserved verbatim on the first connect; the test asserts zero `ApprovalPoll` events on the dead-socket path, proving the ordering |
| T-04-ASK-32 | Elevation of privilege | **The first-connection fail-open copied onto every POLL connection**, so a floor that dies, restarts, or briefly loses its socket *while an ask is pending* yields ALLOW for the exact command being asked about — and an agent that receives a `hive_ask` and then kills the Electron main process converts its own pending deny into an allow. Agents here run permissions-bypassed by design, so this is agent-triggerable | mitigate | The poll loop installs its own error handler that writes the deny JSON to stdout and exits 0, with a comment naming which half of D-08 clause 3 each handler serves; task 2 case 5 kills the server after `hive_ask` and asserts a non-empty deny, contrasted in the same file with case 3's empty-stdout allow |
| T-04-ASK-33 | Repudiation | A POSIX gate making criterion 2's only admissible evidence pass vacuously on the only machine the phase runs on — and making the required RED run unproducible, since a skipped case has no RED | mitigate | No `{ skip: … }`; the harness runs on win32 by plan 04-01's measured design; `grep -c 'skip'` returning 0 and `pass 5 / skipped 0` are both acceptance criteria |
| T-04-CMD-22 | Repudiation | Two new poll loops for engines nobody here can run, shipping with no marker — the exact zero-marker gap plan 02-12 found for qwen | mitigate | Task 3 adds one `LIVE-UNVERIFIED` marker per shim and reconciles all four pins of the ledger from a live grep in the same commit |
| T-04-ASK-14 | Elevation of privilege | kimi expressing BLOCK as exit code 2 where this shim expresses deny via stdout JSON at exit 0 | accept — **open, recorded** | `hiveTemplates.ts:293-297` already records it; kimi is not installed and is LIVE-UNVERIFIED (plan 04-10's ledger) |
| T-04-SC-02 | Tampering | The `HOOK_SHIM` constant and a committed `bin/cth-hook.cjs` drifting apart, so the secret-scrub's false-positive suppression breaks | mitigate | S-2: the constant change and any regenerated committed shim land in the **same commit**; the executor must check for a committed generated shim and report what it found |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step; shims use `node:net`, which they already do. `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed |
</threat_model>

<verification>
- `node --test test/gate05-bounded-wait.test.cjs` — green **on win32**, five cases that ran, zero skipped, under 10 s.
- `node --test test/repo-claims.test.cjs` — green, ledger matching a live grep.
- `node --test test/suite-integrity.test.cjs` — green; `DECLARED_SKIPS` and `FROZEN` unchanged.
- `npm test` — full suite, 0 failures; skipped count stated, delta from 7 confirmed zero.
- `npm run typecheck` — 0 errors. `npm run lint` — exit 0.
- `git show --stat HEAD` for the shim commit shows the constant and any regenerated file together.
</verification>

<success_criteria>
- Unanswered denies, yes allows, a pre-ask dead socket allows, and a **mid-ask** dead socket denies — all four observed on the real shim's real stdout, on win32.
- No shim holds a connection across polls, and a comment at the loop names grok and agy as the reason.
- The connect-error `exit(0)` path survives verbatim in all three shims, proven by a test that asserts zero polls were sent.
- The 5 s non-ask budget is unchanged.
- Two `LIVE-UNVERIFIED` markers cover the agy and grok poll loops, with all four ledger pins reconciled from a live grep in the same commit.
- The SUMMARY records the RED run, the skip-count delta (zero), the before/after marker counts, and what the executor found when checking for a committed generated shim.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-16-SUMMARY.md` when done.
</output>
