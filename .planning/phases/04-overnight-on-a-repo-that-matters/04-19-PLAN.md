---
phase: 04-overnight-on-a-repo-that-matters
plan: 19
type: execute
wave: 7
depends_on: ["04-05", "04-06", "04-09", "04-10", "04-11", "04-12", "04-13", "04-16", "04-17", "04-18"]
files_modified:
  - SECURITY.md
  - TELEMETRY.md
  - test/repo-claims.test.cjs
  - .planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
autonomous: false
requirements: [GATE-02, GATE-03, GATE-04, GATE-05, RECORD-01, RECORD-02, RECORD-05, VIGIL-01, VIGIL-02, VIGIL-03, VIGIL-04]
user_setup: []

must_haves:
  truths:
    - "Every claim this phase makes about itself was re-measured against the tree at wave 7, not carried forward from any prior SUMMARY."
    - "The three inherited fail-opens (D-07's two plus `protectedPathDenial`'s `if (!root) return null`) and the one this phase adds (pi/OpenCode's ask degrading to deny) are all restated in SECURITY.md — never silently carried."
    - "The honest cross-engine claim is in SECURITY.md and pinned by `test/repo-claims.test.cjs`, so a later reader cannot inflate it."
    - "The whole-suite figure, the skipped-count delta from the 7-skip baseline, and the marker ledger are all measured in this session by commands whose output is in the SUMMARY."
    - "Every requirement checkbox flipped in REQUIREMENTS.md is backed by evidence produced at wave 7; a requirement whose live half never ran stays OPEN with the reason."
  artifacts:
    - path: "SECURITY.md"
      provides: "the three new gates' ceilings, the four fail-opens, and the honest cross-engine claim"
      contains: "LIVE-UNVERIFIED"
    - path: "TELEMETRY.md"
      provides: "the tool_calls and events tables, and the honest durability level"
      contains: "tool_calls"
    - path: "test/repo-claims.test.cjs"
      provides: "the final reconciled ledger and the clauses pinning this phase's claims"
  key_links:
    - from: "SECURITY.md"
      to: "hooks.ts:801-859's ceiling list"
      via: "the doc restates what the source already itemizes; the test pins that they agree"
      pattern: "ceiling|fail-open"
---

<objective>
Phase close. Re-measure everything this phase claims, write the two docs the requirements name, reconcile
the marker ledger one last time, and record what could not be verified — as a first-class outcome rather
than a gap.

**Purpose.** PROJECT.md's verification-honesty rule: *"an agent reporting 'fixed' is a claim, not
evidence. Every substantive change is re-verified by the orchestrator against source and a live test run.
This rule exists because it has already caught real over-claims."* Phase 2's plan 02-12 did exactly this
job and found a genuine gap in the process — `qwen` had **zero** `LIVE-UNVERIFIED` markers despite being
as unverified as pi/opencode/crush. **Assume this phase has one too, and go looking.**

`04-CONTEXT.md`'s canonical_refs names the two docs directly: *"`SECURITY.md` — must be updated by
GATE-02/03/05; `TELEMETRY.md` — by RECORD-01."*

**The rule that governs every line written here:** re-measure, never carry forward. Not from a prior
SUMMARY, not from `04-RESEARCH.md`, not from this plan. Every number in the closing SUMMARY comes from a
command run at wave 7.

**Output.** Two updated docs, a final ledger reconciliation, the filled-in verification map, and an honest
close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
</context>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step.
- **D-35** — this plan owns `test/repo-claims.test.cjs` in wave 7 (plans 04-10 and 04-13 owned it in waves
  3 and 4). It touches **no source file**: if closing the phase requires a source change, that is a finding
  to report and a new plan, not a drive-by fix in the rollup.
- **D-33 / D-40** — every repo-fact assertion asserts over joined/parsed text rather than single lines
  where a formatter could wrap the construct, and asserts a **positive lower bound** alongside the
  negative, so deleting the feature fails the test instead of satisfying it.
- **Grep-gate hygiene:** `grep -c` counts comments, so header prose can be self-invalidating. Use
  `grep -v '^#' | grep -c token`. A bare `== 0` gate on an unfiltered file is forbidden.
- The measured Phase 4 starting baseline was **805 tests / 798 pass / 0 fail / 7 skipped** at `ad3d2f7`.
  **Re-measure; do not quote.**
</standing_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the whole phase, then write SECURITY.md and TELEMETRY.md</name>
  <files>SECURITY.md, TELEMETRY.md</files>
  <read_first>
    - `SECURITY.md` (whole file) — the existing structure, and specifically how prior phases recorded
      exposures. Phase 2's plan 02-12 corrected a false tunnel-close claim **in the app's favour** and
      named three real exposures; match that register.
    - `TELEMETRY.md` — the existing description of the span ring
    - `src/main/hooks.ts:801-859` (the ceiling list, now carrying plan 04-06's (j)-(o) and plan 04-15's
      GATE-05 items) and `:54-62` (the two inherited holes)
    - `src/main/shellEnv.ts` — GATE-02's ceiling list (plan 04-05)
    - `src/main/telemetry.ts:96` — *"only in the in-memory ring buffer, never persisted"*. **This sentence
      is now half-true and TELEMETRY.md must say what changed and what did not.**
    - `src/main/db.ts` — the `tool_calls` and `events` tables and the `synchronous = NORMAL` durability level
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Security Domain — the ASVS
      mapping and the eight threat patterns; and § Common Pitfalls **5** (`synchronous = NORMAL` is not
      `FULL`)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-10-SUMMARY.md` — `<the_honest_claim>`
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-13-SUMMARY.md` — GATE-04's spike verdict and
      live-run outcome
  </read_first>
  <action>
    **Measure first, write second.** Run and capture, in this session:
    - `npm test` — tests / pass / fail / skipped, and the **skipped delta from 7**
    - `npm run typecheck`, `npm run lint`, `npm run build`
    - `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`
    - `git diff --stat <phase-base>..HEAD -- package.json package-lock.json` (must be empty)
    - the per-plan SUMMARY claims for the eleven requirements, each re-checked against source

    **SECURITY.md** gains this phase's three new gates and — the part that matters — its **four fail-opens**,
    each with a disposition and an owner, in the `hooks.ts:848` register (*"A ceiling list that omits (b),
    (f) or (g) reads as a guarantee that does not hold"*):

    1. **Inherited, Deferred** — the user-global engine seeds (`~/.codex/config.toml`,
       `~/.gemini/.../hooks.json`, `~/.grok/hooks/`) sit outside the hive root, so an agent appending a
       PreToolUse hook block to a seed gets it executed inside another agent's hook process with that
       agent's env (D-07 hole 1, `hooks.ts:54-59`).
    2. **Inherited, deliberate** — the shim's connect-error `process.exit(0)`, which for a Claude PreToolUse
       hook is *allow*. Bounded by `armSocketWatchdog` rather than removed, because a fail-closed shim
       would break every agent whenever the app is legitimately not running (D-07 hole 2).
    3. **Inherited, ACCEPTED, owner: hive maintainer** — `protectedPathDenial`'s `if (!root) return null`
       (ceiling item (i)). It fails open because *with no hive root there is nothing to protect*, which is
       a true statement about the world — **unlike** GATE-03's empty host allowlist, which fails **closed**
       for exactly the opposite reason. Say both, together; the contrast is the point.
    4. **NEW, this phase, the only one** — pi and OpenCode receive the ask reply's deny and cannot poll, so
       for them GATE-05's "ask" degrades to "deny". That is the safe side, and it is still a behaviour that
       must appear in a ceiling list.

    Also into SECURITY.md: GATE-02's env-allowlist ceiling (from `shellEnv.ts`), GATE-03's (j)-(o) items,
    GATE-05's (a)-(d) items, and **the honest cross-engine claim verbatim from plan 04-10's SUMMARY** —
    refused for Claude and Codex (live-verified on this machine); built for grok, kimi and agy and
    unit-verified through the real `HookServer`, LIVE-UNVERIFIED for want of an installed CLI; built and
    LIVE-UNVERIFIED for pi and OpenCode. Name GATE-04's outcome as plan 04-13 measured it.

    **TELEMETRY.md** gains RECORD-01 and RECORD-02, and must correct the claim `telemetry.ts:96` still
    makes locally: spans are still an in-memory ring of 200 per agent and still never persisted — **the
    ring is deliberately unchanged, because the waterfall UI reads it** — and the durable record is a
    separate `tool_calls` table written from the hook socket, which is the only place agent, tool and
    **target** exist for every engine. State the three measured reasons the telemetry path could not carry
    it (`ToolSpan` has no `target`, `ATTR_ALLOWLIST` admits no path or command key, and the OTel block is
    Claude-only), so nobody "fixes" this by moving it back.

    State the durability level honestly (Pitfall 5): `synchronous = NORMAL` under WAL means a row survives
    a **process** crash but is not guaranteed against an OS or power loss until a checkpoint. That is the
    right trade for this app and it is **stated, not silently upgraded to `FULL`** — which would fsync on
    every tool call.

    Document `events`'s retention as a **query bound**, with the number that shipped, and note that
    `harness.db` lives in `userData`, **outside the hive root**, so it needs no `UNTRACK_PATHS` entry.

    **If any measurement contradicts a plan's SUMMARY, the measurement wins and the contradiction goes in
    the closing SUMMARY.** That is the job.
  </action>
  <acceptance_criteria>
    - SECURITY.md names all four fail-opens, each with a disposition and (where applicable) a named owner, and states the deliberate contrast between item 3's fail-open and GATE-03's fail-closed empty allowlist.
    - SECURITY.md carries the honest cross-engine claim verbatim and does **not** claim grok, pi or OpenCode as live-verified.
    - TELEMETRY.md states that the span ring is unchanged and why, the three reasons the writer is not on the telemetry path, and the `synchronous = NORMAL` durability level.
    - Every figure in both docs was produced by a command run in this session; the SUMMARY shows the commands and their output.
    - `npm test` / `typecheck` / `lint` / `build` all green, with the skipped count and its delta from 7 stated.
    - `git diff --stat -- package.json package-lock.json` over the whole phase is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>Two docs that describe the boundary that exists rather than the one that was designed, with every number measured at wave 7.</done>
</task>

<task type="auto">
  <name>Task 2: The final ledger, and the clauses that stop this phase over-claiming later</name>
  <files>test/repo-claims.test.cjs, .planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md</files>
  <read_first>
    - `test/repo-claims.test.cjs:1249-1262` — why the marker clause reads **raw** text where most clauses
      read stripped-of-comments text; `:1287-1289` — the file's own re-measure command; `:1291-1298`
      `MARKER_LEDGER`; `:1307` `LIVE_UNVERIFIED_TOTAL`; `:1419-1454` the file-set and per-engine assertions
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Per-Task Verification Map —
      the 29 behaviour rows plus the marker row, with `Task ID` / `Plan` / `Wave` still `_tbd_`
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Validation Sign-Off — the
      six boxes and the `nyquist_compliant` flag
    - Every `04-*-SUMMARY.md` in the phase directory
  </read_first>
  <action>
    **Re-measure the ledger with the file's own command**, never by arithmetic over plan 04-10's and
    04-13's figures: `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`. Reconcile all four
    pins if anything moved.

    **Then go looking for the gap 02-12 found.** For each engine and each capability this phase touched,
    ask: *is there something as unverified as pi/opencode/crush that carries zero markers?* Candidates to
    check by name: kimi and antigravity (agy) — both are main-judged hook-bridge engines that CONTEXT.md
    never mentions and neither is installed here; and codex's sandbox path if plan 04-13's spike came back
    `REFUSED/PROMPTED`. Report what you found, including "nothing".

    Add closing clauses to `test/repo-claims.test.cjs`, in its established both-directions style, pinning
    the claims this phase makes so a later edit cannot quietly inflate them:
    - GATE-03's honest cross-engine claim appears in SECURITY.md, and grok / pi / OpenCode are **not**
      described as live-verified (positive: the claim is present and non-empty; negative: the over-claim is
      absent).
    - SECURITY.md names all four fail-opens (positive lower bound: at least four disposition lines).
    - TELEMETRY.md states the `synchronous = NORMAL` level and does **not** claim OS-crash durability.
    - The `tool_calls` writer is in `hooks.ts` and **not** in `telemetry.ts` — assert the negative
      (`telemetry.ts` contains no `recordToolCall`) alongside the positive (`hooks.ts` does).

    Every clause reads joined/parsed text, not single lines, and carries a positive lower bound. Use
    `grep -v '^#' | grep -c token` where a comment could satisfy the gate; a bare `== 0` on unfiltered text
    is forbidden.

    **Fill in `04-VALIDATION.md`'s Per-Task Verification Map.** Every one of the 29 behaviour rows plus the
    marker row gets its `Task ID`, `Plan` and `Wave`, and its `Status` set from a run in this session (✅ /
    ❌ / ⚠️ — never ⬜ at phase close). If any row has no owning task, **say so in the row and in the
    SUMMARY** rather than deleting it. Tick § Validation Sign-Off's six boxes only against evidence, and
    set `nyquist_compliant: true` in the frontmatter **only** if the plan-checker has confirmed Dimension 8
    — otherwise leave it false and say why.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exits 0, and the ledger matches a live grep whose output is in the SUMMARY.
    - Four new closing clauses exist, each asserting both directions.
    - No new clause uses a bare `== 0` gate on unfiltered text; every count-based gate filters comments.
    - All 30 rows of the Per-Task Verification Map carry a `Task ID`, a `Plan`, a `Wave` and a non-⬜ `Status`.
    - The zero-marker sweep is reported explicitly, including a "nothing found" if that is the result.
    - `npm test` — full suite green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs && npm test</automated>
  </verify>
  <done>The ledger is reconciled from a live measurement, this phase's claims are pinned by tests that fail if they inflate, and the validation map is filled in from runs rather than intentions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The phone, on a physical device — and the honest close</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Manual-Only Verifications —
      all four rows, and specifically the GATE-05 phone row: *"The PWA service worker updates on its own
      schedule; no local reproduction. **'Verification needs a real device' is a first-class outcome here,
      not a failure.**"*
    - `.planning/PROJECT.md` — the verification-honesty rule and the zero-recurring-cost constraint
    - `.planning/REQUIREMENTS.md` GATE (167-199), RECORD (200-223), VIGIL (224-241) — the eleven rows whose
      checkboxes this task decides
    - `.planning/phases/02-the-daemon-and-the-protocol/02-CONTEXT.md` — Phase 2 recorded the same
      device gap; do not re-derive it, cite it
  </read_first>
  <action>
    AUTOMATE FIRST, THEN PAUSE. Before this checkpoint may block, the executor must have already run and
    captured: the full suite, typecheck, lint and build; the live marker grep; the kill-and-restart crash
    test (kill the app mid-run, restart, and answer "who wrote this file, and what did the floor run
    overnight" from disk); and the per-requirement evidence re-checked against source at wave 7. Attempt
    the physical-Android GATE-05 answer if a device is available. Present the eleven proposed checkbox
    states with their evidence, then pause. Do NOT flip a checkbox to make the phase look finished.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
    <human-check>The physical-device attempt and its honest outcome; the kill-and-restart result; and the operator's ruling on each of the eleven checkboxes.</human-check>
  </verify>
  <what-built>
    Eleven requirements across eighteen plans: the env allowlist at the one `pty.spawn` choke point; a
    command-shape judge in main reaching seven engines; a per-engine codex sandbox opt-in; a third answer
    with a bounded wait that times out to deny, reachable from the phone; a durable tool-call record and a
    day-scannable event log; per-repo restore points over a shadow `GIT_DIR`; an absence watchdog that
    outlives the god; same-tick card release; blocked detection in main; and age on every card and ask.
  </what-built>
  <how-to-verify>
    Before pausing, the executor must have already run everything automatable and captured the transcripts.
    This checkpoint is for the two things that need the operator and for the checkbox decisions.

    Present, and pause:

    1. **The phone.** Trigger a real GATE-05 ask and answer it **from a physical Android device**. If no
       device is available, say so plainly and record it as a first-class outcome, exactly as Phase 2 did
       for DAEMON-02 — **do not** substitute a desktop browser and call it device-verified.
    2. **The crash test RECORD-01's own criterion names.** Kill the app mid-run and restart it, then answer
       *"who wrote this file, and what did the floor run overnight"* from disk. This is the requirement's
       own wording and it must be exercised, not inferred from a unit test.
    3. **The eleven checkboxes.** For each requirement, present the evidence and the proposed state. A
       requirement whose live half never ran stays **OPEN with the reason**, in this project's standing
       style — a partially-landed fix is an open concern. Specifically expect:
       - GATE-03 — the honest claim; the OpenCode, grok and pi clauses of criterion 1 are **not** ticked.
       - GATE-04 — whatever plan 04-13's live run measured, including LIVE-UNVERIFIED if the upstream
         defect reproduced.
       - GATE-05 — the desktop and endpoint halves are verified; the physical-device half is whatever
         step 1 produced.
    4. **The measured close:** the whole-suite figure, the skipped-count delta from 7, the marker ledger,
       and any claim that a wave-7 measurement contradicted.

    **Do not flip a checkbox to make the phase look finished.** This project's own record is that an honest
    open concern is worth more than a green checklist that overstates reality — it has already caught four
    over-claims.
  </how-to-verify>
  <acceptance_criteria>
    - The physical-device attempt is recorded with its outcome, whichever it is.
    - The kill-and-restart crash test was actually run and its result is in the SUMMARY.
    - Each of the eleven requirements has a stated state with evidence, and every one left OPEN has a reason.
    - The SUMMARY states the whole-suite figure, the skipped delta from 7, the final marker ledger, and any contradiction a wave-7 measurement produced.
    - The operator has typed approval or named an issue.
  </acceptance_criteria>
  <resume-signal>Type "approved" or name what stays open</resume-signal>
  <done>The phase closes on measured evidence, with what could not be verified named out loud rather than rounded up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's claims → the project's own record | SECURITY.md, TELEMETRY.md, REQUIREMENTS.md and ROADMAP.md are read by future planners as fact. An over-claim here becomes a design premise two phases later. |
| the marker ledger → the parity claim | `test/repo-claims.test.cjs` is the mechanism that makes "declare, do not fake" enforceable rather than aspirational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-DOC-01 | Repudiation | A SECURITY.md that describes a tighter boundary than exists, by omitting an inherited fail-open | mitigate | All four fail-opens named with dispositions and owners, in the `hooks.ts:848` register; a repo-fact clause asserts at least four disposition lines |
| T-04-DOC-02 | Repudiation | GATE-03 described as refused for all four of criterion 1's engines | mitigate | The honest claim is written verbatim and pinned by a both-directions clause asserting grok/pi/OpenCode are not called live-verified |
| T-04-DOC-03 | Repudiation | "The record survives the crash" asserted from a WAL read rather than a real kill-and-restart | mitigate | The crash test is a checkpoint acceptance criterion, run against the requirement's own wording; the `synchronous = NORMAL` ceiling is documented (Pitfall 5) |
| T-04-DOC-04 | Repudiation | A capability as unverified as pi/opencode/crush carrying zero markers — the exact gap 02-12 found | mitigate | A named zero-marker sweep over kimi, agy and the codex sandbox path, reported explicitly including a null result |
| T-04-DOC-05 | Repudiation | A repo-fact gate that passes because it parsed nothing | mitigate | D-33/D-40: joined/parsed text, positive lower bounds, comment-filtered counts, and no bare `== 0` gates |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step; the whole phase installed zero packages, asserted by an empty `package.json`/`package-lock.json` diff over the phase |
</threat_model>

<verification>
- `npm test` — full suite, 0 failures; the figure and the skipped delta from 7 stated from this session's run.
- `npm run typecheck`, `npm run lint` (`--max-warnings 0`), `npm run build` — all exit 0.
- `node --test test/repo-claims.test.cjs` — green, ledger matching a live grep.
- `git diff --stat <phase-base>..HEAD -- package.json package-lock.json` — empty.
- Live: a physical-device attempt (with its honest outcome) and a real kill-and-restart crash test.
</verification>

<success_criteria>
- SECURITY.md and TELEMETRY.md describe the boundary and the record that exist, with all four fail-opens and the honest cross-engine claim.
- The marker ledger is reconciled from a live measurement and the zero-marker sweep is reported.
- All 30 rows of the Per-Task Verification Map carry a plan, a wave and a measured status.
- Every requirement is either closed on wave-7 evidence or left open with its reason.
- No number in the closing SUMMARY was carried forward from a prior SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-19-SUMMARY.md` when done.
</output>
