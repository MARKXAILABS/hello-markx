---
phase: 04-overnight-on-a-repo-that-matters
plan: 19
type: execute
wave: 7
depends_on: ["04-01", "04-02", "04-03", "04-04", "04-05", "04-06", "04-07", "04-08", "04-09", "04-10", "04-11", "04-12", "04-13", "04-14", "04-15", "04-16", "04-17", "04-18", "04-20"]
files_modified:
  - SECURITY.md
  - TELEMETRY.md
  - test/repo-claims.test.cjs
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
autonomous: false
requirements: [GATE-02, GATE-03, GATE-04, GATE-05, RECORD-01, RECORD-02, RECORD-05, VIGIL-01, VIGIL-02, VIGIL-03, VIGIL-04]
user_setup: []

must_haves:
  decisions:
    # `D-NN:` citations for GSD's blocking decision-coverage gate, which searches only the
    # frontmatter `must_haves` block. This plan is the phase rollup, so it carries the four
    # phase-wide rules every plan is bound by; each names how THIS plan discharges it.
    - "D-07: the two inherited holes are restated in SECURITY.md as fail-opens 1 and 2 with dispositions and owners, never silently carried (task 1)."
    - "D-33: every closing clause in `repo-claims.test.cjs` asserts over joined/parsed text with a positive lower bound, and no bare `== 0` gate runs on unfiltered text (task 2)."
    - "D-34: SECURITY.md ships GATE-02's, GATE-03's (j)-(u) and GATE-05's (a)-(h) ceilings, and task 1's counting criterion asserts the doc against the source rather than summarizing it."
    - "D-35: this plan owns `repo-claims.test.cjs` in the closing wave and touches no source file; a source change needed to close the phase is a finding and a new plan."
    - "D-36: `git diff --stat -- package.json package-lock.json` over the whole phase is empty, asserted in task 1."
  truths:
    - "Every claim this phase makes about itself was re-measured against the tree at wave 7, not carried forward from any prior SUMMARY."
    - "The three inherited fail-opens (D-07's two plus `protectedPathDenial`'s `if (!root) return null`) and the one this phase adds (pi/OpenCode's ask degrading to deny) are all restated in SECURITY.md — never silently carried."
    - "The honest cross-engine claim is in SECURITY.md and pinned by `test/repo-claims.test.cjs`, so a later reader cannot inflate it."
    - "The whole-suite figure, the skipped-count delta from the 7-skip baseline, and the marker ledger are all measured in this session by commands whose output is in the SUMMARY."
    - "Every requirement checkbox flipped in REQUIREMENTS.md is backed by evidence produced at the closing wave; a requirement whose live half never ran stays OPEN with the reason. REQUIREMENTS.md is IN `files_modified` and a named task edits it — a truth about flipping checkboxes in a file no task owns is a promise the plan cannot keep."
    - "ROADMAP.md's phase-4 criterion-1 wording names four engines GATE-03 is refused on. Three of them were never live-verified here. The wording is annotated in ROADMAP.md itself, in this plan's commit, because an over-claim there becomes a design premise two phases later — this plan's own threat table says so."
  artifacts:
    - path: "SECURITY.md"
      provides: "the three new gates' ceilings, the four fail-opens, and the honest cross-engine claim"
      contains: "LIVE-UNVERIFIED"
    - path: "TELEMETRY.md"
      provides: "the tool_calls and events tables, and the honest durability level"
      contains: "tool_calls"
    - path: "test/repo-claims.test.cjs"
      provides: "the final reconciled ledger and the clauses pinning this phase's claims"
    - path: ".planning/REQUIREMENTS.md"
      provides: "the eleven requirement checkboxes, each flipped against wave-7 evidence or left OPEN with its reason"
    - path: ".planning/ROADMAP.md"
      provides: "criterion 1's four-engine wording annotated with what was actually verified"
  key_links:
    - from: "SECURITY.md"
      to: "hooks.ts:801-859's ceiling list"
      via: "the doc restates what the source already itemizes; the test pins that they agree"
      pattern: "ceiling|fail-open"
---

<objective>
Phase close. Re-measure everything this phase claims, write the two docs the requirements name, reconcile
the marker ledger one last time, and record what could not be verified — as a first-class outcome rather
than a gap.

**Purpose.** PROJECT.md's verification-honesty rule: *"an agent reporting 'fixed' is a claim, not
evidence. Every substantive change is re-verified by the orchestrator against source and a live test run.
This rule exists because it has already caught real over-claims."* Phase 2's plan 02-12 did exactly this
job and found a genuine gap in the process — `qwen` had **zero** `LIVE-UNVERIFIED` markers despite being
as unverified as pi/opencode/crush. **Assume this phase has one too, and go looking.**

`04-CONTEXT.md`'s canonical_refs names the two docs directly: *"`SECURITY.md` — must be updated by
GATE-02/03/05; `TELEMETRY.md` — by RECORD-01."*

**The rule that governs every line written here:** re-measure, never carry forward. Not from a prior
SUMMARY, not from `04-RESEARCH.md`, not from this plan. Every number in the closing SUMMARY comes from a
command run at wave 7.

**Output.** Two updated docs, a final ledger reconciliation, the filled-in verification map, and an honest
close.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
</context>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step.
- **D-35** — this plan owns `test/repo-claims.test.cjs` in the closing wave (plans 04-10, 04-13 and 04-16
  owned it in waves 3, 4 and 5). It touches **no source file**: if closing the phase requires a source
  change, that is a finding to report and a new plan, not a drive-by fix in the rollup.
- **`depends_on` lists every plan in the phase, and that is deliberate.** The first draft omitted 04-08,
  which is not transitively reachable (04-12 depends only on 04-04), while this plan flips VIGIL-02's
  checkbox from evidence 04-08 produces; 04-15 and 04-20 were likewise only transitively reachable. A
  rollup that re-measures everything depends on everything, and an under-declared edge is a plan that can
  be scheduled before the evidence it reads exists.
- **D-33 / D-40** — every repo-fact assertion asserts over joined/parsed text rather than single lines
  where a formatter could wrap the construct, and asserts a **positive lower bound** alongside the
  negative, so deleting the feature fails the test instead of satisfying it.
- **Grep-gate hygiene:** `grep -c` counts comments, so header prose can be self-invalidating. Use
  `grep -v '^#' | grep -c token`. A bare `== 0` gate on an unfiltered file is forbidden.
- The measured Phase 4 starting baseline was **805 tests / 798 pass / 0 fail / 7 skipped** — re-confirmed
  exactly in the round-3 revision session at `939e16a`. Wall time is **not** stable: the same suite
  measured 24 270 / 34 152 / 42 377 ms across three quiet runs. **Re-measure both; do not quote.**
</standing_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the whole phase, then write SECURITY.md and TELEMETRY.md</name>
  <files>SECURITY.md, TELEMETRY.md</files>
  <read_first>
    - `SECURITY.md` (whole file) — the existing structure, and specifically how prior phases recorded
      exposures. Phase 2's plan 02-12 corrected a false tunnel-close claim **in the app's favour** and
      named three real exposures; match that register.
    - `TELEMETRY.md` — the existing description of the span ring
    - `src/main/hooks.ts:801-859` (the ceiling list, now carrying plan 04-06's **(j)-(v)** — thirteen
      items, counted by that plan's own criterion — and plan 04-15's GATE-05 **(a)-(h)** — eight) and
      `:54-62` (the two inherited holes). **The ranges are (j)-(v) and (a)-(h), not (j)-(o) and (a)-(d).**
      Round 3 found the shorter forms here, and the eleven items they omit are almost exactly round 2's
      and round 3's remedies: (s) the argv-array residual, (t) the unjudged deletion spellings, (u) the
      incomplete host allowlist, (v) the `rm` arm's segmentation heuristic, (e) grok/agy's unchanged
      timeouts, (g) Claude's PreToolUse budget, (h) the 150 s latency hazard. Copying a stale range here
      silently undoes prior work in the one document a future planner reads as fact.
    - `src/main/shellEnv.ts` — GATE-02's ceiling list (plan 04-05)
    - `src/main/telemetry.ts:96` — *"only in the in-memory ring buffer, never persisted"*. **This sentence
      is now half-true and TELEMETRY.md must say what changed and what did not.**
    - `src/main/db.ts` — the `tool_calls` and `events` tables and the `synchronous = NORMAL` durability level
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Security Domain — the ASVS
      mapping and the eight threat patterns; and § Common Pitfalls **5** (`synchronous = NORMAL` is not
      `FULL`)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-10-SUMMARY.md` — `<the_honest_claim>`
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-13-SUMMARY.md` — GATE-04's spike verdict and
      live-run outcome
  </read_first>
  <action>
    **Measure first, write second.** Run and capture, in this session:
    - `npm test` — tests / pass / fail / skipped, and the **skipped delta from 7**
    - `npm run typecheck`, `npm run lint`, `npm run build`
    - `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`
    - `git diff --stat <phase-base>..HEAD -- package.json package-lock.json` (must be empty)
    - the per-plan SUMMARY claims for the eleven requirements, each re-checked against source

    **SECURITY.md** gains this phase's three new gates and — the part that matters — its **four fail-opens**,
    each with a disposition and an owner, in the `hooks.ts:848` register (*"A ceiling list that omits (b),
    (f) or (g) reads as a guarantee that does not hold"*):

    1. **Inherited, Deferred** — the user-global engine seeds (`~/.codex/config.toml`,
       `~/.gemini/.../hooks.json`, `~/.grok/hooks/`) sit outside the hive root, so an agent appending a
       PreToolUse hook block to a seed gets it executed inside another agent's hook process with that
       agent's env (D-07 hole 1, `hooks.ts:54-59`).
    2. **Inherited, deliberate** — the shim's connect-error `process.exit(0)`, which for a Claude PreToolUse
       hook is *allow*. Bounded by `armSocketWatchdog` rather than removed, because a fail-closed shim
       would break every agent whenever the app is legitimately not running (D-07 hole 2).
    3. **Inherited, ACCEPTED, owner: hive maintainer** — `protectedPathDenial`'s `if (!root) return null`
       (ceiling item (i)). It fails open because *with no hive root there is nothing to protect*, which is
       a true statement about the world — **unlike** GATE-03's empty host allowlist, which fails **closed**
       for exactly the opposite reason. Say both, together; the contrast is the point.
    4. **NEW, this phase, the only one** — pi and OpenCode receive the ask reply's deny and cannot poll, so
       for them GATE-05's "ask" degrades to "deny". That is the safe side, and it is still a behaviour that
       must appear in a ceiling list.

    Also into SECURITY.md: GATE-02's env-allowlist ceiling (from `shellEnv.ts`), **all thirteen** of
    GATE-03's **(j)-(v)** items, **all eight** of GATE-05's **(a)-(h)** items, and **the honest
    cross-engine claim verbatim from plan 04-10's SUMMARY** —
    refused for Claude and Codex (live-verified on this machine); built for grok, kimi and agy and
    unit-verified through the real `HookServer`, LIVE-UNVERIFIED for want of an installed CLI; built and
    LIVE-UNVERIFIED for pi and OpenCode. Name GATE-04's outcome as plan 04-13 measured it.

    **TELEMETRY.md** gains RECORD-01 and RECORD-02, and must correct the claim `telemetry.ts:96` still
    makes locally: spans are still an in-memory ring of 200 per agent and still never persisted — **the
    ring is deliberately unchanged, because the waterfall UI reads it** — and the durable record is a
    separate `tool_calls` table written from the hook socket, which is the only place agent, tool and
    **target** exist for every engine. State the three measured reasons the telemetry path could not carry
    it (`ToolSpan` has no `target`, `ATTR_ALLOWLIST` admits no path or command key, and the OTel block is
    Claude-only), so nobody "fixes" this by moving it back.

    State the durability level honestly (Pitfall 5): `synchronous = NORMAL` under WAL means a row survives
    a **process** crash but is not guaranteed against an OS or power loss until a checkpoint. That is the
    right trade for this app and it is **stated, not silently upgraded to `FULL`** — which would fsync on
    every tool call.

    Document `events`'s retention as a **query bound**, with the number that shipped, and note that
    `harness.db` lives in `userData`, **outside the hive root**, so it needs no `UNTRACK_PATHS` entry.

    **If any measurement contradicts a plan's SUMMARY, the measurement wins and the contradiction goes in
    the closing SUMMARY.** That is the job.
  </action>
  <acceptance_criteria>
    - SECURITY.md names all four fail-opens, each with a disposition and (where applicable) a named owner, and states the deliberate contrast between item 3's fail-open and GATE-03's fail-closed empty allowlist.
    - **The ceiling is counted against its source, not summarized from it (D-34 makes it part of the deliverable).** Measure the source first — `grep -oE "^\s*\*\s+\((j|k|l|m|n|o|p|q|r|s|t|u|v)\)" src/main/hooks.ts | wc -l` (plan 04-06's own criterion asserts **13**) and the GATE-05 block's **8** — then assert SECURITY.md carries **at least that many** GATE-03 items and **at least that many** GATE-05 items, with the two measured numbers and the two doc counts all four stated in the SUMMARY. **A doc count lower than its source count is a failed criterion, and the ten items a stale `(j)-(o)`/`(a)-(d)` range would have dropped are exactly round 2's remedies.** Count the doc by its own item markers, filtered so a heading cannot satisfy the gate (`grep -v '^#' SECURITY.md | grep -cE '^\s*[-*(]\s*\([a-v]\)'`), and pair it with a positive lower bound so an empty extraction cannot pass.
    - SECURITY.md carries the honest cross-engine claim verbatim — including its positive clauses (`through the real HookServer`, `LIVE-UNVERIFIED for pi`, the agy `exercised by no test at all` clause) — and does **not** claim grok, pi or OpenCode as live-verified or agy as unit-verified. Plan 04-10 task 3's pin asserts both directions; confirm it still does after any rewording here.
    - TELEMETRY.md states that the span ring is unchanged and why, the three reasons the writer is not on the telemetry path, and the `synchronous = NORMAL` durability level.
    - Every figure in both docs was produced by a command run in this session; the SUMMARY shows the commands and their output.
    - `npm test` / `typecheck` / `lint` / `build` all green, with the skipped count and its delta from 7 stated.
    - `git diff --stat -- package.json package-lock.json` over the whole phase is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>Two docs that describe the boundary that exists rather than the one that was designed, with every number measured at wave 7.</done>
</task>

<task type="auto">
  <name>Task 2: The final ledger, and the clauses that stop this phase over-claiming later</name>
  <files>test/repo-claims.test.cjs, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md</files>
  <read_first>
    - `test/repo-claims.test.cjs:1249-1262` — why the marker clause reads **raw** text where most clauses
      read stripped-of-comments text; `:1287-1289` — the file's own re-measure command; `:1291-1298`
      `MARKER_LEDGER`; `:1307` `LIVE_UNVERIFIED_TOTAL`; `:1419-1454` the file-set and per-engine assertions
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Per-Task Verification Map —
      **59 rows, one per task, in exact bijection with the phase's 59 tasks.** Measured in the round-3
      revision: `grep -cE '^\| (\*\*)?04-[0-9]+-T[0-9]+' 04-VALIDATION.md` → **59**, and the same pattern
      with a placeholder-coordinate suffix appended → **0**. *(Scope the placeholder check to ROWS: the
      file's own revision log discusses that token in prose, so a whole-file count measures 4 and would
      fail against a correctly-filled map — the exact grep-gate-hygiene trap this phase's own rules
      name.)* The earlier draft of this plan said *"29 behaviour rows plus a marker row, with `Task ID` /
      `Plan` / `Wave` still `_tbd_`"* — all four claims are false against the file, there is no marker
      row, and an executor who followed them would have filled 30 of 59 and closed. **Re-count the rows
      at wave 7 with the command above and state the number in the SUMMARY** rather than trusting this
      line or the map's own sign-off
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Validation Sign-Off — the
      six boxes and the `nyquist_compliant` flag
    - Every `04-*-SUMMARY.md` in the phase directory
    - `.planning/REQUIREMENTS.md` GATE (167-199), RECORD (200-223), VIGIL (224-241) — **the eleven
      checkbox rows this task edits.** Read the exact markdown the file uses for a ticked, an unticked and
      an OPEN-with-reason row before writing one.
    - `.planning/ROADMAP.md`, the Phase 4 entry — specifically criterion 1's wording, which names four
      engines GATE-03 is refused on. Plan 04-10's honest claim says three of them were never live-verified
      here. **Nothing in the phase currently touches ROADMAP.md**, so that wording would survive this phase
      unannotated and be read as fact by the next planner.
  </read_first>
  <action>
    **Re-measure the ledger with the file's own command**, never by arithmetic over plan 04-10's and
    04-13's figures: `grep -ro 'LIVE-UNVERIFIED' src/ | cut -d: -f1 | sort | uniq -c`. Reconcile all four
    pins if anything moved.

    **Then go looking for the gap 02-12 found.** For each engine and each capability this phase touched,
    ask: *is there something as unverified as pi/opencode/crush that carries zero markers?* Candidates to
    check by name: kimi and antigravity (agy) — both are main-judged hook-bridge engines that CONTEXT.md
    never mentions and neither is installed here; and codex's sandbox path if plan 04-13's spike came back
    `REFUSED/PROMPTED`. Report what you found, including "nothing".

    Add closing clauses to `test/repo-claims.test.cjs`, in its established both-directions style, pinning
    the claims this phase makes so a later edit cannot quietly inflate them.

    **The GATE-03 honest-claim pin is THIS plan's, and this is the first wave in which it can be green
    (R2-BL3).** Plan 04-10 wrote the claim in wave 3 and deliberately did not pin it: SECURITY.md — the
    natural subject, and one this file already pins prose in at `:1606-1624` — does not carry the claim
    until now, so a wave-3 pin would have been red for four waves; and the only other subject available
    then was a comment plan 04-10 had just written, which would have made the pin self-certifying. **This
    plan owns SECURITY.md and `test/repo-claims.test.cjs` in the same commit**, so the pin and its subject
    land together.

    The clause asserts **four** positive literal phrases over SECURITY.md's text, not three. The previous
    draft's set — `through the real \`HookServer\``, `LIVE-UNVERIFIED for pi`, `exercised by no test at
    all` — covers none of *"a live codex agent had a command refused on this machine"*, **which is the
    exact sentence round 1's BL-11 was about**, so inflation in the one named direction would still have
    passed. So:

    - **Positive, three unconditional:** `through the real \`HookServer\``, `LIVE-UNVERIFIED for pi`,
      `exercised by no test at all`. Softening or deleting any clause fails.
    - **Positive, one conditional and cross-checked:** read plan 04-13's SUMMARY for its literal
      `LIVE GATE-03 REFUSAL: yes` / `LIVE GATE-03 REFUSAL: no` line (task 4 is required to write exactly
      one of them). If `yes`, SECURITY.md must contain the live-refusal sentence; if `no`, it must
      **not**, and must instead carry the corrected-downward wording. **The clause fails if the SUMMARY
      line is absent or ambiguous** — an unmeasured run cannot be rounded either way.
    - **Negative:** the text does not describe grok, pi or OpenCode as live-verified, and does not claim
      agy is unit-verified.

    Follow `:1249-1262`'s guidance on raw-vs-stripped text. Use `grep -v '^#' | grep -c token` where a
    Markdown heading could satisfy a gate; a bare `== 0` on unfiltered text is forbidden. **Record a
    deliberate mutation check in the SUMMARY**: temporarily add "grok (live-verified)" to SECURITY.md,
    confirm the clause goes red, revert. A pin that has never been seen failing is a pin nobody has tested.
    - SECURITY.md names all four fail-opens (positive lower bound: at least four disposition lines).
    - TELEMETRY.md states the `synchronous = NORMAL` level and does **not** claim OS-crash durability.
    - The `tool_calls` writer is in `hooks.ts` and **not** in `telemetry.ts` — assert the negative
      (`telemetry.ts` contains no `recordToolCall`) alongside the positive (`hooks.ts` does).

    Every clause reads joined/parsed text, not single lines, and carries a positive lower bound. Use
    `grep -v '^#' | grep -c token` where a comment could satisfy the gate; a bare `== 0` on unfiltered text
    is forbidden.

    **Then edit the two planning files this plan now owns, because a truth about flipping a checkbox in a
    file no task names is a promise the plan cannot keep.**

    `.planning/REQUIREMENTS.md`: set each of the eleven requirement rows to its measured state, using the
    file's own existing markdown for ticked / unticked / OPEN-with-reason. A requirement whose live half
    never ran stays **OPEN with the reason written in the row**, in this project's standing style. The
    checkpoint in task 3 is where the operator rules on each one; this is where the ruling is recorded.

    `.planning/ROADMAP.md`: annotate criterion 1's four-engine wording with what was actually verified —
    plan 04-10's honest claim, in one clause, beside the original text rather than replacing it, so the
    diff shows what the phase learned. This plan's own threat table (T-04-DOC-01) says an over-claim in
    these files *"becomes a design premise two phases later"*; leaving the wording untouched is exactly
    that, with nobody having decided to.

    **Fill in `04-VALIDATION.md`'s Per-Task Verification Map.** The map already carries **59 rows with
    their `Task ID`, `Plan` and `Wave` filled and zero placeholder coordinates in any row** — re-count
    with `grep -cE '^\| (\*\*)?04-[0-9]+-T[0-9]+' …/04-VALIDATION.md` (expect 59) and the same pattern
    with `.*_tbd_` appended (expect 0) before touching anything. **The only cell this task changes is `Status`**, which reads the literal `pending` in all 59
    today, and each becomes ✅ / ❌ / ⚠️ from a run in this session. `⬜` appears **only in the legend
    line**, never in a cell, so "not ⬜" is not a check — assert the cell is one of the three outcome
    glyphs and that **no cell still reads `pending`**. If any row has no owning task, **say so in the row
    and in the SUMMARY** rather than deleting it; if the re-count is not 59, that is a finding, not a
    number to overwrite.

    **Then flip `04-VALIDATION.md`'s three frontmatter flags.** All three are currently wrong for a closed
    phase and none of them flips itself:
    - `wave_0_complete: false` → `true` once plan 04-01's two deliverables exist (the spike verdict and
      `test/gate-harness.cjs` self-checking green on win32);
    - `status: draft` → the file's own convention for a closed phase;
    - `nyquist_compliant: false` → `true`, **and there is no explained-exemption (M7)**. Round 1's M-17
      was downgraded to "false if explained" and round 2 caught the downgrade: the operator's standing
      mandate has no such allowance, and *"a phase does not close with `nyquist_compliant: false` in its
      own frontmatter"* is not softened by a paragraph next to it. So: flip it only after the
      plan-checker confirms Dimension 8, and **if the checker has not confirmed, the flag stays `false`,
      task 3 blocks, and the phase does not close.** That is an outcome, not a failure — but it is not a
      close. Do not write an explanation in place of the flip.

    Tick § Validation Sign-Off's boxes only against evidence.

    **Re-measure the feedback-latency figure while you are here (M6).** `04-VALIDATION.md` § Sampling Rate
    carries a **measured range, not a point**: three quiet runs of the same 805-test suite on this machine
    produced `duration_ms` **24 270 / 34 152 / 42 377** — a 1.75x spread on load alone, so the **worst**
    (42.4 s) is the planning baseline and headroom under the 90 s budget is **~48 s**, not ~55 s. Run
    `npm test` quiet, state the measured full-suite wall time and its delta from **42.4 s**, and say
    whether the phase stayed inside 90 s. If it did not, that is a finding for the SUMMARY with the two
    heaviest files named — not a budget quietly raised. This phase adds eleven test files including two
    with their own multi-second sub-budgets (`gate05-bounded-wait` at 15 s, `gate03-roundtrip`), so name
    those two first if the budget is threatened.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exits 0, and the ledger matches a live grep whose output is in the SUMMARY.
    - Four new closing clauses exist, each asserting both directions.
    - **The GATE-03 claim clause asserts FOUR positive literals**, including the live-refusal sentence, cross-checked against plan 04-13's SUMMARY line `LIVE GATE-03 REFUSAL: yes|no`. The clause **fails** if that line is absent or holds anything but `yes` or `no` — an unmeasured live run cannot be rounded in either direction, which is the whole of round 1's BL-11.
    - **The mutation check is recorded:** SECURITY.md is temporarily inflated ("grok (live-verified)"), the clause is confirmed red, and the change is reverted. Both the red output and the revert are in the SUMMARY.
    - **`nyquist_compliant` is `true`.** If the plan-checker has not confirmed Dimension 8, this criterion **fails** and the phase does not close — there is no explained-exemption (M7). Say so plainly rather than writing a paragraph beside a `false`.
    - No new clause uses a bare `== 0` gate on unfiltered text; every count-based gate filters comments.
    - **Every row** of the Per-Task Verification Map carries a `Task ID`, a `Plan`, a `Wave`, and a `Status` that is **one of `✅` / `❌` / `⚠️`**. Asserted as two measured numbers, not as "not ⬜": `grep -cE '^\| (\*\*)?04-[0-9]+-T[0-9]+.*\| (✅|❌|⚠️) \|$'` equals the row count, **and** the row-scoped draft-state count `grep -cE '^\| (\*\*)?04-[0-9]+-T[0-9]+.*\| pending \|$'` returns **`0`**. **Both greps are row-anchored on purpose:** `04-VALIDATION.md`'s own revision log discusses these tokens in prose, so a whole-file `grep -c` reads the file's commentary about itself and can never reach `0` — the self-invalidating-prose trap the standing grep-gate-hygiene rule names, one level up from a comment. *(The previous form — "a non-⬜ `Status`" — was vacuous: every cell reads the word `pending` and `⬜` occurs only in the legend, so all 59 passed unchanged.)* The row count equals the phase's task count — stated as a number in the SUMMARY, re-counted from the plans rather than quoted from the map's own sign-off line.
    - `04-VALIDATION.md`'s `wave_0_complete`, `status` and `nyquist_compliant` frontmatter flags are each **flipped**. There is no explained-exemption for any of the three, and specifically none for `nyquist_compliant` — see the criterion above, which is the binding one.
    - `.planning/REQUIREMENTS.md`'s eleven rows each carry a measured state, and every OPEN one carries its reason in the row itself. Both files are in this task's own file list as well as in `files_modified` — round 2 found the task-level list omitting them, which is how a plan promises an edit no task performs.
    - `.planning/ROADMAP.md`'s criterion-1 wording is annotated with what was actually verified, in this commit.
    - **The measured full-suite wall time and its delta from the 42.4 s worst-case baseline are in the SUMMARY**, with a statement of whether the phase stayed inside its 90 s budget and, if not, which two files carry the cost. Quote the range (24.3 / 34.2 / 42.4 s across three quiet runs), not a single figure — a one-run baseline on this machine is not reproducible.
    - The zero-marker sweep is reported explicitly, including a "nothing found" if that is the result.
    - `npm test` — full suite green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs && npm test</automated>
  </verify>
  <done>The ledger is reconciled from a live measurement, this phase's claims are pinned by tests that fail if they inflate, and the validation map is filled in from runs rather than intentions.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: The phone, on a physical device — and the honest close</name>
  <files>—</files>
  <read_first>
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Manual-Only Verifications —
      all four rows, and specifically the GATE-05 phone row: *"The PWA service worker updates on its own
      schedule; no local reproduction. **'Verification needs a real device' is a first-class outcome here,
      not a failure.**"*
    - `.planning/PROJECT.md` — the verification-honesty rule and the zero-recurring-cost constraint
    - `.planning/REQUIREMENTS.md` GATE (167-199), RECORD (200-223), VIGIL (224-241) — the eleven rows whose
      checkboxes this task decides
    - `.planning/phases/02-the-daemon-and-the-protocol/02-CONTEXT.md` — Phase 2 recorded the same
      device gap; do not re-derive it, cite it
  </read_first>
  <action>
    AUTOMATE FIRST, THEN PAUSE. Before this checkpoint may block, the executor must have already run and
    captured: the full suite, typecheck, lint and build; the live marker grep; the kill-and-restart crash
    test (kill the app mid-run, restart, and answer "who wrote this file, and what did the floor run
    overnight" from disk); and the per-requirement evidence re-checked against source at wave 7. Attempt
    the physical-Android GATE-05 answer if a device is available. Present the eleven proposed checkbox
    states with their evidence, then pause. Do NOT flip a checkbox to make the phase look finished.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
    <human-check>The physical-device attempt and its honest outcome; the kill-and-restart result; and the operator's ruling on each of the eleven checkboxes.</human-check>
  </verify>
  <what-built>
    Eleven requirements across eighteen plans: the env allowlist at the one `pty.spawn` choke point; a
    command-shape judge in main reaching seven engines; a per-engine codex sandbox opt-in; a third answer
    with a bounded wait that times out to deny, reachable from the phone; a durable tool-call record and a
    day-scannable event log; per-repo restore points over a shadow `GIT_DIR`; an absence watchdog that
    outlives the god; same-tick card release; blocked detection in main; and age on every card and ask.
  </what-built>
  <how-to-verify>
    Before pausing, the executor must have already run everything automatable and captured the transcripts.
    This checkpoint is for the two things that need the operator and for the checkbox decisions.

    Present, and pause:

    1. **The phone.** Trigger a real GATE-05 ask and answer it **from a physical Android device**. If no
       device is available, say so plainly and record it as a first-class outcome, exactly as Phase 2 did
       for DAEMON-02 — **do not** substitute a desktop browser and call it device-verified.
    2. **The crash test RECORD-01's own criterion names.** Kill the app mid-run and restart it, then answer
       *"who wrote this file, and what did the floor run overnight"* from disk. This is the requirement's
       own wording and it must be exercised, not inferred from a unit test.
    3. **The eleven checkboxes**, and the two planning files they are recorded in. For each requirement, present the evidence and the proposed state. A
       requirement whose live half never ran stays **OPEN with the reason**, in this project's standing
       style — a partially-landed fix is an open concern. Specifically expect:
       - GATE-03 — the honest claim; the OpenCode, grok and pi clauses of criterion 1 are **not** ticked.
       - GATE-04 — whatever plan 04-13's live run measured, including LIVE-UNVERIFIED if the upstream
         defect reproduced.
       - GATE-05 — the desktop and endpoint halves are verified; the physical-device half is whatever
         step 1 produced. Plan 04-17's SUMMARY says "answerable through the phone's own endpoints,
         device-unverified"; this is where that becomes a ruling.
       - VIGIL-03 — plan 04-13 task 4's live codex observation is the phase's only measurement of whether
         `BLOCK_HINTS` matches a non-Claude engine. Present it whichever way it went.
    4. **The measured close:** the whole-suite figure, the skipped-count delta from 7, the marker ledger,
       and any claim that a wave-7 measurement contradicted.

    **Do not flip a checkbox to make the phase look finished.** This project's own record is that an honest
    open concern is worth more than a green checklist that overstates reality — it has already caught four
    over-claims.
  </how-to-verify>
  <acceptance_criteria>
    - The physical-device attempt is recorded with its outcome, whichever it is.
    - The kill-and-restart crash test was actually run and its result is in the SUMMARY.
    - Each of the eleven requirements has a stated state with evidence, and every one left OPEN has a reason.
    - The SUMMARY states the whole-suite figure, the skipped delta from 7, the final marker ledger, and any contradiction a wave-7 measurement produced.
    - The operator has typed approval or named an issue.
  </acceptance_criteria>
  <resume-signal>Type "approved" or name what stays open</resume-signal>
  <done>The phase closes on measured evidence, with what could not be verified named out loud rather than rounded up.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| this phase's claims → the project's own record | SECURITY.md, TELEMETRY.md, REQUIREMENTS.md and ROADMAP.md are read by future planners as fact. An over-claim here becomes a design premise two phases later. |
| the marker ledger → the parity claim | `test/repo-claims.test.cjs` is the mechanism that makes "declare, do not fake" enforceable rather than aspirational. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-DOC-01 | Repudiation | A SECURITY.md that describes a tighter boundary than exists, by omitting an inherited fail-open | mitigate | All four fail-opens named with dispositions and owners, in the `hooks.ts:848` register; a repo-fact clause asserts at least four disposition lines |
| T-04-DOC-02 | Repudiation | GATE-03 described as refused for all four of criterion 1's engines | mitigate | The honest claim is written verbatim and pinned by a both-directions clause asserting grok/pi/OpenCode are not called live-verified |
| T-04-DOC-03 | Repudiation | "The record survives the crash" asserted from a WAL read rather than a real kill-and-restart | mitigate | The crash test is a checkpoint acceptance criterion, run against the requirement's own wording; the `synchronous = NORMAL` ceiling is documented (Pitfall 5) |
| T-04-DOC-04 | Repudiation | A capability as unverified as pi/opencode/crush carrying zero markers — the exact gap 02-12 found | mitigate | A named zero-marker sweep over kimi, agy and the codex sandbox path, reported explicitly including a null result |
| T-04-DOC-05 | Repudiation | A repo-fact gate that passes because it parsed nothing | mitigate | D-33/D-40: joined/parsed text, positive lower bounds, comment-filtered counts, and no bare `== 0` gates |
| T-04-DOC-06 | Repudiation | **A truth claiming eleven REQUIREMENTS.md checkboxes were flipped, in a plan whose `files_modified` does not contain REQUIREMENTS.md and none of whose tasks names it** — so the phase closes with the checkboxes untouched and the SUMMARY saying otherwise | mitigate | REQUIREMENTS.md is in `files_modified` and task 2 edits it by name, recording the operator's task-3 ruling row by row |
| T-04-DOC-07 | Repudiation | **ROADMAP.md's criterion-1 wording surviving the phase unannotated**, naming four engines GATE-03 is refused on when three were never live-verified — becoming, in this plan's own words, "a design premise two phases later" | mitigate | ROADMAP.md is in `files_modified` and task 2 annotates the wording beside the original rather than replacing it, so the diff shows what the phase learned |
| T-04-DOC-08 | Repudiation | **Closing the phase with `nyquist_compliant: false`, `wave_0_complete: false` and `status: draft` still in `04-VALIDATION.md`'s own frontmatter** — three flags that say the phase is not finished, on the artifact that certifies it is. Round 1 flagged it; the revision downgraded the remedy to *"false if explained"*; round 2 caught the downgrade against the operator's standing mandate, which has no explained-exemption | mitigate | Task 2 flips all three. `nyquist_compliant: false` at close is a **task failure and a non-close**, not an item with a paragraph beside it; if the plan-checker has not confirmed Dimension 8, task 3 blocks and the phase stays open |
| T-04-DOC-09 | Repudiation | **The honest-claim pin homeless.** Plan 04-10 (wave 3) named no subject file; SECURITY.md does not carry the claim until wave 7 and a comment 04-10 had just written would have been self-certifying. Meanwhile the riskiest clause — *"a live codex agent had a command refused on this machine"*, the exact sentence round 1's BL-11 was about — was in no pinned set at all, so inflation in the one named direction still passed | mitigate | The pin lands here, where SECURITY.md and `test/repo-claims.test.cjs` are both this plan's in one commit. Four positive literals including the live-refusal sentence, cross-checked against plan 04-13 task 4's machine-readable `LIVE GATE-03 REFUSAL: yes\|no`; the clause fails if that line is absent. A recorded mutation check proves the clause bites |
| T-04-DOC-10 | Repudiation | **A per-task file list that omits files the task's own action edits.** Task 2's list held only `repo-claims.test.cjs` and `04-VALIDATION.md` while its action edits REQUIREMENTS.md and ROADMAP.md — the same class of defect as T-04-DOC-06, one level down: an edit promised in prose that no task names | mitigate | Both files are in task 2's own file list as well as in `files_modified`, and both are named acceptance criteria |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step; the whole phase installed zero packages, asserted by an empty `package.json`/`package-lock.json` diff over the phase |
</threat_model>

<verification>
- `npm test` — full suite, 0 failures; the figure and the skipped delta from 7 stated from this session's run.
- `npm run typecheck`, `npm run lint` (`--max-warnings 0`), `npm run build` — all exit 0.
- `node --test test/repo-claims.test.cjs` — green, ledger matching a live grep.
- `git diff --stat <phase-base>..HEAD -- package.json package-lock.json` — empty.
- Live: a physical-device attempt (with its honest outcome) and a real kill-and-restart crash test.
</verification>

<success_criteria>
- SECURITY.md and TELEMETRY.md describe the boundary and the record that exist, with all four fail-opens and the honest cross-engine claim — **and the claim is pinned here**, in the first wave in which its subject file carries it and its pin file is free in the same commit, with the live-refusal sentence in the pinned positive set and cross-checked against plan 04-13's machine-readable line.
- The marker ledger is reconciled from a live measurement and the zero-marker sweep is reported.
- All **59** rows of the Per-Task Verification Map carry a plan, a wave and a measured status — 59, re-counted at wave 7, because the map is in exact bijection with the phase's 59 tasks and already carries its ids, plans and waves; only `Status` moves off `pending`.
- Every requirement is either closed on closing-wave evidence or left open with its reason, **recorded in `.planning/REQUIREMENTS.md`**, and ROADMAP.md's criterion-1 wording is annotated with what was actually verified.
- `04-VALIDATION.md`'s three frontmatter flags are all **flipped**. The phase does not close with `nyquist_compliant: false` — explained or otherwise (M7).
- The full-suite wall time is re-measured against the **42.4 s** worst-case baseline (range 24.3–42.4 s over three quiet runs) and the 90 s phase budget, and the delta is stated.
- No number in the closing SUMMARY was carried forward from a prior SUMMARY.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-19-SUMMARY.md` when done.
</output>
