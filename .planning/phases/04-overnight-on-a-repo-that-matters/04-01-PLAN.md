---
phase: 04-overnight-on-a-repo-that-matters
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-overnight-on-a-repo-that-matters/04-SPIKE-codex-sandbox.md
  - test/gate-harness.cjs
autonomous: true
requirements: [GATE-04, GATE-03, GATE-05]
user_setup: []

must_haves:
  decisions:
    # `D-NN:` citations for GSD's blocking decision-coverage gate, which searches only the
    # frontmatter `must_haves` block. Each names the task or criterion in THIS plan.
    - "D-37: Phase 4 starts after 02-12 lands — task 1 re-measures the codex sandbox verdict against the tree at wave 1 rather than quoting the pre-02-12 measurement CONTEXT.md carries its caveat about."
    - "D-14: the spike measures whether `--add-dir`-style writable roots actually reach the agent folder, with a negative control, so GATE-04's path-tree premise is tested before plan 04-13 builds on it."
    - "D-08: task 2's win32 harness drives a real shim child into a real `HookServer`, which is the only thing that can observe the two-phase wait's join; every GATE-03/GATE-05 integration case in the phase depends on it."
  truths:
    - "A real `codex -s workspace-write --add-dir <agentDir>` run on THIS machine has either written a file inside the agent dir or been refused, and which one it was is recorded on disk with the exact stderr."
    - "The answer to that run decides, in writing, whether plan 04-13 may claim GATE-04 live-verified or must ship a LIVE-UNVERIFIED marker citing openai/codex#23552."
    - "A second test file can drive the real hook shim as a child process into a real HookServer **on this machine (win32)**, so the two GATE integration tests it serves produce local evidence rather than a CI-only claim."
    - "The harness can drive ANY of the six shim constants, not just `HOOK_SHIM`, so a later plan can exercise grok's and agy's own translators through a real child process instead of re-implementing them in a test."
  artifacts:
    - path: ".planning/phases/04-overnight-on-a-repo-that-matters/04-SPIKE-codex-sandbox.md"
      provides: "The measured verdict on codex --add-dir, with the verbatim command, exit code and stderr"
      contains: "VERDICT:"
    - path: "test/gate-harness.cjs"
      provides: "withHookServer()/runShim() for the GATE integration tests — cross-platform, win32 included"
      exports: ["withHookServer", "runShim"]
  key_links:
    - from: "test/gate-harness.cjs"
      to: "src/main/hooks.ts HookServer"
      via: "test/load-ts.cjs transpile + real listenOn"
      pattern: "load-ts|HookServer"
    - from: "test/gate-harness.cjs"
      to: "src/main/hiveTemplates.ts HOOK_SHIM"
      via: "write the constant to a tmp file and spawn node on it"
      pattern: "HOOK_SHIM"
---

<objective>
Wave 0. Settle the one open question that changes what a later plan is allowed to claim, and build the
one piece of shared test scaffolding two later plans both need.

**Purpose.** `04-RESEARCH.md` L-11 / Open Question 1 found an OPEN upstream defect —
[openai/codex#23552](https://github.com/openai/codex/issues/23552), *"workspace-write writable_roots still
prompts for approval on listed Windows directories"* — filed against `codex-cli 0.130.0-alpha.5`. This
machine runs `codex-cli 0.128.0`, logged in via ChatGPT, and it is the **only** one of criterion 1's four
engines installed here. Whether `--add-dir` shares that defect decides whether GATE-04 (plan 04-13) ships
live-verified or ships a `LIVE-UNVERIFIED` marker. That is a measurement, not an assumption to carry
(RESEARCH A8). It is 15 minutes and it must be first.

Separately, `test/gate03-roundtrip.test.cjs` (plan 04-06) and `test/gate05-bounded-wait.test.cjs`
(plan 04-16) both have to do the same hard thing: drive the **real** shim as a child process into a
**real** `HookServer`. `04-PATTERNS.md` § Shared Pattern 6 names `test/hook-auth-roundtrip.test.cjs:65-125`
as the only in-repo model, and § No Analog Found records that no shim in this repo has ever re-connected.
Two known consumers, one hard mechanism: extract it once.

**The harness must work on win32, and that was measured before this plan was written.** The obvious
reading — that a real-shim-child test is inherently POSIX-only, because
`test/hook-auth-roundtrip.test.cjs` carries `{ skip: !POSIX }` — is wrong, and getting it wrong would
make both GATE integration tests pass **vacuously** on the only machine that runs them. That file's own
comment at `:39-41` names the actual limitation: *"On Windows the app uses a `\\.\pipe\` name instead,
**which this fixture does not model**"* — and the thing that does not model it is its hand-built
`path.join(home, 'hive', 'hooks.sock')` at `:89`. Ask `hive.sockPath()` instead and the gate disappears.
`test/boot-floor.test.cjs:178-190` already connects to that pipe on win32 with no skip, and the planner
ran the full child-process round trip on this machine during the revision session:

```
platform= win32
sock= \\.\pipe\hello-markx-8acef5346d14
child exit 0 stdout: {"saw":{"hook_event_name":"PreToolUse","tool_name":"Bash"}}
```

That is why task 2 forbids `{ skip: … }` rather than merely discouraging it.

**Output.** `04-SPIKE-codex-sandbox.md` (a verdict, not a report) and `test/gate-harness.cjs`.

**D-37 is resolved, not carried.** CONTEXT.md D-37 blocked Phase 4 on plan 02-12 being in flight.
RESEARCH's Drift Report row 0 re-measured it: 02-12 merged at `8e85748`, `MARKER_LEDGER` matches reality
at 18 markers, `node --test test/repo-claims.test.cjs` → 31/31. The block is lifted. State that in the
SUMMARY rather than re-deriving it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
</context>

<standing_constraints>
- **D-36 — never edit `package.json` or `package-lock.json`.** No `npm install` step may appear in this
  plan. `test/gate-harness.cjs` must not be added to any glob config; `npm test` is
  `node --test test/*.test.cjs` and a `.cjs` file that is not `*.test.cjs` is not picked up, which is
  exactly why the harness carries that name.
- **D-35** — this plan owns the two files in `files_modified` for wave 1. No drive-by edits.
- Re-measure the baseline at start (`npm test`), never quote it. It was **805 / 798 pass / 0 fail /
  7 skipped** at `ad3d2f7`.
- Wave gate before SUMMARY: `npm test` **full suite**, `npm run typecheck`, `npm run lint`, all green.
</standing_constraints>

<tasks>

<task type="auto">
  <name>Task 1: Run the codex --add-dir spike and record the verdict</name>
  <files>.planning/phases/04-overnight-on-a-repo-that-matters/04-SPIKE-codex-sandbox.md</files>
  <read_first>
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Risks and Landmines **L-11**
      and § Open Questions **1** and **2** — the exact flag, the exact issue number, and the reason
      `codex sandbox windows` is NOT a usable cheap probe on 0.128.0 (it needs `--permissions-profile`
      and then errors `default_permissions requires a '[permissions]' table`)
    - `src/main/hiveProvisioning.ts:207` — verbatim: *"hooks fire in INTERACTIVE codex sessions (how hive
      workers run), not in headless `codex exec`."* This bounds what the spike can settle: `codex exec`
      cannot prompt at all, so a clean `exec` run does NOT transfer to the interactive path plan 04-13
      spawns. The verdict must say so in its own words.
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-CONTEXT.md` **D-14** and **D-15** — the
      blocker is a path-tree problem (agent dir is a different tree from cwd), and the deliverable is
      exactly one engine, opt-in, with a verified fallback
    - `src/shared/agentProvider.ts:255-292` — codex's preset today:
      `autoModeFlag: '--dangerously-bypass-approvals-and-sandbox'` at `:271`, `autoFlag` at `:272`, and
      the `:262-270` comment that states the whole reason the sandbox was dropped
    - `src/main/hive.ts:1017` — `env.CODEX_HOME = installCodexHooks(...)`, the per-agent codex home, the
      nearest existing precedent for a per-agent path
  </read_first>
  <action>
    Create a throwaway probe directory pair on this machine that mirrors the real shape of the problem:
    a `cwd` (standing in for the operator's project) and a **sibling** `agentDir` (standing in for
    `<harnessHome>/hive/agents/<id>/`), i.e. NOT a subdirectory of `cwd`. Use a temp root under
    `%TEMP%`; do not touch the repo.

    Run codex non-interactively with `codex exec` (the floor spawns codex through the exec/interactive
    path, which takes `-s` and `--add-dir` as flags — do NOT chase the `[permissions]` TOML table, Open
    Question 2 explicitly rules that out) with, at minimum, these two runs:

    - **Run A (the control):** `codex exec -s workspace-write --add-dir <agentDir> "write the single
      word ok into <agentDir>/memory.md"` executed with `cwd` as the working directory.
    - **Run B (the negative control):** the identical prompt with `-s workspace-write` and **no**
      `--add-dir`. Run B is what makes Run A's result mean something — if B also succeeds, the sandbox
      was never blocking the agent dir here and Run A proves nothing.

    Record verbatim for each run: the full argv, the exit code, the last 40 lines of stderr, and whether
    `<agentDir>/memory.md` exists afterwards with the expected content. Do not paraphrase stderr.

    **One free extra observation, because it costs nothing and plan 04-10 needs it.** If either transcript
    names codex's shell tool — codex prints the tool it is invoking — record it on a line beginning
    `CODEX SHELL TOOL NAME:` followed by the observed name or the literal `unobserved`. Do **not** go
    looking for it and do **not** guess: `hiveProvisioning.ts:207` records that hooks do not fire in
    `codex exec` at all, so a `codex exec` run cannot show the hook payload and may show nothing useful.
    **The real payload measurement is plan 04-13 task 4 rider 4**, on the live interactive agent. This line
    exists so that if the name IS visible here, plan 04-10 can cite a transcript instead of a placeholder.

    Write `04-SPIKE-codex-sandbox.md` containing: the codex version from `codex --version`, the login
    state from `codex login status`, the two transcripts, and a single line beginning `VERDICT:` that is
    exactly one of:
    - `VERDICT: --add-dir WORKS in codex exec on codex-cli 0.128.0 (win32) — the INTERACTIVE path is
      decided by plan 04-13 task 4` (Run A wrote the file, Run B did not), or
    - `VERDICT: --add-dir REFUSED/PROMPTED on codex-cli 0.128.0 (win32) — GATE-04 ships LIVE-UNVERIFIED
      citing openai/codex#23552`, or
    - `VERDICT: INCONCLUSIVE — <the exact reason>` (e.g. Run B also succeeded, so the sandbox was not the
      blocker in this shape).

    **The scope limit is part of the verdict, not a footnote.** `hiveProvisioning.ts:207` records that
    codex hooks fire in INTERACTIVE sessions, not in `codex exec`, and upstream openai/codex#23552 is a
    *prompt* defect — a non-interactive run cannot prompt, so it cannot reproduce the defect even when the
    defect is present. A `WORKS` verdict therefore licenses plan 04-13 to BUILD, not to CLAIM: the claim is
    decided by 04-13 task 4's live interactive agent. Write that sentence into the file, above the verdict
    line, so plan 04-13 reads the bound at the same moment it reads the result.

    Do NOT write source code in this task and do NOT touch `agentProvider.ts`. The spike's only product
    is the verdict; plan 04-13 consumes it.

    If codex prompts interactively and blocks, that IS the finding — record the prompt text verbatim and
    take the `REFUSED/PROMPTED` verdict. Do not add an approval-bypass flag to make it pass; that would
    be measuring the thing GATE-04 exists to turn off.
  </action>
  <acceptance_criteria>
    - `04-SPIKE-codex-sandbox.md` exists and contains exactly one line matching `^VERDICT: `.
    - The file contains the literal output of `codex --version` and `codex login status` run in this
      session, not a quoted figure from RESEARCH.md.
    - Both Run A and Run B transcripts are present, each with its full argv line, its exit code, and a
      stated `<agentDir>/memory.md exists: yes|no`.
    - The file names `openai/codex#23552` and states whether it reproduced here.
    - The file contains exactly one line matching `^CODEX SHELL TOOL NAME: ` — either an observed name or the literal `unobserved`. A missing line is a failed task; a **guessed** name is worse than `unobserved`, because plan 04-10 would cite it as measured.
    - The file states, in its own words, that `codex exec` is non-interactive (`hiveProvisioning.ts:207`) and that the interactive path is decided by plan 04-13 task 4 — so a `WORKS` verdict cannot be read as a live-verified claim.
    - **No** file under `src/` is modified by this task (`git status --porcelain src/` → empty).
  </acceptance_criteria>
  <verify>
    <automated>grep -c '^VERDICT: ' .planning/phases/04-overnight-on-a-repo-that-matters/04-SPIKE-codex-sandbox.md</automated>
  </verify>
  <done>A single `VERDICT:` line on disk that plan 04-13 can read without re-running anything, backed by two transcripts including a negative control.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the real-shim-child → real-HookServer harness</name>
  <files>test/gate-harness.cjs</files>
  <read_first>
    - `test/hook-auth-roundtrip.test.cjs` lines **65-125** — the only in-repo model for spawning the real
      shim as a child process against a real `HookServer`. Copy its mechanism, do not invent one.
    - `test/load-ts.cjs` lines **46-64** — the electron stub. `hooks.ts` imports `WebContents` and uses
      `Notification` as a **value** (RESEARCH L-03), so the harness cannot `require` `hooks.ts` without it.
    - `test/boot-floor.test.cjs` lines **71-85** (per-test isolated `(userData, harnessHome)`) and
      **:178-190** — the case `bootFloor: net.createConnection(floor.hive.sockPath()) is accepted`, which
      **runs on win32** (it carries no `{ skip: !POSIX }`) and successfully connects to a `\\.\pipe\`
      name. Its `:183` comment *"Connect, never stat"* is an argument for not `stat`-ing, NOT an argument
      for skipping on win32.
    - `src/main/hive.ts:534-541` — `sockPath()`: a `\\.\pipe\hello-markx-<sha1(root)[0:12]>` NAME on
      win32, a `<root>/hooks.sock` file on POSIX, and its own comment: *"Both the server (`listen`) and the
      shim (`createConnection`) read this same value, so they stay in sync."*
    - `test/hook-auth-roundtrip.test.cjs:39-41` — the reason THAT fixture is POSIX-gated, in its own words:
      *"The shim connects to a UNIX domain socket path. On Windows the app uses a `\\.\pipe\` name
      instead, **which this fixture does not model**."* And `:89` — `const sock = path.join(home, 'hive',
      'hooks.sock')`, the hand-built path that is the actual limitation. **A fixture limitation, not a
      platform one.** The new harness fixes it by asking `hive.sockPath()` instead of hand-building.
    - `src/main/hiveTemplates.ts:298-347` — `HOOK_SHIM`: the connect at `:340`, the reply read at
      `:342-345`, `setTimeout(() => process.exit(0), 5000).unref()` at `:347`, and the
      `c.on('error', () => process.exit(0))` fail-open line that D-08 clause 3 requires be preserved
    - `src/main/hooks.ts:586` (`listenOn`), `:626` (`authorized()` deriving agentId from the per-agent
      token), `:630` (`conn.end(JSON.stringify(res ?? {}))` — one write then close)
  </read_first>
  <action>
    Create `test/gate-harness.cjs` — a CommonJS module, **not** a test file (the name must not end
    `.test.cjs`; `npm test` globs `test/*.test.cjs` and this must add zero test cases). Export exactly
    two functions:

    - `withHookServer(opts, fn)` — creates an isolated `(userData, harnessHome)` tmp pair in the
      `boot-floor.test.cjs:71-85` style, constructs a real `HookServer` through `test/load-ts.cjs`,
      calls `server.start()` (which reads `hive.sockPath()` itself), mints a per-agent token for
      `opts.agentId`, `await`s `fn(ctx)` and tears everything down in a `finally`. `ctx` exposes at
      minimum `{ sock, token, agentId, server, hive, harnessHome, userData }`, where **`sock` is
      `hive.sockPath()` — never a hand-built `join(home,'hive','hooks.sock')`.**
    - `runShim(ctx, payload, opts)` — writes a **verbatim shim constant** from
      `src/main/hiveTemplates.ts` to `<tmp>/<name>.cjs`, spawns `process.execPath` on it with
      `HIVE_SOCK`, `HIVE_SOCK_TOKEN` and `AGENT_ID` in its env, pipes `JSON.stringify(payload)` to its
      stdin, and resolves `{ code, stdout, stderr, elapsedMs }`. **`opts.shim` selects WHICH constant**,
      defaulting to `HOOK_SHIM`; `GROK_HOOK_SHIM` and `AGY_HOOK_SHIM` must both be selectable, because
      plan 04-10 needs to exercise grok's own reply translator through a real child process rather than
      re-implementing its decoder in a test. It must **let the shim build the wire bytes** — never
      hand-construct the payload the server receives (RESEARCH Pitfall 2).

    **This harness runs on win32, and that is a requirement, not an aspiration.** `hive.sockPath()`
    returns a `\\.\pipe\` NAME on win32 and both `net.createServer().listen(name)` and the shim's own
    `net.createConnection(process.env.HIVE_SOCK)` accept it — `test/boot-floor.test.cjs:178-190` already
    proves the server half on win32 with no skip, and the planner measured the full child→pipe→server
    round trip on this machine in the revision session (transcript in `<objective>` above).

    The only thing that made `test/hook-auth-roundtrip.test.cjs` POSIX-only is its own hand-built
    `path.join(home,'hive','hooks.sock')` at `:89` — its `:39-41` comment says so in those words
    (*"which this fixture does not model"*). Do not copy that limitation forward. **Add no
    `{ skip: !POSIX }` to this file and hand none to its consumers.** If a win32 round trip genuinely
    cannot be made to work, that is a **stop-and-report** naming the exact failure — not a licence to
    POSIX-gate, because the two consumers (`test/gate03-roundtrip.test.cjs`,
    `test/gate05-bounded-wait.test.cjs`) are criterion 2's ONLY admissible evidence and this is the only
    machine the phase runs on.

    Never call `existsSync` on the socket path (Pitfall 3) — on win32 a pipe name has no filesystem entry
    and the answer would always be `false`.

    Give the module a header comment stating (a) that it is deliberately not a `*.test.cjs` file and why,
    (b) that it exists because `04-PATTERNS.md` § No Analog Found records the shim poll loop as genuinely
    new code with no in-repo precedent, and (c) that its two consumers are `test/gate03-roundtrip.test.cjs`
    and `test/gate05-bounded-wait.test.cjs`.

    Add a `test/gate-harness.selftest.cjs`? **No.** Prove it instead inside the file with an
    `if (require.main === module)` self-check that does one round trip against a `PreToolUse` payload for
    a benign tool and asserts the shim exited 0 — one runnable check, no framework, per house style.
  </action>
  <acceptance_criteria>
    - `node test/gate-harness.cjs` **on this win32 machine** exits 0 and prints a line containing
      `selfcheck ok` — not `skipped`. A skipped self-check is a failed task.
    - The self-check's round trip goes through a spawned child process and a real `HookServer`, and its
      printed line names the socket path it used (so the transcript shows a `\\.\pipe\` name).
    - `node -e "const h=require('./test/gate-harness.cjs'); if(typeof h.withHookServer!=='function'||typeof h.runShim!=='function') process.exit(1)"` exits 0.
    - `grep -c 'existsSync' test/gate-harness.cjs` returns `0` (Pitfall 3).
    - `grep -c 'sockPath' test/gate-harness.cjs` is ≥ 1 and `grep -c "hooks.sock" test/gate-harness.cjs`
      returns `0` — the socket path is asked for, never hand-built (this is the whole of the win32 fix).
    - `grep -c 'HOOK_SHIM' test/gate-harness.cjs` is ≥ 1 — the shim bytes come from the source constant,
      not from a copy pasted into the test — and `runShim` accepts an `opts.shim` selector, proven by a
      self-check round trip through `GROK_HOOK_SHIM` as well as `HOOK_SHIM`.
    - `grep -c 'skip' test/gate-harness.cjs` returns `0`.
    - `npm test` test-count delta is **0** — the harness adds no cases.
    - `node --test test/suite-integrity.test.cjs` is green: its clause-3 census (`DECLARED_SKIPS`,
      `:195-203`) and its clause-3b floor (`FROZEN = { win32: 6, other: 1 }`, `:256`) are both unchanged,
      because this plan adds no `skip:` option anywhere.
  </acceptance_criteria>
  <verify>
    <automated>node test/gate-harness.cjs && node --test test/suite-integrity.test.cjs && npm test</automated>
  </verify>
  <done>Two later plans can write a real-shim-child integration test **that runs on this machine**, without re-solving the spawn/token/teardown problem, and the whole suite's case count and skip count are both unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| codex CLI → this machine's filesystem | The spike deliberately runs a real agent CLI with a sandbox flag against temp directories. It is the only place in this plan where untrusted-ish code executes. |
| test harness → real `HookServer` socket | The harness mints a real per-agent token and spawns a real child process against a real socket, in a tmp `harnessHome`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SBX-01 | Tampering | codex spike writing outside its temp root | mitigate | Both probe dirs live under `%TEMP%`; acceptance criterion asserts `git status --porcelain src/` is empty after the task |
| T-04-SBX-02 | Repudiation | The spike verdict being asserted rather than measured | mitigate | Verbatim argv + exit code + stderr for both runs, plus a **negative control** (Run B) whose success would invalidate Run A |
| T-04-ASK-01 | Spoofing | Harness minting a token that leaks into a committed file | mitigate | Tokens are minted per `withHookServer` call into a tmp `userData` and torn down in `finally`; nothing is written under the repo |
| T-04-CMD-16 | Repudiation | A POSIX-gated harness making both GATE integration tests pass **vacuously** on the only machine the phase runs on — a skipped case has no RED, so 04-16's "the RED run is recorded" would be unproducible | mitigate | The win32 named-pipe path is a **requirement** of this task, measured by the planner before the plan was written; the self-check must print `selfcheck ok` on win32 and `grep -c 'skip'` must return 0 |
| T-04-SBX-09 | Repudiation | A green `codex exec` spike being read as evidence for the INTERACTIVE path 04-13 spawns, when `hiveProvisioning.ts:207` records that hooks (and prompts) only fire interactively | mitigate | The verdict line names `codex exec` explicitly and defers the claim to 04-13 task 4; an acceptance criterion requires the scope sentence in the file |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | **No install step exists in this phase.** `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed. Any `npm install` in this plan is a D-36 violation and a stop-and-report |
</threat_model>

<verification>
- `npm test` — full suite, green, test count **unchanged** from the re-measured baseline.
- `npm run typecheck` — 0 errors (this plan adds no TypeScript).
- `npm run lint` — `--max-warnings 0`, exit 0.
- `git diff --stat -- package.json package-lock.json` — empty (D-36).
</verification>

<success_criteria>
- A `VERDICT:` line exists on disk that plan 04-13 consumes verbatim, with a negative control behind it and its `codex exec`-only scope stated in the file.
- `test/gate-harness.cjs` exists, self-checks **green on win32**, exports `withHookServer` and `runShim`, selects its shim constant by name, and adds zero test cases and zero skips.
- The SUMMARY states the re-measured baseline (tests/pass/fail/skipped) and confirms D-37's block is lifted at `8e85748`.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-01-SUMMARY.md` when done.
</output>
