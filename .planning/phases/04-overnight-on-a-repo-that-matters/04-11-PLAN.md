---
phase: 04-overnight-on-a-repo-that-matters
plan: 11
type: execute
wave: 3
depends_on: ["04-09"]
files_modified:
  - src/main/floor/watchdog.ts
  - src/main/floor/boot.ts
  - src/main/push.ts
  - test/absence-watchdog.test.cjs
  - test/boot-floor.test.cjs
autonomous: true
requirements: [VIGIL-01]
user_setup: []

must_haves:
  truths:
    - "Silence past the threshold alarms ONCE: one alarm on the transition into quiet, zero on the next five ticks, and one again only after activity has resumed and silence returned."
    - "The alarm fires when the GOD is the dead one, and it is addressed to the OPERATOR — not `to: 'god'`. A beat addressed to the god cannot report that the god died, which is the one case VIGIL-01 names explicitly."
    - "The alarm payload names the `doing` cards and their assignees as captured AT THE TRANSITION, not as they look when the operator finally reads it."
    - "The timer is torn down by `floor.shutdown()`, through the one `SHUTDOWN_STEPS` list."
    - "The watchdog survives the thing it watches: it lives in main's boot seam, not in a mission dispatched to an agent and not in the renderer, so it works on a headless floor and after the god dies."
  artifacts:
    - path: "src/main/floor/watchdog.ts"
      provides: "the quiet computation, the edge-trigger latch, and the in-flight snapshot — electron-free"
      exports: ["AbsenceWatchdog"]
    - path: "src/main/floor/boot.ts"
      provides: "the timer, armed beside fleetTimer and torn down in SHUTDOWN_STEPS"
      contains: "watchdog"
    - path: "test/absence-watchdog.test.cjs"
      provides: "once-only, god-death, and in-flight-payload assertions with an injected clock"
  key_links:
    - from: "AbsenceWatchdog"
      to: "ptyManager.idleFor / the ledger rev / appendLog"
      via: "injected readers, never imports"
      pattern: "idleFor|rev"
    - from: "AbsenceWatchdog"
      to: "deps.notify + the phone response + push.ts"
      via: "the three D-25 channels, one alarm per quiet edge"
      pattern: "notify"
---

<objective>
VIGIL-01: nothing happening is itself an event. When no card advances, no mail routes and no spend lands
for longer than a threshold, the operator is told **once**, with what was in flight when it stopped —
**including the case where the god itself died**.

**Purpose, and the trap this plan exists to avoid.** D-23 is emphatic: this is **not** `HEARTBEAT_MISSION`,
and conflating them fails the requirement's hardest clause. Measured at `src/main/config.ts:95-107`:
`enabled: false` (`:104`), `intervalMs: 120_000`, `quietThresholdMs: 300_000` (`:106`), and `to: 'god'`
(`:98`). Its documented job (`config.ts:85-94`) is to *"drop a digest into god's inbox and (if god's PTY is
genuinely idle) nudge it to re-engage anyone stalled"*, and it ships disabled precisely because *"the
heartbeat types into god's PTY."* **A beat addressed to the god cannot report that the god died.** So
VIGIL-01 is a distinct, **operator-directed** watchdog. It may reuse the heartbeat's quiet computation and
its scheduler slot; it may not be the same message. `HEARTBEAT_MISSION` is **read-only** for this plan.

**Where it lives, and why that is the whole design.** D-25: the watchdog cannot live anywhere that dies
with the god. `floor/boot.ts` owns the module-level `let` block (`:150-180`) and the `Floor` interface
(`:97-146`) whose `shutdown` is documented as *"the exact inverse of construction. #34 — ONE list"*. A
`setInterval` created in `bootFloor` and cleared in `shutdown` alongside `fleetTimer` (`:155`) and
`breakerBeatTimer` (`:156`) is the established shape. It survives the god's death because it is not the
god, and it survives a closed window because `floor/headless.ts` boots the same floor with no renderer.

**D-24 — the quiet signal already exists in main; do not add a second one.** `PtySession.lastOutputAt`
(`pty.ts:59`, doc `:54-58`) is *"the most recent byte this PTY emitted"*, and its own doc records that the
heartbeat already reads it for *"floor-quiet detection (an agent printing/thinking counts as activity even
before it writes a hive file)"*. RESEARCH found two further assets CONTEXT.md never names: **`idleFor(id)`
at `pty.ts:940-945`** — the per-PTY silence, already computed — and `outputTail(id)` at `:930`. Combine
`idleFor` with the ledger's last mutation and the router's last delivery for the requirement's three-part
test. Every signal in this plan's `AbsenceWatchdogDeps` is a read of something that already exists.

**And it needs nothing from the RECORD track.** RESEARCH § Open Question 3 recommends the watchdog read
the **ledger `rev`** (`hive.ts:2118-2123`, bumped on every mutation) plus `appendLog`, rather than
`tool_calls` or a per-card `updatedAt`. That dissolves S-3 and S-5 and lets the VIGIL and RECORD tracks
run fully independently under `use_worktrees: false`. **Take it** — one integer that already exists, and
it is exactly "did the ledger change".

**The `index.ts` collision dissolves too.** The Collision Map scores `index.ts` two-way (GATE-05's
`openPhoneAsks` at `:1219-1236`, VIGIL-01's wiring at `:4763-4795`) — 3,527 lines apart. But it notes:
*"If the plan keeps the watchdog entirely inside `boot.ts` and reuses the already-wired `deps.notify`,
`index.ts` needs no VIGIL-01 edit at all and this collision dissolves."* **Do that.** `index.ts` is not in
this plan's `files_modified` and must not appear in its diff.

**Output.** `floor/watchdog.ts`, the timer in `boot.ts`, the push payload, and two test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-UI-SPEC.md
@.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md
</context>

<interfaces>
The three-part quiet signal and the alarm shape. Plans 04-17 (the phone's `floorQuiet` field) and 04-18
(the desktop `QUIET` chip) consume this and will not re-derive it.

```ts
// src/main/floor/watchdog.ts — electron-free.

export interface QuietSnapshot {
  /** Duration, never a timestamp. 04-UI-SPEC rule G-3: the client's clock is not
   *  the floor's clock, and a deadline sent to a client is optimistic by its skew. */
  sinceMs: number;
  /** Cards in `doing` at the MOMENT OF THE TRANSITION, with their assignees. */
  inFlight: Array<{ id: string; title: string; assignee?: string }>;
  /** True when the god's own PTY is among the dead. Changes the copy entirely. */
  godDead: boolean;
}

export interface AbsenceWatchdogDeps {
  now(): number;
  /** Max idleFor across live agent PTYs (pty.ts:940-945). */
  ptyIdleMs(): number;
  /** hive.ts:2118-2123 — bumped on EVERY ledger mutation. One integer. */
  ledgerRev(): number;
  /** Epoch ms of the last appendLog write of any kind. */
  lastEventAt(): number;
  /** Epoch ms of the last telemetry cost sample (telemetry.ts:571). */
  lastSpendAt(): number;
  doingCards(): Array<{ id: string; title: string; assignee?: string }>;
  godAlive(): boolean;
  /** The three D-25 channels. All three, once per quiet edge. */
  notify(a: { title: string; body: string }): void;
  publishQuiet(s: QuietSnapshot | null): void;   // the phone's sibling field + the desktop chip
  push(a: { title: string; body: string; taskId: string }): void;
}

export class AbsenceWatchdog {
  constructor(deps: AbsenceWatchdogDeps, opts?: { thresholdMs?: number });
  /** Called from the boot timer. Idempotent per tick. */
  tick(): void;
  /** Current snapshot, or null when the floor is moving. */
  current(): QuietSnapshot | null;
}
```
</interfaces>

<standing_constraints>
- **D-36** — never edit `package.json` / `package-lock.json`. No install step.
- **D-35 — `src/main/floor/boot.ts` is this plan's in wave 3.** Plan 04-09 owned it in wave 2 for the
  restore-point timer. Both register in the same two places (`:150-180` and `SHUTDOWN_STEPS:928-956`);
  per-wave ownership is what keeps that safe. Read plan 04-09's diff before adding the second timer.
- **D-35 — `src/main/index.ts` is NOT in this plan.** The collision dissolves by keeping the watchdog in
  `boot.ts` and reusing the already-wired `deps.notify`. If the executor concludes `index.ts` must change,
  that is a stop-and-report.
- **`HEARTBEAT_MISSION` (`config.ts:95-107`) is READ-ONLY.** Reuse its quiet *computation* and its
  scheduler slot if useful; do not enable it, do not re-address it, do not extend it.
- **The module must be electron-free** — it runs on all three CI runners.
- **ADR-0001** — this plan adds no PTY writer. The alarm goes to the operator, never into a terminal.
- **D-33 / D-40** — assert a positive lower bound alongside every negative.
- Wave gate: `npm test` (full suite), `npm run typecheck`, `npm run lint`.
</standing_constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The watchdog — quiet computation, edge latch, in-flight snapshot</name>
  <files>src/main/floor/watchdog.ts, test/absence-watchdog.test.cjs</files>
  <behavior>
    - Threshold crossed → exactly ONE alarm; the next five ticks produce zero; activity then silence produces one more.
    - The god's PTY dead → the alarm still fires, `godDead: true`, addressed to the operator.
    - The payload's `inFlight` is the `doing` set as it was at the transition, even if cards changed since.
    - Any one of the four signals moving (PTY output, ledger `rev`, `appendLog`, spend) clears the latch.
    - `current()` returns `null` while the floor is moving.
  </behavior>
  <read_first>
    - `src/main/config.ts:85-108` — `HEARTBEAT_MISSION` in full, including the doc at `:85-94` that says it
      *"types into god's PTY"* and `to: 'god'` at `:98`. **This is the thing VIGIL-01 must not be.**
    - `src/main/pty.ts:54-59` — `lastOutputAt` and its doc: *"an agent printing/thinking counts as activity
      even before it writes a hive file"*; `:930-933` `outputTail`; **`:940-945` `idleFor(id)`** — the
      per-PTY silence, already computed, which CONTEXT.md never names
    - `src/main/hive.ts:2118-2123` — the ledger `rev`, bumped on every mutation. **This is the "no card
      advances" signal** (Open Question 3's recommendation), not a per-card `updatedAt`.
    - `src/main/hive.ts:1377` and `:1641` — `appendLog` sites for `kind:'message'` / `kind:'drain'`, the
      "no mail routes" signal
    - `src/main/telemetry.ts:571` — the last cost sample timestamp, the "no spend lands" signal
    - `src/main/delivery.ts:270` — `quiesced: Set<string>`, cleared at `:735` when an agent is no longer
      quiet. **This is the in-repo model for the edge-trigger-and-clear latch** — same shape, same process.
    - `src/main/floor/deps.ts:1-25` — injected dependencies over imports, house law
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-RESEARCH.md` § Pattern 7 in full, and
      § Open Questions **3**
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-PATTERNS.md` § absence watchdog — the two
      analogs: `delivery.ts:713-753` for the latch, `floor/boot.ts:506-518` (`armAlwaysOnBeats`) for the
      timer shape
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-UI-SPEC.md` § S6a — the three-case copy,
      including the god-died wording *"Michael is gone. The floor has no orchestrator, and nothing has
      moved for 32m."*, and rule Q-3 (*"told once means told once. No repeats, no escalation ladder, no
      re-fire on a timer."*)
  </read_first>
  <action>
    Create `src/main/floor/watchdog.ts` exporting `AbsenceWatchdog` per the `<interfaces>` block. Every
    collaborator arrives injected (house law). No electron import, no `import` of `pty.ts`, `hive.ts`,
    `telemetry.ts` or `config.ts`.

    **The quiet test is four-part, and all four must be silent** for the floor to count as stopped:
    max `ptyIdleMs()` past the threshold, `ledgerRev()` unchanged since the last tick, `lastEventAt()` older
    than the threshold, `lastSpendAt()` older than the threshold. Any one of them moving clears the latch —
    that is the "clear on the first real activity" half of D-25, and `delivery.ts:735` is the in-repo
    precedent for the shape.

    **The latch is the whole requirement.** Edge-trigger on the transition into quiet; store
    `{since, inFlight, godDead}` captured **at the transition**; hold it until real activity resumes; then
    allow a fresh edge. Capturing `inFlight` at the transition is not a detail — the requirement says
    *"with what was in flight when it stopped"*, and re-reading `doingCards()` when the operator opens the
    notification would report a different (possibly empty) set.

    **`godDead` changes the message, not just a flag.** When `godAlive()` is false, the copy is
    04-UI-SPEC's third case. And the message goes to the **operator** through `notify`, `publishQuiet` and
    `push` — never `to: 'god'`, never into a PTY.

    Threshold: a plain module constant, default **300 000 ms** (5 minutes) to match `HEARTBEAT_MISSION`'s
    own `quietThresholdMs` — reusing the number the floor already reasoned about rather than inventing a
    second one. Overridable through `opts`. Mark it `[ASSUMED]` in a comment: nothing in the research
    session measured the right value, and D-25 leaves it open.

    Fire all three channels exactly once per edge (rule Q-3). `publishQuiet(snapshot)` sets the persistent
    surfaces and `publishQuiet(null)` clears them when the latch clears — the persistent channels are a
    **state**, not a repeat notification, and states do not fatigue.
  </action>
  <acceptance_criteria>
    - `node --test test/absence-watchdog.test.cjs` exits 0 with at least six cases.
    - The once-only case asserts the exact sequence: **1 alarm on the transition, 0 across the next 5 ticks, 1 again after activity→silence.** Not "an alarm fired" — the next-tick assertion is what catches per-tick spam (VALIDATION.md's anti-vacuous rule for VIGIL-01).
    - The god-death case asserts `godDead === true`, that `notify` was called, and that **no** call carried a recipient of `'god'` — assert the negative explicitly.
    - The in-flight case mutates `doingCards()` between the transition and the assertion, and asserts the payload still carries the transition-time set.
    - Four separate cases each move exactly one of the four signals and assert the latch clears — a single combined case would pass while three signals were ignored.
    - `grep -c "from 'electron'" src/main/floor/watchdog.ts` returns `0`; `grep -cE "from '\.\./(pty|hive|telemetry|config)'" src/main/floor/watchdog.ts` returns `0`.
    - `grep -c "HEARTBEAT_MISSION" src/main/floor/watchdog.ts` returns `0` and `git diff src/main/config.ts` is empty.
    - The RED run is recorded in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/absence-watchdog.test.cjs</automated>
  </verify>
  <done>A pure, injected, electron-free watchdog that alarms once per quiet edge, carries the transition-time in-flight set, addresses the operator even when the god is the dead one, and clears on any one of four independent signals.</done>
</task>

<task type="auto">
  <name>Task 2: Arm it in the boot seam, and tear it down through the one list</name>
  <files>src/main/floor/boot.ts, src/main/push.ts</files>
  <read_first>
    - `src/main/floor/boot.ts:97-146` — the `Floor` interface and `shutdown`'s doc: *"the exact inverse of
      construction. #34 — ONE list"*
    - `src/main/floor/boot.ts:150-180` — the module-level `let` block; `:155` `fleetTimer`, `:156`
      `breakerBeatTimer`, and (after plan 04-09) `restorePointTimer`
    - `src/main/floor/boot.ts:506-518` — `armAlwaysOnBeats`, the established timer-arming shape
    - `src/main/floor/boot.ts:928-956` — `SHUTDOWN_STEPS`
    - `src/main/floor/boot.ts:411-413` — the `readConfig().notifications` gate every other toast honours
    - `src/main/floor/headless.ts` (66 lines) — the floor that boots with no renderer; the watchdog must
      work there unchanged
    - `src/main/push.ts:6-16` — the injected transport, the leaf exemplar of house DI; and the whole file
      (293 lines, VAPID from `node:crypto`, unit-testable with no network)
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-UI-SPEC.md` § S6a rules **Q-3** and **Q-4** —
      one toast and one push per quiet edge; the push payload's `title` must be **self-sufficient**
      (`The floor has stopped`, `Michael is gone` — never `Floor`, never `Alert`) because an old service
      worker ignores `body`; and `taskId: 'floor-quiet'` as a **distinct, stable tag** so the alarm
      replaces its own previous notification and never collides with a real ask
  </read_first>
  <action>
    In `floor/boot.ts`: declare a `watchdogTimer` in the module `let` block (`:150-180`) beside the
    existing timers, construct the `AbsenceWatchdog` in `bootFloor` with its deps wired from what the boot
    seam already has in scope, arm the interval in the `armAlwaysOnBeats` style (`:506-518`), and register
    the teardown in `SHUTDOWN_STEPS` (`:928-956`).

    **Reuse `deps.notify`, already wired.** That is what makes `index.ts` unnecessary here. Gate the toast
    on `readConfig().notifications` exactly as `boot.ts:411-413` gates every other one — an operator who
    turned notifications off did not ask for this one either.

    In `push.ts`: add the alarm's call site only. Per rule Q-4, the payload's **title must be
    self-sufficient** — `The floor has stopped` or `Michael is gone` — because `sw.js:26-42` hard-codes
    `body: 'is waiting on you'` and an **installed old service worker ignores a new `body` field**. A title
    of `Floor` renders as `Floor is waiting on you`, which is false. Use `taskId: 'floor-quiet'` as the tag
    (it satisfies `PHONE_TASK_ID_RE`, `webhook.ts:231`) so the alarm replaces itself and never stacks on a
    real ask.

    **Do not change `sw.js` here.** Its one-line `body` fallback is plan 04-17's, alongside the rest of the
    phone surface, and Q-5 makes service-worker changes a live-device risk with no local reproduction.

    `publishQuiet` is wired to a no-op or a simple store in this plan; plan 04-17 connects it to the phone
    response's `floorQuiet` sibling field and plan 04-18 to the desktop `QUIET` chip. Declare the seam,
    leave the surfaces to their owners.

    Extend `test/boot-floor.test.cjs` with one assertion: the watchdog timer is **cleared by
    `floor.shutdown()`**. `boot.ts:97-146` calls `SHUTDOWN_STEPS` the one list, and a timer that is not in
    it is a leak.
  </action>
  <acceptance_criteria>
    - `grep -c 'watchdog' src/main/floor/boot.ts` ≥ 3 (declaration, arm, teardown).
    - The teardown is inside `SHUTDOWN_STEPS`; `node --test test/boot-floor.test.cjs` includes a case asserting the timer is cleared by `floor.shutdown()` and it passes.
    - The toast is gated on `readConfig().notifications` — the same expression `boot.ts:411-413` uses.
    - `grep -c "'floor-quiet'" src/main/push.ts` returns `1`.
    - The push title is asserted self-sufficient: a test checks the title alone reads as a complete sentence-fragment (contains `stopped` or `gone`) and is not the bare word `Floor` or `Alert`.
    - `git diff --stat src/main/index.ts` is **empty** and `git diff --stat resources/phone/sw.js` is **empty**.
    - `npm run typecheck` → 0 errors.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/boot-floor.test.cjs && npm run typecheck</automated>
  </verify>
  <done>The watchdog runs from the boot seam on a windowed and a headless floor alike, is torn down through the one shutdown list, and reaches a pocket with a title that is true even on an old service worker.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the whole-suite blast radius and record the numbers</name>
  <files>test/boot-floor.test.cjs</files>
  <read_first>
    - `test/boot-floor.test.cjs` — Phase 2's gate for anything under `src/main/floor/`; the model for
      testing this tier
    - `.planning/phases/04-overnight-on-a-repo-that-matters/04-VALIDATION.md` § Sampling Rate and
      § Validation Sign-Off
  </read_first>
  <action>
    Run the full suite, `typecheck` and `lint`. `boot.ts` is the composition root; a subset run is not
    evidence.

    Record in the SUMMARY: the pre-plan and post-plan test figures, the delta attributed case by case, the
    skipped count (unchanged — nothing here is POSIX-only), the threshold value chosen and its `[ASSUMED]`
    marking, and the explicit statement that `index.ts`, `config.ts` and `sw.js` were **not** touched.

    Also state, in one line, why `HEARTBEAT_MISSION` was not reused: it is addressed `to: 'god'` and types
    into god's PTY, so it cannot report that the god died. That sentence is the phase's hardest clause and
    a reader of the SUMMARY should not have to reconstruct it.
  </action>
  <acceptance_criteria>
    - `npm test` → 0 failures; skipped count equal to the re-measured baseline.
    - `npm run typecheck` → 0 errors. `npm run lint` → exit 0.
    - The SUMMARY states the test-count delta case by case, the threshold and its `[ASSUMED]` marking, and the three untouched files.
    - The SUMMARY contains the one-line reason `HEARTBEAT_MISSION` was not reused.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <done>Full suite green with an explained delta, and the design's load-bearing reasoning on record.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the floor's own liveness signals → an operator alarm | Four independent readings (PTY idle, ledger `rev`, `appendLog`, spend). A watchdog that trusts one of them is blind to three failure shapes. |
| the alarm → the operator's phone / desktop | Notification bodies leave the machine through Web Push. `sw.js:33-35` records why the body must never carry a question, a path or a command. |
| the watchdog's own liveness | It must not depend on the god, on a window, or on an agent. That is the whole design. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ABS-01 | Denial of service | The floor stalling silently — every escalation today is edge-triggered and `HEARTBEAT_MISSION` ships disabled | mitigate | An operator-directed watchdog in the boot seam, on a four-part quiet test, with the three D-25 channels |
| T-04-ABS-02 | Repudiation | A watchdog that dies with the thing it watches — the god, or the window | mitigate | Lives in `floor/boot.ts`, electron-free, works unchanged under `floor/headless.ts`; the god-death test case asserts it fires with `godAlive() === false` |
| T-04-ABS-03 | Denial of service | Per-tick alarm spam training the operator to ignore it | mitigate | Edge-trigger + latch (`delivery.ts:270`'s shape); the test asserts **0 alarms across the next 5 ticks**, not merely that one fired |
| T-04-ABS-04 | Information disclosure | A push body carrying a question, a path or a command off the machine | mitigate | `sw.js:33-35`'s existing rule holds: the alarm body carries a duration, an agent name and a card title only — the card title is already exposed in the ask list, so no new leak |
| T-04-ABS-05 | Repudiation | An old installed service worker rendering `Floor is waiting on you` for a floor-stopped alarm | mitigate | Rule Q-4: the title must be self-sufficient (`The floor has stopped`, `Michael is gone`), asserted by a test, because an old SW ignores `body` |
| T-04-ABS-06 | Spoofing | The alarm colliding with or replacing a real ask notification | mitigate | A distinct stable tag `taskId: 'floor-quiet'`, which satisfies `PHONE_TASK_ID_RE` and can only replace its own previous notification |
| T-04-ABS-07 | Repudiation | An in-flight list read at notification-open time rather than at the transition, reporting an empty floor | mitigate | The snapshot is captured at the edge; the test mutates `doingCards()` in between and asserts the transition-time set |
| T-04-SC | Tampering | npm/pip/cargo installs | mitigate | No install step; `04-RESEARCH.md` § Package Legitimacy Audit records zero packages proposed. `push.ts` is dependency-free VAPID from `node:crypto` |
</threat_model>

<verification>
- `node --test test/absence-watchdog.test.cjs` — green, ≥ 6 cases including the 5-tick silence.
- `node --test test/boot-floor.test.cjs` — green, including the shutdown-clears-the-timer case.
- `npm test` — full suite, 0 failures, skipped count unchanged.
- `npm run typecheck` — 0 errors. `npm run lint` — exit 0.
- `git diff --stat src/main/index.ts src/main/config.ts resources/phone/sw.js` — all empty.
</verification>

<success_criteria>
- One alarm per quiet edge, zero on the following ticks, proven by an explicit 5-tick assertion.
- The god-death case fires, is addressed to the operator, and no channel carries a `'god'` recipient.
- The payload names the transition-time `doing` set.
- The timer is in `SHUTDOWN_STEPS` and `floor.shutdown()` clears it.
- `index.ts` is untouched — the Collision Map's dissolution was taken.
- The SUMMARY carries the threshold with its `[ASSUMED]` marking and the one-line reason `HEARTBEAT_MISSION` was not reused.
</success_criteria>

<output>
Create `.planning/phases/04-overnight-on-a-repo-that-matters/04-11-SUMMARY.md` when done.
</output>
