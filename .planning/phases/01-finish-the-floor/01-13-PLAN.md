---
phase: 01-finish-the-floor
plan: 13
type: execute
wave: 6
depends_on: ["01-09", "01-10"]
files_modified:
  - src/shared/providerAutomation.ts
  - src/main/index.ts
  - src/main/hooks.ts
  - src/preload/index.ts
  - src/renderer/src/hooks/usePtyParser.ts
  - src/renderer/src/hooks/useHive.ts
  - test/engine-parity.test.cjs
  - test/hooks-notify.test.cjs
  - README.md
autonomous: true
requirements: [FLOOR-18, FLOOR-14]

must_haves:
  truths:
    - "Codex-on-Windows' limitation is stated in source, docs and UI — never a bare unexplained return false"
    - "capabilityLine keeps its signature and its ADR-0002 cache-safe position; the platform decision is made in main"
    - "A blocked non-Claude agent produces an OS notification, and clicking it focuses that agent"
    - "A blocked Claude agent does not produce two toasts for one event"
    - "Every FLOOR-14 sentence in docs or UI carries a platform qualifier — macOS notification delivery under Electron 42+ requires code signing this project does not have"
  artifacts:
    - path: "src/shared/providerAutomation.ts"
      provides: "a `remote` bit on ProviderCapabilities and a REMOTE CONTROL clause in capabilityLine"
      contains: "remote"
    - path: "src/main/index.ts"
      provides: "the bare `return false` at :269 replaced by a comment naming the upstream issue"
      contains: "win32"
    - path: "test/hooks-notify.test.cjs"
      provides: "a DI notify fake proving the blocked transition reaches main"
  key_links:
    - from: "renderer block transition (usePtyParser / useHive approval IPC)"
      to: "hooks.ts notify()"
      via: "one IPC from the renderer's block transition to the existing notify, reusing its config gate and click-to-focus"
      pattern: "notify"
    - from: "providerCapabilities remote bit"
      to: "capabilityLine"
      via: "a new clause in the existing bits array, shouting the gap in uppercase"
      pattern: "REMOTE CONTROL"
---

<objective>
Declare the Codex-on-Windows limitation through the channel that already exists, and close FLOOR-14's
one remaining clause — a blocked non-Claude agent producing no OS toast at all.

Purpose: FLOOR-18 and FLOOR-14. `src/main/index.ts:269` is a bare
`if (process.platform === 'win32') return false;` (verified verbatim this session), and D-39 locks the
answer: "supported" is not buildable here — upstream records the Codex app-server daemon lifecycle as
Unix-only, and building it would mean owning a Rust daemon fork or a WSL/named-pipe shim forever on a
one-machine zero-recurring-cost project. D-41 is equally clear that simply deleting the `return false`
is not a fix: the existing best-effort ladder would then fail loudly with two subprocess timeouts per
Windows Codex spawn and still surface nothing in UI or docs.

FLOOR-14 is three-quarters shipped: click-to-focus (`hooks.ts:425`), long-task
(`hooks.ts:343`) and Claude-blocked (`hooks.ts:405-406`) all land. The residual is narrow and
specific — `status: 'blocked'` is a renderer determination that never reaches main's `notify()`.

Output: a `remote` bit, one capability clause, one honest comment, one IPC, one DI-faked test, and a
platform qualifier on every notification claim.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@docs/adr/0002-prompt-cache-invariant.md

<interfaces>
`providerCapabilities` / `capabilityLine` — `src/shared/providerAutomation.ts:258-289`. The
established per-engine gap channel, already carrying `'NO MAIL (bounces to you)'` and
`'spend UNTRACKED (invisible to every budget)'`. Copy contract: **missing capabilities SHOUT in
uppercase, present ones stay quiet**, one clause per capability. So the new clause is
`'REMOTE CONTROL unavailable on Windows'`, not `'remote control: n/a'`.

**ADR-0002 is the live constraint and it is sharp.** A platform-dependent capability line changes
roster-prompt text per OS, which changes the prompt prefix, which invalidates the prompt cache
differently on different machines. `providerAutomation.ts:278-279` carries the cache-safe
roster-position marker. **Take the `remote` bit, not a platform argument** (RESEARCH Open Question 4):
a new boolean field keeps `capabilityLine`'s signature stable and puts the platform decision at the
call site in main, which already knows the platform; a signature change ripples into
`test/engine-parity.test.cjs` and `test/provider-config.test.cjs` and every caller.

**Three exact-equality assertions will break and must be updated in lockstep** —
`test/engine-parity.test.cjs:134-155` asserts the FULL `capabilityLine` string with `assert.equal`.
That is expected. The total-coverage test at `:157-165` is the one to extend with the new field.

`src/main/index.ts:266-270` — `enableCodexRemoteForSpawn`, opening with the bare `return false` at
`:269`. The JSDoc above (`:262-265`) explains that failure is non-fatal in general but says nothing
about Windows. Upstream reference: `openai/codex#30372` (Unix-only daemon lifecycle: pidfile plus Unix
process/file-locking primitives). RESEARCH flags this issue number as not independently re-fetched
(assumption A6) — verify it resolves before citing it, and if it does not, cite what does.

FLOOR-14's residual: `status: 'blocked'` is set in the renderer at `usePtyParser.ts:175` and `:190`
(a `BLOCK_HINTS` regex over the last 400 chars of terminal text) and `useHive.ts:575` (an
approval-request IPC). None reaches main. `usePtyParser.ts:171` already early-returns when the agent
is already `blocked` — **that guard IS the de-dupe; do not add a second one.**
**Do not attempt main-side terminal parsing** — that is VIGIL-03 (Phase 4), and `usePtyParser`'s
"only the mounted terminal is parsed" limitation is out of scope here.

`notify()` — `src/main/hooks.ts:420-431`, `private`, gated on `getConfig().notifications` at `:422`,
already wires click-to-focus at `:425`. Constructor arg order at `:100-131`; the `focus` param is the
seventh.

FLOOR-14 copy contract (UI-SPEC, binding): title is the agent's bare name (matching `hooks.ts:406`
and `:343`); body is a lowercase verb phrase — `is waiting on you` for the god agent,
`is waiting on Michael` for a worker. No exclamation mark (`DESIGN.md:653` reserves those for
completions), no emoji.

**Platform honesty, required not optional:** Electron 42 migrated macOS notifications from
`NSUserNotification` to `UNNotification`, which requires code signing to display. This project's
macOS signing is best-effort (`build/notarize.cjs` no-ops without `APPLE_*`). Under the Electron 43
bump landing in this same phase, an unsigned local macOS build may show **no toasts at all**. Any
FLOOR-14 sentence without a platform qualifier is the defect.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-18 and FLOOR-14 clause against current source and paste the output</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/index.ts:255-285` — `enableCodexRemoteForSpawn`, its JSDoc, the bare `return false`, and the best-effort ladder below it.
    - `src/shared/providerAutomation.ts:250-295` — `ProviderCapabilities`, `providerCapabilities`, `capabilityLine` and the ADR-0002 marker at `:278-279`.
    - `src/main/hooks.ts:330-350`, `:395-435` — the long-task notify, the Claude blocked notify with its `#42` comment, and `notify()` itself.
    - `src/renderer/src/hooks/usePtyParser.ts:165-200` — the block detection, the early return at `:171`, and the summaries computed at `:180` / `:194`.
    - `src/renderer/src/hooks/useHive.ts:565-585` — the approval-request IPC.
    - `test/engine-parity.test.cjs:130-170` — the three exact-equality assertions and the total-coverage test.
    - `test/provider-config.test.cjs` — check whether it also asserts on this surface.
    - `docs/adr/0002-prompt-cache-invariant.md` in full — the constraint on roster-prompt text.
    - `01-UI-SPEC.md` § FLOOR-14 — the binding behaviour and copy contract.
  </read_first>
  <action>
    Quote issues #61 (FLOOR-18) and #42 (FLOOR-14) acceptance text verbatim and run one command per
    clause with ACTUAL output pasted:
    - `grep -n "process.platform === 'win32'" src/main/index.ts` — the bare return. Expect a hit at `:269` with no explanatory comment.
    - `grep -rn "REMOTE\|remote" src/shared/providerAutomation.ts` — expect no remote capability today.
    - `grep -n "notify(" src/main/hooks.ts` — the three shipped notify call sites. Evidence that three of FLOOR-14's four clauses ALREADY ship; do not rebuild them.
    - `grep -rn "'blocked'" src/renderer/src/hooks/usePtyParser.ts src/renderer/src/hooks/useHive.ts` — where blocked is determined.
    - `grep -rn "notify\|Notification" src/renderer/src/hooks/usePtyParser.ts` — expect EMPTY. That is the gap.
    - `grep -rn "capabilityLine" src/ test/` — every caller and every assertion that will move in lockstep.

    Then verify the upstream reference before citing it: confirm `openai/codex#30372` exists and
    records the daemon lifecycle as Unix-only (`gh issue view 30372 --repo openai/codex` or fetch the
    issue). RESEARCH marks this as assumption A6, not independently checked. If the number is wrong,
    find and cite the correct reference — a source comment citing a non-existent issue is exactly the
    class of false claim this phase removes.

    Record per clause OPEN or ALREADY-SATISFIED.

    **Record the four count baselines this plan's later criteria are deltas against.** Several
    tokens this phase adds already occur in these files for unrelated reasons, so a bare
    `at least N` criterion would be satisfied by doing nothing. Run and paste, verbatim:
    - `grep -c "remote" src/shared/providerAutomation.ts` (expect `3` — case-sensitive)
    - `grep -c "notify\|Notification" src/preload/index.ts` (expect `4`)
    - `grep -c "unavailable on Windows" README.md` (expect `0`)
    - `grep -c "unsigned macOS build" README.md` (expect `0`)
    - `grep -c "ipcMain.handle\|ipcMain.on" src/main/index.ts` — label the number you MEASURE HERE
      **B-ipcmain**. Task 3's criterion is "exactly one more", which is unfalsifiable unless the
      "before" number is written down at the moment this plan actually runs.
      **Measure it; do not copy a literal.** At plan time (pre-wave-4) this returned `157`, but two
      intervening waves move it before wave 6: plan 08 (wave 4) adds one `ipcMain.handle` for the
      enqueue channel → `158`, and plan 10 (wave 5) requires `src/main/index.ts` to contain neither
      `hive:memoryWakeUp` nor `memory:reflectNow`, both of which are live `ipcMain.handle(`
      registrations today (verified this session at `index.ts:4041` and `:4046`) → `156`.
      **The expected wave-6 value is therefore `156`, and task 3's target is `157`, not `158`.**
      If the measured number is not `156`, do not adjust the code to reach a written literal — record
      the discrepancy, check whether plan 08's handler or plan 10's two deletions actually landed
      (`grep -c "hive:memoryWakeUp\|memory:reflectNow" src/main/index.ts` must be `0` after plan 10),
      and report it. Task 3 asserts against the number recorded here, whatever it is.
    Label these B-remote, B-preload, B-readme-win, B-readme-mac, B-ipcmain in the task notes. Task 2 and
    task 3 assert **strictly greater than** these recorded numbers, not against a hardcoded threshold.
  </action>
  <acceptance_criteria>
    - **B-sha recorded before any edit**: paste `git rev-parse HEAD`. Every containment criterion in this plan is range-bound against it, so an unrecorded B-sha makes those criteria unrunnable — record it first, in the task notes, verbatim.
    - Task notes contain pasted output for all six greps, with the `usePtyParser` notify grep empty.
    - Task notes list the three ALREADY-SATISFIED FLOOR-14 clauses with their `hooks.ts` line numbers.
    - Task notes contain the verified upstream reference for the Codex Windows limitation, with the command used to confirm it.
    - Task notes list every `capabilityLine` caller and assertion that will change in lockstep.
    - Task notes record the six labelled baselines B-remote, B-preload, B-readme-win, B-readme-mac, B-ipcmain and B-blockguard with pasted command output for each.
    - **B-blockguard**, the de-dupe guard count task 3 asserts preservation against: `grep -c "status === 'blocked') return" src/renderer/src/hooks/usePtyParser.ts` — measured `1` this session. `usePtyParser.ts` is also declared by plans 05 (wave 2) and 07 (wave 3), so re-measure rather than trusting the literal; if it is not `1`, establish why before task 3 asserts equality against it.
    - **The engine-parity NUMERIC baseline, recorded before this plan touches the file.** This plan
      rewrites `test/engine-parity.test.cjs` and nothing downstream re-runs it, so an exit-code-only
      baseline would let this plan delete the breaker and budget tests plan 09 added in wave 4, add two
      of its own, and score every criterion green. Run and paste:
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Label the `# pass` number **B-parity**. Measured at plan time, BEFORE plan 09 ran: `EXIT=0`,
      `# tests 14`, `# pass 14`, `# fail 0`, `# skipped 0`, `# todo 0`. Plan 09 (wave 4) adds at least
      five and normally six tests to this same file, so the number recorded HERE — in wave 6, after
      plan 09 has landed — must be at least `19`. If it is lower, plan 09's coverage has already been
      lost and that is a STOP, not a baseline. Also record
      `grep -c "'constrained'" test/engine-parity.test.cjs` (`0` at plan time, at least `2` after plan
      09) — task 2 re-asserts it as the content check that plan 09's breaker tests are still there.
      Write the TAP through `mktemp`, never into the repo — `*.tap` is not in `.gitignore`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>Every clause is classified with pasted evidence, the upstream citation is verified rather than assumed, and the lockstep assertion set is enumerated before anything changes.</done>
</task>

<task type="auto">
  <name>Task 2: Declare the Codex-on-Windows limitation in source, capability line and docs</name>
  <files>src/shared/providerAutomation.ts, src/main/index.ts, test/engine-parity.test.cjs, README.md</files>
  <read_first>
    - `src/shared/providerAutomation.ts:250-295` — the file and functions being modified, including the `bits` array's copy convention and the ADR-0002 marker.
    - `src/main/index.ts:255-285` — the function being commented.
    - `test/engine-parity.test.cjs:130-170` — the three exact-string assertions and the coverage test.
    - `01-PATTERNS.md` § "FLOOR-18 / FLOOR-09's capability line" `:919-993` — the verbatim channel and the assertions that break.
    - `README.md`'s engine table.
  </read_first>
  <action>
    Add a `remote` boolean to `ProviderCapabilities` and populate it in `providerCapabilities`.
    **Take the bit, not a platform argument** — `capabilityLine`'s signature stays stable, and the
    platform decision is made at the call site in main, which already knows the platform. Threading a
    platform argument through a module both processes import is how ADR-0002's cache-safe position gets
    accidentally violated.

    Add one clause to `capabilityLine`'s `bits` array following the uppercase-shouts-for-gaps
    convention: `'REMOTE CONTROL unavailable on Windows'` when the gap applies, quiet when it does not.
    Keep it to one clause — this string is injected once per roster entry and a paragraph per agent
    would crowd out the prompt it decorates.

    **Watch ADR-0002.** Read it before choosing where the value comes from. The capability line lands
    in a cache-safe roster position (`providerAutomation.ts:278-279`); changing roster-prompt text per
    OS is precisely the prompt-cache hazard the ADR governs. State in the code comment how the chosen
    shape stays inside the ADR's rules, and if it cannot, STOP and report rather than shipping a
    per-OS prompt prefix silently.

    In `src/main/index.ts`, replace the bare `if (process.platform === 'win32') return false;` at
    `:269` with the same early return **plus a comment** naming the verified upstream reference and
    what specifically is Unix-only (pidfile plus Unix process/file-locking primitives), so the line
    stops looking arbitrary. Extend the JSDoc at `:262-265` to say the Windows case is a declared
    limitation, not an oversight. **Do not delete the `return false`** — D-41: letting the best-effort
    ladder fail loudly trades a silent no-op for guaranteed noise plus two subprocess timeouts on every
    Windows Codex spawn, and still surfaces nothing.

    This is a **one-line-plus-comment edit to `index.ts`**. Do not touch anything else in that file
    beyond task 3's IPC handler.

    Update the three exact-equality assertions at `test/engine-parity.test.cjs:134-155` in lockstep —
    they assert the full string with `assert.equal`, so any new clause breaks all three. Extend the
    total-coverage test at `:157-165` to assert `typeof c.remote === 'boolean'` for every engine.
    Add a new test asserting the Windows-gap clause appears for `codex` when the remote bit is false
    and does not appear when it is true. If `test/provider-config.test.cjs` also asserts on this
    surface (task 1 established whether it does), update it in the same commit.

    Update `README.md`'s engine table with the Codex-on-Windows limitation, phrased as a limitation
    rather than a bug, and matching the capability line's wording so source, UI and docs agree.
  </action>
  <acceptance_criteria>
    - `grep -c "remote" src/shared/providerAutomation.ts` returns **strictly greater than B-remote** (the task-1 baseline, `3` today) — the interface field, the population and the capability clause are all additions on top of the three occurrences that already exist. Paste the baseline and the new count side by side.
    - `grep -c "REMOTE CONTROL" src/shared/providerAutomation.ts` returns at least `1`.
    - `grep -A2 -n "win32" src/main/index.ts` output shows the return preceded or followed by a comment naming the upstream reference — paste it.
    - `grep -c "process.platform === 'win32') return false" src/main/index.ts` returns **`1`** — measured `1` this session. The early return survives verbatim; D-41 forbids deleting it. This replaces "the `return false` count at the `enableCodexRemoteForSpawn` region is unchanged", which named no runnable command and no number: bare `grep -c "return false" src/main/index.ts` returns `17` file-wide and says nothing about this function.
    - `grep -c "unavailable on Windows" README.md` returns at least `1` (B-readme-win is `0` today — a bare case-insensitive `windows` grep already returns `3` and would pass untouched). The hit must be in the engine table region and must use the same wording as the capability line clause — paste the sentence.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least B-parity + 2**, **and at least `21` in absolute terms**, **`# fail 0`**, **`# skipped 0`**, `# todo 0`, with all three exact-string assertions updated. Paste B-parity from task 1 next to the new number so the delta is visible.
      The floor is stated relative to B-parity, not as a bare "2 new tests", because this task REWRITES a file plan 09 wrote into and nothing downstream re-runs it: without a floor, dropping plan 09's five-or-six breaker and budget tests and adding two of this plan's own scores green on every other criterion in this plan. The absolute `21` is `14` (plan-time) + `5` (plan 09's minimum) + `2` (this task's minimum).
    - `grep -c "'constrained'" test/engine-parity.test.cjs` returns at least `2` — plan 09's breaker tests are still in the file by CONTENT, not just by count. It was `0` at plan time and plan 09 is what makes it non-zero; a rewrite that drops them fails here even if the count is padded from elsewhere.
      Exit code and a count alone would not close this either: `node --test` counts skipped tests in `# tests` and still exits `0` — verified this session, two `{skip: '...'}` tests one holding a live `assert.fail()` report `# tests 2 / # pass 0 / # fail 0 / # skipped 2` at `EXIT=0`. `{skip}`-ing plan 09's tests instead of deleting them would hold the count up and still lose the coverage; `# skipped 0` is what forbids that. Name the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches.
    - Containment, **per-commit and range-bound** (never a bare whole-tree `git diff`/`git status`): `BASE=<B-sha from task 1>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD -- src/shared/providerAutomation.ts src/main/index.ts test/engine-parity.test.cjs README.md) && test -n "$SHAS" || { echo "UNBOUND BASE OR NO COMMITS — CRITERION FAILS"; false; }; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/shared/providerAutomation\.ts|src/main/index\.ts|test/engine-parity\.test\.cjs|README\.md)$"` must print **nothing**. Bind `$BASE` inside this criterion — a criterion is its own shell invocation, and an unbound variable makes `git log` fatal with empty stdout, which "must print nothing" reads as PASS. The range and the per-commit walk are both required: a bare `git diff --name-only -- <paths>` has no revision range, so once this plan has committed it prints nothing and the check is vacuous; and `use_worktrees: false` means wave-mates are committing to this same tree, so only this plan's own SHAs may be walked.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>The limitation is stated in all three places the criterion names, through the channel that already exists, with the prompt-cache invariant respected and the three lockstep assertions updated.</done>
</task>

<task type="auto">
  <name>Task 3: Route the non-Claude blocked transition to main's existing notify, and qualify every notification claim by platform</name>
  <files>src/main/index.ts, src/main/hooks.ts, src/preload/index.ts, src/renderer/src/hooks/usePtyParser.ts, src/renderer/src/hooks/useHive.ts, test/hooks-notify.test.cjs, README.md</files>
  <read_first>
    - `src/main/hooks.ts:395-435` — `notify()` and the Claude blocked path with its `#42` rationale; the constructor at `:100-131`.
    - `src/renderer/src/hooks/usePtyParser.ts:165-200` — the block transition, the early return at `:171` that IS the de-dupe, and the summaries at `:180` / `:194`.
    - `src/renderer/src/hooks/useHive.ts:565-585` — the approval-request IPC path.
    - `src/main/index.ts:3888-3894` — the `ipcMain.handle('domain:action', …)` convention; and `:465-470` where `focus` is wired into `HookServer`.
    - `src/preload/index.ts:776-781` — the one-line export convention.
    - `01-PATTERNS.md` § "`test/hooks-notify.test.cjs`" `:326-435` — `delivery-main`'s `harness()` idiom, `net-binding`'s real `HookServer` at `:107-136`, the `{notifications: false}` gate that must become `true`, and the recording-`Notification` `require.cache` injection.
    - `01-UI-SPEC.md` § FLOOR-14 — the binding behaviour, copy and platform-honesty contract.
  </read_first>
  <action>
    Add one IPC from the renderer's block **transition** to a main-side call of the existing
    `hooks.ts` `notify(agentId, name, reason)`. Concretely: one `ipcMain.handle` (or `ipcMain.on` if a
    fire-and-forget shape fits better — justify the choice) in `src/main/index.ts` following the
    colon-namespaced convention, one one-line preload export, and a call at the renderer's transition
    site.

    Fire **once per transition**, not per repaint. `usePtyParser.ts:171` already early-returns when the
    agent is already `blocked` — **that guard is the de-dupe; do not add a second one.**

    Do **not** fire for providers whose blocked state already arrives via a hook `Notification` stream
    (Claude). Two toasts for one event is the failure mode. Gate on the provider, and say so in the
    comment.

    Reuse `notify()` as it stands: it already gates on `getConfig().notifications` (`hooks.ts:422`) and
    already wires click-to-focus (`:425`). **Add no new setting and no new click handler.** If `notify`
    must become reachable from `index.ts`, expose it through the narrowest possible surface (a method
    or a dep), not by widening the class's public API broadly.

    Copy, locked by UI-SPEC: title is the agent's bare name; body is `is waiting on you` for the god
    agent and `is waiting on Michael` for a worker. Reuse the summaries the renderer already computes
    at `usePtyParser.ts:180` / `:194`. No exclamation mark, no emoji.

    **Do not build main-side terminal parsing.** That is VIGIL-03, Phase 4.

    Create `test/hooks-notify.test.cjs`. Stand up a **real** `HookServer` following
    `test/net-binding.test.cjs:107-136`'s `hookFloor(t)` shape, but pass
    `() => ({ notifications: true })` as the third constructor argument — `hookFloor` passes `false`,
    which is exactly the gate `notify()` returns on. Inject a **recording** `Notification` class through
    `require.cache['electron']` (the `net-binding.test.cjs:33-41` pattern), widened to capture
    `{title, body}` and to return `true` from `isSupported()`. That makes the whole assertion available
    without touching `notify()`'s `private` modifier. Assert: a blocked non-Claude agent produces one
    notification with the locked title and body; a blocked Claude agent produces none from this path;
    `notifications: false` produces none; and the click handler is registered with the right agent id.

    Finally, the platform honesty pass. Every FLOOR-14 sentence in `README.md` — and any UI copy that
    claims notifications work — must carry a platform qualifier: under Electron 42+ macOS notification
    delivery requires code signing, this project's macOS signing is best-effort, so an unsigned local
    macOS build may show no toasts at all. Any FLOOR-14 sentence without a qualifier is the defect.
    Use the same channel discipline as FLOOR-18: state the limitation rather than claim the capability.
  </action>
  <acceptance_criteria>
    - `grep -c "notify" src/renderer/src/hooks/usePtyParser.ts` returns at least `1` — the clause that was `0` at task 1.
    - `grep -c "ipcMain.handle\|ipcMain.on" src/main/index.ts` returns **exactly `B-ipcmain + 1`, where `B-ipcmain` is the number task 1 MEASURED at the start of this plan** — not a literal copied from this document. Paste task 1's recorded `B-ipcmain` and the post-edit count side by side; the difference must be exactly `1`.
      **Expected values in wave 6: `B-ipcmain = 156`, so this criterion is `157`.** An earlier revision froze `158` off a plan-time measurement of `157`, which was arithmetically wrong by the time this plan runs: plan 08 (wave 4) adds one handler (`157 → 158`) and plan 10 (wave 5) deletes the two live `ipcMain.handle` registrations for `hive:memoryWakeUp` and `memory:reflectNow` (`158 → 156`, verified at `index.ts:4041` and `:4046` this session). Trusting `158` would have failed correct work — or pushed the agent to add a spurious second handler to reach it. **If the measured post-edit number does not equal `B-ipcmain + 1`, fix the code, never the literal; if `B-ipcmain` itself is not `156`, report the discrepancy per task 1 before proceeding.**
    - `grep -c "notify\|Notification" src/preload/index.ts` returns **exactly B-preload + 1** (the task-1 baseline is `4` today). Paste the baseline, the new count and the one added export line.
    - `test/hooks-notify.test.cjs` exists, and:
      `TAP=$(mktemp); node --test --test-reporter=tap test/hooks-notify.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least `4`** — one per asserted behaviour: the blocked non-Claude toast with the locked title and body, the blocked Claude agent producing none from this path, `notifications: false` producing none, and the click handler registered against the right agent id — **`# fail 0`**, **`# skipped 0`**, `# todo 0`.
      A brand-new file cannot be graded on exit code: `node --test` counts skipped tests in `# tests` and still exits `0` (verified this session — two `{skip: '...'}` tests, one holding a live `assert.fail()`, report `# tests 2 / # pass 0 / # fail 0 / # skipped 2` at `EXIT=0`), so a file of four skipped stubs passes an exit-code criterion exactly. `# pass 4` with `# skipped 0` is the gate. This matters more here than anywhere else in the plan: the whole FLOOR-14 residual is *"a blocked non-Claude agent produces no toast at all"*, and a skipped test asserts precisely nothing about it. Name the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches. Write the TAP through `mktemp`; `*.tap` is not in `.gitignore`.
    - `grep -c "notifications: true" test/hooks-notify.test.cjs` returns at least `1` — the gate that `hookFloor` sets to `false`.
    - `grep -c "isSupported" test/hooks-notify.test.cjs` returns at least `1`.
    - `grep -c "unsigned macOS build" README.md` returns at least `1` (B-readme-mac is `0` today — a bare case-insensitive `macos` grep already returns `2` and would pass untouched). Paste the sentence: it must carry the code-signing qualifier, not merely name the platform.
    - `grep -c "status === 'blocked') return" src/renderer/src/hooks/usePtyParser.ts` returns **exactly `B-blockguard`, the value task 1 recorded** (measured `1` this session; plans 05 and 07 also declare this file in earlier waves, so the recorded value governs, not this literal). Paste `B-blockguard` and the post-edit count. Exactly one early return, not two: `usePtyParser.ts:171`'s existing guard IS the de-dupe and no second one was added. This replaces "`git diff` shows no second de-dupe guard", which asked a reader to eyeball a diff instead of running a check with a pass/fail answer. `git diff --name-only -- src/renderer/src/hooks/usePtyParser.ts` may be used alongside it, but only path-scoped: a bare whole-tree diff shows every wave-6 plan's work.
    - Containment, **per-commit and range-bound**: `BASE=<B-sha from task 1>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD -- src/main/index.ts src/main/hooks.ts src/preload/index.ts src/renderer/src/hooks/usePtyParser.ts src/renderer/src/hooks/useHive.ts test/hooks-notify.test.cjs README.md) && test -n "$SHAS" || { echo "UNBOUND BASE OR NO COMMITS — CRITERION FAILS"; false; }; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/main/index\.ts|src/main/hooks\.ts|src/preload/index\.ts|src/renderer/src/hooks/usePtyParser\.ts|src/renderer/src/hooks/useHive\.ts|test/hooks-notify\.test\.cjs|README\.md)$"` must print **nothing**. Bind `$BASE` inside this criterion — a criterion is its own shell invocation, and an unbound variable makes `git log` fatal with empty stdout, which "must print nothing" reads as PASS. The range and the per-commit walk are both required: a bare `git diff --name-only -- <paths>` has no revision range, so once this plan has committed it prints nothing and the check is vacuous; and `use_worktrees: false` means wave-mates are committing to this same tree, so only this plan's own SHAs may be walked.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hooks-notify.test.cjs</automated>
  </verify>
  <done>A blocked non-Claude agent produces exactly one OS toast that focuses the agent on click, no Claude agent gets two, and no notification claim in the repo lacks a platform qualifier.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer block determination → main OS notification | A new IPC that causes an OS-level notification. Unvalidated, it is a notification-spam vector from a compromised renderer state. |
| capability line → the god's roster prompt | Text injected into a model's prompt in a cache-safe position; ADR-0002 governs it. |
| declared limitation → operator's mental model | An overclaimed capability is a trust failure, which is the whole subject of this phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P13-01 | Repudiation | a bare `return false` that looks arbitrary and hides a real limitation | mitigate | Keep the early return, add the comment naming the verified upstream reference; declare it in `capabilityLine` and in the README engine table (D-40, three places). |
| T-P13-02 | Denial of service | notification spam from a repaint loop | mitigate | Fire once per transition; `usePtyParser.ts:171`'s existing early return is the de-dupe and no second guard is added. The new IPC validates the agent id against the live roster in main. |
| T-P13-03 | Denial of service | two toasts for one blocked Claude agent | mitigate | Gate the new path on provider — Claude's blocked state already arrives via its own hook `Notification` stream at `hooks.ts:405-406`. Asserted by a named test. |
| T-P13-04 | Tampering | prompt-cache invalidation differing per OS | mitigate | Take the `remote` bit rather than a platform argument, keeping `capabilityLine`'s signature stable and the platform decision in main; ADR-0002's cache-safe roster position is preserved and the code comment says how. |
| T-P13-05 | Repudiation | claiming notifications work on macOS when Electron 42+ requires code signing to display them | mitigate | Every FLOOR-14 sentence carries a platform qualifier; the project's macOS signing is best-effort and the docs say so. |
| T-P13-06 | Spoofing | a renderer-supplied agent id causing a toast attributed to another agent | mitigate | Main resolves the agent against the live roster before notifying; the click-to-focus target is the resolved id, not the supplied one. |
</threat_model>

<verification>
- `npm test` green LOCALLY, plus the TAP counters from tasks 2 and 3 pasted with `# skipped 0`.
- **Cross-platform CI is read off the draft PR, never off a phase-branch push.** `ci.yml` and `e2e.yml`
  are both `branches: [main]` only, so pushing this phase branch triggers **no run at all** and "green
  on three platforms" would be an unrunnable claim. Plan 01 task 3 (wave 1) opens the phase's draft PR
  to `main`; resolve against that PR's checks. Run `gh pr checks` and paste the rows for the real
  check-run names — `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`,
  `Build` and `Electron smoke (ubuntu-latest)`. There is **no check run named `E2E`** — `E2E` is the
  workflow name; the job is `Electron smoke (ubuntu-latest)`. An empty table means nothing was
  triggered: report that, do not substitute a local run.
- `npm run typecheck` green.
- Manual on Windows: spawn a Codex agent and confirm the roster prompt / capability surface shows the
  `REMOTE CONTROL unavailable on Windows` clause, and that no subprocess timeout noise appears at spawn.
- Manual: block a non-Claude agent (an approval prompt) and confirm exactly one OS toast fires and
  clicking it focuses that agent. Per `01-VALIDATION.md`, macOS delivery is unverifiable on this
  project — record that as a stated limitation rather than as a passed check.
</verification>

<success_criteria>
- ROADMAP criterion 5's FLOOR-18 clause is TRUE: Codex-on-Windows' limitation is stated in source,
  docs and UI — never a bare `return false`.
- ROADMAP criterion 4's FLOOR-14 clause is TRUE: a notification fires when an agent is blocked or
  finishes a long task and clicking it focuses that agent — on every engine, with the macOS platform
  limitation stated rather than papered over.
- Issues #61 and #42 close in their PRs per D-43, with the empty `usePtyParser` notify grep and the
  three shipped `hooks.ts` notify sites both pasted, so neither an open clause nor a satisfied one is
  misread.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-13-SUMMARY.md` when done. It MUST record the verified
upstream reference (and the command that verified it), how the `remote` bit stays inside ADR-0002's
rules, the three lockstep assertions updated, and the explicit statement that macOS notification
delivery is unverifiable on this project.
</output>
