---
phase: 01-finish-the-floor
plan: 16
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/AddAgentModal.tsx
  - src/renderer/src/components/OnboardingWizard.tsx
  - src/renderer/src/components/SetupPanel.tsx
  - src/renderer/src/components/HivePicker.tsx
  - src/renderer/src/components/OfficeThemePicker.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the onboarding and picker cluster sits below the 14px floor except allowlisted decorative glyphs"
    - "Every icon-only button in these five files has an accessible name; no button with visible text gained an aria-label"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
    - "The M1d and M1x blind-spot scans over this group are clean, so no decimal, quoted, ternary or computed sub-14px size ships unexamined"
  artifacts:
    - path: "src/renderer/src/components/OnboardingWizard.tsx"
      provides: "the first-run surface converted to tokens"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the onboarding and picker cluster to the corrected type scale and give every icon-only button an
accessible name.

Purpose: FLOOR-12, sweep sub-group 2 of 6. Measured at plan time: **100 sub-14px `fontSize`
occurrences** — `AddAgentModal.tsx` 35, `OnboardingWizard.tsx` 32, `SetupPanel.tsx` 13,
`HivePicker.tsx` 10, `OfficeThemePicker.tsx` 10. **Occurrences, not lines** — wave 9 counts occurrences
(`grep -o`), so this plan does too; for these five files the two units are equal today, and the unit is
stated beside every pinned number so the two waves reconcile. This is the first surface a new operator ever sees, which is a poor place
for 8px text.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract; its rules are mechanical, checked in order,
with no judgement at the site. Do not re-derive them.

Output: five files clearing the floor, an accessible-name pass, and this group's frozen Rule 0
allowlist handed to plan 23.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, and `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` for the final values and the M1
baseline. A surviving reference to `--cth-text-display-sm` is a dangling `var()` — report it, do not
patch around it.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — the style object sets `fontFamily: 'var(--cth-font-display)'` AND a `fontSize` of 7-13:
  `fontSize` → `var(--cth-text-display-md)`, `lineHeight` → `var(--cth-lh-display-md)`.
  **`fontFamily` is UNCHANGED — Press Start 2P stays.** `DESIGN.md:706` is a size rule; converting the
  display face would retire a typeface, which this contract's second paragraph forbids.
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display` in the same object: → `var(--cth-text-body-md)`,
  or `var(--cth-text-mono-md)` where the object sets `fontFamily: 'var(--cth-font-mono)'`, with the
  matching `--cth-lh-*` token. The mono line-height token is the single `--cth-lh-mono`.
- **Rule 1b and Rule 3 do not apply to this group.**

**Pinned measurement command (M1). Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` to see the sites, and
`grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` to count them.

**Count occurrences (`-o`), never lines (`-c`) — the unit matters.** Wave 9 (plan 23) pins the
completeness bar with `grep -rhoE … | wc -l`, i.e. how many times the trimmed text appears. Measured
today the whole tree is **604 occurrences vs 603 lines**; the single divergence is
`IntegrationsRegistry.tsx:388` (plan 17's file), which carries `fontSize: 12` and `fontSize: 10` on one
physical line. Every per-file number in this plan is therefore an occurrence count, so wave 7 and wave 9
reconcile instead of disagreeing by one. For this plan's five files the two units are equal today (35 / 32 / 13 / 10 / 10 — 100 either way), so
the unit change costs nothing here; it still has to be stated, or wave 9 is comparing occurrences
against lines.

**M1's blind spots — the sub-14px sites the regex structurally cannot see.** M1 needs a digit
immediately after `fontSize:`, and its trailing `[^0-9.]` guard drops decimals. So a quoted (`'13px'`),
decimal (`12.5`), ternary (`lg ? 13 : 11`) or computed (`Math.max(8, …)`) size is invisible to M1 — and
therefore to plan 23's repo-wide bar. Two extra scans close that hole, run over **this plan's files
only**:
- **M1d — decimal and quoted sub-14 literals:**
  `grep -rnoE "fontSize *[:=] *\{?['\"]?(1[0-3]|[1-9])\.[0-9]+|fontSize *[:=] *\{?['\"](1[0-3]|[1-9])(\.[0-9]+)?px['\"]" <this plan's paths>`
- **M1x — expression-valued sizes:**
  `grep -rnE "fontSize *[:=] *\{?[A-Za-z_\$]" <this plan's paths>`

An M1d hit is an ordinary sub-14px literal and takes the same rule a visible one would. An M1x hit must
be recorded with its **evaluated minimum** — the smallest value the expression can return over its
inputs — and a minimum below 14 is a site this plan fixes. **Verified in this plan's files today: zero M1d hits and zero M1x hits — this is the one sub-group with
no blind-spot site.** That is a measurement, not an assumption: run both scans in task 1 and again after
the sweep. A hit either scan finds is in scope and takes the same rule a visible site would.

**Rule 0 candidates in this group: none** per UI-SPEC's 30-candidate table. Verify in task 1 rather
than assuming; if the re-run finds one, apply Rule 0 and add it to this group's allowlist.

**Hoisted shared style objects are NOT exempt.** Sweep the shared declaration normally and give any
glyph-only consumer a **local `fontSize` override**; that override's line is the allowlist entry.

**Accessible names — the rule, not the ratio.** Visible text content → **already named, do NOT add
`aria-label`**. Icon/glyph/symbol only → `aria-label` required, phrased verb + object
(`FullscreenTerminal.tsx:665` is the house pattern). `PixelButton` renders real button text, so a
labelled `PixelButton` needs nothing.

**Containment — UI-SPEC's three steps, in order:** (1) do nothing where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container's integer by the measured delta and
change nothing else; (3) **stop and report** with the file:line and the measured overflow.
**Explicitly forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding any
site below 14px to make room.

**Note on `OnboardingWizard.tsx`:** it carries one of the nine `react-hooks/exhaustive-deps` disable
comments that plan 21 (FLOOR-16) turns into a live suppression. **Do not delete or move that comment.**
A size sweep has no reason to touch it, and moving it off its target line would break the suppression.
The same caution applies to any `eslint-disable-next-line` comment in this group's files.

**Out of the sweep entirely:** `components/terminalFontSize.ts` and anything xterm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site in the five files by rule, before editing anything</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — the rules, the 30-candidate glyph table, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — the corrected token values and the M1 baseline.
    - `src/renderer/src/components/AddAgentModal.tsx`, `OnboardingWizard.tsx`, `SetupPanel.tsx`, `HivePicker.tsx`, `OfficeThemePicker.tsx` in full — every hoisted style object and every `eslint-disable-next-line` comment.
  </read_first>
  <action>
    Run the pinned M1 command per file and paste the output. Baseline at plan time: AddAgentModal 35,
    OnboardingWizard 32, SetupPanel 13, HivePicker 10, OfficeThemePicker 10 — 100 total, **as
    occurrences**. Record any delta against current source. Count with `grep -hoE … | wc -l`, not
    `grep -cE`: wave 9 asserts occurrences and the two units diverge wherever one line carries two
    matches.

    Record `git rev-parse HEAD` as this plan's **base sha** — the path-scoped containment checks in
    task 3 are read against it, and five wave-mates are committing to the same tree.

    Then run the two **blind-spot scans** from `<interfaces>` (M1d and M1x) over this plan's paths and
    paste both outputs. M1 cannot see a quoted, decimal, ternary or computed size, so neither can plan
    23's wave-9 bar; these two scans are what stops sub-14px user-facing text shipping unexamined.
    Expected today: zero hits from each. A hit either scan finds is in scope and takes the same rule
    a visible site would — record it and sweep it, do not carve it out.
    Record every M1x hit's **evaluated minimum**, not just its expression text.

    Run the M3 glyph predicate scoped to these five files and paste the result.

    Produce a per-file classification table: every `fontSize` declaration line, its value, whether the
    enclosing style object sets `--cth-font-display` or `--cth-font-mono`, inline or hoisted, and which
    rule applies. For each hoisted object, enumerate its consumers.

    Inventory every `<button>` and `<PixelButton>` and mark each NAMED or ICON-ONLY.

    Record every `eslint-disable-next-line` comment's line number in these files, so task 2 and task 3
    can confirm none moved. Also record any fixed-height container integer.

    `OfficeThemePicker.tsx:73` is one of the five `disposeTerminal` call sites plan 05 audited — note it
    so a size sweep does not disturb the dispose path.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all five files with a total.
    - Task notes contain the pasted M3 output scoped to these five files.
    - Task notes contain the per-file rule classification table with every `fontSize` line classified and hoisted objects' consumers enumerated.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY.
    - Task notes list every `eslint-disable-next-line` line number in the group.
    - Task notes contain the pasted M1d and M1x output scoped to this plan's paths, with an **evaluated
      minimum** written beside every M1x hit. Expected today: zero M1d hits and zero M1x hits across all five files.
    - Task notes record this plan's **base sha** (`git rev-parse HEAD`).
    - `npm test` exit code is `0`, **and the suite's TAP counters are recorded as this plan's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured today: `EXIT=0`, `# tests 426`, `# pass 422`, `# fail 0`, `# skipped 4`, `# todo 0`. Name
      the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 4`, so a criterion
      grepping `^# skipped` against it never matches and silently passes as "no output, no problem".
      **Write the TAP through `mktemp`, never to a fixed repo-root name.** Six plans (15-20) run
      concurrently in wave 7 against ONE working tree (`use_worktrees: false`), so a shared `sweep.tap`
      is truncated by each wave-mate while the others are still grepping it — this plan could grade a
      wave-mate's half-written counter block against its own `# pass` / `# skipped` gate and score a run
      it never made. `.gitignore` has no `*.tap` entry either, so a stray file would also show up in
      this plan's containment check, in every wave-mate's, and in plan 23's wave-9 sweep.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site and every button in the group is pre-classified and every lint-suppression anchor is recorded before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep AddAgentModal.tsx and OnboardingWizard.tsx</name>
  <files>src/renderer/src/components/AddAgentModal.tsx, src/renderer/src/components/OnboardingWizard.tsx</files>
  <read_first>
    - Both files in full — the files being modified.
    - Task 1's classification table, button inventory, and lint-comment line list for these two files.
    - `01-UI-SPEC.md` § FLOOR-12's containment rule.
  </read_first>
  <action>
    Execute task 1's classification for these two files: Rule 1 → `var(--cth-text-display-md)` +
    `var(--cth-lh-display-md)`, family unchanged; Rule 2 → `var(--cth-text-body-md)` (or
    `var(--cth-text-mono-md)` for mono objects) with the matching `--cth-lh-*`. Convert hoisted objects
    at their declaration, with local overrides for any glyph consumer.

    Add `aria-label` to every ICON-ONLY entry, phrased verb + object. Add none to NAMED entries.

    Preserve every `eslint-disable-next-line` comment on its exact target line. If a sweep edit would
    move one, adjust the edit — the comment must still sit directly above the line it suppresses when
    plan 21 turns it into a live rule.

    Apply containment in UI-SPEC's order; record every container integer changed with its measured
    delta; stop and report rather than reflowing.

    `OnboardingWizard.tsx` also references `autoMode` (`:100-627`). Do not change that logic — plan 12
    owns the auto-mode derivation and this is a size sweep.

    Change no copy, no layout, no colour, no spacing token.
  </action>
  <acceptance_criteria>
    - `grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` returns exactly the number of allowlisted sites for each of the two files (expected `0`). Paste both beside their task-1 baselines (AddAgentModal 35, OnboardingWizard 32). **Occurrences (`-o`), not lines (`-c`)** — the same unit and regex plan 23 uses in wave 9.
    - M1d and M1x over both files return `0` hits each (both `0` today) — paste both. A decimal, quoted or ternary size added here would be invisible to M1 and to plan 23's bar alike.
    - `grep -c "cth-font-display" <file>` returns the same value as before this task for both files.
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/OnboardingWizard.tsx` returns the same value as task 1 recorded, and each comment is still immediately above its target line — paste each with the line following it.
    - Every `aria-label` added maps to an ICON-ONLY entry; paste each beside its button body.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today, of `426` total). Exit code alone
      is not a gate: `node --test` counts skipped tests in its total and exits `0` on an all-skipped file
      (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from a green one by
      exit code. This plan adds no tests, so any rise in `# skipped` is a red test being hidden rather
      than fixed.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>Both onboarding surfaces clear the floor with no family conversion, every icon-only control named, and every lint suppression still anchored.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep SetupPanel, HivePicker and OfficeThemePicker, and freeze this group's allowlist</name>
  <files>src/renderer/src/components/SetupPanel.tsx, src/renderer/src/components/HivePicker.tsx, src/renderer/src/components/OfficeThemePicker.tsx</files>
  <read_first>
    - The three files in full — the files being modified.
    - Task 1's classification table, button inventory and lint-comment list for these three files.
    - `src/renderer/src/components/OfficeThemePicker.tsx:65-80` — the `disposeTerminal` call site, so the sweep leaves the dispose path untouched.
  </read_first>
  <action>
    Execute task 1's classification for the three remaining files under the same rules as task 2.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    Do not touch `OfficeThemePicker.tsx`'s `disposeTerminal` call or the surrounding lifecycle — plan 05
    audited that drop path and a size sweep has no business near it.

    Apply containment in order; record any container integer changed with its measured delta.

    Freeze this group's Rule 0 allowlist as a literal `file:line` array covering all five group files
    (expected empty, or whatever task 1's re-run found) and write it into the SUMMARY. Plan 23 assembles
    all seven sweep plans' arrays (01-14 through 01-20) into `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    Re-run M1 across all five group files and paste the final counts for plan 23.
  </action>
  <acceptance_criteria>
    - For each of the three files, `grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` returns exactly the number of allowlisted sites for that file. Paste all three beside their task-1 baselines (SetupPanel 13, HivePicker 10, OfficeThemePicker 10). **Occurrences (`-o`), not lines (`-c`)** — the same unit and regex plan 23 uses in wave 9.
    - M1d and M1x over all five of this group's files return `0` hits each after the sweep — paste both. This is the completeness bar for the sites M1 is blind to; without it FLOOR-12 can sign off with sub-14px text still shipping.
    - `grep -c "cth-font-display" <file>` is unchanged for each of the three files.
    - `git diff <base sha>..HEAD -- src/renderer/src/components/OfficeThemePicker.tsx` shows no change within the `disposeTerminal` call region. Scoped to the file and to a recorded base sha rather than a bare working-tree diff, so it reads the same whether or not a wave-mate has committed in between.
    - Task notes contain this group's frozen allowlist as a literal `file:line` array (or an explicit "empty").
    - Task notes contain the re-run M1 count for all five group files.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today, of `426` total). Exit code alone
      is not a gate: `node --test` counts skipped tests in its total and exits `0` on an all-skipped file
      (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from a green one by
      exit code. This plan adds no tests, so any rise in `# skipped` is a red test being hidden rather
      than fixed.
    - **Containment, path-scoped so it survives the concurrent wave.** Six plans sweep in wave 7 against
      ONE working tree (`use_worktrees: false`), so a bare whole-tree `git diff --stat` always shows the
      other five agents' edits and can therefore never pass — **do not use one as a containment check.**
      Stage and commit with an explicit pathspec (`git add -- <this plan's five paths>` then
      `git commit -- <the same paths>`); never `git add -A`, `git add .` or `git commit -a`, which sweep
      a wave-mate's unstaged work into this plan's commit. Then assert all three:
      (a) **per commit and filtered — this is the one that catches a genuine cross-set edit.** Bind both
      variables **inside** the criterion: a criterion is its own shell invocation, and an unbound `$SHAS`
      iterates zero times, so the filter would emit nothing and score as PASS having checked nothing.
      Substitute the literal base sha task 1 recorded for `<base sha>`, then run:
      `BASE=<base sha>; SHAS=$(git log --format=%H "$BASE"..HEAD -- <this plan's five paths>); echo "$SHAS"; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/renderer/src/components/(AddAgentModal|OnboardingWizard|SetupPanel|HivePicker|OfficeThemePicker)\.tsx)?$"`
      The filter must produce **no output**, and `echo "$SHAS" | grep -c .` must be **at least 1** — paste
      both, plus `for sha in $SHAS; do git show --stat --format= "$sha"; done` so the touched set is on
      the page. The trailing `?` in the pattern absorbs the blank line `--format=` emits. The `grep -c .`
      floor is load-bearing: with no commits made, the loop body never runs and the filter is empty for
      the wrong reason. Replaces an unfiltered `git show --stat --format=` adjudicated by eye — a
      diffstat with no pass/fail filter is not a criterion. An edit into plan 15, 17, 18, 19 or 20's files shows up here
      as an extra path; it is invisible to any whole-tree diff. **Selected by pathspec, so this is this
      plan's own commits:** five wave-mates commit into this same tree in this same wave, and a tree-wide
      filter would fail this plan on their work. If a wave-mate does commit one of this plan's paths, its
      commit enters `$SHAS` and its other paths fail the filter — exactly the cross-contamination this
      criterion exists to catch.
      (b) **path-scoped and positive — the declared set actually moved.**
      `git diff --name-only <base sha>..HEAD -- <this plan's five paths> | sort -u` lists **all 5** paths
      of `files_modified` — paste the list beside `... | sort -u | grep -c .`, which must print exactly
      `5`. The count is the whole gate, and it is equivalent to set equality here: a pathspec-scoped diff
      can only ever emit paths from that pathspec, so `5` unique lines can only be the 5 declared paths.
      That is why the previous wording — *"lists only paths from `files_modified`"* — had no failing case
      at all, and why empty output, the do-nothing case, satisfied it too. Verified 2026-08-21 that every
      one of these 5 files carries measured sub-14px work (AddAgentModal 35, OnboardingWizard 32, SetupPanel 13, HivePicker 10, OfficeThemePicker 10
      occurrences), so all 5 must appear. Scoped
      to this plan's own paths, it is unaffected by wave-mates and passes while they work.
      (c) **nothing left behind** — `git status --porcelain -- <this plan's five paths>` is empty, so this
      plan leaves none of its own edits uncommitted for the next wave-mate's `git add` to absorb. An
      uncommitted edit of this plan's IS the lost update the whole-tree check was reaching for.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All five onboarding/picker files clear the floor, every icon-only control is named, and the group's allowlist is frozen for plan 23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| first-run onboarding UI → an operator's initial configuration | Onboarding is where auto mode, engines and the hive location are first chosen. Unreadable text here produces a misconfiguration the operator never revisits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P16-01 | Information disclosure | onboarding text below the legibility floor | mitigate | Every site swept to a token; the per-file M1 count must equal the allowlist size. |
| T-P16-02 | Information disclosure | an icon-only control with no accessible name | mitigate | Per-element `aria-label` pass from a pre-built inventory, never a ratio. |
| T-P16-03 | Denial of service | a bumped label clipping and hiding a wizard step's meaning | mitigate | Containment applied in UI-SPEC's order with measured deltas; "stop and report" rather than reflow. |
| T-P16-04 | Repudiation | a moved `eslint-disable-next-line` silently detaching a suppression | mitigate | Every suppression line number is recorded in task 1 and re-asserted after each edit pass, so plan 21's lint gate lands on live suppressions. |
| T-P16-05 | Denial of service | disturbing `OfficeThemePicker`'s terminal dispose path | mitigate | The dispose region is explicitly out of bounds and a `git diff` check asserts it. |
| T-P16-06 | Tampering | editing a file that belongs to plan 15, 17, 18, 19 or 20 in the same wave | mitigate | Per-commit `git show --name-only --format=` piped through an allowlist filter that must be empty, with `$SHAS` bound inside the criterion and a `grep -c .` floor so an executor that committed nothing cannot pass it, plus path-scoped `git diff`/`git status`; a bare whole-tree `git diff --stat` is explicitly forbidden here because in wave 7 it always shows five wave-mates' edits and so can never pass. |
| T-P16-90 | Information disclosure | sub-14px user-facing text M1 structurally cannot see — decimals, quoted px, ternaries and computed sizes — shipping unexamined past plan 23's bar | mitigate | The M1d and M1x scans run over this plan's paths in task 1 and again after the sweep; every M1x hit carries an evaluated minimum and no minimum may be below 14. |
| T-P16-91 | Repudiation | a containment check that can never pass, so it is quietly dropped | mitigate | The whole-tree `git diff --stat` is replaced by a per-commit `git show --name-only --format=` allowlist filter, a path-scoped `git diff --name-only` that must list **all** the plan's declared paths, and a path-scoped `git status --porcelain`. All three pass while five wave-mates edit the same tree, and each has a real failing case: an out-of-set path, a declared file left untouched, and an uncommitted edit respectively. |
| T-P16-92 | Repudiation | a red test hidden as a skip to make the sweep green | mitigate | TAP counters are baselined in task 1 and re-compared after the sweep; `# skipped` may not rise. `node --test` counts skips in its total and exits `0`, so exit code alone is not the gate. |
| T-P16-93 | Repudiation | wave 7 pinning line counts while wave 9 asserts occurrence counts | mitigate | Every per-file number in this plan is an occurrence count taken with `grep -hoE … | wc -l`, the same unit and regex plan 23 uses, and the unit is stated beside each pinned number. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: walk the onboarding wizard end to end in the dev app and open the add-agent modal, hive
  picker and theme picker. Confirm nothing is clipped and no label wraps into a control.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all five files in this group.
- Every `<button>` in the group has an accessible name, by the rule rather than by a ratio.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-16-SUMMARY.md` when done. It MUST contain this group's
frozen Rule 0 allowlist as a literal `file:line` array (or an explicit "empty"), the final per-file M1
counts **stated as occurrences with the unit named**, the M1d/M1x scans before and after with every M1x
hit's evaluated minimum, this plan's base sha with every commit sha and its `git show --stat`, the TAP
counters before and after, every container integer changed with its delta, every `aria-label` added with
its button, and confirmation that every `eslint-disable-next-line` is still on its target line.
</output>
