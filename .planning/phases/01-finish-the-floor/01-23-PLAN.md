---
phase: 01-finish-the-floor
plan: 23
type: execute
wave: 9
depends_on: ["01-13", "01-14", "01-15", "01-16", "01-17", "01-18", "01-19", "01-20", "01-21", "01-22"]
files_modified:
  - test/repo-claims.test.cjs
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-finish-the-floor/01-VALIDATION.md
  # Task 3's bounded doc-claim sweep. Each of these states something this phase makes false and
  # NO other plan declares any of them (verified 2026-08-20 against every 01-*-PLAN.md
  # files_modified block). Line numbers are pointers — re-derive by content, never by line.
  - .planning/codebase/STACK.md          # Electron 32.3.3 / `^32.2.0` (:18 Runtime, :27 Frameworks) — false after wave 1
  - .planning/codebase/CONCERNS.md       # the `#10` row "the 38+ bump never happened" (:11) and the "Electron 32.2.0 is end-of-life" entry (:36-37) — false after wave 1
  - .planning/PROJECT.md                 # Constraints "Electron 32 (end-of-life …)" (:96) — false after wave 1
  - .planning/codebase/INTEGRATIONS.md   # the freeflow.ts "Electron 32 bundles Node 20's undici" clause ONLY (:51). Plan 02 declares this file for `:116` ONLY; the two edits do not overlap and neither may touch the other's line.
  - .planning/codebase/CONVENTIONS.md    # the Linting subsection (:31-37) — "No linter configured" goes false when plan 21 lands, and its ":35 Seven `react-hooks/exhaustive-deps` disables" count is ALREADY wrong today (the sentence says Seven, then lists nine files, and `grep -rc` finds nine in source)
autonomous: false
requirements: [FLOOR-02, FLOOR-05, FLOOR-11, FLOOR-12, FLOOR-17]
# Also VERIFIES (does not deliver) the three requirements whose production wiring crosses a wave
# boundary and is therefore claimable a wave early by a naive rollup: GATE-01 (plan 02 w2 -> plan 06
# w3), FLOOR-09 (plan 06 w3 -> plan 08 w4), FLOOR-10 (plan 09 w4 -> plan 10 w5). See the wholeness
# criteria in task 3 — each fails if the minting plan closed but the applying plan never landed.

must_haves:
  truths:
    - "`gh issue list --state open --label floor-inspection` returns only the four epics"
    - "Every negative grep this phase depends on is a test that runs in `npm test` on three platforms"
    - "The FLOOR-12 completeness bar is asserted mechanically: only allowlisted sites remain below 14px, keyed on content so wave 8's line shifts cannot break it and a new site still can"
    - "The four previously mis-assessed issues were re-verified from fresh context before closing"
    - "No requirement is marked done against a clause that was never checked"
    - "The phase gate reads counters, not exit codes: `# fail 0`, `# skipped` <= the frozen baseline of 4, `# pass` > 422 — and re-runs the phase's own fake-coverage guards at wave 9 rather than trusting the wave that introduced them"
    - "Every CI claim resolves against the draft PR's checks table, and an empty table fails"
  artifacts:
    - path: "test/repo-claims.test.cjs"
      provides: "the whole D-45 accumulator, asserted together"
      contains: "stripComments"
  key_links:
    - from: "test/repo-claims.test.cjs"
      to: "the frozen FLOOR-12 allowlist"
      via: "a content-keyed {file, text, count} multiset so a new sub-14px site fails the suite rather than widening the exemption, and a line inserted above an existing site does not"
      pattern: "allowlist"
    - from: "plan 01-21's renderer edits (wave 8)"
      to: "this plan's FLOOR-12 allowlist assertions (wave 9)"
      via: "the line-anchor contract: plan 21 changes no line matching M1, and this plan keys its allowlist on content rather than on a line number"
      pattern: "fontSize"
---

<objective>
Turn the end-of-phase sweep into `npm test` on three platforms plus one `gh` query, and close the phase
against evidence rather than against an agent's report.

Purpose: D-45 and D-46. The 2026-08-20 error this phase exists to prevent happened at a specific
resolution: four issues with real landed code and exactly **one unmet clause each** were closed against
a bar of "some code exists". A per-clause bar fails all four. This plan makes that bar mechanical and
permanent — every negative grep the phase relies on becomes a test that runs on every future PR.

Output: the completed repo-fact accumulator including the FLOOR-12 completeness bar, the D-47
adversarial re-verify of the four mis-assessed issues, and the D-46 mechanical gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-VALIDATION.md
@.planning/codebase/CONCERNS.md

<interfaces>
`test/repo-claims.test.cjs` was created by plan 05 and appended to by plans 07 and 10 — **one owner per
wave**. This plan is the final owner and asserts it whole. Read the file before appending; do not
duplicate a clause another plan already added.

**Its `stripComments` helper is mandatory, not optional.** Several Phase 1 fixes deliberately add a
comment quoting the very thing they deleted — plan 02's `HIVE_SOCK_TOKEN` deletion, plan 13's
`index.ts:269` explanation. A raw grep would match the explanatory comment and fail the correct fix.
Every source-grep assertion in this file runs on comment-stripped source.

**The FLOOR-12 completeness bar (UI-SPEC, binding).** Criterion 4's clause is *"no user-facing text sits
below the 14px floor DESIGN.md states."* It is TRUE when **all four** hold:
1. `src/renderer/src/design/tokens.css` declares no `--cth-text-*` value below 14px, and
   `--cth-text-display-sm` / `--cth-lh-display-sm` are gone.
2. Command **M1** over `src/renderer/src/**/*.{ts,tsx}` returns **only** sites on the frozen allowlist
   (≤26 entries), i.e. `604 − |allowlist|` sites converted.
3. Every allowlisted site's element is one of the 26 exempt candidates (or a local override created for
   one) and carries `aria-hidden="true"`.
4. The 3 non-literal sites — `ThoughtBubble.ts`'s and `ToolBubble.ts`'s `FONT_SIZE` constant, and
   `FullscreenTerminal.tsx`'s `Math.max(...)` floor — are at or above 14. (Today
   `ThoughtBubble.ts:22` and `ToolBubble.ts:29` both read `const FONT_SIZE = 12;`. Those line numbers
   are pointers for a reader, not assertion keys — see the line-anchor contract below.)

**The allowlist is produced by the sweep, not before it** — some entries are local overrides plans 14-20
created. Assemble it from the **seven** sweep plans' SUMMARYs (`01-14`, `01-15`, `01-16`, `01-17`,
`01-18`, `01-19`, `01-20`).

**The wave-8 -> wave-9 line-anchor contract. This is a two-plan contract; plan `01-21` carries the
matching half and neither may be changed alone.**

The obvious encoding — a literal `file:line` array — is **wrong here, and provably so.** The seven sweep
SUMMARYs are written in wave 7. Plan `01-21` then edits renderer files in wave 8, *between* those
SUMMARYs and this test. Measured on the current tree (2026-08-20): 8 of plan 21's 9 certain-to-be-edited
files are also M1 files, **81 M1 sites sit below a suppression plan 21 edits** (`GitTab.tsx` 12,
`MemoryGraphPanel.tsx` 9, `OnboardingWizard.tsx` 32, `TerminalView.tsx` 1, `SchedulesSection.tsx` 1,
`WebhooksSection.tsx` 3, `IdePanel.tsx` 20, `CompletionToast.tsx` 3), and plan 21 must insert six
explanatory comment lines above six of those suppressions. 44 of the 61 M1 files contain a React hook,
so a dependency fix can shift an anchor in a file plan 21 does not even name. A wave-9 test holding
wave-7 line numbers would be off by at least one line and would fail for a reason unrelated to FLOOR-12.

**The contract has two halves:**

1. **Plan `01-21` changes no line that matches M1**, and asserts it —
   `git diff -U0 "$BASE" -- src/renderer/src | grep -E "^[-+]" | grep -E "fontSize *[:=]"` must be empty
   in its task 2. So the *text* of every M1 site is stable across wave 8, even though its line number is
   not.
2. **This plan keys its allowlist on content, not on a line number.** Each entry is
   `{ file, text, count }` where `text` is the trimmed source line and `count` is how many times that
   exact trimmed text appears as an M1 hit in that file. The assertion compares two **multisets**: the
   `(file, text) -> count` map built by running M1 over the tree as it stands at wave 9, against the
   frozen allowlist map. They must be equal.

   This keeps the anti-widening guarantee intact — a new sub-14px site either introduces a `(file, text)`
   key that is not on the list or bumps an existing count, and either way the suite fails — while making
   a line inserted above an existing site a non-event.

**Line numbers quoted anywhere in this plan are informational pointers, never assertion keys.** Every
FLOOR-12 and guard assertion below locates its target by content: `FONT_SIZE` by identifier,
`Math.max(...)` by identifier, `<div role="button">` by attribute, `PixelButton`'s `{children}` body by
content. Re-derive the allowlist from the tree at wave 9 and reconcile it against the seven SUMMARYs;
if a SUMMARY's `file:line` no longer resolves to the same text, that is the contract working, not a
defect — record the reconciliation and move on. If a SUMMARY's *text* no longer exists anywhere in its
file, half 1 of the contract was broken: STOP and report it against plan `01-21` rather than editing the
allowlist to match.

**M1, pinned. Do not re-derive with a different regex** (the count moves ±6 with regex shape):
`grep -rhoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" src/renderer/src --include=*.tsx --include=*.ts | wc -l`

**The FLOOR-12 accessible-name assertion must be the rule, not the ratio.** 49 `aria-label` against 133
`<button>` is not a bar, and a ratio test would be wrong: a `<button>` with visible text already has an
accessible name, and adding `aria-label` to it *overrides* the visible label and makes the UI worse for
voice-control users. Assert that **icon-only** buttons have an `aria-label`.

**D-46, the mechanical phase gate:**
`gh issue list --state open --label floor-inspection --json labels --jq '[.[]|select(.labels|map(.name)|index("epic")|not)]|length'`
must return `0` — i.e. only #47, #48, #49 and #73 remain, all `epic`-labelled.

**D-47:** the four previously mis-assessed issues — **#4, #5, #10, #34** — additionally get a
fresh-context adversarial re-verify before closing. A subagent verdict is still an agent's report, so
this is an **addition** to D-43's mechanical bar, never a substitute for it. Their single unmet clauses
were: #4 `grep autoMode src/renderer/src/components/AgentCard.tsx` → empty; #5 queue-drain and quiesce
still renderer-only; #10 `electron` still `^32.2.0`; #34 no consumer of `taskSpend().over`.

**D-44 stands: issues close incrementally, in the PR that fixes them.** This plan is **not** an
issue-closing sweep. It closes only what genuinely remains open after every fixing PR has landed, and it
reports any issue that is fixed-but-open as a process failure to record, not to quietly absorb.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Assemble the complete repo-fact accumulator, including the four-clause FLOOR-12 completeness bar</name>
  <files>test/repo-claims.test.cjs</files>
  <read_first>
    - `test/repo-claims.test.cjs` in full — everything plans 05, 07 and 10 added, and the `stripComments` helper.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md`, `01-15-SUMMARY.md`, `01-16-SUMMARY.md`, `01-17-SUMMARY.md`, `01-18-SUMMARY.md`, `01-19-SUMMARY.md`, `01-20-SUMMARY.md` — the **seven** frozen Rule 0 allowlists and the final per-file M1 counts. **None of them carries a whole-renderer total** — plans 15-20 sweep concurrently in wave 7, so any repo-wide number taken inside one of them is stale on arrival. This plan runs the repo-wide M1 itself, once, after every sweep has landed.
    - `01-UI-SPEC.md` § FLOOR-12's completeness bar and the accessible-name rule.
    - `<interfaces>` § "the wave-8 -> wave-9 line-anchor contract" — half 2 is this task's obligation. Plan `01-21`'s task 2 carries half 1.
    - `.planning/phases/01-finish-the-floor/01-21-SUMMARY.md` — confirm its two containment filters (the `fontSize` diff filter and the `git diff --name-only` filter) are both recorded empty. If they are not, half 1 of the contract was broken and this task's allowlist cannot be trusted.
    - `test/ci-config.test.cjs` in full — what is already asserted there (plans 04 and 20), so no clause is duplicated across the two files.
    - `01-PATTERNS.md` § "`test/repo-claims.test.cjs`" `:66-156` — the structural-assertion pattern (parse, then compare two sources of truth; never assert a literal string for a parsed artifact).
  </read_first>
  <action>
    Append every outstanding repo-fact clause to `test/repo-claims.test.cjs`. Read what is already there
    first — plans 05 (FLOOR-11), 07 (FLOOR-02's HIVE.md denials plus the positive Stop-drain wiring) and
    10 (FLOOR-05's preload export, FLOOR-07's deletions and the FTS5 migration) have already landed
    theirs. Do not duplicate.

    Add the **FLOOR-12 completeness bar as four separate tests**, one per clause, so a failure names
    which clause broke:
    1. Parse `tokens.css` and assert no `--cth-text-*` declaration has a px value below 14, and that
       `--cth-text-display-sm` and `--cth-lh-display-sm` are absent. Parse — do not match a literal
       string, which breaks on whitespace.
    2. Run M1's regex over `src/renderer/src/**/*.{ts,tsx}` from within the test, build the
       `(file, trimmed-line-text) -> count` multiset it produces, and assert that multiset equals the
       frozen allowlist multiset. **Embed the allowlist as a literal `{ file, text, count }` array, not
       as `file:line`** — the line-anchor contract in `<interfaces>` explains why a `file:line` array is
       provably wrong here (81 M1 sites sit below a plan-21 edit). Assemble it from the seven sweep
       plans' SUMMARYs (01-14 through 01-20; ≤26 entries), re-derived against the tree as it stands at
       wave 9, and record the reconciliation. The assertion message must print both directions of the
       symmetric difference — sites present but not allowlisted, and allowlisted but absent — so a
       failure is actionable and a stale allowlist entry cannot rot unnoticed.
    3. For every allowlisted site, assert its element carries `aria-hidden="true"` — locate the site by
       matching its `text` in the file, then read the surrounding element. Do not index by line number.
    4. Assert the three non-literal sites are at or above 14, locating each by identifier and not by
       line: `ThoughtBubble.ts`'s and `ToolBubble.ts`'s `FONT_SIZE` constant (both read
       `const FONT_SIZE = 12;` today, so this clause is a real gate on wave 7's work, not a formality),
       and `FullscreenTerminal.tsx`'s `Math.max(...)` floor.

    Add the FLOOR-12 accessible-name test as **the rule, not a ratio**: find `<button>` elements whose
    only child content is an icon, glyph or symbol, and assert each carries an `aria-label`. Do **not**
    assert a count or a ratio of `aria-label` to `<button>` — a text button that gained an `aria-label`
    would pass a ratio test while being a regression.

    **`src/renderer/src/components/PixelButton.tsx` is pre-classified as NOT icon-only, and the test
    must encode that classification rather than discover it.** It is the shared button primitive and
    the only `<button>`-bearing renderer file outside the 61-file FLOOR-12 M1 set (38 of the other 39
    are inside it) and outside every plan's `files_modified` in this phase — nothing in Phase 1 edits
    it, by design. Its `<button>` at `:82` renders `{children}` at `:116`, a `ReactNode` prop, not a
    glyph literal. A predicate that treats `{children}` as "unrecognised content, therefore icon-only"
    would demand an `aria-label` on a primitive whose accessible name is correctly supplied by its
    *callers*, and adding one there would override every caller's visible text — the exact regression
    the no-ratio rule exists to prevent. So: exclude it by name, with a comment stating this reason,
    and assert separately that its `<button>` still renders `{children}` (so the exclusion cannot
    silently outlive its justification). If the predicate is written such that a `{children}` body is
    already classified as text-bearing, say so and skip the exclusion — but state which it is; do not
    leave the classification implicit.

    Add FLOOR-17's clauses if plan 04 did not put them in `test/ci-config.test.cjs`: the bug template
    parses and its Logs field points at Settings; `docs/adr/` holds at least six numbered records and
    `README.md` in that directory names each.

    Add a guard test for the two deliberate `<div role="button">` in `AgentCard.tsx` and
    `FullscreenTerminal.tsx`: assert both still exist and both still carry an accessible name. Locate
    them by matching `role="button"` in each file, **not** by the line numbers `:145` and `:604` — those
    are pointers for a reader, and `FullscreenTerminal.tsx` has `useEffect` at `:142` and `:168`, both
    above both anchors, so plan `01-21` can legitimately shift them.
    `.planning/codebase/CONCERNS.md` records them as resolved history, and a future "cleanup" that
    converts them to native `<button>` would regress a documented decision.

    Every assertion runs on comment-stripped source where it greps, and every assertion message says what
    a failure would MEAN, naming the issue number.
  </action>
  <acceptance_criteria>
    - `node --test test/repo-claims.test.cjs` exit code is `0`.
    - The file contains four distinctly named FLOOR-12 completeness tests — paste their names from the run output.
    - `grep -c "aria-hidden" test/repo-claims.test.cjs` returns at least `1` and the allowlist array is present as a literal — paste it. Every entry carries `file`, `text` and `count`; **no entry carries a line number**. `grep -c "line:" test/repo-claims.test.cjs` returns `0` — paste it.
    - **Line-anchor contract, half 1 is confirmed before the allowlist is trusted:** `01-21-SUMMARY.md` records both of plan 21's containment filters as empty. Paste both lines from that SUMMARY. If either is non-empty or absent, STOP and report against plan `01-21` — do not edit the allowlist to absorb a drift plan 21 was contracted not to create.
    - **Line-anchor contract, half 2 is proven by mutation, not by inspection.** Pick any allowlisted file as `$f` and run both probes, pasting both results and restoring the file after each (`git checkout -- "$f"`, then `git status --porcelain "$f"` empty):
      (a) `sed -i '1i // anchor-shift probe' "$f" && node --test test/repo-claims.test.cjs` — must exit `0`. A line-keyed allowlist fails this, so passing it is the proof the re-keying is real.
      (b) `{ echo ''; echo 'const probe = { fontSize: 9 };'; } >> "$f" && node --test test/repo-claims.test.cjs` — must exit **non-zero**, and the failure message must name the new site. This proves the anti-widening guarantee survived the re-keying and was not traded away for anchor-independence.
    - The accessible-name test contains no ratio or count comparison against the number of `<button>` occurrences — paste the test body.
    - `src/renderer/src/components/PixelButton.tsx`'s classification is explicit in the test file: either an exclusion by name with its stated reason, or a demonstration that the predicate already classifies a `{children}` body as text-bearing. Paste whichever applies, plus the assertion that its `<button>` still renders `{children}`.
    - `PixelButton.tsx` is byte-identical to its pre-phase state. **Do not use `git diff --stat` with no revision range** — that compares the working tree to the index, so it is empty after any commit no matter what the phase did to the file, and asserting on it proves nothing. Use the content hash, which needs no revision range and cannot be satisfied by a touch-then-revert being invisible or by a rebase renumbering history:
      `git hash-object src/renderer/src/components/PixelButton.tsx` returns exactly
      `bd286ebf5654a2647c93546dc135f608aeb5d0f0` — measured 2026-08-20 at the pre-execution tree, last
      genuinely touched by `1947cf0` (2026-08-17), before this phase existed. Paste the command and its
      output. Beside it, paste the audit trail
      `git log --oneline 1947cf0..HEAD -- src/renderer/src/components/PixelButton.tsx`, which must be
      **empty**. If the hash differs, the exclusion's justification is dead: re-derive the
      classification against the file as it now reads — do **not** update the hash to match.
    - Running M1 by hand at the shell returns exactly `sum(allowlist[].count)` **occurrences** — not lines. The unit matters and is the exact wave-7/wave-9 reconciliation point: the pinned command is `grep -rhoE … | wc -l` (occurrences, `604` today), while a `grep -rcE` sums to `603`, because `src/renderer/src/components/IntegrationsRegistry.tsx:388` carries two M1 hits on one line. It is the only such file in the tree (all 61 fontSize-bearing renderer files were swept 2026-08-20). Wave 7 pins its per-file counts in occurrences for this reason. Paste the command output and the summed allowlist size side by side.
    - **The two blind-spot classes are clean, or exempted in writing.** M1's regex requires a bare digit right after `fontSize:`, so it cannot see decimal, quoted, or expression-valued sizes — and red-team round 2 found twelve real sub-14px sites hiding in exactly those forms. Asserting M1 alone would sign FLOOR-12 off with `fontSize: 12.5` re-introduced. Run both scans wave 7 defines and paste both:
      - **M1d** (decimal and quoted literals, e.g. `fontSize: 10.5`, `fontSize: '13px'`) must return `0`.
      - **M1x** (expression-valued: ternaries, `Math.max`, `clamp`, and identifiers resolving below 14) must return **only** the terminal carve-out hits — `terminalFontSize.ts`, `terminalPool.ts`, and `term.options.fontSize` in `PtyTerminalView.tsx`. UI-SPEC's "terminal sizing is user-controlled" covers xterm's own font and nothing else; chrome that merely reads the zoom is not exempt. **One further permitted class, and only this one:** a hit whose value is a **design token**, where
        the token's own value satisfies the floor after plan 14's `tokens.css` migration. There is exactly
        one such site — `src/renderer/src/components/PixelButton.tsx:102`,
        `fontSize: size === 'lg' ? 'var(--cth-text-body-md)' : 'var(--cth-text-body-sm)'` — whose minimum
        branch is `--cth-text-body-sm`, `13px` today (`tokens.css:66`) and `14px` after plan 14, **with no
        edit to the component**. Plan 23 separately requires that file to stay byte-identical, so failing it
        here would demand a change plan 23 itself forbids. Resolve the token rather than the expression:
        paste the token's post-migration value and assert it is ≥ 14. Any hit that is neither the terminal
        carve-out nor a floor-satisfying token fails.
      - `rosterScale` and `composerFontSize` floor at **≥ 14**: paste the `clamp`/`Math.max` minima from `FullscreenTerminal.tsx` and `MessageQueueComposer.tsx`. Baselines today are 7, 7, 10, 8 and 8 — all below the bar, so this criterion fails against current source.
      If any hit is a deliberate exemption rather than a defect, it must appear in the allowlist with a written reason, exactly as the `.tsx` sweeps record theirs. An empty M1d/M1x result with no allowlist entry and no pasted command is not a pass.
    - `npm test` exit code is `0` on this machine, and `npm run typecheck` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>Every negative grep the phase relies on is a test that runs in the real gate, and the FLOOR-12 completeness bar is asserted clause by clause against a content-keyed frozen allowlist that survives wave 8's line shifts but still fails on a new sub-14px site.</done>
</task>

<task type="auto">
  <name>Task 2: Fresh-context adversarial re-verify of the four previously mis-assessed issues, then close whatever genuinely remains open</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/codebase/CONCERNS.md` — **read the "Method" paragraph first.** It records the 2026-08-20 correction verbatim: four issues were wrongly called stale trackers because the bar applied was "some code exists" rather than "the issue's stated done".
    - `01-CONTEXT.md` D-42 … D-47 — the evidence bar, the closing-comment contents, incremental closing, the accumulator, the mechanical gate, and the adversarial re-verify.
    - Every SUMMARY in `.planning/phases/01-finish-the-floor/` — what each plan claims it closed.
    - `.planning/REQUIREMENTS.md` FLOOR-01…18, GATE-01, RECORD-03/04, VERDICT-02/03 — the requirement text, some of which earlier plans corrected.
  </read_first>
  <action>
    Run `gh issue list --state open --label floor-inspection --limit 200` and paste the full output. **The
    `--limit` is load-bearing:** `gh issue list` defaults to 30, so without it a growing backlog silently
    truncates and this sweep certifies against a partial list. For every issue
    still open, determine from its own acceptance text, clause by clause, whether it is genuinely open or
    fixed-but-open.

    **D-47 — the four previously mis-assessed issues (#4, #5, #10, #34) get a fresh-context adversarial
    re-verify before closing.** Dispatch a subagent per issue with **no** access to this phase's
    SUMMARYs, given only the issue's acceptance text and the repository, and asked to find the clause
    that is NOT satisfied. A subagent verdict is still an agent's report, so this is an **addition** to
    D-43's mechanical bar, never a substitute: paste each verdict alongside the mechanical evidence, and
    if a verdict and a grep disagree, **the grep wins and the disagreement is recorded**.

    Their original single unmet clauses, to be re-checked explicitly:
    - #4 — `grep autoMode src/renderer/src/components/AgentCard.tsx` was empty; it must now be non-empty.
    - #5 — queue-drain and quiesce were renderer-only; both must now run in main.
    - #10 — `electron` was `^32.2.0`; it must now be `^43.x`.
    - #34 — nothing consumed `taskSpend().over`; something must now.

    For each issue that is genuinely closable, post the D-43 closing comment: the issue's own acceptance
    text quoted **verbatim**, one command per clause with its **actual output pasted** (an empty grep is
    the evidence), the **named test** added in the fixing PR, and the `npm test` exit line — run on
    Windows too where the fix is platform-touching.

    **D-44 stands: issues close in the PR that fixes them, not in an end-of-phase sweep.** If you find an
    issue that is fixed but still open, close it here **and record it in the SUMMARY as a process
    failure**, naming which plan should have closed it. Do not quietly absorb it — a batch pass under
    time pressure is exactly the shape that produced the recorded 2026-08-20 error.

    If any issue is genuinely still open, **STOP and report** with the unmet clause quoted. Do not close
    it, do not mark its requirement done, and do not proceed to task 3.

    Finally, update `.planning/REQUIREMENTS.md` only where a requirement's own text is now factually
    wrong about source — earlier plans already corrected VERDICT-03. Do not tick checkboxes for
    requirements whose evidence you have not personally pasted in this task.
  </action>
  <acceptance_criteria>
    - Task notes contain the full pasted `gh issue list --state open --label floor-inspection` output.
    - Task notes contain, for each of #4, #5, #10 and #34: the fresh-context subagent verdict, the mechanical per-clause evidence, and an explicit statement of agreement or disagreement.
    - Task notes contain the four specific re-checks with pasted output: `grep -c autoMode src/renderer/src/components/AgentCard.tsx` (now non-zero), the queue-drain and quiesce location in main, the `electron` version line, and the consumer of `taskSpend().over`.
    - Every issue closed in this task has a closing comment containing its verbatim acceptance text, per-clause commands with pasted output, a named test, and an `npm test` exit line.
    - Any fixed-but-open issue is named in the notes together with the plan that should have closed it.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every remaining issue is closed against its own acceptance text with pasted evidence, and the four historically mis-assessed issues carry both a mechanical and an adversarial verdict.</done>
</task>

<task type="auto">
  <name>Task 3: Run the mechanical phase gate and the full verification sweep</name>
  <files>.planning/phases/01-finish-the-floor/01-VALIDATION.md, .planning/codebase/STACK.md, .planning/codebase/CONCERNS.md, .planning/PROJECT.md, .planning/codebase/INTEGRATIONS.md, .planning/codebase/CONVENTIONS.md</files>
  <read_first>
    - `01-VALIDATION.md` in full — the sampling rate, the Nyquist problem, and the Validation Sign-Off checklist.
    - `.planning/ROADMAP.md` § "Phase 1: Finish the Floor" — the five success criteria, read as D-02 restates criterion 2 ("Electron 43.x", not "38+") and as D-37 corrects criterion 1's premise (the Stop-drain was already live).
    - `.planning/phases/01-finish-the-floor/01-01-SUMMARY.md` — confirm D-09's live Windows run is recorded with its three answers.
  </read_first>
  <action>
    Run D-46's mechanical gate and paste the output:
    `gh issue list --state open --label floor-inspection --json labels --jq '[.[]|select(.labels|map(.name)|index("epic")|not)]|length'`
    It must return `0`. If it does not, task 2 is incomplete — stop and report rather than reinterpreting
    the number.

    **The phase's CI evidence is the draft PR's checks. There is no other CI for this branch.**
    `ci.yml:4-7` and `e2e.yml:11-14` both trigger on exactly two events — `push` to `main` and
    `pull_request` based on `main` — so a push to the phase branch produces **zero workflow runs** and
    there is no run URL to paste. Plan 01 task 3 opens a draft PR to `main` for precisely this reason;
    that PR is what every "CI green on three platforms" claim in this phase resolves against. Read it
    with `gh pr checks`, and paste `gh pr view --json url,isDraft,baseRefName` beside it so the PR the
    checks belong to is on the page.

    **`npm run e2e` is not runnable on this host and must not be attempted as this task's gate.**
    `npm run e2e` is `playwright test`; `node_modules/@playwright` does not exist (Playwright is in
    `package-lock.json` and has never been installed), `node_modules/better-sqlite3/build` and
    `node_modules/node-pty/build/Release` are both absent because the only install that has ever
    succeeded here is `--ignore-scripts`, and `e2e/smoke.spec.ts` requires `npm run build` first. All
    verified 2026-08-20. The E2E evidence is the `Electron smoke (ubuntu-latest)` job row on the draft PR — the same rule plan 01
    task 3 already applies.

    Run the full sweep and paste every result:
    - `gh pr checks` on the phase's draft PR — a row for each of `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)` and `Electron smoke (ubuntu-latest)`, every one `pass`. Paste the whole table plus the PR URL. The `Electron smoke (ubuntu-latest)` row is where plan 01's Electron-major assertion (D-10) shows green. **That is the JOB name (`e2e.yml:30`); `E2E` is only the workflow name (`e2e.yml:1`) and `gh pr checks` lists jobs — a criterion demanding an `E2E` row fails on a fully green PR.**
    - The full suite's **TAP counters**, locally, beside the PR table — see "the exit-code hole" below.
    - `npm run typecheck` locally, as a fast cross-check of the PR's `Typecheck` row — never as a replacement for it.
    - `npm run lint` (`eslint . --max-warnings 0`, the local install plan 21 wired — **never bare `npx eslint`**, which fetches an unpinned ESLint from the registry).
    - M1 by hand, with the allowlist size beside it.
    - `continue-on-error` in `.github/workflows/ci.yml`, **counted in both units so this plan cannot disagree with plan 21's notes.** Raw `grep -c "continue-on-error" .github/workflows/ci.yml` is **4** — but two of those four are prose inside comments (`:45` "There is no continue-on-error here", `:113` "It used to make the WHOLE build job continue-on-error"), so the raw count is not the gate. The gate is the **effective** count, non-comment occurrences only: `grep -cE "^[^#]*continue-on-error: *true" .github/workflows/ci.yml` is **2** (`:35`, `:117`). Both baselines measured 2026-08-20. Paste **both** numbers. Effective must be exactly `2` and raw must not exceed `4` — a raw increase with a flat effective count means a new comment, which is fine and must be shown rather than silently absorbed. **No `continue-on-error` was added anywhere, for any reason.**

    **Close the exit-code hole, here, at the phase gate.** `npm test` exiting `0` and a green row on
    the PR's checks table are *not* evidence that the phase's ~40 new tests ran. `node --test` counts skipped tests in its
    total and exits `0` when every test in a file is skipped, so every test added in waves 2-8 could be
    `{skip: '...'}` and this plan would still see exit `0` on three platforms. The only pass-count floor
    anywhere else in the phase is plan 01's wave-1 criterion, whose skip clause is now platform-scoped
    rather than `>=` — but a single wave-1 gate cannot see what waves 2-8 add, which permits
    skip growth by construction. So this task asserts the counters itself:
    `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"` (written to `mktemp`, never the repo root — `.gitignore` has no `*.tap` and this plan's own containment sweep would see it)
    Name the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 4`, so a criterion
    grepping for `#` against it matches nothing and passes silently.

    **And re-run the phase's own fake-coverage guards.** Each was run once, in the wave that introduced
    it, and never again; a guard that ran in wave 2 does not hold a phase closed in wave 9. Run all four
    now and paste each:
    1. **Plan 05's poisoned-assert probe** — proves no hand-rolled harness can fail silently:
       `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" && continue; node -e "const M=require('module'),o=M.prototype.require,b=()=>{throw new Error('poison')};M.prototype.require=function(i){return (i==='assert'||i==='node:assert')?new Proxy(b,{get:()=>b}):o.apply(this,arguments)};require(require('path').resolve(process.argv[1]))" "$f" >/dev/null 2>&1 && echo "SILENT-FAILURE: $f"; done`
    2. **Plan 06's hand-rolled harness enumeration** — proves no `node:test` registration was added to a
       file whose local `test()` would shadow it, and none was silently converted:
       `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done`
    3. **Plan 08's per-file counters** for `test/delivery-main.test.cjs`.
    4. **Plan 10's per-file counters** for `test/db-fts.test.cjs`, plus its bypass-construct grep.

    Then walk ROADMAP's five success criteria one at a time and, for each, cite the specific evidence
    that makes it TRUE — a test name, a pasted command output, or a recorded operator run. Note where a
    criterion's text was corrected during the phase and why: criterion 2's "Electron 38+" reads as
    Electron 43.x (D-02, because 38 is itself end-of-life and a literal reading would let this phase ship
    the exact unsupported-runtime defect FLOOR-03 exists to close), and criterion 1's "dead Stop-drain"
    premise was factually wrong (D-37, the drain is live and guarded). Correcting a roadmap claim IS
    doing this phase, not deviating from it.

    Confirm D-09's live Windows operator run is recorded in `01-01-SUMMARY.md` with its three answers.
    FLOOR-03 **cannot close without it** — "CI green" is explicitly not acceptable closure evidence,
    because all 423 unit tests run with `electron` stubbed and are structurally incapable of failing on
    an Electron-version regression.

    Also confirm the two outstanding live samples are recorded as outstanding rather than claimed:
    FLOOR-06's `gh attestation verify` (needs a real tag) and macOS notification delivery (unverifiable
    on this project — plan 13 states the limitation rather than claiming the capability).

    **Run the bounded final doc-claim sweep.** ROADMAP criterion 1 is written universally ("no doc
    claims a behaviour the code does not have") but plans 04, 07, 10 and 13 each close an *enumerated*
    set of claims. Nothing in the phase re-checks the doc set as a whole, and `01-CONTEXT.md` says
    plainly that the planner should expect to find more. So this task fixes the criterion's operational
    definition to a **bounded, listed** sweep — not an unbounded audit, and not an implicit one.

    Grep the doc set — `README.md`, `HIVE.md`, `DESIGN.md`, `RELEASE.md`, `SECURITY.md`,
    `CONTRIBUTING.md`, `docs/**/*.md`, `.planning/codebase/*.md` — for present-tense claims about the
    six surfaces this phase changed, and paste every hit with a verdict of TRUE or FALSE against the
    code as it now stands:
    - the Stop-drain / quiesce backstop (`Stop`, `drain`, `quiesce`)
    - `cursor.json`
    - responsive layout (`@media`, `responsive`, `mobile`, `breakpoint`)
    - the log folder button (`openLogs`, `open logs`)
    - FTS5 / memory recall (`FTS5`, `fts5`, `full-text`)
    - notifications and Codex remote control (`notification`, `toast`, `REMOTE CONTROL`)
    Also confirm every ADR pointer comment added in wave 2 resolves to a file that exists — **and match
    the format plans 04 and 05 actually mandate.** They require the bare substring `adr/0005` /
    `adr/0006` at the call site (plan 04 `:293`: `grep -rc "adr/0005" src/main/db.ts src/main/telemetry.ts`;
    plan 05 `:205`: the same for `adr/0006` in `terminalPool.ts` and `terminalPoolPolicy.ts`), so a
    pointer written `// see adr/0005-cumulative-cost-ledger` satisfies them while matching **no**
    `docs/adr/....md` pattern. A sweep keyed on `docs/adr/[0-9a-z-]*\.md` returns **empty** on this tree
    today (verified 2026-08-20 — there is not one `adr/` string anywhere in `src/`) and would pass
    vacuously over an empty list. Use the format they mandate, and treat empty as failure:
    `grep -rhoE "adr/[0-9]{4}[0-9a-z-]*" src/ | sort -u`
    Every hit must resolve to a real file — `ls docs/adr/<hit>*.md` for each — and the list must contain
    **at least the four pointers waves 2 and 3 are contracted to add**: `adr/0005` in `src/main/db.ts`
    and `src/main/telemetry.ts` (plan 04), `adr/0006` in `src/renderer/src/components/terminalPool.ts`
    and `src/renderer/src/store/terminalPoolPolicy.ts` (plan 05).

    **A seventh surface: the five planning docs this phase falsifies and no other plan owns.** The six
    surfaces above are grep patterns over a doc set; these five are named files with named sentences
    that go false the moment waves 1 and 8 land, and **no plan's `files_modified` declared any of them**
    (verified 2026-08-20 across all 23 plans). Plan 02 declares `INTEGRATIONS.md` for `:116` only, which
    is a different sentence in the same file — the two edits must not overlap. So this task owns them,
    and they are in its `files_modified`. Fix each in place; line numbers below are pointers, so locate
    every one by its text:
    - `.planning/codebase/STACK.md` — two Electron version claims, `:18` ("Electron 32.3.3 (resolved;
      `package.json` pins `^32.2.0`)") and `:27` ("Electron 32.3.3 — desktop shell"). Rewrite both to the
      version actually in `package.json` after wave 1. `:18` also asserts the runtime bundles **Node 20**
      — re-derive that from the shipped Electron, do not carry it forward.
    - `.planning/codebase/CONCERNS.md` — the `#10` row at `:11` ("`electron` is still `^32.2.0`; the 38+
      bump never happened") and the "**Electron 32.2.0 is end-of-life, and the bump has no open tracking
      issue**" entry at `:36-37`. Both describe a concern this phase closes; rewrite them as resolved
      history naming the plan and the landed version, in the same shape the file already uses for
      resolved items. Do not delete the entry — `CONCERNS.md` is what the phase's own premise is
      audited against.
    - `.planning/PROJECT.md:96` — the Constraints bullet "Electron 32 (end-of-life — Chromium 128,
      outside the latest-3 support window, no CVE backports) … The bump to Electron 38+ is real work,
      not a version string." Rewrite to the landed version. This is the project's own constraint list,
      so a stale entry here outlives the phase.
    - `.planning/codebase/INTEGRATIONS.md:51` — the `freeflow.ts` clause "(Electron 32 bundles Node 20's
      `undici`)", which is load-bearing for the `FormData`/`Blob` multipart claim beside it. Re-derive
      the bundled Node version from the Electron actually installed and rewrite. **Touch this sentence
      only** — `:116` belongs to plan 02.
    - `.planning/codebase/CONVENTIONS.md` — the whole **Linting** subsection, `:31-37`. Two separate
      defects: (a) `:31` "**No linter configured.** No `.eslintrc*`, `eslint.config.*`, no `eslint`
      package…" goes false when plan 21 lands, as does `:37`'s "there is no separate lint gate";
      (b) `:35` is **already wrong today** — it says "Seven `react-hooks/exhaustive-deps` disables" and
      then lists **nine** files. Measured 2026-08-20:
      `grep -rc "react-hooks/exhaustive-deps" src/` totals **9**, `@typescript-eslint` totals **4**,
      `eslint-disable` totals **13** — so `:32`'s "13 orphaned" is right and only the word "Seven" is
      wrong. Plan 21 deletes the 4 `@typescript-eslint/*` disables and resolves or keeps the 9
      `exhaustive-deps` ones per the linter's actual findings, so **re-derive all three counts from the
      post-plan-21 tree** rather than editing "Seven" to "Nine" — the correct number after wave 8 is
      whatever the greps return then.

    A FALSE hit is a defect, not a finding to record: fix the sentence here, in this plan, and list the
    fix. If a FALSE hit needs a code change rather than a doc change, stop and report — do not soften
    the sentence to match the code. **This list is the criterion's operational definition.** If a hit
    falls outside these six surfaces, it is out of this phase's scope; name it in the SUMMARY as
    carried forward rather than silently sweeping it.

    **`01-VALIDATION.md`'s wave rows and method rows are already corrected — verify, do not re-edit.**
    Red-team round 2 fixed six defects in that file directly (2026-08-20), so this task's job here is a
    **preservation** check, not a correction pass. What was fixed, and what must still read that way:
    - repo-claims now reads *"plan 05 (wave 2) → plan 07 (wave 3) → plan 10 (wave 5), asserted whole by
      plan 23 (wave 9)"* — it read "waves 2-6, asserted whole in wave 6".
    - renderer-components now reads **"Plan 22, wave 8"** — it read "Wave 6". (`test/renderer-components.test.cjs`
      is in plan **22**'s `files_modified`, not plan 21's — verified against every plan's frontmatter.)
    - db-fts now reads **"Plan 10, wave 5"** — it read "Wave 4".
    - hooks-notify now carries a wave at all — **"Plan 13, wave 6"**. It carried none.
    - FLOOR-12's accessible-name row now reads **"repo-fact (icon-only rule assertion)"** — it read
      "repo-fact (ratio assertion)", in direct opposition to `01-UI-SPEC.md:329-343`, which plan 14
      declares binding and which states a ratio test *would be wrong*.
    - the lint gate now reads **"After wave 8, add `npm run lint`"** — it read "After wave 6, add
      `npx eslint . --max-warnings 0`", ordering a lint gate two waves before plan 21 creates
      `eslint.config.js` and via an unpinned `npx` that would fetch ESLint from the registry mid-phase.
    - the db-fts row's *"budget the Node-ABI `better-sqlite3` rebuild step"* is **deleted**, and replaced
      by the standing prohibition. It was a third live copy of an instruction round 1 removed from plans
      01 and 10: 13.0.3 is N-API, ships eight prebuilds and declares no install script, so
      `npm rebuild better-sqlite3` discards the prebuild and forces `node-gyp` where CI pins Python on
      Linux only.

    Assert each is still true, and re-derive **every remaining** wave number in the file against the
    final plan set — the file was written before the plan set existed, so treat no wave number in it as
    trustworthy until re-checked against the owning plan's `wave:` frontmatter. Any further drift you
    find, correct here and list the correction.

    Then walk the Validation Sign-Off list **item by item** and tick each box only against named
    evidence — a test name, a pasted command output, or a recorded operator run. Seven boxes are
    unticked today and nothing else in the phase ticks them. An unticked box at the end of this task is
    a gap to report by name, not a box to leave silently unticked.

    Finally, set `nyquist_compliant: true` in `01-VALIDATION.md`'s frontmatter **only if** every box in
    that Sign-Off list is genuinely satisfied. If any is not, leave it false and say which. Set
    `status: signed-off` under the same condition. (`wave_0_complete` is not this task's — plan 01
    sets it in wave 1.)
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted D-46 gate output equal to `0`.
    - **The draft PR's checks are pasted, not a local run.** `gh pr view --json url,isDraft,baseRefName` prints `"baseRefName":"main"`, and `gh pr checks` lists a `pass` row for each of `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)` and `Electron smoke (ubuntu-latest)`. Paste the URL and the full table. **An empty table, "no checks reported", or a non-zero `gh pr checks` exit is a FAIL, not a pass** — it means nothing was triggered, and a phase gate that treats "no checks ran" as "no checks failed" is the exact hole this plan exists to close. Do not substitute a local `npm test` or `npm run e2e` for a missing row: `npm run e2e` does not run on this host at all (no `@playwright`, no native builds, `npm run build` not done).
    - Task notes contain the pasted `npm run lint` exit line, and the pasted command is literally `npm run lint` — **not** `npx eslint …`, which would fetch an unpinned ESLint from the registry instead of running plan 21's pinned local install.
    - **The full suite's TAP counters are pasted and inside the floor.** `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"` (written to `mktemp`, never the repo root — `.gitignore` has no `*.tap` and this plan's own containment sweep would see it) gives `EXIT=0`, **`# fail 0`**, **`# todo 0`**, **`# skipped` no greater than the platform's own baseline** — `4` on win32, `0` on ubuntu/macOS, because all four skips are `{ skip: !POSIX }` (`hive-hook-node`, `hive-runtime-path`, `hook-auth-roundtrip` ×2) and a fifth appears on any host lacking symlink privilege (`main-hardening.test.cjs:112`). Record which platform produced the run (the frozen baseline recorded at `01-VALIDATION.md`'s Test Infrastructure table ("Baseline at plan time") and again in its Sampling Rate section — 426 tests / 422 pass / 0 fail / 4 skip, re-measured 2026-08-20 post origin/main merge), and **`# pass` strictly greater than `422`**, because waves 2-8 add tests. Paste the whole counter block. `# skipped` **greater than 4 is a FAIL, not a note** — a `>=` skip clause is exactly the construction that lets a phase close green on skipped work. If skipped grew, name every new skip and STOP; do not raise the baseline.
    - **Plan 05's poisoned-assert probe re-run at wave 9 prints nothing.** Paste the command and its empty output. Any `SILENT-FAILURE:` line means a hand-rolled harness added this phase can fail while exiting `0` — STOP and name the file.
    - **Plan 06's hand-rolled harness enumeration re-run at wave 9 prints exactly 8 files**, and the same 8: `agent-provider`, `breaker`, `kg-core`, `proc-kill`, `realtime-findcard`, `slack`, `transcript-usage`, `voice-messages`. Paste the list, not just the count — a count of 8 can hide one conversion plus one addition. Fewer than 8 means a harness was converted; more means a new hand-rolled file was added and its registrations may be shadowed. Either is a STOP.
    - **Plan 08's and plan 10's per-file counters re-run at wave 9.** For each of `test/delivery-main.test.cjs` and `test/db-fts.test.cjs`: `TAP=$(mktemp); node --test --test-reporter=tap <file> > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"` gives `EXIT=0`, `# fail 0`, **`# skipped 0`**, `# todo 0`, and `# pass` at least `4` for `db-fts`. Also re-run plan 10's bypass grep: `grep -cE "\.skip\(|\.todo\(|skip:|todo:" test/db-fts.test.cjs` returns `0`. These are the two files where a skip would turn a missing runtime — the real SQLite handle, the main-process delivery path — into a green gate, and each was checked only in its own wave.
    - Task notes contain M1's output and the allowlist size, matching.
    - Both `continue-on-error` counts are pasted: `grep -cE "^[^#]*continue-on-error: *true" .github/workflows/ci.yml` returns exactly **`2`** (the effective gate — `:35` and `:117`), and `grep -c "continue-on-error" .github/workflows/ci.yml` returns no more than **`4`** (raw, which includes the two comment mentions at `:45` and `:113`). Both baselines measured 2026-08-20 against current source. The unit is stated here in full so this criterion cannot drift against plan 21's recorded number.
    - Task notes contain a criterion-by-criterion evidence table for all five ROADMAP success criteria, each citing a test name or a pasted command output.
    - Task notes confirm D-09's live Windows run is recorded with its three answers, and name the two outstanding live samples as outstanding.
    - `01-VALIDATION.md`'s `nyquist_compliant` is set to `true`, or left `false` with the unmet item named. `status` is set to `signed-off` under the same condition.
    - `01-VALIDATION.md`'s three stale wave rows are corrected to plan 05/07/10/23, plan 22 wave 8, and plan 10 wave 5 respectively — paste the diff.
    - **Preservation, not correction — all four already read `0` at plan time (2026-08-20), and any non-zero result means the round-2 fix was reverted.** Paste all four:
      `grep -c "ratio assertion" .planning/phases/01-finish-the-floor/01-VALIDATION.md` returns `0` (the row reads "icon-only rule assertion", matching `01-UI-SPEC.md:329-343`);
      `grep -c "After wave 6, add" .planning/phases/01-finish-the-floor/01-VALIDATION.md` returns `0` (the gate reads "After wave 8");
      `grep -cE "npx eslint" .planning/phases/01-finish-the-floor/01-VALIDATION.md` returns `3` and **every one of the three is prohibition prose, not an invocation** — paste all three lines and confirm none is a command the file tells anyone to run;
      `grep -c "rebuild step is budgeted" .planning/phases/01-finish-the-floor/01-VALIDATION.md` returns `1` — that is the round-2 **prohibition sentence** (`01-VALIDATION.md:147`, *"No `better-sqlite3` rebuild step is budgeted, and none may be added"*), and it must still be there. The earlier form of this criterion grepped the bare phrase `rebuild step` and required `0`, which the prohibition itself matched: the only way to make it green was to delete the sentence it was written to protect. Pair it with the check that actually catches a revert: `grep -c "npm rebuild better-sqlite3" .github/workflows/ci.yml` returns `0` (the value plans 01 and 10 both assert).
    - Every item in `01-VALIDATION.md`'s Validation Sign-Off list (the seven items under `## Validation Sign-Off`) is ticked with its named evidence beside it, or named as a gap. Locate the section by heading, not by line number — the file has been edited since this plan was written.
    - `grep -c "^- \[ \]" .planning/phases/01-finish-the-floor/01-VALIDATION.md` returns `0` — it is `13` today (six Wave 0 test-infrastructure items under `## Wave 0 Requirements` plus the seven Sign-Off items). Plan 01 ticks the two `test/load-ts.cjs` items it owns in wave 1; this task ticks the remaining four Wave 0 items — `repo-claims` (plan 05), `renderer-components` (plan **22**, not 21), `db-fts` (plan 10), `hooks-notify` (plan 13) — and all seven Sign-Off items. Any item left unticked is listed in the task notes as an explicit gap with the reason.
    - Task notes contain the bounded doc-claim sweep's full output: every hit across the six listed surfaces with a TRUE/FALSE verdict, and every FALSE fixed in this plan or escalated. Any hit outside the six surfaces is named as carried forward.
    - **The ADR pointer sweep is non-empty and every hit resolves.** `grep -rhoE "adr/[0-9]{4}[0-9a-z-]*" src/ | sort -u` is pasted; `ls docs/adr/<hit>*.md` succeeds for every hit; and `grep -rlE "adr/000[56]" src/ | sort` lists **all four** contracted files — `src/main/db.ts`, `src/main/telemetry.ts`, `src/renderer/src/components/terminalPool.ts`, `src/renderer/src/store/terminalPoolPolicy.ts`. **An empty sweep is a FAIL.** It is empty today (0 `adr/` strings in `src/`, verified 2026-08-20), so a green-on-empty result means plans 04 and 05 never landed their pointers — not that they all resolve. Do not key the sweep on `docs/adr/....md`: plans 04 `:293` and 05 `:205` mandate the bare `adr/0005` / `adr/0006` substring, which that pattern does not match.
    - **The five unowned doc surfaces are corrected, each with its own executable check. Paste every command and its output.** Baselines are today's measured values (2026-08-20, pre-execution):
      - `grep -cE "Electron 32|\^32\.2\.0" .planning/codebase/STACK.md` returns `0`. Baseline `2` — `grep -c` counts matching *lines*, and `:18` carries both strings on one line, so 2 lines cover all three claims.
      - `grep -cE "Electron 32|\^32\.2\.0|bump never happened" .planning/codebase/CONCERNS.md` returns `0`. Baseline `3`.
      - `grep -c "Electron 32" .planning/PROJECT.md` returns `0`. Baseline `1`.
      - `grep -c "Electron 32" .planning/codebase/INTEGRATIONS.md` returns `0`. Baseline `1`. Beside it, `git diff --stat .planning/codebase/INTEGRATIONS.md` shows this task changed **one** line and it is not `:116` — plan 02 owns that line and the two edits must not collide.
      - `grep -c "No linter configured" .planning/codebase/CONVENTIONS.md` returns `0`. Baseline `1`.
      - `grep -c "Seven \`react-hooks/exhaustive-deps\`" .planning/codebase/CONVENTIONS.md` returns `0`. Baseline `1` — and the sentence listed **nine** files, so this one was already false before the phase started.
      - The rewritten `CONVENTIONS.md` Linting counts are re-derived, not guessed: paste `grep -rc "react-hooks/exhaustive-deps" src/ | awk -F: '{n+=$2} END {print n}'`, the same for `@typescript-eslint`, and `grep -rc "eslint-disable" src/ | awk -F: '{n+=$2} END {print n}'` — and confirm every number written into the file equals the number the grep returned in this task. (Pre-phase baselines were 9, 4 and 13; plan 21 changes all three, so the pre-phase numbers are **not** the answer.)
      - Every rewritten Electron version string equals what the tree actually pins: paste `node -e "console.log(require('./package.json').devDependencies.electron||require('./package.json').dependencies.electron)"` and confirm each of the five files' new text agrees with it. A doc-claim sweep that replaces one wrong version with another wrong version is the same defect.
    - **The GATE-01 deny gate covers all four protected paths, not just `bin/`.** Round 2 widened plan 02's PreToolUse gate from `<hiveRoot>/bin` to `bin` + `.git` + the socket + other agents' directories, but wrote the widening only into narrative — round 3 measured **zero** criteria in plans 02, 06 or 23 naming `.git`, `sockPath`, `hooks.sock`, `agents/` or `realpath`, so a `bin/`-only implementation passed every gate in the phase including this one. Assert the shape here as a backstop to plan 02's behavioural table, and paste each: in `src/main/hooks.ts`, `grep -c "'\.git'"` ≥ `1`, `grep -c "'agents'"` ≥ `1`, `grep -c "realpath"` ≥ `1` (symlink hop — `path.resolve` does not dereference links, so `ln -s "$HIVE_ROOT/bin" /tmp/b` then writing `/tmp/b/cth-hook.cjs` defeats a resolve-only gate), `grep -c "HIVE_ROOT"` ≥ `1` (the literal-variable path — the app puts `HIVE_ROOT` in every agent's env and names it in the injected prompt at `hive.ts:1366`, so `cat >> "$HIVE_ROOT/bin/cth-hook.cjs"` is the documented calling convention, not obfuscation), and `grep -c "sockPath()"` ≥ `3` (baseline `2`). All five baselines are `0` except `sockPath()`; paste before and after. **If any is `0`, GATE-01 is NOT whole** — the gate protects one of four entrances — STOP and report, naming plan 02 task 2.
    - **All six shim templates carry the token, and three of them never did.** Measured 2026-08-21 at HEAD: `for t in HOOK_SHIM AGY_HOOK_SHIM PI_EXTENSION OPENCODE_PLUGIN PROXY_BRIDGE_SHIM GROK_HOOK_SHIM; do sed -n "/^const $t = \`/,/^\`;/p" src/main/hive.ts | grep -c "HIVE_SOCK_TOKEN"; done` prints `1 1 0 0 0 1`, and the same loop for `sock_token` prints `1 1 0 0 0 1`. **`PI_EXTENSION`, `OPENCODE_PLUGIN` and `PROXY_BRIDGE_SHIM` are therefore already dead-hooked at HEAD — a pre-existing defect this phase inherits, NOT one GATE-01 introduces.** Do not misattribute it in the SUMMARY. Plan 06 task 4 lands all three shim bodies; after it, both loops must print `1 1 1 1 1 1`. Paste both loops. Anything less than six means an engine posts hook payloads that `authorized()` drops — silently, with no live status, no Stop→drain and no cost rows — while GATE-01 reads closed. `hooks.ts:139-157` installs exactly one connection handler and `:151` is the only door into `handle()`, so there is no unauthenticated side path for these engines to be fine on.
    - **Both composition-root seams are fed — the two T-INDEX handoffs actually landed.** `src/main/index.ts` is single-owner-per-wave, so two requirements had to cross a wave boundary as named handoffs, and a handoff that is written but never applied leaves a requirement closed on paper with dead code underneath. Assert both, paste both:
      `grep -c "recordCostSample" src/main/index.ts` returns at least `1` (FLOOR-09 — plan 06 minted the sink in wave 3, plan 08 injected it in wave 4), and `grep -c "budgetForAgent" src/main/index.ts` returns at least `1` (FLOOR-10 — plan 09 minted the accessor in wave 4, plan 10 fed the beat in wave 5). Baselines today are `0` and `0`, so both sides fail against current source.
      Then confirm the SUMMARYs closed the windows they opened: `01-08-SUMMARY.md` contains `FLOOR-09 PRODUCTION INJECTION LANDED` and `01-10-SUMMARY.md` contains `FLOOR-10 BUDGET ARM FED IN PRODUCTION`. If a grep is `0`, the requirement is NOT done regardless of what its own plan's SUMMARY says — STOP and report, naming plan 08 task 6 or plan 10 task 5.
      Pin both in `test/repo-claims.test.cjs` so a later refactor cannot quietly unfeed either seam, with messages naming the consequence: *"proxy-tier spend stops reaching getAgentUsage — the budget cap becomes a false cap"* and *"BreakerInput.budget becomes an optional field nothing ever sets — the FLOOR-10 arm is dead code behind a green suite"*.
    - **`CONVENTIONS.md`'s export claim is false today and must not survive the sweep.** `.planning/codebase/CONVENTIONS.md:101` states *"React components use default exports in `.tsx` files per convention"*. Measured 2026-08-21: `grep -rlc "export default" src/renderer/src --include=*.tsx | wc -l` returns `0` — **zero of 63** renderer components use a default export; every one is a named export. This is not a claim the phase makes false, it is one that was already false, in a document downstream agents are told to read as canonical — and plan 22's executor would follow it into expecting `.default` on every import and getting `undefined`. Assert `grep -c "React components use default exports" .planning/codebase/CONVENTIONS.md` returns `0` (baseline `1` — paste both) and that the replacement names the real convention. While correcting it, re-derive the two counts in the same section rather than trusting them: the file's "Seven `react-hooks/exhaustive-deps` disables" is also wrong (there are nine, and `:35` itself lists nine files).
    - **GATE-01 is whole, not just started.** Plan 02 (wave 2) deletes the floor-wide token and hands the qwen proxy sidecar's two-line fix to plan 06 (wave 3), which means GATE-01 is deliberately incomplete at the end of wave 2 and a phase closed before plan 06's task 4 would ship a false claim. Assert both halves and paste both: `grep -c "process.env.HIVE_SOCK_TOKEN *=" src/main/index.ts` returns `0` (the floor-wide assignment is gone), and `sed -n '/private startProxyBridge/,/^  }$/p' src/main/hive.ts | grep -c "HIVE_SOCK_TOKEN"` returns `1` with that line **not** reading `process.env.HIVE_SOCK_TOKEN`. If the second is `0`, GATE-01 is NOT done — STOP and report, naming plan 06 task 4. Baselines today: `1` and `0` respectively, so both sides of this criterion fail against current source.
  </acceptance_criteria>
  <verify>
    <automated>TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; RC=$?; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"; exit $RC</automated>
  </verify>
  <done>The mechanical gate returns zero, the draft PR's checks are green on all three platforms with the TAP counters inside their floor, the phase's own fake-coverage guards still hold at wave 9, and all five success criteria have named evidence rather than a claim.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator sign-off on the phase evidence</name>
  <read_first>
    - Task 3's criterion-by-criterion evidence table, its pasted TAP counters, and its `gh pr checks` table.
    - Task 2's four adversarial verdicts and any grep/verdict disagreements.
    - `.planning/phases/01-finish-the-floor/01-01-SUMMARY.md` — D-09's live Windows run answers.
    - `.planning/codebase/CONCERNS.md` § Method — the recorded 2026-08-20 error this checkpoint exists to prevent repeating.
  </read_first>
  <acceptance_criteria>
    - The SUMMARY records five explicit answers: evidence table sound yes/no, D-46 gate at zero yes/no, TAP counters inside their floor on a non-empty draft-PR checks table yes/no, D-09 recorded yes/no, spot-checked issue comment passed yes/no.
    - The spot-checked issue is named, and its comment is confirmed to carry verbatim acceptance text, per-clause commands with real output, a named test, and an `npm test` exit line.
    - Any criterion whose cited evidence is a sentence rather than a test name or a pasted command output is named as a gap.
  </acceptance_criteria>
  <what-built>
    The phase's closing evidence: `test/repo-claims.test.cjs` asserting every negative grep the phase
    relies on, the four previously mis-assessed issues re-verified from fresh context, the D-46
    mechanical gate at zero, and a criterion-by-criterion evidence table.
  </what-built>
  <how-to-verify>
    The operator's standing bar is that an agent reporting "fixed" is a claim, not evidence — and that
    bar has already caught four over-claims on this repo. This checkpoint is where a human reads the
    evidence rather than the report.

    1. Read the criterion-by-criterion table in task 3's notes. For each of the five ROADMAP success
       criteria, confirm the cited evidence is a test name or a pasted command output, not a sentence.
    2. Confirm the D-46 gate output pasted is literally `0` and that
       `gh issue list --state open --label floor-inspection` shows only #47, #48, #49 and #73.
    2b. Confirm the suite's TAP counters are pasted and inside their floor — `# fail 0`, `# todo 0`,
       `# skipped` no greater than **4**, `# pass` greater than **422**. A green `gh pr checks` table
       proves exit `0`, and `node --test` exits `0` on skipped tests, so the counters are the only
       thing that proves the phase's new tests actually ran. Confirm too that the checks table is the
       draft PR's and is **not empty** — `ci.yml` and `e2e.yml` fire on nothing else this branch does,
       so "no checks reported" means nothing ran, which is a failure, not a pass.
    3. Confirm D-09's live Windows run is recorded in `01-01-SUMMARY.md` with its three answers. If it
       is not, FLOOR-03 is not closed regardless of how green CI is.
    4. Confirm the two roadmap corrections are stated plainly: criterion 2 reads as Electron 43.x
       (D-02), and criterion 1's "dead Stop-drain" premise was wrong (D-37).
    5. Confirm the two outstanding live samples are listed as outstanding — FLOOR-06's
       `gh attestation verify` after the next tag, and macOS notification delivery — and not claimed.
    6. Spot-check one closed issue's comment and confirm it carries verbatim acceptance text, per-clause
       commands with real output, a named test, and an `npm test` exit line.

    Report: evidence table sound yes/no, gate at zero yes/no, TAP counters inside floor on a non-empty
    PR checks table yes/no, D-09 recorded yes/no, spot-check passed yes/no.
  </how-to-verify>
  <files>(none — operator sign-off)</files>
  <action>Pause execution and present the seven numbered checks below to the operator verbatim, together with task 3's criterion-by-criterion evidence table and task 2's four adversarial verdicts. Do not mark the phase complete and do not write the SUMMARY until the four answers are returned. An agent reporting "fixed" is a claim, not evidence — this checkpoint is where a human reads the evidence rather than the report.</action>
  <verify><human-check>Operator returns five yes/no answers: evidence table sound, D-46 gate at zero, TAP counters inside floor on a non-empty PR checks table, D-09 recorded, spot-check passed.</human-check></verify>
  <done>The operator has read the phase evidence and the five answers are recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" with the five yes/no answers, or name the gap.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| an agent's completion report → the project's record of what is true | This is the boundary the whole phase exists to harden. A SUMMARY is a claim; a passing test on three platforms and a pasted command output are evidence. |
| GitHub issue state → the phase's own progress signal | A fixed-but-open issue is invisible to the phase; a closed-but-unfixed issue is a false record that outlives the phase. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P23-01 | Repudiation | closing an issue against "some code exists" rather than its stated done | mitigate | D-42's per-clause bar plus D-43's closing-comment contents; every clause needs a pasted command output, and an empty grep is itself the evidence. |
| T-P23-02 | Repudiation | trusting a subagent verdict as verification | mitigate | D-47's adversarial re-verify is an **addition** to the mechanical bar, never a substitute; where a verdict and a grep disagree, the grep wins and the disagreement is recorded. |
| T-P23-03 | Repudiation | a negative grep that lives only in a SUMMARY and rots | mitigate | D-45 — every one becomes a test in `test/repo-claims.test.cjs`, running on three platforms on every future PR. |
| T-P23-04 | Tampering | silently widening the FLOOR-12 exemption by adding a new sub-14px site | mitigate | The allowlist is a literal `{ file, text, count }` multiset in the test compared for exact equality against what M1 returns, so a new site either adds an unlisted key or bumps a count and fails the suite. Probe (b) in the acceptance criteria proves this by mutation. |
| T-P23-09 | Repudiation | the wave-9 allowlist breaking on wave 8's line shifts, and an executor "fixing" it by editing line numbers until the suite goes green | mitigate | The two-plan line-anchor contract in `<interfaces>`. Half 2 removes line numbers from the assertion entirely; half 1 (plan `01-21`) keeps the matched *text* stable and is confirmed from `01-21-SUMMARY.md` before the allowlist is trusted. Probe (a) proves half 2 by mutation. A text that no longer exists is an escalation against plan 21, never an allowlist edit. |
| T-P23-05 | Tampering | a grep assertion self-invalidated by a comment quoting what was deleted | mitigate | `stripComments` runs before every source grep — several Phase 1 fixes deliberately add such a comment. |
| T-P23-06 | Repudiation | marking FLOOR-03 closed on a CI link | mitigate | D-08/D-09: the checkpoint requires D-09's live Windows run to be recorded with its three answers; the unit suite is structurally incapable of catching an Electron-version regression. |
| T-P23-07 | Repudiation | claiming a live sample that cannot be run on this project | mitigate | FLOOR-06's attestation verify and macOS notification delivery are recorded as **outstanding**, with the reason, rather than claimed. |
| T-P23-08 | Repudiation | closing the phase with `nyquist_compliant: false` still in VALIDATION.md | mitigate | Task 3 sets it true only if every sign-off box is genuinely satisfied, and names the unmet item otherwise. |
| T-P23-10 | Tampering | closing the phase green on skipped work — every test waves 2-8 add is `{skip: '...'}`, and `node --test` counts skips in its total and exits `0` | mitigate | Task 3 asserts the full suite's TAP counters against a frozen baseline (`# fail 0`, `# skipped` <= 4, `# pass` > 422) and **re-runs all four of the phase's fake-coverage guards at wave 9** — plan 05's poisoned-assert probe, plan 06's eight-file harness enumeration, and plans 08/10's per-file `# skipped 0` counters. A guard run once in wave 2 does not hold a phase closed in wave 9, and the only other pass-count floor in the phase (plan 01, wave 1) uses `>=` on skips, which permits skip growth by construction. |
| T-P23-11 | Repudiation | "CI green on three platforms" pointing at nothing — `ci.yml` and `e2e.yml` fire only on `push`/`pull_request` to `main`, so a phase-branch push triggers no run at all | mitigate | The phase's CI evidence is plan 01's draft PR to `main`, read with `gh pr checks`. An empty checks table, "no checks reported", or a non-zero exit is an explicit FAIL — never "nothing failed". `npm run e2e` is not a substitute: it does not run on this host (no `@playwright`, no native builds, no `npm run build`). |
| T-P23-12 | Repudiation | a vacuous sweep passing over an empty result set — the ADR-pointer check, and the doc-claim sweep over surfaces no plan owns | mitigate | The ADR sweep is keyed on the `adr/[0-9]{4}` format plans 04/05 actually mandate (not `docs/adr/....md`, which matches nothing today) and **must be non-empty**, naming all four contracted files. The five doc surfaces this phase falsifies are in this plan's `files_modified` with per-file before/after grep baselines, because no other plan declared any of them. |
</threat_model>

<verification>
- Both line-anchor contract probes pass: (a) suite still green after a line is inserted above an allowlisted site, (b) suite goes red when a new sub-14px site is added.
- Plan `01-21`'s two containment filters are recorded empty in `01-21-SUMMARY.md`.
- `npm test` green on ubuntu, windows and macos via the **draft PR's** checks (`ci.yml` and `e2e.yml` fire on no other event this phase produces), with the PR URL recorded and the full `gh pr checks` table pasted. An empty checks table fails this.
- `npm run typecheck` and `npm run lint` (the local install — never bare `npx eslint`) green locally, and the draft PR's `Typecheck`, three `Test (os)` and `Electron smoke (ubuntu-latest)` check rows all `pass` via `gh pr checks`. `npm run e2e` is NOT a local gate — it does not run on this host (no `@playwright`, no native builds).
- D-46's `gh` query returns `0`.
- No `continue-on-error` was added to `ci.yml` anywhere in the phase: effective count (`grep -cE "^[^#]*continue-on-error: *true"`) is exactly `2`, raw `grep -c` is at most `4`.
- The operator checkpoint's four answers recorded.
</verification>

<success_criteria>
- ROADMAP criterion 5's final clause is TRUE: `gh issue list --state open --label floor-inspection`
  returns only the four epics.
- All five ROADMAP success criteria have named, pasted evidence — with criterion 2 read as D-02 restates
  it and criterion 1's premise corrected per D-37.
- The phase's own premise holds: every claim the project makes about itself is true, and the greps that
  prove it run in the gate rather than living in a report.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-23-SUMMARY.md` when done. It MUST contain the
criterion-by-criterion evidence table, the D-46 gate output, the full-suite TAP counter block, the
draft PR's URL and its full `gh pr checks` table, all four re-run fake-coverage guards (plan 05's
poisoned-assert probe, plan 06's eight-file harness enumeration, plan 08's and plan 10's per-file
`# skipped 0` counters), the four adversarial verdicts with any grep/verdict disagreements, the final
content-keyed allowlist with its summed count matching M1, both line-anchor contract probe results,
the wave-7-SUMMARY-to-wave-9 allowlist reconciliation, the five doc-surface corrections with their
before/after grep counts, every fixed-but-open issue named with the plan that should have closed it,
the two outstanding live samples, and the five checkpoint answers.
</output>
