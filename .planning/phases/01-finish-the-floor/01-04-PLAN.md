---
phase: 01-finish-the-floor
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - .github/workflows/release.yml
  - .github/ISSUE_TEMPLATE/bug_report.yml
  - README.md
  - RELEASE.md
  - SECURITY.md                  # TWO distinct edits: the SmartScreen sentence (FLOOR-06, task 2) and the hook-token paragraph at :34-39 (GATE-01, task 4 — carried here because this plan owns the file in this wave)
  - CONTRIBUTING.md
  - docs/adr/0005-cumulative-cost-ledger.md
  - docs/adr/0006-terminal-pool-lifetime.md
  - docs/adr/README.md
  - test/ci-config.test.cjs
  - src/main/db.ts               # one-line ADR-0005 pointer comment only
  - src/main/telemetry.ts        # one-line ADR-0005 pointer comment only
autonomous: true
requirements: [FLOOR-06, FLOOR-17]

must_haves:
  truths:
    - "A downloaded release artifact can be traced to this repo and this commit via actions/attest-build-provenance plus published checksums"
    - "The docs say plainly that provenance does NOT suppress SmartScreen"
    - "The bug template asks only for logs a user can actually reach"
    - "docs/adr/ holds the rationale currently buried in long source comments, with the source comments linked rather than deleted"
    - "CONTRIBUTING.md's Windows-gate paragraph matches ci.yml, and test/ci-config.test.cjs pins it so it cannot silently rot back"
    - "SECURITY.md's hook-server paragraph (:34-39) describes the PER-AGENT tokens plan 02 lands in this same wave, not the floor-wide secret they replace. The requirement is GATE-01 (plan 02's); the edit lands here because this plan owns SECURITY.md this wave and two writers on one file in one wave is a lost update"
  artifacts:
    - path: ".github/workflows/release.yml"
      provides: "attestation step with job-level contents/id-token/attestations permissions"
      contains: "attest-build-provenance"
    - path: "docs/adr/0005-cumulative-cost-ledger.md"
      provides: "the ledger row-semantics contract RECORD-03/04 exist because it was violated"
    - path: "test/ci-config.test.cjs"
      provides: "YAML-parsed assertions on the attestation step, its permissions and its ordering, plus the repo-fact pin on SECURITY.md's per-agent hook-token paragraph"
      contains: "attestations"
    - path: "SECURITY.md"
      provides: "the SmartScreen honesty sentence (FLOOR-06) AND the per-agent hook-token correction carried for GATE-01"
      contains: "per-agent"
  key_links:
    - from: ".github/workflows/release.yml publish job"
      to: "release/SHA256SUMS.txt"
      via: "subject-checksums input attesting every artifact named in the merged checksums file"
      pattern: "subject-checksums"
    - from: ".github/ISSUE_TEMPLATE/bug_report.yml"
      to: "Settings → open logs (plan 10, wave 5)"
      via: "the template's Logs ask becomes answerable"
      pattern: "[Ll]ogs"
    - from: "SECURITY.md:34-39 hook-server paragraph"
      to: "01-02-PLAN.md's per-agent token registry (GATE-01, this same wave)"
      via: "CROSS-PLAN — plan 02 builds it and owns the other three doc surfaces; this plan owns SECURITY.md, so the fourth lands here"
      pattern: "HIVE_SOCK_TOKEN"
---

<objective>
Finish release provenance (the one missing clause of three), state the SmartScreen limitation
plainly, make the bug template ask only for what a user can reach, and move two pieces of
ADR-shaped rationale out of long source comments into `docs/adr/`.

Purpose: FLOOR-06 and FLOOR-17. Two of FLOOR-06's three clauses already ship — published checksums
and the release-link gate. Only the Sigstore attestation is missing, plus the honesty sentence that
REQUIREMENTS.md states is part of the requirement, not a nicety. FLOOR-17's ADR home exists; what is
missing is the migration of rationale, and a bug template that asks for logs the user cannot reach.

One thing here is NOT FLOOR-06/FLOOR-17 work and is labelled as such throughout: `SECURITY.md`'s
hook-server paragraph (`:34-39`) still describes ONE floor-wide hook secret, which `01-02-PLAN.md`
replaces with per-agent tokens in this same wave. The requirement is GATE-01 and belongs to plan 02,
which owns the other three stale doc surfaces (`HIVE.md`, `ARCHITECTURE.md`, `INTEGRATIONS.md`) — but
`SECURITY.md` is declared in THIS plan's `files_modified`, and two plans editing one file in one wave
in one working tree is a lost update. So the fourth surface lands here, in task 4, credited to
GATE-01. Left undone, the phase ships a false statement about its own trust boundary.

Output: one workflow step with correct permissions, three doc honesty edits, two new ADRs, one
carried GATE-01 doc correction, and repo-fact assertions in the file that already pins CI config.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@docs/adr/0002-prompt-cache-invariant.md

<interfaces>
`actions/attest-build-provenance@v4` (action.yml read this session): accepts exactly one of
`subject-path` | `subject-digest` | `subject-checksums`. `subject-checksums` takes a path to a
sha256sum-format file — which the `publish` job's existing "Flatten + merge checksums" step already
produces as `release/SHA256SUMS.txt`.

`release.yml` today: a `links` job runs `npm run check:links`; `build` declares `needs: links`;
`publish` needs `build`, downloads artifacts, flattens and merges the three per-platform
`SHA256SUMS-<os>.txt`, then uploads via `softprops/action-gh-release`. Workflow-level
`permissions: contents: write`.

Verification command for the docs: `gh attestation verify <artifact> --repo MARKXAILABS/hello-markx`.

`docs/adr/` currently holds `0001-one-gate-for-pty-writes.md`, `0002-prompt-cache-invariant.md`,
`0003-fail-safe-worktree-gc.md`, `0004-single-committer-git.md`, `README.md`. Numbering rule
(`CONVENTIONS.md:85`): never renumber; a reversed decision gets a new record, not an edit.

`SECURITY.md:34-39` (re-read verbatim this session, post-merge): "- **The hook server** is how each
agent's lifecycle shims report in. Every payload must carry the process-local token
`HIVE_SOCK_TOKEN` (`hookSockToken()` in `hooks.ts`), minted fresh at each app start and injected only
into agent child environments. A payload without it is dropped at the socket boundary. This stops any
other local process from forging agent events; it is not a defence against a process that can already
read this app's child environments." The LAST sentence stays true after GATE-01 and is the model for
the ceiling; everything before it describes the design plan 02 deletes. `SECURITY.md:31-32` states
this file's own rule — "Symbols rather than line numbers on purpose — stale line numbers are how this
document went wrong the first time" — which the replacement prose must follow.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-06 and FLOOR-17 clause against current source and paste the output</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `.github/workflows/release.yml` in full — the `links` job, `build`'s `needs:`, the checksum generation and merge steps, the workflow-level `permissions:` block, and the `::warning::` on the unsigned Windows path.
    - `.github/ISSUE_TEMPLATE/bug_report.yml` in full.
    - `CONTRIBUTING.md:82-101` — the "Before you open a PR" section. **Measured at plan time it is already correct**: it states all three platforms are hard gates, that there is no `continue-on-error` anywhere in the test matrix, that `test:focused` is never a PR gate, and it triages the old "11 failures" baseline to the 7 real Windows source bugs (#57/#58/#60). Read it to confirm, not to replace.
    - `docs/adr/README.md` and `docs/adr/0002-prompt-cache-invariant.md` — the numbering rule and the file:line-pointer-plus-depth pattern.
    - `test/ci-config.test.cjs` in full — the YAML-parse-and-assert idiom and its header convention.
    - `.planning/REQUIREMENTS.md` FLOOR-06 (with its deliberate scope change) and FLOOR-17.
  </read_first>
  <action>
    Quote issue #15's and issue #41's acceptance text verbatim and run one command per clause with
    ACTUAL output pasted.

    Required, because RESEARCH classifies two of three FLOOR-06 clauses as ALREADY SATISFIED and an
    executor told to "implement" them will rebuild working CI:
    - `grep -n "sha256sum\|shasum\|SHA256SUMS" .github/workflows/release.yml` — the published-checksums clause. Expect hits.
    - `grep -n "check:links\|needs: links" .github/workflows/release.yml` — the release-link gate clause. Expect hits.
    - `grep -n "attest" .github/workflows/release.yml` — the provenance clause. Expect EMPTY. An empty grep IS the evidence (D-43).
    - `grep -rn "SmartScreen" README.md RELEASE.md SECURITY.md .github/workflows/release.yml` — where the honesty sentence exists today and where it does not.
    - `grep -n -i "logs" .github/ISSUE_TEMPLATE/bug_report.yml` — what the template asks for.
    - `grep -c "11 tests" CONTRIBUTING.md` — **returns `0` today.** The stale *"11 tests fail on Windows, non-blocking"* paragraph is already gone; this grep is a re-confirmation, not a finding. Do not use it as a completion criterion — it passes before any work is done.

    Also re-verify `actions/attest-build-provenance`'s current major tag and that `subject-checksums`
    is still an accepted input (`gh api repos/actions/attest-build-provenance/tags` or read the
    action's `action.yml`). RESEARCH is only valid to 2026-09-03.

    Record per clause: OPEN or ALREADY-SATISFIED, with the pasted evidence line.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all six greps above.
    - The `grep -n "attest" .github/workflows/release.yml` output is pasted and empty, establishing the one genuinely open FLOOR-06 clause.
    - Task notes classify each of FLOOR-06's three clauses and each of FLOOR-17's two clauses as OPEN or ALREADY-SATISFIED.
    - `node --test test/ci-config.test.cjs` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ci-config.test.cjs</automated>
  </verify>
  <done>Each clause is classified with pasted evidence; the two already-satisfied FLOOR-06 clauses are explicitly NOT scheduled for rebuild.</done>
</task>

<task type="auto">
  <name>Task 2: Add the attestation step with correct job-level permissions, and state the SmartScreen limitation in all three docs</name>
  <files>.github/workflows/release.yml, README.md, RELEASE.md, SECURITY.md</files>
  <read_first>
    - `.github/workflows/release.yml` in full — the file being modified; note the exact position of the "Flatten + merge checksums" step and the `softprops/action-gh-release` step.
    - `01-RESEARCH.md` § "FLOOR-06 — one step, and a doc sentence" and § "Code Examples 3" — the literal step shape and the two things to get right.
    - `README.md` / `RELEASE.md` / `SECURITY.md` download-link sections — where the honesty sentence must sit.
    - `.planning/PROJECT.md` — the zero-recurring-cost constraint that made this scope change deliberate.
  </read_first>
  <action>
    In the `publish` job of `.github/workflows/release.yml`, add a job-level `permissions:` block with
    `contents: write`, `id-token: write` and `attestations: write`. **`contents: write` must be
    restated here** — a job-level permissions block REPLACES the workflow-level one, and omitting it
    silently breaks the release upload.

    Add a step named "Attest build provenance" using `actions/attest-build-provenance` at the major
    tag confirmed in task 1, with `subject-checksums: release/SHA256SUMS.txt`. Place it **after** the
    flatten-and-merge step and **before** `softprops/action-gh-release`, so the attested digests are
    the bytes that actually ship. Carry a comment naming FLOOR-06, the reason `subject-checksums` was
    chosen (the merged checksums file already exists — one call attests every artifact), and the
    verification command.

    Do not restructure the workflow, do not touch the `links` gate, do not touch the checksum
    generation steps — both of those clauses already ship.

    Then add the honesty sentence to `README.md`, `RELEASE.md` and `SECURITY.md`, next to the download
    links in each. It must say plainly: provenance plus checksums prove the artifact came from this
    repo and this commit; they do **not** suppress SmartScreen, because paid code signing is out on
    this project's zero-recurring-cost constraint. Include the verification command
    `gh attestation verify <artifact> --repo MARKXAILABS/hello-markx` so the claim is actionable.
    REQUIREMENTS.md states this sentence is part of the requirement, not a nicety.
  </action>
  <acceptance_criteria>
    - `grep -c "attest-build-provenance" .github/workflows/release.yml` returns `1`.
    - `grep -c "subject-checksums" .github/workflows/release.yml` returns `1`.
    - `grep -c "id-token: write" .github/workflows/release.yml` returns at least `1`, and `attestations: write` and a job-level `contents: write` are present in the same block.
    - `node -e "const y=require('js-yaml')" 2>/dev/null; python -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release.yml'))"` — the workflow still parses as valid YAML (use whichever parser is available; `test/ci-config.test.cjs` already parses workflows, so mirror its method).
    - `grep -c -i "smartscreen" README.md` , `RELEASE.md` and `SECURITY.md` each return at least `1`.
    - `grep -c "gh attestation verify" README.md RELEASE.md SECURITY.md` returns at least `1` in total.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ci-config.test.cjs</automated>
  </verify>
  <done>One attestation step ships with the right permissions in the right position, and all three docs state the SmartScreen limitation with a runnable verification command.</done>
</task>

<task type="auto">
  <name>Task 3: Fix the bug template and CONTRIBUTING.md, write two ADRs, and pin all of it in test/ci-config.test.cjs</name>
  <files>.github/ISSUE_TEMPLATE/bug_report.yml, CONTRIBUTING.md, docs/adr/0005-cumulative-cost-ledger.md, docs/adr/0006-terminal-pool-lifetime.md, docs/adr/README.md, test/ci-config.test.cjs</files>
  <read_first>
    - `.github/ISSUE_TEMPLATE/bug_report.yml` and `feature_request.yml` — the field shapes to match.
    - `CONTRIBUTING.md:82-101` and `.github/workflows/ci.yml` in full — the doc and the source of truth it must match, clause by clause.
    - `docs/adr/0002-prompt-cache-invariant.md` in full — the ADR shape, and specifically the file:line-pointer-at-the-call-site plus depth-in-the-ADR pattern.
    - `docs/adr/README.md` — the numbering rule.
    - `src/main/db.ts:33-50` and `src/main/telemetry.ts:11` — the cumulative-snapshot contract that ADR 0005 records.
    - `src/renderer/src/components/terminalPool.ts:1-16` and `src/renderer/src/store/terminalPoolPolicy.ts:1-19` — the two bounding rules ADR 0006 records; the policy file's header is already ADR prose.
    - `test/ci-config.test.cjs` in full — the file being modified.
  </read_first>
  <action>
    Bug template: the template asks for "Logs". Until plan 10's Settings button ships — **wave 5,
    three waves after this plan** — that ask is unanswerable, and pointing the template at a button
    that does not exist yet would ship, transiently, the exact false-claim class this plan exists to
    remove. So **write the wording conditionally**: give the Logs field a description that is true
    today (the platform log paths, so the user can find the folder by hand) **and** name the Settings
    route as the shortcut, marked as arriving with FLOOR-05. Plan 10 owns
    `.github/ISSUE_TEMPLATE/bug_report.yml`'s final wording — it drops the by-hand paths and the
    "arriving" marker once its button ships. This plan does **not** declare a `depends_on` for that;
    it is a forward dependency, declared here in prose and discharged in plan 10's task notes. Also check every other field for asks that reference something the user
    cannot produce, and fix or remove them. Do not add fields.

    CONTRIBUTING.md: **verify and pin — do not rewrite.** Measured at plan time the stale
    *"11 tests fail on Windows, non-blocking"* paragraph is already gone (`grep -c "11 tests"` returns
    `0`) and `:82-101` already states all three of the replacement facts verbatim: all three platforms
    are hard gates, *"there is no `continue-on-error` anywhere in the test matrix, and please do not add
    one"*, and `test:focused` is *"never the thing you gate a PR on"* — plus a correct triage of the old
    baseline to the 7 real Windows source bugs (#57/#58/#60). **Deleting and replacing that paragraph
    would destroy correct, informative prose.**

    So: read `:82-101` against `ci.yml` clause by clause and confirm each of the three claims still
    holds. Note that `ci.yml` carries `continue-on-error` at `:35` (npm-audit) and `:117` (native
    rebuild) — **both outside the `test` matrix**, so the doc's "anywhere in the test matrix" wording is
    accurate and must not be broadened to "anywhere in ci.yml". Correct only a clause the read shows to
    be false, and report any such correction with the `ci.yml` line that disproves it. Then pin the
    paragraph in `test/ci-config.test.cjs` so it cannot rot back: parse `ci.yml`, assert no job inside
    the `test` matrix carries `continue-on-error`, and assert `CONTRIBUTING.md` still contains the
    hard-gate sentence. A doc claiming a non-blocking gate that is in fact blocking is exactly the class
    of false self-claim this phase exists to remove — and a *test* is what keeps it from returning.

    Write two ADRs, numbered 0005 and 0006, following `0002`'s shape:
    - **0005 — the cost ledger holds cumulative snapshots.** Why rows are cumulative per agent per
      beat, how to derive velocity (diff consecutive rows), and why the contract matters: RECORD-03
      and RECORD-04 exist because it was violated by a second appender writing per-response deltas
      into the same file. Point at `src/main/db.ts:44` and `src/main/telemetry.ts:11`.
    - **0006 — the terminal pool is one Terminal per pty for the app's lifetime.** The two bounding
      rules (`TERMINAL_POOL_MAX` LRU eviction of detached entries; the roster orphan sweep), and the
      recorded reason the per-call-site dispose approach failed: the call sites did not agree with each
      other and the next one added would not have either.

    **Do not delete the source comments those ADRs come from — link them.** Add a one-line
    file:line pointer at each source site referencing the new ADR, mirroring `0002`'s pattern.

    **This plan adds exactly TWO pointer comments — the ADR-0005 pair.** `src/main/db.ts` and
    `src/main/telemetry.ts` are declared in this plan's `files_modified` and are edited here, in
    wave 2. Plan 06 (wave 3) and plan 10 (wave 5) also own those files, in later waves, so the
    ordering is safe; list both one-line edits explicitly in the SUMMARY so neither plan undoes them.

    **The two ADR-0006 pointers are NOT added here.** `src/renderer/src/components/terminalPool.ts`
    and `src/renderer/src/store/terminalPoolPolicy.ts` belong to **plan 05, in this same wave**, which
    may conditionally edit `terminalPool.ts` as part of its drop-path fix. Two plans editing one file
    in one wave in one working tree is a lost update, so plan 05 owns both files outright and adds
    both ADR-0006 pointers. Write `0006` here; plan 05 points at it. Do not touch either file.
    Update `docs/adr/README.md`'s index with the two new records.

    Finally, extend `test/ci-config.test.cjs` with repo-fact assertions:
    - `release.yml`'s `publish` job contains an `attest-build-provenance` step, that step declares
      `subject-checksums`, the job's `permissions` block carries all three of `contents: write`,
      `id-token: write`, `attestations: write`, and the attest step's index is greater than the
      flatten/merge step's and less than the release-upload step's. Parse the YAML — do not
      string-match a literal, following the file's existing parse-then-compare-two-sources-of-truth idiom.
    - `docs/adr/` contains at least six numbered records and `docs/adr/README.md` names each one.
    - `.github/ISSUE_TEMPLATE/bug_report.yml` parses and its Logs field description mentions Settings.
    Every assertion carries a message saying what a failure would MEAN.
  </action>
  <acceptance_criteria>
    - `ls docs/adr/*.md | wc -l` returns at least `7` (six records plus README).
    - `grep -c "0005" docs/adr/README.md` and `grep -c "0006" docs/adr/README.md` each return at least `1`.
    - `grep -rc "adr/0005" src/main/db.ts src/main/telemetry.ts` shows a pointer in **both** — they are this plan's files. (The two `adr/0006` pointers are plan 05's, in the same wave; do not assert them here and do not add them.)
    - `git diff --stat` for this task shows **no** change to `src/renderer/src/components/terminalPool.ts` or `src/renderer/src/store/terminalPoolPolicy.ts`.
    - `grep -c "continue-on-error" .github/workflows/ci.yml` returns `4` — the baseline; nothing was added. Paste it.
    - `grep -c "there is no .continue-on-error. anywhere in the test matrix" CONTRIBUTING.md` returns `1` (**a preservation criterion** - baseline today is `1` and it must still be `1`) — the hard-gate sentence survived the read-through and was not "cleaned up".
    - `grep -c "continue-on-error" test/ci-config.test.cjs` returns at least `1` - the test-matrix assertion is new. Baseline today: `0`. Paste both.
    - The task notes record the clause-by-clause read of `CONTRIBUTING.md:82-101` against `ci.yml`, with either "all three claims confirmed" or the specific clause corrected and the `ci.yml` line that disproved it.
    - `node --test test/ci-config.test.cjs` exit code is `0`, with a test count at least 3 higher than the task 1 baseline.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ci-config.test.cjs</automated>
  </verify>
  <done>The bug template asks only for reachable logs, CONTRIBUTING.md matches ci.yml, two ADRs exist with their source comments linked rather than deleted, and all of it is pinned by a parsed repo-fact test.</done>
</task>

<task type="auto">
  <name>Task 4: Carry GATE-01's SECURITY.md correction — the fourth stale trust-boundary claim</name>
  <files>SECURITY.md, test/ci-config.test.cjs</files>
  <read_first>
    - `SECURITY.md:34-39` — quoted verbatim in `<interfaces>` above; read it in place with the bullets
      either side, because the proxy-sidecar bullet at `:40-45` sits directly under it and must not be
      disturbed.
    - `01-02-PLAN.md` in full — specifically its task 2 (what the tokens actually become), its D-14
      ceiling wording, and its task 4, which corrects the other THREE surfaces (`HIVE.md`,
      `.planning/codebase/ARCHITECTURE.md`, `.planning/codebase/INTEGRATIONS.md`). This task must
      agree with that prose, not invent a second description of the same mechanism.
    - `01-CONTEXT.md` D-11 … D-16 — the locked decisions behind the design being described.
  </read_first>
  <action>
    Rewrite `SECURITY.md:34-39`'s hook-server bullet to describe what plan 02 lands in this same wave:
    a token minted PER SPAWN and injected into that one agent's PTY (and, for the qwen proxy tier,
    into that one sidecar), with the sender's identity DERIVED server-side from the token rather than
    read from the payload's `agent_id`. State the ceiling in plan 02's D-14 terms: the property is
    impersonation resistance — agent A cannot authenticate as agent B — not secrecy, because an
    agent's own shell can always read what its own shim can read.

    **Keep the existing last sentence's honesty**, adapted: it already says the token "is not a
    defence against a process that can already read this app's child environments", which remains
    exactly true and is the sentence this whole file is trying to be. Do not delete it, and do not
    replace it with a stronger claim.

    Follow this file's own rule at `:31-32` — name symbols, not line numbers.

    **Scope fence.** This task edits ONLY that bullet. `HIVE.md`, `.planning/codebase/ARCHITECTURE.md`
    and `.planning/codebase/INTEGRATIONS.md` are plan 02's in this same wave — do not touch them, and
    do not "helpfully" correct them here.

    **Sequencing.** Plan 02 is the same wave, so its code may not have landed when this runs. That is
    fine and is the point of writing this from the LOCKED decisions rather than from the diff — but if
    plan 02 reported its GATE-01 work as blocked (see its task 2 D-13 scope gap), say so in this
    plan's SUMMARY rather than letting SECURITY.md describe a boundary the code does not yet have.
    A doc that runs ahead of the code is the same defect as a doc that lags it.

    Then PIN it, in the repo-fact file task 3 already extends. Add an assertion to
    `test/ci-config.test.cjs` that `SECURITY.md` no longer carries the floor-wide wording and does
    carry the per-agent wording — same shape as task 3's `docs/adr/` and `bug_report.yml` assertions,
    with a message saying what a failure would MEAN. Assert on `SECURITY.md` ONLY: `HIVE.md`,
    `.planning/codebase/ARCHITECTURE.md` and `.planning/codebase/INTEGRATIONS.md` are plan 02's in
    this same wave and it pins them in `test/net-binding.test.cjs`; asserting them here would fail
    this plan on another plan's unlanded work. Do not name those three files in this test at all,
    comments included — the criterion below is a plain `grep -c`, and a mention in a comment would
    make it ambiguous. Record the split in the SUMMARY instead.
  </action>
  <acceptance_criteria>
    - `grep -c "process-local token" SECURITY.md` returns `0`. Baseline today: `1` — paste both.
    - `grep -c "minted fresh at each app start" SECURITY.md` returns `0`. Baseline today: `1` — paste both.
    - `grep -c "per-agent" SECURITY.md` returns **at least `2`**. Baseline today: `1` — a strict delta, so the claim was REPLACED rather than merely deleted. Paste both.
    - `tr -s '[:space:]' ' ' < SECURITY.md | grep -c "not a defence against a process that can already read this app's child environments"` returns `1` — **a preservation criterion**: baseline today is `1` and the honest sentence must survive the rewrite. Whitespace is squeezed first because the sentence wraps across `:38-39` today and a rewrite may re-wrap it; a plain `grep` on the phrase returns `0` even now, so it would prove nothing.
    - `git diff --name-only` for this task lists `SECURITY.md` and `test/ci-config.test.cjs` and nothing else — in particular not `HIVE.md`, `.planning/codebase/ARCHITECTURE.md` or `.planning/codebase/INTEGRATIONS.md` (plan 02's, this wave). `grep -c "ARCHITECTURE.md" test/ci-config.test.cjs` returns `0`.
    - `node --test test/ci-config.test.cjs` exit code is `0`, and the new SECURITY.md assertion is proved able to fail: temporarily restore the stale phrase, paste the RED output, restore the fix, paste the GREEN.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ci-config.test.cjs</automated>
  </verify>
  <done>SECURITY.md describes per-agent hook tokens and the honest impersonation-resistance ceiling; the file's existing honest sentence survives; the other three doc surfaces are untouched and left to plan 02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions OIDC → Sigstore public-good instance | The workflow's own identity is what signs the provenance. This is why attestation cannot live in app code. |
| published release artifact → end user's machine | The user has no other way to tell a tampered binary from a real one; provenance plus checksums is the entire control. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P04-01 | Tampering | published release binaries | mitigate | `actions/attest-build-provenance` over `release/SHA256SUMS.txt`, attested after flatten and before upload so the attested digests are the shipped bytes; verified with `gh attestation verify`. |
| T-P04-02 | Spoofing | a user trusting an unsigned Windows binary because "it has provenance" | mitigate | The honesty sentence in README/RELEASE/SECURITY states plainly that provenance does NOT suppress SmartScreen. Overclaiming here is the defect. |
| T-P04-03 | Elevation of privilege | over-broad workflow token | mitigate | Job-level `permissions` scoped to exactly `contents: write`, `id-token: write`, `attestations: write` on the `publish` job only; the rest of the workflow keeps the narrower workflow-level grant. |
| T-P04-04 | Repudiation | attestation step silently dropped in a later refactor | mitigate | `test/ci-config.test.cjs` parses the workflow and asserts the step, its input, its permissions and its ordering — a deletion fails `npm test` on the PR that does it. |
| T-P04-05 | Repudiation | docs claiming a CI gate that is not enforced | mitigate | FLOOR-17's CONTRIBUTING.md correction plus the parsed assertions; `ci.yml` is the single source of truth. |
| T-P04-06 | Repudiation | `SECURITY.md` describing a floor-wide hook secret after plan 02 replaces it with per-agent tokens | mitigate | A stale trust-boundary claim is a false statement about what the app protects, and this is the file a reader checks first. Task 4 rewrites `:34-39` for GATE-01 (plan 02's requirement, this plan's file), keeping the existing honest ceiling sentence. Pinned by greps that fail while the stale wording survives. |
</threat_model>

<verification>
- `npm test` green on all three platforms.
- The workflow YAML parses (asserted inside `test/ci-config.test.cjs`, not just by eye).
- Live verification is deferred and named: after the next `v*` tag, run
  `gh attestation verify <artifact> --repo MARKXAILABS/hello-markx` and paste the output. Per
  `01-VALIDATION.md` § Manual-Only Verifications this is FLOOR-06's live sample; record in the
  SUMMARY that it is outstanding rather than claiming it.
- `npm run check:links` green after the doc edits.
- Task 4's carried GATE-01 edit is reconciled with plan 02 rather than assumed: state in the SUMMARY
  whether plan 02's per-agent tokens landed this wave, and whether its D-13 `hive.ts` scope gap is
  open. SECURITY.md must not describe a boundary the shipped code does not have.
</verification>

<success_criteria>
- ROADMAP criterion 2's provenance half is TRUE: a downloaded release artifact traces to this repo
  and this commit via `actions/attest-build-provenance` plus published checksums, the release-link
  gate runs in the pipeline rather than being a documented intention, and the docs say plainly that
  this does not suppress SmartScreen.
- ROADMAP criterion 5's FLOOR-17 clause is TRUE: the bug template asks for logs that exist, and
  `docs/adr/` is the home for rationale that was buried in long source comments.
- GATE-01's documentation surface is whole across the wave: `SECURITY.md` here, and `HIVE.md` /
  `ARCHITECTURE.md` / `INTEGRATIONS.md` in plan 02. No document still describes one floor-wide hook
  secret. This clause belongs to GATE-01's issue, not to #15 or #41 — report it there.
- Issues #15 and #41 close in their PRs per D-43 — acceptance text verbatim, one command per clause
  with pasted output (including the empty `attest` grep as evidence the clause was open), named
  tests in `test/ci-config.test.cjs`, and the `npm test` exit line.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-04-SUMMARY.md` when done. It MUST list the four
one-line ADR pointer comments added to files owned by other plans, so a later wave does not undo
them, and MUST record that FLOOR-06's live `gh attestation verify` sample is outstanding until the
next tag.

It MUST also record task 4 separately from this plan's own requirements: the `SECURITY.md:34-39`
rewrite is GATE-01 work (`01-02-PLAN.md`'s requirement) carried here because this plan owns the file
this wave. Paste `grep -c "process-local token" SECURITY.md` and `grep -c "per-agent" SECURITY.md`,
and name plan 02's status so GATE-01's issue can cite all four corrected doc surfaces in one place.
</output>
