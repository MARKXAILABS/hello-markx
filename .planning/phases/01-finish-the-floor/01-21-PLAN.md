---
phase: 01-finish-the-floor
plan: 21
type: execute
wave: 8
depends_on: ["01-15", "01-16", "01-17", "01-18", "01-19", "01-20"]
files_modified:
  - eslint.config.js   # or eslint.config.mjs — this package declares no `"type"`, so `.js` is CJS; see <interfaces>
  - package.json
  - package-lock.json
  - .github/workflows/ci.yml
  - test/ci-config.test.cjs
  - src/main/knowledge.ts
  - src/main/nodeInstall.ts
  - src/main/slack.ts
  - src/renderer/src/ide/monaco.ts
  # Task 2's renderer edit set is MEASUREMENT-DRIVEN and cannot be enumerated in advance.
  # 60 of the 130 renderer files contain `useEffect` (verified 2026-08-20:
  # `grep -rl "useEffect" src/renderer --include=*.tsx --include=*.ts | wc -l` -> 60), and any
  # of them can produce an `exhaustive-deps` finding that task 2 must resolve. The nine listed
  # below are the files that ALREADY carry a suppression and are therefore certain to be
  # touched; the trailing glob is the honest outer bound. Task 2's containment criterion is
  # what makes that bound enforceable rather than decorative.
  - src/renderer/src/components/GitTab.tsx
  - src/renderer/src/components/MemoryGraphPanel.tsx
  - src/renderer/src/components/OnboardingWizard.tsx
  - src/renderer/src/components/TerminalView.tsx
  - src/renderer/src/components/triggers/SchedulesSection.tsx
  - src/renderer/src/components/triggers/WebhooksSection.tsx
  - src/renderer/src/hooks/useRestoreTeam.ts
  - src/renderer/src/ide/IdePanel.tsx
  - src/renderer/src/realtime/CompletionToast.tsx
  - src/renderer/src/**/*.{ts,tsx}
autonomous: false
requirements: [FLOOR-16]

must_haves:
  truths:
    - "ESLint runs as a hard CI gate at zero warnings, or the 13 orphaned eslint-disable comments are gone — and in fact both"
    - "Every one of the 9 react-hooks/exhaustive-deps disables is resolved by measurement: kept in place where ESLint still reports the underlying finding, deleted where ESLint reports the directive itself unused"
    - "The 4 @typescript-eslint/* disables, whose rules will never be configured, are deleted"
    - "The rule surface is exactly two named rules — no preset, no React Compiler rule set, no typescript-eslint ruleset"
    - "The finding count was measured and pasted before the gate was committed"
  artifacts:
    - path: "eslint.config.js"
      provides: "a flat config with a parser and exactly two explicitly named rules"
      contains: "rules-of-hooks"
    - path: ".github/workflows/ci.yml"
      provides: "a lint step in the typecheck job invoking `npm run lint`, whose script carries --max-warnings 0"
      contains: "npm run lint"
  key_links:
    - from: ".github/workflows/ci.yml typecheck job"
      to: "eslint.config.js"
      via: "`run: npm run lint` as a hard gate; the script is `eslint . --max-warnings 0`, so the flag is asserted in package.json rather than in the workflow"
      pattern: "npm run lint"
    - from: "the 9 exhaustive-deps disable comments"
      to: "the configured react-hooks/exhaustive-deps rule"
      via: "the rule becoming real, which turns orphans into honoured suppressions"
      pattern: "exhaustive-deps"
    - from: "this plan's renderer edits (wave 8)"
      to: "plan 01-23's FLOOR-12 allowlist assertions (wave 9)"
      via: "the line-anchor contract: this plan changes no line matching M1, and plan 23 keys its allowlist on content rather than on a line number"
      pattern: "fontSize"
---

<objective>
Make the 13 orphaned `eslint-disable` comments mean something: configure ESLint with exactly two named
rules, delete the four that reference rules that will never exist, resolve the nine survivors against
what ESLint actually reports, and wire lint into CI as a hard gate at zero warnings.

Purpose: FLOOR-16. The repo has never had a linter (`CONVENTIONS.md:30-31`) yet carries 13 disable
comments. D-29's split is **9 + 4** (verified; research's 10 + 3 was wrong): the nine
`react-hooks/exhaustive-deps` disables become live suppressions — they stop being orphans by the rule
becoming real. Which of the nine survive is decided by measurement, not by this sentence: with
`reportUnusedDisableDirectives: 'error'` a directive that no longer suppresses anything is itself a
finding, so a directive ESLint reports unused is **deleted**, not kept (see amendment D). The four
`@typescript-eslint/*` disables reference rules that will not be configured and are **deleted** — not
stylistically, but because ESLint errors with *"Definition for rule '…' was not found"* on a disable
naming a rule no configured plugin provides.

This plan runs last among the code plans because ESLint must lint the **final** tree. Linting before
the FLOOR-12 sweep would mean linting twice.

Output: a ten-line flat config, three devDependencies, four deletions, one CI step, and a measured
finding count on the page before the gate is committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@.planning/codebase/CONVENTIONS.md

<interfaces>
**The 13 sites, verified exactly (D-29's 9 + 4 split confirmed):**

*9 × `react-hooks/exhaustive-deps`* — `components/GitTab.tsx`, `components/MemoryGraphPanel.tsx`,
`components/OnboardingWizard.tsx`, `components/TerminalView.tsx`,
`components/triggers/SchedulesSection.tsx`, `components/triggers/WebhooksSection.tsx`,
`hooks/useRestoreTeam.ts`, `ide/IdePanel.tsx`, `realtime/CompletionToast.tsx`. **Keep each one exactly
where it is unless ESLint reports it unused** — see amendment D, which makes that call mechanical.

Six of the nine carry no explanatory sentence today (`GitTab.tsx`, `OnboardingWizard.tsx`,
`TerminalView.tsx`, `triggers/SchedulesSection.tsx`, `triggers/WebhooksSection.tsx`, `ide/IdePanel.tsx`);
three do (`MemoryGraphPanel.tsx`, `hooks/useRestoreTeam.ts`, and `realtime/CompletionToast.tsx`).
Adding the six missing sentences means **inserting six comment lines** — which is precisely what the
line-anchor contract below exists to bound.

*4 × `@typescript-eslint/*`* — `src/main/knowledge.ts` (`no-var-requires`),
`src/main/nodeInstall.ts` (`no-var-requires`), `src/main/slack.ts` (`no-require-imports`),
`src/renderer/src/ide/monaco.ts` (`no-explicit-any`). **Delete all four.**

**Two amendments to D-28, both mechanical and both load-bearing:**

**A — a TypeScript parser is mandatory.** ESLint's default parser (espree) cannot parse a type
annotation, so without `@typescript-eslint/parser` ESLint lints **nothing** in `src/`. This does not
violate D-31, which rejects the ~100-rule *ruleset*: adopting zero rules from it adds zero findings.
So the dependency count is **3, not 2**: `eslint`, `@typescript-eslint/parser`,
`eslint-plugin-react-hooks`.

**B — never use `eslint-plugin-react-hooks@7`'s recommended preset.** D-28's bounded-surface premise
was true of v5. v7 ships the React Compiler rule set (~17 rules: `config`, `error-boundaries`,
`gating`, `globals`, `immutability`, `preserve-manual-memoization`, `purity`, `refs`,
`set-state-in-effect`, `set-state-in-render`, `static-components`, `unsupported-syntax`, `use-memo`,
`incompatible-library`), most at `error`. Whether `configs.flat.recommended` carries them is ambiguous
in the plugin's own README (RESEARCH assumption A1). **Write the two rules explicitly and never touch a
preset** — that makes D-28's bounded-surface guarantee mechanically true regardless of what the
plugin's presets do now or in a future minor.

**C — `--format compact` does not exist in this setup, and the package that supplies it is not
eligible.** ESLint 9 removed the `checkstyle`, `compact`, `jslint-xml`, `junit`, `tap`, `unix` and
`visualstudio` formatters from core; `--format compact` fails with a formatter-load error. The
standalone `eslint-formatter-compact` is **not** an option here: `npm view eslint-formatter-compact
description` returns *"ESLint's official `compact` formatter, unofficially published as a standalone
module"*, published from `github.com/fregante/eslint-formatters` — a third-party republish, which the
RESEARCH Package Legitimacy Audit has not dispositioned and which would fail its canonical-publisher
bar. **Use the built-in `json` formatter instead.** It is better for this job anyway: the measurement
task needs a per-rule breakdown, and `json` gives one exactly, including unused-directive reports,
which arrive as messages with `ruleId: null`. The dependency count stays at 3.

**D — the nine suppressions are resolved by measurement, and the two rules that decide it cannot both
be absolute.** `reportUnusedDisableDirectives: 'error'` plus `--max-warnings 0` means an unused
directive is itself a gate failure. So "keep all nine, no matter what" and "the gate must be green" are
not simultaneously satisfiable if even one of the nine is unused — and whether any is unused is
**unknown until task 1 runs**, because it depends on how the installed `eslint-plugin-react-hooks`
version analyses each effect. Static reading cannot settle it: all nine directives are correctly
positioned on the line directly above their dependency array (verified 2026-08-20), so none is
*obviously* dead.

The mechanical resolver, one command per file, is `--no-inline-config` — it makes ESLint ignore inline
directives and report the underlying finding:

```
npx eslint <file> --no-inline-config --format json
```

- The file's run reports a `react-hooks/exhaustive-deps` message -> the directive is **live**: keep it.
- The file's run reports none -> the directive suppresses nothing: **delete it**, and record the pasted
  run as the proof.

This is not a weakening of anything. It is the only reading under which the gate is reachable, and it
replaces an unfalsifiable "keep all nine" with a per-file test whose answer is produced by the linter
rather than asserted by the plan.

**The wave-8 -> wave-9 line-anchor contract. This is a two-plan contract; plan `01-23` carries the
matching half and neither may be changed alone.**

Plan `01-23` (wave 9) asserts the FLOOR-12 completeness bar against an allowlist of sub-14px `fontSize`
sites. This plan edits renderer files in wave 8, *between* the wave-7 sweep that produces those sites
and the wave-9 test that asserts them. Measured on the current tree (2026-08-20):

- 8 of this plan's 9 certain-to-be-edited renderer files are also M1 files (all but `useRestoreTeam.ts`).
- **81 M1 sites sit BELOW a suppression this plan edits** — `GitTab.tsx` 12, `MemoryGraphPanel.tsx` 9,
  `OnboardingWizard.tsx` 32, `TerminalView.tsx` 1, `SchedulesSection.tsx` 1, `WebhooksSection.tsx` 3,
  `IdePanel.tsx` 20, `CompletionToast.tsx` 3.
- 44 of the 61 M1 files contain a React hook, so a dependency fix anywhere in the sweep set can shift
  an anchor in a file this plan does not even name.

Inserting the six missing reason sentences therefore shifts up to 81 `file:line` anchors by at least one
line. A wave-9 test holding wave-7 line numbers would break silently.

**The contract has two halves:**

1. **This plan changes no line that matches M1.** Its work is hooks and dependency arrays; it has no
   reason to touch a `fontSize`. Task 2 asserts this mechanically, so the guarantee is checked rather
   than assumed.
2. **Plan `01-23` keys its allowlist on content, not on a line number**, and re-derives it at wave 9
   against the tree as it then stands. Line numbers quoted anywhere in either plan are informational
   pointers, never assertion keys.

Half 1 alone is not sufficient — a comment inserted above an M1 site still moves it — which is why
half 2 is the load-bearing one and half 1 is the cheap guarantee that keeps the *content* stable.

**Task 2's real scope is the whole renderer, and it is bounded by a containment check rather than by a
file list.** 60 of the 130 renderer files contain `useEffect`
(`grep -rl "useEffect" src/renderer --include=*.tsx --include=*.ts | wc -l` -> 60, verified
2026-08-20), plus `useCallback`/`useMemo` sites. Any of them can produce a finding that `--max-warnings 0`
turns into work. Pretending the edit set is 13 files would be a false declaration; the honest bound is
`src/main/{knowledge,nodeInstall,slack}.ts` plus `src/renderer/src/**/*.{ts,tsx}`, enforced by a
`git diff --name-only` assertion in task 2 that fails if anything else moved.

**The config to write** (`01-RESEARCH.md:874-902` supplies the literal file — it is the analog, because
this repo has never had a linter and `01-PATTERNS.md:438-448` records `NO ANALOG` for it):
flat config, ESM default export, one entry with `files: ['src/**/*.{ts,tsx}']`,
`languageOptions: { parser: tsParser, ecmaVersion: 2022, sourceType: 'module', parserOptions: { ecmaFeatures: { jsx: true } } }`,
`linterOptions: { reportUnusedDisableDirectives: 'error' }`,
`plugins: { 'react-hooks': reactHooks }`, and exactly two rules:
`'react-hooks/rules-of-hooks': 'error'` and `'react-hooks/exhaustive-deps': 'warn'`.
It must carry the comment paragraph explaining why the parser is present with zero rules, or a future
contributor will "simplify" it away (`CONVENTIONS.md:78`).

**Name the file for the module system this package actually has.** `package.json` declares no `"type"`
(verified 2026-08-20), so a bare `.js` file is loaded as **CommonJS** and an `export default` in
`eslint.config.js` is a syntax error at config-load time. Write `eslint.config.mjs` (ESLint 9 resolves
it natively) or keep `eslint.config.js` with `module.exports`. Record which you chose; every check in
this plan uses ESLint's own resolver or a filename-independent path, so either is fine — silently
shipping an ESM `export default` in a `.js` file inside a CJS package is not.

**Version choice.** ESLint 10 (`10.8.1`) requires `node: ^20.19.0 || ^22.13.0 || >=24`, while
`package.json` `engines` says `>=20 <23`, which admits Node 20.0-20.18. CI (`node-version: 22`) is
fine either way. **ESLint 9 maintenance (`9.39.5`, engines `^18.18.0 || ^20.9.0 || >=21.1.0`) is the
safer pick** — the repo has never linted and nothing depends on ESLint 10 features; the react-hooks
plugin peer-supports both. `@typescript-eslint/parser` 8.67.0's peer range covers the exact
`typescript@5.9.3` pin.

**`--max-warnings 0` is load-bearing (D-30).** Both `exhaustive-deps` and unused-directive reporting
default to *warnings*, which a hard gate would otherwise wave straight through.
`reportUnusedDisableDirectives: 'error'` in the config is belt-and-braces.

**CI target job** (`.github/workflows/ci.yml:16-29`): the `typecheck` job already runs
`npm ci --ignore-scripts` and needs no native build, so the lint step belongs there. **Do not add a
`continue-on-error`** — the two that exist (the advisory `npm audit` at `:30-35`, and the flaky native
rebuild in `build`) are pre-existing, deliberate and documented inline. A third is forbidden.

**RESEARCH Open Question 1 — the single largest unbounded item in the phase.** Nobody has measured how
many `exhaustive-deps` warnings 131 `useEffect` + 45 `useCallback` + 26 `useMemo` call sites across 130
renderer files actually produce. Nine sites already carry a disable, which is evidence the rule has real
findings here. With `--max-warnings 0`, every finding becomes phase work — a genuine dependency fix or a
reviewed suppression.

**Package legitimacy:** `01-RESEARCH.md` § Package Legitimacy Audit dispositions all three additions as
**Approved** — `eslint` is canonical, `eslint-plugin-react-hooks` is first-party React (published from
`facebook/react`, `packages/eslint-plugin-react-hooks`), `@typescript-eslint/parser` is the canonical
typescript-eslint project. None is `[ASSUMED]`, `[SUS]` or `[SLOP]`, so no blocking legitimacy
checkpoint is required. All three are **devDependencies**; no runtime dependency is added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the three devDependencies, write the config, and MEASURE the finding count before committing any gate</name>
  <files>eslint.config.js, package.json, package-lock.json</files>
  <read_first>
    - `.planning/codebase/CONVENTIONS.md:25-35` — the "no linter configured" record this plan changes, and `:78` the comment convention.
    - `01-RESEARCH.md` § "FLOOR-16 — two amendments to D-28" and § "Code Examples 4" — the literal config and both amendments.
    - `01-PATTERNS.md` § "`eslint.config.js` (config) — **NO ANALOG**" `:438-448`.
    - `package.json` — `engines`, `devDependencies`, `scripts`, and the exact `typescript` pin.
    - `playwright.config.ts` — the only structural analog for a root-level ESM default-export config.
  </read_first>
  <action>
    Re-verify the three packages before installing (`npm view eslint dist-tags`,
    `npm view eslint-plugin-react-hooks version`, `npm view @typescript-eslint/parser version`) —
    RESEARCH is valid only to 2026-09-03. Paste the output. All three are dispositioned **Approved** in
    the research audit, so no legitimacy checkpoint is required; if any resolves to a different
    publisher or scope than recorded, STOP and report.

    Install all three as **devDependencies**. Take ESLint 9 maintenance unless you can show the engines
    range makes 10 safe; record the choice and the reason in the SUMMARY. Regenerate `package-lock.json`
    on a **clean tree**, never hand-edited.

    **The "npm 10 under Node 22" rule is dead — do not reinstate it, and do not cite plan 01 as its
    authority.** Red-team round 2 withdrew that clause from D-07 and plan 01 deleted it: npm 9, 10 and 11
    all write `lockfileVersion: 3`, so no check could tell the writers apart, and this host is
    node v24.13.0 / npm 11.6.2 with **no Node 22 installed**, which made the instruction unfollowable
    here. What replaces it is checks that actually discriminate — run and paste all three:

    ```
    node --version && npm --version            # the writer, recorded in the SUMMARY as a fact, not a gate
    node -e "console.log(require('./package-lock.json').lockfileVersion)"   # must print 3
    npm ci --ignore-scripts && git diff --exit-code package-lock.json       # must exit 0
    ```

    The last one is the real gate: it proves the lock matches `package.json` and reproduces. CI's
    `setup-node` Node 22 *consuming* this lockfile is the direction that matters.

    Write the flat config from the literal in `<interfaces>` — as `eslint.config.mjs`, or as
    `eslint.config.js` with `module.exports`, because this package declares no `"type"` and a bare `.js`
    is loaded as CommonJS. **Do not use any preset.** After
    install, inspect the installed `eslint-plugin-react-hooks`'s entry point to confirm what its
    `configs.flat.recommended` actually carries (RESEARCH assumption A1) and record the finding — the
    recommendation holds either way, but the fact is worth knowing.

    Then **measure**, before committing any CI gate. **Do not use `--format compact`** — ESLint 9
    removed that formatter from core (amendment C) and the standalone package that supplies it is a
    third-party republish that fails the legitimacy bar. Use the built-in `json` formatter and derive
    the breakdown from it, in one command with no temp file:

    ```
    npx eslint . --max-warnings 999 --format json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const m=JSON.parse(s).flatMap(f=>f.messages);const by={};for(const x of m){const k=x.ruleId||'(unused-disable-directive)';by[k]=(by[k]||0)+1;}console.log('TOTAL',m.length);console.log(by);})"
    ```

    Paste `TOTAL` and the full per-rule object — `react-hooks/rules-of-hooks` errors,
    `react-hooks/exhaustive-deps` warnings, and `(unused-disable-directive)` reports, which arrive with
    `ruleId: null` and are the count that decides amendment D for the nine suppressions. This is
    Open Question 1 and it is the single largest unbounded item in the phase.

    Then run amendment D's per-file resolver over the nine suppression sites and paste all nine runs:

    ```
    for f in src/renderer/src/components/GitTab.tsx src/renderer/src/components/MemoryGraphPanel.tsx src/renderer/src/components/OnboardingWizard.tsx src/renderer/src/components/TerminalView.tsx src/renderer/src/components/triggers/SchedulesSection.tsx src/renderer/src/components/triggers/WebhooksSection.tsx src/renderer/src/hooks/useRestoreTeam.ts src/renderer/src/ide/IdePanel.tsx src/renderer/src/realtime/CompletionToast.tsx; do echo "$f $(npx eslint "$f" --no-inline-config --format json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{console.log(JSON.parse(s).flatMap(f=>f.messages).filter(m=>m.ruleId==='react-hooks/exhaustive-deps').length)})")"; done
    ```

    A `0` means that file's directive suppresses nothing and task 2 must delete it; anything above `0`
    means the directive is live and task 2 must keep it. This list IS task 2's keep/delete decision —
    the executor does not get to make it by reading the code.

    **Decision gate, and it is not the executor's to soften:**
    - If the count is small enough that task 2 can fix or reviewedly suppress every finding inside this
      plan's budget, proceed.
    - If it is not, **STOP and report** with the pasted count and a proposed split. Do NOT weaken
      `--max-warnings 0` (D-30 is locked), do NOT blanket-suppress, and do NOT add a
      `continue-on-error`. The correct response to "more work than fits" is to say so, not to ship a
      gate that waves findings through.

    Add an `npm run lint` script whose command is exactly `eslint . --max-warnings 0`, so the gate is
    discoverable and byte-identical locally and in CI. **`--max-warnings 0` lives in this script and
    nowhere else** — task 3 wires CI as `run: npm run lint` and asserts the flag here, in
    `package.json`, rather than grepping `ci.yml` for a flag that a correct implementation never puts
    there.
  </action>
  <acceptance_criteria>
    - `node -e "const p=require('./package.json');console.log(p.devDependencies.eslint, p.devDependencies['@typescript-eslint/parser'], p.devDependencies['eslint-plugin-react-hooks'])"` prints three versions, and none of the three appears under `dependencies`.
    - The config exists at the repo root under whichever name task 1 chose — resolve it once and use it in the next three checks: `CFG=$(ls eslint.config.mjs eslint.config.js 2>/dev/null | head -1); echo "CFG=$CFG"` prints a non-empty name.
    - `grep -c "recommended" "$CFG"` returns `0` — no preset is used.
    - `grep -c "rules-of-hooks" "$CFG"` returns `1` and `grep -c "exhaustive-deps" "$CFG"` returns `1`; the total rule count in the file is exactly 2 (the task verify below asserts that against ESLint's own resolver, which is the check that cannot be fooled by formatting).
    - `grep -c "reportUnusedDisableDirectives" "$CFG"` returns `1`.
    - Task notes contain the pasted `TOTAL` and per-rule object from the `--format json` measurement command above. The removed formatter was never **invoked** — proven at the two places that can show it, rather than by banning a phrase this plan itself uses seven times and an executor is likely to quote: `grep -cE "^ *(npx )?eslint .*--format[ =]compact" .planning/phases/01-finish-the-floor/01-21-SUMMARY.md` returns `0` (no pasted command line invokes it), **and** `node -e "const p=require('./package.json');if((p.devDependencies||{})['eslint-formatter-compact']||(p.dependencies||{})['eslint-formatter-compact'])throw new Error('the third-party formatter republish was installed');console.log('OK')"` exits `0`. Prose *explaining* why `--format compact` is unavailable is expected and must not trip either check.
    - Task notes contain the pasted nine-line output of the amendment-D resolver loop, one `<file> <count>` line per suppression site. This is task 2's binding keep/delete list.
    - Task notes contain the ESLint major choice with its reason, and the inspection result for the plugin's flat preset.
    - Lockfile discrimination, all three pasted: `node --version && npm --version` (recorded as the writer — **not** a gate; the npm-major rule was withdrawn from D-07 and no criterion here asserts one), `node -e "console.log(require('./package-lock.json').lockfileVersion)"` prints `3`, and `npm ci --ignore-scripts && git diff --exit-code package-lock.json` exits `0`. **The fake this catches:** a hand-edited or partially-resolved lockfile, which `lockfileVersion` alone cannot see.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <!-- NOT `npx eslint . --max-warnings 999 ...`: `--max-warnings` only adds an exit-1 condition for
         WARNINGS. Any error-severity problem exits 1 regardless, and at task-1 time this tree still has
         four `@typescript-eslint/*` disables that ESLint errors on ("Definition for rule '…' was not
         found") plus any `rules-of-hooks` error — all of which task 2, not task 1, resolves. That verify
         was RED by construction on correct work. The measurement command in the action survives only
         because its pipe to `node` masks the exit status; this tag has no pipe.
         What is verified here is task 1's actual deliverable: a loadable config whose rule surface is
         exactly the two named rules, with a real TypeScript parser. It fails if a preset was used
         (surface > 2), if the parser is missing (ESLint would lint nothing in src/), or if the config
         does not load at all. Uses ESLint's own resolver, so it holds for `eslint.config.js` or
         `eslint.config.mjs`. -->
    <automated>node -e "const {ESLint}=require('eslint');new ESLint().calculateConfigForFile('src/renderer/src/App.tsx').then(c=>{const r=Object.keys(c.rules||{}).sort();if(r.join()!=='react-hooks/exhaustive-deps,react-hooks/rules-of-hooks')throw new Error('rule surface is ['+r.join()+'], expected exactly the two named rules');if(!c.languageOptions||!c.languageOptions.parser)throw new Error('no TypeScript parser configured - ESLint would lint nothing in src/');console.log('CONFIG OK',r.join(','))})"</automated>
  </verify>
  <done>Three devDependencies installed, a two-rule config written with no preset, and the finding count measured and on the page before any gate exists.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the four dead disables, resolve every finding, and resolve the nine suppressions against what ESLint reports</name>
  <files>src/main/knowledge.ts, src/main/nodeInstall.ts, src/main/slack.ts, src/renderer/src/**/*.{ts,tsx} — the renderer half is measurement-driven and bounded by the containment criterion below, not by an enumerated list. Certain: src/renderer/src/ide/monaco.ts plus the nine suppression files (GitTab.tsx, MemoryGraphPanel.tsx, OnboardingWizard.tsx, TerminalView.tsx, triggers/SchedulesSection.tsx, triggers/WebhooksSection.tsx, hooks/useRestoreTeam.ts, ide/IdePanel.tsx, realtime/CompletionToast.tsx). Possible: any of the 60 renderer files containing useEffect.</files>
  <read_first>
    - Each of the four `@typescript-eslint/*` disable sites in context — enough to confirm the comment is the only thing being removed.
    - Each of the nine `react-hooks/exhaustive-deps` sites in context, including the effect each suppresses and any comment recording WHY.
    - Task 1's pasted finding list AND its nine-line amendment-D resolver output — this task's work is exactly those two lists.
    - `.planning/codebase/CONVENTIONS.md:78` — comments explain WHY at length; a suppression that records intent keeps it.
    - `<interfaces>` § "the wave-8 -> wave-9 line-anchor contract" — half 1 of that contract is this task's obligation and is asserted below.
  </read_first>
  <action>
    Delete the four `@typescript-eslint/*` disable comments and nothing else at those sites. They are
    forced deletions, not stylistic: ESLint errors with *"Definition for rule '…' was not found"* on a
    disable naming a rule no configured plugin provides, so leaving them would make the gate red for a
    reason unrelated to code quality.

    **First, record the base commit** so this task's containment is checkable:
    `BASE=$(git rev-parse HEAD)` — paste it. Every `git diff` below is against `$BASE`.

    **Resolve the nine `react-hooks/exhaustive-deps` disables against task 1's amendment-D resolver
    output, not against a preference.** For each of the nine:
    - resolver count `> 0` -> the directive is live. **Keep it exactly where it is, with zero
      rewriting.** If it carries no explanatory comment, add one sentence saying why the dependency is
      deliberately omitted — that is the difference between a suppression and a silenced bug. Six of the
      nine need this sentence (`GitTab.tsx`, `OnboardingWizard.tsx`, `TerminalView.tsx`,
      `SchedulesSection.tsx`, `WebhooksSection.tsx`, `IdePanel.tsx`); three already have one.
    - resolver count `= 0` -> the directive suppresses nothing, `reportUnusedDisableDirectives: 'error'`
      will fail the gate on it, and it must be **deleted**. Name it in the notes with its pasted
      resolver run. This is not a softened gate — it is the gate being obeyed. Deleting a directive that
      suppresses nothing removes no protection, because there was nothing under it to protect.

    Do not decide any of the nine by reading the code. The resolver decides.

    **Line-anchor contract, half 1 — this task changes no line matching M1.** Plan `01-23` (wave 9)
    asserts the FLOOR-12 allowlist over sub-14px `fontSize` sites, and 81 of them sit below a
    suppression this task edits. This task's work is hooks and dependency arrays; it has no reason to
    touch a `fontSize`, so the guarantee is free — but it is asserted, not assumed:
    `git diff -U0 "$BASE" -- src/renderer/src | grep -E "^[-+]" | grep -E "fontSize *[:=]"` must be
    empty. If a genuine hooks fix somehow requires touching a `fontSize` line, STOP and report it — do
    not land it silently, because plan `01-23`'s allowlist is derived from that exact text.

    Then work task 1's finding list:
    - **`rules-of-hooks` errors are real runtime bugs** — `tsc --strict` structurally cannot model them,
      and that is exactly why this rule was chosen. Fix the code. Do NOT suppress a `rules-of-hooks`
      error; if one appears genuinely unfixable inside this plan, stop and report it with the file:line.
    - **`exhaustive-deps` warnings**: prefer a genuine dependency fix. Where the omission is deliberate,
      add a suppression **with a one-sentence reason**, matching the nine that already exist. A bare
      suppression with no reason is what created this requirement in the first place.
    - **Unused-directive reports** are the config telling you a suppression no longer suppresses
      anything. Delete those directives.

    **Do not modify a test so a source path passes, and do not weaken a rule to reduce findings.** If a
    fix changes runtime behaviour, land it as its own `fix(...)` commit with the behaviour change named
    in the message, and re-run `npm test`.

    **Containment, and it is asserted PER-COMMIT, not over the whole tree.** The renderer half of this
    task is measurement-driven and can reach any of the 60 `useEffect`-bearing renderer files, so the
    bound is enforced by a diff assertion rather than by a file list: nothing outside
    `src/main/{knowledge,nodeInstall,slack}.ts` and `src/renderer/src/**/*.{ts,tsx}` may be committed
    **by this task**. Record each commit SHA as you make it (`SHAS="$SHAS $(git rev-parse HEAD)"`) and
    assert over those SHAs. `SHAS` holds this task's **code** commits only — not the separate
    `.planning/` bookkeeping commits (STATE, SUMMARY), which are not this task's edit set and are not in
    the bound.

    A whole-tree `git diff "$BASE"` would be **wrong** here: plan `01-22` is this plan's wave-mate, runs
    in the **same tree** (`use_worktrees: false`), and commits `test/renderer-components.test.cjs` — a
    tree-wide filter would fail this task on a file it never touched, and the two plans could not both
    pass. Scope to your own commits and the check means what it says. Paste the filter, its empty
    result, and `for sha in $SHAS; do git show --stat "$sha"; done` in full so the actual touched set is
    on the page. If a finding genuinely requires editing outside that bound, STOP and report it with the
    file and the finding — widening the bound is a plan change, not an executor call.

    Re-run `npx eslint . --max-warnings 0` until it exits `0`, and paste the final run.
  </action>
  <acceptance_criteria>
    - `grep -rn "@typescript-eslint/" src/ --include=*.ts --include=*.tsx` returns `0` matches.
    - For each of the original nine files, this **pairing command** is run and pasted, and every line reads `OK`. Any `MISMATCH` line is a criterion failure:
      `for f in src/renderer/src/components/GitTab.tsx src/renderer/src/components/MemoryGraphPanel.tsx src/renderer/src/components/OnboardingWizard.tsx src/renderer/src/components/TerminalView.tsx src/renderer/src/components/triggers/SchedulesSection.tsx src/renderer/src/components/triggers/WebhooksSection.tsx src/renderer/src/hooks/useRestoreTeam.ts src/renderer/src/ide/IdePanel.tsx src/renderer/src/realtime/CompletionToast.tsx; do n=$(npx eslint "$f" --no-inline-config --format json | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{console.log(JSON.parse(s).flatMap(x=>x.messages).filter(m=>m.ruleId==='react-hooks/exhaustive-deps').length)})"); d=$(grep -c "react-hooks/exhaustive-deps" "$f"); if { [ "$n" -gt 0 ] && [ "$d" -ge 1 ]; } || { [ "$n" -eq 0 ] && [ "$d" -eq 0 ]; }; then r=OK; else r=MISMATCH; fi; echo "$r $f findings=$n directives=$d"; done`
      The rule it encodes: a file ESLint still reports an `exhaustive-deps` finding for MUST keep its suppression; a file ESLint reports none for MUST have had the dead directive deleted. Nine `OK` lines is the pass.
    - Every file that ended `findings=0 directives=0` is listed in the task notes by name with its pasted resolver run, so a deletion can never be mistaken for a suppression that was quietly dropped. If none did, state that plainly — nine live suppressions is the expected outcome.
    - Every suppression still present has an adjacent explanatory sentence — paste each one with its surrounding comment.
    - **Every suppression beyond the original nine is listed separately in the task notes**, with its `file:line`, its adjacent one-sentence reason, and why a genuine dependency fix was rejected there. A new suppression with no reason sentence is a criterion failure. If the count is `9`, state that plainly — zero new suppressions is the expected and preferred outcome, not a missing deliverable.
    - `npx eslint . --max-warnings 0` exit code is `0`; paste the run.
    - **Line-anchor contract, half 1:** `git diff -U0 "$BASE" -- src/renderer/src | grep -E "^[-+]" | grep -E "fontSize *[:=]"` produces **no output** — this task moved no `fontSize` text, so plan `01-23`'s content-keyed allowlist stays derivable. Paste the command and its empty result.
    - **Containment (per-commit):** `for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/main/(knowledge|nodeInstall|slack)\.ts|src/renderer/src/.*\.(ts|tsx))?$"` produces **no output** — nothing outside this task's declared bound was committed by it. (The trailing `?` absorbs the blank line `--format=` emits.) Paste the command, its empty result, and `for sha in $SHAS; do git show --stat "$sha"; done` beside it so the actual touched set is on the page. **Scoped to this task's own commits on purpose:** plan `01-22` commits `test/renderer-components.test.cjs` into this same tree in this same wave, so a tree-wide `git diff "$BASE"` filter would fail this task on a wave-mate's file and make the two plans mutually unsatisfiable.
    - `npm test` exit code is `0` and `npm run typecheck` exit code is `0` — no test was modified to accommodate a lint fix. The per-commit containment command above already proves this task committed no `test/*.test.cjs`; if a real behaviour fix required one, the containment check fails by design and the plan must be amended rather than the check loosened. Paste `for sha in $SHAS; do git show --stat "$sha"; done`.
  </acceptance_criteria>
  <verify>
    <automated>npx eslint . --max-warnings 0</automated>
  </verify>
  <done>Four dead disables gone, every one of the nine resolved the way ESLint's own report dictates, no `fontSize` line touched, nothing edited outside the declared bound, and lint clean at zero warnings.</done>
</task>

<task type="auto">
  <name>Task 3: Wire lint into CI as a hard gate and pin the step in test/ci-config.test.cjs</name>
  <files>.github/workflows/ci.yml, test/ci-config.test.cjs</files>
  <read_first>
    - `.github/workflows/ci.yml` in full — the `typecheck` job at `:16-29`, the advisory `npm audit` step's `continue-on-error` at `:30-35` and its inline rationale, and the `build` job's documented second one.
    - `test/ci-config.test.cjs` in full — the YAML-parse-and-assert idiom and the assertions plan 04 added.
    - `01-PATTERNS.md` § "FLOOR-16 — the CI step" `:1140-1172`.
  </read_first>
  <action>
    Add a `Lint` step to the **`typecheck`** job in `.github/workflows/ci.yml`, after the typecheck step,
    written as `run: npm run lint` — task 1's script, whose command is `eslint . --max-warnings 0`.
    That job already installs with `--ignore-scripts` and needs no native build, so lint costs nothing
    extra there. **Do not inline the flag into the workflow**: one gate, one definition, and the
    criteria below assert the flag in `package.json` where it lives.

    **Add no `continue-on-error`.** The two that exist are pre-existing, deliberate and documented; a
    third is forbidden by this project's constraints and by ROADMAP criterion 2.

    Extend `test/ci-config.test.cjs` with parsed assertions (parse the YAML, do not string-match a
    literal, following the file's existing parse-then-compare idiom):
    - the `typecheck` job contains a step whose `run` is `npm run lint`;
    - `package.json`'s `lint` script invokes eslint **and** carries `--max-warnings 0` — D-30 is the
      whole point, because both `exhaustive-deps` and unused-directive reporting default to warnings and
      a gate without the flag waves them through. The assertion crosses both files deliberately: the
      workflow names the script, the script carries the flag, and neither half is a gate alone;
    - the step declares no `continue-on-error`;
    - the count of `continue-on-error: true` **declarations** across the parsed `ci.yml` is exactly
      `2` — the pre-existing number, measured 2026-08-20 — so a third can never be added silently. Count
      parsed declarations, never raw string occurrences: the string appears **4** times in the file
      because `:45` and `:113` are comments explaining the absence of one;
    - the flat config exists at the repo root under either name (`eslint.config.mjs` or `eslint.config.js`)
      and declares exactly two rules — so a future preset swap that silently adopts the React Compiler
      rule set fails the suite.
    Every assertion message says what a failure would MEAN.

    Push and confirm CI is green on the remote with the lint step running. A local green run is not the gate.
  </action>
  <acceptance_criteria>
    - The CI gate is written as `run: npm run lint` — task 1's script, so the command is byte-identical locally and in CI: `grep -cE "^ *run: *npm run lint *$" .github/workflows/ci.yml` returns exactly `1`.
    - The flag is asserted where it lives: `node -e "const s=(require('./package.json').scripts||{}).lint||'';if(!/eslint/.test(s))throw new Error('lint script does not invoke eslint: '+s);if(!/--max-warnings 0/.test(s))throw new Error('lint script lacks --max-warnings 0 - the gate would wave warnings through: '+s);console.log('LINT SCRIPT OK',s)"` exits `0`. **Do not grep `ci.yml` for `max-warnings`:** with `run: npm run lint` in the workflow that grep returns `0` on a fully correct implementation, so it would fail the right answer.
    - `grep -cE "^[^#]*continue-on-error: *true" .github/workflows/ci.yml` returns exactly `2`. **The unit is non-comment `continue-on-error: true` declarations**, and it is the same unit `test/ci-config.test.cjs` counts after parsing. A bare `grep -c "continue-on-error" .github/workflows/ci.yml` returns `4` and is the wrong unit — two of those four are comments at `:45` and `:113` explaining why there is no `continue-on-error` there. Record `2` in the notes.
    - `node --test test/ci-config.test.cjs` exit code is `0`, with a test count at least 4 higher than plan 04's.
    - `npm test` exit code is `0`.
    - CI on the pushed branch is green for all three `test` jobs, `typecheck` (now including lint), and `e2e`; the run URL is pasted into the task notes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/ci-config.test.cjs</automated>
  </verify>
  <done>Lint is a hard three-platform-visible gate at zero warnings, and the gate itself is pinned by a parsed test so it cannot be softened silently.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 4: Decision — does the measured finding count fit this plan?</name>
  <read_first>
    - Task 1's pasted `npx eslint . --max-warnings 999 --format json` total and per-rule breakdown (amendment C — `--format compact` was removed from ESLint 9 core and is not used anywhere in this plan).
    - `01-RESEARCH.md` § Open Questions, item 1 — why this number was never measured before now.
    - `01-CONTEXT.md` D-30, D-31 — `--max-warnings 0` is locked and the ruleset is deliberately bounded.
  </read_first>
  <acceptance_criteria>
    - This checkpoint is skipped only if task 2 completed with `npx eslint . --max-warnings 0` exiting `0`; otherwise the measured count is pasted with the selected option.
    - The selected option is one of `proceed-in-plan`, `split-plan`, `split-phase` and is recorded in the SUMMARY.
    - No option that weakens `--max-warnings 0`, blanket-suppresses findings, or adds a `continue-on-error` was taken.
  </acceptance_criteria>
  <decision>Whether the measured `exhaustive-deps` finding count fits this plan, or the phase needs a split</decision>
  <context>
    RESEARCH Open Question 1 flags this as the single largest unbounded item in the phase: nobody has
    measured how many `exhaustive-deps` warnings 131 `useEffect` + 45 `useCallback` + 26 `useMemo` call
    sites across 130 renderer files produce. With `--max-warnings 0` (D-30, locked), every finding
    becomes phase work.

    This checkpoint fires **only if task 1's measured count does not fit this plan's budget.** If task 2
    completed with lint clean, mark it satisfied and move on — do not stop the operator for a decision
    that has already been made by the number.
  </context>
  <options>
    <option id="proceed-in-plan">
      <name>Findings fit — already resolved in task 2</name>
      <pros>Nothing to decide; FLOOR-16 closes in this phase as planned.</pros>
      <cons>None.</cons>
    </option>
    <option id="split-plan">
      <name>Split the fix work into an additional plan in this wave</name>
      <pros>Keeps each executor inside a healthy context budget; the gate still lands in this phase; nothing is suppressed to make a number go down.</pros>
      <cons>One more plan and one more review pass.</cons>
    </option>
    <option id="split-phase">
      <name>Recommend a phase split, with the count as the evidence</name>
      <pros>Honest about scope; the operator decides rather than an agent quietly trading rigor for finish-speed.</pros>
      <cons>Phase 1 does not close FLOOR-16 in this pass.</cons>
    </option>
  </options>
  <files>(none — decision only)</files>
  <action>Fire this checkpoint ONLY if task 2 did not reach `npx eslint . --max-warnings 0` exiting 0 inside this plan's budget. Present task 1's measured count and per-rule breakdown to the operator with the three options below, and pause. Do not select an option yourself, do not weaken the gate, and do not blanket-suppress findings while waiting.</action>
  <verify><human-check>Operator selects proceed-in-plan, split-plan, or split-phase, with the measured count pasted alongside.</human-check></verify>
  <done>Either lint is clean at zero warnings and this checkpoint is marked not-required, or the operator has selected an option and it is recorded in the SUMMARY.</done>
  <resume-signal>Select: proceed-in-plan, split-plan, or split-phase. Paste the measured count with your choice.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repo's devDependencies | Three new packages enter the build toolchain. They run on every contributor's machine and in CI. |
| CI gate → what is allowed to merge | A gate that reports but does not block is indistinguishable from no gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P21-SC | Tampering | `eslint`, `@typescript-eslint/parser`, `eslint-plugin-react-hooks` installs | mitigate | All three are dispositioned **Approved** in `01-RESEARCH.md` § Package Legitimacy Audit — canonical / first-party React / canonical typescript-eslint, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. Re-verify publisher and scope with `npm view` before install; all three are devDependencies only; the lockfile is regenerated on a clean tree, never hand-edited, with the writer's `node --version && npm --version` recorded and reproducibility proven by `npm ci --ignore-scripts && git diff --exit-code package-lock.json`. The npm-major clause of D-07 was **withdrawn** (npm 9/10/11 all emit `lockfileVersion: 3`, so it was unverifiable) — it is not a mitigation here and must not be reinstated. |
| T-P21-01 | Tampering | a preset silently adopting ~17 React Compiler rules in a future minor | mitigate | The config names both rules explicitly and uses no preset; `test/ci-config.test.cjs` asserts the file declares exactly two rules. |
| T-P21-02 | Repudiation | a lint gate that reports warnings and merges anyway | mitigate | `--max-warnings 0` (D-30) plus `reportUnusedDisableDirectives: 'error'`; both the flag and the absence of `continue-on-error` are asserted by a parsed test. |
| T-P21-03 | Repudiation | a third `continue-on-error` appearing in `ci.yml` | mitigate | The parsed test asserts the exact pre-existing count of **2** parsed `continue-on-error: true` declarations (the raw string occurs 4 times; two are comments), so a new one fails the suite on the PR that adds it. |
| T-P21-04 | Repudiation | suppressing findings to make the gate green | mitigate | `rules-of-hooks` errors must be fixed, never suppressed; every `exhaustive-deps` suppression carries a one-sentence reason; the decision checkpoint exists so "too much work" is reported rather than silently absorbed. |
| T-P21-05 | Tampering | modifying a test so a lint-driven source change passes | mitigate | The per-commit containment filter over this task's own SHAs admits only `src/main/{knowledge,nodeInstall,slack}.ts` and `src/renderer/src/**/*.{ts,tsx}`, so a committed test edit fails it by construction; `git show --stat` for each SHA is pasted. |
| T-P21-06 | Denial of service | `@typescript-eslint/parser` pulling in type-aware linting cost | accept | The parser is configured as a parser only, with zero rules and no `project` option, so no type information is requested and no typescript-eslint rule runs. |
| T-P21-07 | Tampering | wave 8 silently invalidating plan `01-23`'s wave-9 FLOOR-12 allowlist by shifting `file:line` anchors | mitigate | The two-plan line-anchor contract in `<interfaces>`. Half 1 is asserted here: `git diff -U0 "$BASE" -- src/renderer/src \| grep "^[-+]" \| grep "fontSize *[:=]"` must be empty. Half 2 lives in plan `01-23`, which keys its allowlist on content and re-derives it at wave 9. Neither half may be changed without the other. |
| T-P21-08 | Tampering | task 2's measurement-driven renderer scope quietly growing into unrelated files | mitigate | The declared bound is `src/main/{knowledge,nodeInstall,slack}.ts` plus `src/renderer/src/**/*.{ts,tsx}`, and a per-commit filter over this task's own SHAs (`git show --name-only --format=`) must return empty. It is scoped per-commit rather than tree-wide because plan `01-22` commits into the same tree in the same wave. Widening the bound is a plan amendment, never an executor call. |
| T-P21-09 | Repudiation | a formatter that does not exist in ESLint 9 making the measurement step unrunnable, and the executor "fixing" it by installing a third-party republish | mitigate | Amendment C: `--format compact` is removed from ESLint 9 core and `eslint-formatter-compact` is an unofficial republish outside the legitimacy audit. The plan specifies the built-in `json` formatter. The check is that the formatter was never *invoked* (no pasted `eslint … --format compact` command line) and that `eslint-formatter-compact` appears in neither `dependencies` nor `devDependencies` — not that the phrase is absent, which would trap a SUMMARY quoting this very row. |
</threat_model>

<verification>
- `npx eslint . --max-warnings 0` exits `0` locally and in CI.
- `npm test` and `npm run typecheck` green on all three platforms.
- CI green on the remote with the lint step visible in the `typecheck` job; run URL recorded.
- The nine-file pairing command in task 2 prints nine `OK` lines.
- Task 2's `fontSize` diff filter and containment filter both print nothing.
</verification>

<success_criteria>
- ROADMAP criterion 5's lint clause is TRUE: ESLint runs, and the 13 orphaned `eslint-disable`
  comments are resolved — the four `@typescript-eslint/*` ones deleted, and each of the nine
  `react-hooks/exhaustive-deps` ones either made live (ESLint still reports the finding under it) or
  deleted as unused (ESLint reports the directive itself unused), with the resolver run pasted per file.
- The rule surface is exactly two rules, so D-28's bounded-surface guarantee is mechanically true and
  D-31's rejection of the full ruleset and the React Compiler rules is preserved.
- Issue #36 closes in this PR per D-43: acceptance text verbatim, one command per clause with pasted
  output (including the measured finding count), the named tests in `test/ci-config.test.cjs`, and the
  `npm test` exit line.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-21-SUMMARY.md` when done. It MUST record the measured
finding count with its per-rule breakdown (closing RESEARCH Open Question 1), the ESLint major chosen
and why, what the installed plugin's flat preset actually contains (closing assumption A1), the
amendment-D resolver output for all nine suppressions with the kept/deleted disposition of each and its
reason, the per-commit touched-file list (`for sha in $SHAS; do git show --stat "$sha"; done`) with both
containment filters shown empty, the lockfile writer's `node --version && npm --version` with the
`npm ci --ignore-scripts && git diff --exit-code package-lock.json` result, and the pre-existing `continue-on-error` count now pinned by the test. It must contain no pasted
`eslint … --format compact` command line, and `eslint-formatter-compact` must appear in neither
`dependencies` nor `devDependencies` — explaining in prose why that formatter is unavailable is
expected, not forbidden.
</output>
