---
phase: 01-finish-the-floor
plan: 09
type: execute
wave: 4
depends_on: ["01-06"]
files_modified:
  - src/main/breaker.ts
  - src/main/hive.ts
  - test/engine-parity.test.cjs
  - test/hive-protocol-v2.test.cjs   # only if task 3's D-22 row-shape test lands here rather than in engine-parity; plan 06 (wave 3) released it
# NOT modified here, and deliberately so: src/main/index.ts. `runBreakerBeat` (`:1492-1537`) is the
# SOLE production constructor of `BreakerInput` (verified: `grep -rn "BreakerInput" src/main/*.ts`
# returns the interface, `tick()`, `evaluate()`, the import, and that one `const inputs:
# BreakerInput[] = []`), so without one line there the budget arm is an optional field nothing ever
# sets. index.ts is T-INDEX: plan 08 holds it in wave 4 (this wave) and plan 10 holds it in wave 5.
# The line cannot go to plan 08 either — it calls `hive.budgetForAgent()`, a method THIS plan mints
# in wave 4, so a same-wave sibling applying it would break `npm run typecheck` whichever of the two
# landed first. It is therefore HANDED TO PLAN 10 BY NAME, in wave 5, in task 2's <handoff>; the
# wiring test travels inside that handoff and lands in plan 10's own commit alongside the line it
# asserts, so the suite is never red between waves and this plan closes green with the arm unfed.
autonomous: true
# **FLOOR-10 IS SPLIT ACROSS TWO PLANS AND IS NOT CLOSED BY THIS ONE.** This plan (wave 4) ships the
# breaker arm, the widened `hive:tasks` row, `hive.budgetForAgent()` and the recorded handoff.
# **Plan 01-10 (wave 5) is the designated closer:** it lands the one `runBreakerBeat` line that feeds
# the arm in production together with the wiring test that asserts it, in the same commit. Plan 10's
# own frontmatter declares `requirements: [FLOOR-07, FLOOR-05]` and does NOT name FLOOR-10 — so any
# requirement→plan rollup reading the line below MUST NOT mark FLOOR-10 complete at the end of wave 4.
# Plan 23 pins the wiring permanently in wave 9.
requirements: [FLOOR-10]   # SPLIT — 01-09: arm + accessor + handoff. 01-10: production wiring (closer).

must_haves:
  truths:
    - "The breaker ladder gained an arm that consumes taskSpend().over: an over-cap BreakerInput escalates the assignee to constrained across two beats. Whether a REAL beat feeds that arm is plan 01-10's truth, not this plan's — FLOOR-10 is NOT closed here"
    - "hive.budgetForAgent(agentId) exists as the single accessor the production beat will call, so plan 01-10's edit to src/main/index.ts is exactly one line"
    - "The one-line production injection AND the wiring test that reads src/main/index.ts are recorded verbatim in 01-09-SUMMARY.md under the heading T-INDEX HANDOFF → 01-10 (FLOOR-10), carrying the literal status line FLOOR-10 BUDGET ARM NOT FED IN PRODUCTION — T-INDEX INJECTION HANDED TO 01-10, so the open window is visible in writing rather than hidden behind green unit tests"
    - "A card given a small cap and run long reaches constrained; a card given a generous cap is not stopped early"
    - "Enforcement is per-agent, not a floor-wide trip — only the assignee is affected"
    - "Nothing is silently killed: hardStop stays false, so the ceiling is constrained (paused and reported)"
    - "The card's meter data reaches the renderer through the channel it already polls"
  artifacts:
    - path: "src/main/breaker.ts"
      provides: "one budget arm in the existing evaluate() chain, placed between the (b) storm arms and the (a) floor-wide caps"
      contains: "budget"
    - path: "src/main/hive.ts"
      provides: "hive:tasks rows widened with {tokens, budgetTokens, pct}"
      contains: "pct:"
    - path: "src/main/hive.ts"
      provides: "budgetForAgent(agentId) — the one accessor the beat calls, so index.ts's edit is a single line"
      contains: "budgetForAgent"
    - path: "test/engine-parity.test.cjs"
      provides: "a two-beat budget test asserting the FINAL level and the reason string, plus the wiring test handed to plan 10 in the <handoff> so it lands in the same commit as the line it asserts"
  key_links:
    - from: "src/main/hive.ts taskSpend()"
      to: "src/main/breaker.ts evaluate() budget arm"
      via: "hive.budgetForAgent(agentId) → BreakerInput.budget, populated in runBreakerBeat (the index.ts line handed to plan 10)"
      pattern: "budgetForAgent"
    - from: "hive.tasks() row"
      to: "the renderer's existing hive:tasks poll"
      via: "widened row — no new IPC channel"
      pattern: "pct:"
---

<objective>
Make a per-task token budget **enforced** rather than merely reported, as one new arm in the existing
breaker ladder — now that plan 06 has made the number it enforces against correct.

Purpose: FLOOR-10. `taskSpend().over` currently has no consumer. Enforcement is not a second state
machine: `src/main/breaker.ts` is already per-agent, already wired to `control:breakerState` → the
renderer's avatar/meter, already emits a native operator toast, already calls `forget()` on teardown,
and already ships `hardStop: false`. One arm on a path that is otherwise complete.

Output: a budget arm, `BreakerInput` widened, the `hive:tasks` row widened so the card can show a
meter through a channel the renderer already polls, and a test that ticks twice.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@.planning/phases/01-finish-the-floor/01-06-SUMMARY.md

<interfaces>
**Line numbers below are informational pointers, never assertion keys.** Plan 03 (wave 2) deletes the
`!previous.has(task.id)` clause at `src/main/hive.ts:1772` and rewrites the surrounding mint, and plan
06 (wave 3) rewrites `taskSpend` and adds a startup rescan — so every `hive.ts` offset in this
document has shifted at least twice before this plan runs. Re-derive by content match before any read
or edit, and use the number the grep prints:

```
grep -n "  tasks(): unknown" src/main/hive.ts
grep -n "taskSpend(taskId: string)" src/main/hive.ts
grep -n "interface BreakerInput\|repeatedToolLimit\|errorStormLimit\|agentTokenCaps" src/main/breaker.ts
grep -n "const inputs: BreakerInput\[\]" src/main/index.ts
```

Never write a `sed -n 'N,Mp'` with a number copied from this document. The one place line numbers ARE
load-bearing — task 2's arm-placement criterion — compares numbers **both re-derived in the same
command run**, never one re-derived against one written here.

**Where `BreakerInput` is actually built.** Verified this session:
`grep -rn "BreakerInput" src/main/*.ts` returns five hits — the interface (`breaker.ts:51`),
`tick()`'s parameter (`:228`), `evaluate()`'s parameter (`:300`), the type-only import
(`index.ts:37`), and `const inputs: BreakerInput[] = []` inside `runBreakerBeat`
(`index.ts:1492-1537`). **That last one is the sole production constructor**, and
`grep -rn "\.over\b" src/ --include=*.ts` returns nothing at all — `taskSpend().over` has no
consumer anywhere today. So a `budget?` field that only task 3's test ever populates is dead code
with a green test beside it. Task 2's `<handoff>` is what prevents that.

**The breaker does not work the way D-17 describes, and this is the single most important thing to
read before writing the arm.** From `src/main/breaker.ts:269-294`:

1. `evaluate()` returns `{tripping, reason}` — a **boolean, not a level**. A new arm can only say
   "trip" or "don't"; it cannot target `constrained` directly.
2. Escalation is **one rank per beat** from the current level:
   `target = LEVELS[Math.min(rank(s.level) + 1, rank(ceiling))]` where
   `LEVELS = ['healthy','steering','constrained','stopped']`. A healthy agent that goes over budget
   reaches `steering` on the first beat and `constrained` on the **second**.
3. `hardStop: false` (default at `:85`) caps the ceiling at `constrained` (`:277`). D-18 is therefore
   already the shipped behaviour — nothing gets killed, and the budget arm inherits that for free with
   no new guard.

`evaluate()` is `private`, pure, has an eight-argument signature, and its arms are an ordered
`if`-chain with early `return`. Placement decides which `reason` string the operator sees when two
conditions trip at once. Existing arm order (`:309-330`): repeated-tool-call arm, then error-storm
arm, then the per-agent token cap. **Put the budget arm after the two `(b)` storm arms and before the
floor-wide `(a)` caps** — a looping agent is a more urgent diagnosis than an expensive one, and a
per-card cap is more specific than a floor total.

`BreakerInput` (`:50-57`): `{ agentId, sample: AgentUsageSample | null, progressing: boolean }` —
D-17 widens it with `budget: {taskId, tokens, cap}`.

The `steering` arm's mail-to-assignee side effect does **not** belong in `evaluate()`; that function
is pure by design and `actionFor(level)` → `'steer'` is the established channel for side effects.

`hive:tasks` row: `ipcMain.handle('hive:tasks', () => hive.tasks())` at `index.ts:3894` — a
one-liner. The widening happens inside `hive.tasks()`. **Verified this session:
`src/preload/index.ts:779` is `hiveTasks: (): Promise<unknown> => ipcRenderer.invoke('hive:tasks')`
— the return type is already `unknown`, so this plan needs NO preload edit.** Do not open
`src/preload/index.ts`; plan 08 holds it this wave.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-10 clause against current source, and confirm the corrected arithmetic from plan 06 is in</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/breaker.ts` in full, with attention to `BreakerInput` `:50-57`, the config defaults around `:85`, the escalation block `:269-294`, and the arm chain `:299-362`.
    - `src/main/hive.ts` at `taskSpend` (`:2566`) as plan 06 left it, and at `tasks()`.
    - `test/engine-parity.test.cjs:40-100` — `makeBreaker` at `:46-51` and the two-beat pattern at `:70-83`.
    - `test/breaker.test.cjs:1-40` and `:215-224` — read enough to confirm it is NOT a `node:test` file. It is one of eight such hand-rolled harnesses, not the only one; the action below enumerates the full set.
    - `.planning/phases/01-finish-the-floor/01-06-SUMMARY.md` — the arithmetic contract this plan enforces against.
    - `01-RESEARCH.md` § "Breaker Ladder Mechanics" and § Pitfall 6.
  </read_first>
  <action>
    Quote issue #34's acceptance text verbatim and run one command per clause with ACTUAL output
    pasted:
    - `grep -rn "taskSpend" src/ --include=*.ts` and `grep -rn "\.over\b" src/ --include=*.ts` —
      expect the definition plus, before this plan, **no consumer of `.over` at all** (the second grep
      returned nothing this session). The absence of a consumer IS the evidence the clause is open
      (D-42's four mis-assessed issues included exactly this one).
    - `grep -rn "BreakerInput" src/main/*.ts` — expect five hits and exactly ONE production
      constructor, `const inputs: BreakerInput[] = []` in `runBreakerBeat`. Paste it and record the
      re-derived line number as **B-beat**. This is the site task 2 hands to **plan 10 (wave 5)**, not
      to a wave-4 sibling; if this grep shows a second production constructor, the handoff must name
      both before task 2 proceeds.
    - `grep -n "over" src/main/hive.ts` at the `taskSpend` region — what the returned object actually
      carries today.
    - `grep -n "budget\|budgetTokens\|budget-tokens" src/main/*.ts src/shared/*.ts` — where a per-task
      cap is configured today, if anywhere. If no cap field exists on the task shape, its creation is in
      scope for task 2 and must be named here.
    - Enumerate the hand-rolled harnesses — every test file that defines its own `test()` instead of
      importing one, where a `node:test` registration would be shadowed and never run:
      `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done`
      Expect exactly eight lines: `test/agent-provider.test.cjs`, `test/breaker.test.cjs`,
      `test/kg-core.test.cjs`, `test/proc-kill.test.cjs`, `test/realtime-findcard.test.cjs`,
      `test/slack.test.cjs`, `test/transcript-usage.test.cjs`, `test/voice-messages.test.cjs`.
      Confirm this whole set before writing any test — `breaker` is the one this plan sits next to,
      but the trap is identical in all eight, and the plans previously named only `breaker`.
      Also record the denominator: `git ls-files | grep -cE '\.test\.(c|m)?[jt]s$'` prints `56`
      post-merge (pre-merge artifacts said `55`), so 8 of 56 files are hand-rolled and 48 are
      `node:test`. Label these B-handrolled (`8`) and B-testfiles (`56`).

    **Record the two count baselines task 2's criteria are deltas against.** Both tokens already occur
    in these files for unrelated reasons, so a bare `at least N` criterion would be satisfied by doing
    nothing. Run and paste, verbatim:
    - `grep -c "budget" src/main/breaker.ts` (expect `2` — a FLOOR-budget doc comment at `:65` and a
      "user-facing budget" comment at `:255`; **neither is code**)
    - `grep -c "budgetTokens" src/main/hive.ts` (expect `8` — the card field, `taskSpend`'s
      signature and body, and the two front-matter parsers; **none of these is the `tasks()` row**)
    Label these B-breaker and B-hive. Task 2 asserts **strictly greater than** these numbers.

    Then confirm plan 06 landed: `taskSpend` must be computing clamped consecutive diffs over all
    rows, with no `COST_TAIL_BYTES` in its path. Paste the relevant lines. **If it is not in, stop and
    report** — enforcing a cap against the old number is worse than having no cap, which is exactly why
    RECORD-03/04 were pulled forward as prerequisites.

    **Then gate plan 06's T-INDEX handoff, which this plan is the designated closer of.** Plan 06
    (wave 3) minted `HookServer`'s `recordCost` sink but could not write `src/main/index.ts` — plan 07
    held T-INDEX that wave — so it handed one argument to plan 07 by name and recorded the status
    verbatim in `01-06-SUMMARY.md`. Wave 3 is complete before this task runs, so the check is
    deterministic here in a way it never was inside wave 3. Run and paste:
    - `grep -n "recordCostSample" src/main/index.ts` — must return at least one hit, inside the
      `new HookServer(` argument list.
    - `grep -c "T-INDEX HANDOFF → 01-08 (FLOOR-09)" .planning/phases/01-finish-the-floor/01-06-SUMMARY.md` — must return `1`.

    **Record the result; do NOT gate on it.** Plan 06's FLOOR-09 injection is applied by **plan 08, in
    this same wave 4** — so at the moment this task runs it may legitimately not be there yet, and a
    STOP here would fire on correct work. Write the observed state into the notes as
    `FLOOR-09 INJECTION: PRESENT` or `FLOOR-09 INJECTION: NOT YET (01-08, same wave)`. Do not apply it
    yourself — `index.ts` is plan 08's this wave, and a second writer in one wave is a lost update.
    The hard gate lives one wave later, in plan 10 task 5 (wave 5, after wave 4 has closed), and
    permanently in plan 23's wave-9 pins. That placement is deliberate: a budget arm enforcing against
    a number that silently excludes every qwen/crush agent's spend is the same "worse than no cap"
    failure as enforcing against the pre-plan-06 arithmetic, so it must be gated — just not from
    inside the wave that is still producing it.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all three clause greps plus the hand-rolled enumeration.
    - Task notes record the two labelled baselines B-breaker (`2`) and B-hive (`8`) with pasted command output.
    - **Task notes record `B-sha`, the base commit this plan's containment criteria diff against**, so task 2's range is bound to a real SHA rather than to a `<base>` placeholder that expands to nothing: run `git rev-parse HEAD` and paste it verbatim. An unbound range makes `git diff` go fatal with empty stdout, which a "must be empty" criterion reads as PASS.
    - **Task notes record `B-hardstop`, the `hardStop` occurrence count in `src/main/breaker.ts`**: `grep -c "hardStop" src/main/breaker.ts` — measured `4` this session. Task 2 asserts equality against this recorded number, because "the same value as before this task" cannot be checked once the edit has landed.
    - The `taskSpend` consumer grep is pasted and shows no consumer of `.over`, establishing the clause as open. `grep -rn "\.over\b" src/ --include=*.ts` returns nothing (measured baseline this session).
    - The `BreakerInput` grep is pasted, shows exactly one production constructor, and B-beat records its re-derived line number.
    - Plan 06's FLOOR-09 injection state is **recorded, not gated**: paste `grep -c "recordCostSample" src/main/index.ts` and the `01-06-SUMMARY.md` handoff-heading grep, and write `FLOOR-09 INJECTION: PRESENT` or `FLOOR-09 INJECTION: NOT YET (01-08, same wave)`. Plan 08 applies it in this same wave, so either value is a legitimate observation here and neither stops this plan. Plan 10 task 5 (wave 5) turns it into a hard STOP-gate once wave 4 has closed, and plan 23 pins it in wave 9.
    - Task notes confirm, with pasted lines, that plan 06's clamped-diff arithmetic is in place and `COST_TAIL_BYTES` is out of the `taskSpend` path.
    - The hand-rolled enumeration's output is pasted and lists exactly the eight files above — B-handrolled is `8`, B-testfiles is **re-measured here, not asserted as `56`**: `56` is the pre-phase count, but plan 05 (wave 2) adds `test/repo-claims.test.cjs` and plan 08 (wave 4) adds `test/queue-delivery.test.cjs`, so a wave-4 run legitimately measures `57` or `58` and a hard `56` is guaranteed to trip on correct work. Record the measured value and assert only that it is **not lower** than 56. B-handrolled `8` IS a hard equality — the hand-rolled set must not grow. If the hand-rolled number differs, the inventory has moved and the plan's harness claims must be re-derived before proceeding.
    - **The test baseline is recorded under the TAP reporter, not the exit code.** `node --test` counts skipped tests in its total and exits `0`, so N `{skip: '…'}` stubs satisfy any "test count at least N higher" criterion (verified at plan time: two skipped tests give `EXIT=0`, `# pass 0`, `# skipped 2`). Run and paste both files, since plan 06 has moved both since this plan was written:
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      `TAP=$(mktemp); node --test --test-reporter=tap test/hive-protocol-v2.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Also record **B-nan**, the baseline task 3's degenerate-value criterion is a strict delta against: `grep -c "NaN\|Infinity" test/hive-protocol-v2.test.cjs` — measured `1` this session, and plan 06 has touched the file since, so re-measure and paste. Without this number written down, "strictly greater than the baseline" is unfalsifiable after the edit lands.
      Required: `EXIT=0`, `# fail 0`, `# skipped 0`, `# todo 0` on both. Record the two `# pass` numbers as **B-pass-ep** and **B-pass-hp**; task 3 asserts deltas against those, never against `# tests`. (Pre-plan-06 measurements were `14` and `12`; plan 06 adds to both, so re-measure — do not reuse those numbers.) Name the TAP reporter explicitly: the default (spec) reporter prints `ℹ skipped 0`, so a criterion grepping `^# ` against it matches nothing and passes vacuously.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>Every clause is classified with pasted evidence, the prerequisite arithmetic is confirmed in place, and all eight hand-rolled harnesses are enumerated before any test is written.</done>
</task>

<task type="auto">
  <name>Task 2: Add the budget arm to the existing ladder and widen the hive:tasks row</name>
  <files>src/main/breaker.ts, src/main/hive.ts</files>
  <read_first>
    - `src/main/breaker.ts` in full — the file being modified; the arm chain's exact ordering and each arm's `reason` string style.
    - `src/main/hive.ts` at `tasks()` and `taskSpend()` — the file being modified.
    - `01-PATTERNS.md` § "the spend cluster" `:765-812` — the verbatim arm-chain excerpt, the `BreakerInput` interface, and the one-level-per-beat block at its line.
    - `src/main/config.ts` around the circuit-breaker defaults — where `hardStop` and the caps live.
  </read_first>
  <action>
    Widen `BreakerInput` with `budget?: { taskId: string; tokens: number; cap: number }`. Optional, so
    an agent with no card in flight is unaffected and every existing caller keeps compiling.

    Add one arm to `evaluate()`, placed **after** the repeated-tool-call and error-storm arms and
    **before** the per-agent token cap arm. Two thresholds, per D-17, expressed as trips because
    `evaluate()` cannot target a level: at >= 80% of cap it trips with a `reason` naming the card, the
    spend and the cap (the ladder takes the agent to `steering`, whose existing `actionFor` →
    `'steer'` action is the established side-effect channel — that is where the mail-to-assignee
    belongs, NOT inside the pure `evaluate()`); over 100% it trips with a reason naming the overage
    (the ladder reaches `constrained` on the following beat, which is what actually stops active work
    and requires god sign-off). Match the existing arms' `reason` prose style — they are what the
    operator reads.

    Do **not** add a hard-kill path and do **not** change the `hardStop` default. D-18: an agent killed
    mid-edit with unsaved work is precisely the trust failure this product exists to prevent. With
    `hardStop: false` the ceiling is already `constrained` — paused and reported — and the arm inherits
    that with no new guard. Write the comment that says so, citing #34 and D-18, so a future
    contributor does not "finish" it into a kill.

    **Feed the arm — and note that the arm is dead code until it is fed.** `runBreakerBeat`
    (`index.ts`, re-derive with `grep -n "const inputs: BreakerInput\[\]" src/main/index.ts`) is the
    SOLE production constructor of `BreakerInput`, so an optional `budget?` that nothing populates is
    a field only task 3's test would ever set. This plan cannot write `index.ts` — plan 08 holds
    T-INDEX this wave — so shape the work so the edit there is exactly one line, and hand that line
    over.

    In `hive.ts` (this plan's file), mint the single accessor the beat will call:

    ```ts
    /** FLOOR-10 (#34) — the assignee's in-flight card as a breaker budget, or null when the agent
     *  has no card or the card has no cap. One call so the beat's edit is one line. */
    budgetForAgent(agentId: string): { taskId: string; tokens: number; cap: number } | null
    ```

    It resolves the agent's active card (`activeTaskId`, the same resolver `appendCostLedger` already
    uses so the ledger's `task_id` and the enforcement decision can never disagree), calls
    `taskSpend(taskId)`, and returns `null` when `budgetTokens` is unset — **no cap means no budget
    arm, never a surprise trip.** If task 1 found no per-task cap field on the task shape, add one
    here, defaulting to unset.

    <handoff to="01-10" wave="5" token="T-INDEX">
    **FLOOR-10's last line, handed to plan 10 by name — in wave 5, not this wave.** `src/main/index.ts`
    is T-INDEX; plan 08 holds it in wave 4 and plan 10 holds it in wave 5. `use_worktrees` is `false`,
    so two plans editing one file in one wave is a lost update, and this plan must not write that file.
    **It goes to wave 5, not to plan 08, for a second and harder reason:** the property being injected
    calls `hive.budgetForAgent(...)`, a method *this plan mints in wave 4*. A wave-4 sibling applying
    it before this plan's `hive.ts` change lands would call a method that does not yet exist — `npm run
    typecheck` fails, and whichever of the two lands first breaks the tree. Wave 5 is the earliest wave
    where the method is guaranteed to exist. PATTERNS.md `:538-539` prescribes
    this exact mechanism for GATE-01 and FLOOR-07 — "hand them to whichever agent holds T-INDEX that
    wave rather than splitting the requirement".

    The change is one property in the existing `inputs.push({…})` literal inside `runBreakerBeat`:

    ```
        inputs.push({
          agentId: id,
          sample,
    +     budget: hive.budgetForAgent(id) ?? undefined,   // FLOOR-10 (#34) — per-card token cap
          progressing: now - lastCoordinationAt(id) < progressWindowMs || now - lastSpanAt < progressWindowMs
        });
    ```

    `hive` is already in scope there. `budget` is optional, so the tree typechecks whether or not this
    lands — which is exactly why task 3's wiring test exists and why the status must be recorded
    literally rather than assumed.

    Reproduce this diff **verbatim** in `01-09-SUMMARY.md` under a heading that reads exactly
    `T-INDEX HANDOFF → 01-10 (FLOOR-10)`, and record exactly one of these two literal status lines:
    - `FLOOR-10 BUDGET ARM FED IN PRODUCTION` — if `grep -c "budgetForAgent" src/main/index.ts`
      returns at least `1`; or
    - `FLOOR-10 BUDGET ARM NOT FED IN PRODUCTION — T-INDEX INJECTION HANDED TO 01-10` — which is the
      expected value here, since plan 10 is a wave later. This plan is complete with the arm unfed and
      the handoff recorded; what would make it incomplete is failing to record it, or quietly
      populating `budget` from somewhere other than the production beat so the gap stops being
      visible. **FLOOR-10 is not closed at the end of this plan** and this SUMMARY must not say it is
      — plan 10 lands the line and its wiring test together, and plan 23 pins it in wave 9. Mirrors
      plan 02's `QWEN SIDECAR DEAD-HOOKED UNTIL 01-06` marker: a named open window with a named
      closer.
    </handoff>

    Widen `hive.tasks()`'s rows with `{tokens, budgetTokens, pct}` per D-22, so the card gets a meter
    through a channel the renderer already polls. `tasks()` is a bare `return this.ledger();`
    pass-through today (re-derive with `grep -n "  tasks(): unknown" src/main/hive.ts` — plans 03 and
    06 have both moved `hive.ts` since `:1831` was measured), so this is a real map over
    `ledger().tasks`, not a field tweak — keep the `rev` and `updatedAt` envelope intact, because
    every mutation path compare-and-swaps on `rev`. The `ipcMain.handle('hive:tasks', …)` one-liner in
    `index.ts` does not change and **must not be touched** — `index.ts` belongs to plan 08 this wave,
    and the only `index.ts` line this plan needs is the one handed over above.
    `src/preload/index.ts:779` already returns `unknown`, so no preload edit is needed either; if that
    turns out to be false, STOP and report rather than editing preload.

    Keep the accumulator bound plan 06 established — widening the row must not introduce a second,
    unbounded per-card cache.

    **The row shape is D-22's whole deliverable, so it gets a real assertion, not a grep.** No count
    over `hive.ts` can distinguish a widened row from the eight `budgetTokens` occurrences already in
    the file. Task 3 adds the row-shape test; name in this task's notes which existing test file will
    carry it and what the `tasks()` row looks like before and after — paste both literals.
  </action>
  <acceptance_criteria>
    - `grep -c "budget" src/main/breaker.ts` returns **strictly greater than B-breaker + 2** (baseline `2` today, both of which are prose comments, not code) — the `BreakerInput` field, the arm condition and the reason string are three additions on top. Paste the baseline and the new count.
    - `grep -cE "^[^*/]*\bbudget\b" src/main/breaker.ts` returns at least `3` — the same token counted on non-comment lines only, so the two pre-existing doc comments cannot satisfy this criterion.
    - The budget arm's position is correct: in `src/main/breaker.ts`, the line number of the budget arm is greater than the line numbers of the `repeatedToolLimit` and `errorStormLimit` arms and less than the `agentTokenCaps` arm. Derive all four in **one** command run so no number comes from this document — `grep -n "repeatedToolLimit\|errorStormLimit\|agentTokenCaps\|input.budget" src/main/breaker.ts` — and paste its output alongside the ordering conclusion.
    - `grep -c "budgetForAgent" src/main/hive.ts` returns at least `1` (baseline today: `0`), and the accessor returns `null` rather than a zero-cap object when the card has no cap — paste the `return null` line and the test that covers it.
    - `grep -c "hardStop" src/main/breaker.ts` returns **exactly `B-hardstop` = `4`** (measured `4` this session; task 1 re-records it). Paste the recorded baseline and the post-edit count side by side — "the same value as before this task" is unfalsifiable once the edit has landed. The default in `src/main/config.ts` is still `false`: `grep -n "hardStop" src/main/config.ts` shows it, pasted.
    - `grep -c "budgetTokens" src/main/hive.ts` returns **strictly greater than B-hive** (baseline `8` today) — the `tasks()` row literal is an addition on top of the eight occurrences that already exist. Paste the baseline and the new count.
    - The `tasks()` row literal itself is pasted in the task notes and carries all three of `tokens`, `budgetTokens` and `pct`. The mechanical check is task 3's row-shape test, not this grep.
    - **Containment, scoped to this plan's own commits and paths.** A bare whole-tree `git diff --stat` cannot pass here: plan 08 is editing `src/main/index.ts`, `src/preload/index.ts` and `src/main/delivery.ts` in this same wave, in this same working tree, under `use_worktrees: false`. Assert per commit, path-scoped, and paste the output:
      **`BASE` must be bound inside the same command run** — substitute the literal `B-sha` value task 1 recorded, and keep the guard, because an unbound range makes `git diff` exit fatal with empty stdout and a "must be empty" criterion reads that as PASS:
      `BASE=<paste B-sha from task 1>; test -n "$BASE" && git rev-parse --verify "$BASE^{commit}" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD) && test -n "$SHAS" || { echo "UNBOUND OR EMPTY RANGE — CRITERION FAILS"; false; }`
      Then, with `BASE` and `SHAS` bound in that same run:
      `for s in $SHAS; do echo "== $s"; git show --stat "$s" -- src/main/index.ts src/preload/ src/main/delivery.ts; done` — every block shows no files, and
      `git diff --name-only "$BASE"..HEAD -- src/main/breaker.ts src/main/hive.ts test/engine-parity.test.cjs test/hive-protocol-v2.test.cjs` lists only files from `files_modified`. Paste the guard line's output alongside both, so a fatal-and-empty run cannot be mistaken for a clean one.
    - `npm run typecheck` exit code is `0`.
    - `npm test` exit code is `0`.
    - `01-09-SUMMARY.md` carries the `T-INDEX HANDOFF → 01-10 (FLOOR-10)` heading with the diff reproduced verbatim and one of its two literal status lines. `grep -c "T-INDEX HANDOFF → 01-10 (FLOOR-10)" .planning/phases/01-finish-the-floor/01-09-SUMMARY.md` returns `1`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>One budget arm sits in the correct position in the existing ladder, nothing is silently killed, and the card's meter data rides the channel the renderer already polls.</done>
</task>

<task type="auto">
  <name>Task 3: Prove enforcement with a two-beat test in a file that actually runs</name>
  <files>test/engine-parity.test.cjs</files>
  <read_first>
    - `test/engine-parity.test.cjs` in full — `makeBreaker` at `:46-51`, the two-beat pattern at `:70-83`, and the exact-string `capabilityLine` assertions at `:134-155` (do not disturb them; plan 13 owns those).
    - `test/breaker.test.cjs:30-40` and `:215-224` — the local `test()` at `:35` and the terminal `process.exit(failures ? 1 : 0)` at `:224`.
    - `01-PATTERNS.md` § "⚠ The breaker test is a different animal" `:814-865` — the full explanation and the two acceptable options.
    - `01-RESEARCH.md` § Pitfall 6 — a budget test with exactly one `tick()` asserts the wrong level and gets "fixed" by weakening the assertion.
  </read_first>
  <action>
    Write the FLOOR-10 test in `test/engine-parity.test.cjs`, which is a real `node:test` file and
    already builds a `CircuitBreaker` with `makeBreaker` and already ticks twice.

    **Do not put it in `test/breaker.test.cjs`.** That file defines its own `test()` at `:35`, has no
    `require('node:test')`, and ends `process.exit(failures ? 1 : 0)` at `:224` — a `node:test`
    registration added there is shadowed by the local function and killed by the exit before it runs,
    producing a green run that asserted nothing. (The alternative — following that file's synchronous
    hand-rolled idiom — is acceptable in principle but pointless here, since `engine-parity` already
    has the helper.)

    Tests to add:
    1. **Over-cap reaches `constrained` after two beats.** A card with a small cap and spend over 100%.
       Tick once, assert `steering`. Tick again, assert `constrained` **and** assert the `reason`
       string matches the budget arm's wording. Asserting only after one tick is Pitfall 6 and will get
       "fixed" by weakening the assertion — assert the FINAL level explicitly.
    2. **A generous cap does not trip.** The same spend against a large cap stays `healthy` across two
       beats. This is criterion 3's "a card given a generous cap is not stopped early by double
       counting" — and it only means anything because plan 06 removed the double count.
    3. **80% steers but does not constrain.** Spend at 85% of cap: after two beats the level is
       `steering`, not `constrained`, because the arm is threshold-dependent, not just tripping.
    4. **Per-agent, not floor-wide.** A second agent with no card in flight stays `healthy` in the same
       tick. This is D-17's "the breaker is already per-agent, so this affects only the assignee".
    5. **`hardStop: false` never reaches `stopped`.** Tick four times over-cap and assert the level
       never exceeds `constrained`.
    6. **The `hive:tasks` row carries the meter (D-22).** Build a hive with one card that has a
       `budgetTokens` cap and recorded spend, call `tasks()`, and assert the returned row has all
       three of `tokens`, `budgetTokens` and `pct` — by key presence **and** by value, with `pct`
       equal to `tokens / budgetTokens` at the row's own numbers. Assert a second card with **no**
       cap returns `budgetTokens: null` and a `pct` of `null` rather than `NaN` or `Infinity`
       (`taskSpend` already returns `null` for an unset cap; a naive division would produce `NaN`
       and render as a blank meter). Without this test D-22 could be skipped in full and every
       other criterion in this plan would still pass — no grep over `hive.ts` can tell a widened
       row from the eight `budgetTokens` occurrences that predate this phase.
       If `engine-parity` cannot construct a hive cheaply, put this one test in the hive-protocol
       file plan 06 already touches and say so in the notes — but it ships in this plan, not later.
    7. **The arm is actually fed in production (the wiring test).** Tests 1-5 each construct their own
       `BreakerInput`, so all five stay green if `runBreakerBeat` never populates `budget` — which is
       the exact shape of dead code behind a green suite. Add a test that reads the real source:

       ```js
       const main = fs.readFileSync(path.join(root, 'src/main/index.ts'), 'utf8');
       assert.match(main, /budget:\s*hive\.budgetForAgent\(/,
         'the breaker beat must populate BreakerInput.budget, or the budget arm is an optional field nothing ever sets');
       ```

       This is an established idiom in this repo, not an invention: `test/hive-task-mutation.test.cjs`
       (`'renderer task actions never send a whole stale ledger back to main'`) already
       `readFileSync`s `src/main/index.ts` and `src/preload/index.ts` and asserts on their text for
       exactly this class of claim — "the production call site does the right thing", where no unit
       test can reach. Re-read that test before writing this one and match its style.

       **Slice and comment-strip before matching — mandatory, not conditional.** `src/main/index.ts` is
       ~5,600 lines, so an unsliced `assert.match` is satisfied by any mention anywhere, including a
       comment like `// budget: hive.budgetForAgent(id) — pulled in wave 6`. Anchor on
       `const inputs: BreakerInput[]`, take the following ~2000 characters, strip `//` and `/* */`
       comments from that slice, and match inside it. The sibling FLOOR-09 pin in plan 08 makes the
       same requirement for the same reason; the two must not diverge.

       **Write it, but do NOT land it in this plan — hand it to plan 10 with the injection.** The
       injection it asserts is plan 10's, in wave 5 (see task 2's `<handoff>` for why it cannot be a
       wave-4 edit). If this test shipped here it would be red from the moment wave 4 closes until
       plan 10 runs — and a red `npm test` is a criterion in every wave-5 plan, so one coordination
       gap would fail four unrelated plans. Instead: reproduce the test verbatim inside the
       `<handoff>` block so plan 10 lands the assertion and the code it asserts **in the same commit**,
       and record in this SUMMARY that it is pending. Nothing is weakened — the assertion still ships,
       still cannot be skipped, and plan 23 pins it permanently in wave 9. What changes is only that
       it arrives together with the line it checks, instead of ahead of it.

    Every assertion carries a message stating what a failure would MEAN.
  </action>
  <acceptance_criteria>
    - **Test counts are asserted from the TAP reporter's `# pass` line, never from the exit code or from `# tests`.** `node --test` counts skipped tests in its total and exits `0`, so seven `{skip: '…'}` stubs would satisfy a "test count at least 7 higher" criterion outright (verified at plan time: two skipped tests give `EXIT=0`, `# pass 0`, `# skipped 2`). Run and paste:
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least B-pass-ep + 6** (5 breaker tests and the D-22 row-shape test; the wiring test lands with plan 10, not here), `# fail 0`, **`# skipped 0`**, `# todo 0`. If the row-shape test landed in the hive-protocol file instead, this file's floor is B-pass-ep + 5 and that file's is B-pass-hp + 1 — run both under TAP and paste both sets of counters. Name the TAP reporter explicitly: the default (spec) reporter prints `ℹ skipped 0`, so a criterion grepping `^# ` against it matches nothing and passes vacuously.
    - No bypass construct exists in the files this task touched: `grep -cE "\.skip\(|\.todo\(|skip:|todo:" test/engine-parity.test.cjs` returns `0` (and the same for `test/hive-protocol-v2.test.cjs` if the row-shape test landed there). **The fake this catches:** closing one of the six tests that DO land here with `{ skip: 'pending 01-10' }` — most dangerously the D-22 row-shape test, whose deliverable no grep over `hive.ts` can verify. (The wiring test is not in this file at all: it ships inside the `<handoff>` and lands with plan 10, so a `skip` bearing its name here is doubly wrong.)
    - The wiring test is **written into the `<handoff>` block, not into the test file**: `grep -c "budgetForAgent" .planning/phases/01-finish-the-floor/01-09-SUMMARY.md` returns at least `1` and the SUMMARY's handoff section contains the verbatim `assert.match(main, /budget:\s*hive\.budgetForAgent\(/` line. It must NOT be in `test/engine-parity.test.cjs` yet: `grep -c "budgetForAgent" test/engine-parity.test.cjs` returns `0` here — plan 10 adds it in the same commit as the injection it asserts, so the suite is never red between waves.
    - The wiring test's negative control was run **without touching `src/main/index.ts`** — that file belongs to plan 08 this wave and must not be edited even temporarily. Point the assertion's regex at a token known to be absent (e.g. `budgetForAgentXX`) in the test file this plan owns, paste the **red** output showing a non-zero exit, then restore. A wiring test that cannot be made to fail has not been demonstrated to be a test.
    - `grep -c "require('node:test')" test/breaker.test.cjs` still returns `0` and `grep -c "process.exit" test/breaker.test.cjs` still returns `1` — both are today's measured baselines, so this is **a preservation criterion**: the hand-rolled file must not be touched.
    - The over-cap test contains at least two `tick(` calls; paste the test body's tick count.
    - `grep -c "'constrained'" test/engine-parity.test.cjs` returns at least `2`.
    - The D-22 row-shape test exists and fails when any one of `tokens`, `budgetTokens` or `pct` is removed from the `tasks()` row — demonstrate by deleting one key, pasting the red output, and restoring it.
    - The no-cap case asserts `budgetTokens === null` and `pct === null`; `grep -c "NaN\|Infinity" test/hive-protocol-v2.test.cjs` returns **strictly greater than `B-nan`, the number task 1 recorded** (measured `1` this session; plan 06 has touched the file since, so the recorded value governs, not this one). Paste `B-nan` and the post-edit count side by side — a bare `at least 1` would pass untouched.
    - **`npm test` exit code is `0`, unconditionally — there is no caveat and no licence to close this wave red.** The wiring test does NOT land here: it travels inside task 2's `<handoff>` and lands in plan 10's commit (wave 5) alongside the line it asserts, so nothing in this plan can be red for want of an injection that has not happened yet. `npm test` exit `0` is also a criterion in every wave-5 plan, so a red suite left behind by this plan would fail four unrelated plans. The expected end state of this plan is: green suite, arm unfed, handoff recorded, `FLOOR-10 BUDGET ARM NOT FED IN PRODUCTION — T-INDEX INJECTION HANDED TO 01-10` written into `01-09-SUMMARY.md`, and **FLOOR-10 not claimed complete**. If `npm test` is non-zero here, something this plan wrote is broken — fix it; never delete, skip or loosen a test to reach green.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>Enforcement is proven across two beats in a file that actually runs, including the negative cases that make the assertion meaningful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cost ledger (agent-influenced) → enforcement decision | The numbers a budget stops work against originate, for the proxy tier, in payloads an agent-adjacent sidecar posts. `hooks.ts`'s `authorized()` gate is what makes them trustworthy; plan 02's per-agent tokens are what make that gate meaningful. |
| breaker level → the operator's toast and the card meter | The only channel by which the human learns work was stopped. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P09-01 | Denial of service | an agent burning the subscription quota overnight | mitigate | The budget arm trips at >= 80% and > 100% of the per-card cap, escalating the assignee to `constrained` — active work stops and god sign-off is required. |
| T-P09-02 | Denial of service | an agent killed mid-edit with unsaved work | accept-by-design | `hardStop` stays `false`, so the ceiling is `constrained` (paused and reported). A hard kill remains available only behind explicit opt-in. Pinned by a test that ticks four times and never reaches `stopped` (D-18). |
| T-P09-03 | Denial of service | a floor-wide trip from one expensive card | mitigate | The breaker is per-agent; the arm reads the assignee's card only. Pinned by the "second agent stays healthy" test. |
| T-P09-04 | Tampering | forged cost data causing a spurious stop | mitigate | Enforcement reads the ledger behind `authorized()`; the `?? 0` coercions and clamped diffs from plan 06 keep garbage from moving the number. Inherited, not re-implemented. |
| T-P09-05 | Repudiation | a test that ticks once and asserts the wrong level | mitigate | Pitfall 6 is called out in the task; the acceptance criteria require at least two `tick(` calls and an explicit `constrained` assertion. |
| T-P09-06 | Repudiation | a budget test added to `test/breaker.test.cjs` that never runs | mitigate | The acceptance criteria assert that file was not modified, and the task states the shadowing/`process.exit` mechanism explicitly. |
| T-P09-07 | Repudiation | the budget arm ships as dead code — an optional `budget?` that only the tests ever populate, because `runBreakerBeat` (the SOLE production `BreakerInput` constructor) was never edited | mitigate | `hive.budgetForAgent()` reduces the production edit to one line; task 2's `<handoff>` gives that line to plan 10 by name with a verbatim diff; the wiring test that reads `src/main/index.ts` travels inside that handoff and lands in plan 10's own commit alongside the line it asserts, following `test/hive-task-mutation.test.cjs`'s existing source-assertion idiom — so the assertion still ships and still cannot be skipped, without a red suite between waves. Plan 10 task 5 gates it; plan 23 pins it. |
| T-P09-08 | Repudiation | a green run that asserted nothing — `node --test` counts skipped tests and exits `0`, so seven `{skip}` stubs satisfy "test count at least 7 higher" | mitigate | Every test-count criterion asserts `# pass` from `--test-reporter=tap` with `# fail 0` / `# skipped 0` / `# todo 0`, and the touched files are checked for `.skip(` / `.todo(` constructs — the specific fake being one of the six landing tests closed with `{ skip: 'pending 01-10' }`, most dangerously the D-22 row-shape test. |
| T-P09-09 | Repudiation | a containment criterion that can never pass — a bare whole-tree `git diff --stat` while plan 08 edits the same tree in the same wave under `use_worktrees: false` | mitigate | Every containment criterion is path-scoped to this plan's declared files or asserted per-commit with `git show --stat <sha> -- <paths>`. |
</threat_model>

<verification>
- `npm test` green LOCALLY, with `test/engine-parity.test.cjs` re-run under `--test-reporter=tap` and
  its `# pass` / `# fail 0` / `# skipped 0` / `# todo 0` counters pasted. `npm test` exits `0` on a
  skipped test, so "green" is only meaningful next to `# skipped 0`.
- **Cross-platform CI is read off the draft PR, never off a phase-branch push.** `ci.yml` and
  `e2e.yml` are both `branches: [main]` only, so pushing this phase branch triggers **no run at all**
  and "green on three platforms" would be an unrunnable claim. Plan 01 task 3 (wave 1) opens the
  phase's draft PR to `main`; resolve against that PR's checks. Run `gh pr checks` and paste the rows
  for the real check-run names — `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`,
  `Test (macos-latest)`, `Build` and `Electron smoke (ubuntu-latest)`. There is **no check run named
  `E2E`** — `E2E` is the workflow name; the job is `Electron smoke (ubuntu-latest)`. An empty table
  means nothing was triggered: report that, do not substitute a local run.
- **The wiring test is NOT in this plan and its absence is the expected state.** It travels inside
  task 2's `<handoff>` and lands in plan 10's commit in wave 5, together with the `runBreakerBeat`
  line it asserts. Verify the handoff instead:
  `grep -c "budgetForAgent" test/engine-parity.test.cjs` returns `0`, and
  `grep -c "T-INDEX HANDOFF → 01-10 (FLOOR-10)" .planning/phases/01-finish-the-floor/01-09-SUMMARY.md`
  returns `1` with the verbatim `assert.match(main, /budget:\s*hive\.budgetForAgent\(/` line inside
  that section. A wiring test found in the test file here is the failure, not its absence.
- `npm run typecheck` green.
- Manual: set a small `budget-tokens` cap on a card in the dev app, let the assignee run, and confirm
  the operator toast fires and the agent's avatar meter reflects the constrained level. This is
  criterion 3 in operator-legible form.
</verification>

<success_criteria>
- The breaker ladder has a budget arm that consumes `taskSpend().over`, correctly placed, proven
  across two beats, and enforcing against the arithmetic plan 06 corrected. `hive.budgetForAgent()`
  exists so plan 10's production edit is one line, and the `hive:tasks` row carries the meter.
- **ROADMAP criterion 3's enforcement clause is NOT yet true in production at the end of this plan,
  and this plan must not claim it is.** `runBreakerBeat` still does not populate `BreakerInput.budget`
  — that line and its wiring test are recorded in the `T-INDEX HANDOFF → 01-10 (FLOOR-10)` section
  and land together in plan 10 (wave 5), which is FLOOR-10's designated closer. Plan 23 pins it in
  wave 9. Success here is: green suite, arm unfed, handoff recorded verbatim, gap stated in writing.
- **Issue #34 does NOT close in this plan.** Its acceptance is the production enforcement path, which
  plan 10 lands; closing it here would file a false completion against the exact clause this phase
  exists to stop overclaiming. Paste the empty `taskSpend` consumer grep as evidence the clause was
  open, and record that #34 remains open pending 01-10. Per D-47, #34 is one of the four previously mis-assessed issues and
  additionally gets a fresh-context adversarial re-verify before closing — an addition to the
  mechanical bar, never a substitute for it.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-09-SUMMARY.md` when done. Record the four arm line
numbers proving placement (all four re-derived in one grep run), whether a per-task cap field had to
be created, and the **path-scoped, per-commit** `git show --stat <sha> -- src/main/index.ts
src/preload/` evidence that neither `index.ts` nor `preload/index.ts` was touched by this plan — never
a bare whole-tree diff, which a wave-mate's concurrent edits would fail regardless of this plan.

It MUST also carry, under a heading that reads exactly `T-INDEX HANDOFF → 01-10 (FLOOR-10)`:
- the one-property diff for `runBreakerBeat` reproduced verbatim from task 2's `<handoff>`;
- the re-derived `grep -n "const inputs: BreakerInput\[\]" src/main/index.ts` line number;
- exactly one of the two literal status lines — `FLOOR-10 BUDGET ARM FED IN PRODUCTION` or
  `FLOOR-10 BUDGET ARM NOT FED IN PRODUCTION — T-INDEX INJECTION HANDED TO 01-10`;
- the FLOOR-09 gate result from task 1 (`grep -n "recordCostSample" src/main/index.ts`), proving the
  number this arm enforces against includes the proxy tier;
- the pasted TAP counters, including `# skipped 0`.
</output>
