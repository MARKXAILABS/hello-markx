---
phase: 01-finish-the-floor
plan: 17
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/CommandCenterPanel.tsx
  - src/renderer/src/components/TasksKanban.tsx
  - src/renderer/src/components/triggers/ui.tsx
  - src/renderer/src/components/IntegrationsRegistry.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the command-centre, task-board, triggers-form and integrations-registry files sits below the 14px floor except allowlisted decorative glyphs"
    - "All four of the phase's NOT-exempt single-character sites are handled as content, not hidden as decoration"
    - "Every icon-only button in these four files has an accessible name; no button with visible text gained an aria-label"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
  artifacts:
    - path: "src/renderer/src/components/TasksKanban.tsx"
      provides: "the empty-column copy replacing a bare em dash, plus the waiting-on-you chip's accessible name"
    - path: "src/renderer/src/components/triggers/ui.tsx"
      provides: "the percent unit label kept visible and named"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the four densest FLOOR-12 files — the command centre, the task board, the triggers form and the
integrations registry — to the corrected type scale, handle **all four** of the phase's NOT-exempt
single-character sites, and give every icon-only button in them an accessible name.

Purpose: FLOOR-12, sweep sub-group 3 of 6 — the densest four files in the renderer, and the sole home
of every special case in the phase. Measured at plan time: **110 sub-14px `fontSize` occurrences across four
files** — `CommandCenterPanel.tsx` 53, `TasksKanban.tsx` 25, `triggers/ui.tsx` 17,
`IntegrationsRegistry.tsx` 14.

These four files were split off from the remaining twelve of the original cluster (now plan 18, 68
sites) because 177 sites in one plan exceeded the context budget: task 2 alone carried more sites than
plans 19 and 20 combined. **Nothing was dropped in the split** — the two plans' file sets are disjoint
and together cover the same sixteen files. Plan 18 runs in the same wave and owns the triggers-tab, git
and IDE files.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract. Its glyph-exemption predicate is decidable and
its four exclusions were each read individually — **a single non-alphanumeric character is not proof of
decoration**, and all four exceptions live in this plan's files.

Output: four files clearing the floor, four special cases handled as content, an accessible-name pass,
and this sub-group's frozen Rule 0 allowlist handed to plan 23.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this sub-group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>`

**This sub-group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`CommandCenterPanel.tsx` `⚠`, `✓`; `IntegrationsRegistry.tsx` `✕`; `TasksKanban.tsx` `✕`;
`triggers/ui.tsx` `{open ? '▾' : '▸'}`. Each gains `aria-hidden="true"` and keeps its size.

**The four NOT-exempt sites — all four in this plan's files, each read individually by the UI checker:**

| Site | Glyph | Why it is content | Action |
|---|---|---|---|
| `TasksKanban.tsx:198` | `—` | It is the **empty-state body** of a Kanban column. `aria-hidden` would make an empty column silent to a screen reader. | Replace with the copy `Nothing here yet` at 14px. **Rule 2 applies.** |
| `triggers/ui.tsx:348` | `%` | A **unit label** beside a numeric input (`:344-347`). Hiding it loses the unit. | Keep the visible `%`, raise to 14px under Rule 2, add `aria-label="percent"`. |
| `TasksKanban.tsx:263` | `?` | A **status chip** — `title="waiting on YOUR answer — see the ASK ME tab"` (`:258`), solid `--cth-lilac` fill (`:261`). It carries meaning and has no accessible name. | Keep the visible `?`, raise to 14px under **Rule 1** (it sets `--cth-font-display` at `:260`), add `aria-label="Waiting on your answer"`. |
| `IntegrationsRegistry.tsx:293` | `.` | Sentence punctuation ending prose inside `<span style={hint}>`, after a `<code>`. Not a glyph element — a residual regex artifact. | **No exemption.** The `hint` style is swept normally. |
| `IntegrationsRegistry.tsx:388` | `{st.dot}` (resolves to `●` / `○` / `▲`, defined `:375-378`) | A genuine glyph-only element, but rendered from a **variable**, so UI-SPEC's literal-only M3 candidate scan (`01-UI-SPEC.md:478`) never saw it. | **No exemption.** This is the phase's only two-occurrences-on-one-line site: the outer `<span>` carries `fontSize: 12` on real text and the inner carries `fontSize: 10` on the dot. Sweep the OUTER text span to ≥ 14px first. Do **not** allowlist `:388` before that edit lands — a line-granular allowlist would mask the text span on the same physical line, which is exactly the hole UI-SPEC's hoisted-object rule exists to close, occurring on one line instead of one shared object. |

`/` is dropped from the glyph vocabulary entirely — a `/` match is a JSX self-close, an arrow-function
return or a template-string artifact, never a decorative element.

**Hoisted shared style objects are NOT exempt.** If task 1 finds a hoisted object in these four files
serving both a text element and a glyph element, sweep the shared declaration normally, give the glyph
element a **local `fontSize` override**, and put that override's line in the allowlist. Allowlisting a
shared declaration would leave real text below the floor with the test green. The phase's one verified
worked example (`ide/GitPanes.tsx:35` `const smallBtn`) is in **plan 18's** file set — do not touch it.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern). Measured at plan time, these four files carry 16
`<button>` elements and 4 existing `aria-label`s.

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**`PathLine`** at `CommandCenterPanel.tsx:1581-1595` left-truncates with `MAX = 46` plus `nowrap` +
`ellipsis`, and its comment explains the two work together so a path can never wrap. **It is already
satisfied and out of scope** — sweep its `fontSize` if it has one, and change nothing else about it.

**Do not touch** `CommandCenterPanel.tsx:501`'s `disposeTerminal` call (plan 05's audited drop path)
or the `useHiveTasks` migration plan 05 landed in `TasksKanban.tsx`.

**No `eslint-disable-next-line` lives in these four files** — measured `0` in all four at plan time.
All four of the cluster's suppression anchors are in plan 18's file set. If task 1 finds one here
anyway, record it and preserve it on its exact target line; plan 21 turns those rules live and moving
one detaches it.

**Plan 18 owns the other twelve files of this cluster and runs in the same wave.** Its file set is
disjoint from this one. Do not edit any file outside this plan's `files_modified`, and do not run a
repo-wide fix — a cross-set edit in wave 7 is a lost update, because plans 15, 16, 18, 19 and 20 are
all editing the renderer concurrently in the same working tree.

**Do not re-run M1 across the whole renderer and do not record a whole-renderer total.** Any repo-wide
number taken here is stale before it is written, and a stale number in a SUMMARY is the exact
false-claim class this phase exists to remove. The repo-wide completeness bar is plan 23's, asserted
once after every sweep has landed. Record **this sub-group's** four per-file counts and nothing wider.

**Out of the sweep entirely:** `components/terminalFontSize.ts` and anything xterm.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the four files, and confirm all four special cases against source</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table with its four exclusions, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - `src/renderer/src/components/CommandCenterPanel.tsx`, `TasksKanban.tsx`, `IntegrationsRegistry.tsx` in full.
    - `src/renderer/src/components/triggers/ui.tsx` in full, and `:340-355` specifically — the numeric input and its `%` unit label.
    - `src/renderer/src/components/TasksKanban.tsx:190-205` and `:255-270` — the empty-column body and the waiting-on-you chip with its `title` and fill.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time: `CommandCenterPanel.tsx`
    53, `TasksKanban.tsx` 25, `triggers/ui.tsx` 17, `IntegrationsRegistry.tsx` 14 lines / **15 occurrences** (`:388` carries two on one line) — **110 occurrences total**, which is the number that reconciles with plan 23's repo-wide M1 of 604.
    Record any delta.

    Run the M3 glyph predicate scoped to these four files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm the exempt list in `<interfaces>` and confirm all four NOT-exempt sites still sit where
    UI-SPEC says. **Read each of the four and paste the surrounding lines** — the whole point of the
    four exclusions is that a single non-alphanumeric character is not proof of decoration, and line
    numbers drift.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. For every hoisted object, enumerate its consumers
    and mark which need a local override.

    Inventory every `<button>` and `<PixelButton>` across the four files, marked NAMED or ICON-ONLY.
    Baseline at plan time: 16 `<button>` and 4 existing `aria-label` across the four files.

    Record every `eslint-disable-next-line` line number found (expected: none — all four files measured
    `0` at plan time) and every fixed-height container integer.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all four files with a total, and the 110-occurrence baseline beside it (grep -c counts lines, so the four per-file line counts sum to 109; `IntegrationsRegistry.tsx:388` carries two occurrences on one line).
    - Task notes contain the pasted M3 output scoped to these files, and the exempt/not-exempt split matching UI-SPEC or an explicit recorded delta.
    - Task notes contain all four NOT-exempt sites with their current line numbers and pasted surrounding source.
    - Task notes contain the per-file rule classification table and, for any hoisted object found, its consumer map.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY against the 16/4 baseline, and the `eslint-disable-next-line` count per file.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the four files is pre-classified, and all four special cases are re-confirmed against current source rather than taken from the table.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the four files, handle all four NOT-exempt sites, and freeze this sub-group's allowlist</name>
  <files>src/renderer/src/components/CommandCenterPanel.tsx, src/renderer/src/components/TasksKanban.tsx, src/renderer/src/components/triggers/ui.tsx, src/renderer/src/components/IntegrationsRegistry.tsx</files>
  <read_first>
    - The four files in full — the files being modified.
    - Task 1's classification table, button inventory, hoisted-object map and lint-comment list.
    - `01-UI-SPEC.md` § FLOOR-12's four-exclusion table and copywriting contract.
  </read_first>
  <action>
    Execute task 1's classification for all four files under Rules 0, 1 and 2.

    Then handle the four NOT-exempt sites exactly as the table in `<interfaces>` specifies:
    - `TasksKanban.tsx:198` — replace the bare `—` with the copy `Nothing here yet`, swept under Rule 2
      to 14px. Do NOT `aria-hidden` it; an empty column must not be silent to a screen reader.
    - `triggers/ui.tsx:348` — keep the visible `%`, raise it under Rule 2, add `aria-label="percent"`.
      Do not remove the glyph; hiding it loses the unit.
    - `TasksKanban.tsx:263` — keep the visible `?`, raise it under **Rule 1** (the object sets
      `--cth-font-display` at `:260`), add `aria-label="Waiting on your answer"`. Leave the solid
      `--cth-lilac` fill and the `title` at `:258` alone.
    - `IntegrationsRegistry.tsx:293` — no exemption. Sweep the `hint` style normally; the `.` is
      sentence punctuation, not an element.

    Apply the exempt Rule 0 glyphs (`CommandCenterPanel` `⚠` and `✓`, `IntegrationsRegistry` `✕`,
    `TasksKanban` `✕`, `triggers/ui`'s `▾`/`▸` ternary): size unchanged, `aria-hidden="true"` added,
    allowlist entry recorded in `fontSize`-declaration-line space.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    Do not touch `CommandCenterPanel.tsx:501`'s `disposeTerminal` call, `PathLine`'s truncation logic at
    `:1581-1595`, or the `useHiveTasks` call plan 05 landed in `TasksKanban.tsx`.

    Apply containment in order; record every container integer changed with its measured delta.

    Freeze this sub-group's Rule 0 allowlist as a literal `file:line` array covering all four files and
    write it into the SUMMARY. Plan 23 assembles all seven sweep plans' arrays (01-14 through 01-20)
    into `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    Re-run M1 across the four files and paste the final counts for plan 23.
  </action>
  <acceptance_criteria>
    - For each of the four files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all four beside their 53 / 25 / 17 / 14 baselines.
    - `grep -c "Nothing here yet" src/renderer/src/components/TasksKanban.tsx` returns `1`. Baseline today: `0`.
    - `grep -c 'aria-label="percent"' src/renderer/src/components/triggers/ui.tsx` returns `1`. Baseline today: `0`.
    - `grep -c 'aria-label="Waiting on your answer"' src/renderer/src/components/TasksKanban.tsx` returns `1` (baseline today: `0`), and the visible `?` is still rendered — paste the element.
    - `grep -c "cth-font-display" <file>` returns the same value as before this task for all four files — paste both numbers per file.
    - `grep -c "useHiveTasks" src/renderer/src/components/TasksKanban.tsx` still returns at least `1`.
    - `grep -c "disposeTerminal" src/renderer/src/components/CommandCenterPanel.tsx` returns the same value as before this task.
    - Every allowlisted line carries `aria-hidden="true"`: paste each allowlisted line with the two lines following it.
    - `git diff --stat` for this plan shows changes ONLY to the four files in `files_modified` — no file from plan 18's set, and no repo-wide fix.
    - Task notes contain this sub-group's frozen allowlist as a literal `file:line` array and the re-run M1 counts for the four files, with **no whole-renderer total**.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>The four densest files clear the floor, all four content-bearing single-character sites are treated as content with an accessible name rather than hidden as decoration, and the sub-group's allowlist is frozen for plan 23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task board and command centre → the operator's view of what agents are doing | This is the surface where the operator decides whether work is progressing or stuck. Unreadable status text or an unnamed status chip is a misreading risk on the phase's core trust property. |
| triggers form → the operator's configuration of automated runs | A numeric input that loses its unit is how a wrong threshold gets saved. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P17-01 | Information disclosure | task-board and command-centre text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size, pasted beside the 53/25/17/14 baselines. |
| T-P17-02 | Information disclosure | an empty Kanban column reading as silence to a screen reader | mitigate | `TasksKanban.tsx:198`'s bare `—` becomes the copy `Nothing here yet` rather than being `aria-hidden`. |
| T-P17-03 | Information disclosure | the `?` waiting-on-you status chip having no accessible name | mitigate | Keep the visible glyph, raise it under Rule 1, and add `aria-label="Waiting on your answer"`; its `title` and lilac fill are untouched. |
| T-P17-04 | Information disclosure | a numeric input losing its unit | mitigate | `triggers/ui.tsx:348`'s `%` stays visible, is raised under Rule 2, and gains `aria-label="percent"`. |
| T-P17-05 | Tampering | allowlisting a hoisted shared style object and leaving real text below the floor with the test green | mitigate | Hoisted declarations are swept normally and only the glyph element gets a local override; task 1's consumer map is the evidence. |
| T-P17-06 | Denial of service | disturbing `CommandCenterPanel`'s terminal dispose path or `TasksKanban`'s shared poller | mitigate | Both regions are explicitly out of bounds and asserted by an unchanged-count grep. |
| T-P17-07 | Tampering | editing a file that belongs to plan 18, 15, 16, 19 or 20 in the same wave | mitigate | `git diff --stat` must show only this plan's four files; a cross-set edit in wave 7 is a lost update with no test that catches it. |
| T-P17-08 | Repudiation | recording a whole-renderer M1 total that five concurrent plans invalidate | mitigate | Explicitly forbidden here; the repo-wide bar is plan 23's, asserted once after every sweep has landed. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the command centre, the task board (with at least one empty column) and the triggers
  form in the dev app. Confirm nothing is clipped, the empty column reads `Nothing here yet`, and the
  `%` unit is still beside its input.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all four files in this sub-group.
- All four NOT-exempt single-character sites are handled as content with an accessible name.
- Every icon-only `<button>` in these four files has an accessible name, by the rule rather than by a ratio.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-17-SUMMARY.md` when done. It MUST contain this
sub-group's frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts beside
their 53 / 25 / 17 / 14 baselines, the four NOT-exempt sites' resolutions, and every container integer
changed with its delta. It MUST NOT contain a whole-renderer M1 total.
</output>
