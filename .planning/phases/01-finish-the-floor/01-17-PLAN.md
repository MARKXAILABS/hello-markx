---
phase: 01-finish-the-floor
plan: 17
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/CommandCenterPanel.tsx
  - src/renderer/src/components/TasksKanban.tsx
  - src/renderer/src/components/IntegrationsRegistry.tsx
  - src/renderer/src/components/GitTab.tsx
  - src/renderer/src/components/triggers/ui.tsx
  - src/renderer/src/components/triggers/TriggerHistoryTab.tsx
  - src/renderer/src/components/triggers/WebhooksSection.tsx
  - src/renderer/src/components/triggers/SchedulesSection.tsx
  - src/renderer/src/components/triggers/OrgSection.tsx
  - src/renderer/src/components/git/CommitGraph.tsx
  - src/renderer/src/ide/IdePanel.tsx
  - src/renderer/src/ide/GitPanes.tsx
  - src/renderer/src/ide/chrome.ts
  - src/renderer/src/ide/ImagePreview.tsx
  - src/renderer/src/ide/MonacoEditor.tsx
  - src/renderer/src/ide/MonacoDiff.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the command-centre, tasks, triggers and IDE cluster sits below the 14px floor except allowlisted decorative glyphs"
    - "All four NOT-exempt single-character sites are handled as content, not hidden as decoration"
    - "Every icon-only button in these sixteen files has an accessible name; no button with visible text gained an aria-label"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
  artifacts:
    - path: "src/renderer/src/components/TasksKanban.tsx"
      provides: "the empty-column copy replacing a bare em dash, plus the waiting-on-you chip's accessible name"
    - path: "src/renderer/src/components/triggers/ui.tsx"
      provides: "the percent unit label kept visible and named"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the command-centre, tasks, triggers and IDE cluster to the corrected type scale, handle all four
of the phase's NOT-exempt single-character sites, and give every icon-only button an accessible name.

Purpose: FLOOR-12, group 3 of 4 — the largest by site count and the only group carrying special cases.
Measured at plan time: **177 sub-14px `fontSize` sites across sixteen files**, led by
`CommandCenterPanel.tsx` (53), `TasksKanban.tsx` (25) and `ide/IdePanel.tsx` (20).

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract. Its glyph-exemption predicate is decidable and
its four exclusions were each read individually — **a single non-alphanumeric character is not proof of
decoration**, and all four exceptions live in this group's files.

Output: sixteen files clearing the floor, four special cases handled as content, an accessible-name
pass, and this group's frozen Rule 0 allowlist handed to plan 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>`

**This group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`CommandCenterPanel.tsx` `⚠`, `✓`; `IntegrationsRegistry.tsx` `✕`; `TasksKanban.tsx` `✕`;
`triggers/ui.tsx` `{open ? '▾' : '▸'}`; `ide/GitPanes.tsx` `✕`, `⇄`;
`ide/IdePanel.tsx` `{gitCollapsed ? '▸' : '▾'}`. Each gains `aria-hidden="true"` and keeps its size.

**The four NOT-exempt sites — all in this group, each read individually by the UI checker:**

| Site | Glyph | Why it is content | Action |
|---|---|---|---|
| `TasksKanban.tsx:198` | `—` | It is the **empty-state body** of a Kanban column. `aria-hidden` would make an empty column silent to a screen reader. | Replace with the copy `Nothing here yet` at 14px. **Rule 2 applies.** |
| `triggers/ui.tsx:348` | `%` | A **unit label** beside a numeric input (`:344-347`). Hiding it loses the unit. | Keep the visible `%`, raise to 14px under Rule 2, add `aria-label="percent"`. |
| `TasksKanban.tsx:263` | `?` | A **status chip** — `title="waiting on YOUR answer — see the ASK ME tab"` (`:258`), solid `--cth-lilac` fill (`:261`). It carries meaning and has no accessible name. | Keep the visible `?`, raise to 14px under **Rule 1** (it sets `--cth-font-display` at `:260`), add `aria-label="Waiting on your answer"`. |
| `IntegrationsRegistry.tsx:293` | `.` | Sentence punctuation ending prose inside `<span style={hint}>`, after a `<code>`. Not a glyph element — a residual regex artifact. | **No exemption.** The `hint` style is swept normally. |

`/` is dropped from the glyph vocabulary entirely — a `/` match is a JSX self-close, an arrow-function
return or a template-string artifact, never a decorative element.

**Hoisted shared style objects are NOT exempt — two verified cases live in this group's neighbourhood:**
`ide/GitPanes.tsx:35` `const smallBtn` (`fontSize: 11`) serves `:110` `load older…` and `:127` (both
text) and `:130` `✕` (glyph, via spread). Sweep the shared declaration normally, give the glyph element
a **local `fontSize` override**, and put that override's line in the allowlist. Allowlisting the shared
declaration would leave real text below the floor with the test green.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern).

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**`PathLine`** at `CommandCenterPanel.tsx:1581-1595` left-truncates with `MAX = 46` plus `nowrap` +
`ellipsis`, and its comment explains the two work together so a path can never wrap. **It is already
satisfied and out of scope** — sweep its `fontSize` if it has one, and change nothing else about it.

**Do not touch** `CommandCenterPanel.tsx:501`'s `disposeTerminal` call (plan 05's audited drop path)
or the `useHiveTasks` migration plan 05 landed in `TasksKanban.tsx`.

**Preserve every `eslint-disable-next-line` on its exact target line** — `GitTab.tsx`,
`triggers/SchedulesSection.tsx`, `triggers/WebhooksSection.tsx` and `ide/IdePanel.tsx` each carry one
of the nine `react-hooks/exhaustive-deps` suppressions plan 19 turns live. Moving one detaches it.

**Out of the sweep entirely:** `components/terminalFontSize.ts` and anything xterm. Monaco's own editor
font is configured by Monaco, not by these style objects — sweep only the chrome around it.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the sixteen files, and confirm the four special cases against source</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table with its four exclusions, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - `src/renderer/src/components/CommandCenterPanel.tsx`, `TasksKanban.tsx`, `IntegrationsRegistry.tsx`, `GitTab.tsx` in full.
    - `src/renderer/src/components/triggers/ui.tsx:340-355` specifically — the numeric input and its `%` unit label.
    - `src/renderer/src/components/TasksKanban.tsx:190-205` and `:255-270` — the empty-column body and the waiting-on-you chip with its `title` and fill.
    - `src/renderer/src/ide/GitPanes.tsx:30-135` — `const smallBtn` and all three of its consumers.
    - The remaining triggers, git and ide files in full.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time: CommandCenterPanel 53,
    TasksKanban 25, IdePanel 20, triggers/ui 17, TriggerHistoryTab 14, IntegrationsRegistry 14,
    GitTab 12, GitPanes 6, CommitGraph 4, WebhooksSection 3, SchedulesSection 2, OrgSection 1,
    ide/chrome 2, ImagePreview 2, MonacoEditor 1, MonacoDiff 1 — 177 total. Record any delta.

    Run the M3 glyph predicate scoped to these sixteen files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm the exempt list in `<interfaces>` and confirm all four NOT-exempt sites still sit where
    UI-SPEC says. **Read each of the four and paste the surrounding lines** — the whole point of the
    four exclusions is that a single non-alphanumeric character is not proof of decoration, and line
    numbers drift.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. For every hoisted object, enumerate its consumers
    and mark which need a local override.

    Inventory every `<button>` and `<PixelButton>` across the sixteen files, marked NAMED or ICON-ONLY.

    Record every `eslint-disable-next-line` line number in the group, and every fixed-height container
    integer.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all sixteen files with a total.
    - Task notes contain the pasted M3 output scoped to these files, and the exempt/not-exempt split matching UI-SPEC or an explicit recorded delta.
    - Task notes contain the four NOT-exempt sites with their current line numbers and pasted surrounding source.
    - Task notes contain the per-file rule classification table and the hoisted-object consumer map, including `GitPanes.tsx`'s `smallBtn`.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY and every `eslint-disable-next-line` line number.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the group is pre-classified, and all four special cases are re-confirmed against current source rather than taken from the table.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep CommandCenterPanel and TasksKanban, and handle all four NOT-exempt sites</name>
  <files>src/renderer/src/components/CommandCenterPanel.tsx, src/renderer/src/components/TasksKanban.tsx, src/renderer/src/components/triggers/ui.tsx, src/renderer/src/components/IntegrationsRegistry.tsx</files>
  <read_first>
    - The four files in full — the files being modified.
    - Task 1's classification table, button inventory, hoisted-object map and lint-comment list for these files.
    - `01-UI-SPEC.md` § FLOOR-12's four-exclusion table and copywriting contract.
  </read_first>
  <action>
    Execute task 1's classification for these four files under Rules 0, 1 and 2.

    Then handle the four NOT-exempt sites exactly as the table in `<interfaces>` specifies:
    - `TasksKanban.tsx:198` — replace the bare `—` with the copy `Nothing here yet`, swept under Rule 2
      to 14px. Do NOT `aria-hidden` it; an empty column must not be silent to a screen reader.
    - `triggers/ui.tsx:348` — keep the visible `%`, raise it under Rule 2, add `aria-label="percent"`.
      Do not remove the glyph; hiding it loses the unit.
    - `TasksKanban.tsx:263` — keep the visible `?`, raise it under **Rule 1** (the object sets
      `--cth-font-display` at `:260`), add `aria-label="Waiting on your answer"`. Leave the solid
      `--cth-lilac` fill and the `title` at `:258` alone.
    - `IntegrationsRegistry.tsx:293` — no exemption. Sweep the `hint` style normally; the `.` is
      sentence punctuation, not an element.

    Apply the exempt Rule 0 glyphs in these files (`CommandCenterPanel` `⚠` and `✓`,
    `IntegrationsRegistry` `✕`, `TasksKanban` `✕`, `triggers/ui`'s `▾`/`▸` ternary): size unchanged,
    `aria-hidden="true"` added, allowlist entry recorded in `fontSize`-declaration-line space.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    Do not touch `CommandCenterPanel.tsx:501`'s `disposeTerminal` call, `PathLine`'s truncation logic at
    `:1581-1595`, or the `useHiveTasks` call plan 05 landed in `TasksKanban.tsx`. Preserve every
    `eslint-disable-next-line` on its target line.

    Apply containment in order; record every container integer changed with its measured delta.
  </action>
  <acceptance_criteria>
    - For each of the four files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all four.
    - `grep -c "Nothing here yet" src/renderer/src/components/TasksKanban.tsx` returns `1`.
    - `grep -c 'aria-label="percent"' src/renderer/src/components/triggers/ui.tsx` returns `1`.
    - `grep -c 'aria-label="Waiting on your answer"' src/renderer/src/components/TasksKanban.tsx` returns `1`, and the visible `?` is still rendered — paste the element.
    - `grep -c "cth-font-display" <file>` is unchanged for all four files.
    - `grep -c "useHiveTasks" src/renderer/src/components/TasksKanban.tsx` still returns at least `1`.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The two largest files in the group clear the floor and all four content-bearing single-character sites are treated as content with an accessible name, not hidden as decoration.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the triggers, git and IDE files, and freeze this group's allowlist</name>
  <files>src/renderer/src/components/GitTab.tsx, src/renderer/src/components/triggers/TriggerHistoryTab.tsx, src/renderer/src/components/triggers/WebhooksSection.tsx, src/renderer/src/components/triggers/SchedulesSection.tsx, src/renderer/src/components/triggers/OrgSection.tsx, src/renderer/src/components/git/CommitGraph.tsx, src/renderer/src/ide/IdePanel.tsx, src/renderer/src/ide/GitPanes.tsx, src/renderer/src/ide/chrome.ts, src/renderer/src/ide/ImagePreview.tsx, src/renderer/src/ide/MonacoEditor.tsx, src/renderer/src/ide/MonacoDiff.tsx</files>
  <read_first>
    - The twelve files in full — the files being modified.
    - Task 1's classification table, hoisted-object map (especially `GitPanes.tsx:35` `const smallBtn` and its three consumers), button inventory and lint-comment list for these files.
  </read_first>
  <action>
    Execute task 1's classification for the twelve remaining files under Rules 0, 1 and 2.

    `ide/GitPanes.tsx:35`'s `const smallBtn` is the worked example of the hoisted-object rule: it serves
    two text buttons and one `✕` glyph. Sweep `smallBtn` itself normally, then give the `✕` element a
    **local `fontSize` override** at its own site and record that override's line in the allowlist. Do
    not allowlist `smallBtn`'s declaration — doing so would hold `load older…` below the floor with the
    test green.

    Apply Rule 0 to this group's remaining exempt glyphs (`GitPanes` `✕` and `⇄`, `IdePanel`'s
    `▸`/`▾` ternary): size unchanged, `aria-hidden="true"`, allowlist entry recorded.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    For `MonacoEditor.tsx`, `MonacoDiff.tsx` and `ide/chrome.ts`: sweep only the chrome's own style
    objects. Monaco configures its own editor font; do not route Monaco's `fontSize` option through a
    CSS token, and do not touch anything xterm.

    Preserve every `eslint-disable-next-line` on its exact target line — `GitTab.tsx`,
    `SchedulesSection.tsx`, `WebhooksSection.tsx` and `IdePanel.tsx` each carry one.

    Apply containment in order; record every container integer changed with its measured delta.

    Freeze this group's Rule 0 allowlist as a literal `file:line` array covering all sixteen group files
    and write it into the SUMMARY. Plan 21 assembles all four groups' arrays into
    `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    Re-run M1 across all sixteen group files and paste the final counts for plan 21.
  </action>
  <acceptance_criteria>
    - For each of the twelve files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all twelve.
    - `grep -n "smallBtn" src/renderer/src/ide/GitPanes.tsx` output shows the declaration swept to a token, and the `✕` element carries its own local `fontSize` — paste both.
    - `grep -c "cth-font-display" <file>` is unchanged for all twelve files.
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/GitTab.tsx src/renderer/src/components/triggers/SchedulesSection.tsx src/renderer/src/components/triggers/WebhooksSection.tsx src/renderer/src/ide/IdePanel.tsx` returns the same values task 1 recorded, each still immediately above its target line.
    - Task notes contain this group's frozen allowlist as a literal `file:line` array and the re-run M1 counts for all sixteen files.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All sixteen files clear the floor, the hoisted-object rule is applied correctly at its worked example, every icon-only control is named, and the group's allowlist is frozen for plan 21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| task board and command centre → the operator's view of what agents are doing | This is the surface where the operator decides whether work is progressing or stuck. Unreadable status text or an unnamed status chip is a misreading risk on the phase's core trust property. |
| IDE and git panes → the operator's view of what changed | Diff and git chrome that cannot be read is how an unwanted change ships. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P17-01 | Information disclosure | task-board and command-centre text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size. |
| T-P17-02 | Information disclosure | an empty Kanban column reading as silence to a screen reader | mitigate | `TasksKanban.tsx:198`'s bare `—` becomes the copy `Nothing here yet` rather than being `aria-hidden`. |
| T-P17-03 | Information disclosure | the `?` waiting-on-you status chip having no accessible name | mitigate | Keep the visible glyph, raise it under Rule 1, and add `aria-label="Waiting on your answer"`; its `title` and lilac fill are untouched. |
| T-P17-04 | Information disclosure | a numeric input losing its unit | mitigate | `triggers/ui.tsx:348`'s `%` stays visible, is raised under Rule 2, and gains `aria-label="percent"`. |
| T-P17-05 | Repudiation | allowlisting `GitPanes.tsx`'s hoisted `smallBtn` and leaving `load older…` below the floor | mitigate | The hoisted declaration is swept and the glyph gets a local override; asserted by a paste of both lines. |
| T-P17-06 | Repudiation | a moved `eslint-disable-next-line` detaching a suppression before plan 19's lint gate | mitigate | Four suppression anchors recorded in task 1 and re-asserted after the edit passes. |
| T-P17-07 | Denial of service | disturbing `CommandCenterPanel`'s terminal dispose path or `TasksKanban`'s shared poller | mitigate | Both regions are explicitly out of bounds and asserted by grep. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the command centre, the task board (with at least one empty column), the triggers tabs
  and the IDE panel in the dev app. Confirm nothing is clipped, the empty column reads
  `Nothing here yet`, and the `%` unit is still beside its input.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all sixteen files in this group.
- All four NOT-exempt single-character sites are handled as content with an accessible name.
- Every `<button>` in the group has an accessible name, by the rule rather than by a ratio.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-17-SUMMARY.md` when done. It MUST contain this group's
frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts, the four
NOT-exempt sites' resolutions, every container integer changed with its delta, and confirmation that
all four `eslint-disable-next-line` comments are still on their target lines.
</output>
