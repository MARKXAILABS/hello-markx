---
phase: 01-finish-the-floor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/load-ts.cjs
  - package.json
  - package-lock.json
  - electron-builder.yml
  - e2e/smoke.spec.ts
  - .github/workflows/ci.yml
  - .github/workflows/e2e.yml
  - .planning/phases/01-finish-the-floor/01-VALIDATION.md
autonomous: false
requirements: [FLOOR-03, FLOOR-15]

must_haves:
  truths:
    - "The app runs on Electron 43.x — inside the latest-3 supported window as of 2026-08 (D-02 restates ROADMAP criterion 2's stale 'Electron 38+')"
    - "node-pty and better-sqlite3 load in the packaged app under Electron 43 (via postinstall's electron-rebuild — NOT via any added `npm rebuild` step in ci.yml's test jobs; better-sqlite3 13.x is N-API and ships prebuilds)"
    - "npm test is green on ubuntu + windows + macos with no continue-on-error added"
    - "A draft PR from the phase branch to `main` is open, because ci.yml and e2e.yml fire on `pull_request: branches: [main]` and on no other event this phase produces — its URL is the one every later plan's `CI green` claim resolves against"
    - "A real PTY spawns and a real better-sqlite3 write lands in the built app on the Windows machine (D-09)"
    - "test/load-ts.cjs resolves .tsx and never calls the real electron loader"
  artifacts:
    - path: "package.json"
      provides: "electron ^43.4.1, electron-builder ^26.15.3, @electron/rebuild ^4.2.0"
      contains: "\"electron\": \"^43"
    - path: "test/load-ts.cjs"
      provides: "require.cache-only electron resolution + .tsx/JsxEmit support"
      contains: "JsxEmit.ReactJSX"
    - path: "e2e/smoke.spec.ts"
      provides: "D-10 Electron major-version assertion"
      contains: "process.versions.electron"
  key_links:
    - from: "test/load-ts.cjs"
      to: "require.cache['electron']"
      via: "injected-fake lookup that never invokes the real loader"
      pattern: "require\\.cache\\["
    - from: "e2e/smoke.spec.ts"
      to: "electronApp.evaluate"
      via: "runtime version read from the launched app"
      pattern: "versions\\.electron"
---

<objective>
Move the runtime from Electron 32 to Electron 43.x as ONE atomic, revertable plan, and fix the
two `test/load-ts.cjs` defects that plan owns (the Electron-42 lazy-download break, and D-25's
`.tsx` support that four later requirements depend on).

Purpose: D-03. Phase 1's premise is that every closure is checked against a live test run.
Closing 22 requirements against Electron 32 and swapping the runtime at the end converts 22
verified closures into 22 unverified ones. **This plan gates every other plan in the phase.**

Output: a bumped, lockfile-regenerated, three-platform-green runtime; a loader that can resolve
`.tsx`; the only automated assertion in the repo that can catch a silent revert to Electron 32;
and D-09's live Windows operator evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-VALIDATION.md
@.planning/codebase/STACK.md
@.planning/codebase/TESTING.md

<interfaces>
Current pins (`package.json`, read this session): `electron ^32.2.0`, `electron-builder 25.1.8`,
`@electron/rebuild ^3.7.0`, `better-sqlite3 11.10.0`, `@types/better-sqlite3 ^7.6.13`,
`node-pty 1.1.0` (exact, DO NOT CHANGE), `typescript 5.9.3` (exact, DO NOT CHANGE),
`electron-updater ^6.8.9` (already the matching version — no pairing work).

Targets, from `npm view` this session (01-RESEARCH.md Findings 2/4/5):
`electron ^43.4.1` · `electron-builder ^26.15.3` · `@electron/rebuild ^4.2.0` ·
`better-sqlite3 ^13.0.3` (first N-API release; 11.10.0 is raw-V8 and will not build against V8 15).

Verified this session about `better-sqlite3@13.0.3`, because it changes what CI must do:
- It declares **no `install` script** (13.0.2 dropped the one 13.0.0/13.0.1 had):
  `npm view better-sqlite3@13.0.3 --json` → `scripts` has `test/clean/download/benchmark/build-*` and no `install`.
- Its tarball ships **N-API prebuilds for all eight platform/arch pairs**
  (`prebuilds/{darwin,linux,linuxmusl,win32}-{x64,arm64}.node`), so `npm ci --ignore-scripts`
  alone leaves a loadable module. Proven on this host under Node 24:
  `npm i better-sqlite3@13.0.3 --ignore-scripts` then a `:memory:` open + `CREATE VIRTUAL TABLE t USING fts5(x)`
  + `MATCH` round-trip returned `{ x: 'hello' }` with nothing compiled.
- It **still ships `binding.gyp`**, and npm's documented default for a package with a `binding.gyp`
  and no declared `install` script is to synthesise one. Proven: `npm rebuild better-sqlite3`
  printed `> better-sqlite3@13.0.3 install` / `> node-gyp rebuild` and went straight into
  `gyp info find Python` + `gyp info find VS`.
  **Consequence: `npm rebuild better-sqlite3` throws the shipped prebuild away and demands a full
  Python + C++ toolchain. It is never a no-op. Do not add it anywhere.**

`test/load-ts.cjs` exports `loadTs(relativePath)`; `requireElectron()` at `:28-42`;
`resolveTs(fromDir, request)` at `:63-71`; transpile options at `:77-84`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-03 clause against current source, then fix test/load-ts.cjs and prove it green on the OLD Electron</name>
  <files>test/load-ts.cjs</files>
  <read_first>
    - `test/load-ts.cjs` in full (116 lines) — `requireElectron()` `:28-42`, `resolveTs()` `:63-71`, transpile options `:77-84`. This is the file being modified; read its current state, not the excerpts.
    - `01-RESEARCH.md` § "Finding 1 (BLOCKING)" and § "Code Examples 1" — the literal replacement body and the reason the real loader must never be called.
    - `01-PATTERNS.md` § "`require.cache` injection" (`:1236-1253`) — the property the fix must preserve: an injected fake must still win.
    - `01-PATTERNS.md` § "`test/renderer-components.test.cjs`" `:193-245` — what `resolveTs` does not handle today and why `.tsx` plus `esModuleInterop` matter.
    - `.planning/codebase/TESTING.md` — `node --test` doctrine, the `require.cache` seeding order rule.
  </read_first>
  <action>
    First, produce the D-42 per-clause evidence block for FLOOR-03 and paste ACTUAL command output
    into the task notes — one command per clause of issue #10's acceptance text, quoted verbatim.
    At minimum: `grep -n '"electron"' package.json` (expect `^32.2.0` — this is the clause that is
    genuinely open), `grep -n '"node-pty"\|"better-sqlite3"\|"@electron/rebuild"\|"electron-builder"' package.json`,
    `node -e "console.log(process.version)"`, and `npm view electron dist-tags` (re-verify D-01's
    window; RESEARCH is valid only until 2026-09-03 and Electron 44 goes stable ~2026-08-25 — if
    43 has left the latest-3 window, STOP and report, do not silently retarget).

    Then make exactly two edits to `test/load-ts.cjs`:
    (a) Rewrite `requireElectron()` so it never calls `require('electron')`. Resolve the id with
    `require.resolve('electron')` inside a try (return `electronStub()` on throw), read
    `require.cache[id]?.exports`, return it when it is a non-null object, otherwise return
    `electronStub()`. Carry a comment naming Electron 42's `module.exports = getElectronPath()`
    lazy download and stating that the only reason this ever asked for the real module was to let
    an injected fake win — `require.cache` answers that directly.
    (b) D-25: add `${base}.tsx` and `path.join(base, 'index.tsx')` to `resolveTs()`'s candidate
    list, and add `jsx: ts.JsxEmit.ReactJSX` to the `compilerOptions` object at `:77-84`, leaving
    `esModuleInterop: true` and its comment untouched.

    Run `npm test` and `npm run typecheck` on the CURRENT (Electron 32) tree BEFORE touching any
    version. This isolates the loader change from the version change — a failure after both land
    is undiagnosable. Commit this as its own commit.

    Then close out Wave 0 in `01-VALIDATION.md`. That file's Wave 0 test-infrastructure list at
    `:120-136` has six unticked items; the two `test/load-ts.cjs` items (`:120` the lazy-download fix,
    `:125` the `.tsx` + `ts.JsxEmit.ReactJSX` addition) are exactly what this task just shipped — tick
    both, each with the command output that proves it. The other four (`repo-claims`,
    `renderer-components`, `db-fts`, `hooks-notify`) belong to plans 05, 21, 10 and 13 and are ticked
    by plan 23 at the gate; leave them.

    Set `wave_0_complete: true` in `01-VALIDATION.md`'s frontmatter, because the Wave 0 *loader*
    prerequisite — the thing every other Wave 0 item is blocked on ("❌ W0 — needs W1 loader") — is
    now in. **Nobody else in this phase sets that flag**; leaving it `false` here leaves it `false`
    through the whole phase. Do not touch `nyquist_compliant` or `status` — those are plan 23's.
  </action>
  <acceptance_criteria>
    - `grep -vE "^\s*(//|\*|/\*)" test/load-ts.cjs | grep -c "require('electron')"` returns `0`. State it as this literal pipeline, not as a `grep -c` with an "after stripping comments" qualifier — `grep -c` cannot strip comments, and the qualifier silently converts an automated check into a manual one. The baseline for the same pipeline is **`1`** today (raw `grep -c` returns `2`: the real call at `test/load-ts.cjs:30` and a comment mentioning it at `:34`, which the strip correctly removes). Paste both the stripped and the raw counts.
    - `grep -c "JsxEmit.ReactJSX" test/load-ts.cjs` returns at least `1`, **and** `grep -cE "JsxEmit[.]React([^J]|$)" test/load-ts.cjs` returns `0` (the character-class tail is load-bearing: a bare `grep -c "JsxEmit.React"` also matches `JsxEmit.ReactJSX`, so it would return `1` on the CORRECT implementation and fail it). The constant matters and the wrong one fails silently at render time, not at compile time: this repo sets `"jsx": "react-jsx"` (`tsconfig.web.json:7`) — the automatic runtime — and only **1 of 63** renderer `.tsx` files imports `React`. `ts.JsxEmit.React` emits `React.createElement(...)`, so every component transpiled with it throws `ReferenceError: React is not defined` the moment it renders. `ts.JsxEmit.ReactJSX` emits `require("react/jsx-runtime")`, which is self-contained. Verified 2026-08-21 by transpiling `export const A = () => <div>hi</div>` both ways. Plan 22's entire renderer-markup suite depends on this constant being right.
    - `grep -c "tsx" test/load-ts.cjs` returns at least `1`.
    - `npm test` exit code is `0` on the unbumped tree, and the TAP counters match the platform-scoped baseline below. Read the counters from the TAP reporter, not the default one, and write the TAP through `mktemp` rather than into the repo (`.gitignore` has no `*.tap` entry, and a stray file trips this phase's `git status --porcelain` containment checks):
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; node -p "process.platform"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required on **every** platform: `EXIT=0`, `# tests` **at least `426`**, `# fail` **exactly `0`**, `# todo` **exactly `0`**, and `# pass` **at least `# tests` minus the platform ceiling below** — i.e. every test that is not one of the enumerated skips must PASS.
      The skip ceiling is **per platform, and it is a ceiling, not an equality**:

      | Platform | `# skipped` must be | `# pass` must be at least |
      |---|---|---|
      | `win32` | at most `4` (`5` only if the symlink guard below fires) | `422` |
      | `linux` / `darwin` (ubuntu-latest, macos-latest) | at most `0` (`1` only if the symlink guard fires) | `426` |

      **Why it is scoped and not the flat `4` this criterion used to state.** All four skips in the tree are `{ skip: !POSIX }` where `POSIX = process.platform !== 'win32'` — `test/hive-hook-node.test.cjs:153`, `test/hive-runtime-path.test.cjs:83`, `test/hook-auth-roundtrip.test.cjs:78` and `:108` (enumerate them with `grep -rn "skip: !POSIX" test/*.test.cjs`, which must return exactly those four lines). On the two POSIX legs of the hard gate they RUN, so a correct ubuntu or macos run reports `# skipped 0` / `# pass 426`, and the old unscoped `exactly 4` would have manufactured a false finding out of a correct run. The `01-VALIDATION.md:30` baseline of 56 files / 426 tests / 422 pass / 0 fail / 4 skip (re-measured 2026-08-20 post origin/main merge) is a **win32** measurement — label it that way in the notes.
      **The +1 symlink exception**, and nothing else: `test/main-hardening.test.cjs:112` calls `t.skip('this machine will not create symlinks')` when `symlinkSync` throws, so a host or runner without symlink/junction privilege legitimately reports one more skip than the table. If and only if the extra skip appears, paste the TAP line naming that test; any other extra skip is a finding to report, not a number to relax.
      **The ceiling is still strict, and that is the point.** `# skipped` at most the platform's number, combined with `# pass` at least `# tests` minus that number, means a live test converted to `{ skip: ... }` fails this criterion on both halves — `# pass` drops and `# skipped` rises. Do not relax either bound: a plain `>=` on skips is satisfied by 422 pass / 40 skip (eighteen live tests disabled), and `node --test` exits `0` on an all-skipped file, so exit code alone cannot tell a passing test from a disabled one. `grep -rcE "\.skip\(|\.todo\(|skip:|todo:" test/*.test.cjs` must not gain a hit in any file this task touched.
    - `npm run typecheck` exit code is `0`.
    - The task notes contain one pasted command output per FLOOR-03 acceptance clause.
    - `grep -n "wave_0_complete" .planning/phases/01-finish-the-floor/01-VALIDATION.md` shows `true`, and the file's two `test/load-ts.cjs` Wave 0 items (`:120`, `:125`) are ticked with their evidence — paste the diff.
    - `grep -n "nyquist_compliant\|^status:" .planning/phases/01-finish-the-floor/01-VALIDATION.md` is unchanged from before this task (`false` and `draft`) — plan 23 owns both.
    - **Recorded count baselines** (this plan's later criteria are strict deltas against these, because each token already occurs in its file for unrelated reasons — a bare `at least N` would be satisfied by doing nothing). Run and paste each, verbatim:
      `grep -c "better-sqlite3" .github/workflows/ci.yml` (expect `2` — the `NODE_VERSION` comment at `:9` and the `build` job's rebuild step name at `:111`),
      `grep -c "npm rebuild better-sqlite3" .github/workflows/ci.yml` (expect `0`, and Task 2 keeps it at `0`), and
      `grep -vE "^\s*(//|\*|/\*)" e2e/smoke.spec.ts | grep -cE "toBeGreaterThanOrEqual\(\s*43\s*\)"` (expect `0` — verified at plan time; Task 3 turns it into `1`).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Loader no longer invokes the real electron loader, resolves `.tsx`, and the full suite is green on Electron 32 in its own commit.</done>
</task>

<task type="auto">
  <name>Task 2: Bump the runtime, regenerate the lockfile on a clean tree, and read electron-builder.yml through against the v26 schema</name>
  <files>package.json, package-lock.json, electron-builder.yml, .github/workflows/ci.yml, .github/workflows/e2e.yml</files>
  <read_first>
    - `package.json` in full — every dependency line being changed, plus `engines`, `scripts.postinstall`, `scripts.test`.
    - `electron-builder.yml` in full — v26 makes unknown keys an ERROR, not a warning.
    - `.github/workflows/ci.yml` in full — the three `test` jobs' `npm ci --ignore-scripts`, the existing `npm rebuild node-pty` dance with its Python 3.11 pin, and the two pre-existing `continue-on-error` uses at `:30-35` and in `build`.
    - `01-RESEARCH.md` § Finding 2 (better-sqlite3 raw-V8 → 13.x N-API, and the glibc >= 2.41 note flagged as assumption A3), § Finding 4 (`@electron/rebuild` 4.x needs node >= 22.12.0 while `engines` still admits Node 20), § Finding 5 (the `mac.notarize: false` key, the `-c.win.azureSignOptions.*` CLI flags in release.yml).
    - `tools/patch-node-pty-conpty.cjs` header — read its `process.exit(1)` as "node-pty version changed", NEVER as "Electron version changed" (D-05).
    - `01-RESEARCH.md` § Pitfall 7 and D-07 — the lockfile rules.
  </read_first>
  <action>
    **READ THIS FIRST — a full-scripts install has never succeeded on this host, and this task
    mandates one.** Verified this session, on the tree as it stands: `node_modules/better-sqlite3/build`
    does not exist, `node_modules/node-pty/build/Release` does not exist, `node_modules/@playwright`
    does not exist, and `node -e "new (require('better-sqlite3'))(':memory:')"` throws
    `Error: Could not locate the bindings file`. The only install that has ever completed here is
    `npm ci --ignore-scripts`; the postinstall path this task's `<done>` line claims
    (`electron-rebuild -f`) has never been observed to run to completion on this machine, and this
    host is Node v24.13.0 with no Node 22 and no nvm (Task 4's constraint block, same blocker).
    This is the same class of honesty the plan already applies to D-09 and to `npm run e2e`, and it
    is stated here because **four of this task's own criteria sit downstream of it**: `npm test`
    locally, `npm run typecheck` locally, the native-load claim in `<done>`, and the lockfile
    regeneration itself.

    So run the sequence below with an explicit, pre-declared fallback rather than discovering the
    failure mid-task:

    - **Lockfile regeneration is NOT blocked by the native rebuild.** If the bare `npm install`
      below dies in postinstall, re-run it as `npm install --ignore-scripts`. Dependency resolution
      — which is the entire input to `package-lock.json` — does not depend on lifecycle scripts, so
      the committed lockfile is identical either way, and `npm ci --ignore-scripts` then proves it.
      Record in the notes which of the two actually ran. **Do not** hand-edit the lockfile and do not
      skip the regeneration because the rebuild failed.
    - **If the native rebuild cannot run here, say so and route it to CI — do not delete the gate,
      and do not claim a rebuild you did not watch succeed.** Write, verbatim:
      `NATIVE REBUILD UNAVAILABLE — full-scripts install fails on this host (Node v24.13.0, no Node 22, no nvm; better-sqlite3/node-pty build dirs absent, bindings load throws). Electron-ABI native load routed to the phase PR's Electron smoke (ubuntu-latest) job.`
      The `Electron smoke (ubuntu-latest)` job in `e2e.yml` runs a **full-scripts** `npm ci`
      (`e2e.yml:61`, deliberately not `--ignore-scripts`) and then boots real Electron, so it is a
      hard gate on exactly the claim that cannot be made here. `ci.yml`'s `Build` job also runs
      `npx electron-rebuild -f`, but it is `continue-on-error: true` (`ci.yml:117`) — it is a signal,
      never the gate. Cite the smoke job, not `Build`.
    - **If `npm test` or `npm run typecheck` cannot run locally under `--ignore-scripts`**, report
      the failing module by name and route those two to `Test (ubuntu-latest)` / `Test (windows-latest)` /
      `Test (macos-latest)` and `Typecheck` on the draft PR. `typecheck` needs only `.d.ts` files and
      should survive `--ignore-scripts`; a failure there is a real finding, not an environment
      excuse.

    Wipe `node_modules` completely before the first install — a stale Electron-32-ABI `.node` plus
    `-f` is the classic works-locally / red-on-CI split.

    Set in `package.json`: `"electron": "^43.4.1"`, `"electron-builder": "^26.15.3"`,
    `"@electron/rebuild": "^4.2.0"`, `"better-sqlite3": "^13.0.3"`, and set
    `"@types/better-sqlite3"` to `"^9.6.0"`.

    **Do not go looking for a 13.x types package — there is none, and asking for one ETARGETs the
    install.** Verified at plan time: `npm view @types/better-sqlite3 versions` ends
    `... 7.6.12, 7.6.13, 9.6.0` and `npm view @types/better-sqlite3 version` is `9.6.0`;
    DefinitelyTyped's major does not track better-sqlite3's. `better-sqlite3@13.0.3` also ships no
    `types` field of its own (`npm view better-sqlite3@13.0.3 types` prints nothing), so the DT
    package cannot simply be dropped — the TS build needs it. `9.6.0` is therefore the decision, not
    a guess, and the current pin is `^7.6.13`.
    The check that `9.6.0` is actually adequate for the 13.x surface this repo uses is
    `npm run typecheck`, which is already a hard gate in this task's acceptance criteria. If it
    fails on a 13.x API the types package does not know about, that is a finding to report in the
    SUMMARY with the failing symbol — do not silence it with `any`, a `@ts-ignore`, or a
    hand-written `declare module`. Leave `node-pty` at exact
    `1.1.0`, `typescript` at exact `5.9.3`, `electron-updater` at `^6.8.9`, `engines` at
    `">=20 <23"` and `.nvmrc` at `22` (D-07 — Electron's bundled Node 24 is internal to the
    binary and does not relax the host pin).

    D-06 is planned work, not a branch: bump `better-sqlite3` to 13.x. **There is no
    "keep Electron 43, go back to 11.10.0" state** — 11.10.0 is raw-V8 and cannot compile against
    Electron 43's V8 15, so the two bumps are one indivisible unit. If it breaks, you do not fall
    back to 11.10.0; you follow the § Recovery procedure in this plan's verification block, which reverts
    the whole commit. Record the compile evidence in the SUMMARY either way.

    Regenerate `package-lock.json` with the **installed** npm on the clean tree (`npm install`, no
    flags), then record the writer with `node --version && npm --version` in the task notes. Never
    hand-edit the lockfile. Review the lockfile diff before committing: the Electron 43 dep tree
    re-resolves broadly and this is the phase's moment of maximum supply-chain exposure (T-P01-03).

    **The former "the lockfile must be written by npm 10 under Node 22" rule is deleted, on purpose.**
    It had no check that could fail: npm 9, 10 and 11 all emit `lockfileVersion: 3`, so grepping
    `lockfileVersion` cannot tell them apart, and this host is Node v24.13.0 / npm 11.6.2 with no
    Node 22 installed (`which node` → `/c/Program Files/nodejs/node`; no nvm), so the rule was also
    locally unenforceable — it would have been satisfied by writing "done" and nothing else.
    Nothing in this repo depends on the writer's npm major. What actually has to hold is that the
    committed lockfile is complete and reproducible, and that is enforced by two checks below
    (`npm ci` idempotency locally, and `npm ci` on all three CI runners under the Node 22 that
    `setup-node` installs — which is npm 10 consuming this file, the direction that matters).

    Run the native rebuild (`electron-rebuild -f` via the existing postinstall path) and confirm
    both `node-pty` and `better-sqlite3` produce loadable `.node` files — **or**, per the constraint
    block at the top of this action, record the `NATIVE REBUILD UNAVAILABLE — ...` sentence and cite
    the `Electron smoke (ubuntu-latest)` check instead. Those are the only two acceptable outcomes;
    "assumed fine" is not one. If `tools/patch-node-pty-conpty.cjs` exits 1, the cause is a node-pty
    version change — investigate that, do not assume Electron.

    Read `electron-builder.yml` key by key against the v26 schema. Start with `mac.notarize: false`
    — an explicitly-set deprecated key is the single most likely schema error on this file. Try
    `npx electron-builder migrate-schema --dry-run` once; if the subcommand does not exist, do the
    read-through by hand and record that in the notes. Do not remove `afterSign: build/notarize.cjs`.
    Also re-read the `-c.win.azureSignOptions.*` flag names in `.github/workflows/release.yml`
    against v26's schema; that path never executes on this project (no `AZURE_*` secrets) but the
    flag names must still be correct.

    In `.github/workflows/ci.yml`: **do NOT add `npm rebuild better-sqlite3` to the three `test`
    jobs.** Open Question 2 is answered NO, and the evidence is in `<interfaces>` above. Short form
    for whoever is tempted to re-add it later:
    - 13.0.3 ships an N-API prebuild for every platform in the matrix, so after `npm ci
      --ignore-scripts` the module already loads under plain Node — verified here with a live FTS5
      round-trip on Node 24. Plan 10's FTS5 test needs nothing added.
    - `npm rebuild better-sqlite3` is not a cheap no-op like `npm rebuild node-pty` is. node-pty
      declares `install: node scripts/prebuild.js || node-gyp rebuild`, which finds a prebuild and
      exits 0 everywhere except Linux — that is why that step is safe. better-sqlite3 13.0.3
      declares no install script at all, so npm synthesises `node-gyp rebuild` off its `binding.gyp`
      and compiles from source unconditionally, discarding the prebuild.
    - The workflow's `setup-python@v5` 3.11 pin is `if: runner.os == 'Linux'`. A node-gyp build on
      the windows and macos runners would run against whatever Python the image ships, which is the
      exact 3.12+/no-distutils failure the Linux pin exists to prevent. Adding the step would turn
      two green hard-gate jobs red for a rebuild nothing needs.

    Keep the existing `npm rebuild node-pty` step and the Linux Python 3.11 pin exactly as they are.
    **Add no `continue-on-error` anywhere, for any reason.**

    Do make two workflow-comment edits — **`ci.yml` and `e2e.yml`, both**. The same false claim
    lives in both files and no other plan in this phase owns `e2e.yml`, so if this task does not
    take it, the falsehood ships. `.github/workflows/e2e.yml:16-17` reads "Node 22 everywhere, same
    reason as ci.yml: Node 24 ships no better-sqlite3 / prebuild and breaks node-pty's winpty gyp
    build." Note it is **line-wrapped** there — `prebuild` sits on `:17` — which is why a repo-wide
    `grep -rn "no better-sqlite3 prebuild" .github/` returns only the `ci.yml:9` hit and makes
    `e2e.yml` look clean. Correct it the same way as `ci.yml`'s: the Node 22 pin now rests on
    node-pty's winpty gyp build alone, and better-sqlite3 13.x is N-API. Change nothing else in
    `e2e.yml` — not `NODE_VERSION`, not the trigger block, not the xvfb steps.

    First the ci.yml edit: the `NODE_VERSION` comment at `:9-11` says "Node 24 ships no
    better-sqlite3 prebuild", which the bump makes false — 13.x ships N-API prebuilds and loads on
    Node 24. Correct that clause to say the Node 22 pin now rests on node-pty's winpty gyp build
    alone, and note that better-sqlite3 is N-API and must not be rebuilt in the `test` jobs. Leave
    `NODE_VERSION: "22"`, `engines` and `.nvmrc` unchanged — `test/ci-config.test.cjs:72-85` pins
    `engines` against `.nvmrc` and will fail if you drift them. That test reads only `package.json`
    and `.nvmrc`, never `ci.yml`, so the comment edit is free.

    **RESEARCH assumption A3 (ubuntu runner glibc >= 2.41): do NOT try to settle it with a local
    `ldd --version`.** This host is Windows under a Git-for-Windows shell, where `ldd --version`
    prints `ldd (cygwin) 3.6.5` — measured this session. That number is about Cygwin, not about
    ubuntu-latest's glibc, and pasting it into the task notes would satisfy a "paste the output"
    criterion while leaving A3 completely untested. The old form of this instruction did exactly
    that, and it is deleted.

    Two honest routes, in order:
    1. **If** a `Test (ubuntu-latest)` run log from this phase's PR is available, read the glibc off
       the runner there and paste the log line with its run URL. That is a real measurement of the
       real machine. Do **not** add an `ldd` step to `ci.yml` to obtain it — this task asserts a
       fixed `ci.yml` diff that other plans in this phase read as a baseline.
    2. **Otherwise** write, verbatim:
       `MEASUREMENT UNAVAILABLE — A3 (ubuntu-latest glibc >= 2.41) cannot be measured from this host; ldd here is Cygwin 3.6.5. Needs a ubuntu-latest run log.`
       and route the evidence to the PR check instead: A3 exists only to predict whether
       better-sqlite3 13.x's `prebuilds/linux-x64.node` loads on the ubuntu runner, and the
       `Test (ubuntu-latest)` job installs with `npm ci --ignore-scripts` and then runs the suite —
       so a green `Test (ubuntu-latest)` row on the draft PR IS the load proof, and a glibc too old
       for the prebuild shows up there as a hard failure, not as a silent pass. Record the PR check
       row as A3's disposition.

    Do not change the four `dialog.showOpenDialog` / `showSaveDialog` call sites
    (`index.ts:2539`, `:3432`, `:4080`, `:4103`) — Electron 43 defaults them to Downloads when
    `defaultPath` is omitted. Record the behaviour change in the SUMMARY as a known regression with
    the four anchors so a later phase can add `defaultPath`; changing `index.ts` here would take
    T-INDEX away from plan 02 in the very next wave.
  </action>
  <acceptance_criteria>
    - `node -e "console.log(require('./package.json').devDependencies.electron)"` prints a `^43.` string.
    - `grep -n '"node-pty"' package.json` still shows exact `1.1.0` with no caret or tilde.
    - `grep -n '"typescript"' package.json` still shows exact `5.9.3`.
    - `node -e "const l=require('./package-lock.json');console.log(l.lockfileVersion)"` prints `3`. (This is the shared npm 7+ format — it discriminates against a lockfile downgraded to v1/v2, which is all it can do. It says nothing about which npm major wrote the file, and no criterion here claims otherwise.)
    - The lockfile is complete and reproducible: `npm ci --ignore-scripts` exits `0`. **The discriminator is `npm ci`'s own exit code and nothing else.** `npm ci` never writes `package-lock.json` — it aborts with `npm error ... can only install packages when your package.json and package-lock.json are in sync` when the two disagree — so the `git diff --exit-code package-lock.json` half of the old formulation can never fail on its own and is not the check. Run it anyway, chained as `npm ci --ignore-scripts && git diff --exit-code package-lock.json`, but read it for what it is: a tripwire for a hand-edit or a stray tool touching the file between the two commands, not evidence about the lockfile's completeness. Paste `npm ci`'s exit code, not just the `git diff` one. This is the check the deleted npm-10 rule never had — a lockfile out of sync with `package.json` fails it regardless of writer.
    - `node -e "const l=require('./package-lock.json');const b=l.packages['node_modules/better-sqlite3'];console.log(b.version, b.hasInstallScript)"` prints a `13.` version and `undefined` (13.0.3 declares no install script). Paste it — it is the standing proof that the CI rebuild step below must stay absent.
    - The task notes record the lockfile writer verbatim: `node --version && npm --version`, **and** which install actually produced the lockfile — bare `npm install` or the `--ignore-scripts` fallback. One of those two words appears; "install" alone does not satisfy it.
    - **The Electron-ABI native-load claim is evidenced, not assumed.** Either the task notes paste the successful `electron-rebuild -f` output together with `ls node_modules/better-sqlite3/build/Release` and `ls node_modules/node-pty/build/Release` both listing a `.node` file (all three absent today — baseline verified at plan time), **or** they contain the verbatim `NATIVE REBUILD UNAVAILABLE — ...` sentence from the action plus the `Electron smoke (ubuntu-latest)` row from `gh pr checks` showing `pass`. A `<done>` line claiming "natives rebuilt and loadable" with neither artifact present is a failed criterion.
    - `npm test` exit code is `0` locally.
    - `npm run typecheck` exit code is `0`.
    - `grep -c "continue-on-error" .github/workflows/ci.yml` returns the same number as before this task — paste both. Baseline today: `4` (two live directives at `:35` npm-audit and `:117` native-rebuild, both outside the `test` matrix, plus two mentions in comments at `:45` and `:113`). **None may be added anywhere, for any reason.**
    - `grep -c "npm rebuild better-sqlite3" .github/workflows/ci.yml` returns `0`, and `grep -c "npm rebuild node-pty" .github/workflows/ci.yml` returns `1`. The first is the dropped gate made executable; the second proves the drop did not take the node-pty step with it.
    - `grep -vE "^\s*#" .github/workflows/ci.yml | grep -c "better-sqlite3"` returns `1` — unchanged from today's baseline (the `build` job's rebuild step name at `:111`, which this plan does not touch). A new non-comment occurrence in the `test` jobs would push this to `2` and fail.
    - `grep -c "setup-python" .github/workflows/ci.yml` returns `1`, and its `if:` guard is on the line **below** it, not above (`ci.yml:84` is the `uses: actions/setup-python@v5` line, `:85` is `if: runner.os == 'Linux'`). Check that as one pipeline so it is pass/fail without reading by eye: `grep -A1 "setup-python" .github/workflows/ci.yml | grep -c "if: runner.os == 'Linux'"` returns `1` (verified against the current file at plan time). Paste both counts. No node-gyp build may be introduced on a runner with no pinned Python.
    - The corrected `NODE_VERSION` comment landed: `sed -n '1,16p' .github/workflows/ci.yml | grep -ci "no better-sqlite3 prebuild"` returns `0` (baseline today is `1` — the phrase sits whole on `:9`), and `sed -n '1,16p' .github/workflows/ci.yml | grep -ci "n-api"` returns at least `1` (baseline today is `0`). Paste both baselines and both new counts.
    - The same correction landed in `e2e.yml`, which carries the identical false claim and which no other plan owns: `sed -n '1,20p' .github/workflows/e2e.yml | grep -ci "no better-sqlite3"` returns `0` (baseline today is `1`), and `sed -n '1,20p' .github/workflows/e2e.yml | grep -ci "n-api"` returns at least `1` (baseline today is `0`). Grep the wrapped fragment `no better-sqlite3`, **not** the whole phrase — the phrase breaks across `:16`/`:17` there, so `grep -c "no better-sqlite3 prebuild" .github/workflows/e2e.yml` returns `0` today and would pass with the falsehood still live. Paste both baselines and both new counts.
    - `git diff --stat .github/workflows/e2e.yml` shows a comment-only change: `grep -c "NODE_VERSION: \"22\"" .github/workflows/e2e.yml` still returns `1`, and `grep -c "branches: \[main\]" .github/workflows/e2e.yml` still returns `2` (the `push` and `pull_request` triggers plan 01-01 task 3 depends on — do not touch them).
    - The task notes settle RESEARCH assumption A3 by **one** of the two routes in the action, with its literal artifact pasted: either a ubuntu-latest run-log line showing the runner's glibc (with the run URL), or the exact `MEASUREMENT UNAVAILABLE — A3 ...` sentence plus the green `Test (ubuntu-latest)` row from `gh pr checks`. A local `ldd --version` from this host does **not** satisfy this criterion and must not be pasted as if it did.
    - The task notes contain the electron-builder.yml read-through result including the `mac.notarize` decision.
  </acceptance_criteria>
  <verify>
    <automated>npm test && npm run typecheck</automated>
  </verify>
  <done>Runtime bumped in one revertable commit; lockfile regenerated on a clean tree and proven complete by `npm ci --ignore-scripts` exiting `0`; natives either rebuilt and loadable here, or explicitly recorded `NATIVE REBUILD UNAVAILABLE` with the `Electron smoke (ubuntu-latest)` check standing as the Electron-ABI load proof; builder config read through; ci.yml's stale better-sqlite3 comment corrected with no rebuild step and no new continue-on-error added.</done>
</task>

<task type="auto">
  <name>Task 3: Add the D-10 Electron-version assertion to the e2e smoke spec and run the three-platform gate plus e2e</name>
  <files>e2e/smoke.spec.ts</files>
  <read_first>
    - `e2e/smoke.spec.ts` in full — how `electronApp` is obtained and where an `evaluate` call fits.
    - `playwright.config.ts` — the app is launched from `out/main/index.js`; `workers: 1`, `retries: 0`.
    - `.github/workflows/e2e.yml` — Linux/xvfb only, the ONLY real-Electron job.
    - `01-RESEARCH.md` § "Code Examples 2" and § Finding 7 — the literal assertion and why the MAJOR is pinned rather than the exact string.
    - `01-VALIDATION.md` § "The Nyquist Problem" — why this assertion is not optional.
  </read_first>
  <action>
    Add one assertion inside the launched app: `electronApp.evaluate(({ process }) => ({ electron:
    process.versions.electron, modules: process.versions.modules }))`, then assert the parsed major
    of `versions.electron` is `>= 43`, written literally as
    `expect(major, ...).toBeGreaterThanOrEqual(43)` — that exact call is what the acceptance
    criterion below greps for, and `expect(...).toBe(43)` would both fail the criterion and wrongly
    turn a patch bump red. Record `versions.modules` (the native ABI number) in the same
    assertion's message because that is what a native-module mismatch actually reports. Pin the
    MAJOR only — a patch bump must not turn the gate red, a silent revert to 32 must. Carry a
    comment naming D-08: `npm test` stubs `electron` and is structurally incapable of catching this.

    **Open the phase's draft PR to `main` BEFORE claiming any CI result — without it nothing runs
    at all.** `.github/workflows/ci.yml:4-7` and `.github/workflows/e2e.yml:11-14` both trigger on
    exactly two events: `push` to `main` and `pull_request` whose BASE is `main`. This phase's work
    happens on a phase branch, never on `main`, so `git push` of that branch produces **zero
    workflow runs** — there is no run URL to paste and no checks to read. A `pull_request` targeting
    `main` DOES fire both, and neither workflow has a draft guard (no `types:` filter, no
    `github.event.pull_request.draft` condition anywhere in either file — verified at plan time), so
    a **draft** PR is sufficient and does not imply the phase is ready to merge.

    Do **NOT** "fix" this by editing the workflow `on:` triggers. That changes CI policy for the
    whole repository, and Task 2 already asserts `ci.yml` diffs that other plans in this phase read
    as a fixed baseline.

    From the phase branch, run:
    ```
    git push -u origin HEAD
    gh pr create --draft --base main --title "Phase 01: finish the floor" --body "Draft PR opened by plan 01-01 task 3. ci.yml and e2e.yml only run on a push to main or a pull_request based on main, so this PR is what makes CI run for the phase branch at all. Every phase-01 plan that closes on CI green resolves against this PR's checks. Kept draft until plan 23's gate."
    ```
    If a PR from this branch to `main` already exists (a re-run, or one was opened earlier), do
    **not** open a second: `gh pr view --json url,isDraft,baseRefName` and use that URL, after
    confirming `baseRefName` is `main` — a PR based on anything else triggers neither workflow.

    Record the PR URL in the task notes **and** in the SUMMARY. It is the phase's only pointer to
    its own CI; roughly twenty later plans close on "CI green" and have no other way to find the
    checks they are claiming.

    Then confirm CI green on that PR for every job, by its **job** name as `gh pr checks` reports it:
    `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`, `Build` and
    `Electron smoke (ubuntu-latest)`. Run `gh pr checks --watch`. A local green run is not the gate.
  </action>
  <acceptance_criteria>
    - `grep -c "process.versions.electron" e2e/smoke.spec.ts` returns at least `1`.
    - `grep -vE "^\s*(//|\*|/\*)" e2e/smoke.spec.ts | grep -cE "toBeGreaterThanOrEqual\(\s*43\s*\)"` returns at least `1`. Paste the matched line. **This criterion was rewritten because the previous one was backwards and would have inverted the phase's single automated Electron-version gate**: it required `===?` — a literal `==`/`===` — next to `43`, so the `>= 43` assertion this task actually prescribes scored `0`, while a file containing nothing but a comment reading `versions.electron >= 43` scored `1` (both verified at plan time). The pipeline above matches the literal assertion form and strips comment lines first, so a comment cannot satisfy it. Baseline today is `0`.
    - **The e2e gate is the `Electron smoke (ubuntu-latest)` check on the draft PR, not a local run.** `gh pr checks` shows a row named exactly `Electron smoke (ubuntu-latest)` with status `pass` on the PR opened above — paste the row. This is the binding evidence for D-10.
      **Use the JOB name, never the workflow name.** `gh pr checks` lists **job** names. `E2E` is the *workflow* name (`.github/workflows/e2e.yml:1` is `name: E2E`) and appears in **no** `gh pr checks` row; the job's name is set at `e2e.yml:30` — `name: Electron smoke (ubuntu-latest)`. A criterion demanding an `E2E` row therefore fails a fully green PR. The three `ci.yml` names in the criteria below were already taken from the right field: `Typecheck` (`ci.yml:17`), `Test (${{ matrix.os }})` (`ci.yml:39`, expanding to `Test (ubuntu-latest)` / `Test (windows-latest)` / `Test (macos-latest)`) and `Build` (`ci.yml:100`). If a job is ever renamed in either workflow, re-derive these strings from the `name:` under `jobs.<id>` — never from the file's top-level `name:`.
    - `npm run e2e` locally is OPTIONAL and, on this host, currently impossible — do not claim it without having actually run it, and do not treat its absence as a failure of this task. `npm run e2e` is `playwright test`, and on this tree `node_modules/@playwright` **does not exist** (it is in `package-lock.json` but was never installed), `node_modules/better-sqlite3/build` does not exist and `node_modules/node-pty/build/Release` does not exist — all three verified at plan time, and together they mean the only install that has ever succeeded here is `npm ci --ignore-scripts`. A local e2e run needs, in order: (1) Node 22 present (see Task 4's constraint block — this host is Node v24.13.0 with no Node 22 and no nvm, and it is the same blocker), (2) a **full-scripts** `npm ci` so postinstall's `electron-rebuild -f` produces Electron-ABI natives, (3) `npx playwright install`, (4) `npm run build` — `e2e/smoke.spec.ts:30` states outright that it drives `out/main/index.js` and no plan in this phase runs the build before e2e. If all four hold, run it and paste the exit code. If they do not, write `MEASUREMENT UNAVAILABLE — local e2e needs Node 22 + full-scripts npm ci + npx playwright install + npm run build; none present on this host` and rely on the PR's `Electron smoke (ubuntu-latest)` check (the job name at `e2e.yml:30`; `E2E` at `e2e.yml:1` is the workflow name and is never a `gh pr checks` row), which does all four on its own runner.
    - A PR from the phase branch to `main` exists and its checks run: `gh pr view --json url,number,state,isDraft,baseRefName` exits `0` and prints `"state":"OPEN"` with `"baseRefName":"main"`. Paste the JSON. **Do NOT assert `"isDraft":true`.** Measured 2026-08-21 on this branch: PR **#74** is already OPEN against `main` with `"isDraft":false`, so `gh pr create --draft` errors and the action's reuse path yields a non-draft PR — an `isDraft:true` criterion fails on the correct outcome. Draft status is irrelevant to the thing this gate exists for: neither workflow has a `types:` filter or a `pull_request.draft` condition, so both fire on draft and non-draft alike. What matters is that the PR targets `main` and that its checks actually ran. The URL it prints is the phase's CI pointer.
    - That PR's checks actually ran — `gh pr checks` exits `0` and lists a row for each of `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`, `Build` and `Electron smoke (ubuntu-latest)`, every one of them `pass`. Those six strings are **job** names read from `ci.yml:17`, `:39`, `:100` and `e2e.yml:30`; there is no row named `E2E`. Paste the full table. An empty table or "no checks reported" means the PR is not based on `main` and nothing was triggered — fix that before claiming anything, do not paste a local run in its place.
    - The PR URL is written into `01-01-SUMMARY.md`, not only into the task notes: `grep -c "github.com/.*/pull/" .planning/phases/01-finish-the-floor/01-01-SUMMARY.md` returns at least `1`.
  </acceptance_criteria>
  <verify>
    <!-- Nyquist note: this is the slowest automated verify in the phase, and it is kept deliberately.
         D-10's clause is an assertion about the RUNTIME Electron major inside a launched app. All 426
         unit tests run with `electron` stubbed through `require.cache`, so they are structurally
         incapable of failing on an Electron-version regression — `npm test` here would be a green
         run that asserted nothing. No faster command samples this signal.

         The command below is `gh pr checks`, NOT `npm run e2e`, and that is deliberate: `npm run e2e`
         is not executable on this host (no `node_modules/@playwright`, no built natives, no
         `out/main/index.js` — see the acceptance criteria), so naming it here would be an automated
         verify that cannot run. `gh pr checks` reads the `Electron smoke (ubuntu-latest)` job on the
         draft PR this task opens (the JOB name at `e2e.yml:30` — `E2E` at `e2e.yml:1` is the workflow
         name and is never a `gh pr checks` row),
         which boots real Electron under xvfb and evaluates the same D-10 assertion. Same signal,
         actually runnable. It exits non-zero while any check is pending or failing. -->
    <automated>gh pr checks</automated>
  </verify>
  <done>The one automated Electron-version gate exists and is green; the three-platform matrix and e2e are green on the remote.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live Windows operator run (D-09) — FLOOR-03's closure evidence</name>
  <read_first>
    - `.planning/phases/01-finish-the-floor/01-VALIDATION.md` § "The Nyquist Problem" and § "Manual-Only Verifications" — why this run cannot be replaced by a CI link.
    - `01-CONTEXT.md` D-08, D-09 — the blind spot and the required evidence.
    - Task 2's notes — the better-sqlite3 outcome and the electron-builder read-through result.
  </read_first>
  <acceptance_criteria>
    - The SUMMARY records three explicit answers: PTY live yes/no, persisted better-sqlite3 write survived a relaunch yes/no, visual pass over Pixi + xterm + Monaco clean yes/no.
    - The built binary launched was from `dist/win-unpacked/`, not `npm run dev` — state which was run.
    - Any visual defect found under Chromium 150 is recorded with the surface it appeared on.
    - The Node 22 precondition is accounted for explicitly: the SUMMARY names which of (a) install Node 22 here, (b) build where Node 22 exists, or (c) record UNAVAILABLE was taken, and pastes the `node --version` of the machine that actually produced `dist/win-unpacked/`. If (a) or (b), that output reads `v22.` — a `v24.` reading with a claimed successful build is self-contradictory and must be investigated, not recorded. If (c), the verbatim UNAVAILABLE line above is in the SUMMARY, this checkpoint is NOT approved, and FLOOR-03 is reported still open.
  </acceptance_criteria>
  <what-built>
    Electron 32 → 43.4.1, electron-builder 25 → 26.15.3, @electron/rebuild 3 → 4.2.0,
    better-sqlite3 11.10.0 → 13.x, lockfile regenerated on a clean tree and verified reproducible,
    natives rebuilt, three-platform CI green, e2e version assertion green.
  </what-built>
  <how-to-verify>
    This is D-09, and it is FLOOR-03's closure evidence. "CI green" is explicitly NOT acceptable
    (D-08): all 426 unit tests run under plain Node with `electron` stubbed, and the only
    real-Electron job is Linux-only, so the Windows ConPTY path is exercised by no automated job
    anywhere.

    **PRECONDITION — read this before step 1; it is not routine and it is very likely to block.**
    Building here is not a plain `npm run build`. Producing `dist/win-unpacked/` requires a
    **full-scripts** install first, and this repo's `postinstall` is
    `electron-rebuild -f && node tools/ensure-pty-perms.cjs && node tools/patch-node-pty-conpty.cjs`
    — `electron-rebuild -f` source-compiles `node-pty` against the Electron headers, which is
    exactly the winpty gyp build that `.github/workflows/ci.yml:9-11` and `.nvmrc` pin Node 22 to
    avoid. **This host is Node v24.13.0 with no Node 22 installed and no nvm** (`node --version` →
    `v24.13.0`; `which nvm` → not found), which is the same fact Task 2 already states about the
    lockfile writer. Evidence that the full install has in fact never succeeded here, all verified
    at plan time: `node_modules/better-sqlite3/build` does not exist,
    `node_modules/node-pty/build/Release` does not exist, and `node_modules/@playwright` does not
    exist — only `--ignore-scripts` installs have run on this tree. `package.json` `engines` is
    `">=20 <23"`, so Node 24 is out of range for this package regardless.

    So pick ONE of these three and say in the SUMMARY which you took:
    - **(a) Install Node 22 on this machine first** (nvm-windows, or the Node 22 LTS MSI), confirm
      `node --version` reports `v22.x`, then `rm -rf node_modules && npm ci` **without**
      `--ignore-scripts` and check that `electron-rebuild -f` exits `0` and
      `node_modules/node-pty/build/Release` now has a `.node` in it. Only then run step 1.
    - **(b) Run the build where Node 22 already exists** — another Windows box, or a Windows CI
      runner with `setup-node` at 22 — and do the operator steps there. The requirement is a
      Windows build launched from `dist/win-unpacked/`; it is not a requirement that it be built on
      *this* box.
    - **(c) Record D-09 as UNAVAILABLE.** Write, verbatim and in the SUMMARY:
      `D-09 UNAVAILABLE — needs Node 22 (this host is v24.13.0, no nvm) for a full-scripts npm ci so postinstall's electron-rebuild -f can build node-pty against the Electron headers; no Windows dist/win-unpacked/ was produced and no PTY / persistence / visual answers exist.`
      **UNAVAILABLE is not a pass.** This checkpoint is `gate="blocking"` and it is FLOOR-03's
      closure evidence; taking (c) means FLOOR-03 stays OPEN and the phase reports blocked on a
      missing toolchain. Do not substitute a CI link (D-08 forbids it), do not substitute
      `npm run dev`, and do not mark the checkpoint approved.

    With the precondition satisfied, on this Windows machine:
    1. Build the app: `npm run build` then the packaged target (`npx electron-builder --win --dir`
       is enough — a full installer is not required).
    2. Launch the built binary from `dist/win-unpacked/` (not `npm run dev`).
    3. Open a terminal pane and spawn one agent. Confirm a real PTY is live: type `echo hello` and
       see it echo back. This is the byte-exact-patched ConPTY path that no job tests.
    4. Confirm a real `better-sqlite3` write lands: change any setting that persists, quit, relaunch,
       confirm it survived. (`PersistStore` is the store; a failed native load degrades silently to
       defaults, which is exactly the failure this step exists to catch.)
    5. Budget one visual pass over the office floor (Pixi), one terminal pane (xterm + WebGL addon)
       and one editor pane (Monaco) under Chromium 150 — RESEARCH assumption A8 is LOW confidence
       and this is the only sample that settles it. Report anything visually broken.
    6. Paste the app's own reported Electron version if surfaced, or note it from the e2e assertion.

    Report: PTY live yes/no, persisted write survived yes/no, visual pass clean yes/no.
  </how-to-verify>
  <files>(none — operator verification of the built app)</files>
  <action>Pause execution and present the numbered verification steps below to the operator verbatim. Do not proceed, do not mark FLOOR-03 closed, and do not write the SUMMARY until the three answers are returned. Record the answers verbatim in the SUMMARY — this run IS FLOOR-03's closure evidence and a CI link may not be substituted for it (D-08/D-09).</action>
  <verify><human-check>Operator returns three yes/no answers: PTY live, persisted better-sqlite3 write survived a relaunch, visual pass clean.</human-check></verify>
  <done>The operator has run the built Windows binary and the three answers are recorded in the SUMMARY.</done>
  <resume-signal>Type "approved" with the three yes/no answers, or describe what broke. If the Node 22 precondition cannot be met, the correct signal is not "approved" — it is the verbatim `D-09 UNAVAILABLE — ...` line, which leaves this gate closed and FLOOR-03 open.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → this repo | Every bumped package and its re-resolved transitive tree crosses here. The Electron 43 tree re-resolves broadly, which is why this plan is the phase's maximum supply-chain exposure. |
| GitHub Actions runner → build artifacts | The three-platform matrix compiles native addons from source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P01-01 | Tampering | `package-lock.json` regeneration | mitigate | Regenerate on a wiped `node_modules`, never hand-edit, review the lockfile diff before commit, and prove reproducibility with `npm ci --ignore-scripts && git diff --exit-code package-lock.json` (D-07, Pitfall 7). The writer's npm major is not a mitigation — npm 9/10/11 all emit `lockfileVersion: 3`, so it was unverifiable; the `npm ci` reproduction is what actually catches a tampered or incomplete tree. |
| T-P01-02 | Tampering | native addon rebuild (`node-pty`, `better-sqlite3`) | mitigate | `tools/patch-node-pty-conpty.cjs` does a byte-exact match and exits 1 on mismatch; keep `node-pty` exact-pinned at 1.1.0 so the patch's precondition is unchanged (D-05). |
| T-P01-03 | Tampering | transitive dependency confusion during re-resolve | mitigate | `npm ci` from the committed lockfile in CI; the existing advisory `npm audit --audit-level=high` step at `ci.yml:31-36` stays; dependabot opens bump PRs. |
| T-P01-SC | Tampering | npm installs in this plan | mitigate | `01-RESEARCH.md` § Package Legitimacy Audit dispositions every package here as **Approved** — all six are existing dependencies being version-bumped, none `[ASSUMED]`/`[SUS]`/`[SLOP]`. No blocking legitimacy checkpoint is required. Re-verify `npm view electron dist-tags` and `npm view better-sqlite3 version` before installing (research valid only to 2026-09-03). |
| T-P01-04 | Denial of service | CI test jobs under Electron 42+ lazy download | mitigate | Task 1's `require.cache`-only `requireElectron()` removes the ~100 MB per-runner download entirely. |
| T-P01-05 | Repudiation | claiming FLOOR-03 closed on a CI link | mitigate | D-08/D-09: the human-verify checkpoint is mandatory closure evidence; a CI link alone is explicitly rejected. |
</threat_model>

<verification>
- `npm test` green on ubuntu, windows and macos with no `continue-on-error` added (`ci.yml`).
- `npm run typecheck` green.
- The draft PR's `Electron smoke (ubuntu-latest)` job green including the new Electron-major assertion, read with
  `gh pr checks`. A local `npm run e2e` is NOT the gate and is not runnable on this host — Task 3
  states the four preconditions and the `MEASUREMENT UNAVAILABLE` wording if they are unmet.
- The phase's draft PR to `main` is open and its checks ran (`gh pr checks` lists and passes every
  job) — the workflows fire on nothing else this phase does, so with no PR there is no CI at all
  and every "CI green" claim in this phase is unresolvable.
- D-09's live Windows run recorded with the three yes/no answers.
- The rollback pin: Task 2's version-bump commit SHA recorded in the SUMMARY as the single revert
  target (D-03), and Task 1's loader commit SHA recorded separately as the commit that must NOT be
  reverted with it.

## Recovery — what to do when the Electron bump breaks the native rebuild

Not a "fallback version". There is no independently-revertable piece: `better-sqlite3` 11.10.0 is
raw-V8 and cannot compile against Electron 43's V8 15, and 13.x declares `engines.node >= 22`, so
"Electron 43 with the old sqlite" and "Electron 32 with the new sqlite" are both non-states. The
unit of revert is Task 2's whole commit.

**How you know it broke** — in the order the signals fire:
1. `npx electron-rebuild -f` (postinstall) exits non-zero. That is the rebuild itself failing.
2. `tools/patch-node-pty-conpty.cjs` exits 1. Per D-05 this means **node-pty's sources changed**,
   not that Electron changed. `node-pty` is exact-pinned at `1.1.0`, so if this fires while
   `grep '"node-pty"' package.json` still reads `1.1.0`, a wrong tree got installed — wipe
   `node_modules` and `npm ci` again before concluding anything about Electron.
3. Everything exits 0 but the module will not load under the Electron ABI. Both natives are N-API
   (`better-sqlite3` → `node-addon-api ^8`, `node-pty` → `node-addon-api ^7`), so a plain-Node
   `require()` proves nothing about Electron. The load check that counts is Task 3's `Electron smoke
   (ubuntu-latest)` job on the
   draft PR (not a local `npm run e2e` — it does not run here) and, for the ConPTY path, Task 4's
   live Windows run — a failed native load degrades silently to
   defaults rather than throwing, which is exactly why D-09 exists.

**The recovery sequence**, run in this order:
1. `git rev-parse HEAD` — record the broken SHA in the SUMMARY before touching anything.
2. `git revert --no-edit <Task 2 SHA>`. This returns `package.json`, `package-lock.json` and
   `electron-builder.yml` together. Do not cherry-pick parts of it.
3. `rm -rf node_modules && npm ci`. **Mandatory** — reverting the lockfile without wiping
   `node_modules` leaves Electron-43-ABI `.node` files sitting under an Electron 32 install, which
   is the works-locally/red-on-CI split this plan already wipes `node_modules` to avoid.
4. Do NOT revert Task 1's loader commit. It is deliberately a separate commit and was proven green
   on Electron 32 before the bump landed; reverting it re-opens D-25 and re-breaks four later
   requirements for no reason.
5. Task 3's e2e assertion (`electron` major >= 43) will now be RED, correctly. Do not relax it to
   make the tree green — that is the one automated guard against a silent revert to 32.

**The state this leaves the repo in, stated plainly:** a buildable dev tree on Electron 32, with
FLOOR-03 still open, Task 3's gate red, and the whole phase still blocked — because every other
plan in this phase is gated on this one (D-03). A revert is a stop, not a recovery. Report it as a
blocked phase and re-plan (retarget the bump, or fix the specific native break) rather than
proceeding to Wave 2 on Electron 32.
</verification>

<success_criteria>
- ROADMAP criterion 2's runtime half is TRUE, read as D-02 restates it: Electron 43.x, inside the
  latest-3 supported window, with `node-pty` and `better-sqlite3` rebuilt and the three-platform
  suite green.
- Issue #10's runtime clauses are closed in this PR per D-43: acceptance text quoted verbatim, one
  command per clause with pasted output, the named test (`e2e/smoke.spec.ts` version assertion),
  and the `npm test` exit line — run on Windows, since this is platform-touching.
- `test/load-ts.cjs` can resolve `.tsx`, unblocking FLOOR-01, FLOOR-07's panel, FLOOR-13 and
  FLOOR-15 in later waves.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-01-SUMMARY.md` when done. It MUST record: the phase's
draft-PR URL (Task 3's — the pointer every later plan's "CI green" claim resolves against), the
rollback pin SHA (Task 2's) and Task 1's loader SHA as the commit excluded from any revert, the
better-sqlite3 outcome (bumped or not, with the compile evidence), the electron-builder.yml
read-through result, the lockfile writer (`node --version && npm --version`) plus the `npm ci` exit
code (the real reproduction discriminator — see Task 2), A3's disposition (a ubuntu-latest glibc
reading with its run URL, or the `MEASUREMENT UNAVAILABLE — A3 ...` sentence and the green
`Test (ubuntu-latest)` row), whether the full-scripts install and native rebuild ran here or fell to
`--ignore-scripts` with the `NATIVE REBUILD UNAVAILABLE` sentence, the four `dialog.*` sites left
unchanged as a known Downloads-default regression, and D-09's live-run answers.
</output>
