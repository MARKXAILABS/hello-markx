---
phase: 01-finish-the-floor
plan: 19
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/SkillsTab.tsx
  - src/renderer/src/components/MessageQueueComposer.tsx
  - src/renderer/src/components/WorkersTab.tsx
  - src/renderer/src/components/MemoryPanel.tsx
  - src/renderer/src/components/MemoryGraphPanel.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/realtime/CostHud.tsx
  - src/renderer/src/components/AskMeTab.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the eight highest-density renderer files sits below the 14px floor except allowlisted decorative glyphs"
    - "Every icon-only button in these eight files has an accessible name; no button with visible text gained an aria-label"
    - "Plan 12's 1024px collapse, plan 10's scope warning and plan 05's useHiveTasks call all survive the sweep unchanged"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
    - "The three sites M1 cannot see — SkillsTab's decimal, MemoryGraphPanel's ternary and the composer's zoom-derived size — are all at or above 14, and the M1d/M1x scans over this group are clean"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "the shell swept without disturbing the 1024px collapse plan 12 added"
    - path: "src/renderer/src/components/SkillsTab.tsx"
      provides: "the single densest sweep surface in the group (16 sites)"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the eight highest-density remaining renderer files to the corrected type scale and give every
icon-only button in them an accessible name.

Purpose: FLOOR-12, sweep sub-group 5 of 6. Measured at plan time and re-verified today: **85 sub-14px
`fontSize` occurrences across 8 files** — `SkillsTab.tsx` (16), `MessageQueueComposer.tsx` (16),
`WorkersTab.tsx` (10), `MemoryPanel.tsx` (9), `MemoryGraphPanel.tsx` (9), `App.tsx` (9),
`CostHud.tsx` (8), `AskMeTab.tsx` (8). **Occurrences, not lines** — wave 9 counts occurrences
(`grep -o`), so this plan does too; for these eight files the two units are equal today, and the unit is
stated beside every pinned number so the two waves reconcile.

Plus **three M1-invisible sites** these eight files also own and none of those counts includes:
`SkillsTab.tsx:254` `fontSize: 10.5`, `MemoryGraphPanel.tsx:333` `fontSize: isTopic ? 12 : 11`, and
`MessageQueueComposer.tsx:355` `fontSize: composerFontSize` (minimum **8**).

**Why this plan is one half of a pair.** The remaining-renderer surface is 31 files and ~160 sites.
Classifying all 31 (rule table + hoisted-object consumer map + button inventory + lint anchors +
fixed-height integers) and then editing 23 of them exceeds a single agent's context budget by roughly
2×, and a sweep that runs out of context mid-file produces a *partial* sweep whose only failure
surface is plan 23's completeness bar in wave 9 — five waves and one full context reset later. So the
surface is split on its natural boundary: this plan owns the eight densest files, plan 20 owns the
other 23. The two file sets are disjoint, both run in wave 7, and neither depends on the other.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract; its rules are mechanical and checked in order.

Output: 8 files clearing the floor, an accessible-name pass, and this sub-group's frozen Rule 0
allowlist handed to plan 23.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` to see the sites, and
`grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` to count them.

**Count occurrences (`-o`), never lines (`-c`) — the unit matters.** Wave 9 (plan 23) pins the
completeness bar with `grep -rhoE … | wc -l`, i.e. how many times the trimmed text appears. Measured
today the whole tree is **604 occurrences vs 603 lines**; the single divergence is
`IntegrationsRegistry.tsx:388` (plan 17's file), which carries `fontSize: 12` and `fontSize: 10` on one
physical line. That file is plan 17's; for this plan's eight files the two units are equal today (16 / 16 / 10 / 9 / 9
/ 9 / 8 / 8 — 85 either way). Every per-file number in this plan is therefore an occurrence count, so wave 7 and
wave 9 reconcile instead of disagreeing by one.

**M1's blind spots — the sub-14px sites the regex structurally cannot see.** M1 needs a digit
immediately after `fontSize:`, and its trailing `[^0-9.]` guard drops decimals. So a quoted (`'13px'`),
decimal (`12.5`), ternary (`isTopic ? 12 : 11`) or computed (`Math.max(8, …)`) size is invisible to M1 —
and therefore to plan 23's repo-wide bar. Two extra scans close that hole, run over **this plan's paths
only**:
- **M1d — decimal and quoted sub-14 literals:**
  `grep -rnoE "fontSize *[:=] *\{?['\"]?(1[0-3]|[1-9])\.[0-9]+|fontSize *[:=] *\{?['\"](1[0-3]|[1-9])(\.[0-9]+)?px['\"]" <this plan's paths>`
- **M1x — expression-valued sizes:**
  `grep -rnE "fontSize *[:=] *\{?[A-Za-z_\$]" <this plan's paths>`

An M1d hit is an ordinary sub-14px literal and takes the same rule a visible one would. An M1x hit is
recorded with its **evaluated minimum** — the smallest value the expression can return over its inputs —
and a minimum below 14 is a site this plan fixes. **Verified present in this plan's files today — three sites, none of them in the 85:**

| Site | Today | Rule |
|---|---|---|
| `SkillsTab.tsx:254` `fontSize: 10.5` | a **decimal**, dropped by M1's `[^0-9.]` guard; the skill's filesystem path, `fontFamily: 'var(--cth-font-mono)'` in the same object | **Rule 2 mono** — `var(--cth-text-mono-md)` + `var(--cth-lh-mono)` |
| `MemoryGraphPanel.tsx:333` `fontSize: isTopic ? 12 : 11` | a **ternary**; the SVG node label in the memory graph, family also a ternary between mono and ui | **Rule 2**, per branch — `isTopic ? 'var(--cth-text-mono-md)' : 'var(--cth-text-body-md)'`, both branches at 14 |
| `MessageQueueComposer.tsx:355` `fontSize: composerFontSize` | `composerFontSize = useTerminalFontSize()` (`:64`), whose own minimum is **8**, so the box the operator types into renders at 8px at minimum zoom | **floor it at 14** — `Math.max(14, useTerminalFontSize())`; `composerLineHeight` at `:65` derives from it and follows |

**The terminal carve-out, stated once and applied consistently.** UI-SPEC's *"terminal sizing is
user-controlled and is not governed by `DESIGN.md:706`"* covers **xterm's own font and nothing else**:
`components/terminalFontSize.ts`, `components/terminalPool.ts`, and the `term.options.fontSize`
assignments in `PtyTerminalView.tsx`. It does **not** cover app chrome that merely *reads* the shared
zoom. The message composer is a textarea, not a terminal — `terminalFontSize.ts`'s own comment says it
was wired to the shared zoom precisely so it would stop being stuck at a hardcoded size, which is an
argument for following the zoom, never for going below the floor while doing so. Carving out
`terminalFontSize.ts` and silently also its readers is how a sub-14px site survives the bar.

> **DECISION — the composer is chrome, and it is floored locally.** Verified 2026-08-21 against source:
> `MessageQueueComposer.tsx:64` is `const composerFontSize = useTerminalFontSize();` with **no clamp or
> `Math.max` of its own**, so its minimum of 8 comes entirely from `MIN_TERMINAL_FONT_SIZE = 8` in
> `terminalFontSize.ts:14` — the file UI-SPEC carves out as user-controlled terminal sizing. Raising that
> constant to 14 would floor the composer *by changing xterm's own minimum zoom*, contradicting the
> carve-out and taking a user-facing setting away to satisfy a typography rule. So the floor goes on the
> **consumer**, not the shared store: `Math.max(14, useTerminalFontSize())` in this file only, and
> `MIN_TERMINAL_FONT_SIZE` stays **8**. This plan does not edit `terminalFontSize.ts`, and the criterion
> below asserts that constant is still `8` afterwards. It is the same treatment plan 14 gives
> `FullscreenTerminal.tsx`'s zoom readers (the three `rosterScale` clamps, `noteFontSize`,
> `noteLabelSize`), so all six of the shared zoom's chrome consumers are handled identically and the
> carve-out means one thing across the phase: **xterm's own font, nothing that merely reads the zoom.**

**This sub-group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`AskMeTab.tsx` `✕` and `└`. That is the complete list for these eight files. Each gains
`aria-hidden="true"` and keeps its size. **None of the four NOT-exempt cases is in this group** —
all four live in plan 17's files. **No hoisted shared style object in this sub-group has a mixed
text/glyph consumer set** — this pair's verified case (`FullscreenFileEditor.tsx:59` `const chip`)
is in plan 20's file set. If task 1 finds a mixed hoisted object here anyway, apply the same rule:
sweep the declaration normally, give the glyph consumer a **local `fontSize` override**, and allowlist
the override's line — never the declaration.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern).

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**Regions that are out of bounds in this group:**
- `App.tsx:222-226` (`vpWidth`), `:440-460` and the sidebar overlay/toggle plan 12 added — sweep their
  `fontSize` values only; do not touch the layout logic, the `aria-expanded` toggle, or add an
  `aria-label` to that toggle (its visible label IS its accessible name).
- `MemoryPanel.tsx`'s scope-warning line plan 10 added — it was written at the corrected size on
  purpose. Do not normalise it back down.
- `AskMeTab.tsx`'s `useHiveTasks` call plan 05 landed — do not disturb it.

**Preserve every `eslint-disable-next-line` on its exact target line** — `MemoryGraphPanel.tsx`
carries one of the nine `react-hooks/exhaustive-deps` suppressions plan 21 turns live.
`src/renderer/src/ide/monaco.ts` carries a `@typescript-eslint/no-explicit-any` disable that plan 21
**deletes** — it is not in this group and must not be touched here.

**Plan 20 owns the other 23 files and runs in the same wave.** Its file set is disjoint from this
one. Do not edit any file outside this plan's `files_modified`, and do not run a repo-wide fix — a
cross-set edit in wave 7 is a lost update, because plans 15, 16, 17, 18 and 20 are all
editing the renderer concurrently in the same working tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the eight files, before editing anything</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - All eight files in full: `SkillsTab.tsx`, `MessageQueueComposer.tsx`, `WorkersTab.tsx`, `MemoryPanel.tsx`, `MemoryGraphPanel.tsx`, `App.tsx`, `CostHud.tsx`, `AskMeTab.tsx`.
    - `src/renderer/src/App.tsx:215-235` and `:440-470` specifically — plan 12's collapse logic and toggle.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time (85 total, 8 files):
    SkillsTab 16, MessageQueueComposer 16, WorkersTab 10, MemoryPanel 9, MemoryGraphPanel 9, App 9,
    CostHud 8, AskMeTab 8 — **as occurrences**. Count with `grep -hoE … | wc -l`, not `grep -cE`: wave 9
    asserts occurrences and the two units diverge wherever one line carries two matches.
    Plans 10 and 12 edited `MemoryPanel.tsx` and `App.tsx` after that measurement, so the current
    numbers are the truth; record any delta.

    Record `git rev-parse HEAD` as this plan's **base sha** — the path-scoped containment checks in
    task 2 are read against it, and five wave-mates are committing to the same tree.

    Then run the two **blind-spot scans** from `<interfaces>` (M1d and M1x) over this plan's eight paths
    and paste both outputs. Expected today: one M1d hit (`SkillsTab.tsx:254`) and two M1x hits
    (`MemoryGraphPanel.tsx:333`, minimum `11`; `MessageQueueComposer.tsx:355`, minimum `8`). Record every
    M1x hit's **evaluated minimum**, not just its expression text — M1 cannot see these, so neither can
    plan 23's wave-9 bar, and they are exactly the sites that would otherwise ship unexamined.

    Run the M3 glyph predicate scoped to these eight files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm this sub-group's exempt list is exactly `AskMeTab.tsx`'s `✕` and `└`, and confirm none of
    the four NOT-exempt cases has moved into it.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. Enumerate every hoisted object's consumers; state
    explicitly whether any hoisted object in these eight files serves both a text consumer and a glyph
    consumer, and if one does, name it with both consumer lines.

    Inventory every `<button>` and `<PixelButton>` across the eight files, marked NAMED or ICON-ONLY.

    Record `MemoryGraphPanel.tsx`'s `eslint-disable-next-line` line number, and every fixed-height
    container integer in the group.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all 8 files with a total.
    - Task notes contain the pasted M3 output scoped to these files and the confirmed exempt list.
    - Task notes contain the per-file rule classification table and the hoisted-object consumer map, with an explicit yes/no on mixed text/glyph consumers.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY and `MemoryGraphPanel.tsx`'s `eslint-disable-next-line` line number.
    - Task notes contain the pasted M1d and M1x output scoped to this plan's eight paths, with an **evaluated minimum** beside every M1x hit. Expected today: one M1d hit (`SkillsTab.tsx:254`) and two M1x hits (`MemoryGraphPanel.tsx:333` minimum `11`, `MessageQueueComposer.tsx:355` minimum `8`).
    - Task notes record this plan's **base sha** (`git rev-parse HEAD`).
    - `npm test` exit code is `0`, **and the suite's TAP counters are recorded as this plan's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured today: `EXIT=0`, `# tests 426`, `# pass 422`, `# fail 0`, `# skipped 4`, `# todo 0`. Name
      the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 4`, so a criterion
      grepping `^# skipped` against it never matches and silently passes as "no output, no problem".
      **Write the TAP through `mktemp`, never to a fixed repo-root name.** Six plans (15-20) run
      concurrently in wave 7 against ONE working tree (`use_worktrees: false`), so a shared `sweep.tap`
      is truncated by each wave-mate while the others are still grepping it — this plan could grade a
      wave-mate's half-written counter block against its own `# pass` / `# skipped` gate and score a run
      it never made. `.gitignore` has no `*.tap` entry either, so a stray file would also show up in
      this plan's containment check, in every wave-mate's, and in plan 23's wave-9 sweep.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the eight files is pre-classified before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the eight files and freeze this sub-group's allowlist</name>
  <files>src/renderer/src/components/SkillsTab.tsx, src/renderer/src/components/MessageQueueComposer.tsx, src/renderer/src/components/WorkersTab.tsx, src/renderer/src/components/MemoryPanel.tsx, src/renderer/src/components/MemoryGraphPanel.tsx, src/renderer/src/App.tsx, src/renderer/src/realtime/CostHud.tsx, src/renderer/src/components/AskMeTab.tsx</files>
  <read_first>
    - The eight files in full — the files being modified.
    - Task 1's classification table, button inventory and lint-comment list.
    - `src/renderer/src/App.tsx:215-235` and `:440-470` — plan 12's collapse logic and toggle, which are out of bounds beyond their `fontSize` values.
    - `01-UI-SPEC.md` § FLOOR-12's containment rule.
  </read_first>
  <action>
    Execute task 1's classification for these eight files under Rules 0, 1 and 2. Convert hoisted
    objects at their declaration with local overrides for any glyph consumer.

    Apply this sub-group's exempt glyphs (`AskMeTab.tsx` `✕` and `└`): size unchanged,
    `aria-hidden="true"`, allowlist entry recorded in `fontSize`-declaration-line space.

    Then close the **three M1-invisible sites** in `<interfaces>`' table, which none of the eight
    per-file counts includes and which no M1-based check can gate:
    - `SkillsTab.tsx:254` — `fontSize: 10.5` → `var(--cth-text-mono-md)` + `var(--cth-lh-mono)`
      (the object sets the mono family). A decimal is an ordinary literal; M1's guard just drops it.
    - `MemoryGraphPanel.tsx:333` — `fontSize: isTopic ? 12 : 11` → the same ternary shape with both
      branches at 14: `isTopic ? 'var(--cth-text-mono-md)' : 'var(--cth-text-body-md)'`, mirroring the
      family ternary directly above it. Then check `truncate(n.label, isTopic ? 20 : 16)` on the line
      below — wider glyphs in the same node radius is a containment question, answered with UI-SPEC's
      three steps in order.
    - `MessageQueueComposer.tsx:355` — floor `composerFontSize` at 14
      (`Math.max(14, useTerminalFontSize())` at `:64`), so the composer still follows Cmd +/- upward but
      never renders below the floor. `composerLineHeight` (`:65`) and the `minHeight`/`maxHeight` at
      `:348-349` all derive from it; re-check those integers after the change and record the delta.
      Do **not** apply the terminal carve-out here — `<interfaces>` states its predicate once, and a
      textarea is not xterm.

    Add `aria-label` to every ICON-ONLY entry and to none of the NAMED ones. **Do not add an
    `aria-label` to `App.tsx`'s sidebar toggle** — its visible `hide panel` / `show panel` label is its
    accessible name and an `aria-label` would override it.

    Out of bounds: `App.tsx`'s collapse logic and `aria-expanded` toggle behaviour; `MemoryPanel.tsx`'s
    scope-warning line (already at the corrected size); `AskMeTab.tsx`'s `useHiveTasks` call. Sweep
    their sizes, change nothing else.

    Preserve `MemoryGraphPanel.tsx`'s `eslint-disable-next-line` on its exact target line.

    Apply containment in order; record every container integer changed with its measured delta; stop and
    report rather than reflowing.

    Freeze this sub-group's Rule 0 allowlist as a literal `file:line` array covering all eight files and
    write it into the SUMMARY. Plan 23 assembles all seven sweep plans' arrays (01-14 through 01-20) into
    `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    **Do not re-run M1 across the whole renderer and do not record a whole-renderer total.** Plans 15,
    16, 17, 18 and 20 are sweeping concurrently in wave 7, so any repo-wide number taken here is stale
    before it is written, and a stale number in a SUMMARY is the exact false-claim class this phase
    exists to remove. The repo-wide completeness bar is plan 23's, asserted once after every sweep has
    landed. Record **this sub-group's** eight per-file counts and nothing wider.
  </action>
  <acceptance_criteria>
    - For each of the eight files, `grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` returns exactly that file's allowlist size. Paste all eight, each beside its task-1 occurrence baseline. **Occurrences (`-o`), not lines (`-c`)** — the same unit and regex plan 23 uses in wave 9.
    - **The three M1-invisible sites are closed, each asserted on its own** — no per-file M1 count can gate any of them:
      `grep -cE "fontSize: 10\.5" src/renderer/src/components/SkillsTab.tsx` returns `0` (today `1`).
      `grep -cE "fontSize: isTopic \? 12 : 11" src/renderer/src/components/MemoryGraphPanel.tsx` returns `0` (today `1`), and the replacement is pasted showing both branches at a `--cth-text-*` token.
      `grep -cE "const composerFontSize = useTerminalFontSize\(\);" src/renderer/src/components/MessageQueueComposer.tsx` returns `0` (today `1`), and `grep -nE "const composerFontSize = " <the same file>` shows a floor of `14` — paste the line with its evaluated minimum.
      **And the floor was put on the consumer, not on xterm's minimum:** `grep -c "MIN_TERMINAL_FONT_SIZE = 8" src/renderer/src/components/terminalFontSize.ts` returns **`1`**, unchanged from today. That file is UI-SPEC's carve-out; raising its constant to 14 would floor the composer by taking the user's terminal zoom range away, which is the contradiction red-team round 3 flagged between plan 23's carve-out and its ≥ 14 demand. Paste the command. This plan's containment filter already forbids *committing* that file; this criterion forbids the change even reaching the working tree.
    - **Residual blind-spot scan over all eight files:** M1d returns `0` hits after the sweep. **M1x is NOT required to reach `0`** — this plan's own prescribed fixes leave M1x hits by construction: the `MemoryGraphPanel.tsx` ternary becomes `isTopic ? 'var(--cth-text-mono-md)' : ...` (still `fontSize: isTopic`, still M1x) and `composerFontSize` is floored with `Math.max(14, useTerminalFontSize())` (still M1x). Requiring `0` would make this criterion satisfiable only by violating this plan's own actions. The bar is per-hit: every surviving M1x hit in these files evaluates to a minimum of at least 14 — paste each with its evaluated minimum, which is what plan 23 grades against — paste both. Without this, FLOOR-12 signs off with sub-14px text M1 is blind to still shipping.
    - `grep -c "cth-font-display" <file>` is unchanged for all eight files, each compared against its task-1 baseline.
    - `grep -c "aria-expanded" src/renderer/src/App.tsx` is unchanged and the toggle still carries no `aria-label` — paste the toggle's JSX.
    - `grep -c "useHiveTasks" src/renderer/src/components/AskMeTab.tsx` still returns at least `1` (plan 05's call survived).
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/MemoryGraphPanel.tsx` matches task 1's record and the comment is still immediately above its target line — paste both line numbers.
    - **Containment, path-scoped so it survives the concurrent wave.** Six plans sweep in wave 7 against
      ONE working tree (`use_worktrees: false`), so a bare whole-tree `git diff --stat` always shows the
      other five agents' edits and can therefore never pass — **do not use one as a containment check.**
      Stage and commit with an explicit pathspec (`git add -- <this plan's eight paths>` then
      `git commit -- <the same paths>`); never `git add -A`, `git add .` or `git commit -a`, which sweep
      a wave-mate's unstaged work into this plan's commit. Then assert all three:
      (a) **per commit and filtered — this is the one that catches a genuine cross-set edit.** Bind both
      variables **inside** the criterion: a criterion is its own shell invocation, and an unbound `$SHAS`
      iterates zero times, so the filter would emit nothing and score as PASS having checked nothing.
      Substitute the literal base sha task 1 recorded for `<base sha>`, then run:
      `BASE=<base sha>; SHAS=$(git log --format=%H "$BASE"..HEAD -- <this plan's eight paths>); echo "$SHAS"; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/renderer/src/components/(SkillsTab|MessageQueueComposer|WorkersTab|MemoryPanel|MemoryGraphPanel|AskMeTab)\.tsx|src/renderer/src/App\.tsx|src/renderer/src/realtime/CostHud\.tsx)?$"`
      The filter must produce **no output**, and `echo "$SHAS" | grep -c .` must be **at least 1** — paste
      both, plus `for sha in $SHAS; do git show --stat --format= "$sha"; done` so the touched set is on
      the page. The trailing `?` in the pattern absorbs the blank line `--format=` emits. The `grep -c .`
      floor is load-bearing: with no commits made, the loop body never runs and the filter is empty for
      the wrong reason. Replaces an unfiltered `git show --stat --format=` adjudicated by eye — a
      diffstat with no pass/fail filter is not a criterion. An edit into plan 15, 16, 17, 18 or 20's files shows up here
      as an extra path; it is invisible to any whole-tree diff. **Selected by pathspec, so this is this
      plan's own commits:** five wave-mates commit into this same tree in this same wave, and a tree-wide
      filter would fail this plan on their work. If a wave-mate does commit one of this plan's paths, its
      commit enters `$SHAS` and its other paths fail the filter — exactly the cross-contamination this
      criterion exists to catch.
      (b) **path-scoped and positive — the declared set actually moved.**
      `git diff --name-only <base sha>..HEAD -- <this plan's eight paths> | sort -u` lists **all 8** paths
      of `files_modified` — paste the list beside `... | sort -u | grep -c .`, which must print exactly
      `8`. The count is the whole gate, and it is equivalent to set equality here: a pathspec-scoped diff
      can only ever emit paths from that pathspec, so `8` unique lines can only be the 8 declared paths.
      That is why the previous wording — *"lists only paths from `files_modified`"* — had no failing case
      at all, and why empty output, the do-nothing case, satisfied it too. Verified 2026-08-21 that every
      one of these 8 files carries measured sub-14px work (SkillsTab 16 plus its decimal, MessageQueueComposer 16, WorkersTab 10, MemoryPanel 9,
      MemoryGraphPanel 9, App 9, CostHud 8, AskMeTab 8 occurrences), so all 8 must appear. Scoped
      to this plan's own paths, it is unaffected by wave-mates and passes while they work.
      (c) **nothing left behind** — `git status --porcelain -- <this plan's eight paths>` is empty, so this
      plan leaves none of its own edits uncommitted for the next wave-mate's `git add` to absorb. An
      uncommitted edit of this plan's IS the lost update the whole-tree check was reaching for.
    - Task notes contain this sub-group's frozen allowlist as a literal `file:line` array. They contain **no** whole-renderer M1 total.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today, of `426` total). Exit code alone
      is not a gate: `node --test` counts skipped tests in its total and exits `0` on an all-skipped file
      (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from a green one by
      exit code. This plan adds no tests, so any rise in `# skipped` is a red test being hidden rather
      than fixed.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The eight densest remaining files clear the floor with no family conversion, no disturbance to plans 05, 10 or 12's landed work, and no cross-set edit into plan 20's files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered app shell and panels → the operator's continuous view of the floor | These are the surfaces the operator watches while agents run unattended. Illegible status, cost or blocked-state text is a direct failure of criterion 4's "legible to the operator watching it". |
| this plan's file set → plan 20's file set, same wave, one working tree | Two agents sweep the renderer concurrently. A cross-set edit is a lost update with no test that catches it until wave 9. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P19-01 | Information disclosure | the eight densest renderer files' text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size, pasted beside its task-1 baseline. |
| T-P19-02 | Information disclosure | an icon-only control with no accessible name | mitigate | Per-element `aria-label` pass from a pre-built inventory, never a ratio. |
| T-P19-03 | Repudiation | allowlisting a hoisted style declaration and leaving its text consumer below the floor | mitigate | Task 1 states explicitly whether any hoisted object here has a mixed consumer set; if one does, the declaration is swept and the glyph gets a local override. |
| T-P19-04 | Denial of service | a bumped label clipping a cost readout or a memory panel row | mitigate | Containment applied in UI-SPEC's order with measured deltas; "stop and report" rather than reflow. |
| T-P19-05 | Repudiation | a moved `eslint-disable-next-line` detaching `MemoryGraphPanel`'s suppression before plan 21's lint gate | mitigate | The anchor is recorded in task 1 and re-asserted by line number after the edit. |
| T-P19-06 | Tampering | editing a file that belongs to plan 15, 16, 17, 18 or 20 in the same wave | mitigate | Per-commit `git show --name-only --format=` piped through an allowlist filter that must be empty, with `$SHAS` bound inside the criterion and a `grep -c .` floor so an executor that committed nothing cannot pass it, plus path-scoped `git diff`/`git status`; a bare whole-tree `git diff --stat` is explicitly forbidden here because in wave 7 it always shows five wave-mates' edits and so can never pass. |
| T-P19-07 | Repudiation | recording a whole-renderer M1 total that five concurrent plans invalidate | mitigate | Explicitly forbidden here; the repo-wide bar is plan 23's, run once after every sweep has landed. |
| T-P19-08 | Information disclosure | the message composer rendering at 8px because it follows the terminal zoom, with the terminal carve-out stretched to cover it | mitigate | The carve-out is stated once with its predicate (xterm's own font only) and `composerFontSize` is floored at 14, asserted by its own grep rather than by any M1 count. |
| T-P19-90 | Information disclosure | sub-14px user-facing text M1 structurally cannot see — decimals, quoted px, ternaries and computed sizes — shipping unexamined past plan 23's bar | mitigate | The M1d and M1x scans run over this plan's paths in task 1 and again after the sweep; every M1x hit carries an evaluated minimum and no minimum may be below 14. |
| T-P19-91 | Repudiation | a containment check that can never pass, so it is quietly dropped | mitigate | The whole-tree `git diff --stat` is replaced by a per-commit `git show --name-only --format=` allowlist filter, a path-scoped `git diff --name-only` that must list **all** the plan's declared paths, and a path-scoped `git status --porcelain`. All three pass while five wave-mates edit the same tree, and each has a real failing case: an out-of-set path, a declared file left untouched, and an uncommitted edit respectively. |
| T-P19-92 | Repudiation | a red test hidden as a skip to make the sweep green | mitigate | TAP counters are baselined in task 1 and re-compared after the sweep; `# skipped` may not rise. `node --test` counts skips in its total and exits `0`, so exit code alone is not the gate. |
| T-P19-93 | Repudiation | wave 7 pinning line counts while wave 9 asserts occurrence counts | mitigate | Every per-file number in this plan is an occurrence count taken with `grep -hoE … | wc -l`, the same unit and regex plan 23 uses, and the unit is stated beside each pinned number. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the app shell, the skills tab, the workers tab, the memory panel, the memory graph, the
  message-queue composer, the ask-me tab and the cost HUD in the dev app. Confirm nothing is clipped and
  the sidebar collapse plan 12 added still works at both sides of 1024px.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all eight files in this sub-group.
- Every `<button>` in the eight files has an accessible name, by the rule rather than by a ratio.
- The sub-group's frozen Rule 0 allowlist is in the SUMMARY as a literal `file:line` array, ready for
  plan 23 to assemble.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-19-SUMMARY.md` when done. It MUST contain this
sub-group's frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts **as
occurrences with the unit named** beside their task-1 baselines, the M1d/M1x scans before and after with
all three M1-invisible sites' resolutions and every M1x hit's evaluated minimum, this plan's base sha
with every commit sha and its `git show --stat`, the TAP counters before and after, every container
integer changed with its delta, and confirmation that `MemoryGraphPanel.tsx`'s
`eslint-disable-next-line` is still on its target line. It MUST NOT contain a whole-renderer M1 total.
</output>
