---
phase: 01-finish-the-floor
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]  # 01-02 added: task 4 calls the per-agent token accessor plan 02 mints
files_modified:
  - src/main/hive.ts
  - src/main/hooks.ts
  - src/main/telemetry.ts
  - test/hive-protocol-v2.test.cjs
  - test/engine-parity.test.cjs
  - test/hook-auth-roundtrip.test.cjs    # task 4 ONLY — widen the derived shim guard from the three templates that carry `sock_token` today to all six, once this task adds the missing three. Plan 02 (wave 2, an EARLIER wave) rewrote that guard and left it deliberately narrow. No wave-3 plan declares this file — checked 2026-08-21.
# NOT modified here, and deliberately so: src/main/index.ts. FLOOR-09 needs exactly ONE line there
# (the cost sink argument at the `new HookServer(...)` call, :465-469) because `telemetry` is a
# module-local const at `:356` and the HookServer constructor at `hooks.ts:117-132` takes no
# telemetry dependency — see <handoff> in task 2. index.ts is T-INDEX; plan 07 holds it in wave 3 and
# plan 08 holds it in wave 4, and a same-wave second writer under `use_worktrees: false` is a lost
# update. That line is therefore HANDED TO **PLAN 08 TASK 6** BY NAME, in **wave 4**, and hard-gated
# by **plan 10 task 5** in **wave 5**. (Earlier revisions of this file said "plan 07" and "plan 09
# task 1" in seven places; both were wrong. Plan 09 states TWICE that it records the FLOOR-09
# injection state and does NOT gate on it — `01-09-PLAN.md:205`, `:221` — and the hard STOP-gate is
# `01-10-PLAN.md` task 5, `:481-489`. Plan 08's own frontmatter now carries `requirements:
# [FLOOR-02, FLOOR-09]` marked as the CLOSING half of the split, and its tasks are deliberately
# numbered 1,2,3,4,6 — task 6 is the FLOOR-09 one. Plan 08 STOPs with
# `FLOOR-09 HANDOFF MISSING FROM 01-06` if this plan's SUMMARY heading is anything other than
# `T-INDEX HANDOFF → 01-08 (FLOOR-09)`, so the prose and the heading must agree.)
autonomous: true
requirements: [RECORD-03, RECORD-04, FLOOR-09, GATE-01]  # GATE-01 partial: task 4 only, the qwen sidecar handoff from plan 02 (env AND shim body — see task 4). FLOOR-09 partial: this plan mints the `recordCost?` sink and proves it at runtime, but FLOOR-09 does not BECOME TRUE here — `01-08-PLAN.md` task 6 applies the production injection in wave 4 and declares FLOOR-09 as the closing half of the split. Nothing in this plan may say FLOOR-09 closed in wave 3.

must_haves:
  truths:
    - "The qwen proxy sidecar authenticates with its own agent's token, closing the wave-2→wave-3 window plan 02 opened when it deleted the floor-wide assignment (task 4)"
    - "Every cost-ledger row is a cumulative per-agent snapshot — the two appenders no longer disagree"
    - "A card's spend is derived from clamped consecutive differences, not from summing snapshots"
    - "Spend is summed over ALL of a card's rows, not through a 1 MB tail"
    - "A card in flight across an app restart does not silently reset its spend to zero — proven across a REAL process boundary (`spawnSync(process.execPath, …)`), never by constructing a second manager in the same process, which a module-level Map satisfies"
    - "A CostSample posted at the hook socket reaches getAgentUsage through telemetry.recordCostSample — proven by a runtime test that reads the number back out of getAgentUsage, not by a string count"
    - "The one line of production wiring FLOOR-09 needs in index.ts is handed to **plan 08 task 6** by name in **wave 4**, recorded verbatim in the SUMMARY under the exact heading plan 08 greps for, and hard-gated by **plan 10 task 5** in **wave 5** — never left implicit, and never attributed to plan 07 or plan 09, neither of which carries it"
    - "FLOOR-09 is a SPLIT requirement and this plan is the OPENING half: the sink exists and is proven at runtime here, but proxy spend does not reach production `getAgentUsage` until plan 08 task 6 lands the injection in wave 4"
    - "The qwen sidecar fix is the env argument AND the `sock_token` field in `PROXY_BRIDGE_SHIM`'s `emit()` — the env change alone is a no-op, because that shim sends no `sock_token` today (measured: 0)"
  artifacts:
    - path: "src/main/hive.ts"
      provides: "cumulative-diff taskSpend, in-memory per-card accumulator, startup ledger rescan"
      contains: "taskSpend"
    - path: "src/main/hooks.ts"
      provides: "CostSample branch routed through an injected cost sink instead of writing the ledger by hand"
      contains: "recordCost"
    - path: "test/hive-protocol-v2.test.cjs"
      provides: "the rewritten per-card cost test asserting diffs, a >1 MB ledger test, and a restart test that spawns a real second process with a negative control"
    - path: "test/engine-parity.test.cjs"
      provides: "the FLOOR-09 runtime test (a CostSample handled by a real HookServer shows up in a real TelemetryCollector's getAgentUsage and trips the breaker) plus the GATE-01 sidecar tests: the env carries a per-agent token AND PROXY_BRIDGE_SHIM's emit() actually writes sock_token into the bytes"
      contains: "sock_token"
  key_links:
    - from: "src/main/hooks.ts CostSample branch"
      to: "telemetry.recordCostSample()"
      via: "an injected `recordCost` sink on the HookServer constructor; the production injection is the one index.ts line handed to plan 08 task 6 in wave 4"
      pattern: "recordCost"
    - from: "src/main/hive.ts taskSpend()"
      to: "the full cost ledger"
      via: "per-card accumulator rebuilt by one startup scan, no COST_TAIL_BYTES"
      pattern: "taskSpend"
---

<objective>
Make the number a budget could be enforced against correct, before anything enforces it.

Purpose: RECORD-03 and RECORD-04 are prerequisites of FLOOR-10, and `taskSpend()` currently computes
the wrong number twice over. The ledger holds **mixed row semantics** — `index.ts:1513` appends
cumulative per-beat snapshots while `hooks.ts:272` appends per-response deltas — and `taskSpend()`
sums both, over-counting roughly quadratically. It then reads through a 1 MB tail, so a long card's
early spend falls out of the window and `over` reads false when it is true. `db.ts:44` states the
intended contract verbatim. FLOOR-09 closes almost for free in the same edit, because
`telemetry.recordCostSample()` exists, is documented as exactly this wiring, and has zero production
callers.

Output: one row semantics, spend from clamped diffs over all rows, proxy-tier spend reaching the
breaker, and the test that encoded the defect rewritten — source first, in its own commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@docs/adr/0004-single-committer-git.md

<interfaces>
**Line numbers below are informational pointers, never assertion keys.** Plan 03 (wave 2) deletes the
`!previous.has(task.id)` clause at `src/main/hive.ts:1772` and rewrites the surrounding mint, which
shifts every `hive.ts` anchor below it — including every anchor in this section. Before any read or
edit at a numbered `hive.ts` anchor, **re-derive it by content match** and use the number the grep
prints, not the number written here:

```
grep -n "COST_TAIL_BYTES" src/main/hive.ts
grep -n "appendCostLedger(sample: AgentUsageSample)" src/main/hive.ts
grep -n "taskSpend(taskId: string)" src/main/hive.ts
```

Never write a `sed -n 'N,Mp'` with a number copied from this document, and never make a line number
the subject of an acceptance criterion. If a grep returns nothing, the symbol moved or was renamed —
stop and re-locate it rather than editing at the stale offset.

Anchors, all read this session (`appendCostLedger` is **CORRECTED** from RESEARCH's `:2520`):

- `src/main/hive.ts:244` — `const COST_TAIL_BYTES = 1024 * 1024;` with a comment that already names
  the defect and tags it `ponytail:`.
- `src/main/hive.ts:2513` — `appendCostLedger`.
- `src/main/hive.ts:2566-2582` — `taskSpend()`; the tail read is at `:2571` via
  `this.tailLines(join(root,'cost-ledger.jsonl'), COST_TAIL_BYTES)`; it accumulates
  `tokens += (row.input ?? 0) + (row.output ?? 0)`.
- `src/main/index.ts:1505-1513` — the beat: `getAgentUsage(id)` (a CUMULATIVE `AgentUsageSample`)
  then `hive.appendCostLedger(sample)`. **This line does not change** — the accumulator and the
  rescan belong in `hive.ts` next to `taskSpend`, which keeps `index.ts` out of this plan entirely.
- `src/main/hooks.ts:261-289` — the `CostSample` branch. Its `return {}` at `:288` is deliberate and
  documented (it keeps proxy cost OUT of the Claude-only OTel/breaker/drain paths). FLOOR-09 exists
  to change that, so it needs a deliberate rewrite, not a deletion. The `?? 0` coercions are pinned
  by `test/engine-parity.test.cjs:85-94` — keep them.
- `src/main/telemetry.ts:11` — the cumulative contract; `:251` — `recordCostSample()`, zero
  production callers (only two doc comments in `src/shared/agentProvider.ts:119,:321` and six calls
  in `test/engine-parity.test.cjs`).
- **D-21's wiring gap, measured this session — read this before writing a line of task 2.**
  `HookServer` cannot reach `telemetry` today and no edit inside this plan's five files can give it
  one:
  - `src/main/hooks.ts:117-132` — the constructor takes `hive, getWebContents, getConfig, control?,
    breaker?, drainAtStop?, focus?`. **No telemetry dependency, and no import of `telemetry.ts`.**
  - `src/main/index.ts:356` — `const telemetry = new TelemetryCollector({…})` is a **module-local
    `const`, not exported**, so `hooks.ts` cannot import it.
  - `src/main/index.ts:465-469` — the sole `new HookServer(…)` call. It is the only place a new
    dependency can be injected, and `index.ts` is the composition root, so this is not a smell to be
    designed around — it is where wiring belongs.
  - `HiveManager` is no back door: its constructor (`hive.ts:431-434`) takes `getHome` and `emit`
    only, and its setter seams (`setOtelEndpoint`, `setRoutedObserver`) are themselves called from
    `index.ts`. Every route to the collector passes through the composition root.

  Therefore FLOOR-09 is **one added optional constructor parameter in `hooks.ts` (this plan) plus
  exactly one argument at `index.ts:465-469` (NOT this plan)**. Task 2's `<handoff>` writes that
  argument out verbatim for **plan 08 task 6, in wave 4** — NOT for plan 07, this wave's T-INDEX
  holder, because the argument is passed to a constructor parameter *this plan adds in this same
  wave* and a wave-3 sibling applying it first would fail `npm run typecheck`. Do not attempt to close it inside
  `telemetry.ts` with a module-level self-registering singleton: two collectors in one test file
  would silently fight over the slot, which converts a wiring bug into a flaky-test bug.
- `src/main/db.ts:44` — *"Rows are CUMULATIVE snapshots (one per agent per heartbeat beat) — diff
  consecutive rows for velocity."*
- A `cost_ledger` table already exists in `PersistStore` with a matching row shape. D-20 chose the
  in-memory Map; the SQL alternative (`SUM(...) GROUP BY task_id`) exists if the Map proves
  insufficient. Do not switch without saying why.

The test to rewrite is the WHOLE test at `test/hive-protocol-v2.test.cjs:256-284` —
`test('cost rows carry the card they were spent on, and a card knows its cap', …)` under the banner
`// ─── #34 — per-card cost attribution ───` at `:256`. Rewriting `:276-284` in isolation will not
compile; the body is one arrange/act/assert block. Its current expectation is
`assert.equal(spend.tokens, 20)` — the summing contract this plan removes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every RECORD-03 / RECORD-04 / FLOOR-09 clause against current source and paste the output</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/hive.ts` at `COST_TAIL_BYTES` (and its comment), `appendCostLedger`, and `taskSpend`. **Re-derive each offset by content first** — plan 03 shifted them in wave 2 — then read a window around the number the grep prints:
      `grep -n "COST_TAIL_BYTES\|appendCostLedger(sample: AgentUsageSample)\|taskSpend(taskId: string)" src/main/hive.ts`
      Paste that output; every `hive.ts` read in this plan uses those numbers, not the ones written in `<interfaces>`.
    - `src/main/hooks.ts` — the `CostSample` branch in full including the early return and its comment. Locate it with `grep -n "CostSample" src/main/hooks.ts`, and read the constructor with `grep -n "constructor(" -A 20 src/main/hooks.ts` (D-21's wiring gap).
    - `src/main/telemetry.ts:1-30` and `:240-270` — the cumulative contract and `recordCostSample`.
    - `src/main/index.ts:1500-1520` — the beat appender (read only; this plan does not modify `index.ts`).
    - `src/main/db.ts:33-50` — the contract stated verbatim.
    - `test/hive-protocol-v2.test.cjs:250-290` and `test/engine-parity.test.cjs:40-100` — the two test surfaces.
    - `01-CONTEXT.md` D-19, D-20, D-21, D-23; `01-RESEARCH.md` § "Spend Arithmetic".
  </read_first>
  <action>
    Quote REQUIREMENTS.md RECORD-03, RECORD-04 and issue #19's acceptance text verbatim, and run one
    command per clause with ACTUAL output pasted:
    - `grep -n "COST_TAIL_BYTES" src/main/hive.ts` — expect the definition and the read at `:2571`.
    - `grep -n "appendCostLedger" src/main/*.ts` — expect exactly two production appenders plus the definition; paste them. This is the mixed-semantics evidence.
    - `grep -rn "recordCostSample" src/ test/` — expect zero production callers outside `telemetry.ts` (two doc comments in `agentProvider.ts` and six calls in `test/engine-parity.test.cjs`). The absence of production callers IS the FLOOR-09 evidence.
    - `grep -n "tokens +=" src/main/hive.ts` — the summing line.

    Then paste the **D-21 wiring evidence**, because it is what decides where FLOOR-09's last line
    lives and it is the finding that a `grep -c` criterion previously hid:
    - `grep -n "constructor(" -A 20 src/main/hooks.ts | head -30` — the `HookServer` constructor takes no telemetry dependency (measured baseline: seven parameters, `hive` through `focus?`).
    - `grep -n "^const telemetry = new TelemetryCollector" src/main/index.ts` and `grep -c "export const telemetry" src/main/index.ts` — expect one hit and `0`: the collector is module-local and unexported, so `hooks.ts` cannot import it.
    - `grep -n "new HookServer(" src/main/index.ts` — expect exactly ONE hit. That single call site is the only injection point in the process, and it is `index.ts`, which this plan does not own.
    Record these three as **B-hookctor**, **B-telemetry-export** (`0`) and **B-hookserver-sites** (`1`).

    Then write down, before touching code, the exact arithmetic contract you are about to implement,
    as prose with a worked two-row example: given cumulative snapshots S1 then S2 for one agent, the
    card's spend contribution is `max(0, S2 − S1)` — clamped, because `telemetry.forget()` on respawn
    resets the accumulator and an unclamped diff would go negative. State how the first row for an
    agent is treated (its own value, or zero) and justify the choice against `db.ts:44`. The next task
    implements exactly this; a fuzzy contract here is how the test gets bent later.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all four greps.
    - The `recordCostSample` grep output is pasted and shows no production caller.
    - Task notes contain pasted output for the three D-21 wiring greps, and record B-hookctor, B-telemetry-export (`0`) and B-hookserver-sites (`1`). If `export const telemetry` is non-zero or `new HookServer(` has more than one hit, the wiring gap has moved — re-derive task 2's `<handoff>` patch before writing it.
    - Task notes contain the re-derived `hive.ts` offsets for `COST_TAIL_BYTES`, `appendCostLedger` and `taskSpend`, pasted from the grep. Any later `sed`/read in this plan cites those numbers.
    - Task notes contain the written arithmetic contract with a worked two-row example and an explicit rule for the first row per agent.
    - **The baseline is recorded under the TAP reporter, not the exit code** — `node --test` counts skipped tests in its total and exits `0`, so an exit code cannot tell a passing file from an all-skipped one (verified at plan time: two `{skip}` tests give `EXIT=0`, `# pass 0`, `# skipped 2`). Run and paste both:
      `TAP=$(mktemp); node --test --test-reporter=tap test/hive-protocol-v2.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# skipped 0`, `# todo 0` on both. Label the two `# pass` numbers **B-pass-hp** and **B-pass-ep**; tasks 3 and 4 assert deltas against the numbers you measure HERE, never against `# tests` and **never against the plan-time numbers below**.
      **The plan-time numbers are stale by construction and must not be reused.** They were measured before wave 2: `test/hive-protocol-v2.test.cjs` `# pass 12` and `test/engine-parity.test.cjs` `# pass 14`. `01-03-PLAN.md` (wave 2) adds **five** tests to `hive-protocol-v2` and requires `# pass` at least `17` — so by the time this plan runs, B-pass-hp is `17` or higher and a floor computed as `12 + 2 = 14` is **already satisfied by adding zero tests**. That is not a hypothetical: it is the defect this criterion was rewritten to remove. Re-measure and cite what you measured.
      **Two STOP conditions on the measured values**, because a baseline lower than the guaranteed one means a dependency did not land and every delta below it is meaningless:
      - If **B-pass-hp < 17**, STOP and report `01-03 DID NOT LAND — B-pass-hp=<n>, EXPECTED >= 17`. Do not proceed by lowering the floor.
      - If **B-pass-ep != 14**, say so explicitly and re-derive task 3's and task 4's absolute floors from the measured number. No wave-2 plan declares `test/engine-parity.test.cjs` (checked 2026-08-21 across plans 01-05), so `14` is expected — a different number means an undeclared writer touched it, which is worth a line in the SUMMARY either way.
      Record a third baseline while the file is open — **B-gau** = `grep -c "getAgentUsage" test/engine-parity.test.cjs`. **Measured 2026-08-21: `6`** (`:8` a doc comment, `:60`, `:80`, `:91`, `:99`, `:232`). An earlier revision of task 3 asserted "at least `1`, baseline today: `0`" against this file — an **inverted baseline**, satisfied by current source with no work done at all. Task 3's criterion is now a strict delta against B-gau. Name the TAP reporter explicitly: the default (spec) reporter prints `ℹ skipped 0`, so a criterion grepping `^# ` against it matches nothing and passes vacuously.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-protocol-v2.test.cjs</automated>
  </verify>
  <done>Every clause has pasted evidence, and the target arithmetic is written down with a worked example before any code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Unify row semantics, derive spend from clamped diffs over all rows, and wire proxy-tier cost into the breaker — source only, in ordered commits</name>
  <files>src/main/hive.ts, src/main/hooks.ts, src/main/telemetry.ts</files>
  <read_first>
    - `src/main/hive.ts` at the anchors above — the file being modified; read current bodies.
    - `src/main/hooks.ts:255-295` — the branch being rewritten, and the `authorized()` gate above it that must stay ahead of any cost path.
    - `src/main/telemetry.ts:240-270` — `recordCostSample`'s own arithmetic, including its handling of negative and `NaN` inputs (pinned by `test/engine-parity.test.cjs:85-94`).
    - `01-PATTERNS.md` § "FLOOR-09 / FLOOR-10 / RECORD-03 / RECORD-04 — the spend cluster" `:765-918`.
    - `.planning/codebase/CONVENTIONS.md` — comments explain WHY at length, citing the issue number.
  </read_first>
  <action>
    Land this as **three ordered commits, source only. No test file is touched in this task** — D-23 is
    explicit that the source fix lands first, in its own `fix(...)` commit, precisely because the test
    that must change encodes the defect and changing both together is indistinguishable from bending a
    test to match buggy output.

    Commit 1 — `fix(cost): unify ledger row semantics`. Make **every** row a cumulative per-agent
    snapshot. The `hooks.ts` `CostSample` branch currently appends per-response deltas: route it
    through `telemetry.recordCostSample()` (which already accumulates cumulatively) and append the
    resulting cumulative sample, rather than building a delta row by hand. Keep every `?? 0` coercion
    and the `NaN`/negative handling — `test/engine-parity.test.cjs:85-94` pins them. Keep the
    `authorized()` gate ahead of the cost path; FLOOR-09 must not open a second unauthenticated route
    into the ledger.

    Commit 2 — `fix(cost): sum over all rows`. Remove `COST_TAIL_BYTES` from the `taskSpend` path
    (leave the constant if another caller still needs it; delete it if not, and say which). Replace the
    summing loop with the clamped-consecutive-diff arithmetic written down in task 1. Per D-20, hold an
    in-memory `Map<taskId, tokens>` accumulated in the beat, rebuilt by **one full ledger scan at
    startup** so a card in flight across an app restart does not silently reset to zero. **Bound the
    Map by card lifetime** — RECORD-03 deliberately removes a memory bound (the 1 MB tail) and the Map
    is what replaces it; an entry for an archived or deleted card must be dropped, not kept forever.
    Carry a comment explaining the whole thing: why rows are cumulative, why the diff is clamped
    (`telemetry.forget()` on respawn), and why the tail had to go, citing #34 and RECORD-03/04.

    Commit 3 — `feat(cost): wire recordCostSample`. Rewrite the `hooks.ts` `CostSample` early
    `return {}` deliberately — do not simply delete it. The return exists to keep proxy cost
    out of the Claude-only OTel/breaker/drain paths; FLOOR-09 exists to change exactly that, so the
    branch must now feed `getAgentUsage` while still not feeding the loop detector (the branch's own
    comment says "pure telemetry — never feeds the loop detector"). State in the comment which paths it
    now reaches and which it deliberately still does not.

    Per D-21's wiring gap in `<interfaces>`, `HookServer` has no route to the collector, so give it
    one **the way this codebase already injects every other main-process dependency — as a
    constructor parameter**. Add an eighth parameter, last and optional so no existing call site or
    test breaks:

    ```ts
    /** FLOOR-09 (#19) — the proxy tier's cost sink. `telemetry.recordCostSample` in production
     *  (injected at index.ts's `new HookServer(...)`), omitted in tests that don't care. Optional
     *  so the server still runs without it, exactly like `breaker` and `control` above. */
    private recordCost?: (s: AgentUsageSample) => void
    ```

    **There are two different `AgentUsageSample` interfaces in `src/main/` and picking the wrong one
    fails `npm run typecheck`.** `src/main/usage.ts:42` declares `sessionId: string | null` and
    `model: string | null` (that is the one `hive.ts:29` imports, for `appendCostLedger`);
    `src/main/telemetry.ts:44` declares both non-nullable plus an optional `accountUuid`, and that is
    the one `recordCostSample` takes. Import the **telemetry** one in `hooks.ts`
    (`import type { AgentUsageSample } from './telemetry';`). The branch already satisfies the
    stricter shape — it guards on `p.session_id` being truthy and coerces `model: p.model ?? ''` — so
    nothing about the payload handling has to change; only the import target.

    The `CostSample` branch then builds the sample **once** and hands it to `this.recordCost?.(sample)`
    instead of calling `this.hive.appendCostLedger(...)` by hand. That is what makes the row cumulative
    (commit 1's contract): the collector accumulates it and the ~30s beat writes the ledger row for
    this agent like everyone else's. Do not keep a "fall back to `appendCostLedger` when the sink is
    absent" branch — that is precisely the mixed-semantics defect commit 1 removes, reintroduced as a
    default. An unwired sink must mean "no proxy row", loudly and visibly, not "the old wrong row".

    **Do not touch `src/main/index.ts`.** The beat's one-line `hive.appendCostLedger(sample)` call is
    unchanged by design — that is the seam that keeps this plan out of T-INDEX, which plan 07 holds
    this wave. Do not touch `src/main/breaker.ts`; plan 09 adds the budget arm next wave.

    <handoff to="01-08" wave="4" token="T-INDEX">
    **FLOOR-09's last line, handed to plan 08 by name — in wave 4, not this wave.** `src/main/index.ts`
    is T-INDEX; plan 07 holds it in wave 3 and plan 08 holds it in wave 4. `use_worktrees` is `false`,
    so two plans editing one file in one wave is a lost update, and this plan must not write that file.
    **It goes to wave 4, not to plan 07, for a second and harder reason:** the injected argument is a
    parameter *this plan adds to `HookServer`'s constructor in wave 3*. A wave-3 sibling applying it
    before this plan's `hooks.ts` change lands would pass an argument to a constructor that does not
    yet accept it — `npm run typecheck` fails, and whichever of the two lands first breaks the tree.
    Wave 4 is the earliest wave where the parameter is guaranteed to exist. PATTERNS.md `:538-539` already
    prescribes this exact mechanism for GATE-01 and FLOOR-07 — "hand them to whichever agent holds
    T-INDEX that wave rather than splitting the requirement".

    The change is one argument at the sole `new HookServer(` call (`index.ts:465-469`; re-derive with
    `grep -n "new HookServer(" src/main/index.ts`):

    ```
      const hookServer = new HookServer(
        hive, () => liveWebContents(), () => readConfig(), control, breaker,
        (agentId) => delivery.drainAtStop(agentId),
        (agentId) => focusAgent(agentId),
    +   (s) => telemetry.recordCostSample(s)          // FLOOR-09 (#19) — proxy-tier cost sink
      );
    ```

    Two lines change, not one: the new argument, and a trailing comma appended to the existing
    `(agentId) => focusAgent(agentId)` line above it. `telemetry` is already in scope there (declared
    `:356`, above the call). Nothing else in `index.ts` changes, and the parameter is optional, so the
    tree typechecks whether or not this lands — which is exactly why it must be recorded loudly rather
    than assumed.

    Reproduce this diff **verbatim** in `01-06-SUMMARY.md` under a heading that reads exactly
    `T-INDEX HANDOFF → 01-08 (FLOOR-09)`, so plan 08's agent can apply it without re-deriving it.
    Then check whether it has already landed and record the result verbatim in the SUMMARY as one of:
    - `FLOOR-09 PRODUCTION INJECTION LANDED` — if `grep -c "recordCostSample" src/main/index.ts`
      returns at least `1`; or
    - `FLOOR-09 PROXY SPEND NOT YET IN getAgentUsage — T-INDEX INJECTION HANDED TO 01-08` — if it
      returns `0`. This mirrors plan 02's `QWEN SIDECAR DEAD-HOOKED UNTIL 01-06` marker: an open
      window that a named later plan closes. **Plan 10 task 5** (wave 5, the first wave after plan 08
      lands it) turns this into a hard STOP-gate — `01-10-PLAN.md:481-489`, which STOPs with
      `FLOOR-09 INJECTION MISSING — 01-08 DID NOT APPLY 01-06'S HANDOFF` — and plan 23 pins it
      permanently in wave 9, so an unlanded handoff blocks the phase rather than silently shipping an
      unfed sink. It is **not** plan 09 task 1 and **not** plan 10 task 1: plan 09 states twice
      (`:205`, `:221`) that it records this state and deliberately does NOT gate on it, because plan 08
      lands the injection in plan 09's own wave.
    </handoff>
  </action>
  <acceptance_criteria>
    - `grep -n "COST_TAIL_BYTES" src/main/hive.ts` output shows no remaining use inside `taskSpend`.
    - **The `CostSample` branch actually routes through the sink, checked on the branch's own body rather than on the whole file.** A bare `grep -c "recordCostSample" src/main/hooks.ts` is void as a criterion: a doc comment naming the function satisfies it, and this plan's own commit-3 comment is required to name it. Slice the branch and check it, pasting both:
      `sed -n "/=== 'CostSample'/,/^    }$/p" src/main/hooks.ts | grep -cE "this\.recordCost\?\.\("` returns at least `1`, and
      `sed -n "/=== 'CostSample'/,/^    }$/p" src/main/hooks.ts | grep -c "appendCostLedger"` returns `0` — the hand-built delta row is gone, not merely supplemented.
    - The sink is a real constructor dependency: `grep -cE "recordCost\?: \(s: AgentUsageSample\) => void" src/main/hooks.ts` returns `1`, and it is the LAST parameter (paste `grep -n "constructor(" -A 24 src/main/hooks.ts`) so no existing call site changes position. Baseline today is `0`, so this criterion fails against current source — that is the point.
    - `grep -c "appendCostLedger" src/main/hooks.ts` returns `0` — the whole file, not just the branch. The proxy tier no longer has a second, direct route into the ledger.
    - Three commits exist with the messages `fix(cost): unify ledger row semantics`, `fix(cost): sum over all rows`, `feat(cost): wire recordCostSample`, and **none of their diffs touches a file under `test/` or `src/main/index.ts`** — verify per commit, path-scoped, and paste the output:
      `for s in <sha1> <sha2> <sha3>; do echo "== $s"; git show --stat "$s" -- test/ src/main/index.ts src/preload/; done`
      Every block must show no files. Scope it to paths, never a bare whole-tree `git diff` — plan 07 is editing this same tree in this same wave under `use_worktrees: false`, so a tree-wide diff assertion cannot pass and would be failing on a wave-mate's work, not this plan's.
    - `npm run typecheck` exit code is `0`.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"` gives `EXIT=0`, `# pass` equal to **B-pass-ep as measured in task 1** (expected `14`, but cite the measured number), `# fail 0`, `# skipped 0`, `# todo 0` — the six existing `recordCostSample` calls and the garbage-sample test still pass unchanged, and none of them was skipped to get there.
    - `node --test test/hive-protocol-v2.test.cjs` is expected to FAIL at this point on the `spend.tokens` assertion. Paste that failure — it is the evidence that the source contract actually changed, and task 3 is what resolves it.
    - `01-06-SUMMARY.md` carries the `T-INDEX HANDOFF → 01-08 (FLOOR-09)` heading with the diff reproduced verbatim, and exactly one of the two literal status lines from the `<handoff>` block. Paste `grep -c "T-INDEX HANDOFF → 01-08 (FLOOR-09)" .planning/phases/01-finish-the-floor/01-06-SUMMARY.md` returning `1`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>One row semantics, clamped-diff spend over all rows with a lifetime-bounded accumulator and a startup rescan, proxy-tier cost reaching `getAgentUsage` — in three source-only commits, with the old test's failure pasted as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the test that encoded the defect, and add the ledger-larger-than-1 MB and proxy-tier-breaker tests</name>
  <files>test/hive-protocol-v2.test.cjs, test/engine-parity.test.cjs</files>
  <read_first>
    - `test/hive-protocol-v2.test.cjs` — the WHOLE test being rewritten, plus the `floor(t)` harness. **Plan 03 (wave 2) also edits this file**, so re-derive both offsets by content before reading: `grep -n "per-card cost attribution\|assert.equal(spend.tokens, 20)\|const floor = " test/hive-protocol-v2.test.cjs`.
    - `test/engine-parity.test.cjs` — `makeBreaker`, the six `recordCostSample` calls, and the two-beat pattern. Re-derive with `grep -n "makeBreaker\|recordCostSample\|\.tick(" test/engine-parity.test.cjs`.
    - `01-PATTERNS.md` § "The test to rewrite (D-23)" `:892-918` and § "⚠ The breaker test is a different animal" `:814-865`.
    - `01-CONTEXT.md` D-23 — the commit-message requirement.
  </read_first>
  <action>
    Rewrite the whole test at `test/hive-protocol-v2.test.cjs:256-284` — not the line range — to assert
    the corrected arithmetic. Its two `appendCostLedger` calls are now cumulative snapshots, so the
    card's spend is the clamped diff, not `10+5+4+1 = 20`. **The commit message must quote the old
    expectation `assert.equal(spend.tokens, 20)` verbatim and state that it encoded the defect
    RECORD-04 removes.** This is a deliberate, documented contract change, and the commit message is
    what distinguishes it from bending a test so a buggy source path passes.

    Add a RECORD-03 test: write a cost ledger larger than 1 MB for one card, with real spend in the
    early rows that the old tail would have dropped, and assert `taskSpend()` still counts it and
    `over` reads true when it is true. Use the real-tempdir `floor(t)` harness so the ledger is a real
    file — never mock the thing under test.

    Add a restart test — and it must **cross a real process boundary**. An earlier revision said
    *"simulate a restart by constructing a fresh manager over the same home"*, which a module-level
    `Map` satisfies: the accumulator this plan introduces lives in memory, a second `HiveManager` in
    the same process can read it, and the test reports green while the durability claim is false.
    `01-08-PLAN.md:262-265` states the standard for the identical claim on the delivery queue —
    *"Rebuilding a `DeliveryService` inside the same process proves nothing: a module-level `Map`
    passes it, and a durability claim backed by a `Map` is worse than no test because it reports
    green"* — and backs it with a `process.execPath` grep. Apply that standard here, in three steps
    against the real-tempdir `floor(t)` harness:
    1. **Parent process:** build the manager over a temp home, append cumulative cost rows for a card
       that is still in flight, and assert on the FILESYSTEM before any restart —
       `existsSync(join(root, 'cost-ledger.jsonl'))` is `true` and its contents contain the card id.
       This is a separate, earlier failure point than the reload, on purpose: an in-memory
       accumulator cannot pass it.
    2. **Restart:** `spawnSync(process.execPath, [<a small child script>, home, taskId], { encoding: 'utf8' })`.
       The child requires `src/main/hive.ts` through `test/load-ts.cjs`, constructs a **fresh**
       `HiveManager` over that same home, and prints `taskSpend(taskId)` as JSON on stdout. This file
       already drives `process.execPath` with `spawnSync` twice (`:114`, `:143`) — reuse that shape;
       there is no new machinery to invent.
    3. **Parent** asserts the child's `status` is `0` and that the parsed stdout's `tokens` equals the
       specific expected value — not merely "non-zero".
    Add a **negative control** in the same test: repeat step 2 after `rmSync` of the ledger file and
    assert the fresh process reports `tokens: 0`. If the positive and the negative both hold, the test
    is reading real bytes; if the negative ALSO finds spend, the rescan is serving it from somewhere
    that is not the ledger, and that is the defect to report rather than route around.
    A `fork()`ed worker sharing module state, a `vm` context, or `delete require.cache` in-process do
    **not** count and must not be substituted.

    Add the FLOOR-09 test to `test/engine-parity.test.cjs`, and make it the **runtime proof that
    replaces the string count** — the vacuous `grep -c "recordCostSample"` criterion is gone from task
    2 precisely because this test is what establishes the claim. It must exercise the real objects,
    end to end:

    1. Construct a real `TelemetryCollector` (no stub — `getAgentUsage` is the thing under test).
    2. Construct a real `HookServer` with `recordCost` wired to `(s) => telemetry.recordCostSample(s)`
       — the same argument the `<handoff>` gives plan 08 task 6, so this test and production share
       one wiring shape rather than two.
    3. Drive a proxy-tier `CostSample` payload (qwen or crush) **through `HookServer`'s own payload
       path**, carrying a valid `sock_token` so it passes `authorized()`. Do not call
       `telemetry.recordCostSample` directly from the test: that would prove only that the collector
       adds numbers, which the six pre-existing calls already prove, and would stay green if the
       `hooks.ts` branch were deleted outright.
    4. Assert `telemetry.getAgentUsage(agentId)` is non-null and its `input`/`output` reflect the
       posted sample. **This assertion is FLOOR-09** — a doc comment cannot satisfy it.
    5. Then feed that sample to the breaker via the existing two-beat pattern and assert the level
       moves. Tick once for the baseline, tick again after the sample.
    6. Add the negative control and paste its red output: with `recordCost` omitted from the
       `HookServer` construction, `getAgentUsage` returns `null` for that agent. This is what makes
       the positive assertion mean something, and it is the shape that would go red if plan 08 task
       6's injection were dropped in production.

    **Do not write this test in any hand-rolled harness.** Eight of the repo's 56 test files define their own local `test()` and
    have no `require('node:test')`; a `node:test` registration added to one of them is shadowed by the
    local function and killed by the file's own `process.exit` before it runs — a green run that
    asserted nothing. `test/breaker.test.cjs` is the one nearest this work (`test()` at `:34`,
    `process.exit(failures ? 1 : 0)` at `:224`), but the same trap sits in all eight. The full set,
    produced by
    `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done`
    is: `agent-provider`, `breaker`, `kg-core`, `proc-kill`, `realtime-findcard`, `slack`,
    `transcript-usage`, `voice-messages` (all `test/*.test.cjs`). Write the FLOOR-09 test in
    `test/engine-parity.test.cjs`, which is a real `node:test` file.

    Every assertion carries a message stating what a failure would MEAN.
  </action>
  <acceptance_criteria>
    - Both files are re-run **under the TAP reporter** and their counters pasted. Exit code alone is not evidence here: `node --test` counts skipped tests in its total and exits `0`, so N `{skip: '…'}` stubs would satisfy any "test count at least N higher" criterion (verified at plan time — two skipped tests give `EXIT=0`, `# pass 0`, `# skipped 2`).
      `TAP=$(mktemp); node --test --test-reporter=tap test/hive-protocol-v2.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# skipped 0`, `# todo 0`, and **both** of these — a delta AND an absolute, because either alone is defeatable:
      - `# pass` at least **B-pass-hp + 2** using the number task 1 MEASURED (the >1 MB ledger test and the restart test), and
      - `# pass` at least **`19`** in absolute terms — `01-03-PLAN.md`'s wave-2 floor of `17` plus this task's two.
      The stale plan-time arithmetic was `12 + 2 = 14`, which sits **below** the `17` plan 03 guarantees: at wave 3 the file is already at or above `17`, so a floor of `14` was satisfied by adding nothing and RECORD-03 plus the restart claim would have shipped untested. If the measured B-pass-hp is above `17`, the absolute floor rises with it — `B-pass-hp + 2` is the binding one; the `19` is the guard against reusing a stale B-pass-hp.
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# skipped 0`, `# todo 0`, and **`# pass` at least B-pass-ep + 2 as measured in task 1** (the FLOOR-09 test and its negative control) **and at least `16`** in absolute terms. Assert on `# pass`, never `# tests` — `# tests` counts skips.
    - No bypass construct was introduced in either file: `grep -cE "\.skip\(|\.todo\(|skip:|todo:" test/engine-parity.test.cjs test/hive-protocol-v2.test.cjs` returns `0` for both.
    - The FLOOR-09 test is a runtime proof, not a string count. **`grep -c "getAgentUsage" test/engine-parity.test.cjs` returns strictly greater than B-gau** — the number task 1 measured, **`6` today, not `0`**. An earlier revision of this criterion claimed "baseline today: `0`" and required "at least `1`", which current source already satisfies: it was an inverted baseline that proved nothing about the sink being wired, and it survived three red-team rounds because nobody re-measured it. Paste B-gau and the new count.
    - **The FLOOR-09 test drives the REAL server, which is the part a `getAgentUsage` count cannot show.** `grep -c "new HookServer(" test/engine-parity.test.cjs` returns at least `1` and `grep -c "sock_token" test/engine-parity.test.cjs` returns at least `1` — baseline **`0` for both**, measured 2026-08-21, so unlike the count above these fail against current source. They are what pins step 3's requirement that the payload go through `HookServer`'s own path and past `authorized()`, rather than calling `telemetry.recordCostSample` directly (which the six pre-existing calls already cover and which would stay green with the `hooks.ts` branch deleted outright).
    - The SUMMARY pastes the assertion line that reads the posted sample's tokens back out of `getAgentUsage`.
    - The FLOOR-09 negative control was run: paste the **red** output of the variant with `recordCost` omitted from the `HookServer` construction, showing a non-zero exit. A wiring test that cannot be made to fail has not been demonstrated to be a test.
    - `grep -c "assert.equal(spend.tokens, 20)" test/hive-protocol-v2.test.cjs` returns `0`.
    - **The restart test spawns a real second process, proved by a strict delta rather than a bare presence check.** This file ALREADY uses `process.execPath` twice (`:114`, `:143`) and `spawnSync` four times (`:23`, `:114`, `:143`, `:319`), so plan 08's `at least 1` form would pass here untouched — measured 2026-08-21: `grep -c "process.execPath" test/hive-protocol-v2.test.cjs` → **`2`**, `grep -cE "spawnSync|execFileSync"` → **`4`**. Required instead: `grep -c "process.execPath"` **strictly greater than the baseline task 1 records** (expect at least `4`: the restart spawn and the negative control), and `grep -c "taskSpend" test/hive-protocol-v2.test.cjs` strictly greater than its baseline of **`3`** (`:276`, `:283`, `:284`). Paste every baseline and every new count. **The fake this catches:** constructing a second `HiveManager` in-process over the same home, which satisfies every prose reading of "survives a restart" and passes without moving either number.
    - **The restart negative control was run:** paste the run with the ledger file removed before the child spawns, showing the fresh process reports `tokens: 0` — and paste a run with that removal commented out showing the test can distinguish the two. A durability test that cannot be made to fail has not been demonstrated to be a test.
    - `git log -1 --format=%B` for the test-rewrite commit contains the literal string `spend.tokens, 20` — the old expectation quoted per D-23.
    - The hand-rolled set is unchanged — **a preservation criterion**, not a completion criterion. Run:
      `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done | wc -l`
      It prints `8`, today's measured baseline, confirming no `node:test` registration was added to any hand-rolled harness and none of them was silently converted. Paste the list, not just the count; it must still be exactly `agent-provider`, `breaker`, `kg-core`, `proc-kill`, `realtime-findcard`, `slack`, `transcript-usage`, `voice-messages`.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-protocol-v2.test.cjs</automated>
  </verify>
  <done>The defect-encoding test is rewritten with its old expectation recorded in the commit message, a >1 MB ledger and a restart are both covered, and proxy-tier spend is proven to reach the breaker.</done>
</task>

<task type="auto">
  <name>Task 4: Close GATE-01's qwen sidecar hole — the per-agent token into startProxyBridge's env AND the missing `sock_token` field into the three shims that never sent one (handed over from plan 02)</name>
  <files>
    - src/main/hive.ts
    - test/engine-parity.test.cjs
    - test/hook-auth-roundtrip.test.cjs
  </files>
  <action>
    **Why this task lives here and not in plan 02.** Plan 02 (wave 2) replaces the floor-wide hook
    secret with per-agent identity, and to do it deletes `src/main/index.ts:5534`
    (`process.env.HIVE_SOCK_TOKEN = hookSockToken();`). The qwen proxy-bridge sidecar is spawned by
    `startProxyBridge(agentId, cfg)` at `src/main/hive.ts:1170-1223` (called only from `:930`), whose
    `env:` literal at `:1183-1192` spreads `...process.env` and names `HIVE_SOCK`, `AGENT_ID`,
    `UPSTREAM_BASE_URL`, `HIVE_PROXY_SESSION`, `HIVE_PROXY_API` — and **no `HIVE_SOCK_TOKEN`**. It
    lives entirely on that deleted assignment, as `index.ts:5525-5533` says in as many words.

    **And it is worse than that paragraph says, which is why this task is bigger than two lines.**
    `PROXY_BRIDGE_SHIM` sends **no `sock_token` field at all** — not an empty one. `authorized()`
    (`hooks.ts:194-196`) coerces a missing field with
    `typeof p.sock_token === 'string' ? p.sock_token : ''`, so the *effect* is an empty token and the
    payload is dropped either way; but the *cause* is a missing field in the shim body, not a missing
    env var. That distinction is exactly what an earlier revision of this task got wrong. So from the
    end of wave 2 until this task lands — **and, for the sidecar, already today at HEAD** — every
    qwen/crush agent is dropped by `authorized()`: dead-hooked, silently, with no live status, no
    Stop→drain and no cost rows.

    Plan 02 could not make this edit (its wave's `hive.ts` owner is plan 03) and plan 03 could not
    either — the edit references the per-agent token accessor plan 02 creates *in the same wave*, so
    whichever landed first would leave the tree failing `npm run typecheck`. This plan is wave 3, owns
    `hive.ts` and `hooks.ts`, and runs after plan 02's mint exists. Read `01-02-SUMMARY.md` first: it
    records the accessor's real name and signature, which is what this task calls.

    **The change.** Re-derive the site by content first — plan 03 shifted every `hive.ts` offset in
    wave 2, so the `:1170-1223` and `:1183-1192` numbers above are pointers, not addresses:
    `grep -n "private startProxyBridge" src/main/hive.ts` and read from the number it prints. Then
    thread the minted per-agent token through `startProxyBridge`'s `cfg` and add
    `HIVE_SOCK_TOKEN: cfg.token` (or whatever plan 02's SUMMARY names it) to the `env:` literal.
    Do **not** write `HIVE_SOCK_TOKEN: process.env.HIVE_SOCK_TOKEN` — that reintroduces the
    floor-wide secret by a longer route and is the specific thing GATE-01 removes.

    **AND THE SHIM BODY, WHICH IS THE HALF THAT MAKES THE ENV CHANGE MEAN ANYTHING.** An earlier
    revision of this task said *"do not touch the six shim templates; they already read the var from
    inherited env"*, and `01-02-PLAN.md` said the same. **That is false for three of the six**, and
    it made this task a no-op that still passed its own criterion, plan 23's wholeness check and both
    SUMMARYs. Measured 2026-08-21:

    ```
    for t in HOOK_SHIM AGY_HOOK_SHIM PI_EXTENSION OPENCODE_PLUGIN PROXY_BRIDGE_SHIM GROK_HOOK_SHIM; do
      sed -n "/^const $t = \`/,/^\`;/p" src/main/hive.ts | grep -c "HIVE_SOCK_TOKEN"; done
    # HOOK_SHIM 1 · AGY_HOOK_SHIM 1 · GROK_HOOK_SHIM 1
    # PI_EXTENSION 0 · OPENCODE_PLUGIN 0 · PROXY_BRIDGE_SHIM 0
    ```

    `PROXY_BRIDGE_SHIM` **is the qwen sidecar's own shim.** Its only socket writer is
    `emit(payload)` (`hive.ts:3307-3313`; re-derive with `grep -n "^const PROXY_BRIDGE_SHIM" src/main/hive.ts`),
    and every `Stop`, `Status`, `CostSample` and `PostToolUse` it sends goes through that one
    function with **no `sock_token` field**. So adding `HIVE_SOCK_TOKEN: cfg.token` to the env
    changes nothing the sidecar reads: it would receive a variable it never sends, `authorized()`
    would keep dropping it, and qwen/crush would stay dead-hooked with every gate green. Fix `emit()`
    the way the three working shims do it. Two spellings, both fine — `HOOK_SHIM:3092` assigns onto
    the payload it parsed (`payload.sock_token = process.env.HIVE_SOCK_TOKEN || '';`) while
    `AGY_HOOK_SHIM:3162` and `GROK_HOOK_SHIM:3534` set it as an object-literal property
    (`sock_token: process.env.HIVE_SOCK_TOKEN || '',`), and the latter two carry the comment
    *"See HOOK_SHIM: without this the socket rejects every payload."* `emit()` receives an
    already-built payload object, so it takes `HOOK_SHIM`'s assignment form. Add the field inside
    `emit()`, before `JSON.stringify(payload)`, so every payload shape gets it from one place rather
    than five call sites.

    **`PI_EXTENSION` and `OPENCODE_PLUGIN` need the identical one-line fix, and here is the evidence
    that decides it.** The question is whether their payloads reach `authorized()` at all or ride a
    different, fire-and-forget path. They reach it. `src/main/hooks.ts:139-157` installs **exactly
    one** connection handler and `if (!this.authorized(payload)) { conn.end('{}'); return; }` at
    `:151` is the only door into `handle()`; there is no second server, no unauthenticated branch and
    no bypass keyed on engine or event. Both shims write to the same `process.env.HIVE_SOCK` the
    others do — `PI_EXTENSION`'s `post()` at `hive.ts:3210-3217`, `OPENCODE_PLUGIN`'s `post()` at
    `:3247-3254`, each a single `net.createConnection(SOCK, …)`. "Fire and forget" describes only
    that they ignore the response; it does not mean a different code path. **Consequence: pi and
    opencode are already dead-hooked at HEAD**, today, independent of GATE-01 — a pre-existing
    defect this task closes rather than one this phase introduced. Say exactly that in the SUMMARY so
    plan 23 does not misattribute it. Add `payload.sock_token = process.env.HIVE_SOCK_TOKEN || '';`
    to each `post()` beside the existing `payload.agent_id = payload.agent_id || AGENT;` line.

    So: **five lines across `hive.ts`, not two** — the sidecar env argument, and one `sock_token`
    field in each of the three shims that lack it. All of it in `hive.ts`, which is this plan's file
    in this wave, so there is no further handoff.

    **Also widen the shim guard in `test/hook-auth-roundtrip.test.cjs`.** Plan 02 (wave 2) rewrote
    that test's third case to enumerate the shim templates out of `hive.ts` rather than iterate a
    hardcoded three-element array, but had to keep the per-template `sock_token` assertion scoped to
    the three that carry it today — `hive.ts` was not plan 02's file, so asserting all six would have
    left a red test between waves. This task lands the three missing fields, so widen that assertion
    to **all** templates the enumeration finds, and delete the comment naming this task as the plan
    that would. That is what makes the file's own claim — *"it fails loudly if a fourth shim is added
    without it"* — true for the first time.

    Revoke on sidecar exit the same way plan 02 revokes on PTY exit, if its accessor exposes that.

    **Also take plan 02's second handoff: stop the hive's own git from running agent-written hooks.**
    Plan 02's task 2 names this and cannot make it — `hive.ts` is not its file. Re-verified this
    session: the hive root is a git repo (`this.git(['init','-q'], root)`, `hive.ts:696`), and BOTH
    git wrappers spawn with a fixed `-c` prefix and **no hook suppression at all** —
    `git()` at `:2615-2620` and `gitAsync()` at `:2627-2645` each pass
    `['-c','commit.gpgsign=false','-c','user.name=Hive','-c','user.email=hive@local', …]`, and
    `grep -c "no-verify" src/main/hive.ts` returns `0`. Neither passes an `env`, so a hook they
    trigger inherits the Electron main process's environment — more privilege than the agent that
    planted it has, and reached from **outside** plan 02's PreToolUse gate.
    Fix it **once, in the shared `-c` prefix of both functions**, not at the seven `commit()` call
    sites: whatever you choose (`-c core.hooksPath=<a path that does not exist>` disables every hook
    for every hive git invocation; `--no-verify` on the commit only covers `pre-commit` and
    `commit-msg` and leaves `post-commit`) must apply to `git()` and `gitAsync()` alike, because
    either one can be the next writer. State the choice and its ceiling in the comment.
    Plan 02's gate denying writes under `<root>/.git` is the front door; this is the backstop for the
    engines that gate cannot see (pi, opencode, the proxy tier) — neither alone is sufficient, which
    is the reason both exist.

    **If `01-02-SUMMARY.md` shows plan 02 did not land its mint**, stop and report — do not invent an
    accessor and do not restore the floor-wide assignment to make the sidecar work again.
  </action>
  <acceptance_criteria>
    - The sidecar carries a per-agent token. Run and paste:
      `sed -n '/private startProxyBridge/,/^  }$/p' src/main/hive.ts | grep -n "HIVE_SOCK_TOKEN"`
      It must return exactly ONE line, that line must sit inside the `env:` literal, and it must NOT
      read `process.env.HIVE_SOCK_TOKEN`. Baseline today is `0` lines, so this criterion fails against
      current source — that is the point. **On its own this criterion is satisfied by a no-op**, which
      is why the next one exists.
    - **ALL SIX shim templates read the token. Anything below `6` fails this task.** This is the
      criterion that makes the env change mean something: `PROXY_BRIDGE_SHIM` is the sidecar's own
      shim, and until its `emit()` sends a `sock_token` the env argument feeds a variable nothing
      reads. Run and paste the whole loop, one number per template, in this order:
      ```
      for t in HOOK_SHIM AGY_HOOK_SHIM PI_EXTENSION OPENCODE_PLUGIN PROXY_BRIDGE_SHIM GROK_HOOK_SHIM; do
        sed -n "/^const $t = `/,/^`;/p" src/main/hive.ts | grep -c "HIVE_SOCK_TOKEN"; done
      ```
      Required: **every one of the six is at least `1`**. Baseline measured 2026-08-21: `1 1 0 0 0 1`
      — three of six — so this criterion fails against current source, which is the point. The
      correct number is `6` and not fewer, because all six write to the same socket and
      `hooks.ts:151` is the only door: `PI_EXTENSION` (`post()`, `hive.ts:3210-3217`) and
      `OPENCODE_PLUGIN` (`post()`, `:3247-3254`) are fire-and-forget only in the sense that they
      ignore the response — their payloads still pass through `authorized()` and are still dropped.
      If a future revision argues one of the six legitimately needs no token, it must first show a
      second connection handler or an unauthenticated branch in `hooks.ts`; there is neither today.
    - **The same six carry the FIELD, not just the read** — a template could read the env var into an
      unused const and pass the count above. Paste:
      ```
      for t in HOOK_SHIM AGY_HOOK_SHIM PI_EXTENSION OPENCODE_PLUGIN PROXY_BRIDGE_SHIM GROK_HOOK_SHIM; do
        sed -n "/^const $t = `/,/^`;/p" src/main/hive.ts | grep -c "sock_token"; done
      ```
      Required: every one at least `1`. Baseline 2026-08-21: `1 1 0 0 0 1`.
    - **Runtime proof that the sidecar's payload actually carries it**, in `test/engine-parity.test.cjs`:
      evaluate `PROXY_BRIDGE_SHIM`'s `emit()` (or drive the written shim file) with
      `HIVE_SOCK_TOKEN` set to a known value and assert the JSON written to the socket parses to an
      object whose `sock_token` equals that value. A grep proves the field is in the template; only
      this proves it is in the bytes. Assert the negative too: with `HIVE_SOCK_TOKEN` unset the field
      is `''` and `authorized()` drops it — that is the pre-fix behaviour and it must stay legible.
    - **`test/hook-auth-roundtrip.test.cjs`'s derived shim guard is widened to all six.** After this
      task, `grep -cE "HOOK_SHIM|PI_EXTENSION|OPENCODE_PLUGIN" test/hook-auth-roundtrip.test.cjs`
      returns `0` — no template is named individually any more, the enumeration is derived — and the
      test's own count assertion sees at least `6`. Baseline in the file as plan 02 leaves it: plan 02
      already removed the `const HOOK_SHIM` array literal, so the remaining hits are the narrowing
      comment this task deletes. Re-measure against `01-02-SUMMARY.md`'s state rather than against
      today's file, which has not been through plan 02 yet. Paste its TAP counters. On win32 its first two tests are `{ skip: !POSIX }`, so
      `# skipped 2` there is expected and correct; the shim guard is the test that runs everywhere.
    - The floor-wide route did not come back: `grep -c "process.env.HIVE_SOCK_TOKEN *=" src/main/index.ts` returns `0` (plan 02's value — this task must not restore it), and `sed -n '/private startProxyBridge/,/^  }$/p' src/main/hive.ts | grep -c "process.env.HIVE_SOCK_TOKEN"` returns `0`.
    - **The hive's git no longer runs agent-planted hooks.** Both wrappers carry the suppression:
      `sed -n '/private git(args: string\[\], cwd: string)/,/^  }$/p' src/main/hive.ts | grep -cE "no-verify|hooksPath"` returns at least `1`, and the same slice for `private gitAsync` returns at least `1`. Baseline for both: `0` (`grep -c "no-verify" src/main/hive.ts` and `grep -c "hooksPath" src/main/hive.ts` are both `0` today) — paste all four numbers.
      **Behavioural proof, because a `-c` flag can be present and still not take effect:** in a temp hive root, write `.git/hooks/pre-commit` as a script that creates a sentinel file and `exit 1`, drive a real `commit()`, then assert the sentinel does NOT exist and the commit succeeded. Paste the run. Do this in `test/engine-parity.test.cjs` alongside this task's other additions, and count it in the `# pass` floor below.
    - `npm run typecheck` exit code is `0` — the accessor this task calls actually exists.
    - A test proves the sidecar env carries a token that is NOT the floor-wide one: add it to `test/engine-parity.test.cjs` (a real `node:test` file — do not write it into any of the eight hand-rolled harnesses), asserting on the env object `startProxyBridge` builds. Verify under the TAP reporter, not the exit code — a `{skip: '…'}` stub raises `# tests` and still exits `0`:
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# skipped 0`, `# todo 0`, and **`# pass` at least B-pass-ep + 5 as measured in task 1** — task 3's two, plus this task's three: the sidecar-env test, the `sock_token`-in-the-bytes runtime test, and the git-hook suppression test — **and at least `19`** in absolute terms.
    - This task's commits stay inside this plan's declared files: `git show --stat <sha> -- src/main/index.ts src/preload/ src/main/breaker.ts` prints no files for each. Path-scoped per commit, never a bare `git diff --stat` — plan 07 is writing `src/main/index.ts` in this same wave under `use_worktrees: false`, so a tree-wide assertion would fail on a wave-mate's work rather than on this plan's.
    - `01-06-SUMMARY.md` states that GATE-01's sidecar hole is closed and names the wave-2→wave-3 window it was open for. Plan 02's SUMMARY opened that window with the literal `QWEN SIDECAR DEAD-HOOKED UNTIL 01-06`; this SUMMARY closes it.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>The qwen proxy sidecar authenticates with its own agent's token, the floor-wide assignment stays deleted, and the one-wave dead-hook window plan 02 opened is closed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook socket payload (`CostSample`) → cost ledger | An agent-adjacent sidecar supplies the numbers a budget is enforced against. `authorized()` is the gate and must stay ahead of every cost path. |
| cost ledger file on disk → `taskSpend()` | The ledger is append-only text; anything that can write the hive directory can influence enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P06-01 | Tampering | forged `CostSample` payloads poisoning the ledger | mitigate | FLOOR-09's rewiring stays behind `hooks.ts`'s `authorized()` gate; no second unauthenticated path into `appendCostLedger` is created. Plan 02's per-agent tokens are what make that gate meaningful. |
| T-P06-02 | Tampering | garbage/negative/NaN usage values rewinding a cumulative accumulator | mitigate | Keep every `?? 0` coercion and the clamp-to-zero on the consecutive diff; `test/engine-parity.test.cjs:85-94` already pins this and must stay green. |
| T-P06-03 | Repudiation | a card's spend silently resetting to zero across an app restart | mitigate | One full ledger scan at startup rebuilds the per-card accumulator (D-20). |
| T-P06-04 | Denial of service | the per-card accumulator growing without bound now that the 1 MB tail is gone | mitigate | Bound the `Map<taskId, tokens>` by card lifetime — drop entries for archived or deleted cards. RECORD-03 deliberately removes a bound and this is what replaces it. |
| T-P06-05 | Denial of service | a full ledger scan at every startup on a large ledger | accept | One scan at boot, on a file already bounded by `LOG_ROTATE_BYTES`. If it ever measurably delays boot, the `cost_ledger` table in `PersistStore` is a one-query alternative — recorded, not built. |
| T-P06-06 | Repudiation | changing a test so a buggy source path passes | mitigate | D-23's exact sequence is enforced by acceptance criteria: three source-only commits verified with path-scoped `git show --stat`, then the test rewrite in a separate commit quoting the old expectation. |
| T-P06-07 | Repudiation | FLOOR-09 reported closed while proxy spend still never reaches `getAgentUsage` (D-21) — the `recordCost` sink is minted but the one `index.ts` argument is never injected | mitigate | The vacuous `grep -c "recordCostSample" src/main/hooks.ts` criterion (satisfiable by a doc comment) is replaced by a runtime test that reads the posted sample back out of `telemetry.getAgentUsage()` with a red negative control, and by task 2's `<handoff>` naming **plan 08 task 6** with a verbatim diff and one of two literal SUMMARY status lines under the exact heading plan 08 greps for. **Plan 10 task 5** turns it into a hard STOP-gate in wave 5 (`01-10-PLAN.md:481-489`) and plan 23 pins it in wave 9. Not plan 07, not plan 09 — plan 09 records the state and declines to gate on it (`01-09-PLAN.md:205`, `:221`), because plan 08 lands the injection in plan 09's own wave. |
| T-P06-09 | Spoofing | task 4's sidecar env fix landing as a **no-op**: `HIVE_SOCK_TOKEN` added to `startProxyBridge`'s env while `PROXY_BRIDGE_SHIM`'s `emit()` still sends no `sock_token`, so `authorized()` keeps dropping every qwen payload while this plan's criterion, plan 23's wholeness check and both SUMMARYs all report success | mitigate | This is not hypothetical — it is the shape an earlier revision of this plan and of `01-02-PLAN.md` actively prescribed ("the six shim templates already read `process.env.HIVE_SOCK_TOKEN` — they need NO body change"), which is false for three of the six. Task 4 now carries the shim-body change as well as the env change, and its acceptance criterion counts token reads across all six templates and fails at anything below `6`. The runtime proof is the payload-shape assertion on `emit()`'s output, not the env grep. |
| T-P06-08 | Repudiation | a green run that asserted nothing — `node --test` counts skipped tests and exits `0`, so `{skip}` stubs satisfy any "test count at least N higher" criterion | mitigate | Every test-count criterion asserts `# pass` from `--test-reporter=tap` with `# fail 0` / `# skipped 0` / `# todo 0`, and both target files are checked for `.skip(` / `.todo(` constructs. |
</threat_model>

<verification>
- `npm test` green on all three platforms — and that claim resolves against **the draft PR plan 01
  opens to `main` in wave 1**, not against a phase-branch push. Both workflows are
  `branches: [main]` only (`ci.yml:4-7`, `e2e.yml:11-14`), so pushing the phase branch triggers **no
  run at all**. Read it with `gh pr checks` on that PR and paste the rows under their real check-run
  names: `Typecheck`, `Test (ubuntu-latest)`, `Test (windows-latest)`, `Test (macos-latest)`,
  `Electron smoke (ubuntu-latest)`. **There is no check run named `E2E`** — that is the workflow's
  `name:`, not a job's.
  Locally, `test/engine-parity.test.cjs` and `test/hive-protocol-v2.test.cjs` are re-run under
  `--test-reporter=tap` with their `# pass` / `# fail 0` / `# skipped 0` / `# todo 0` counters
  pasted. `npm test` exits `0` on a skipped test, so "green" is only meaningful next to a
  `# skipped 0` counter. `test/hook-auth-roundtrip.test.cjs` is the one exception in this plan's set:
  its first two tests carry `{ skip: !POSIX }`, so `# skipped 2` on the Windows run is expected and
  `# skipped 0` is required only on a POSIX run — say which platform produced which.
- `npm run typecheck` green.
- `git show --stat <sha> -- test/ src/main/index.ts` output for the three source commits pasted,
  proving no test file and no `index.ts` line was touched in the same commit as the arithmetic
  change. Path-scoped per commit — a wave-mate is writing this tree concurrently.
- The `T-INDEX HANDOFF → 01-08 (FLOOR-09)` block is in the SUMMARY with its verbatim diff and one of
  the two literal status lines.
</verification>

<success_criteria>
- ROADMAP criterion 3's arithmetic clause is TRUE: spend is summed over **all** of that card's rows
  rather than a 1 MB tail, and derived from the **difference** between cumulative usage snapshots
  rather than their sum — so a card given a small cap and run long is stopped, and a card given a
  generous cap is not stopped early by double counting.
- ROADMAP criterion 3's coverage clause is TRUE **at the seam this plan owns**: a `CostSample` posted
  at the hook socket is read back out of `telemetry.getAgentUsage()` and trips the breaker, proven by
  a runtime test with a red negative control — not by a `grep -c` over `hooks.ts`.
- Issue #19's spend-reaches-the-breaker clause closes in this PR per D-43, with the empty
  `recordCostSample` production-caller grep pasted as the evidence the clause was open, **and** with
  the FLOOR-09 production-injection status recorded verbatim. If the injection has not landed, #19
  does not close here — and it will not, because the injection is **plan 08 task 6's, in wave 4**.
  The clause stays open through this plan by design; **plan 10 task 5** (wave 5) is the hard gate
  that says so, and plan 23 pins it in wave 9. Neither plan 07 nor plan 09 carries any part of it:
  plan 09 records the state and explicitly declines to gate on it (`01-09-PLAN.md:205`, `:221`).
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-06-SUMMARY.md` when done. It MUST record the written
arithmetic contract with its worked example, whether `COST_TAIL_BYTES` survived for other callers,
the accumulator's lifetime bound, and the path-scoped `git show --stat` evidence for D-23's commit
ordering.

It MUST also record, under a heading containing the literal string `SHIM TOKEN COVERAGE`, the
per-template count both BEFORE (`1 1 0 0 0 1`) and AFTER (`1 1 1 1 1 1`) task 4, and state plainly
that `PI_EXTENSION`, `OPENCODE_PLUGIN` and `PROXY_BRIDGE_SHIM` were dead-hooked **at HEAD**, before
this phase — a pre-existing defect this task closes, not one the phase introduced. Plan 23 needs that
attribution to read its wave-9 sweep correctly.

It MUST state that **FLOOR-09 does not close in this plan**: the sink is minted and proven at runtime
here, and `01-08-PLAN.md` task 6 applies the production injection in wave 4, which is the wave FLOOR-09
becomes true. Plan 08's frontmatter declares FLOOR-09 as the closing half of that split; nothing in
this SUMMARY may imply otherwise.

It MUST also carry, under a heading that reads exactly `T-INDEX HANDOFF → 01-08 (FLOOR-09)` — plan 08
task 6 greps for that exact string and STOPs with `FLOOR-09 HANDOFF MISSING FROM 01-06` if it is
absent or misspelled:
- the one-argument diff for `index.ts:465-469` reproduced verbatim from task 2's `<handoff>`;
- the re-derived line number `grep -n "new HookServer(" src/main/index.ts` printed at execution time;
- exactly one of the two literal status lines — `FLOOR-09 PRODUCTION INJECTION LANDED` or
  `FLOOR-09 PROXY SPEND NOT YET IN getAgentUsage — T-INDEX INJECTION HANDED TO 01-08`;
- the pasted TAP counters for both test files.
</output>
