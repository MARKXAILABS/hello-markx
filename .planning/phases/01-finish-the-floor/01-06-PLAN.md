---
phase: 01-finish-the-floor
plan: 06
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]  # 01-02 added: task 4 calls the per-agent token accessor plan 02 mints
files_modified:
  - src/main/hive.ts
  - src/main/hooks.ts
  - src/main/telemetry.ts
  - test/hive-protocol-v2.test.cjs
  - test/engine-parity.test.cjs
autonomous: true
requirements: [RECORD-03, RECORD-04, FLOOR-09, GATE-01]  # GATE-01 partial: task 4 only, the qwen sidecar handoff from plan 02

must_haves:
  truths:
    - "The qwen proxy sidecar authenticates with its own agent's token, closing the wave-2→wave-3 window plan 02 opened when it deleted the floor-wide assignment (task 4)"
    - "Every cost-ledger row is a cumulative per-agent snapshot — the two appenders no longer disagree"
    - "A card's spend is derived from clamped consecutive differences, not from summing snapshots"
    - "Spend is summed over ALL of a card's rows, not through a 1 MB tail"
    - "A card in flight across an app restart does not silently reset its spend to zero"
    - "qwen/crush proxy-tier spend reaches getAgentUsage and therefore the breaker's cost, token and velocity arms"
  artifacts:
    - path: "src/main/hive.ts"
      provides: "cumulative-diff taskSpend, in-memory per-card accumulator, startup ledger rescan"
      contains: "taskSpend"
    - path: "src/main/hooks.ts"
      provides: "CostSample branch routed through telemetry.recordCostSample"
      contains: "recordCostSample"
    - path: "test/hive-protocol-v2.test.cjs"
      provides: "the rewritten per-card cost test asserting diffs, and a >1 MB ledger test"
  key_links:
    - from: "src/main/hooks.ts CostSample branch"
      to: "telemetry.recordCostSample()"
      via: "the documented wiring that had zero production callers"
      pattern: "recordCostSample"
    - from: "src/main/hive.ts taskSpend()"
      to: "the full cost ledger"
      via: "per-card accumulator rebuilt by one startup scan, no COST_TAIL_BYTES"
      pattern: "taskSpend"
---

<objective>
Make the number a budget could be enforced against correct, before anything enforces it.

Purpose: RECORD-03 and RECORD-04 are prerequisites of FLOOR-10, and `taskSpend()` currently computes
the wrong number twice over. The ledger holds **mixed row semantics** — `index.ts:1513` appends
cumulative per-beat snapshots while `hooks.ts:272` appends per-response deltas — and `taskSpend()`
sums both, over-counting roughly quadratically. It then reads through a 1 MB tail, so a long card's
early spend falls out of the window and `over` reads false when it is true. `db.ts:44` states the
intended contract verbatim. FLOOR-09 closes almost for free in the same edit, because
`telemetry.recordCostSample()` exists, is documented as exactly this wiring, and has zero production
callers.

Output: one row semantics, spend from clamped diffs over all rows, proxy-tier spend reaching the
breaker, and the test that encoded the defect rewritten — source first, in its own commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@docs/adr/0004-single-committer-git.md

<interfaces>
Anchors, all read this session (`appendCostLedger` is **CORRECTED** from RESEARCH's `:2520`):

- `src/main/hive.ts:244` — `const COST_TAIL_BYTES = 1024 * 1024;` with a comment that already names
  the defect and tags it `ponytail:`.
- `src/main/hive.ts:2513` — `appendCostLedger`.
- `src/main/hive.ts:2566-2582` — `taskSpend()`; the tail read is at `:2571` via
  `this.tailLines(join(root,'cost-ledger.jsonl'), COST_TAIL_BYTES)`; it accumulates
  `tokens += (row.input ?? 0) + (row.output ?? 0)`.
- `src/main/index.ts:1505-1513` — the beat: `getAgentUsage(id)` (a CUMULATIVE `AgentUsageSample`)
  then `hive.appendCostLedger(sample)`. **This line does not change** — the accumulator and the
  rescan belong in `hive.ts` next to `taskSpend`, which keeps `index.ts` out of this plan entirely.
- `src/main/hooks.ts:261-289` — the `CostSample` branch. Its `return {}` at `:288` is deliberate and
  documented (it keeps proxy cost OUT of the Claude-only OTel/breaker/drain paths). FLOOR-09 exists
  to change that, so it needs a deliberate rewrite, not a deletion. The `?? 0` coercions are pinned
  by `test/engine-parity.test.cjs:85-94` — keep them.
- `src/main/telemetry.ts:11` — the cumulative contract; `:251` — `recordCostSample()`, zero
  production callers (only two doc comments in `src/shared/agentProvider.ts:119,:321` and six calls
  in `test/engine-parity.test.cjs`).
- `src/main/db.ts:44` — *"Rows are CUMULATIVE snapshots (one per agent per heartbeat beat) — diff
  consecutive rows for velocity."*
- A `cost_ledger` table already exists in `PersistStore` with a matching row shape. D-20 chose the
  in-memory Map; the SQL alternative (`SUM(...) GROUP BY task_id`) exists if the Map proves
  insufficient. Do not switch without saying why.

The test to rewrite is the WHOLE test at `test/hive-protocol-v2.test.cjs:256-284` —
`test('cost rows carry the card they were spent on, and a card knows its cap', …)` under the banner
`// ─── #34 — per-card cost attribution ───` at `:256`. Rewriting `:276-284` in isolation will not
compile; the body is one arrange/act/assert block. Its current expectation is
`assert.equal(spend.tokens, 20)` — the summing contract this plan removes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every RECORD-03 / RECORD-04 / FLOOR-09 clause against current source and paste the output</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/hive.ts:240-250` (`COST_TAIL_BYTES` and its comment), `:2505-2525` (`appendCostLedger`), `:2560-2590` (`taskSpend`).
    - `src/main/hooks.ts:255-295` — the `CostSample` branch in full including the early return and its comment.
    - `src/main/telemetry.ts:1-30` and `:240-270` — the cumulative contract and `recordCostSample`.
    - `src/main/index.ts:1500-1520` — the beat appender (read only; this plan does not modify `index.ts`).
    - `src/main/db.ts:33-50` — the contract stated verbatim.
    - `test/hive-protocol-v2.test.cjs:250-290` and `test/engine-parity.test.cjs:40-100` — the two test surfaces.
    - `01-CONTEXT.md` D-19, D-20, D-21, D-23; `01-RESEARCH.md` § "Spend Arithmetic".
  </read_first>
  <action>
    Quote REQUIREMENTS.md RECORD-03, RECORD-04 and issue #19's acceptance text verbatim, and run one
    command per clause with ACTUAL output pasted:
    - `grep -n "COST_TAIL_BYTES" src/main/hive.ts` — expect the definition and the read at `:2571`.
    - `grep -n "appendCostLedger" src/main/*.ts` — expect exactly two production appenders plus the definition; paste them. This is the mixed-semantics evidence.
    - `grep -rn "recordCostSample" src/ test/` — expect zero production callers outside `telemetry.ts` (two doc comments in `agentProvider.ts` and six calls in `test/engine-parity.test.cjs`). The absence of production callers IS the FLOOR-09 evidence.
    - `grep -n "tokens +=" src/main/hive.ts` — the summing line.

    Then write down, before touching code, the exact arithmetic contract you are about to implement,
    as prose with a worked two-row example: given cumulative snapshots S1 then S2 for one agent, the
    card's spend contribution is `max(0, S2 − S1)` — clamped, because `telemetry.forget()` on respawn
    resets the accumulator and an unclamped diff would go negative. State how the first row for an
    agent is treated (its own value, or zero) and justify the choice against `db.ts:44`. The next task
    implements exactly this; a fuzzy contract here is how the test gets bent later.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all four greps.
    - The `recordCostSample` grep output is pasted and shows no production caller.
    - Task notes contain the written arithmetic contract with a worked two-row example and an explicit rule for the first row per agent.
    - `node --test test/hive-protocol-v2.test.cjs test/engine-parity.test.cjs` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-protocol-v2.test.cjs</automated>
  </verify>
  <done>Every clause has pasted evidence, and the target arithmetic is written down with a worked example before any code changes.</done>
</task>

<task type="auto">
  <name>Task 2: Unify row semantics, derive spend from clamped diffs over all rows, and wire proxy-tier cost into the breaker — source only, in ordered commits</name>
  <files>src/main/hive.ts, src/main/hooks.ts, src/main/telemetry.ts</files>
  <read_first>
    - `src/main/hive.ts` at the anchors above — the file being modified; read current bodies.
    - `src/main/hooks.ts:255-295` — the branch being rewritten, and the `authorized()` gate above it that must stay ahead of any cost path.
    - `src/main/telemetry.ts:240-270` — `recordCostSample`'s own arithmetic, including its handling of negative and `NaN` inputs (pinned by `test/engine-parity.test.cjs:85-94`).
    - `01-PATTERNS.md` § "FLOOR-09 / FLOOR-10 / RECORD-03 / RECORD-04 — the spend cluster" `:765-918`.
    - `.planning/codebase/CONVENTIONS.md` — comments explain WHY at length, citing the issue number.
  </read_first>
  <action>
    Land this as **three ordered commits, source only. No test file is touched in this task** — D-23 is
    explicit that the source fix lands first, in its own `fix(...)` commit, precisely because the test
    that must change encodes the defect and changing both together is indistinguishable from bending a
    test to match buggy output.

    Commit 1 — `fix(cost): unify ledger row semantics`. Make **every** row a cumulative per-agent
    snapshot. The `hooks.ts` `CostSample` branch currently appends per-response deltas: route it
    through `telemetry.recordCostSample()` (which already accumulates cumulatively) and append the
    resulting cumulative sample, rather than building a delta row by hand. Keep every `?? 0` coercion
    and the `NaN`/negative handling — `test/engine-parity.test.cjs:85-94` pins them. Keep the
    `authorized()` gate ahead of the cost path; FLOOR-09 must not open a second unauthenticated route
    into the ledger.

    Commit 2 — `fix(cost): sum over all rows`. Remove `COST_TAIL_BYTES` from the `taskSpend` path
    (leave the constant if another caller still needs it; delete it if not, and say which). Replace the
    summing loop with the clamped-consecutive-diff arithmetic written down in task 1. Per D-20, hold an
    in-memory `Map<taskId, tokens>` accumulated in the beat, rebuilt by **one full ledger scan at
    startup** so a card in flight across an app restart does not silently reset to zero. **Bound the
    Map by card lifetime** — RECORD-03 deliberately removes a memory bound (the 1 MB tail) and the Map
    is what replaces it; an entry for an archived or deleted card must be dropped, not kept forever.
    Carry a comment explaining the whole thing: why rows are cumulative, why the diff is clamped
    (`telemetry.forget()` on respawn), and why the tail had to go, citing #34 and RECORD-03/04.

    Commit 3 — `feat(cost): wire recordCostSample`. Rewrite the `hooks.ts` `CostSample` early
    `return {}` at `:288` deliberately — do not simply delete it. The return exists to keep proxy cost
    out of the Claude-only OTel/breaker/drain paths; FLOOR-09 exists to change exactly that, so the
    branch must now feed `getAgentUsage` while still not feeding the loop detector (the branch's own
    comment says "pure telemetry — never feeds the loop detector"). State in the comment which paths it
    now reaches and which it deliberately still does not.

    **Do not touch `src/main/index.ts`.** The beat's one-line `hive.appendCostLedger(sample)` call is
    unchanged by design — that is the seam that keeps this plan out of T-INDEX, which plan 07 holds
    this wave. Do not touch `src/main/breaker.ts`; plan 09 adds the budget arm next wave.
  </action>
  <acceptance_criteria>
    - `grep -n "COST_TAIL_BYTES" src/main/hive.ts` output shows no remaining use inside `taskSpend`.
    - `grep -c "recordCostSample" src/main/hooks.ts` returns at least `1`.
    - Three commits exist with the messages `fix(cost): unify ledger row semantics`, `fix(cost): sum over all rows`, `feat(cost): wire recordCostSample`, and **none of their diffs touches a file under `test/`** — verify with `git show --stat` on each and paste the output.
    - `npm run typecheck` exit code is `0`.
    - `node --test test/engine-parity.test.cjs` exit code is `0` — the six existing `recordCostSample` calls and the garbage-sample test still pass unchanged.
    - `node --test test/hive-protocol-v2.test.cjs` is expected to FAIL at this point on the `spend.tokens` assertion. Paste that failure — it is the evidence that the source contract actually changed, and task 3 is what resolves it.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>One row semantics, clamped-diff spend over all rows with a lifetime-bounded accumulator and a startup rescan, proxy-tier cost reaching `getAgentUsage` — in three source-only commits, with the old test's failure pasted as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the test that encoded the defect, and add the ledger-larger-than-1 MB and proxy-tier-breaker tests</name>
  <files>test/hive-protocol-v2.test.cjs, test/engine-parity.test.cjs</files>
  <read_first>
    - `test/hive-protocol-v2.test.cjs:250-290` — the WHOLE test being rewritten, plus the `floor(t)` harness at `:31-37`.
    - `test/engine-parity.test.cjs:40-100` — `makeBreaker` at `:46-51`, the six `recordCostSample` calls at `:58-98`, and the two-beat pattern at `:70-83`.
    - `01-PATTERNS.md` § "The test to rewrite (D-23)" `:892-918` and § "⚠ The breaker test is a different animal" `:814-865`.
    - `01-CONTEXT.md` D-23 — the commit-message requirement.
  </read_first>
  <action>
    Rewrite the whole test at `test/hive-protocol-v2.test.cjs:256-284` — not the line range — to assert
    the corrected arithmetic. Its two `appendCostLedger` calls are now cumulative snapshots, so the
    card's spend is the clamped diff, not `10+5+4+1 = 20`. **The commit message must quote the old
    expectation `assert.equal(spend.tokens, 20)` verbatim and state that it encoded the defect
    RECORD-04 removes.** This is a deliberate, documented contract change, and the commit message is
    what distinguishes it from bending a test so a buggy source path passes.

    Add a RECORD-03 test: write a cost ledger larger than 1 MB for one card, with real spend in the
    early rows that the old tail would have dropped, and assert `taskSpend()` still counts it and
    `over` reads true when it is true. Use the real-tempdir `floor(t)` harness so the ledger is a real
    file — never mock the thing under test.

    Add a restart test: build an accumulator, simulate a restart by constructing a fresh manager over
    the same home, and assert the in-flight card's spend is rebuilt from the ledger rather than
    resetting to zero.

    Add the FLOOR-09 test to `test/engine-parity.test.cjs`: a proxy-tier `CostSample` (qwen or crush)
    reaches `getAgentUsage` and can trip the breaker. Follow the existing two-beat pattern at
    `:70-83` — tick once for the baseline, tick again after the sample. **Do not write this test in
    any hand-rolled harness.** Eight of the repo's 56 test files define their own local `test()` and
    have no `require('node:test')`; a `node:test` registration added to one of them is shadowed by the
    local function and killed by the file's own `process.exit` before it runs — a green run that
    asserted nothing. `test/breaker.test.cjs` is the one nearest this work (`test()` at `:34`,
    `process.exit(failures ? 1 : 0)` at `:224`), but the same trap sits in all eight. The full set,
    produced by
    `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done`
    is: `agent-provider`, `breaker`, `kg-core`, `proc-kill`, `realtime-findcard`, `slack`,
    `transcript-usage`, `voice-messages` (all `test/*.test.cjs`). Write the FLOOR-09 test in
    `test/engine-parity.test.cjs`, which is a real `node:test` file.

    Every assertion carries a message stating what a failure would MEAN.
  </action>
  <acceptance_criteria>
    - `node --test test/hive-protocol-v2.test.cjs` exit code is `0`.
    - `node --test test/engine-parity.test.cjs` exit code is `0`, with a test count at least 1 higher than the task 1 baseline.
    - `grep -c "assert.equal(spend.tokens, 20)" test/hive-protocol-v2.test.cjs` returns `0`.
    - `git log -1 --format=%B` for the test-rewrite commit contains the literal string `spend.tokens, 20` — the old expectation quoted per D-23.
    - The hand-rolled set is unchanged — **a preservation criterion**, not a completion criterion. Run:
      `for f in test/*.test.cjs; do grep -qE "require\('node:test'\)" "$f" || echo "$f"; done | wc -l`
      It prints `8`, today's measured baseline, confirming no `node:test` registration was added to any hand-rolled harness and none of them was silently converted. Paste the list, not just the count; it must still be exactly `agent-provider`, `breaker`, `kg-core`, `proc-kill`, `realtime-findcard`, `slack`, `transcript-usage`, `voice-messages`.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-protocol-v2.test.cjs</automated>
  </verify>
  <done>The defect-encoding test is rewritten with its old expectation recorded in the commit message, a >1 MB ledger and a restart are both covered, and proxy-tier spend is proven to reach the breaker.</done>
</task>

<task type="auto">
  <name>Task 4: Close GATE-01's qwen sidecar hole — thread the per-agent token into startProxyBridge's env (handed over from plan 02)</name>
  <files>
    - src/main/hive.ts
    - test/engine-parity.test.cjs
  </files>
  <action>
    **Why this task lives here and not in plan 02.** Plan 02 (wave 2) replaces the floor-wide hook
    secret with per-agent identity, and to do it deletes `src/main/index.ts:5534`
    (`process.env.HIVE_SOCK_TOKEN = hookSockToken();`). The qwen proxy-bridge sidecar is spawned by
    `startProxyBridge(agentId, cfg)` at `src/main/hive.ts:1170-1223` (called only from `:930`), whose
    `env:` literal at `:1183-1192` spreads `...process.env` and names `HIVE_SOCK`, `AGENT_ID`,
    `UPSTREAM_BASE_URL`, `HIVE_PROXY_SESSION`, `HIVE_PROXY_API` — and **no `HIVE_SOCK_TOKEN`**. It
    lives entirely on that deleted assignment, as `index.ts:5525-5533` says in as many words. So from
    the end of wave 2 until this task lands, every qwen/crush agent posts `sock_token: ''` and is
    dropped by `authorized()` — dead-hooked, silently: no live status, no Stop→drain, no cost rows.

    Plan 02 could not make this edit (its wave's `hive.ts` owner is plan 03) and plan 03 could not
    either — the edit references the per-agent token accessor plan 02 creates *in the same wave*, so
    whichever landed first would leave the tree failing `npm run typecheck`. This plan is wave 3, owns
    `hive.ts` and `hooks.ts`, and runs after plan 02's mint exists. Read `01-02-SUMMARY.md` first: it
    records the accessor's real name and signature, which is what this task calls.

    **The change.** Thread the minted per-agent token through `startProxyBridge`'s `cfg` and add
    `HIVE_SOCK_TOKEN: cfg.token` (or whatever plan 02's SUMMARY names it) to the `env:` literal at
    `:1183-1192`. Two lines. Do **not** write `HIVE_SOCK_TOKEN: process.env.HIVE_SOCK_TOKEN` — that
    reintroduces the floor-wide secret by a longer route and is the specific thing GATE-01 removes.
    Do **not** touch the six shim templates; they already read the var from inherited env.

    Revoke on sidecar exit the same way plan 02 revokes on PTY exit, if its accessor exposes that.

    **If `01-02-SUMMARY.md` shows plan 02 did not land its mint**, stop and report — do not invent an
    accessor and do not restore the floor-wide assignment to make the sidecar work again.
  </action>
  <acceptance_criteria>
    - The sidecar carries a per-agent token. Run and paste:
      `sed -n '/private startProxyBridge/,/^  }$/p' src/main/hive.ts | grep -n "HIVE_SOCK_TOKEN"`
      It must return exactly ONE line, that line must sit inside the `env:` literal, and it must NOT
      read `process.env.HIVE_SOCK_TOKEN`. Baseline today is `0` lines, so this criterion fails against
      current source — that is the point.
    - The floor-wide route did not come back: `grep -c "process.env.HIVE_SOCK_TOKEN *=" src/main/index.ts` returns `0` (plan 02's value — this task must not restore it), and `sed -n '/private startProxyBridge/,/^  }$/p' src/main/hive.ts | grep -c "process.env.HIVE_SOCK_TOKEN"` returns `0`.
    - `npm run typecheck` exit code is `0` — the accessor this task calls actually exists.
    - A test proves the sidecar env carries a token that is NOT the floor-wide one: add it to `test/engine-parity.test.cjs` (a real `node:test` file — do not write it into any of the eight hand-rolled harnesses), asserting on the env object `startProxyBridge` builds. `node --test test/engine-parity.test.cjs` exit code is `0` with a test count at least 1 higher than this task's own pre-edit run.
    - `01-06-SUMMARY.md` states that GATE-01's sidecar hole is closed and names the wave-2→wave-3 window it was open for. Plan 02's SUMMARY opened that window with the literal `QWEN SIDECAR DEAD-HOOKED UNTIL 01-06`; this SUMMARY closes it.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>The qwen proxy sidecar authenticates with its own agent's token, the floor-wide assignment stays deleted, and the one-wave dead-hook window plan 02 opened is closed and recorded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hook socket payload (`CostSample`) → cost ledger | An agent-adjacent sidecar supplies the numbers a budget is enforced against. `authorized()` is the gate and must stay ahead of every cost path. |
| cost ledger file on disk → `taskSpend()` | The ledger is append-only text; anything that can write the hive directory can influence enforcement. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P06-01 | Tampering | forged `CostSample` payloads poisoning the ledger | mitigate | FLOOR-09's rewiring stays behind `hooks.ts`'s `authorized()` gate; no second unauthenticated path into `appendCostLedger` is created. Plan 02's per-agent tokens are what make that gate meaningful. |
| T-P06-02 | Tampering | garbage/negative/NaN usage values rewinding a cumulative accumulator | mitigate | Keep every `?? 0` coercion and the clamp-to-zero on the consecutive diff; `test/engine-parity.test.cjs:85-94` already pins this and must stay green. |
| T-P06-03 | Repudiation | a card's spend silently resetting to zero across an app restart | mitigate | One full ledger scan at startup rebuilds the per-card accumulator (D-20). |
| T-P06-04 | Denial of service | the per-card accumulator growing without bound now that the 1 MB tail is gone | mitigate | Bound the `Map<taskId, tokens>` by card lifetime — drop entries for archived or deleted cards. RECORD-03 deliberately removes a bound and this is what replaces it. |
| T-P06-05 | Denial of service | a full ledger scan at every startup on a large ledger | accept | One scan at boot, on a file already bounded by `LOG_ROTATE_BYTES`. If it ever measurably delays boot, the `cost_ledger` table in `PersistStore` is a one-query alternative — recorded, not built. |
| T-P06-06 | Repudiation | changing a test so a buggy source path passes | mitigate | D-23's exact sequence is enforced by acceptance criteria: three source-only commits verified with `git show --stat`, then the test rewrite in a separate commit quoting the old expectation. |
</threat_model>

<verification>
- `npm test` green on all three platforms.
- `npm run typecheck` green.
- `git show --stat` output for the three source commits pasted, proving no test file was touched in
  the same commit as the arithmetic change.
</verification>

<success_criteria>
- ROADMAP criterion 3's arithmetic clause is TRUE: spend is summed over **all** of that card's rows
  rather than a 1 MB tail, and derived from the **difference** between cumulative usage snapshots
  rather than their sum — so a card given a small cap and run long is stopped, and a card given a
  generous cap is not stopped early by double counting.
- ROADMAP criterion 3's coverage clause is TRUE: spend on every engine, including the proxy-tier
  ones, reaches the breaker and can trip it.
- Issue #19's spend-reaches-the-breaker clause closes in this PR per D-43, with the empty
  `recordCostSample` production-caller grep pasted as the evidence the clause was open.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-06-SUMMARY.md` when done. It MUST record the written
arithmetic contract with its worked example, whether `COST_TAIL_BYTES` survived for other callers,
the accumulator's lifetime bound, and the `git show --stat` evidence for D-23's commit ordering.
</output>
