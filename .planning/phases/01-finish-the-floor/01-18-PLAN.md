---
phase: 01-finish-the-floor
plan: 18
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/App.tsx
  - src/renderer/src/components/SkillsTab.tsx
  - src/renderer/src/components/MessageQueueComposer.tsx
  - src/renderer/src/components/WorkersTab.tsx
  - src/renderer/src/components/MemoryPanel.tsx
  - src/renderer/src/components/MemoryGraphPanel.tsx
  - src/renderer/src/components/AskMeTab.tsx
  - src/renderer/src/components/UpdateToast.tsx
  - src/renderer/src/components/FileTree.tsx
  - src/renderer/src/components/AgentDetailPanel.tsx
  - src/renderer/src/components/AgentControlStrip.tsx
  - src/renderer/src/components/ToolWaterfall.tsx
  - src/renderer/src/components/QuitWarningModal.tsx
  - src/renderer/src/components/FullscreenFileEditor.tsx
  - src/renderer/src/components/UpdatesSection.tsx
  - src/renderer/src/components/CodeEditor.tsx
  - src/renderer/src/components/ErrorBoundary.tsx
  - src/renderer/src/components/CommandBar.tsx
  - src/renderer/src/components/BlockedBanner.tsx
  - src/renderer/src/components/TerminalView.tsx
  - src/renderer/src/components/RecentText.tsx
  - src/renderer/src/components/RealtimeMichaelToggle.tsx
  - src/renderer/src/components/PtyTerminalView.tsx
  - src/renderer/src/components/UpdateBadge.tsx
  - src/renderer/src/components/SidebarTabs.tsx
  - src/renderer/src/components/ReleaseDrop.tsx
  - src/renderer/src/components/Modal.tsx
  - src/renderer/src/components/MichaelBooting.tsx
  - src/renderer/src/realtime/CostHud.tsx
  - src/renderer/src/realtime/DevicePicker.tsx
  - src/renderer/src/realtime/CompletionToast.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the remaining renderer files sits below the 14px floor except allowlisted decorative glyphs"
    - "Every icon-only button in these files has an accessible name; no button with visible text gained an aria-label"
    - "The components plan 20 will render to static markup are final after this plan"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
  artifacts:
    - path: "src/renderer/src/App.tsx"
      provides: "the shell swept without disturbing the 1024px collapse plan 12 added"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the remaining 31 renderer files to the corrected type scale and give every icon-only button an
accessible name — completing the FLOOR-12 surface.

Purpose: FLOOR-12, group 4 of 4. Measured at plan time: **160 sub-14px `fontSize` sites across 31
files**, led by `SkillsTab.tsx` (16), `MessageQueueComposer.tsx` (16), `WorkersTab.tsx` (10),
`MemoryPanel.tsx` (9), `MemoryGraphPanel.tsx` (9) and `App.tsx` (9). After this plan the renderer's
components are final, which is why plan 19's lint gate and plan 20's static-render tests come next.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract; its rules are mechanical and checked in order.

Output: 31 files clearing the floor, an accessible-name pass, and this group's frozen Rule 0 allowlist
handed to plan 21.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>`

**This group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`AskMeTab.tsx` `✕` and `└`; `FullscreenFileEditor.tsx` `✕`; `PtyTerminalView.tsx` `−` and `+`;
`ReleaseDrop.tsx` `✕`; `UpdateBadge.tsx` `·`; `UpdatesSection.tsx` `•`; `UpdateToast.tsx` `•`.
Each gains `aria-hidden="true"` and keeps its size. **None of the four NOT-exempt cases is in this
group** — all four live in plan 17's files.

**Hoisted shared style objects are NOT exempt — one verified case is in this group:**
`FullscreenFileEditor.tsx:59` `const chip` (`fontSize: 12`) serves `:111` `open in IDE` (text) and
`:112` `✕` (glyph). Sweep `chip` normally, give the `✕` element a **local `fontSize` override**, and
put that override's line in the allowlist. Allowlisting `chip`'s declaration would hold `open in IDE`
below the floor with the test green.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern).

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**Regions that are out of bounds in this group:**
- `App.tsx:222-226` (`vpWidth`), `:440-460` and the sidebar overlay/toggle plan 12 added — sweep their
  `fontSize` values only; do not touch the layout logic, the `aria-expanded` toggle, or add an
  `aria-label` to that toggle (its visible label IS its accessible name).
- `MemoryPanel.tsx`'s scope-warning line plan 10 added — it was written at the corrected size on
  purpose. Do not normalise it back down.
- `AskMeTab.tsx`'s `useHiveTasks` call plan 05 landed — do not disturb it.
- `AgentDetailPanel.tsx:93`'s `disposeTerminal` call — plan 05's audited drop path.
- `components/terminalFontSize.ts` and anything xterm — out of the sweep entirely.
  `PtyTerminalView.tsx` and `TerminalView.tsx` are in scope for their **chrome** only; terminal sizing
  is user-controlled and is not governed by `DESIGN.md:706`.

**Preserve every `eslint-disable-next-line` on its exact target line** — `MemoryGraphPanel.tsx`,
`TerminalView.tsx` and `realtime/CompletionToast.tsx` each carry one of the nine
`react-hooks/exhaustive-deps` suppressions plan 19 turns live. `src/renderer/src/ide/monaco.ts` carries
a `@typescript-eslint/no-explicit-any` disable that plan 19 **deletes** — it is not in this group and
must not be touched here.

**Plan 20 renders some of these to static markup.** `ErrorBoundary.tsx`, `BlockedBanner.tsx`,
`SidebarTabs.tsx`, `ToolWaterfall.tsx` and `UpdateBadge.tsx` are candidates. After this plan they are
final; do not change their DOM structure, only sizes and accessible names.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the 31 files, before editing anything</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - The six largest files in full: `SkillsTab.tsx`, `MessageQueueComposer.tsx`, `WorkersTab.tsx`, `MemoryPanel.tsx`, `MemoryGraphPanel.tsx`, `App.tsx`.
    - `src/renderer/src/components/FullscreenFileEditor.tsx:50-120` specifically — `const chip` and both its consumers.
    - The remaining 25 files at their `fontSize` sites and around every `<button>`.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time (160 total, 31 files):
    SkillsTab 16, MessageQueueComposer 16, WorkersTab 10, MemoryPanel 9, MemoryGraphPanel 9, App 9,
    CostHud 8, AskMeTab 8, UpdateToast 6, FileTree 6, AgentDetailPanel 6, AgentControlStrip 6,
    ToolWaterfall 5, QuitWarningModal 5, FullscreenFileEditor 5, UpdatesSection 4, CodeEditor 4,
    DevicePicker 3, CompletionToast 3, ErrorBoundary 3, CommandBar 3, BlockedBanner 3, TerminalView 2,
    RecentText 2, RealtimeMichaelToggle 2, PtyTerminalView 2, UpdateBadge 1, SidebarTabs 1,
    ReleaseDrop 1, Modal 1, MichaelBooting 1.
    Plans 10 and 12 edited `MemoryPanel.tsx` and `App.tsx` after that measurement, so the current
    numbers are the truth; record any delta.

    Run the M3 glyph predicate scoped to these 31 files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm this group's exempt list and confirm none of the four NOT-exempt cases has moved into it.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. Enumerate every hoisted object's consumers,
    including `FullscreenFileEditor.tsx`'s `chip`.

    Inventory every `<button>` and `<PixelButton>` across the 31 files, marked NAMED or ICON-ONLY.

    Record every `eslint-disable-next-line` line number in the group, and every fixed-height container
    integer.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all 31 files with a total.
    - Task notes contain the pasted M3 output scoped to these files and the confirmed exempt list.
    - Task notes contain the per-file rule classification table and the hoisted-object consumer map, including `FullscreenFileEditor.tsx`'s `chip`.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY and every `eslint-disable-next-line` line number.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the group is pre-classified before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the eight largest files in the group</name>
  <files>src/renderer/src/components/SkillsTab.tsx, src/renderer/src/components/MessageQueueComposer.tsx, src/renderer/src/components/WorkersTab.tsx, src/renderer/src/components/MemoryPanel.tsx, src/renderer/src/components/MemoryGraphPanel.tsx, src/renderer/src/App.tsx, src/renderer/src/realtime/CostHud.tsx, src/renderer/src/components/AskMeTab.tsx</files>
  <read_first>
    - The eight files in full — the files being modified.
    - Task 1's classification table, button inventory and lint-comment list for these files.
    - `src/renderer/src/App.tsx:215-235` and `:440-470` — plan 12's collapse logic and toggle, which are out of bounds beyond their `fontSize` values.
    - `01-UI-SPEC.md` § FLOOR-12's containment rule.
  </read_first>
  <action>
    Execute task 1's classification for these eight files under Rules 0, 1 and 2. Convert hoisted
    objects at their declaration with local overrides for any glyph consumer.

    Apply this group's exempt glyphs that fall in these files (`AskMeTab.tsx` `✕` and `└`): size
    unchanged, `aria-hidden="true"`, allowlist entry recorded in `fontSize`-declaration-line space.

    Add `aria-label` to every ICON-ONLY entry and to none of the NAMED ones. **Do not add an
    `aria-label` to `App.tsx`'s sidebar toggle** — its visible `hide panel` / `show panel` label is its
    accessible name and an `aria-label` would override it.

    Out of bounds: `App.tsx`'s collapse logic and `aria-expanded` toggle behaviour; `MemoryPanel.tsx`'s
    scope-warning line (already at the corrected size); `AskMeTab.tsx`'s `useHiveTasks` call. Sweep
    their sizes, change nothing else.

    Preserve every `eslint-disable-next-line` on its exact target line — `MemoryGraphPanel.tsx` carries
    one.

    Apply containment in order; record every container integer changed with its measured delta; stop and
    report rather than reflowing.
  </action>
  <acceptance_criteria>
    - For each of the eight files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all eight.
    - `grep -c "cth-font-display" <file>` is unchanged for all eight files.
    - `grep -c "aria-expanded" src/renderer/src/App.tsx` is unchanged and the toggle still carries no `aria-label` — paste the toggle's JSX.
    - `grep -c "useHiveTasks" src/renderer/src/components/AskMeTab.tsx` still returns at least `1`.
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/MemoryGraphPanel.tsx` matches task 1's record and the comment is still immediately above its target line.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The eight largest files clear the floor with no family conversion and no disturbance to plans 05, 10 or 12's landed work.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep the remaining 23 files and freeze this group's allowlist</name>
  <files>src/renderer/src/components/UpdateToast.tsx, src/renderer/src/components/FileTree.tsx, src/renderer/src/components/AgentDetailPanel.tsx, src/renderer/src/components/AgentControlStrip.tsx, src/renderer/src/components/ToolWaterfall.tsx, src/renderer/src/components/QuitWarningModal.tsx, src/renderer/src/components/FullscreenFileEditor.tsx, src/renderer/src/components/UpdatesSection.tsx, src/renderer/src/components/CodeEditor.tsx, src/renderer/src/components/ErrorBoundary.tsx, src/renderer/src/components/CommandBar.tsx, src/renderer/src/components/BlockedBanner.tsx, src/renderer/src/components/TerminalView.tsx, src/renderer/src/components/RecentText.tsx, src/renderer/src/components/RealtimeMichaelToggle.tsx, src/renderer/src/components/PtyTerminalView.tsx, src/renderer/src/components/UpdateBadge.tsx, src/renderer/src/components/SidebarTabs.tsx, src/renderer/src/components/ReleaseDrop.tsx, src/renderer/src/components/Modal.tsx, src/renderer/src/components/MichaelBooting.tsx, src/renderer/src/realtime/DevicePicker.tsx, src/renderer/src/realtime/CompletionToast.tsx</files>
  <read_first>
    - The 23 files at their `fontSize` sites and around every `<button>` — the files being modified.
    - Task 1's classification table, hoisted-object map (especially `FullscreenFileEditor.tsx:59` `const chip`) and lint-comment list.
    - `src/renderer/src/components/AgentDetailPanel.tsx:85-100` — the `disposeTerminal` call site to leave alone.
  </read_first>
  <action>
    Execute task 1's classification for the 23 remaining files under Rules 0, 1 and 2.

    `FullscreenFileEditor.tsx:59`'s `const chip` is this group's hoisted-object case: sweep the
    declaration normally, give the `✕` at `:112` a **local `fontSize` override**, and record that
    override's line in the allowlist. Do not allowlist `chip`'s declaration — that would hold
    `open in IDE` at `:111` below the floor with the test green.

    Apply this group's remaining exempt glyphs (`FullscreenFileEditor` `✕`, `PtyTerminalView` `−` and
    `+`, `ReleaseDrop` `✕`, `UpdateBadge` `·`, `UpdatesSection` `•`, `UpdateToast` `•`): size unchanged,
    `aria-hidden="true"`, allowlist entry recorded.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    `PtyTerminalView.tsx` and `TerminalView.tsx`: sweep the **chrome only**. Do not touch
    `terminalFontSize.ts`, xterm configuration, or any size the user controls.

    `AgentDetailPanel.tsx:93`'s `disposeTerminal` call is out of bounds.

    `ErrorBoundary.tsx`, `BlockedBanner.tsx`, `SidebarTabs.tsx`, `ToolWaterfall.tsx` and
    `UpdateBadge.tsx` are plan 20's static-render candidates. Change sizes and accessible names only —
    **do not change their DOM structure**, or plan 20 will be asserting against markup that moved for no
    requirement's reason.

    Preserve every `eslint-disable-next-line` on its exact target line — `TerminalView.tsx` and
    `realtime/CompletionToast.tsx` each carry one.

    Apply containment in order; record every container integer changed with its measured delta.

    Freeze this group's Rule 0 allowlist as a literal `file:line` array covering all 31 group files and
    write it into the SUMMARY. Plan 21 assembles all four groups' arrays into
    `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    Re-run M1 across the **whole renderer** (`src/renderer/src`, both `--include` patterns) and paste
    the total. Combined with the other three groups' SUMMARYs, that number is what plan 21 asserts the
    completeness bar against: `604 − |allowlist|` sites converted, with only allowlisted lines remaining.
  </action>
  <acceptance_criteria>
    - For each of the 23 files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all 23.
    - `grep -n "chip" src/renderer/src/components/FullscreenFileEditor.tsx` output shows the declaration swept to a token and the `✕` carrying its own local `fontSize` — paste both lines.
    - `grep -c "cth-font-display" <file>` is unchanged for all 23 files.
    - `git diff --stat` shows no change to `src/renderer/src/components/terminalFontSize.ts`.
    - `git diff src/renderer/src/components/ErrorBoundary.tsx src/renderer/src/components/BlockedBanner.tsx src/renderer/src/components/SidebarTabs.tsx` contains no added or removed JSX element — only style and attribute changes.
    - Task notes contain this group's frozen allowlist as a literal `file:line` array and the whole-renderer M1 total.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every remaining renderer file clears the floor, the hoisted-object rule is applied at its second worked example, plan 20's candidates keep their DOM shape, and the whole-renderer M1 total is recorded for plan 21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered app shell and panels → the operator's continuous view of the floor | These are the surfaces the operator watches while agents run unattended. Illegible status, cost or blocked-state text is a direct failure of criterion 4's "legible to the operator watching it". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P18-01 | Information disclosure | remaining renderer text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size, and the whole-renderer total is recorded for plan 21's completeness bar. |
| T-P18-02 | Information disclosure | an icon-only control with no accessible name | mitigate | Per-element `aria-label` pass from a pre-built inventory, never a ratio. |
| T-P18-03 | Repudiation | allowlisting `FullscreenFileEditor`'s hoisted `chip` and leaving `open in IDE` below the floor | mitigate | The hoisted declaration is swept and the glyph gets a local override; asserted by a paste of both lines. |
| T-P18-04 | Denial of service | a bumped label clipping a blocked-state banner or a cost readout | mitigate | Containment applied in UI-SPEC's order with measured deltas; "stop and report" rather than reflow. |
| T-P18-05 | Repudiation | a moved `eslint-disable-next-line` detaching a suppression before plan 19's lint gate | mitigate | Two suppression anchors in this group are recorded in task 1 and re-asserted after the edit passes. |
| T-P18-06 | Denial of service | changing user-controlled terminal font sizing | mitigate | `terminalFontSize.ts` and xterm configuration are out of the sweep entirely, asserted by a `git diff --stat` check. |
| T-P18-07 | Repudiation | changing plan 20's candidate components' DOM after their tests are designed | mitigate | Only style and attribute changes are permitted in the five candidates, asserted by a JSX-element diff check. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the app shell, the skills tab, the workers tab, the memory panel, the message-queue
  composer and the file tree in the dev app. Confirm nothing is clipped and the sidebar collapse plan 12
  added still works at both sides of 1024px.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all 31 files in this group, completing the sweep
  surface across plans 14-18.
- Every `<button>` in the group has an accessible name, by the rule rather than by a ratio.
- The renderer's components are final, unblocking plan 19's lint gate and plan 20's static-render tests.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-18-SUMMARY.md` when done. It MUST contain this group's
frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts, the
whole-renderer M1 total, every container integer changed with its delta, and confirmation that both
`eslint-disable-next-line` comments in the group are still on their target lines.
</output>
