---
phase: 01-finish-the-floor
plan: 18
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/GitTab.tsx
  - src/renderer/src/components/triggers/TriggerHistoryTab.tsx
  - src/renderer/src/components/triggers/WebhooksSection.tsx
  - src/renderer/src/components/triggers/SchedulesSection.tsx
  - src/renderer/src/components/triggers/OrgSection.tsx
  - src/renderer/src/components/git/CommitGraph.tsx
  - src/renderer/src/ide/IdePanel.tsx
  - src/renderer/src/ide/GitPanes.tsx
  - src/renderer/src/ide/chrome.ts
  - src/renderer/src/ide/ImagePreview.tsx
  - src/renderer/src/ide/MonacoEditor.tsx
  - src/renderer/src/ide/MonacoDiff.tsx
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the triggers-tab, git and IDE-chrome files sits below the 14px floor except allowlisted decorative glyphs"
    - "The hoisted-shared-object rule is applied at its one verified worked example — the shared declaration is swept and only the glyph gets a local override"
    - "Every icon-only button in these twelve files has an accessible name; no button with visible text gained an aria-label"
    - "All four eslint-disable-next-line suppressions in this set are still on their exact target lines"
  artifacts:
    - path: "src/renderer/src/ide/GitPanes.tsx"
      provides: "the swept `smallBtn` declaration plus the local fontSize override on its ✕ consumer"
    - path: "src/renderer/src/ide/IdePanel.tsx"
      provides: "the swept IDE chrome with its ▸/▾ ternary allowlisted and its suppression anchor intact"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the triggers-tab, git and IDE-chrome files to the corrected type scale, apply the hoisted-object
rule at its one verified worked example, and give every icon-only button in them an accessible name.

Purpose: FLOOR-12, sweep sub-group 4 of 6. Measured at plan time: **68 sub-14px `fontSize` sites across
twelve files** — `IdePanel.tsx` 20, `TriggerHistoryTab.tsx` 14, `GitTab.tsx` 12, `GitPanes.tsx` 6,
`CommitGraph.tsx` 4, `WebhooksSection.tsx` 3, `chrome.ts` 2, `ImagePreview.tsx` 2,
`SchedulesSection.tsx` 2, `OrgSection.tsx` 1, `MonacoEditor.tsx` 1, `MonacoDiff.tsx` 1.

These twelve files were split off from plan 17's original sixteen-file cluster because 177 sites in one
plan exceeded the context budget. **Nothing was dropped in the split** — plan 17 keeps
`CommandCenterPanel.tsx`, `TasksKanban.tsx`, `triggers/ui.tsx` and `IntegrationsRegistry.tsx` (109
sites); this plan takes the remaining twelve (68 sites); 109 + 68 = 177. The two file sets are
disjoint. Plan 17 runs in the same wave and owns all four of the phase's NOT-exempt glyph cases —
**none of them is in this file set**.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract.

Output: twelve files clearing the floor, the hoisted-object rule applied at its worked example, an
accessible-name pass, four lint anchors provably intact, and this sub-group's frozen Rule 0 allowlist
handed to plan 23.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this sub-group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>`

**This sub-group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`ide/GitPanes.tsx` `✕`, `⇄`; `ide/IdePanel.tsx` `{gitCollapsed ? '▸' : '▾'}`. Each gains
`aria-hidden="true"` and keeps its size.

**No NOT-exempt single-character case is in this sub-group** — all four live in plan 17's files. `/` is
dropped from the glyph vocabulary entirely: a `/` match is a JSX self-close, an arrow-function return or
a template-string artifact, never a decorative element.

**The hoisted-shared-object rule and its one verified worked example, which lives here.**
`ide/GitPanes.tsx:35` `const smallBtn` (`fontSize: 11`) serves `:110` `load older…` and `:127` (both
text) and `:130` `✕` (glyph, via spread). Sweep the shared declaration normally, give the glyph element
a **local `fontSize` override** at its own site, and put that override's line in the allowlist.
**Allowlisting the shared declaration would leave `load older…` below the floor with the test green** —
that is the failure this rule exists to prevent. Apply the same rule to any further mixed hoisted object
task 1 finds.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern). Measured at plan time, these twelve files carry 22
`<button>` elements and 8 existing `aria-label`s.

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**Preserve every `eslint-disable-next-line` on its exact target line.** Measured at plan time this set
carries exactly four, one each in `GitTab.tsx`, `triggers/SchedulesSection.tsx`,
`triggers/WebhooksSection.tsx` and `ide/IdePanel.tsx` — four of the nine `react-hooks/exhaustive-deps`
suppressions plan 21 turns live. Moving one detaches it and plan 21's gate goes red for a reason that
has nothing to do with plan 21. `src/renderer/src/ide/monaco.ts` carries a
`@typescript-eslint/no-explicit-any` disable that plan 21 **deletes** — it is **not** in this file set
and must not be touched here.

**Monaco and xterm.** For `MonacoEditor.tsx`, `MonacoDiff.tsx` and `ide/chrome.ts`: sweep only the
chrome's own style objects. Monaco configures its own editor font; do not route Monaco's `fontSize`
option through a CSS token. `components/terminalFontSize.ts` and anything xterm are out of the sweep
entirely.

**Plan 17 owns the other four files of this cluster and runs in the same wave.** Its file set is
disjoint from this one. Do not edit any file outside this plan's `files_modified`, and do not run a
repo-wide fix — a cross-set edit in wave 7 is a lost update, because plans 15, 16, 17, 19 and 20 are
all editing the renderer concurrently in the same working tree.

**Do not re-run M1 across the whole renderer and do not record a whole-renderer total.** Any repo-wide
number taken here is stale before it is written, and a stale number in a SUMMARY is the exact
false-claim class this phase exists to remove. The repo-wide completeness bar is plan 23's, asserted
once after every sweep has landed. Record **this sub-group's** twelve per-file counts and nothing wider.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the twelve files, and map the hoisted objects and lint anchors</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - `src/renderer/src/ide/GitPanes.tsx:30-135` specifically — `const smallBtn` and all three of its consumers.
    - `src/renderer/src/ide/IdePanel.tsx` and `src/renderer/src/components/GitTab.tsx` in full — the two largest files in the set.
    - The remaining nine triggers, git and ide files in full.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time: `IdePanel.tsx` 20,
    `TriggerHistoryTab.tsx` 14, `GitTab.tsx` 12, `GitPanes.tsx` 6, `CommitGraph.tsx` 4,
    `WebhooksSection.tsx` 3, `chrome.ts` 2, `ImagePreview.tsx` 2, `SchedulesSection.tsx` 2,
    `OrgSection.tsx` 1, `MonacoEditor.tsx` 1, `MonacoDiff.tsx` 1 — **68 total**. Record any delta.

    Run the M3 glyph predicate scoped to these twelve files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm the exempt list in `<interfaces>` against current line numbers, which drift. If the predicate
    surfaces a single-character site that is **not** on the exempt list, do not assume decoration —
    read it, and if it is content, keep it visible, sweep it, and give it an accessible name.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. **For every hoisted object, enumerate its
    consumers and mark which need a local override** — start with `GitPanes.tsx:35` `smallBtn` and its
    three consumers at `:110`, `:127` and `:130`.

    Inventory every `<button>` and `<PixelButton>` across the twelve files, marked NAMED or ICON-ONLY.
    Baseline at plan time: 22 `<button>` and 8 existing `aria-label` across the twelve files.

    Record the line number of every `eslint-disable-next-line` in the set. Baseline at plan time:
    exactly four — one each in `GitTab.tsx`, `SchedulesSection.tsx`, `WebhooksSection.tsx` and
    `IdePanel.tsx`, and zero in the other eight files. Record every fixed-height container integer.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all twelve files with a total, and the 68 baseline beside it.
    - Task notes contain the pasted M3 output scoped to these files, and the exempt split matching UI-SPEC or an explicit recorded delta with each surprise site read and classified.
    - Task notes contain the per-file rule classification table and the hoisted-object consumer map, including `GitPanes.tsx`'s `smallBtn` with all three consumers named.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY against the 22/8 baseline.
    - Task notes contain the four `eslint-disable-next-line` line numbers with the rule each suppresses and the line each targets.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the twelve files is pre-classified against current source rather than taken from a table.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the twelve files, apply the hoisted-object rule at GitPanes, and freeze this sub-group's allowlist</name>
  <files>src/renderer/src/components/GitTab.tsx, src/renderer/src/components/triggers/TriggerHistoryTab.tsx, src/renderer/src/components/triggers/WebhooksSection.tsx, src/renderer/src/components/triggers/SchedulesSection.tsx, src/renderer/src/components/triggers/OrgSection.tsx, src/renderer/src/components/git/CommitGraph.tsx, src/renderer/src/ide/IdePanel.tsx, src/renderer/src/ide/GitPanes.tsx, src/renderer/src/ide/chrome.ts, src/renderer/src/ide/ImagePreview.tsx, src/renderer/src/ide/MonacoEditor.tsx, src/renderer/src/ide/MonacoDiff.tsx</files>
  <read_first>
    - The twelve files in full — the files being modified.
    - Task 1's classification table, hoisted-object map, button inventory and the four lint-anchor line numbers.
  </read_first>
  <action>
    Execute task 1's classification for all twelve files under Rules 0, 1 and 2.

    `ide/GitPanes.tsx:35`'s `const smallBtn` is the worked example of the hoisted-object rule: it serves
    two text buttons and one `✕` glyph. Sweep `smallBtn` itself normally, then give the `✕` element a
    **local `fontSize` override** at its own site and record that override's line in the allowlist. Do
    not allowlist `smallBtn`'s declaration — doing so would hold `load older…` below the floor with the
    test green. Apply the same treatment to any further mixed hoisted object task 1 found.

    Apply Rule 0 to this sub-group's exempt glyphs (`GitPanes` `✕` and `⇄`, `IdePanel`'s `▸`/`▾`
    ternary): size unchanged, `aria-hidden="true"`, allowlist entry recorded in
    `fontSize`-declaration-line space.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    For `MonacoEditor.tsx`, `MonacoDiff.tsx` and `ide/chrome.ts`: sweep only the chrome's own style
    objects. Do not route Monaco's `fontSize` option through a CSS token, and do not touch anything
    xterm.

    Preserve every `eslint-disable-next-line` on its exact target line — `GitTab.tsx`,
    `SchedulesSection.tsx`, `WebhooksSection.tsx` and `IdePanel.tsx` each carry one. Do not touch
    `src/renderer/src/ide/monaco.ts`; it is not in this file set.

    Apply containment in order; record every container integer changed with its measured delta.

    Freeze this sub-group's Rule 0 allowlist as a literal `file:line` array covering all twelve files
    and write it into the SUMMARY. Plan 23 assembles all seven sweep plans' arrays (01-14 through
    01-20) into `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    Re-run M1 across the twelve files and paste the final counts for plan 23.
  </action>
  <acceptance_criteria>
    - For each of the twelve files, `grep -cE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` returns exactly that file's allowlist size. Paste all twelve beside their 20 / 14 / 12 / 6 / 4 / 3 / 2 / 2 / 2 / 1 / 1 / 1 baselines.
    - `grep -n "smallBtn" src/renderer/src/ide/GitPanes.tsx` output shows the declaration swept to a token, and the `✕` element carries its own local `fontSize` — paste both, plus the `load older…` line proving it is now at or above 14px.
    - `grep -c "cth-font-display" <file>` returns the same value as before this task for all twelve files — paste both numbers per file.
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/GitTab.tsx src/renderer/src/components/triggers/SchedulesSection.tsx src/renderer/src/components/triggers/WebhooksSection.tsx src/renderer/src/ide/IdePanel.tsx` returns `1` for each — the baseline task 1 recorded — and each is still on the exact line task 1 recorded, immediately above its target. Paste each with the line following it.
    - Every allowlisted line carries `aria-hidden="true"`: paste each allowlisted line with the two lines following it.
    - `git diff --stat` for this plan shows changes ONLY to the twelve files in `files_modified` — no file from plan 17's set, nothing under `src/renderer/src/ide/monaco.ts`, no change to `src/renderer/src/components/terminalFontSize.ts`, and no repo-wide fix.
    - Task notes contain this sub-group's frozen allowlist as a literal `file:line` array and the re-run M1 counts for the twelve files, with **no whole-renderer total**.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm test</automated>
  </verify>
  <done>All twelve files clear the floor, the hoisted-object rule is applied correctly at its worked example, every icon-only control is named, all four lint anchors are provably intact, and the sub-group's allowlist is frozen for plan 23.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IDE and git panes → the operator's view of what changed | Diff and git chrome that cannot be read is how an unwanted change ships. |
| triggers tabs → the operator's record of what fired and when | Unreadable trigger history is an audit surface the operator cannot audit. |
| this plan's file set → plan 17's file set, same wave, one working tree | Two agents sweep the renderer concurrently. A cross-set edit is a lost update with no test that catches it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P18-01 | Information disclosure | git, diff and trigger-history text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size, pasted beside its baseline. |
| T-P18-02 | Tampering | allowlisting `GitPanes.tsx`'s hoisted `smallBtn` and leaving `load older…` below the floor with the test green | mitigate | The hoisted declaration is swept and only the glyph gets a local override; asserted by a paste of the declaration, the override and the `load older…` line. |
| T-P18-03 | Repudiation | a moved `eslint-disable-next-line` detaching a suppression before plan 21's lint gate | mitigate | All four anchors recorded in task 1 with their target lines and re-asserted after the edit, each pasted with the line it targets. |
| T-P18-04 | Information disclosure | an icon-only IDE or git control with no accessible name | mitigate | The rule, not a ratio: task 1's NAMED / ICON-ONLY inventory against the 22/8 baseline drives the pass. |
| T-P18-05 | Tampering | routing Monaco's own editor font through a CSS token and breaking the editor's metrics | mitigate | Explicitly out of scope; only the chrome's style objects are swept. |
| T-P18-06 | Tampering | editing a file that belongs to plan 17, 15, 16, 19 or 20 in the same wave, or `ide/monaco.ts` which plan 21 owns | mitigate | `git diff --stat` must show only this plan's twelve files. |
| T-P18-07 | Repudiation | recording a whole-renderer M1 total that five concurrent plans invalidate | mitigate | Explicitly forbidden here; the repo-wide bar is plan 23's, asserted once after every sweep has landed. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the git tab, the triggers tabs and the IDE panel (with a diff open) in the dev app.
  Confirm nothing is clipped, `load older…` is legible, and the Monaco editor's own font is unchanged.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all twelve files in this sub-group.
- The hoisted-object rule is demonstrated correct at `GitPanes.tsx`'s `smallBtn`.
- Every icon-only `<button>` in these twelve files has an accessible name, by the rule rather than by a ratio.
- All four `eslint-disable-next-line` suppressions are still on their target lines.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-18-SUMMARY.md` when done. It MUST contain this
sub-group's frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts beside
their baselines, `GitPanes.tsx`'s swept `smallBtn` and its glyph override pasted, every container
integer changed with its delta, and confirmation that all four `eslint-disable-next-line` comments are
still on their target lines. It MUST NOT contain a whole-renderer M1 total.
</output>
