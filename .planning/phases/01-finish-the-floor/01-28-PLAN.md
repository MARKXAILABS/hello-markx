---
phase: 01-finish-the-floor
plan: 28
type: execute
wave: 2
depends_on:
  - "01-27"
gap_closure: true
files_modified:
  - src/renderer/src/store/store.ts
  - src/renderer/src/components/MessageQueueComposer.tsx
  - src/renderer/src/hooks/useHive.ts
  - src/main/delivery.ts
  - src/preload/index.ts
  - test/renderer-queue.test.cjs
  - test/delivery-main.test.cjs
autonomous: true
requirements: [FLOOR-02, FLOOR-14]

must_haves:
  truths:
    - "When main declines a queued message the operator's text survives AND the refusal reason is in the rendered markup, with an empty queue as well as a full one"
    - "`enqueueMessage` resolves main's result, so every one of its seven call sites can tell acceptance from refusal"
    - "A SYNTHESIZED turn-end (main's quiesce backstop) never idles an agent the floor calls blocked; a REAL turn-end from the harness still does, so a falsely-blocked agent still recovers its status on its next real turn-end"
    - "No comment in useHive.ts describes any renderer-side drain as the live delivery path — checked on the CLAIM, not on the label `effect #4`"
    - "roster.json stops re-publishing the pre-FLOOR-02 queue slice forever, so a Change Home cannot re-type already-delivered messages into live terminals"
  artifacts:
    - path: "src/renderer/src/store/store.ts"
      provides: "queueOp returning main's QueueResult through setQueues, a per-agent refusal slice, and a one-shot roster queue publication"
      contains: "queueError"
    - path: "src/renderer/src/components/MessageQueueComposer.tsx"
      provides: "a composer that clears only after main accepts and renders the refusal at any queue length"
      contains: "await enqueueMessage"
    - path: "src/renderer/src/hooks/useHive.ts"
      provides: "an exported pure decision for the Stop arm, called from the effect, plus three corrected comments"
      contains: "stopArmDecision"
    - path: "src/main/delivery.ts"
      provides: "the quiesce emit marked as synthesized, so the renderer can tell main's backstop from the harness's own turn-end"
      contains: "synthesized: true"
    - path: "src/preload/index.ts"
      provides: "the hook-event payload type carrying the new discriminator"
      contains: "synthesized"
    - path: "test/renderer-queue.test.cjs"
      provides: "store-level refusal behaviour, a rendered-markup assertion on the reason, the four stopArmDecision cases, and the one-shot roster publication"
      contains: "renderToStaticMarkup"
    - path: "test/delivery-main.test.cjs"
      provides: "the quiesce emit's discriminator asserted where the emit lives"
      contains: "synthesized"
  key_links:
    - from: "src/renderer/src/components/MessageQueueComposer.tsx"
      to: "main's QueueResult"
      via: "queueIt — await, then clear only when ok"
      pattern: "await enqueueMessage"
    - from: "src/renderer/src/hooks/useHive.ts"
      to: "the event's source"
      via: "effect 2's Stop arm — stopArmDecision reads e.synthesized, not the agent's status alone"
      pattern: "e\.synthesized"
    - from: "src/renderer/src/components/MessageQueueComposer.tsx"
      to: "the store's refusal slice"
      via: "statusHint — a rendered line seeded from the server snapshot"
      pattern: "queueError"
---

<objective>
Stop the composer throwing the operator's message away before main has accepted it; stop main's
silence-based backstop erasing the one cue that an agent needs a human; and stop `roster.json`
re-publishing a queue slice the renderer stopped owning at FLOOR-02.

Purpose: `01-VERIFICATION.md` gap SC-4 and code-review `b/CR-01`, `b/CR-02`, `b/CR-03`, plus
`c/WR-08`'s three stale source comments. All three defects are the same root shape: **a fact moved to
main, and the renderer kept a half of it that no longer matches.**

Output: three behavioural fixes with one new test file, one assertion added to an existing main-side
test, and three comment corrections in a file this plan already owns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-VERIFICATION.md
@.planning/phases/01-finish-the-floor/01-REVIEW-b-renderer.md
@.planning/phases/01-finish-the-floor/01-REVIEW-c-tests.md
@.planning/phases/01-finish-the-floor/01-REDTEAM-5-regression.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-27-SUMMARY.md
@src/renderer/src/store/store.ts
@src/renderer/src/components/MessageQueueComposer.tsx
@src/renderer/src/hooks/useHive.ts

## Coordination — two files this plan borrows from plan 01-27

`src/main/delivery.ts` and `test/delivery-main.test.cjs` are in **01-27's** `files_modified`. They are
in this plan's too, and that is deliberate, not an oversight:

- The one-key fix for `b/CR-02` (below) lives on **main's emit**, because the renderer provably cannot
  tell main's synthesized Stop from Claude Code's real one — the payloads are byte-equivalent.
  Measured at HEAD, `src/main/delivery.ts:671` sends
  `{ agentId, event: 'Stop', blocked: false }` and `src/main/hooks.ts:841-850` sends the same three
  keys with `tool`, `notificationType`, `source` and `message` all `undefined` for a real Stop.
- **`wave: 2` and `depends_on: ["01-27"]` serialize the two plans**, so at no point do both hold the
  file. `use_worktrees: false` makes concurrency the only real hazard and the wave graph removes it.
- The regions do not overlap either. 01-27 edits `loadQueue` (`:371-384` at HEAD) and the budget band
  in `breaker.ts`; this plan edits `quiesce`'s emit line and appends one assertion to the existing
  quiesce test. **Re-derive both line numbers after 01-27 lands** — 01-27 inserts above them.

If 01-27's landed shape makes either edit impossible, STOP and report rather than improvising: the
alternative (a renderer-side heuristic on a byte-equivalent payload) is the defect, not the fix.

<interfaces>
<!-- EVERY line below was re-derived by running the command shown, in this session, at HEAD a217018.
     Nothing here is copied from round 1. Round 1's `<interfaces>` block asserted a `window` stub
     "VERIFIED by probe at 47a48cd"; that stub does not exist and the claim has been deleted. -->

### The test harness, measured rather than asserted

```
$ grep -rn "global.window\|globalThis.window\|global\.localStorage" test/
(no output — exit 1. There is NO window stub anywhere in test/.)

$ grep -rn "store/store" test/
test/renderer-components.test.cjs:113:  ({ useStore } = loadTs('src/renderer/src/store/store.ts'));
```

`test/renderer-runstate.test.cjs` **does not load `store.ts` and installs no stub** — round 1 named it
as "the harness idiom to copy" and it is not. The one file that loads `store.ts` is
`test/renderer-components.test.cjs`, whose harness is (`:96-122`, read this session):

- `Module._load` intercepted for exactly two request shapes: anything ending `.css` returns `{}`, and
  anything starting `@/` is resolved to `src/renderer/src/…` and handed to `loadTs`. Everything else,
  `react` and `react-dom` included, passes through to the real module.
- `globalThis.self = globalThis` — `@xterm/addon-fit`'s UMD header reads `self` at module scope.
- Both restored in a `finally`, so the shims exist for the LOAD only.
- `seedServerSnapshot(t, patch)` (`:155-161`) mutates `useStore.getInitialState()` and restores it in
  `t.after`. This is the ONLY seam a server render reads: `renderToStaticMarkup` takes zustand's third
  argument, so `useStore.setState(...)` is invisible to it. That file measured and documented this on
  2026-08-21; do not re-litigate it.

**`store.ts` needs no `window` to load.** Every `window` access in it is inside a `try/catch`
(`:341`, `:392`, `:405`, `:421`, `:452`, `:485`, `:523`, `:540`, `:550`, `:567`, `:575`, `:583`,
`:873`, `:877` — read this session). That is why no stub exists. But a test that needs to SCRIPT
`window.cth.hiveQueue` must install `globalThis.window` itself and restore it in `t.after` — **there
is no existing idiom for that in this repo; you are writing the first one.** Say so in the SUMMARY
rather than implying you copied one.

For `useHive.ts`, the idiom DOES exist and is proven: `test/pty-sanitize.test.cjs:11-19` proxies `@/…`
and `react` through `Module._load`, loads the real `useHive.ts`, and tests the exported pure function
`sanitizePtyText` — *"the guard is asserted where it actually ships, not against a copy"*. `useHive.ts`
already exports exactly two symbols (`grep -n "^export" src/renderer/src/hooks/useHive.ts` →
`:114 sanitizePtyText`, `:323 useHive`). A third pure export is the same shape, not a new one.

### Probe run this session — the composer DOES render, and the store IS the draft

Round 1 claimed *"There is no draft store for this composer … so a cleared textarea is the message
gone"* and *"no test here can press Enter and observe the textarea."* The first half is **false** and
the second half made "the reason is on screen" checkable by nothing.

`MessageQueueComposer.tsx:37-39` (read this session):
```
const text = useStore((s) => s.drafts[agent.id] ?? '');
const setDraft = useStore((s) => s.setDraft);
const setText = (t: string) => setDraft(agent.id, t);
```
The draft is store state (`store.ts:241 drafts: Record<string,string>`, `:790-792`), so it is
seedable through `getInitialState()` and it RENDERS. Probe, run this session under the
`renderer-components` harness shape:

```
seeded drafts:{a1:'do not lose me'}, messageQueues:{a1:[one message]}
  RENDER OK, length = 3942
  visible: QUEUE 1 sending to Ada one-by-one… 1. hi do not lose me files send
  has draft text: true

seeded drafts:{a1:'do not lose me'}, messageQueues:{}          ← empty queue
  RENDER OK, length = 2211
  visible: QUEUE do not lose me files send
  has draft text: true                       ← and NO status line at all
```

Two consequences, both load-bearing:
1. "the operator's text is still in the textarea" and "the reason is on screen" are BOTH assertable on
   real markup. Assert them.
2. `statusHint` (`:168`) begins `queue.length === 0 ? null : …`, so **a refusal on an empty queue
   renders nothing today** — and a refused first message is exactly the empty-queue case. The refusal
   line must not be gated on queue length.

### Main's refusals — FIVE, not four

`sed -n '433,447p' src/main/delivery.ts`, read this session. Round 1 said "four, verified at
`delivery.ts:437-445`"; the stated range excludes the first and the last:

| line | returned `error` |
|---|---|
| 436 | `invalid agentId` |
| 437 | `empty message` |
| 441 | `unknown agent: ${agentId}` |
| 444 | `no harness home — nowhere durable to park this` |
| 446 | `queue full for ${agentId} (${MAX_QUEUED_PER_AGENT})` |

`MAX_QUEUED_PER_AGENT = 200` (`src/shared/queueDelivery.ts:73`) — round 1's example said 25.
`QueueResult` is `{ ok: boolean; error?: string; id?: string; queues?: QueueSnapshot }`
(`src/main/delivery.ts:60-65`); it is exported from a MAIN module, so the renderer store should
declare its own narrow local shape rather than importing across the boundary.

**Cross-plan hazard (`01-REDTEAM-5` R-25 × this plan).** 01-27 makes `enqueue` return
`'no harness home — nowhere durable to park this'` on a transient FS error too. Rendering that verbatim
tells an operator mid-virus-scan that their harness home is gone. Attribute the string to main rather
than adopting it as the app's own diagnosis (see task 1).

### `enqueueMessage` — SEVEN call sites

`grep -rn "enqueueMessage" src/`, run this session. Round 1 named two, one of them wrong, and its
`<verification>` said "three call sites":

```
src/renderer/src/components/MessageQueueComposer.tsx:146   ← the site being changed
src/renderer/src/hooks/useHive.ts:668    (terminal work order)
src/renderer/src/hooks/useHive.ts:672
src/renderer/src/hooks/useHive.ts:895    (Slack ingress — round 1 said :891)
src/renderer/src/hooks/useHive.ts:914    (non-Claude enqueue)
src/renderer/src/hooks/useHive.ts:972
src/renderer/src/hooks/useHive.ts:1033
```
All seven are inside `files_modified`. Also `store.ts:822-823` is `const trimmed = text.trim(); if
(!trimmed) return;` — a bare `return` that becomes a type error the moment the signature widens.

### The Stop arm and its two producers

`sed -n '525,535p' src/renderer/src/hooks/useHive.ts`, read this session:
```
} else if (e.event === 'Stop' || e.event === 'SubagentStop') {
  if (e.blocked) {
    if (!breakerArmed) updateAgent(e.agentId, { status: 'working', action: 'reading inbox', carrying: undefined });
  } else {
    breakerLevel.current[e.agentId] = 'healthy';          // :533
    updateAgent(e.agentId, { status: 'idle', action: 'idle', carrying: undefined });
  }
}
```
- `:533` is the **only** writer of `'healthy'`: `grep -n "breakerLevel.current\[" useHive.ts` →
  `:500` (read), `:533` (this clear), `:581` (the breaker channel setting a level). Every other status
  arm is gated `if (!breakerArmed)`, so removing this clear unconditionally would strand a
  constrained/stopped agent permanently (`01-REDTEAM-5` R-22).
- `useHive.ts`'s `onHiveDelivered` subscriber — `if (!self || self.status === 'blocked') return;
  // never talk over a prompt`. **`R-21`'s stated consequence is FALSE and this plan does not repeat
  it.** Read live this session: that guard sits in the SUBSCRIBER, so it only skips the renderer's
  status paint AFTER main has already delivered. Mail delivery is main's `DeliveryService.drainQueue`,
  whose gate list is `switching` / `vetoed` / `bootGrace` / `idleMs` — **no status check anywhere**
  (`setStatus` is wired to a hive log line and nothing else). So a stuck-`blocked` agent DOES keep
  receiving mail; what it loses is the `reading inbox` paint, the `!` clearing and the seed-prompt
  retry. That is still a real defect and still the reason the guard keys on the event SOURCE — but
  state it at its true size.
- `usePtyParser.ts:31-37` `BLOCK_HINTS` includes `/\(y\/n\)/i` and `/\[y\/n\]/i` matched against the
  terminal tail, so any agent that echoes `(y/n)` is falsely marked blocked. Today it recovers at its
  next real Stop; a status-only guard would remove that recovery. This is why the guard keys on the
  event's SOURCE and not on the agent's status alone.
- `updateAgent` merges, and the idle write never clears `blockReason` — so a normally-idled agent
  already carries a stale `blockReason` at HEAD (`R-23`). **Do not assert "keeps its blockReason" as
  the contract**; it is a pre-existing leak, and pinning it would make a later legitimate fix look
  like a regression.

### The three false comments

`grep -n "effect #4" src/renderer/src/hooks/useHive.ts`, run this session:
```
752:  //         cooldown so effect #4 does not type on top of it. (#4's idle gate
873:  //    into the composer — effect #4 above then drains it to his PTY.
908:  //     task text here so effect #4 types it into the REPL when the agent idles.
```
**Line 766 does not contain the string** — the tombstone reads
`// 4) THE QUEUE AND ITS DRAIN ARE MAIN'S NOW (#5 / FLOOR-02).` Round 1's `<done>` demanded the grep
return "only the tombstone at :766", which is unsatisfiable. After the fix it returns **zero**.
The live writer is `drainQueue` (`src/main/delivery.ts:518`) riding the tick.

### `b/CR-03` — the orphan Critical, taken here

Round 1 declined it while owning the file. Re-derived this session:

```
src/renderer/src/store/store.ts:535   function persistQueues(...)   ← grep -rn "persistQueues" src/
                                        returns ONE line: its own definition. Dead.
src/renderer/src/store/store.ts:600   rosterMirror.queues = initialQueues;
src/renderer/src/store/store.ts:366-378 flushRosterNow() writes the WHOLE mirror, queues included,
                                        to roster.json on a 500 ms debounce, forever.
src/main/index.ts:3821  changeHome copies ['hive','palace','roster.json','roster-backups'].
                        `delivery-queue.json` lives at the home ROOT (index.ts:491) and is NOT copied.
src/main/index.ts:4044  adoptRendererQueues: `if (existsSync(join(home,'delivery-queue.json'))) return;`
                        then re-enqueues every message in roster.json's `queues`.
```
So after a Change Home the guard passes, and the frozen pre-migration slice is re-enqueued into live
terminals — every time, forever. The slice is frozen because `persistQueues` is dead: nothing ever
prunes a delivered message out of it.

`RosterSnapshot.queues` is a REQUIRED field (`src/main/roster.ts:38`), so the slice cannot simply be
dropped from the write — it becomes `{}`. And it cannot be blanked at boot either: on a machine
upgrading from a pre-FLOOR-02 build, that slice is the ONLY bridge main has to those messages
(main cannot read localStorage). The fix is therefore a genuine one-shot, not a deletion — see task 3.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The composer clears only after main accepts, and says why when it does not</name>
  <files>src/renderer/src/store/store.ts, src/renderer/src/components/MessageQueueComposer.tsx, test/renderer-queue.test.cjs</files>
  <behavior>
    - `await useStore.getState().enqueueMessage('a1','hi')` with `window.cth.hiveQueue` resolving
      `{ok:false, error:'queue full for a1 (200)'}` resolves to that same object, and the store's
      refusal slice for `a1` now holds that error string.
    - The same call with `{ok:true, queues:{…}}` resolves `ok:true`, `messageQueues` holds the returned
      snapshot, and the refusal slice for `a1` is cleared. **Negative control: acceptance must still
      clear.**
    - `hiveQueue` REJECTING resolves `{ok:false, error:<the rejection text>}` — no unhandled rejection
      and no throw out of a click handler.
    - `window.cth` absent entirely resolves `{ok:false, …}` rather than throwing.
      **Negative control: the existing "no preload (unit tests)" path must stay non-fatal.**
    - `enqueueMessage('a1','   ')` (whitespace only) resolves `{ok:false, error:'empty message'}` —
      the bare `if (!trimmed) return;` inside `enqueueMessage` must not resolve `undefined`.
    - Rendered markup, via `renderToStaticMarkup` with the refusal seeded through
      `useStore.getInitialState()`: the markup CONTAINS the refusal text, **with `messageQueues` empty**
      as well as with one queued message. **Negative control: with no refusal seeded and one queued
      message, the markup still contains `sending to Ada one-by-one…` — the existing statusHint must
      not be displaced.**
    - Rendered markup contains the seeded draft text in both the refused and the accepted case
      (`drafts` is store state, so this is directly observable).
  </behavior>
  <action>
    **Every target in this task is located by SYMBOL or by quoted text, never by a line number.**
    This task inserts into `store.ts` in three separate places and task 3 deletes from it afterwards, so
    every number in this plan's `<interfaces>` block is HEAD evidence, not a seek offset. Where a file
    takes more than one edit, work from the BOTTOM of the file upward.

    **Store.** Change `queueOp` — located by its declaration `function queueOp(op: QueueOp): void {` —
    to RETURN a promise of main's result rather than swallowing it, normalising both failure shapes — a
    rejected promise, and no preload at all — into the same `{ok:false, error}` object main's five
    refusals already use. Apply the success snapshot through `setQueues`, not the raw
    `useStore.setState` it uses today: `setQueues`'s own doc comment (the one opening *"Apply main's
    queue snapshot. FLOOR-02"*) and `useHive.ts`'s single `setQueues` call both declare it the only
    writer, and a third writer that skips the declared gate means any invariant added there silently
    misses this path.

    Widen `enqueueMessage`'s **declaration on the state interface** — the line beginning
    `enqueueMessage: (agentId: string, text: string, meta?:` — from `=> void` to the promise; return the
    result from the **action implementation**, `enqueueMessage: (agentId, text, meta) => {`; and make its
    whitespace early return — `const trimmed = text.trim();` / `if (!trimmed) return;` — resolve
    `{ok:false, error:'empty message'}`, the same string main's `enqueue` returns for an empty message,
    so one refusal has one wording wherever it is produced. Declare the result shape LOCALLY in the store
    (`{ ok: boolean; error?: string; queues?: Record<string, QueuedMessage[]> }`); do not import
    `QueueResult` from `src/main/delivery.ts` across the process boundary.

    Rewrite `queueOp`'s doc block — the comment immediately above `function queueOp(`, whose
    load-bearing sentence is *"Fire-and-forget on purpose."* It justifies fire-and-forget on the grounds
    that *"the next push corrects the view"* — true for the VIEW, false for the MESSAGE: main returns
    `{ok:false}` with no `queues` key on five reachable paths, so nothing renders and nothing is said
    while the text is already gone.

    Add a per-agent refusal slice — `queueError: Record<string, string | undefined>` with a
    `setQueueError` action, in the same shape as `drafts` (its interface line `drafts: Record<string,
    string>;` and its `setDraft` initializer). It must be STORE state,
    not component state: component state is invisible to a server render, and the whole point of this
    task is that the reason becomes observable. `queueOp` sets it on `{ok:false}` and clears it on
    `{ok:true}`.

    **Composer.** In `queueIt` — delimited structurally by `const queueIt` and the next top-level
    `const onKey`; use those delimiters, never a line number or a byte offset — await the result and
    clear `text` and `attachments` ONLY when `ok` is true. Render the refusal through the existing
    `statusHint` surface (the `const statusHint =` binding and the JSX element that renders it) — do not
    add a second surface and do not add a toast — but move the refusal ahead of the `queue.length === 0 ?
    null` branch, because a refused FIRST message is the empty-queue case and it renders nothing today
    (measured: 2211 chars of markup with no status line at all).

    Attribute the string to main rather than adopting it: render it as main's word (e.g.
    `main declined: ${err}`), not as the app's own diagnosis. 01-27 makes `'no harness home — nowhere
    durable to park this'` reachable on a transient FS error, and an operator mid-virus-scan should not
    be told their harness home is gone in the app's own voice (`01-REDTEAM-5` R-25).

    Update `onKey` so the Enter path still works with an async `queueIt`. A floating promise
    is fine — `queueOp` no longer rejects. Confirmed harmless on lint: `eslint.config.js` runs two
    rules with no `project` option, so `no-floating-promises` is not in play.

    **All seven call sites.** One in `MessageQueueComposer.tsx`, six in `useHive.ts`. **Re-derive them
    with `grep -rn "enqueueMessage" src/` and edit each file from the BOTTOM upward** — the HEAD numbers
    in `<interfaces>` are evidence that there are seven, not offsets to seek to; the first edit in
    `useHive.ts` invalidates the other five. All are inside `files_modified`; `npm run typecheck` is the
    backstop but reconcile against SEVEN, not three.

    **Tests first**, in a new `test/renderer-queue.test.cjs`. Harness, honestly: replicate
    `test/renderer-components.test.cjs:96-122`'s `Module._load` interceptor (`.css` → `{}`, `@/…` →
    `loadTs`) plus `globalThis.self = globalThis`, restored in a `finally` — that keeps the blast
    radius at one file, exactly as that file's own header argues. **Additionally install
    `globalThis.window = { cth: { hiveQueue: <scriptable> }, localStorage: <in-memory> }` and restore
    it — no existing test does this, so you are writing the first one; say so rather than implying you
    copied it.** Use `seedServerSnapshot`'s shape (mutate `useStore.getInitialState()`, restore in
    `t.after`) for the render assertions: `useStore.setState` is invisible to `renderToStaticMarkup`.

    Paste the RED output. At HEAD `enqueueMessage` returns `undefined`, so the first assertion cannot
    read `.ok`; and the seeded refusal appears in no markup because the slice does not exist.
  </action>
  <verify>
    <automated>node --test test/renderer-queue.test.cjs</automated>
  </verify>
  <done>
    All six behaviour groups pass including every negative control. Pre-edit measurements, RE-RUN and
    pasted in the SUMMARY, that were RED at HEAD a217018 and must be GREEN after:
    `grep -c "await enqueueMessage" src/renderer/src/components/MessageQueueComposer.tsx` → **0 at
    HEAD**; `grep -c "queueError" src/renderer/src/store/store.ts` → **0 at HEAD**;
    `grep -c "queueError" src/renderer/src/components/MessageQueueComposer.tsx` → **0 at HEAD**;
    `ls test/renderer-queue.test.cjs` → **No such file at HEAD**. `npm run typecheck` clean across all
    seven call sites.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: A synthesized turn-end cannot idle a blocked agent — and three comments stop naming a deleted path</name>
  <files>src/main/delivery.ts, src/preload/index.ts, src/renderer/src/hooks/useHive.ts, test/delivery-main.test.cjs, test/renderer-queue.test.cjs</files>
  <behavior>
    - `stopArmDecision({ status:'blocked' }, { blocked:false, synthesized:true }, false)` returns the
      do-nothing outcome: no patch and no breaker clear.
    - `stopArmDecision({ status:'blocked' }, { blocked:false })` — a REAL turn-end, no discriminator —
      still returns `{status:'idle', action:'idle', carrying:undefined}` AND the breaker clear.
      **Negative control: a falsely-blocked agent that genuinely finishes still recovers its status,
      so the floor stops painting it as needing a human. (`R-21`'s stronger claim — that mail STOPS —
      is false; `drainQueue` has no status gate. Do not restate it.)**
    - `stopArmDecision({ status:'working' }, { blocked:false, synthesized:true }, false)` still idles.
      **Negative control: the backstop's whole purpose survives.**
    - `stopArmDecision({ status:'idle' }, { blocked:false, synthesized:true }, false)` is idempotent.
    - `stopArmDecision({ status:'blocked' }, { blocked:true }, false)` still returns
      `{status:'working', action:'reading inbox', …}`; with `breakerArmed = true` it returns no patch.
      **Negative control: breaker precedence is unchanged.**
    - `delivery-main`: one `svc.tick()` over a silent PTY emits a `hive:hookEvent` whose payload has
      `event:'Stop'`, `blocked:false` **and `synthesized:true`**. **Negative control: the existing
      "flipped idle with NO window attached, once per quiet spell" test stays green — the durable
      `setStatus` half and the once-per-spell arming are untouched.**
    - No assertion anywhere claims `blockReason` survives. It is a pre-existing merge leak (`R-23`),
      not this fix's contract.
  </behavior>
  <action>
    **Main, one key.** In `quiesce` (`src/main/delivery.ts`), add `synthesized: true` to the emitted
    payload. **Locate it by symbol, not by line:** the `quiesce(` method declaration, then the
    `this.deps.emit('hive:hookEvent', { agentId: a.agentId, event: 'Stop', blocked: false })` call
    inside it — 01-27 lands first and moves every number in this file, and task 1 of this plan does not
    touch it, so a number written here would be stale on arrival. Comment it with the reason: the
    renderer cannot otherwise distinguish this from Claude Code's real Stop, because `hooks.ts`'s Stop
    emit sends the same three populated keys and `undefined` for the rest. Widen the `onHiveHookEvent`
    payload type in `src/preload/index.ts` — located by the `onHiveHookEvent` identifier — in **both**
    the callback signature and the listener's, with `synthesized?: boolean`. Widening only one of the
    two satisfies the frontmatter `contains` gate while leaving the boundary half-typed; do both, and
    say in the SUMMARY that you checked both.

    **Renderer, one exported decision.** Extract the Stop arm's choice out of the effect as a pure,
    exported function in `useHive.ts` — `stopArmDecision(self, e, breakerArmed)` returning the patch to
    apply and whether to clear the breaker (or a do-nothing outcome) — and call it from the arm,
    located by its opening line `} else if (e.event === 'Stop' || e.event === 'SubagentStop') {` rather
    than by a line number: task 1 of this plan edits six `enqueueMessage` call sites in this same file.
    Extraction is the point: the guard lives INSIDE the effect, a React hook that cannot be
    driven under `node --test`, so a test that calls `updateAgent` directly would be GREEN before and
    after the change. `sanitizePtyText` (`:114`) is the precedent and `test/pty-sanitize.test.cjs`
    proves the load idiom. **There is no fallback to a source grep in this task. If the extraction is
    blocked, STOP and report — do not downgrade to a structural pin.**

    The rule the function encodes: **a SYNTHESIZED turn-end is not turn-end for an agent the floor
    already calls blocked.** Guard on `e.synthesized`, never on `e.blocked === false` plus the status
    alone. A permission prompt paints a static frame and emits no further bytes, so main's silence
    heuristic cannot see it; but Claude Code's own Stop is a real report and must keep working exactly
    as it does today, including the `breakerLevel.current[e.agentId] = 'healthy'` clear — the only
    clear there is, and removing it for the blocked case would strand a constrained agent forever.

    Read the current status from the store the effect already reads (`useStore.getState()`).
    Add no new subscription and no new ref: a second copy of the status is the "one fact, two owners"
    shape both of this plan's defects come from.

    **Residual, named not papered over.** Main still EMITS for a blocked agent and the renderer drops
    it; main holds no blocked status to filter on (`setStatus` is wired at `src/main/index.ts:533-536`
    to a hive log line and nothing else). Do NOT build a blocked-status channel in main here.

    **The three comments — located by their own text, since task 1 edits above all three.** Find them
    with `grep -n "effect #4" src/renderer/src/hooks/useHive.ts`, which returns exactly three lines: the
    `lastFlush` cooldown comment containing *"cooldown so effect #4 does not type on top of it"*, the
    one reading *"effect #4 above then drains it to his PTY"*, and the one reading *"effect #4 types it
    into the REPL"*. Repoint all three at `drainQueue` in `src/main/delivery.ts` and reference the
    tombstone by its TEXT — `// 4) THE QUEUE AND ITS DRAIN ARE MAIN'S NOW` — never by its line number;
    01-27 and task 1 both shift this file. **Name symbols, not line numbers, in every comment you
    write** — 01-27 lands before this plan and moves `delivery.ts`'s numbers, so a fresh numeric anchor
    would be stale on arrival, in the phase whose criterion 1 is about exactly that.

    Add to `test/renderer-queue.test.cjs` a comment-stripped assertion on the CLAIM rather than on the
    label, so rewording cannot defeat it:
    `assert.doesNotMatch(src, /(renderer|effect #?\d|composer effect)[^.\n]{0,80}(drains?|types?)[^.\n]{0,60}(PTY|REPL|terminal)/i)`.
    Run this session at HEAD it matches TWO — `:873 "effect #4 above then drains it to his PTY"` and
    `:908 "effect #4 types it into the REPL"` — and it also matches the cheapest evasions
    (*"the renderer then drains it to his PTY"*, *"the composer effect types it into the REPL"*).
  </action>
  <verify>
    <automated>node --test test/renderer-queue.test.cjs test/delivery-main.test.cjs</automated>
  </verify>
  <done>
    Every behaviour case above passes including all four negative controls. Pre-edit measurements,
    RE-RUN and pasted, that were RED at HEAD a217018 and must be GREEN after:
    `grep -c "synthesized: true" src/main/delivery.ts` → **0 at HEAD** (`grep -c synthesized` was 1, a
    prose comment at `:639` — the precise string is what is gated);
    `grep -c "synthesized" src/preload/index.ts` → **0 at HEAD**;
    `grep -c "synthesized" test/delivery-main.test.cjs` → **0 at HEAD**;
    `grep -c "stopArmDecision" src/renderer/src/hooks/useHive.ts` → **0 at HEAD**;
    `grep -c "e\.synthesized" src/renderer/src/hooks/useHive.ts` → **0 at HEAD**;
    `grep -c "effect #4" src/renderer/src/hooks/useHive.ts` → **3 at HEAD (`:752`, `:873`, `:908`), and
    ZERO after** — line 766 does not contain the string, so "only the tombstone at :766" was never
    reachable; the claim-shaped regex above → **2 matches at HEAD, 0 after**.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: roster.json publishes the pre-FLOOR-02 queue once, not forever (b/CR-03)</name>
  <files>src/renderer/src/store/store.ts, test/renderer-queue.test.cjs</files>
  <behavior>
    - With a non-empty pre-migration queue loaded at boot, the FIRST `rosterWrite` payload carries those
      messages under `queues`. **Negative control, and the reason this is a one-shot rather than a
      deletion: main cannot read localStorage, so this write is the only bridge
      `adoptRendererQueues` has to pre-FLOOR-02 messages. The migration must still work.**
    - After that write resolves `{ok:true}`, every SUBSEQUENT `rosterWrite` payload carries
      `queues: {}` — so a later Change Home cannot re-adopt them.
    - A write that resolves `{ok:false}` or `{skipped:'empty-first-write'}` does NOT consume the
      one-shot; the next flush still carries the messages. **Negative control: a failed write must not
      destroy the only copy.**
    - With no pre-migration queue (the normal case), every write carries `queues: {}` and nothing else
      changes. `agents`, `archived`, `restorable` and `selectedId` are byte-identical to today in every
      case above. **Negative control: the roster mirror's other four slices are untouched.**
  </behavior>
  <action>
    **Task 1 has already inserted into this file by the time this task runs, so every anchor below is
    a SYMBOL.** The module-level statement `rosterMirror.queues = initialQueues;` seeds the slice and
    `flushRosterNow()` re-publishes it to `roster.json` on every debounce, forever. `persistQueues` is
    dead — `grep -rn "persistQueues" src/` returns only its own definition, run this session — so the
    slice is frozen at boot and nothing ever prunes a delivered message out of it. Meanwhile
    `changeHome` in `src/main/index.ts` copies `roster.json` but not `delivery-queue.json` (which lives
    at the home ROOT), so `adoptRendererQueues`' `existsSync` guard passes at the new home and
    re-enqueues the frozen slice into live terminals — every Change Home, forever. (`index.ts` is
    01-25's file and 01-25 runs before this plan; the `<interfaces>` numbers for it are HEAD evidence,
    not seek offsets, and nothing in this task edits it.)

    Make the publication a genuine one-shot: keep the boot seed, and once a `rosterWrite` carrying a
    non-empty `queues` has resolved successfully, clear `rosterMirror.queues` and remove the
    `LS_QUEUES` key (the constant `const LS_QUEUES = 'cth.messageQueues';`), so every later write
    carries `{}`. `RosterSnapshot.queues` is a REQUIRED field (`RosterSnapshot` in
    `src/main/roster.ts`) — write `{}`, do not omit it. Only a result that is `ok` and not `skipped`
    consumes the one-shot; anything else must leave the slice intact.

    Then DELETE `persistQueues` **and its doc block**, delimited structurally: from the `/**` that opens
    the comment beginning *"DEAD on the delivery path since FLOOR-02"* down to the `}` that closes
    `function persistQueues(`. **Do not use a line range.** `:529-544` was accurate at HEAD and is
    wrong by the time this task runs, because task 1 of this same plan inserts above it — the state
    interface widening, the `queueError` slice, the `setQueueError` action and the rewritten `queueOp`
    doc block all land higher in the file. Deleting `:529-544` after task 1 deletes live code.

    The doc block's stated reason for existing —
    *"`rosterMirror.queues` is still seeded from the persisted copy at boot"* — is exactly what this
    task bounds, and a dead function whose comment argues for a live invariant is how `b/CR-03` stayed
    invisible through a whole phase. Say in the SUMMARY that the pair was resolved together rather than
    half-removed, which is the objection round 1 raised against touching either.

    **Ceiling, stated rather than claimed away.** This closes the RE-delivery. It does not migrate a
    home whose `roster.json` was copied before this lands: the messages there were already adopted at
    the source home, so main's `delivery-queue.json` holds them and the `existsSync` guard is the
    protection. Say so; do not imply this fixes an already-forked home.
  </action>
  <verify>
    <automated>node --test test/renderer-queue.test.cjs</automated>
  </verify>
  <done>
    All four behaviour cases pass including both negative controls. `grep -rn "persistQueues" src/`
    returns **nothing** (it returned exactly one line — the dead definition — at HEAD a217018, re-run
    and pasted). The first-flush and second-flush payloads are both pasted in the SUMMARY.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator keystrokes → main's durable queue | The composer is the operator's only path into an agent's inbox. A cleared draft on a refused write is unrecoverable. |
| main's synthesized `Stop` → the renderer's agent status | Main emits a Stop for any agent quiet for the quiesce interval, including one waiting on a human. The renderer decides what that means, and until now could not tell the two producers apart. |
| `roster.json` → `adoptRendererQueues` → live terminals | A frozen queue slice republished forever is an unauthenticated replay into a live PTY at every Change Home. |
| source comments → the next maintainer | A comment naming a deleted code path as live routes the next change into the wrong file. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P28-01 | Tampering | the operator's message discarded on a refused enqueue | mitigate | `queueOp` returns main's result; the composer clears only on `ok`; the refusal renders through `statusHint` at any queue length. |
| T-P28-02 | Repudiation | a refusal with no operator-visible trace | mitigate | The five documented `{ok:false}` reasons are surfaced, attributed to main so a transient FS error is not read as an app diagnosis. |
| T-P28-03 | Denial of service | a blocked agent silently reclassified as a spare worker | mitigate | A SYNTHESIZED Stop cannot idle a blocked agent, so the `!` escalation and `blockReason` stay consistent. |
| T-P28-04 | Denial of service | a falsely-blocked agent wedged out of its own status paint forever | mitigate | The guard keys on the event source, so a REAL Stop still idles and the floor resumes painting the agent normally. `R-21`'s stronger claim — that mail stops — is false (`drainQueue` has no status gate); a status-only guard here would still have wedged the paint. |
| T-P28-05 | Denial of service | a constrained agent stranded by a skipped breaker clear | mitigate | `:533`'s clear — the only one — is skipped only for the synthesized+blocked case; a real Stop always clears it (`R-22`). |
| T-P28-06 | Repudiation | main still emitting a Stop for a blocked agent | accept + document | Main holds no blocked status to filter on (`index.ts:533-536`). The renderer guard covers every producer; the missing main-side source is named as the residual. |
| T-P28-07 | Tampering | pre-migration messages replayed into live terminals at every Change Home | mitigate | The roster queue publication becomes a one-shot consumed only by a successful write; `persistQueues` deleted with it. |
| T-P28-08 | Tampering | a third writer of `messageQueues` skipping the declared gate | mitigate | The op-reply path routes through `setQueues`, restoring the single-writer claim its own doc makes. |
| T-P28-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. `react` / `react-dom` are existing production dependencies; `package.json` untouched. |
</threat_model>

<verification>
- `node --test test/renderer-queue.test.cjs test/delivery-main.test.cjs` green, with the RED output
  pasted for: `enqueueMessage`'s return value, the seeded refusal's absence from the markup, the four
  `stopArmDecision` cases, the `synthesized` payload key, and the claim-shaped comment regex.
- `npm test` green; paste the TAP counters
  (`node --test --test-reporter=tap test/*.test.cjs | grep -E "^# (tests|pass|fail|skipped|todo) "`).
  Report the delta this plan adds. Plan 01-31 owns the authoritative whole-suite figure.
- `npx eslint . --max-warnings 0` exit 0; `npm run typecheck` clean — the widened `enqueueMessage`
  signature crosses **seven** call sites.
- State plainly which assertions are BEHAVIOURAL and which are STRUCTURAL, and what each structural one
  cannot see. Exactly one structural assertion is planned (the comment-claim regex); everything else in
  this plan is behavioural, including the markup assertions. A source pin reported as a behavioural test
  is the defect class this phase's own review found three times.
- Confirm in the SUMMARY that `src/main/delivery.ts` and `test/delivery-main.test.cjs` were edited
  AFTER 01-27 landed, and paste the re-derived line numbers for `quiesce`'s emit.

- **REQUIRED — record these TWO residuals in the SUMMARY under the exact heading
  `## Quiesce residuals — ACCEPTED, not fixed`, so plan 01-31's register sweep can reach them.**
  The orchestrator accepted both deliberately: fixing either reverses a negative control this plan
  explicitly states (*"the once-per-spell arming untouched"*), and widening a gap-closure plan into a
  `quiesce` redesign is how this plan set reached 49 red-team blockers on its first pass. They are
  ACCEPTED, which means RECORDED WITH AN OWNER — not silently narrowed away.

  **(a) A false `blocked` that lands AFTER the last real Stop is unrecoverable.** `delivery.ts:668`
  suppresses the repeat via `this.quiesced.has()`, and `:667` only resets on new output, so no further
  synthesized Stop arrives; `useHive.ts:533` is the only clearer of `breakerLevel`. `usePtyParser.ts:35`
  can raise a false block off `/\(y\/n\)/i` against the terminal tail.
  Recovery path: a renderer clear-on-`PostToolUse`, or change `quiesce`'s once-per-spell arming.

  **(b) `quiesce`'s durable half still writes `idle` for an agent the floor paints `blocked`.**
  `delivery.ts:670` calls `setStatus?.(id,'idle')` unconditionally — the discriminator reaches the
  renderer arm only. Recovery path: pass the discriminator to both halves.

  **Do NOT satisfy this by narrowing truth #3's wording.** Truth #3 already moved from *"still
  recovers and still receives mail"* to *"still recovers its status on its next real turn-end"*, which
  assumes a next real turn-end exists — that is exactly case (a). State the residual explicitly.

**MEASUREMENT UNAVAILABLE without an operator:** filling a queue to `MAX_QUEUED_PER_AGENT = 200`,
typing a message, pressing Enter and seeing the text survive with the reason on screen; and watching a
permission-prompted agent hold its `!` through a quiesce interval on the real floor. Record both as
unrun — `renderToStaticMarkup` runs no effects and fires no events, so no test here presses a key.
</verification>

<success_criteria>
- A refused message stays in the draft with main's reason rendered, at any queue length.
- `enqueueMessage` reports acceptance to all seven of its callers.
- A permission-prompted agent keeps its `needs you` state through the quiesce interval, and a
  falsely-blocked one still recovers on its next real turn-end.
- No comment in `useHive.ts` presents any renderer-side drain as live.
- `roster.json` carries the pre-FLOOR-02 queue at most once.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-28-SUMMARY.md` when done.
</output>
