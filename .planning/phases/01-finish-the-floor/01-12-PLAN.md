---
phase: 01-finish-the-floor
plan: 12
type: execute
wave: 5
depends_on: ["01-01", "01-05"]
files_modified:
  - src/renderer/src/components/AgentCard.tsx
  - src/renderer/src/components/FullscreenTerminal.tsx
  - src/renderer/src/components/CommandCenterPanel.tsx
  - src/renderer/src/App.tsx
  - src/renderer/src/store/autoMode.ts
  - src/renderer/src/store/sidebarLayout.ts
  - test/renderer-runstate.test.cjs
autonomous: false
requirements: [FLOOR-01, FLOOR-13]

must_haves:
  truths:
    - "An agent card shows at a glance that the agent runs with permissions bypassed"
    - "The auto-mode state is derived once, from the agent's own command string, and is truthful per agent"
    - "The three text renderings of an agent agree on the field set, cost included"
    - "The sidebar collapses below a 1024px viewport and a single toggle re-opens it as an overlay"
    - "No rendering claims auto mode for an engine that has no auto path"
  artifacts:
    - path: "src/renderer/src/store/autoMode.ts"
      provides: "one shared derivation used by all three renderings — not three re-derivations that drift"
      contains: "autoMode"
    - path: "src/renderer/src/store/sidebarLayout.ts"
      provides: "a pure viewport→layout function, testable under node --test"
      contains: "1024"
    - path: "test/renderer-runstate.test.cjs"
      provides: "unit coverage of both pure helpers"
  key_links:
    - from: "AgentCard / FullscreenTerminal roster row / CommandCenterPanel row"
      to: "src/renderer/src/store/autoMode.ts"
      via: "one shared helper called by all three"
      pattern: "autoMode"
    - from: "App.tsx vpWidth"
      to: "src/renderer/src/store/sidebarLayout.ts"
      via: "the existing viewport state — no second source of viewport truth"
      pattern: "vpWidth"
---

<objective>
Surface auto mode on every rendering of an agent, add the one missing field to the card, and make the
sidebar collapse responsively — against the approved UI contract, without redesigning anything.

Purpose: FLOOR-01 and FLOOR-13, both of ROADMAP criterion 4. `grep -c autoMode
src/renderer/src/components/AgentCard.tsx` returns **0** — the card is silent about an agent running
with permissions bypassed. And `DESIGN.md:678` promises *"Right panel collapses below 1024 to bottom
drawer"* while `grep -rn "@media" src/renderer/src` returns exactly one hit and it is
`prefers-reduced-motion` — a doc promising a code path that does not run, which criterion 1 bans.

`01-UI-SPEC.md` is the **binding** design contract for both. It is precise enough that nothing needs
inventing, and it marks several clauses ALREADY SATISFIED — including the cost field on all three
renderings and the `sidebarWidth` resize clamp. Do not rebuild those.

Output: one shared auto-mode derivation, an AUTO chip in three renderings, the model field on the
card, and a 1024px collapse driven off the viewport state that already exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@DESIGN.md

<interfaces>
**The four renderings, named authoritatively by issue #39's own text (UI-SPEC corrects RESEARCH's
guess):**

| # | Rendering | Anchor |
|---|-----------|--------|
| 1 | Agent card (bottom strip) | `components/AgentCard.tsx` |
| 2 | Fullscreen roster row | `components/FullscreenTerminal.tsx:612-750` |
| 3 | Command-centre row | `components/CommandCenterPanel.tsx:699-775` |
| 4 | Floor avatar (Pixi sprite) | `scene/office/Character.ts` + `scene/office/OfficeFloor.tsx` |

`AgentStrip` and `AgentDetailPanel` are **not** in the set. **The avatar is a deliberate exception and
gains no field set** — `Character.ts` imports no pixi `Text`: its pixi import is exactly
`{ Container, Graphics, Texture }`. It *does* import `CharacterSprite`, `findPath`,
`TiledMapRenderer` and `ThoughtBubble`, and `ThoughtBubble` renders text and carries a `FONT_SIZE`
that plan 14 changes — so "no `Text` at all" would be wrong. The avatar exception holds on the
narrower, true fact: the avatar itself draws no text, so there is no field set to widen there.
The agreement contract binds the **three text renderings**.

**Agreed field set — gaps are exactly two:** field 3 (Auto mode) in all three, and field 5 (Model) on
the card. Name, Status, Cost, Location and Context all already render in all three. **Cost is already
shown in all three** (`AgentCard.tsx:256-265`, `FullscreenTerminal.tsx:711-718`,
`CommandCenterPanel.tsx:743-747`) — issue #39's *"None shows cost"* is stale. Specify nothing for it.

**Auto-mode derivation — locked by UI-SPEC.** `config.autoMode` is a single GLOBAL toggle;
`src/renderer/src/store/config.ts:427` appends the provider's own flag at spawn. Two traps:
`agentProvider.ts:357` (`opencode`) and `:533` (`custom`) both carry `autoFlag: ''` — `opencode` IS
still bypassed by a different route (`index.ts:3277-3279` writes `permission: {edit:'allow',
bash:'allow', webfetch:'allow'}` into `OPENCODE_CONFIG_CONTENT`, gated on `cfg.autoMode`), and
`custom` has **no auto path at all**. And the flag is baked at spawn, so toggling `autoMode` off does
not de-bypass a running agent.
Therefore: source of truth is the agent's own `command` string (`store.ts:67`), tested against
`autoModeFlagForProvider(provider)` (`agentProvider.ts:624`). `opencode` falls back to the live
`config.autoMode` with the reason in an inline comment. `custom` is never bypassed.

**AUTO chip visual contract** (clone the `BOSS` chip at `AgentCard.tsx:227-231` exactly — same
`padding: '1px 4px 0'`, same `flexShrink: 0`): label `AUTO`; position in the identity row
(`AgentCard.tsx:216`) immediately after the name and after `BOSS` if present, before `<PixelBadge>`;
fill `var(--cth-lilac-light)`; border `inset 0 0 0 1px var(--cth-lilac)`; text `var(--cth-ink-900)`;
font `var(--cth-font-display)` at `var(--cth-text-display-md)` / `var(--cth-lh-display-md)`;
**interaction: none** — not focusable, not clickable, no `role="button"`.
Write it at the TOKEN, not at `fontSize: 7` — wave 6 raises that token to 14px and a literal would be
swept separately.

**The accessible-name trap.** `AgentCard.tsx:155` sets `aria-label` to the name, optional `(boss)` and
the status; `FullscreenTerminal.tsx:622` sets it to the name and project. An `aria-label` on the
container **replaces** all inner text for a screen reader, so a chip added inside is visually present
and completely inaudible. Contract: **any rendering whose root carries an `aria-label` must fold the
auto-mode state into that label** (e.g. `Ada (boss) — working — auto mode, permissions bypassed`) and
the chip itself is then `aria-hidden="true"`.

**Copy:** chip label `AUTO`; accessible name `Auto mode — {name} runs with permissions bypassed`;
hover title `Auto mode: {name} acts without asking for tool approval.`

**Model field on the card:** follow `FullscreenTerminal.tsx:704` — `shortModel(agent.model) ?? 'CLI
default'`, in the existing context line (`AgentCard.tsx:243-252`), `flexShrink: 0`, before the cost
span, with the full model id in the row's `title`.

**Deliberately excluded: duration.** There is no spawn timestamp on the store's `Agent` shape
(`store.ts:55-90`), so duration means new durable state plus a per-second re-render of every roster
row — a direct collision with FLOOR-11 landing in the same phase. Criterion 4 grades *"agree on what
they show, cost included"*. Do not build it.

**Sidebar — two of three clauses ALREADY SATISFIED.** The resize clamp is live at
`SidebarSplitter.tsx:35-37` (a `useEffect` on `[width, clampMax, onChange]`), fed by `App.tsx:222-226`
tracking `window.innerWidth` into `vpWidth` and passed at `App.tsx:448`; the store's `[320, 1200]`
clamp at `store.ts:865` is the deliberately viewport-blind absolute-bounds half. **Do not add a second
resize listener.** Left-truncating long paths is also live (`PathLine` at
`CommandCenterPanel.tsx:1581-1595`). **Specify nothing for either.**

**The one genuine gap — responsive collapse.** Below 1024px viewport width the sidebar collapses to
hidden and the floor canvas takes the full width; a single persistent toggle re-opens it as an overlay
at `z-index: 2` (`DESIGN.md:686`, the drawer/sidebar layer — layer 3 is toasts and using it would
manufacture the doc/code contradiction criterion 1 bans). Toggle:
`<PixelButton variant="secondary" size="sm">` labelled `hide panel` when open and `show panel` when
collapsed, with `aria-expanded`; **the visible label is the accessible name — do not add an
`aria-label`**. Placement: top-right of the floor canvas, `position: absolute`,
`top: var(--cth-space-2)`, `right: var(--cth-space-2)`, `z-index: 2`, kept clickable above the overlay
by DOM order (render the toggle after the overlay in the same stacking context). Rendered **only**
below 1024px. The agent strip is unaffected. Drive it off the existing `vpWidth` — **no `matchMedia`,
no CSS breakpoint**; a second source of viewport truth is how this drifts. `SidebarSplitter` is hidden
while collapsed; its clamp effect is untouched. Below 1024 the overlay's width is
`Math.min(sidebarWidth, vpWidth - 48)` and it must **not** be written back through `setSidebarWidth`.

Build it or delete `DESIGN.md:678`. There is no third option.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-01 and FLOOR-13 clause, and paste the actual field list of each of the four renderings</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/renderer/src/components/AgentCard.tsx` in full — the identity row `:216-238`, the `BOSS` chip `:226-232`, the info line `:243-252`, the cost span `:253-265`, and the root `aria-label` at `:155`.
    - `src/renderer/src/components/FullscreenTerminal.tsx:598-760` — the roster row, its `aria-label` at `:622`, `shortModel` at `:701-705`, cost at `:711-718`, `<ContextBar>` at `:720`.
    - `src/renderer/src/components/CommandCenterPanel.tsx:699-780` — model at `:713-714`, name at `:735`, badge at `:736`, cost at `:743-747`, `<PathLine>` at `:751`, usage row at `:771`.
    - `src/renderer/src/scene/office/Character.ts` — confirm it imports no `Text`.
    - `src/renderer/src/App.tsx:215-235` and `:440-460` — `vpWidth` and the sidebar wiring.
    - `src/renderer/src/components/SidebarSplitter.tsx:20-40` — the live clamp effect and its comment.
    - `src/renderer/src/store/config.ts:420-435` and `src/shared/agentProvider.ts:350-360`, `:525-540`, `:615-630` — the spawn-time flag, the two `autoFlag: ''` providers, and `autoModeFlagForProvider`.
    - `01-UI-SPEC.md` § FLOOR-01 and § FLOOR-13 in full — the binding contract.
  </read_first>
  <action>
    Quote issues #4, #38 and #39's acceptance text verbatim and run one command per clause with ACTUAL
    output pasted:
    - `grep -c autoMode src/renderer/src/components/AgentCard.tsx` — expect `0`. This is one of the four previously mis-assessed issues (D-42/D-47); the empty result IS the evidence.
    - `grep -rn "usd\|toFixed(2)" src/renderer/src/components/AgentCard.tsx src/renderer/src/components/FullscreenTerminal.tsx src/renderer/src/components/CommandCenterPanel.tsx` — evidence that cost ALREADY renders in all three, contradicting issue #39's "None shows cost".
    - `grep -rn "@media" src/renderer/src` — expect exactly one hit (`global.css:151`, `prefers-reduced-motion`). Evidence the responsive collapse is genuinely absent.
    - `grep -rn "1024" src/renderer/src` — expect nothing relevant.
    - `grep -n "clampMax\|viewportWidth" src/renderer/src/components/SidebarSplitter.tsx src/renderer/src/App.tsx` — evidence the resize clamp ALREADY ships. Do not rebuild it.

    Then read all three text renderings and **paste their actual field lists side by side** as a table,
    field by field. UI-SPEC gives the expected table; current source is what must be confirmed. Record
    any drift. State explicitly that the avatar is the deliberate exception and gains no fields, so the
    checker reads it as intentional rather than as a miss.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all five commands, with the `autoMode` grep empty and the `@media` grep showing exactly one unrelated hit.
    - Task notes contain a three-column field table for the three text renderings, built by reading them, with each cell carrying a `file:line` or the word `absent`.
    - Task notes state the avatar exception explicitly.
    - **Recorded count baselines** (this plan's later criteria are strict deltas against these, because each token already occurs in its file for unrelated reasons — a bare `at least N` would be satisfied by doing nothing). Run and paste each, verbatim:
      `grep -c "AUTO" src/renderer/src/components/CommandCenterPanel.tsx` (expect `4` — pre-existing
      `AUTO`-cased identifiers, none of them the FLOOR-01 chip),
      `grep -c "AUTO" src/renderer/src/components/AgentCard.tsx` (expect `0`) and the same for
      `FullscreenTerminal.tsx` (expect `0`).
      **Three more baselines, for the criteria phrased as "the same value as before this task".** That
      phrasing is unfalsifiable once the edit has landed — "before" is unrecoverable — so the numbers
      are measured here and the later criteria assert the number, not the phrase. All three measured
      this session:
      `grep -cE "fontSize: *[0-9]" src/renderer/src/components/AgentCard.tsx` → `7`, label **B-fontsize**;
      `grep -c "role=\"button\"" src/renderer/src/components/AgentCard.tsx` → `2`, label **B-rolebutton**;
      `grep -c "setSidebarWidth" src/renderer/src/App.tsx` → `2`, label **B-setsidebarwidth**.
    - Baseline TAP counters for the file tasks 2 and 3 extend, pasted verbatim:
      `TAP=$(mktemp); node --test --test-reporter=tap test/renderer-runstate.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured this session: `EXIT=0`, `# tests 8`, `# pass 8`, `# fail 0`, `# skipped 0`, `# todo 0`. Label the `# pass` number **B-runstate**. Write the TAP through `mktemp`, never into the repo — `*.tap` is not in `.gitignore` and a stray file would trip this wave's containment checks.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every clause is classified with pasted evidence and the field-set gap is confirmed to be exactly two items, not assumed from the issue text.</done>
</task>

<task type="auto">
  <name>Task 2: One shared auto-mode derivation, and the AUTO chip in all three text renderings</name>
  <files>src/renderer/src/store/autoMode.ts, src/renderer/src/components/AgentCard.tsx, src/renderer/src/components/FullscreenTerminal.tsx, src/renderer/src/components/CommandCenterPanel.tsx, test/renderer-runstate.test.cjs</files>
  <read_first>
    - The three rendering files at the anchors from task 1 — the files being modified.
    - `src/shared/agentProvider.ts:615-630` — `autoModeFlagForProvider`'s exact signature and return type.
    - `src/renderer/src/store/store.ts:55-90` — the `Agent` shape, especially `command` at `:67`.
    - `01-PATTERNS.md` § "FLOOR-01 / FLOOR-12 / FLOOR-13 — the renderer" `:1046-1090` — the verbatim `BOSS` chip, the `flexShrink: 0` rationale, and the cost span.
    - `01-UI-SPEC.md` § FLOOR-01 — the locked derivation, the visual contract table, and the accessible-name contract.
    - `test/renderer-runstate.test.cjs` in full — the existing pure-function test idiom to extend.
  </read_first>
  <action>
    Create `src/renderer/src/store/autoMode.ts` exporting one pure predicate — given an agent's
    `provider`, its `command` string, and the live `config.autoMode`, return whether that agent runs
    with permissions bypassed. Implement exactly the locked derivation: test the command against
    `autoModeFlagForProvider(provider)`; special-case `opencode` to fall back to the live
    `config.autoMode` **with the reason in an inline comment** (its bypass is env-based via
    `OPENCODE_CONFIG_CONTENT` and is not recorded on the agent); return false for `custom`, which has
    no auto path at all. Carry the comment explaining why the command string rather than the toggle:
    the flag is baked at spawn, so a chip driven off the live toggle lies in both directions.

    Pure and dependency-free so it is testable under `node --test` — no React, no store import, no
    `@/` value imports (type-only `@/` is fine; it is erased at transpile).

    Add the AUTO chip to all three text renderings, cloning the `BOSS` chip's shape exactly and
    applying the visual contract table in `<interfaces>` verbatim. Write it at
    `var(--cth-text-display-md)` / `var(--cth-lh-display-md)` from the start — not at a literal size.
    It is an indicator: not focusable, not clickable, no `role="button"`.

    **Fold the state into the container's accessible name** wherever the root carries an `aria-label`
    (`AgentCard.tsx:155`, `FullscreenTerminal.tsx:622`), and set the chip itself `aria-hidden="true"`.
    Use the locked copy: `Auto mode — {name} runs with permissions bypassed` in the label, and
    `Auto mode: {name} acts without asking for tool approval.` as the chip's `title`.

    Extend `test/renderer-runstate.test.cjs` with unit tests for the predicate: a claude agent whose
    command carries the auto flag is bypassed; the same provider without it is not; an `opencode`
    agent follows the live toggle; a `custom` agent is never bypassed even with the toggle on; and
    toggling `config.autoMode` off does not change a running agent's answer for a flag-carrying command.
    That last one is the "lies in the other direction" case and is the reason the derivation is not the
    toggle.

    Do not touch `AgentStrip.tsx` or `AgentDetailPanel.tsx` — they are not in the four renderings, and
    `AgentStrip` belongs to plan 05's wave and plan 14's sweep.
  </action>
  <acceptance_criteria>
    - `ls src/renderer/src/store/autoMode.ts` succeeds and the file contains no `import React` and no value import from `@/`.
    - `grep -c "autoMode" src/renderer/src/components/AgentCard.tsx` returns at least `1` — the clause that was `0` at task 1.
    - `grep -c "autoMode" <file>` returns at least `1` for **each** of `AgentCard.tsx`, `FullscreenTerminal.tsx` and `CommandCenterPanel.tsx` — all three are `0` today, and this is the criterion that proves all three renderings call the one shared derivation. A bare `AUTO` grep does not: `CommandCenterPanel.tsx` already returns `4` for it (pre-existing `AUTO`-cased identifiers) and would pass untouched.
    - `grep -c "AUTO" src/renderer/src/components/AgentCard.tsx` and the same for `FullscreenTerminal.tsx` each return at least `1` (both are `0` today — the visible chip glyph). For `CommandCenterPanel.tsx` the count must be **strictly greater than `4`**, its recorded baseline.
    - `grep -c "aria-hidden" src/renderer/src/components/AgentCard.tsx` returns at least `1` (baseline `0`, measured this session).
    - Containment, **path-scoped to this plan's own declared files** — never a bare whole-tree `git diff`/`git status`, because `use_worktrees: false` means the other wave-5 plans are editing this same tree at the same time and their files show up in it. Both must hold: `git diff --name-only -- src/renderer/src/store/autoMode.ts src/renderer/src/components/AgentCard.tsx src/renderer/src/components/FullscreenTerminal.tsx src/renderer/src/components/CommandCenterPanel.tsx test/renderer-runstate.test.cjs` lists only paths from that set, and `git diff --name-only -- src/renderer/src/components/AgentStrip.tsx src/renderer/src/components/AgentDetailPanel.tsx` prints **nothing**.
    - `grep -cE "fontSize: *[0-9]" src/renderer/src/components/AgentCard.tsx` returns **`7`** — B-fontsize, measured this session. The new chip added no numeric `fontSize` literal for wave 6 to sweep. The number is written down because "the same value as before this task" cannot be checked after the edit has landed.
    - `grep -c "role=\"button\"" src/renderer/src/components/AgentCard.tsx` returns **`2`** — B-rolebutton, measured this session. The chip is an indicator, not a control.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/renderer-runstate.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least `13`** (B-runstate = `8` plus this task's five), **`# fail 0`**, **`# skipped 0`**, `# todo 0`.
      Exit code plus a test count is not a gate on its own: `node --test` counts skipped tests in `# tests` and still exits `0` — verified this session, two `{skip: '...'}` tests one of which holds a live `assert.fail()` report `# tests 2 / # pass 0 / # fail 0 / # skipped 2` at `EXIT=0`. Five `{skip}` stubs would satisfy "exit 0, five more tests" exactly, including the `custom`-never-bypassed test that this plan's whole safety argument rests on. The `# pass` floor plus `# skipped 0` forbids that. Name the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-runstate.test.cjs</automated>
  </verify>
  <done>One derivation, three renderings, truthful for every provider including the two with no auto flag, and audible to a screen reader through the container's name.</done>
</task>

<task type="auto">
  <name>Task 3: The model field on the card, and the 1024px responsive collapse</name>
  <files>src/renderer/src/components/AgentCard.tsx, src/renderer/src/App.tsx, src/renderer/src/store/sidebarLayout.ts, test/renderer-runstate.test.cjs</files>
  <read_first>
    - `src/renderer/src/components/AgentCard.tsx:243-265` — the context line and the cost span the model field sits before.
    - `src/renderer/src/components/FullscreenTerminal.tsx:700-710` — `shortModel`'s established use, which the card copies.
    - `src/renderer/src/App.tsx:215-235` and `:440-460` — `vpWidth`, the sidebar render, and where the overlay and toggle go.
    - `src/renderer/src/components/SidebarSplitter.tsx` in full — the clamp effect that must be left untouched, and the conditions under which the splitter should not render.
    - `DESIGN.md:678` and `:686` — the promise being made true, and the layer assignment.
    - `01-UI-SPEC.md` § FLOOR-13 — the binding contract, reproduced in `<interfaces>`.
  </read_first>
  <action>
    Add the model field to the agent card, following `FullscreenTerminal.tsx:704`'s established
    pattern: `shortModel(agent.model) ?? 'CLI default'`, rendered in the existing context line
    (`AgentCard.tsx:243-252`), `flexShrink: 0`, positioned **before** the cost span, with the full
    model id included in the row's `title`. Use existing tokens for size; add no numeric `fontSize`
    literal.

    Do **not** add cost, and do not touch the cost spans — cost already renders in all three. Do **not**
    add duration to anything.

    Create `src/renderer/src/store/sidebarLayout.ts` exporting a pure function from
    `(vpWidth, sidebarWidth, isOpen)` to the layout decision: whether the sidebar is collapsed
    (`vpWidth < 1024`), the overlay width (`Math.min(sidebarWidth, vpWidth - 48)`), and whether the
    toggle renders. Pure and dependency-free so it is testable under `node --test`.

    In `App.tsx`, drive the collapse off the **existing** `vpWidth` state at `:222-226`. **Add no
    `matchMedia` listener and no CSS breakpoint** — a second source of viewport truth is how this
    drifts. Below 1024px: hide the sidebar, let the floor canvas take the full width, and render the
    toggle plus, when open, the sidebar as an overlay at `z-index: 2` per `DESIGN.md:686`. Render the
    toggle **after** the overlay in the same stacking context so DOM order keeps it clickable. Above
    1024px the toggle is not in the tree at all.

    Toggle copy and semantics, locked: `<PixelButton variant="secondary" size="sm">` reading
    `hide panel` when open and `show panel` when collapsed, with `aria-expanded` carrying the state.
    **The visible label is the accessible name — do not add an `aria-label`**; doing so would override
    it and make the control worse for voice control.

    The overlay's computed width must **not** be written back through `setSidebarWidth`. Persisting a
    small-window width would strand the user's chosen width on the next large-window boot — the exact
    bug class `SidebarSplitter.tsx:27-34` was written to kill. Hide `SidebarSplitter` while collapsed
    (nothing to drag) and leave its clamp effect untouched. The agent strip is unaffected in both states.

    Extend `test/renderer-runstate.test.cjs` with unit tests for `sidebarLayout`: at 1200px the sidebar
    is not collapsed and the toggle does not render; at 1023px it is collapsed and the toggle renders;
    the overlay width is capped at `vpWidth - 48`; and crossing the boundary in both directions does not
    mutate the stored `sidebarWidth`.
  </action>
  <acceptance_criteria>
    - `grep -c "shortModel" src/renderer/src/components/AgentCard.tsx` returns at least `1`.
    - `ls src/renderer/src/store/sidebarLayout.ts` succeeds; `grep -c "1024" src/renderer/src/store/sidebarLayout.ts` returns at least `1`.
    - `grep -c "matchMedia" src/renderer/src/App.tsx` **still** returns `0` and `grep -rc "@media" src/renderer/src/App.tsx` **still** returns `0` — both are `0` today, so this is **a preservation criterion**: the breakpoint stays a measured-width comparison and no media query or `matchMedia` listener is introduced. It is not a completion criterion; completion is the `sidebarLayout.ts` + `aria-expanded` clauses above.
    - `grep -c "aria-expanded" src/renderer/src/App.tsx` returns at least `1`, and the toggle carries no `aria-label` — paste the toggle's JSX.
    - `grep -c "setSidebarWidth" src/renderer/src/App.tsx` returns **`2`** — B-setsidebarwidth, measured this session. The overlay width is not persisted, and the number is written down because "the same value as before this task" cannot be checked after the edit has landed.
    - `git diff --name-only -- src/renderer/src/components/SidebarSplitter.tsx` prints **nothing** — the clamp effect is untouched. Scope the diff to the path: a bare `git diff --stat` shows every wave-mate's work in this shared tree and can decide nothing about this file.
    - Containment, path-scoped: `git diff --name-only -- src/renderer/src/components/AgentCard.tsx src/renderer/src/App.tsx src/renderer/src/store/sidebarLayout.ts test/renderer-runstate.test.cjs` lists only paths from that set.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/renderer-runstate.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least `17`** (B-runstate = `8`, plus task 2's five, plus this task's four), **`# fail 0`**, **`# skipped 0`**, `# todo 0`. Paste task 2's recorded `# pass` alongside, so the delta is visible as well as the absolute floor.
      Same reasoning as task 2: `node --test` counts skipped tests in `# tests` and still exits `0` (verified this session), so "exit 0 with four more tests" is satisfied by four `{skip: '...'}` stubs. The `# pass` floor plus `# skipped 0` is the gate. Name the TAP reporter explicitly; the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/renderer-runstate.test.cjs</automated>
  </verify>
  <done>The card shows the model, the sidebar collapses below 1024 with one accessible toggle driven off the existing viewport state, and `DESIGN.md:678` is now a true statement.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Operator verification of the auto-mode indicator and the responsive collapse</name>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-01 and § FLOOR-13 — the visual contract being checked.
    - Task 1's three-column field table — what each rendering is supposed to show.
    - Task 2's notes on the `opencode` and `custom` exceptions.
  </read_first>
  <acceptance_criteria>
    - The SUMMARY records seven explicit yes/no answers, one per numbered verification step.
    - Step 2 (chip STAYS after toggling auto mode off without restarting the agent) and step 3 (`custom` shows NO chip) are answered individually — they are the two directions the indicator can lie.
    - Step 7 confirms the pre-collapse sidebar width survived a quit and relaunch.
  </acceptance_criteria>
  <what-built>
    An `AUTO` chip on the agent card, the fullscreen roster row and the command-centre row, derived
    from each agent's own command string; the model field added to the card; and a responsive sidebar
    collapse below 1024px with a `hide panel` / `show panel` toggle.
  </what-built>
  <how-to-verify>
    Criterion 4 is *"the floor is legible to the operator watching it"* — the only instrument that
    grades legibility is a person looking at it.

    1. Launch the dev app. Turn auto mode ON in Settings and spawn a Claude agent. Confirm an `AUTO`
       chip appears on the card, in the fullscreen roster row and in the command-centre row, and that
       all three look like the `BOSS` chip in shape but lilac with a hairline.
    2. Turn auto mode OFF **without** restarting that agent. The chip must STAY — the flag was baked
       at spawn. Then spawn a second agent and confirm the new one has no chip. This is the specific
       lie the derivation exists to prevent.
    3. Spawn a `custom`-provider agent with auto mode ON. It must show **no** chip.
    4. Confirm the chip is not clickable and cannot be tabbed to.
    5. Confirm the card now shows the model, before the cost.
    6. Resize the window below 1024px wide. The sidebar must collapse, the canvas take the full width,
       and a `show panel` toggle appear top-right of the canvas. Click it: the sidebar opens as an
       overlay above the canvas and the toggle reads `hide panel` and is still clickable.
    7. Resize back above 1024px, then quit and relaunch. The sidebar width must be the width you had
       chosen before, not the narrow overlay width.

    Report each of the seven as yes/no.
  </how-to-verify>
  <files>(none — operator verification of the running app)</files>
  <action>Pause execution and present the seven numbered verification steps below to the operator verbatim. Do not proceed and do not write the SUMMARY until seven answers are returned. Record them verbatim — criterion 4 grades legibility, and the only instrument for legibility is a person looking at it.</action>
  <verify><human-check>Operator returns seven yes/no answers, one per numbered step.</human-check></verify>
  <done>The operator has checked the AUTO chip in all three renderings, both lying directions, the model field, and the 1024px collapse, and the seven answers are recorded.</done>
  <resume-signal>Type "approved" with the seven yes/no answers, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent state (provider, command) → an operator-facing safety indicator | The AUTO chip is a **safety** indicator: it tells the operator an agent acts without asking for tool approval. A chip that lies in either direction is worse than no chip. |
| viewport size → layout state persisted to localStorage | A layout decision made in a small window must not be written back as the user's chosen width. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P12-01 | Repudiation | a false-negative AUTO chip (agent bypassed, chip absent) | mitigate | Derivation reads the agent's own command string against `autoModeFlagForProvider`, plus the documented `opencode` env-based exception. Pinned by unit tests and by checkpoint step 3. |
| T-P12-02 | Repudiation | a false-positive AUTO chip (chip shown for `custom`, which has no auto path) | mitigate | `custom` returns false unconditionally; asserted by a unit test and by checkpoint step 3. |
| T-P12-03 | Repudiation | the indicator following the live toggle rather than the running agent | mitigate | The flag is baked at spawn; the derivation ignores the live toggle except for `opencode`. Checkpoint step 2 is the specific test. |
| T-P12-04 | Information disclosure | a safety indicator invisible to a screen-reader user | mitigate | Any root carrying an `aria-label` folds the auto-mode state into it and the chip is `aria-hidden="true"` — an `aria-label` on the container otherwise replaces all inner text. |
| T-P12-05 | Tampering | the responsive layout persisting a small-window width | mitigate | The overlay width is computed, never written through `setSidebarWidth`; asserted by a unit test and by checkpoint step 7. |
| T-P12-06 | Denial of service | a per-second re-render of every roster row | mitigate | Duration is explicitly not built — no ticking value is added, which would collide with FLOOR-11 landing in the same phase. |
</threat_model>

<verification>
- `npm test` green on all three platforms.
- `npm run typecheck` green.
- The seven checkpoint answers recorded.
- UI-SPEC checker sign-off dimensions (copywriting, visuals, color, typography, spacing, registry
  safety) are satisfied by construction: the only new colour is the lilac chip, the only new control is
  the sidebar toggle, and no dependency, component or spacing token is added.
</verification>

<success_criteria>
- ROADMAP criterion 4's FLOOR-01 and FLOOR-13 clauses are TRUE: an agent card shows at a glance that
  the agent runs with permissions bypassed; the renderings of an agent agree on what they show, cost
  included; the sidebar collapses responsively.
- `DESIGN.md:678`'s promise is now backed by code, so it is no longer a doc promising a path that does
  not run (criterion 1).
- Issues #4, #38 and #39 close in their PRs per D-43. Per D-47, #4 is one of the four previously
  mis-assessed issues and additionally gets a fresh-context adversarial re-verify before closing.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-12-SUMMARY.md` when done. It MUST include the
three-column field table from task 1, state the avatar exception as intentional, record the seven
checkpoint answers, and confirm that no new sub-14px `fontSize` literal was introduced for wave 6 to
sweep.
</output>
