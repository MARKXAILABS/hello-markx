---
phase: 01-finish-the-floor
plan: 07
type: execute
wave: 3
depends_on: ["01-01", "01-05"]   # 01-05 creates test/repo-claims.test.cjs, which task 3 appends to and <verify> runs
files_modified:
  - src/main/delivery.ts
  - src/main/index.ts
  - src/renderer/src/hooks/useHive.ts
  - HIVE.md
  - test/delivery-main.test.cjs
  - test/repo-claims.test.cjs
autonomous: true
requirements: [FLOOR-02]

must_haves:
  truths:
    - "An agent that goes idle mid-queue is still woken with the window closed — the quiesce backstop runs in main"
    - "The Stop-drain stays live and guarded — it is not restored and not deleted"
    - "grep finds no doc promising a code path that does not run: ALL TWELVE of HIVE.md's stale Stop-drain denials are gone — five in §2 decision 5 (`HIVE.md:85-103`), one in §3's on-disk-layout block (`:125`), one in §5's control-flow diagram (`:184`), two in §7's phase note (`:223`, `:228`), three in §8's risk table (`:269` ×2, `:271`)"
    - "HIVE.md decision 5 no longer presents the Stop-drain as struck out and reversed — it ships, guarded, and the doc says so"
    - "HIVE.md and .planning/codebase/ARCHITECTURE.md no longer contradict each other about the Stop-drain and cursor.json"
  artifacts:
    - path: "src/main/delivery.ts"
      provides: "the idle-quiesce backstop as a branch in the existing tick, driven by DeliveryDeps"
      contains: "quiesce"
    - path: "test/delivery-main.test.cjs"
      provides: "fake-clock coverage of the quiesce transition with no window attached"
    - path: "HIVE.md"
      provides: "twelve stale denials removed — §2 decision 5 (`:85-103`) rewritten, plus §3 `:125`, §5's diagram `:184`, §7 `:223`/`:228`, §8 `:269`/`:271`"
  key_links:
    - from: "src/main/delivery.ts tick"
      to: "liveAgents() lastOutputAt + breakerLevel + bootGraceUntil"
      via: "main-side quiesce branch replacing the renderer's setInterval"
      pattern: "lastOutputAt"
    - from: "src/main/index.ts"
      to: "DeliveryDeps"
      via: "the quiesce inputs folded in as deps, not leaked as a new timer in index.ts"
      pattern: "DeliveryDeps|delivery"
---

<objective>
Move the idle-quiesce backstop out of the renderer into main's existing delivery tick, and delete
the twelve `HIVE.md` claims that describe code paths which do not run.

Purpose: FLOOR-02's first half. **The roadmap's premise for this requirement is factually wrong and
the plan must not act on it.** The Stop-drain is NOT dead: `index.ts:467` passes
`(agentId) => delivery.drainAtStop(agentId)` into `HookServer`, `hooks.ts:332` calls it at the Stop
boundary, `delivery.ts:216` guards on `paused()` then `vetoed()`, and
`test/delivery-main.test.cjs:117-142` already covers four guard paths. Criterion 1's "live under a
guard" branch is already satisfied — **do not restore it and do not delete it.**

What is actually left is (a) the quiesce backstop, which is near-mechanical because main already has
every input, and (b) the doc contradiction, which is itself the "doc promising a code path that does
not run" the criterion bans — pointing the opposite way from what the roadmap assumed.

Output: a quiesce branch in `delivery.ts`'s tick, a fake-clock test, twelve deleted HIVE.md claims, and
a repo-fact test so they cannot come back. The queue-drain half is plan 08.

**Scope correction (red-team round 1).** An earlier revision of this plan froze only four literals —
`:125`, `:223`, `:269`, `:271`, which that revision described as "all in §7/§8" and which are in fact
§3, §7, §8, §8. `HIVE.md:85-103` — decision 5 in §2, *"Autonomous loop = `Stop` hook"* — holds
five MORE denials of the same fact, and it is the load-bearing one: it strikes the whole decision out
as *"Reversed"* and states in prose that `hooks.ts` *"answers **every** `Stop`/`SubagentStop` with
`{}`"*, *"never forces a continuation"*, that `drainForStop()` *"still exists and still works, but
nothing in the app calls it"*, and that dedup is a *"per-renderer-session"* set. Every one of those is
false against `src/main/hooks.ts:332`, `src/main/index.ts:455` and `:467`. Freezing four literals
while five survive would let this plan report FLOOR-02 done with the project's own architecture doc
still denying the feature exists.

**Scope correction (red-team round 3) — the twelfth denial, in §5.** `HIVE.md:184`, inside §5's
control-flow diagram, reads *"hook POSTs to the hive socket; main answers {} — never a forced
continue"*. It is false against `src/main/hooks.ts:332-337`, which returns `{decision:'block', reason}`
when the drain has mail — and it matches **none** of the eleven literals frozen above
(`never forces a continuation` ≠ `never a forced continue`; `` `Stop` returns `{}` `` ≠
`main answers {}`). With task 3 forbidding any other `HIVE.md` change, FLOOR-02 would have shipped
"done" while §5's diagram — the picture a reader reaches before §7 or §8 — still denied the feature.
The freeze set is now **twelve**. The twelfth literal is frozen as the WHOLE clause,
`main answers {} — never a forced continue`, not either half: freezing `main answers {}` alone would
leave `never a forced continue` standing, which is exactly the split-half failure mode that round 2
caught at `:269`. It contains `{}` and an em dash, so `grep -F` is mandatory and a `RegExp` is wrong.
Measured this session: `grep -cF -- 'main answers {} — never a forced continue' HIVE.md` returns `1`,
so the assertion can actually fail.

**Scope: `HIVE.md` is shared with plan 02, in an EARLIER wave.** `01-02-PLAN.md:12` also declares
`HIVE.md`, scoped in its own frontmatter comment to *"the §5 hook-socket paragraph ONLY (`:191-194`)"*
— the `HIVE_SOCK_TOKEN` prose that follows the diagram — and it runs in **wave 2**, one wave before
this plan. So plan 02's edit will already be in the tree when this plan starts: it is not an intruder
and **must not be reverted, reformatted, or "restored"**. The two regions are disjoint — the diagram
this plan's twelfth denial sits in ends at the closing fence `HIVE.md:189` (fences at `:171` and
`:189`, verified this session), and plan 02's paragraph starts at `:191`. Re-derive both by content
match before editing; plan 02's edit will have shifted line numbers. No plan other than 02 and 07
declares `HIVE.md` (plan 23 only greps the doc set at `01-23-PLAN.md:290`; it does not modify it), so
the full twelve-literal set is this plan's and nowhere else's.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@.planning/codebase/ARCHITECTURE.md
@docs/adr/0001-one-gate-for-pty-writes.md
@docs/message-queue.md

<interfaces>
`DeliveryDeps` — `src/main/delivery.ts:54-79`. Every dep is a plain function:
`liveAgents: () => LiveAgentPty[]`, `inbox: (agentId) => DeliverableMessage[]`,
`write: (ptyId, data) => {ok, error?}`, `paused: (agentId) => boolean`,
`emit: (channel, payload) => void`, `log?`, `now?: () => number`,
`sleep?: (ms) => Promise<void>` — the `now`/`sleep` pair exists specifically so a test can drive the
whole loop on a fake clock. Wired at `:153`: `constructor(private readonly deps: DeliveryDeps) {}`.
The wiring site in main is `src/main/index.ts:455-465`.

The renderer code being moved: `src/renderer/src/hooks/useHive.ts:704-746` — effect "2e", a
`setInterval(QUIESCE_POLL_MS)` that calls `window.cth.listPtys()` and flips `working` → `idle` after
`QUIESCE_IDLE_MS`. It reads `p.lastOutputAt` (main already tracks it) and consults `breakerLevel` and
`bootGraceUntil` (main has both). `useHive.ts:693` records why the backstop exists at all:
`usePtyParser` only parses the MOUNTED terminal.

The TWELVE stale `HIVE.md` claims — **all anchors, sections and counts re-measured against current
HIVE.md this session** (line numbers drift; the fixed strings are what identify a denial). Section
starts, measured with `grep -n "^## " HIVE.md`: §1 `:16`, §2 `:36`, §3 `:107`, §4 `:140`, §5 `:169`,
§6 `:198`, §7 `:217`, §8 `:264`, §9 `:277`. The `Section` column below is derived from those bounds —
an earlier revision of this table mis-attributed three rows (`:125` is §3, not §7; `:223` and `:228`
are §7, not §8), which is why the section is now a column rather than a prose aside:

| # | Anchor | Section | Freeze literal (`grep -cF`, baseline `1`) | Stale text | Why it is false |
|---|---|---|---|---|---|
| 1 | `HIVE.md:124-126` | §3 on-disk layout (`:107`-`:139`) | `nothing calls that` | *"only `drainForStop()` advances it and nothing calls that today"* | `drainForStop` IS called — `src/main/index.ts:455` via `DeliveryService.deps.drain`, wired at `:467` — and it advances the cursor at `src/main/hive.ts:1243-1253`. |
| 2 | `HIVE.md:223` | §7 phased plan (`:217`-`:263`) | `shipped, but not as planned` | *"Phase 1 — Autonomy ⚠️ shipped, but not as planned … `Stop` returns `{}`"* | `hooks.ts:332` calls `drainAtStop` at the Stop boundary and returns `{decision:'block', reason}` when mail is present. |
| 3 | `HIVE.md:269` | §8 risk table (`:264`-`:276`) | `Moot today` | *"Infinite Stop-hook loop \| Moot today — `Stop` always answers `{}`"* | Same. |
| 4 | `HIVE.md:271` | §8 risk table | `never advanced` | *"`cursor.json` is seeded but never advanced; the live dedup is the renderer's per-session set"* | Cursor advances (`hive.ts:1253`); the dedup set lives in main (`delivery.ts` `seenSet`, pruned against the live inbox in `tick()`). |
| 5 | `HIVE.md:85-88` | §2 decision 5 (`:36`-`:106`) | `**Reversed` | decision 5 struck through (`~~…~~`) and closed with *"**Reversed — see below.**"* | The decision was not reversed. The UNGUARDED drain was removed; the drain itself ships behind `paused()`/`vetoed()` (`delivery.ts:216`) and is called at `hooks.ts:332`. |
| 6 | `HIVE.md:90` | §2 decision 5 | `answers **every**` | *"`hooks.ts` now answers **every** `Stop`/`SubagentStop` with `{}`"* | It answers `{}` only when the drain returns no mail; with mail pending it returns `{decision:'block', reason}` (`hooks.ts:332-337`). |
| 7 | `HIVE.md:91` | §2 decision 5 | `never forces a continuation` | *"it notifies and emits, and never forces a continuation"* | A block+reason IS a forced continuation; that is the documented Claude/Codex contract quoted in `hooks.ts:335-336`. |
| 8 | `HIVE.md:93-94` | §2 decision 5 | `nothing in the app calls it` | *"`HiveManager.drainForStop()` still exists and still works, but nothing in the app calls it"* | `src/main/index.ts:455` calls it inside `deps.drain`; `:467` passes `delivery.drainAtStop` into `HookServer`. |
| 9 | `HIVE.md:96-103` | §2 decision 5 | `per-renderer-session` | *"dedup is a per-renderer-session set of message ids, **not** the durable `cursor.json`"* | The dedup set lives in main (`delivery.ts` `seenSet`) and the cursor advances at `hive.ts:1253` — neither dies with the window. |
| 10 | `HIVE.md:228` | §7 phased plan | ``` `Stop` returns `{}` ``` | *"The **`Stop`-loop did not**: `Stop` returns `{}` and inbox delivery is the renderer's idle-only nudge instead"* | False against `src/main/hooks.ts:332-337`: the Stop boundary calls `drainAtStop` and returns `{decision:'block', reason}` whenever the drain has mail. Delivery is also main's `tick()`, not the renderer's idle-only nudge. |
| 11 | `HIVE.md:269` | §8 risk table | ``` always answers `{}` ``` | *"Infinite Stop-hook loop \| Moot today — `Stop` always answers `{}` (§2.5)"* | Same falsehood, and it is the SECOND half of the `:269` row: deleting the frozen literal `Moot today` on its own leaves *"`Stop` always answers `{}`"* standing, so the row still denies the feature. |
| 12 | `HIVE.md:184` | §5 control-flow diagram (`:169`-`:197`), inside the fenced block `:171`-`:189` | `main answers {} — never a forced continue` | *"agent C finishes its current turn → Stop hook fires / hook POSTs to the hive socket; main answers {} — never a forced continue"* | False against `src/main/hooks.ts:332-337`: with mail pending the Stop boundary returns `{decision:'block', reason}`, which IS a forced continuation. Matches none of literals 1-11, so the eleven-literal gate left §5's picture denying the feature. Frozen as the whole clause, not either half — see the round-3 scope correction above. |

Denials 5-9 all live in §2 decision 5 (`HIVE.md:85-103`) and were NOT in this plan's earlier scope.
They are the load-bearing ones: a reader who reaches decision 5 is told the feature was reversed and
is dead before ever reaching §7's table. Correcting the later sections without §2 — or without §5's
diagram, which a reader meets even earlier — leaves the contradiction intact.
By section the twelve are: **five** in §2 decision 5, **one** in §3 (`:125`), **one** in §5's diagram
(`:184`), **two** in §7 (`:223`, `:228`), **three** in §8 (`:269` ×2, `:271`). Any prose in a SUMMARY
or commit message that says "the six in §7/§8" is repeating the corrected mis-attribution — the §7/§8
count is five, and the §3 and §5 denials are their own.

**Scope correction (red-team round 2) — denials 10 and 11.** The nine-literal freeze set passed over
the plainest false claim in the file. `HIVE.md:228` says *"`Stop` returns `{}`"* and matches **none**
of the nine literals, so a gate built on those nine leaves it standing. It is false against
`src/main/hooks.ts:332-337`, which returns `{decision:'block', reason}` when the drain has mail.
`HIVE.md:269` has the same shape: the frozen literal there is `Moot today`, and deleting only that
leaves *"`Stop` always answers `{}`"* — the actually-false half — untouched. Both were measured at
exactly `1` this session, so both assertions can fail. Freezing a literal that sits NEXT TO the false
claim rather than ON it is the failure mode this correction closes; that took the freeze set to eleven,
and round 3's §5 denial takes it to **twelve**.
Note for the executor: denials 10, 11 and 12 contain backticks and/or `{}`, so in the JS array they
must be written with quote delimiters (not template literals) and compared with `includes`, never a
`RegExp` — `{}` is a quantifier to a regex engine. Denial 12 also contains an em dash (`—`, U+2014),
copied byte-for-byte from `HIVE.md:184`; a hyphen-minus in its place makes the assertion permanently
green.

`.planning/codebase/ARCHITECTURE.md` describes all of this **correctly** at `:56`, `:201`, `:307`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify every FLOOR-02 clause against current source, confirm the Stop-drain is live, and re-read all twelve HIVE.md anchors</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/index.ts:450-475` — the `DeliveryService` wiring, `deps.drain`, `drainAtStop`, and `focusAgent`.
    - `src/main/hooks.ts:320-345` — the Stop boundary and the `drainAtStop?.()` call.
    - `src/main/delivery.ts:200-240` — `drainAtStop` and its `paused()` / `vetoed()` guards; plus `tick()` and the `seenSet`.
    - `test/delivery-main.test.cjs` in full — the `harness()` of plain-function fakes on a fake clock at `:21-53`, and the four existing guard tests at `:117-142`.
    - `src/renderer/src/hooks/useHive.ts:690-750` — effect 2e and the comment at `:693`.
    - `HIVE.md` at all twelve anchors above, read in context (they are blocks, not single lines). Read `HIVE.md:85-103` — decision 5 — **in full**, including the strikethrough at `:85-87` and the whole indented block-quote at `:90-103`; five of the twelve denials are in there and they are prose, not table rows. Read `HIVE.md:169-197` — §5 in full — as well: denial 12 sits at `:184` INSIDE the ASCII diagram fenced at `:171`-`:189`, and plan 02 (wave 2) has already edited the prose paragraph at `:191-194` in the same section. Confirm plan 02's edit is present and record it as expected, not as drift.
    - `.planning/phases/01-finish-the-floor/01-02-PLAN.md:12` and its SUMMARY — the other declared writer of `HIVE.md`, one wave earlier, scoped to `:191-194`. Its landed edit must survive this plan.
    - `.planning/codebase/ARCHITECTURE.md:50-60`, `:195-210`, `:300-315`.
  </read_first>
  <action>
    Quote issue #5's acceptance text and ROADMAP criterion 1 verbatim, and run one command per clause
    with ACTUAL output pasted:
    - `grep -n "drainAtStop\|drainForStop" src/main/*.ts` — the evidence the Stop-drain is LIVE. Paste it. This clause is ALREADY SATISFIED; do not restore, do not delete.
    - `grep -n "cursor.json\|lastProcessed" src/main/hive.ts` — the evidence the cursor advances.
    - `grep -n "QUIESCE" src/renderer/src/hooks/useHive.ts` — the backstop still in the renderer. Expect hits; this is the open clause.
    - Run this loop and paste its whole output — it is the baseline for all twelve denials:
      ```
      for s in 'nothing calls that' 'shipped, but not as planned' 'Moot today' 'never advanced' '**Reversed' 'answers **every**' 'never forces a continuation' 'nothing in the app calls it' 'per-renderer-session' '`Stop` returns `{}`' 'always answers `{}`' 'main answers {} — never a forced continue'; do printf '%-48s %s
' "$s" "$(grep -cF -- "$s" HIVE.md)"; grep -nF -- "$s" HIVE.md; done
      ```
      **Measured this session, every one of the twelve returns exactly `1`.** Current line numbers:
      `**Reversed` `:88`, `answers **every**` `:90`, `never forces a continuation` `:91`,
      `nothing in the app calls it` `:94`, `per-renderer-session` `:101`, `nothing calls that` `:125`,
      `main answers {} — never a forced continue` `:184`,
      `shipped, but not as planned` `:223`, `` `Stop` returns `{}` `` `:228`,
      `Moot today` `:269`, `` always answers `{}` `` `:269`, `never advanced` `:271`.
      Line numbers here are informational pointers only — re-derive each denial by content match from
      the pasted `grep -nF` output before editing. They are never assertion keys; the frozen literal is.
      Any literal that comes back `0` has already drifted — re-locate the denial in HIVE.md and record
      the replacement literal WITH its measured count before touching anything, because a literal that
      is already `0` is a permanently-green assertion, not a gate.
      Three traps, all real: (a) denial 1 is **line-wrapped** inside a fenced block at `HIVE.md:124-126`
      (`…and nothing calls that` / `today, so it stays…`), so the longer form `nothing calls that today`
      matches **0** lines and is unusable; (b) use `grep -F` throughout — `answers **every**` and
      `**Reversed` contain `*`, and denials 10-12 contain backticks and `{}`, none of which a regex grep
      reads literally; (c) denial 12's em dash is U+2014 — copy the literal from the `grep -nF` output,
      never retype it, or the assertion is green before any work is done.

    Also record the SECTION each denial falls in, derived from `grep -n "^## " HIVE.md` (paste that
    too), and check it against the `Section` column in the table above. An earlier revision of this
    plan attributed `:125` to §7 and `:223`/`:228` to §8; both were wrong, and a SUMMARY repeating them
    would misdescribe what was edited.

    Then confirm all twelve anchors above against the pasted output and record any further drift. If a
    claim's text differs from the table, current source and current HIVE.md win — record the delta
    rather than editing blind.

    Finally, state explicitly in the notes which FLOOR-02 clauses this plan closes (quiesce + docs)
    and which plan 08 closes (queue-drain), so the checker does not read a partial requirement as a
    partial fix.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all four greps.
    - The `drainAtStop` grep output is pasted and non-empty, and the notes state ALREADY-SATISFIED for criterion 1's Stop-drain branch.
    - Task notes contain all twelve HIVE.md stale claims with their current line numbers, their SECTION derived from the pasted `grep -n "^## " HIVE.md` output, AND their measured `grep -cF` counts, and every count is `1` (or a drifted replacement literal is recorded with its own non-zero count). The section tally recorded must be five §2 / one §3 / one §5 / two §7 / three §8.
    - Task notes state explicitly that denials 5-9 live in `HIVE.md:85-103` (§2 decision 5) and denial 12 at `HIVE.md:184` lives in §5's fenced diagram, and that both are in this plan's scope.
    - Task notes record plan 02's landed `HIVE.md` edit (§5's `:191-194` hook-socket paragraph, wave 2) as **expected and to be preserved**, not as drift: `grep -c "per-spawn" HIVE.md` returns at least `1` (plan 02's own criterion; baseline before wave 2 was `0`). If it returns `0`, plan 02 has not landed — stop and confirm the wave ordering before editing a file whose other writer is still pending.
    - **Recorded count baselines. Task 2's criteria are deltas against these**, so they must be pasted here or they are unrecoverable once the edit lands:
      `grep -cE "setInterval" src/main/index.ts` — measured `20` on `main` this session. Label the number THIS task measures **B-setinterval**, and paste it: plan 02 (wave 2) also edits `src/main/index.ts`, so if the number differs from `20`, the number measured here is B-setinterval and task 2 asserts against it. Paste both numbers and say which one task 2 uses.
    - Baseline TAP counters for the delivery test, pasted verbatim:
      `TAP=$(mktemp); node --test --test-reporter=tap test/delivery-main.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured this session: `EXIT=0`, `# tests 16`, `# pass 16`, `# fail 0`, `# skipped 0`, `# todo 0`. Label the `# pass` number **B-delivery**. Write the TAP through `mktemp`, never into the repo — `*.tap` is not in `.gitignore`.
    - The four existing guard tests at `:117-142` are named in the notes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/delivery-main.test.cjs</automated>
  </verify>
  <done>The Stop-drain is proven live with pasted evidence, the open clause is isolated to the quiesce backstop and the docs, and all twelve stale claims have current anchors, correct sections and non-zero baselines.</done>
</task>

<task type="auto">
  <name>Task 2: Move the idle-quiesce backstop into the delivery tick and cover it on a fake clock</name>
  <files>src/main/delivery.ts, src/main/index.ts, src/renderer/src/hooks/useHive.ts, test/delivery-main.test.cjs</files>
  <read_first>
    - `src/main/delivery.ts` in full — `DeliveryDeps` at `:54-79`, the constructor at `:153`, and `tick()`. This is the file being modified.
    - `src/main/index.ts:450-470` — the wiring site; note how `deps.drain` folds a main-side computation into a dep rather than leaking it into `index.ts`.
    - `src/renderer/src/hooks/useHive.ts:704-746` — the effect being deleted, including `QUIESCE_POLL_MS`, `QUIESCE_IDLE_MS`, and its reads of `breakerLevel` and `bootGraceUntil`.
    - `test/delivery-main.test.cjs:21-53` — the `harness()` idiom to extend.
    - `01-PATTERNS.md` § "2. The DI-config-object idiom" `:541-583` — extend the existing interface, do not add a second one.
  </read_first>
  <action>
    Add the quiesce backstop as a **branch inside `DeliveryService`'s existing tick**, not as a new
    timer in `index.ts`. `delivery.ts` already owns a tick and already receives `now`, `sleep` and
    `liveAgents`, which is exactly why this is the right tier.

    Extend `DeliveryDeps` with the inputs the renderer effect consulted and main already has:
    a way to read each live agent's `lastOutputAt` (add it to `LiveAgentPty` if it is not already
    there), the current breaker level for an agent, the boot-grace deadline, and a way to write the
    agent's status back to `working`/`idle`. Every one is a plain function, matching the file's
    convention — this is what makes it drivable on a fake clock.

    Port `QUIESCE_POLL_MS` and `QUIESCE_IDLE_MS` as module constants in `delivery.ts` with the
    reasoning comment from `useHive.ts:693` carried over: the backstop exists because `usePtyParser`
    only parses the MOUNTED terminal, so an unmounted agent's transition to idle is otherwise never
    observed. Cite #5.

    In `src/main/index.ts`, wire the new deps at the existing `:455-465` site. Add **no new
    `setInterval`** in `index.ts` — the whole point of the seam is that the backstop rides the tick
    that already exists.

    In `src/renderer/src/hooks/useHive.ts`, delete effect 2e entirely, including its interval and its
    constants. Leave the renderer's veto/draft signal path alone — per the responsibility map the
    renderer keeps exactly one job here, and it is reporting the human's draft up as a veto.
    **Do not touch the queue-drain effect (#4, `:819-968`)** — that is plan 08's, next wave.

    Extend `test/delivery-main.test.cjs`: an agent with `lastOutputAt` older than `QUIESCE_IDLE_MS`
    flips `working` → `idle` on the tick, with **no window attached** (`emit` is a no-op when no
    webContents exists — that is precisely the "survives the window" property). Cover the two
    suppressors: an agent inside boot grace does not flip, and an agent at a constrained breaker level
    behaves as the renderer effect did. Drive the whole thing on the harness's fake clock — zero
    real elapsed time.
  </action>
  <acceptance_criteria>
    - `grep -c "QUIESCE" src/renderer/src/hooks/useHive.ts` returns `0`.
    - `grep -c "QUIESCE" src/main/delivery.ts` returns at least `2` — **D-38(a)**'s first half: the idle-quiesce backstop now runs in main.
    - `grep -cE "setInterval" src/main/index.ts` returns **exactly B-setinterval — the number task 1 recorded and pasted, measured `20` on `main` this session**. "The same value as before this task" is unfalsifiable once the edit has landed, so the number is written down in task 1 first: no new timer was added to `index.ts`. Paste task 1's number next to this one; if they differ this criterion FAILS, and equality with `20` alone is not the gate (plan 02 edits this file one wave earlier).
    - `TAP=$(mktemp); node --test --test-reporter=tap test/delivery-main.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least `19`** (B-delivery = `16` plus this task's three), **`# fail 0`**, **`# skipped 0`**, `# todo 0`, and the four pre-existing guard tests at `:117-142` still passing.
      Exit code plus a test count is not a gate on its own: `node --test` counts skipped tests in `# tests` and still exits `0` — verified this session, two `{skip: '...'}` tests, one holding a live `assert.fail()`, report `# tests 2 / # pass 0 / # fail 0 / # skipped 2` at `EXIT=0`. Three `{skip}` stubs would satisfy "exit 0, three more tests" exactly. The `# pass` floor plus `# skipped 0` is what forbids it. Name the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches.
    - `npm run typecheck` exit code is `0`.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/delivery-main.test.cjs</automated>
  </verify>
  <done>The backstop runs in main's tick with no window attached, driven by injected deps and covered on a fake clock; the renderer effect is gone and the Stop-drain is untouched.</done>
</task>

<task type="auto">
  <name>Task 3: Delete all twelve of HIVE.md's stale denials and pin every one with a repo-fact test</name>
  <files>HIVE.md, test/repo-claims.test.cjs</files>
  <read_first>
    - `HIVE.md` at the twelve anchors from task 1, read as whole blocks — `:124-126` is the tail of a three-line block, `:184` is one line of an ASCII diagram fenced at `:171`-`:189`, `:223-230` is a phase note, and `:85-103` is a whole numbered decision with a strikethrough and a multi-paragraph block-quote.
    - `.planning/codebase/ARCHITECTURE.md:56`, `:201`, `:307` — the correct description, which is the text HIVE.md must stop contradicting.
    - `test/repo-claims.test.cjs` in full — created by plan 05 in wave 2; read its header, its `stripComments` helper and its existing assertions before appending.
    - `01-PATTERNS.md` § "`test/repo-claims.test.cjs`" `:66-156` — the assertion-message convention.
  </read_first>
  <action>
    Delete all TWELVE stale claims from `HIVE.md`. Where a claim sits inside a larger sentence or table
    row, rewrite that row to say what source actually does now, citing the file:line — do not leave a
    dangling fragment. Specifically: `cursor.json` advances via `drainForStop` (`hive.ts:1253`); the
    Stop boundary calls `drainAtStop` (`hooks.ts:332`) and returns `{decision:'block', reason}` when
    mail is present; the dedup set lives in main (`delivery.ts` `seenSet`), not in a renderer
    per-session set lost on reload.

    **§5's diagram line (`HIVE.md:184`) — denial 12 — is a rewrite of one diagram row.** It currently
    reads `hook POSTs to the hive socket; main answers {} — never a forced continue`. Replace it with
    what `hooks.ts:332-337` does: the hook POSTs to the hive socket, main runs the guarded drain, and
    unread mail comes back as `{decision:'block', reason}` — the agent's own turn continues — while an
    empty or vetoed drain answers `{}`. Keep the diagram's column width and box-drawing characters
    intact; it is a fenced ASCII block and a reflowed line is a diff nobody can read. The following
    line's *"C goes idle; the renderer sees idle + unread inbox → enqueues a nudge"* stays: the
    renderer nudge is still a real path, it is just no longer the only one.

    **Do not touch `HIVE.md:191-194`** — the `HIVE_SOCK_TOKEN` paragraph immediately after the same
    fence. Plan 02 rewrote it in wave 2 (`01-02-PLAN.md:12`) and its edit is expected to be in the tree.
    Same section, different block; leave it exactly as plan 02 left it.

    **§2 decision 5 (`HIVE.md:85-103`) — denials 5-9 — needs a rewrite, not a line delete.** As it
    stands the whole decision is struck through (`~~…~~` at `:85-87`) and closed with *"**Reversed —
    see below.**"*, then the block-quote at `:90-103` spells the reversal out. Un-strike the decision
    and rewrite the block-quote to state what actually ships: the drain runs at the `Stop` boundary
    (`hooks.ts:332`) through `DeliveryService.drainAtStop` (`delivery.ts:216`), which returns
    `{decision:'block', reason}` only after `paused()` and `vetoed()` both clear — so the HITL and
    terminal-draft gates that motivated the original removal are still honoured, and the drain works
    with no window attached. Keep the historical note that the UNGUARDED version was removed and why;
    that part is true and is the reason the guards exist. Keep the renderer-side nudge path
    description (`useHive.ts` effects #3/#4, ADR-0001) — the renderer is still a delivery path, it is
    just no longer the ONLY one — but correct the dedup sentence: the durable dedup is main's
    `seenSet` plus `cursor.json`, not a per-renderer-session set lost on reload.

    Do not "improve" the rest of `HIVE.md`. Twelve claims, decision 5's block-quote, §5's one diagram
    row, and any row made incoherent by removing them — and nothing in `:191-194`.

    Then append FLOOR-02's clause to `test/repo-claims.test.cjs` — **this plan is wave 3's sole owner
    of that file.** Assert that `HIVE.md`, after `stripComments`, contains **none of all twelve** stale
    strings. Drive it from a single array so no denial can be quietly dropped (section attributions
    below are derived from `grep -n "^## " HIVE.md`: §2 `:36`, §3 `:107`, §5 `:169`, §7 `:217`,
    §8 `:264`):

    ```js
    const STALE_STOP_DRAIN_DENIALS = [
      'nothing calls that',            // HIVE.md:125 — §3 on-disk layout, cursor.json block
      'shipped, but not as planned',   // HIVE.md:223 — §7 phased plan
      'Moot today',                    // HIVE.md:269 — §8 risk table
      'never advanced',                // HIVE.md:271 — §8 risk table
      '**Reversed',                    // HIVE.md:88  — §2 decision 5 struck out
      'answers **every**',             // HIVE.md:90  — §2 "Stop always returns {}"
      'never forces a continuation',   // HIVE.md:91  — §2
      'nothing in the app calls it',   // HIVE.md:94  — §2 drainForStop "uncalled"
      'per-renderer-session',          // HIVE.md:101 — §2 dedup denial
      '`Stop` returns `{}`',           // HIVE.md:228 — §7 phased plan, second half
      'always answers `{}`',           // HIVE.md:269 — §8 risk table, second half
      'main answers {} — never a forced continue' // HIVE.md:184 — §5 control-flow diagram
    ];
    ```

    Each of the twelve measured exactly `1` in HIVE.md before this task, so each assertion can actually
    fail. Compare with `String.prototype.includes` (a plain substring test), **not** a `RegExp` — two
    of the twelve contain `*` (`**Reversed`, `answers **every**`) and three more contain `{}`
    (`` `Stop` returns `{}` ``, `` always answers `{}` ``, `main answers {} — never a forced continue`),
    and both classes are silently mis-parsed as a regex. The `{}` literals also contain backticks or an
    em dash, so write them with quote delimiters in the JS array, never as template literals, and copy
    denial 12's `—` (U+2014) straight from HIVE.md rather than retyping it. **Do not use "nothing calls
    that today":** the denial is line-wrapped at `HIVE.md:124-126`, that form returns `0` before any
    work is done, and baking it into this file would ship a permanently-green assertion in the one
    test whose job is to stop this phase's negative greps from rotting. Likewise do not shorten denial
    12 to `main answers {}` or to `never a forced continue` — either half alone lets a rewrite leave
    the other half standing, which is exactly the failure round 2 caught at `:269`. The assertion
    message must name the contradiction: HIVE.md said the Stop-drain does not run while
    ARCHITECTURE.md says it does, and ROADMAP criterion 1 bans exactly that. Add, verbatim so the
    acceptance grep can find it, `assert.equal(STALE_STOP_DRAIN_DENIALS.length, 12, ...)` alongside —
    shortening the list to make the suite pass then fails the suite instead.

    Add a second assertion in the same file pinning the positive direction: `src/main/index.ts`
    contains a `drainAtStop` wiring and `src/main/hooks.ts` calls it — so a future refactor that
    deletes the live feature fails the suite rather than silently making the deleted docs retroactively
    correct.
  </action>
  <acceptance_criteria>
    - This one command returns `0` for **every** line — it is the gate for all twelve of **D-38(b)**'s stale denials, and it fails against HIVE.md as it stands today because each literal is currently present exactly once:
      ```
      for s in 'nothing calls that' 'shipped, but not as planned' 'Moot today' 'never advanced' '**Reversed' 'answers **every**' 'never forces a continuation' 'nothing in the app calls it' 'per-renderer-session' '`Stop` returns `{}`' 'always answers `{}`' 'main answers {} — never a forced continue'; do printf '%-48s %s
' "$s" "$(grep -cF -- "$s" HIVE.md)"; done
      ```
      Baselines before this task, all measured this session: `nothing calls that` `1` (`:125`), `shipped, but not as planned` `1` (`:223`), `Moot today` `1` (`:269`), `never advanced` `1` (`:271`), `**Reversed` `1` (`:88`), `answers **every**` `1` (`:90`), `never forces a continuation` `1` (`:91`), `nothing in the app calls it` `1` (`:94`), `per-renderer-session` `1` (`:101`), `` `Stop` returns `{}` `` `1` (`:228`), `` always answers `{}` `` `1` (`:269`), `main answers {} — never a forced continue` `1` (`:184`). `-F` is required throughout: two of the twelve contain `*`, three contain `{}`, and one of those also contains an em dash. Do not substitute `nothing calls that today` — the denial is line-wrapped at `HIVE.md:124-126`, so that longer form returns `0` already and the criterion would pass by doing nothing. Do not substitute either half of denial 12 for the same reason in reverse: half of it can be deleted while the other half survives.
    - Denial 12 is frozen as the WHOLE clause in the test file, not a half: `grep -cF -- "main answers {} — never a forced continue" test/repo-claims.test.cjs` returns `1`, and `grep -cF -- "'main answers {}'" test/repo-claims.test.cjs` returns `0`.
    - `grep -cF -- '~~' HIVE.md` returns `0` — decision 5 (`HIVE.md:85-87`) is no longer struck through. Baseline today: `2` (the open and close markers of that one strikethrough; it is the only strikethrough in the file).
    - `grep -c "drainAtStop" src/main/hooks.ts` still returns at least `1` — the live feature was not deleted along with the docs.
    - `grep -cF "STALE_STOP_DRAIN_DENIALS" test/repo-claims.test.cjs` returns at least `3` — the declaration, the loop that uses it, and the length assertion.
    - `grep -cF "STALE_STOP_DRAIN_DENIALS.length, 12" test/repo-claims.test.cjs` returns `1`, and `grep -cF "STALE_STOP_DRAIN_DENIALS.length, 11" test/repo-claims.test.cjs` returns `0`. Combined with the suite exiting `0` below, that is what proves the list still holds twelve literals: trim it to eleven to make the suite green and the length assertion fails instead.
    - Plan 02's landed `HIVE.md` edit survived this task: `grep -c "per-spawn" HIVE.md` returns at least `1` — the same number task 1 recorded, pasted alongside. A `0` here means this plan reverted an earlier wave's work while rewriting §5.
    - `TAP=$(mktemp); node --test --test-reporter=tap test/repo-claims.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, **`# pass` at least `2` higher than the `# pass` number plan 05's SUMMARY recorded as B-repo-claims, and at least `5` in absolute terms** (plan 05's three plus this task's two), **`# fail 0`**, **`# skipped 0`**, `# todo 0`. Paste plan 05's recorded number alongside the new one.
      The `# pass` floor and `# skipped 0` are the gate, not the exit code: `node --test` counts skipped tests in `# tests` and still exits `0` (verified this session), so "exit 0 with two more tests" is satisfied by two `{skip: '...'}` stubs — including one holding a live `assert.fail()`. Name the TAP reporter explicitly; the default (spec) reporter prints `ℹ skipped 0`, which no `^# ` grep matches. Write the TAP through `mktemp` — `*.tap` is not in `.gitignore`.
    - Containment, path-scoped to this plan's own declared files (never a bare whole-tree `git diff`/`git status` — `use_worktrees: false` means wave-mates are editing this same tree): `git diff --name-only -- HIVE.md test/repo-claims.test.cjs` lists exactly those two paths, and `git diff --name-only -- .planning/codebase/ARCHITECTURE.md docs/message-queue.md` prints nothing.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>No doc in the repo promises the Stop-drain does not run — §2 decision 5 and §5's control-flow diagram included, not just §7/§8 — the live feature is pinned in the same test, plan 02's paragraph is untouched, and HIVE.md no longer contradicts ARCHITECTURE.md.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer → main delivery decisions | Autonomy that lives in the renderer dies with the window. Anything moved to main crosses from "the human is watching" to "nothing is watching", which is the trust property this phase exists to establish. |
| main → agent PTY (a write into a terminal) | ADR-0001's one-gate rule governs anything that types into a terminal; the quiesce backstop must not become a second writer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P07-01 | Denial of service | an agent stuck `working` forever with the window closed | mitigate | The quiesce backstop moves into `delivery.ts`'s tick, where it runs with no window attached; covered by a fake-clock test that asserts the flip with `emit` a no-op. |
| T-P07-02 | Tampering | the backstop becoming a second PTY writer | mitigate | It only flips status; ADR-0001's one-gate rule is preserved because the backstop writes no terminal bytes. State this in the code comment. |
| T-P07-03 | Repudiation | docs asserting a live security-relevant path is dead | mitigate | Delete all TWELVE of HIVE.md's denials — five in §2 decision 5 (`:85-103`), one in §3 (`:125`), one in §5's diagram (`:184`), two in §7 (`:223`, `:228`), three in §8 (`:269` ×2, `:271`) — and pin both directions in `test/repo-claims.test.cjs`: absence of every stale string AND presence of the live wiring. |
| T-P07-06 | Repudiation | a partial doc fix: prose corrected while §5's diagram — the picture a reader reaches first — still shows `main answers {} — never a forced continue`, or §2 decision 5 still says the drain was reversed and is uncalled | mitigate | The twelve-literal gate and the `~~` strikethrough gate are single commands covering every region, each literal frozen ON its claim and (for `:269` and `:184`) as the whole clause rather than a half, and the repo-fact test asserts the literal list is still twelve long, so trimming the list fails the suite. |
| T-P07-07 | Tampering | this plan clobbering plan 02's landed `HIVE.md:191-194` edit while rewriting the adjacent §5 diagram, because an earlier revision claimed no other plan declared the file | mitigate | The scope justification now names `01-02-PLAN.md:12` and its `:191-194` region, task 1 records plan 02's edit as expected, task 3 forbids touching it, and `grep -c "per-spawn" HIVE.md` is asserted non-zero after the edit. |
| T-P07-04 | Denial of service | deleting the working Stop-drain because the roadmap called it dead | mitigate | Task 1's pasted `drainAtStop` grep is a hard gate before any edit; the plan states explicitly that this clause is already satisfied and must not be touched. |
| T-P07-05 | Repudiation | a quiesce flip that the operator cannot see because no window is attached | accept | `emit` is a documented no-op with no webContents; the flip is still recorded in the hive log. Surfacing it to a closed window is DAEMON-01's problem (Phase 2), not this phase's. |
</threat_model>

<verification>
- `npm test` green **on the phase's draft PR to `main`**, not merely locally: `.github/workflows/ci.yml`
  fires only on `branches: [main]` for both `push` (`:5`) and `pull_request` (`:7`), so a push to the
  phase branch triggers no run at all. Evidence is `gh pr checks <phase-pr-number>` on the draft PR
  plan 01 opens in wave 1, showing `pass` for the check runs named `Test (ubuntu-latest)`,
  `Test (windows-latest)` and `Test (macos-latest)` — the real job names from `ci.yml:39`
  (`name: Test (${{ matrix.os }})`). Paste the rows.
- `npm run typecheck` green locally, and the check run named `Typecheck` (`ci.yml:17`) `pass` on the
  same PR.
- The **phase PR's Electron smoke job green**. The check run is named **`Electron smoke (ubuntu-latest)`**
  (`e2e.yml:30`) — `E2E` is the WORKFLOW name (`e2e.yml:1`), not a check run, so `gh pr checks` never
  prints a row called `E2E` and a claim written against that name can never be satisfied. Per
  `01-VALIDATION.md` the boot-time delivery path is one of the two waves that additionally warrant the
  e2e run. It is **not** a local `npm run e2e`, which cannot run on this host and which no plan in this
  phase makes runnable. Re-verified this session: `npm run e2e` resolves to `playwright test`
  (`package.json:27`) and `node_modules/@playwright` does not exist — it is in `package-lock.json` but
  was never installed, because the only install that has ever succeeded here is `--ignore-scripts`,
  which is also why `node_modules/better-sqlite3/build` and `node_modules/node-pty/build/Release` are
  both absent. `e2e/smoke.spec.ts` additionally requires `npm run build` first, and
  `.github/workflows/e2e.yml` triggers only on `main` (`:12` push, `:14` pull_request) — a push to the
  phase branch produces no run at all. So the evidence is the draft PR to `main` that plan 01 opens in
  wave 1: paste the `gh pr checks` row named `Electron smoke (ubuntu-latest)`. If that PR is not open
  yet, write `MEASUREMENT UNAVAILABLE — phase PR to main not open` rather than reporting a green that
  never ran. If `gh pr checks` prints no row by that name, do not substitute a differently-named row —
  re-derive the job name from `.github/workflows/e2e.yml` and record the delta.
- Manual: with the dev app running, close the window (not quit) and confirm an agent that goes idle
  is still woken. This is criterion 1 in operator-legible form.
</verification>

<success_criteria>
- ROADMAP criterion 1's backstop and doc clauses are TRUE: an agent that goes idle mid-queue is
  still woken with the window closed, and `grep` finds no doc promising a code path that does not run
  — checked across ALL TWELVE stale literals: five in §2 decision 5, one in §3, one in §5's
  control-flow diagram, two in §7, three in §8.
- Plan 02's `HIVE.md:191-194` edit from wave 2 is still in the file (`grep -c "per-spawn" HIVE.md`
  non-zero) — the shared file was extended, not reverted.
- The Stop-drain is neither restored nor deleted — it was already live under a guard, with pasted
  evidence.
- Issue #5's clauses closed by this plan are closed in this PR per D-43; the queue-drain clause
  remains open and is explicitly named as plan 08's, so a partial requirement is never read as done.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-07-SUMMARY.md` when done. It MUST state which FLOOR-02
clauses this plan closed and which remain open for plan 08, list the `DeliveryDeps` fields added, and
record all twelve HIVE.md deletions with their pre-edit line numbers, their SECTION (five §2 / one §3 /
one §5 / two §7 / three §8 — do NOT write "the six in §7/§8", which is the corrected mis-attribution)
and pre-edit `grep -cF` counts, plus the before/after of §2 decision 5 and of §5's diagram row at
`:184`, and a line confirming plan 02's `:191-194` paragraph was left untouched.
</output>
