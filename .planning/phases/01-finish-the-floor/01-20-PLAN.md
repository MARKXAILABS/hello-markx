---
phase: 01-finish-the-floor
plan: 20
type: execute
wave: 7
depends_on: ["01-14"]
files_modified:
  - src/renderer/src/components/UpdateToast.tsx
  - src/renderer/src/components/FileTree.tsx
  - src/renderer/src/components/AgentDetailPanel.tsx
  - src/renderer/src/components/AgentControlStrip.tsx
  - src/renderer/src/components/ToolWaterfall.tsx
  - src/renderer/src/components/QuitWarningModal.tsx
  - src/renderer/src/components/FullscreenFileEditor.tsx
  - src/renderer/src/components/UpdatesSection.tsx
  - src/renderer/src/components/CodeEditor.tsx
  - src/renderer/src/components/ErrorBoundary.tsx
  - src/renderer/src/components/CommandBar.tsx
  - src/renderer/src/components/BlockedBanner.tsx
  - src/renderer/src/components/TerminalView.tsx
  - src/renderer/src/components/RecentText.tsx
  - src/renderer/src/components/RealtimeMichaelToggle.tsx
  - src/renderer/src/components/PtyTerminalView.tsx
  - src/renderer/src/components/UpdateBadge.tsx
  - src/renderer/src/components/SidebarTabs.tsx
  - src/renderer/src/components/ReleaseDrop.tsx
  - src/renderer/src/components/Modal.tsx
  - src/renderer/src/components/MichaelBooting.tsx
  - src/renderer/src/realtime/DevicePicker.tsx
  - src/renderer/src/realtime/CompletionToast.tsx
  - src/shared/releaseDrop.ts            # task 3 only — 11 sub-14px CSS declarations in the release-drop page this plan's ReleaseDrop.tsx renders. Invisible to M1/M1d/M1x (src/shared, CSS in a template literal, no `fontSize` token). No plan declared it before red-team round 3.
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No user-facing text in the remaining 23 renderer files sits below the 14px floor except allowlisted decorative glyphs"
    - "Every icon-only button in these 23 files has an accessible name; no button with visible text gained an aria-label"
    - "The hoisted-object rule is applied at this pair's verified case: FullscreenFileEditor's `chip` declaration is swept and only the glyph consumer's local override is allowlisted"
    - "The components plan 22 will render to static markup keep their DOM structure"
    - "Nothing was reflowed, moved, dropped, or held below 14px to make room"
    - "ReleaseDrop's two decimal sizes — invisible to M1 and to plan 23's bar — are swept, and the terminal carve-out is applied only to xterm's own font, not to terminal-adjacent chrome"
  artifacts:
    - path: "src/renderer/src/components/FullscreenFileEditor.tsx"
      provides: "the phase's one verified mixed-consumer hoisted style object, swept with a local glyph override"
      contains: "cth-text-"
  key_links:
    - from: "swept fontSize sites"
      to: "src/renderer/src/design/tokens.css"
      via: "var(--cth-text-*) replacing numeric literals"
      pattern: "cth-text-"
---

<objective>
Sweep the remaining 23 renderer files to the corrected type scale and give every icon-only button in
them an accessible name — completing the FLOOR-12 surface.

Purpose: FLOOR-12, sweep sub-group 6 of 6. Measured at plan time and re-verified today: **75 sub-14px
`fontSize` occurrences across 23 files**, led by `UpdateToast.tsx` (6), `FileTree.tsx` (6),
`AgentDetailPanel.tsx` (6) and `AgentControlStrip.tsx` (6), with a long tail of 1-5 site files.
**Occurrences, not lines** — wave 9 counts occurrences (`grep -o`), so this plan does too; for these 23
files the two units are equal today, and the unit is stated beside every pinned number so the two waves
reconcile. Plus **two M1-invisible sites** none of those counts includes: `ReleaseDrop.tsx:65`
`fontSize: 13.5` and `:117` `fontSize: 11.5`.

**Why this plan is one half of a pair.** The remaining-renderer surface is 31 files and ~160 sites.
Classifying all 31 and then editing 23 of them exceeds a single agent's context budget by roughly 2×,
and a sweep that runs out of context mid-file produces a *partial* sweep whose only failure surface is
plan 23's completeness bar in wave 9. So the surface is split on its natural boundary: plan 19 owns
the eight densest files, this plan owns the other 23. The two file sets are disjoint, both run in
wave 7, and neither depends on the other.

After plans 19 and 20 the renderer's components are final, which is why plan 21's lint gate and plan
22's static-render tests come next.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract; its rules are mechanical and checked in order.

Output: 23 files clearing the floor, an accessible-name pass, and this sub-group's frozen Rule 0
allowlist handed to plan 23.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-14-SUMMARY.md
@DESIGN.md

<interfaces>
**Plan 14 has already landed the token migration.** `--cth-text-display-md` is 14px,
`--cth-text-body-sm` and `--cth-text-mono-sm` are 14px, `--cth-text-display-sm` /
`--cth-lh-display-sm` **no longer exist**. Read `01-14-SUMMARY.md` first.

**Sweep rules (UI-SPEC, checked in order):**
- **Rule 0** — decorative glyph on the frozen allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  → `var(--cth-text-display-md)` + `var(--cth-lh-display-md)`. **`fontFamily` UNCHANGED.**
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display`: → `var(--cth-text-body-md)`, or
  `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`, with the matching `--cth-lh-*`
  (mono's is the single `--cth-lh-mono`).
- **Rule 1b and Rule 3 do not apply to this group.**

**Pinned M1. Do not re-derive with a different regex:**
`grep -rnE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file>` to see the sites, and
`grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` to count them.

**Count occurrences (`-o`), never lines (`-c`) — the unit matters.** Wave 9 (plan 23) pins the
completeness bar with `grep -rhoE … | wc -l`, i.e. how many times the trimmed text appears. Measured
today the whole tree is **604 occurrences vs 603 lines**; the single divergence is
`IntegrationsRegistry.tsx:388` (plan 17's file), which carries `fontSize: 12` and `fontSize: 10` on one
physical line. That file is plan 17's; for this plan's 23 files the two units are equal today. Every per-file number in this plan is therefore an occurrence count, so wave 7 and
wave 9 reconcile instead of disagreeing by one.

**M1's blind spots — the sub-14px sites the regex structurally cannot see.** M1 needs a digit
immediately after `fontSize:`, and its trailing `[^0-9.]` guard drops decimals. So a quoted (`'13px'`),
decimal (`12.5`), ternary (`isTopic ? 12 : 11`) or computed (`Math.max(8, …)`) size is invisible to M1 —
and therefore to plan 23's repo-wide bar. Two extra scans close that hole, run over **this plan's paths
only**:
- **M1d — decimal and quoted sub-14 literals:**
  `grep -rnoE "fontSize *[:=] *\{?['\"]?(1[0-3]|[1-9])\.[0-9]+|fontSize *[:=] *\{?['\"](1[0-3]|[1-9])(\.[0-9]+)?px['\"]" <this plan's paths>`
- **M1x — expression-valued sizes:**
  `grep -rnE "fontSize *[:=] *\{?[A-Za-z_\$]" <this plan's paths>`

An M1d hit is an ordinary sub-14px literal and takes the same rule a visible one would. An M1x hit is
recorded with its **evaluated minimum** — the smallest value the expression can return over its inputs —
and a minimum below 14 is a site this plan fixes. **Verified present in this plan's files today: two M1d hits, both in `ReleaseDrop.tsx` — `:65`
`fontSize: 13.5` (the drop's action-button label, `fontFamily: 'inherit'`) and `:117` `fontSize: 11.5`
(the `Hello MarkX · {version}` title). Decimals, so M1's `[^0-9.]` guard drops both; neither is inside
`ReleaseDrop.tsx`'s M1 baseline of 1. Both are Rule 2. Zero M1x hits outside the carve-out below.**

**The terminal carve-out, stated once and applied consistently — this is the sub-group that owns it.**
UI-SPEC's *"terminal sizing is user-controlled and is not governed by `DESIGN.md:706`"* covers **xterm's
own font and nothing else**: `components/terminalFontSize.ts`, `components/terminalPool.ts`, and the
`term.options.fontSize` assignments in `PtyTerminalView.tsx` (`:148`, `:244`, `:303`, reading
`fontSize`/`fontSizeRef` from `:133-134`). Those four M1x hits are the carve-out and are the only
expression-valued sizes in this group; they stay exactly as they are. The carve-out does **not** extend
to app chrome that merely *reads* the zoom — `PtyTerminalView.tsx`'s and `TerminalView.tsx`'s own
buttons, labels and toolbars are ordinary UI and are swept like any other. Stretching the carve-out from
"the terminal's font" to "anything near a terminal" is how a sub-14px site survives the bar.

**This sub-group's Rule 0 exempt glyphs (from UI-SPEC's 30-candidate table):**
`FullscreenFileEditor.tsx` `✕`; `PtyTerminalView.tsx` `−` and `+`; `ReleaseDrop.tsx` `✕`;
`UpdateBadge.tsx` `·`; `UpdatesSection.tsx` `•`; `UpdateToast.tsx` `•`. That is the complete list for
these 23 files. Each gains `aria-hidden="true"` and keeps its size. **None of the four NOT-exempt
cases is in this group** — all four live in plan 17's files.

**Hoisted shared style objects are NOT exempt — this pair's verified case is in this group:**
`FullscreenFileEditor.tsx:59` `const chip` (`fontSize: 12`) serves `:111` `open in IDE` (text) and
`:112` `✕` (glyph). Sweep `chip` normally, give the `✕` element a **local `fontSize` override**, and
put that override's line in the allowlist. Allowlisting `chip`'s declaration would hold `open in IDE`
below the floor with the test green.

**Accessible names — the rule, not the ratio.** Visible text content → **do NOT add `aria-label`**.
Icon/glyph/symbol only → `aria-label` required, verb + object, naming the agent where one is in scope
(`FullscreenTerminal.tsx:665` is the house pattern).

**Containment — UI-SPEC's three steps, in order:** (1) nothing, where `nowrap` + `overflow:hidden` +
`textOverflow:ellipsis` already truncates; (2) raise the container integer by the measured delta and
change nothing else; (3) **stop and report** with file:line and measured overflow.
**Forbidden:** reflowing, moving a field, dropping a field, collapsing a row, or holding a site below
14px to make room.

**Regions that are out of bounds in this group:**
- `AgentDetailPanel.tsx:93`'s `disposeTerminal` call — plan 05's audited drop path.
- `components/terminalFontSize.ts`, `components/terminalPool.ts` and xterm's own `term.options.fontSize`
  — out of the sweep entirely, per the carve-out stated above and nothing wider.
  `PtyTerminalView.tsx` and `TerminalView.tsx` are in scope for their **chrome** only: their buttons,
  labels and toolbars are ordinary UI and are swept like any other file's.

**Preserve every `eslint-disable-next-line` on its exact target line** — `TerminalView.tsx` and
`realtime/CompletionToast.tsx` each carry one of the nine `react-hooks/exhaustive-deps` suppressions
plan 21 turns live. `src/renderer/src/ide/monaco.ts` carries a `@typescript-eslint/no-explicit-any`
disable that plan 21 **deletes** — it is not in this group and must not be touched here.

**Plan 22 renders some of these to static markup.** `ErrorBoundary.tsx`, `BlockedBanner.tsx`,
`SidebarTabs.tsx`, `ToolWaterfall.tsx` and `UpdateBadge.tsx` are candidates. After this plan they are
final; do not change their DOM structure, only sizes and accessible names.

**Plan 19 owns the other 8 files and runs in the same wave.** Its file set is disjoint from this
one. Do not edit any file outside this plan's `files_modified`, and do not run a repo-wide fix — a
cross-set edit in wave 7 is a lost update, because plans 15, 16, 17, 18 and 19 are all
editing the renderer concurrently in the same working tree.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Classify every fontSize site across the 23 files, before editing anything</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — rules, the 30-candidate table, the hoisted-object rule, containment, the completeness bar.
    - `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` — corrected token values and the M1 baseline.
    - `src/renderer/src/components/FullscreenFileEditor.tsx:50-120` in full — `const chip` and both its consumers. This is the group's one worked hoisted-object case.
    - The 23 files at their `fontSize` sites and around every `<button>`. These are small files — the largest is 6 sites — so read each at its sites rather than in full, except `FullscreenFileEditor.tsx`.
  </read_first>
  <action>
    Run the pinned M1 per file and paste the output. Baseline at plan time (75 total, 23 files):
    UpdateToast 6, FileTree 6, AgentDetailPanel 6, AgentControlStrip 6, ToolWaterfall 5,
    QuitWarningModal 5, FullscreenFileEditor 5, UpdatesSection 4, CodeEditor 4, DevicePicker 3,
    CompletionToast 3, ErrorBoundary 3, CommandBar 3, BlockedBanner 3, TerminalView 2, RecentText 2,
    RealtimeMichaelToggle 2, PtyTerminalView 2, UpdateBadge 1, SidebarTabs 1, ReleaseDrop 1, Modal 1,
    MichaelBooting 1 — **as occurrences**. Count with `grep -hoE … | wc -l`, not `grep -cE`: wave 9
    asserts occurrences and the two units diverge wherever one line carries two matches. Record any
    delta from the current numbers.

    Record `git rev-parse HEAD` as this plan's **base sha** — the path-scoped containment checks in
    task 2 are read against it, and five wave-mates are committing to the same tree.

    Then run the two **blind-spot scans** from `<interfaces>` (M1d and M1x) over this plan's 23 paths
    and paste both outputs. Expected today: two M1d hits (`ReleaseDrop.tsx:65` `13.5` and `:117` `11.5`)
    and four M1x hits, all four inside the terminal carve-out (`PtyTerminalView.tsx:133`, `:148`, `:244`,
    `:303` — xterm's own font). Record every M1x hit's **evaluated minimum** and say explicitly which
    ones the carve-out covers and why, rather than leaving it implied.

    Run the M3 glyph predicate scoped to these 23 files with `LC_ALL=C.UTF-8` and paste the result.
    Confirm this sub-group's exempt list and confirm none of the four NOT-exempt cases has moved into it.

    Produce a per-file classification table: every `fontSize` declaration line, its value, the enclosing
    object's family, inline or hoisted, and the rule. Enumerate every hoisted object's consumers —
    `FullscreenFileEditor.tsx`'s `chip` is the known case; confirm whether any of the other 22 files has
    a second one, and name it with both consumer lines if so.

    Inventory every `<button>` and `<PixelButton>` across the 23 files, marked NAMED or ICON-ONLY.

    Record `TerminalView.tsx`'s and `realtime/CompletionToast.tsx`'s `eslint-disable-next-line` line
    numbers, and every fixed-height container integer in the group.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted per-file M1 output for all 23 files with a total.
    - Task notes contain the pasted M3 output scoped to these files and the confirmed exempt list.
    - Task notes contain the per-file rule classification table and the hoisted-object consumer map, including `FullscreenFileEditor.tsx`'s `chip` with both consumer lines.
    - Task notes contain the button inventory marked NAMED / ICON-ONLY and both `eslint-disable-next-line` line numbers.
    - Task notes contain the pasted M1d and M1x output scoped to this plan's 23 paths, with an **evaluated minimum** beside every M1x hit and, for each one the carve-out covers, the reason. Expected today: two M1d hits (`ReleaseDrop.tsx:65`, `:117`) and four M1x hits, all four in `PtyTerminalView.tsx` and all four xterm's own font — but **only three of them are `term.options.fontSize` assignments** (`:148`, `:244`, `:303`); the fourth is `:133` `const fontSize = useTerminalFontSize();`, the value that feeds them through `fontSizeRef`. Verified 2026-08-21. Record it that way: a note expecting four `term.options.fontSize` lines is wrong against correct source and invites an executor to reconcile the number instead of the claim.
    - Task notes record this plan's **base sha** (`git rev-parse HEAD`).
    - `npm test` exit code is `0`, **and the suite's TAP counters are recorded as this plan's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured today: `EXIT=0`, `# tests 426`, `# pass 422`, `# fail 0`, `# skipped 4`, `# todo 0`. Name
      the TAP reporter explicitly — the default (spec) reporter prints `ℹ skipped 4`, so a criterion
      grepping `^# skipped` against it never matches and silently passes as "no output, no problem".
      **Write the TAP through `mktemp`, never to a fixed repo-root name.** Six plans (15-20) run
      concurrently in wave 7 against ONE working tree (`use_worktrees: false`), so a shared `sweep.tap`
      is truncated by each wave-mate while the others are still grepping it — this plan could grade a
      wave-mate's half-written counter block against its own `# pass` / `# skipped` gate and score a run
      it never made. `.gitignore` has no `*.tap` entry either, so a stray file would also show up in
      this plan's containment check, in every wave-mate's, and in plan 23's wave-9 sweep.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every site, button, hoisted object and lint anchor in the 23 files is pre-classified before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep the 23 files and freeze this sub-group's allowlist</name>
  <files>src/renderer/src/components/UpdateToast.tsx, src/renderer/src/components/FileTree.tsx, src/renderer/src/components/AgentDetailPanel.tsx, src/renderer/src/components/AgentControlStrip.tsx, src/renderer/src/components/ToolWaterfall.tsx, src/renderer/src/components/QuitWarningModal.tsx, src/renderer/src/components/FullscreenFileEditor.tsx, src/renderer/src/components/UpdatesSection.tsx, src/renderer/src/components/CodeEditor.tsx, src/renderer/src/components/ErrorBoundary.tsx, src/renderer/src/components/CommandBar.tsx, src/renderer/src/components/BlockedBanner.tsx, src/renderer/src/components/TerminalView.tsx, src/renderer/src/components/RecentText.tsx, src/renderer/src/components/RealtimeMichaelToggle.tsx, src/renderer/src/components/PtyTerminalView.tsx, src/renderer/src/components/UpdateBadge.tsx, src/renderer/src/components/SidebarTabs.tsx, src/renderer/src/components/ReleaseDrop.tsx, src/renderer/src/components/Modal.tsx, src/renderer/src/components/MichaelBooting.tsx, src/renderer/src/realtime/DevicePicker.tsx, src/renderer/src/realtime/CompletionToast.tsx</files>
  <read_first>
    - Task 1's classification table, hoisted-object map (especially `FullscreenFileEditor.tsx:59` `const chip`) and lint-comment list — this task's work is exactly that table.
    - `src/renderer/src/components/AgentDetailPanel.tsx:85-100` — the `disposeTerminal` call site to leave alone.
  </read_first>
  <action>
    Execute task 1's classification for the 23 files under Rules 0, 1 and 2.

    `FullscreenFileEditor.tsx:59`'s `const chip` is this group's hoisted-object case: sweep the
    declaration normally, give the `✕` at `:112` a **local `fontSize` override**, and record that
    override's line in the allowlist. Do not allowlist `chip`'s declaration — that would hold
    `open in IDE` at `:111` below the floor with the test green.

    Apply this sub-group's exempt glyphs (`FullscreenFileEditor` `✕`, `PtyTerminalView` `−` and `+`,
    `ReleaseDrop` `✕`, `UpdateBadge` `·`, `UpdatesSection` `•`, `UpdateToast` `•`): size unchanged,
    `aria-hidden="true"`, allowlist entry recorded.

    Close the **two M1-invisible sites**, which `ReleaseDrop.tsx`'s M1 baseline of 1 does not include
    and no M1-based check can gate: `:65` `fontSize: 13.5` (the action button's label) and `:117`
    `fontSize: 11.5` (the `Hello MarkX · {version}` title). Both are Rule 2 — a decimal is an ordinary
    literal that M1's trailing `[^0-9.]` guard happens to drop.

    Add `aria-label` to every ICON-ONLY button and to none of the NAMED ones.

    `PtyTerminalView.tsx` and `TerminalView.tsx`: sweep the **chrome only**. Do not touch
    `terminalFontSize.ts`, xterm configuration, or any size the user controls.

    `AgentDetailPanel.tsx:93`'s `disposeTerminal` call is out of bounds.

    `ErrorBoundary.tsx`, `BlockedBanner.tsx`, `SidebarTabs.tsx`, `ToolWaterfall.tsx` and
    `UpdateBadge.tsx` are plan 22's static-render candidates. Change sizes and accessible names only —
    **do not change their DOM structure**, or plan 22 will be asserting against markup that moved for no
    requirement's reason.

    Preserve `TerminalView.tsx`'s and `realtime/CompletionToast.tsx`'s `eslint-disable-next-line` on
    their exact target lines.

    Apply containment in order; record every container integer changed with its measured delta; stop and
    report rather than reflowing.

    Freeze this sub-group's Rule 0 allowlist as a literal `file:line` array covering all 23 files and
    write it into the SUMMARY. Plan 23 assembles all seven sweep plans' arrays (01-14 through 01-20) into
    `test/repo-claims.test.cjs`. **Do not edit that test file here.**

    **Do not re-run M1 across the whole renderer and do not record a whole-renderer total.** Plans 15,
    16, 17, 18 and 19 are sweeping concurrently in wave 7, so any repo-wide number taken here is stale
    before it is written, and a stale number in a SUMMARY is the exact false-claim class this phase
    exists to remove. The repo-wide completeness bar is plan 23's, asserted once after every sweep has
    landed. Record **this sub-group's** 23 per-file counts and nothing wider.
  </action>
  <acceptance_criteria>
    - For each of the 23 files, `grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` returns exactly that file's allowlist size. Paste all 23, each beside its task-1 occurrence baseline. **Occurrences (`-o`), not lines (`-c`)** — the same unit and regex plan 23 uses in wave 9.
    - **The two M1-invisible sites are closed:** `grep -cE "fontSize: 1[13]\.5" src/renderer/src/components/ReleaseDrop.tsx` returns `0` (today `2`, at `:65` and `:117`). `ReleaseDrop.tsx`'s M1 count of 1 never included them, so only this criterion gates them.
    - **Residual blind-spot scan over all 23 files:** M1d returns `0` hits, and every M1x hit is pasted with its evaluated minimum — the only permitted hits are the **four** in `PtyTerminalView.tsx` that carry xterm's own font: the three `term.options.fontSize` assignments at `:148`, `:244`, `:303` and the `const fontSize = useTerminalFontSize();` at `:133` that feeds them. Four hits, three of which are assignments — verified 2026-08-21, and stated precisely so the criterion passes on correct source. Each is named as carve-out with its reason. Any other M1x hit is a site this plan must fix.
    - `grep -n "chip" src/renderer/src/components/FullscreenFileEditor.tsx` output shows the declaration swept to a token and the `✕` carrying its own local `fontSize` — paste both lines. The allowlist contains the override's line and **not** the declaration's line.
    - `grep -c "cth-font-display" <file>` is unchanged for all 23 files, each compared against its task-1 baseline.
    - **Containment, path-scoped so it survives the concurrent wave.** Six plans sweep in wave 7 against
      ONE working tree (`use_worktrees: false`), so a bare whole-tree `git diff --stat` always shows the
      other five agents' edits and can therefore never pass — **do not use one as a containment check.**
      Stage and commit with an explicit pathspec (`git add -- <this plan's 23 paths>` then
      `git commit -- <the same paths>`); never `git add -A`, `git add .` or `git commit -a`, which sweep
      a wave-mate's unstaged work into this plan's commit. Then assert all three:
      (a) **per commit and filtered — this is the one that catches a genuine cross-set edit.** Bind both
      variables **inside** the criterion: a criterion is its own shell invocation, and an unbound `$SHAS`
      iterates zero times, so the filter would emit nothing and score as PASS having checked nothing.
      Substitute the literal base sha task 1 recorded for `<base sha>`, then run:
      `BASE=<base sha>; SHAS=$(git log --format=%H "$BASE"..HEAD -- <this plan's 23 paths>); echo "$SHAS"; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/renderer/src/components/(UpdateToast|FileTree|AgentDetailPanel|AgentControlStrip|ToolWaterfall|QuitWarningModal|FullscreenFileEditor|UpdatesSection|CodeEditor|ErrorBoundary|CommandBar|BlockedBanner|TerminalView|RecentText|RealtimeMichaelToggle|PtyTerminalView|UpdateBadge|SidebarTabs|ReleaseDrop|Modal|MichaelBooting)\.tsx|src/renderer/src/realtime/(DevicePicker|CompletionToast)\.tsx)?$"`
      The filter must produce **no output**, and `echo "$SHAS" | grep -c .` must be **at least 1** — paste
      both, plus `for sha in $SHAS; do git show --stat --format= "$sha"; done` so the touched set is on
      the page. The trailing `?` in the pattern absorbs the blank line `--format=` emits. The `grep -c .`
      floor is load-bearing: with no commits made, the loop body never runs and the filter is empty for
      the wrong reason. Replaces an unfiltered `git show --stat --format=` adjudicated by eye — a
      diffstat with no pass/fail filter is not a criterion. An edit into plan 15, 16, 17, 18 or 19's files shows up here
      as an extra path; it is invisible to any whole-tree diff. **Selected by pathspec, so this is this
      plan's own commits:** five wave-mates commit into this same tree in this same wave, and a tree-wide
      filter would fail this plan on their work. If a wave-mate does commit one of this plan's paths, its
      commit enters `$SHAS` and its other paths fail the filter — exactly the cross-contamination this
      criterion exists to catch.
      (b) **path-scoped and positive — the declared set actually moved.**
      `git diff --name-only <base sha>..HEAD -- <this plan's 23 paths> | sort -u` lists **all 23** paths
      of `files_modified` — paste the list beside `... | sort -u | grep -c .`, which must print exactly
      `23`. The count is the whole gate, and it is equivalent to set equality here: a pathspec-scoped diff
      can only ever emit paths from that pathspec, so `23` unique lines can only be the 23 declared paths.
      That is why the previous wording — *"lists only paths from `files_modified`"* — had no failing case
      at all, and why empty output, the do-nothing case, satisfied it too. Verified 2026-08-21 that every
      one of these 23 files carries measured sub-14px work (between 1 and 6 occurrences each — the thinnest are UpdateBadge, SidebarTabs, ReleaseDrop, Modal
      and MichaelBooting at 1 apiece, and ReleaseDrop carries two decimals on top of it), so all 23 must appear. Scoped
      to this plan's own paths, it is unaffected by wave-mates and passes while they work.
      (c) **nothing left behind** — `git status --porcelain -- <this plan's 23 paths>` is empty, so this
      plan leaves none of its own edits uncommitted for the next wave-mate's `git add` to absorb. An
      uncommitted edit of this plan's IS the lost update the whole-tree check was reaching for.
      The same per-commit output must list no `src/renderer/src/components/terminalFontSize.ts` and no
      `src/renderer/src/components/terminalPool.ts` — named here so the carve-out is checked in one
      place, not by a second whole-tree diff.
    - `git diff <base sha>..HEAD -- src/renderer/src/components/ErrorBoundary.tsx src/renderer/src/components/BlockedBanner.tsx src/renderer/src/components/SidebarTabs.tsx src/renderer/src/components/ToolWaterfall.tsx src/renderer/src/components/UpdateBadge.tsx` contains no added or removed JSX element — only style and attribute changes. Anchored to the base sha recorded in task 1 and scoped to these five paths, so it reads the same whether or not a wave-mate has committed in between, and so it stays true after this plan commits.
    - `grep -c "eslint-disable-next-line" src/renderer/src/components/TerminalView.tsx` and the same for `src/renderer/src/realtime/CompletionToast.tsx` match task 1's records, and each comment is still immediately above its target line — paste the line numbers.
    - Task notes contain this sub-group's frozen allowlist as a literal `file:line` array. They contain **no** whole-renderer M1 total.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today, of `426` total). Exit code alone
      is not a gate: `node --test` counts skipped tests in its total and exits `0` on an all-skipped file
      (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from a green one by
      exit code. This plan adds no tests, so any rise in `# skipped` is a red test being hidden rather
      than fixed.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every remaining renderer file clears the floor, the hoisted-object rule is applied at this pair's verified case, plan 22's candidates keep their DOM shape, and no cross-set edit reaches plan 19's files.</done>
</task>

<task type="auto">
  <name>Task 3: Floor the release-drop page's own CSS — the sub-14px text no fontSize scan can see</name>
  <files>
    - src/shared/releaseDrop.ts
  </files>
  <action>
    **Why this is here and why nothing caught it.** `src/shared/releaseDrop.ts` is the release-drop
    "what's new" page. It is rendered in-app in a sandboxed iframe by `ReleaseDrop.tsx` — this plan's
    file — and injected by `src/main/updater.ts:285`. Its type is CSS inside a template literal, in
    `src/shared`, with no `fontSize` identifier anywhere, so **M1, M1d and M1x are all structurally
    blind to it**: they root at `src/renderer/src` and match on `fontSize:`. Measured 2026-08-21:
    `grep -c "font-size" src/shared/releaseDrop.ts` → `23`, of which **11 are below the 14px floor**.

    **Two constants, and only these two are yours.** Re-derive the line numbers by content; do not
    `sed` by the numbers below.

    1. `FRAME_BASE_CSS` — wraps **every** drop, authored or default, so flooring it is the change that
       reaches the most text: `:107` `12px`, `:140` `13px` (figcaption), `:150` `13px`.
    2. `DEFAULT_DROP_HTML` — the drop shown when a release ships without one: `:212` `13.5px`,
       `:219` `11.5px`, `:230` `12px`, `:232` `10.5px`, `:239` `13.5px`, `:240` `10px`,
       `:243` `12.5px`, `:246` `13.5px`.

    Raise each to at least `14px`, or record it in this plan's allowlist with a written reason, the
    same way the `.tsx` sweeps record theirs. Keep the visual hierarchy — where a value was
    deliberately smaller than its neighbour, raise both and preserve the relationship rather than
    flattening everything to 14.

    **State the ceiling honestly in the plan and in the SUMMARY.** The *authored* half of a drop is
    remote, author-controlled markup that this app renders; it cannot be floored from here, and no
    criterion in this phase should imply otherwise. What this task guarantees is that the frame and
    the default drop — the two surfaces the project itself authors — are at or above the floor.

    Do not touch `ReleaseDrop.tsx` here; task 2 already owns it.
  </action>
  <acceptance_criteria>
    - No sub-14px declaration survives in the two project-authored constants: `grep -cE "font-size: *(1[0-3]|[0-9])(\.[0-9]+)?px" src/shared/releaseDrop.ts` returns `0`, or every remaining hit is listed in this plan's allowlist with a written reason. **Baseline today is `11`** — paste before and after, so this criterion demonstrably fails against current source.
    - Nothing was deleted to clear the grep: `grep -c "font-size" src/shared/releaseDrop.ts` returns **at least `23`**, today's total count. A drop from 23 means a rule was removed rather than raised — paste both numbers.
    - The frame still styles every drop: `grep -c "FRAME_BASE_CSS" src/shared/releaseDrop.ts` and `grep -c "DEFAULT_DROP_HTML" src/shared/releaseDrop.ts` each return at least `1`, unchanged.
    - Containment, per-commit and range-bound: `BASE=<B-sha from task 1>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && SHAS=$(git log --format=%H "$BASE"..HEAD -- src/shared/releaseDrop.ts) && test -n "$SHAS" || { echo "UNBOUND BASE OR NO COMMITS — CRITERION FAILS"; false; }; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(src/shared/releaseDrop\.ts)?$"` prints nothing.
    - `npm run typecheck` exit code is `0`.
    - The SUMMARY states the ceiling in one sentence: the authored half of a drop is remote markup and is not floored by this phase.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The release-drop frame and default drop — the two surfaces this project authors — are at or above the 14px floor, and the remote-authored ceiling is stated rather than implied away.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered app shell and panels → the operator's continuous view of the floor | These are the surfaces the operator watches while agents run unattended. Illegible status, cost or blocked-state text is a direct failure of criterion 4's "legible to the operator watching it". |
| this plan's file set → plan 19's file set, same wave, one working tree | Two agents sweep the renderer concurrently. A cross-set edit is a lost update with no test that catches it until wave 9. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P20-01 | Information disclosure | remaining renderer text below the legibility floor | mitigate | Every site swept to a token; per-file M1 count must equal the allowlist size, pasted beside its task-1 baseline. |
| T-P20-02 | Information disclosure | an icon-only control with no accessible name | mitigate | Per-element `aria-label` pass from a pre-built inventory, never a ratio. |
| T-P20-03 | Repudiation | allowlisting `FullscreenFileEditor`'s hoisted `chip` and leaving `open in IDE` below the floor | mitigate | The hoisted declaration is swept and the glyph gets a local override; asserted by a paste of both lines plus an explicit check that the declaration line is not on the allowlist. |
| T-P20-04 | Denial of service | a bumped label clipping a blocked-state banner or an update toast | mitigate | Containment applied in UI-SPEC's order with measured deltas; "stop and report" rather than reflow. |
| T-P20-05 | Repudiation | a moved `eslint-disable-next-line` detaching a suppression before plan 21's lint gate | mitigate | Both suppression anchors in this group are recorded in task 1 and re-asserted by line number after the edit. |
| T-P20-06 | Denial of service | changing user-controlled terminal font sizing | mitigate | The carve-out is stated once with its predicate (xterm's own font: `terminalFontSize.ts`, `terminalPool.ts`, `term.options.fontSize`) and asserted inside the per-commit `git show --name-only --format=` containment filter, whose allowlist admits neither file. Terminal-adjacent chrome is NOT carved out and is swept. |
| T-P20-07 | Repudiation | changing plan 22's candidate components' DOM after their tests are designed | mitigate | Only style and attribute changes are permitted in the five candidates, asserted by a JSX-element diff check. |
| T-P20-08 | Tampering | editing a file that belongs to plan 15, 16, 17, 18 or 19 in the same wave | mitigate | Per-commit `git show --name-only --format=` piped through an allowlist filter that must be empty, with `$SHAS` bound inside the criterion and a `grep -c .` floor so an executor that committed nothing cannot pass it, plus path-scoped `git diff`/`git status`; a bare whole-tree `git diff --stat` is explicitly forbidden here because in wave 7 it always shows five wave-mates' edits and so can never pass. |
| T-P20-90 | Information disclosure | sub-14px user-facing text M1 structurally cannot see — decimals, quoted px, ternaries and computed sizes — shipping unexamined past plan 23's bar | mitigate | The M1d and M1x scans run over this plan's paths in task 1 and again after the sweep; every M1x hit carries an evaluated minimum and no minimum may be below 14. |
| T-P20-91 | Repudiation | a containment check that can never pass, so it is quietly dropped | mitigate | The whole-tree `git diff --stat` is replaced by a per-commit `git show --name-only --format=` allowlist filter, a path-scoped `git diff --name-only` that must list **all** the plan's declared paths, and a path-scoped `git status --porcelain`. All three pass while five wave-mates edit the same tree, and each has a real failing case: an out-of-set path, a declared file left untouched, and an uncommitted edit respectively. |
| T-P20-92 | Repudiation | a red test hidden as a skip to make the sweep green | mitigate | TAP counters are baselined in task 1 and re-compared after the sweep; `# skipped` may not rise. `node --test` counts skips in its total and exits `0`, so exit code alone is not the gate. |
| T-P20-93 | Repudiation | wave 7 pinning line counts while wave 9 asserts occurrence counts | mitigate | Every per-file number in this plan is an occurrence count taken with `grep -hoE … | wc -l`, the same unit and regex plan 23 uses, and the unit is stated beside each pinned number. |
| T-P20-09 | Repudiation | recording a whole-renderer M1 total that five concurrent plans invalidate | mitigate | Explicitly forbidden here; the repo-wide bar is plan 23's, run once after every sweep has landed. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: open the file tree, an agent detail panel, the fullscreen file editor, the command bar, a
  terminal view and the updates section in the dev app. Confirm nothing is clipped, `open in IDE` is
  legible, and terminal font sizing still responds to the user's own controls.
</verification>

<success_criteria>
- UI-SPEC completeness-bar clauses 2 and 3 hold for all 23 files in this sub-group, completing the
  sweep surface across plans 14-20.
- Every `<button>` in the group has an accessible name, by the rule rather than by a ratio.
- The renderer's components are final, unblocking plan 21's lint gate and plan 22's static-render tests.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-20-SUMMARY.md` when done. It MUST contain this
sub-group's frozen Rule 0 allowlist as a literal `file:line` array, the final per-file M1 counts **as
occurrences with the unit named** beside their task-1 baselines, the M1d/M1x scans before and after with
`ReleaseDrop.tsx`'s two decimal sites resolved and every carve-out M1x hit named with its reason, this
plan's base sha with every commit sha and its `git show --stat`, the TAP counters before and after,
every container integer changed with its delta, and confirmation that both `eslint-disable-next-line`
comments in the group are still on their target lines. It MUST NOT contain a whole-renderer M1 total.
</output>
