---
phase: 01-finish-the-floor
plan: 10
type: execute
wave: 5
depends_on: ["01-01", "01-08"]
files_modified:
  - src/main/db.ts
  - src/main/memory.ts
  - src/main/index.ts
  - src/preload/index.ts
  - src/renderer/src/components/MemoryPanel.tsx
  - src/renderer/src/components/SettingsModal.tsx
  - README.md
  - test/db-fts.test.cjs
  - test/repo-claims.test.cjs
  - test/engine-parity.test.cjs        # task 5 only — plan 09's FLOOR-10 wiring test, landed here with the line it asserts. No other wave-5 plan declares this file (checked 2026-08-20).
  # `.github/workflows/ci.yml` is DELIBERATELY ABSENT and must stay absent — plan 01 (wave 1) owns it
  # and this plan asserts its shape without editing it. See task 2's "Do not touch" paragraph.
  - .github/ISSUE_TEMPLATE/bug_report.yml   # final Logs wording, once the Settings button exists
autonomous: true
requirements: [FLOOR-07, FLOOR-05, FLOOR-10]   # FLOOR-10 is a SPLIT and this is the CLOSING half — 01-09 (wave 4) mints the arm, the `hive.budgetForAgent()` accessor and the handoff but explicitly does NOT close it; task 5 here applies `budget: hive.budgetForAgent(id)` in `runBreakerBeat` and lands the wiring assertion in the same commit, which is the wave at which FLOOR-10 actually becomes true. Reciprocal of the note at `01-09-PLAN.md:30`. Without this line a requirement→plan rollup marks FLOOR-10 complete at end of wave 4 with the production wiring absent.

must_haves:
  truths:
    - "Memory recall is backed by a real SQLite FTS5 index in the already-open PersistStore, and the index is queryable"
    - "Recall is scoped per agent/project by a WHERE predicate, and the docs say plainly that scope is agent-supplied until RECALL-02 lands in Phase 5"
    - "The MemoryPanel shows the shared-by-default scope as a plain warning rather than leaving it implicit"
    - "Two dead preload exports and their unreferenced ipcMain handlers are gone"
    - "An operator can open the log folder from Settings without knowing where Electron puts it"
  artifacts:
    - path: "src/main/db.ts"
      provides: "an appended migration creating memory_fts(text, agent_id UNINDEXED, project UNINDEXED)"
      contains: "fts5"
    - path: "test/db-fts.test.cjs"
      provides: "integration coverage over a REAL SQLite handle — the FakeDatabase cannot serve this"
      contains: "PersistStore"
    - path: "src/renderer/src/components/SettingsModal.tsx"
      provides: "the Log folder row with an `open logs` button"
      contains: "openLogs"
  key_links:
    - from: "src/main/memory.ts recall"
      to: "memory_fts"
      via: "MATCH query with a WHERE agent_id predicate"
      pattern: "memory_fts"
    - from: "SettingsModal Log folder row"
      to: "app:openLogs"
      via: "the new preload export over the handler that already exists at index.ts:4461"
      pattern: "openLogs"
---

<objective>
Give memory recall the SQLite FTS5 index the docs already promise, scope it by a real predicate,
state the `--wing` limitation plainly, delete two dead exports, and wire the log-folder button to the
main-process handler that has been complete and unreachable.

Purpose: FLOOR-07 and FLOOR-05. `app:openLogs` (`index.ts:4461-4469`) is finished code with **zero
callers** — no preload export, no UI. FLOOR-07 is three subsystems wearing one line, and the honesty
half is the larger half: the sharing model is spelled out verbatim at `memory.ts:10-21` and surfaced
nowhere, two preload exports are dead, and `README.md` plus `preload/index.ts:838` call a keyword
knowledge store an "Enterprise Knowledge Graph" while `kg-core.cjs:3-8` honestly calls it what it is.

Output: one appended migration, a real-SQLite test, a scope warning line, two deletions, a Settings
row, and a rename.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@docs/design/knowledge-graph.md

<interfaces>
`MIGRATIONS` — `src/main/db.ts:33-50` is the doc comment stating the contract (**append only**; index
N takes the DB from `user_version` N to N+1; never edit a shipped entry), `:51-69` is the single
existing entry, `:110-121` is the runner (each migration and its version bump share one transaction).
`openOnce` at `:99-107` sets four pragmas; the corrupt-DB quarantine path at `:82-96` is what makes a
bad migration survivable, so **do not add a `throw` inside a migration**.
`PersistStore`'s constructor at `:74` takes `dbPath?: string` — *"Override the DB location (tests)"* —
so a test needs no `app.getPath` fake.

D-33's target shape: `// → user_version 2 (FLOOR-07): FTS5 keyword recall.` plus one `db.exec`
containing `CREATE VIRTUAL TABLE ... memory_fts USING fts5(text, agent_id UNINDEXED, project UNINDEXED)`.
**`CREATE VIRTUAL TABLE` does not accept `IF NOT EXISTS` in older SQLite grammars — verify against the
shipped better-sqlite3 before assuming, and do not silently drop the guard.**

`SQLITE_ENABLE_FTS5` was verified present at `node_modules/better-sqlite3/deps/defines.gypi:24`
**before** plan 01's 13.x bump. That file is version-specific: re-verify. Note what that file does and
does not prove — it is the define list for a **source build**. A prebuilt binary is not obliged to
match it. The only proof that counts is `CREATE VIRTUAL TABLE … USING fts5` succeeding against the
binary that will actually be loaded, which is why task 1 runs that statement rather than trusting the
grep.

**Wave order changes the answer — read this first.** This plan is **wave 5**; plan 01 is **wave 1**,
and one of the things plan 01 lands is `better-sqlite3` `^11.10.0` → `^13.0.3`. 11.10.0 is raw-V8 and
must be compiled; **13.0.3 is N-API and ships eight prebuilt binaries in the tarball**
(`prebuilds/{darwin,linux,linuxmusl,win32}-{x64,arm64}.node`), with no `install` script. Verified
independently 2026-08-20 in a clean directory on this machine:

```
npm i better-sqlite3@13.0.3 --ignore-scripts          → added 2 packages, nothing compiled
node -e "new (require('better-sqlite3'))(':memory:')
         .exec(\"CREATE VIRTUAL TABLE t USING fts5(x)\")"  → FTS5 OK, exit 0   (plain node v24.13.0)
```

So the constraint below is the **pre-plan-01** state, and it dissolves once wave 1 lands: the CI
`test` jobs install with `npm ci --ignore-scripts`, which is exactly the condition under which the
N-API prebuild is used as-is. **`test/db-fts.test.cjs` therefore runs in the CI `test` matrix with no
rebuild step added, and `.github/workflows/ci.yml` must NOT gain one.** Plan 01 owns that file and
asserts `grep -c "npm rebuild better-sqlite3" .github/workflows/ci.yml` returns `0`, because
`npm rebuild` on 13.0.3 discards the shipped prebuild and synthesises a `node-gyp rebuild` — which
then needs Python on the macOS and Windows runners, where `setup-python` is Linux-gated. Adding the
step here would break wave 1's gate and the two hard-gate jobs with it. This plan asserts the same
`0`, so the two plans cannot drift apart.

**The pre-bump constraint, measured — kept because it explains `FakeDatabase` and the require-probe
trap.** Before plan 01, `better-sqlite3` in this repo is built for **Electron's ABI**, not Node's:
`postinstall` is `electron-rebuild -f`, and `test/config-secrets.test.cjs:46-51` states the
consequence verbatim — *"it cannot load under plain node"*. Measured on the plan-time dev machine
(node v24.13.0, ABI 137, better-sqlite3 11.10.0):

```
node -e "require('better-sqlite3')"                  → exits 0   ← FALSE GREEN
node -e "new (require('better-sqlite3'))(':memory:')" → Error: Could not locate the bindings file
find node_modules/better-sqlite3 -name '*.node'      → no matches at all
```

**A `require()`-only probe reports success on a machine where SQLite does not work.** Never use one to
decide whether the FTS5 test can run. Two further facts, both verified this session against
`.github/workflows/ci.yml`:
- CI pins `NODE_VERSION: "22"` (`:13`) and the header comment at `:9` records why: *"Node 24 ships no
  better-sqlite3 prebuild."* Node 24 is a build, not a download.
- **The `test` job does NOT rebuild `better-sqlite3`, and must not start.** It runs
  `npm ci --ignore-scripts` (`:70`) then `npm rebuild node-pty` (`:92`) and nothing else. Under
  11.10.0 that left no loadable binary for `node --test`; under 13.0.3 it is precisely right — the
  prebuild ships and `--ignore-scripts` leaves it alone. The `npx electron-rebuild -f` in the
  **`build`** job (`:118`) is for the packaged app and is not this test's runtime.

Consequence to plan around honestly: **the test's guaranteed home is the CI `test` matrix on Node 22
after wave 1's bump**, where `npm ci --ignore-scripts` leaves the N-API prebuild in place. Whether it
also runs on the dev machine depends on what `postinstall`'s `electron-rebuild -f` leaves behind for
13.x — do not assume either way. Run the probe in task 1 and record the result: if the local
`new Database(':memory:')` opens, run the test locally and paste the output; if it throws, the
SUMMARY says `NOT RUN LOCALLY` verbatim with the thrown error **and** links the green CI run. What is
forbidden is making it pass by weakening it — no skip, no try/catch, no fake.

**The test trap, with its line.** `test/config-secrets.test.cjs:52-73` injects a `FakeDatabase` via
`require.cache`. Its `pragma()` at `:59-62` **implements the migration rail** while `exec()` at `:64`
is a **no-op** — so a `PersistStore.open()` against the fake runs the migration loop and bumps
`user_version` while executing no DDL. An FTS5 test written against it passes with an empty database.
`node --test` runs each file in its own child process, so `test/db-fts.test.cjs` simply does not
inject. It does need a `better-sqlite3` that opens under plain node, which wave 1's bump to 13.x N-API
supplies to the CI `test` jobs without any rebuild step — see the wave-order note above.

**The second trap, and the one this plan is being corrected for: a skip is not a pass.** `node --test`
exits `0` when every test in a file is skipped, so `t.skip('native module unavailable')` around the
FTS5 test makes "the index was never exercised" and "the index works" produce byte-identical gate
output. That is strictly worse than having no test, because it reports green. `test/db-fts.test.cjs`
therefore contains **no** `t.skip`, no `{ skip: … }` option, no `describe.skip`, and no
`try { require(…) } catch { return }` early-return. If the native module cannot open a database, the
test **throws** with a message naming the runtime that is missing — a loud non-zero exit — and the
gate reports unavailable-and-failing.

FLOOR-07 honesty anchors: `src/main/memory.ts:10-21` (the sharing model, verbatim) and `status().scope`
at `:220-233`; `src/preload/index.ts:828-829` (`memoryWakeUp`) and `:834-835` (`reflectNow`) — both
verified present this session — with their `ipcMain.handle` registrations at `index.ts:4041` and
`:4046`; `src/preload/index.ts:838`, the *"Enterprise Knowledge Graph (multimodal context for
agents)"* section header. `reflect.ts`'s own timer calls the class method directly, so deleting the
two IPC paths breaks nothing. `src/main/config.ts:497` — `knowledgeGraph: { enabled: false }` **stays
false** (D-34).

FLOOR-05, from `01-UI-SPEC.md` § FLOOR-05 (binding):
- Main handler at `index.ts:4461-4469` is complete and correct. **Do not touch it.**
- Preload export shape: `/** Reveal the log folder (#13). */ openLogs: (): Promise<{ ok: boolean; path?: string; error?: string }> => ipcRenderer.invoke('app:openLogs'),` — modelled on `skillsReveal` at `preload:822-824`.
- One row in Settings → General, directly beneath the existing `Home folder` row (`SettingsModal.tsx:880-894`) and above the divider at `:897`. Section label `Log folder`. Path in `var(--cth-font-mono)` with `wordBreak: 'break-all'`, colour `--cth-ink-900`, rendering `—` before first invocation (matching `:892`).
- Action: `<PixelButton variant="secondary" size="sm">open logs</PixelButton>`. **No `aria-label`** — PixelButton renders real button text and an `aria-label` would override the visible label.
- Error copy, inline below the row in `--cth-coral`: `Could not open the log folder. The path is {path} — open it yourself.` No modal, no toast (`SettingsModal.tsx:696` `setChangeErr` is the established inline pattern).
- The surrounding section-header style at `:882-886` uses `fontSize: 8`, which is a FLOOR-12 violation swept in wave 6. **Write the new row at the corrected size (`var(--cth-text-display-md)`), not copied at 8px.**
</interfaces>
</context>

<tasks>

<!-- TASK NUMBERING: this plan's tasks are **1, 2, 3, 5**. There is no Task 4, and that is
     DELIBERATE, not an omission — the numbers are NOT being compacted. Task 5 is the FLOOR-10
     T-INDEX handoff and carries the FLOOR-09 hard STOP-gate. Renumbering it to 4 would silently
     invalidate the correct cross-plan pointers that already name "plan 10 task 5" (e.g.
     `01-23-PLAN.md:545`, which pairs it with "plan 08 task 6" — plan 08's tasks are likewise
     1, 2, 3, 4, 6 with no 5, for the same reason). Any pointer elsewhere in this phase that names
     **"plan 10 task 1"** as the FLOOR-09 / FLOOR-10 gate is STALE and belongs to the owner of that
     file: task 1 here is FTS5 / FLOOR-07 verification and contains no such gate. Known stale
     pointers at the time of writing: `01-09-PLAN.md:210` and `01-09-PLAN.md:482`. The
     `01-RESEARCH.md` references to "plan 10 task 1" and "plan 10 task 2" are about FTS5 and are
     CORRECT — do not touch them. -->

<task type="auto">
  <name>Task 1: Verify every FLOOR-07 and FLOOR-05 clause against current source, and re-verify FTS5 after the better-sqlite3 bump</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/db.ts` in full — the migration contract, the single existing entry, the runner, `openOnce`'s pragmas and the quarantine path.
    - `src/main/memory.ts:1-40` and `:210-240` — the sharing-model comment and `status()`.
    - `src/main/index.ts:4035-4050` and `:4455-4475` — the two handlers to delete and the `app:openLogs` handler to leave alone.
    - `src/preload/index.ts:820-845` — the three anchors: `skillsReveal`, the two dead exports, the section header.
    - `src/renderer/src/components/SettingsModal.tsx:875-900` — the Home folder row being cloned, and the divider at `:897`.
    - `src/renderer/src/components/MemoryPanel.tsx` — the local `MemoryStatus` interface.
    - `test/config-secrets.test.cjs:31-73` — read it to know exactly what NOT to do.
    - `01-UI-SPEC.md` § FLOOR-05 — the binding affordance contract.
  </read_first>
  <action>
    Quote the acceptance text of issues #16, #31, #32 (FLOOR-07) and #13 (FLOOR-05) verbatim and run
    one command per clause with ACTUAL output pasted:
    - `grep -rn "openLogs" src/preload/ src/renderer/` — expect EMPTY. That empty grep is FLOOR-05's evidence: the main half is done and the exposure half is missing.
    - `grep -n "app:openLogs" src/main/index.ts` — expect a hit. Confirms nothing needs building in main.
    - `grep -rn "memoryWakeUp\|reflectNow" src/ --include=*.ts --include=*.tsx` — the dead exports and their handlers, plus proof nothing else references them.
    - `grep -rn "fts5\|FTS5\|CREATE VIRTUAL TABLE" src/` — expect EMPTY. The index the docs promise does not exist.
    - `grep -rn "Enterprise Knowledge Graph" README.md src/preload/index.ts` — the overclaim.
    - `grep -n "SQLITE_ENABLE_FTS5" node_modules/better-sqlite3/deps/defines.gypi` — **re-verify after plan 01's 13.x bump.** The defines file is version-specific, and it only governs a source build; it is necessary evidence, never sufficient. If FTS5 is absent, STOP and report: D-32's zero-new-dependency premise fails and the plan needs revising, not working around.
    - The runtime FTS5 probe — the only command that actually proves the feature, run against the binary that will be loaded:
      ```
      node -e "const D=require('better-sqlite3'); const db=new D(':memory:'); db.exec('CREATE VIRTUAL TABLE t USING fts5(text, agent_id UNINDEXED)'); console.log('FTS5 USABLE', db.prepare('select sqlite_version() v').get().v)"; echo "EXIT=$?"
      ```
      Paste it with its `EXIT` line. **`node -e "require('better-sqlite3')"` is NOT an acceptable substitute** — measured at plan time it exits `0` on a machine with no native binary at all, while `new D(':memory:')` throws `Could not locate the bindings file`. Also run and paste the same statement with `IF NOT EXISTS` inserted, to settle the grammar question task 2 asks.
      A non-zero `EXIT` here is expected on the dev box and is **not** a blocker for the plan — it is the constraint to record. It IS a blocker for claiming the FTS5 test passed locally.
    - `find node_modules/better-sqlite3 -name '*.node'` — paste it. Empty means no native binary is installed at all, which is what makes the `require`-only probe a false green.
    - `grep -n "rebuild" .github/workflows/ci.yml` — establish, per job, which ABI each rebuild targets. Expected at plan time: the `test` job rebuilds **only** `node-pty` (`:92`) for the Node ABI, and the sole `better-sqlite3` rebuild is `npx electron-rebuild -f` in the **`build`** job (`:118`) — the wrong ABI for `node --test`. Record which job, if any, produces a Node-ABI `better-sqlite3`. **"None" is the expected and correct answer, and it is not a gap to close.** After wave 1's bump, 13.0.3 is N-API and ships prebuilds, so the `test` jobs' `npm ci --ignore-scripts` leaves a loadable binary with no rebuild at all. Task 2 must NOT add a rebuild step, and this plan does not modify `.github/workflows/ci.yml` at any point — adding `npm rebuild better-sqlite3` would discard the prebuild, synthesise a `node-gyp rebuild` needing Python on the macOS and Windows runners, and break both plan 01's `grep -c … returns 0` criterion and the two hard-gate jobs. **Do not record this as satisfied on the strength of the string `better-sqlite3` appearing somewhere in the file** — it appears in comments.

    Record per clause OPEN or ALREADY-SATISFIED. Note explicitly that `knowledgeGraph.enabled` stays
    `false` (D-34) and that the knowledge graph was formally **retired**, not deferred — a renderer
    panel for it is out of scope and must not be resurrected as a nicety.
  </action>
  <acceptance_criteria>
    - **B-sha recorded before any edit**: paste `git rev-parse HEAD`. This plan's CI-containment criterion is range-bound against it, so an unrecorded B-sha makes that criterion unrunnable — record it first, verbatim, in the task notes.
    - Task notes contain pasted output for all nine commands.
    - The `openLogs` and `fts5` greps are pasted and empty.
    - The `SQLITE_ENABLE_FTS5` grep is pasted and non-empty (or the task stopped and reported).
    - The runtime FTS5 probe is pasted **with its `EXIT` line**, and the `IF NOT EXISTS` variant with its own. A `require`-only probe appearing in place of either fails this criterion.
    - The `find … -name '*.node'` output is pasted.
    - Task notes name the CI job that produces a **Node-ABI** `better-sqlite3`, or state that none does. "The string appears in ci.yml" does not satisfy this.
    - Task notes state, in one sentence, where `test/db-fts.test.cjs` will actually be executed (which job, which runtime) and whether it can run on this machine at all.
    - `npm test` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Every clause is classified with pasted evidence and FTS5 availability is re-confirmed against the bumped better-sqlite3.</done>
</task>

<task type="auto">
  <name>Task 2: Append the FTS5 migration, scope recall by predicate, and cover it with a REAL SQLite handle</name>
  <files>src/main/db.ts, src/main/memory.ts, test/db-fts.test.cjs</files>
  <!-- `.github/workflows/ci.yml` is NOT in this list on purpose: plan 01 owns it, this task only
       asserts its shape. See the "Do not touch" paragraph in the action. -->
  <read_first>
    - `src/main/db.ts` in full — the file being modified. Read `MIGRATIONS`'s doc comment before appending anything.
    - `src/main/memory.ts` in full — where recall happens and where `MemoryManager` degrades to a silent no-op when `mempalace` is absent from PATH.
    - `01-PATTERNS.md` § "3. `PRAGMA user_version` migration rail" `:585-663` — the existing entry's exact shape (the `→ user_version N` comment, `CREATE TABLE IF NOT EXISTS`, one `db.exec` with a single template literal, an explicit index) and the `CREATE VIRTUAL TABLE` grammar warning.
    - `01-PATTERNS.md` § "`test/db-fts.test.cjs`" `:252-322` — the inverted analog, the `floor(t)` real-tempdir harness, and the `realpathSync`-wrapped `tempDir` helper.
    - `docs/design/knowledge-graph.md:45` and `:191` — FTS5 is the documented next step; `:191`'s loopback query endpoint is RECALL-02's job in Phase 5 and must NOT be built here.
  </read_first>
  <action>
    Append **one** entry to `MIGRATIONS` — never edit the existing one; shipped DBs have already run
    it. Comment it `// → user_version 2 (FLOOR-07): FTS5 keyword recall.` and give it one `db.exec`
    with a single template literal creating `memory_fts` as an FTS5 virtual table over
    `(text, agent_id UNINDEXED, project UNINDEXED)`. D-33: scoping then becomes a `WHERE agent_id = ?`
    predicate — the exact shape RECALL-02 later binds server-side instead of trusting `--wing`.
    Verify whether the shipped SQLite accepts `IF NOT EXISTS` on `CREATE VIRTUAL TABLE`; if it does not,
    use the accepted grammar rather than dropping the guard silently, and say which in a comment.
    Add no `throw` inside the migration — the quarantine path at `:82-96` is what makes a bad migration
    survivable.

    In `src/main/memory.ts`, write recalled notes into `memory_fts` and add the keyword recall path
    that queries it with a `MATCH` plus the `WHERE agent_id = ?` (and project) predicate. This also
    gives a free recall path for the common case where `mempalace` is not on PATH and `MemoryManager`
    degrades to a silent no-op — say so in the comment.

    **Do not build server-side scope enforcement.** Scope remains agent-supplied via `--wing` until
    RECALL-02 lands in Phase 5; building the loopback query verb now hollows out that requirement
    (`docs/design/knowledge-graph.md:191`). **Do not flip `knowledgeGraph.enabled`** (D-34). **Do not
    touch `kg-core.cjs`** — it runs as plain `node` outside Electron and must stay native-free; it is
    a different subsystem and conflating the two is the mistake D-32 exists to prevent.

    Create `test/db-fts.test.cjs`. It must use a **real** `better-sqlite3` handle: do NOT inject the
    `FakeDatabase` from `test/config-secrets.test.cjs` — its `exec()` is a no-op while its `pragma()`
    bumps `user_version`, so a test against it would report a successful migration over an empty
    database. `node --test` runs each file in its own process, so simply omitting the injection is
    sufficient; no un-injection dance is needed. Use `new PersistStore(join(tmp, 'x.db'))` with a
    `realpathSync`-wrapped self-cleaning temp dir. Assert: the virtual table exists after `open()`
    (query `sqlite_master`), `user_version` is 2, a row inserted for agent A is returned by a MATCH
    query scoped to A, and the **same** query scoped to agent B returns nothing. That last assertion
    is the scoping clause; without it the test proves only that FTS5 compiles.

    **The file must contain no skip of any kind.** No `t.skip(…)`, no `test('…', {skip: true}, …)`, no
    `describe.skip`, no `try { require('better-sqlite3') } catch { return }`. The reason is mechanical:
    `node --test` exits `0` when every test in a file is skipped, so a skip makes "FTS5 was never
    exercised" and "FTS5 works" produce the same gate result — the coverage vanishes while the gate
    stays green. If the native module cannot open a database, the test must **fail loudly**. Open the
    handle at the top of the file, outside any `try`, so the throw is the runner's own error, and give
    it a message that names what is missing and how to get it, e.g.:

    ```
    // Fail LOUD, never quietly bypass. `node --test` exits 0 on a file whose tests
    // were all bypassed, so doing that here would silently delete the FTS5 coverage
    // while the gate still reported green.
    // Wave 1 bumps better-sqlite3 to 13.x, which is N-API and ships prebuilt
    // binaries; the CI test jobs install with `npm ci --ignore-scripts`, so the
    // prebuild is what loads. If this throws, the prebuild is missing for this
    // platform — fix that, do not skip the file.
    const Database = require('better-sqlite3');
    new Database(':memory:').close();   // throws here if the ABI is wrong — that is intended
    ```

    **Do not touch `.github/workflows/ci.yml`.** Plan 01 (wave 1) owns that file, and its bump to
    `better-sqlite3` 13.x is what makes this test runnable: 13.0.3 is N-API, ships eight prebuilds, and
    declares no `install` script, so the `test` jobs' `npm ci --ignore-scripts` leaves a working binary
    in place. Adding `npm rebuild better-sqlite3` here would **discard** that prebuild and synthesise a
    `node-gyp rebuild`, which needs Python on the macOS and Windows runners where `setup-python` is
    Linux-gated — breaking the two hard-gate jobs and plan 01's own `grep -c … returns 0` criterion.
    Keep `NODE_VERSION: "22"`; that pin belongs to plan 01 too.

    Where the test actually runs, stated plainly for the SUMMARY: **the CI `test` matrix on Node 22**,
    once wave 1 has landed. On the dev box, whether it runs depends on what `postinstall`'s
    `electron-rebuild -f` leaves behind for a 13.x N-API module — run task 1's probe and record the
    answer rather than predicting it. If it does not open locally, the honest SUMMARY line is
    `db-fts: NOT RUN LOCALLY — <the thrown error>; gated in CI`, and that line must sit next to a
    **link to the green CI run**. Do not write "passed" for a file that was never executed, and do not
    weaken the test so it executes.
  </action>
  <acceptance_criteria>
    - `grep -c "fts5" src/main/db.ts` returns at least `1` (case-insensitive).
    - `grep -c "memory_fts" src/main/memory.ts` returns at least `1`.
    - `node --test test/db-fts.test.cjs` exit code is `0`, and the test file contains **no** `require.cache` injection of `better-sqlite3` — `grep -c "FakeDatabase" test/db-fts.test.cjs` returns `0`.
    - The run is repeated under the TAP reporter and its counters are pasted:
      `out=$(node --test --test-reporter=tap test/db-fts.test.cjs); echo "EXIT=$?"; echo "$out" | grep -E "^# (pass|fail|skipped|todo) "`
      (Captured in a shell variable rather than written to `fts.tap` in the repo root: `.gitignore` has no `*.tap` entry, so a stray TAP file shows up in every later `git status --porcelain` containment check in this phase. The assignment preserves the runner's exit status for the `EXIT=` line, which a pipe would mask.)
      Required: `EXIT=0`, `# pass` at least `4`, `# fail 0`, **`# skipped 0`**, `# todo 0`. The TAP reporter is named explicitly because the default (spec) reporter prints `ℹ skipped 0` instead, and a criterion that greps for the wrong string is itself unfalsifiable. This is the check that closes the skip loophole: `node --test` exits `0` on an all-skipped file (verified at plan time), so exit code alone cannot tell a passing FTS5 test from an absent one.
    - No bypass construct exists anywhere in the file: `grep -cE "\.skip\(|\.todo\(|skip:|todo:" test/db-fts.test.cjs` returns `0`. (Matched deliberately on the constructs, not the bare words — prose explaining *why* the file must not be skipped is expected and must not trip the check.) **The fake this catches:** `t.skip('native module unavailable')`, which turns a missing runtime into a green gate.
    - The `better-sqlite3` load is at module top level, not inside a guard: `grep -cE "^(const|let|var) .*require\('better-sqlite3'\)" test/db-fts.test.cjs` returns at least `1` (column-0 anchored, so a load nested inside a `try` or a function body does not match), and `grep -cE "^try" test/db-fts.test.cjs` returns `0`. **The fake this catches:** `try { require('better-sqlite3') } catch { return }`, an early-return that leaves the file with zero tests and still exits `0`.
    - This plan left CI alone and wave 1's shape held: `grep -c "npm rebuild better-sqlite3" .github/workflows/ci.yml` returns `0` (the same value plan 01 task 2 asserts — the two plans must not drift), this plan authored no commit touching that directory: `BASE=<B-sha from task 1>; test -n "$BASE" && git rev-parse --verify -q "$BASE" >/dev/null && N=$(git log --format=%H "$BASE"..HEAD -- .github/workflows/ | wc -l) && echo "WORKFLOW-COMMITS=$N" || { echo "UNBOUND BASE — CRITERION FAILS"; false; }` prints `WORKFLOW-COMMITS=0`. The `&&` chaining is load-bearing: an earlier form separated the guard with `;`, so execution continued past it — and `git log` with an empty revision range is NOT fatal, it exits 0 with empty output, so `wc -l` printed `0` and the criterion read PASS on a plan that had edited the path. A missing `WORKFLOW-COMMITS=` line is a FAIL, not silence. Bind `$BASE` inside the criterion: unbound, `git log` goes fatal with empty stdout and a `wc -l` of `0`, which reads as PASS on a plan that DID edit CI. The earlier form used `git diff --name-only HEAD -- .github/workflows/`, which compares the worktree to HEAD and is therefore empty once the edit is committed — vacuous exactly when it matters, and `node -e "const l=require('./package-lock.json');console.log(l.packages['node_modules/better-sqlite3'].version)"` prints a `13.` version, proving wave 1's bump is actually in the tree this test ran against. **The fake this catches:** "making the test pass" by rebuilding against the Node ABI in CI, which trades a green FTS5 test for two broken hard-gate jobs.
    - `grep -n 'NODE_VERSION' .github/workflows/ci.yml` still shows `"22"` — unchanged. Bumping it removes the prebuild this step relies on (`ci.yml:9`).
    - If the test was not executed locally, the SUMMARY says `NOT RUN LOCALLY` verbatim with the reason **and** links the green CI run for this branch. An unexecuted test reported as passing fails this criterion outright.
    - The test asserts the table exists by querying `sqlite_master`: `grep -c "sqlite_master" test/db-fts.test.cjs` returns at least `1`.
    - The cross-agent negative assertion is present: the test contains a query scoped to a second agent expecting zero rows; paste that assertion.
    - `grep -c "knowledgeGraph" src/main/config.ts` output is unchanged and the value is still `false`.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/db-fts.test.cjs</automated>
  </verify>
  <done>An append-only FTS5 migration ships, recall is scoped by a real predicate, and the test proves both against a real SQLite handle rather than a fake that executes no DDL.</done>
</task>

<task type="auto">
  <name>Task 3: The honesty and exposure sweep — scope warning, two deletions, the Settings log row, the rename, and the repo-fact clauses</name>
  <files>src/main/index.ts, src/preload/index.ts, src/renderer/src/components/MemoryPanel.tsx, src/renderer/src/components/SettingsModal.tsx, README.md, test/repo-claims.test.cjs</files>
  <read_first>
    - `src/preload/index.ts:820-845` — the file being modified; the three anchors in one region.
    - `src/main/index.ts:4035-4050` — the two `ipcMain.handle` registrations being deleted, and `:4455-4475` which must NOT change.
    - `src/renderer/src/components/SettingsModal.tsx:875-900` — the Home folder row being cloned and the divider at `:897`; also `:696` `setChangeErr` for the inline error pattern.
    - `src/renderer/src/components/MemoryPanel.tsx` — the local `MemoryStatus` interface and where a warning line fits.
    - `src/main/memory.ts:10-21` — the exposure text to surface, verbatim.
    - `01-UI-SPEC.md` § FLOOR-05 — the binding copy and styling contract, reproduced in this plan's `<interfaces>`.
    - `01-PATTERNS.md` § "FLOOR-05 / FLOOR-07 / FLOOR-10 — preload" `:995-1031` — the export shape.
    - `test/repo-claims.test.cjs` in full — plan 05 created it and plan 07 appended; read both before appending again.
  </read_first>
  <action>
    Preload (**this plan is wave 5's sole holder of `src/preload/index.ts`**):
    - Delete `memoryWakeUp` (`:828-829`) and `reflectNow` (`:834-835`). `reflect.ts`'s own timer calls
      the class method directly, so nothing breaks.
    - Add the `openLogs` export exactly as specified in `<interfaces>`, placed with the other
      `{ok, error}` returners near `skillsReveal`.
    - Rename the `:838` section header from *"Enterprise Knowledge Graph (multimodal context for
      agents)"* to what `kg-core.cjs:3-8` honestly calls itself: a keyword knowledge store.

    `src/main/index.ts`: delete the two now-unreferenced `ipcMain.handle` registrations at `:4041` and
    `:4046`. **Two line-level deletions only.** Do not touch `app:openLogs` at `:4461-4469` — it is
    complete and correct.

    `MemoryPanel.tsx`: add `scope` to the local `MemoryStatus` interface and render the `'shared'`
    default as a plain warning line. The exposure is already spelled out verbatim at `memory.ts:14-16`
    — use that language. Also state plainly, in the panel and in `README.md`, that scope is
    **agent-supplied** until RECALL-02 lands in Phase 5, and that the docs must not claim more (D-36).
    Write any new text at `var(--cth-text-body-md)` or above; do not introduce a new sub-14px site
    for wave 6 to sweep.

    `.github/ISSUE_TEMPLATE/bug_report.yml`: plan 04 (wave 2) wrote the Logs field conditionally —
    the by-hand platform log paths plus the Settings route marked as arriving with FLOOR-05 — because
    the button did not exist yet. It exists now. Drop the by-hand paths and the "arriving" marker and
    leave the plain route: Settings → General → Log folder → `open logs`. One field's description;
    add no fields. This discharges plan 04's declared forward dependency; say so in the SUMMARY.

    `SettingsModal.tsx`: add the Log folder row per the UI-SPEC contract in `<interfaces>` — position,
    label, mono path with `—` before first invocation, the `open logs` PixelButton with **no**
    `aria-label`, and the inline error copy in `--cth-coral`. Write the section header at the corrected
    size, not copied at 8px.

    `README.md`: rename the "Enterprise Knowledge Graph" claim, and add the `--wing` limitation sentence.

    Append to `test/repo-claims.test.cjs` — **this plan is wave 5's sole owner of that file**:
    - `src/preload/index.ts` exports `openLogs` (FLOOR-05's exposure clause — the pattern of landing a
      main-process half and never wiring it is this repo's recurring failure mode).
    - `src/preload/index.ts` contains neither `memoryWakeUp` nor `reflectNow`, and `src/main/index.ts`
      contains neither `hive:memoryWakeUp` nor `memory:reflectNow`, after `stripComments`.
    - No file in `README.md` or `src/preload/index.ts` contains the string "Enterprise Knowledge Graph".
    - `src/main/db.ts` contains an FTS5 migration and `MIGRATIONS` has at least two entries — so the
      documented index cannot silently disappear again.
    - `test/db-fts.test.cjs` exists and matches none of `.skip(`, `.todo(`, `skip:`, `todo:` after
      `stripComments`. This runs in a file that
      `npm test` always executes, so it is the guard that survives even when the FTS5 test itself
      cannot load: someone who "fixes" a red CI by skipping the FTS5 test turns this claim red instead
      of turning the suite green. Message: *"the FTS5 test was skipped — a skipped file exits 0, so the
      index would be untested while CI stayed green."*
    - `package.json`'s `better-sqlite3` range is a `13.` major and `.github/workflows/ci.yml` contains
      no `npm rebuild better-sqlite3` and no `continue-on-error`. Reverting to an 11.x range removes
      the N-API prebuild the CI `test` jobs rely on (they install `--ignore-scripts`), and adding the
      rebuild back discards it — either is the cheapest way to make this coverage disappear. Message:
      *"better-sqlite3 is no longer a prebuilt N-API 13.x under --ignore-scripts, so
      test/db-fts.test.cjs cannot load in CI."*
    Every assertion message says what a failure would MEAN.

    Cross-reference in the notes: FLOOR-17's bug template (plan 04) asks the user for "Logs", and until
    this row ships that ask is unanswerable. Confirm the template's wording matches the row that now
    exists.
  </action>
  <acceptance_criteria>
    - `grep -c "openLogs" src/preload/index.ts` returns `1` and `grep -c "openLogs" src/renderer/src/components/SettingsModal.tsx` returns at least `1`.
    - `grep -c "memoryWakeUp\|reflectNow" src/preload/index.ts` returns `0`.
    - `grep -c "hive:memoryWakeUp\|memory:reflectNow" src/main/index.ts` returns `0`.
    - `grep -rc "Enterprise Knowledge Graph" README.md src/preload/index.ts` returns `0` for both.
    - `grep -c "app:openLogs" src/main/index.ts` still returns `1` — today's measured baseline, so this is **a preservation criterion**: the working handler must not be touched.
    - `.github/ISSUE_TEMPLATE/bug_report.yml` parses, and its Logs description names the Settings route with **no** "arriving"/"coming"/"once ... ships" marker and no by-hand platform paths — paste the field. `grep -ciE "arriving|will ship|once .* ships" .github/ISSUE_TEMPLATE/bug_report.yml` returns `0`.
    - `grep -c "scope" src/renderer/src/components/MemoryPanel.tsx` returns at least `1` — **D-35**'s honesty half: the shared-by-default scope is stated as a plain warning in the panel, and the docs say scope is agent-supplied until RECALL-02 lands in Phase 5.
    - `grep -c "aria-label" src/renderer/src/components/SettingsModal.tsx` returns the same value as before this task — no `aria-label` was added to the new PixelButton.
    - `node --test test/repo-claims.test.cjs` exit code is `0`, with a test count at least 6 higher than plan 07's.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>The log folder opens from Settings, the dead exports and their handlers are gone, the scope limitation is stated in UI and docs, the overclaim is renamed, and all of it is pinned.</done>
</task>

<task type="auto">
  <name>Task 5: Apply plan 09's T-INDEX handoff — feed the FLOOR-10 budget arm at the breaker beat, with its wiring test in the same commit</name>
  <files>
    - src/main/index.ts
    - test/engine-parity.test.cjs
  </files>
  <read_first>
    - `.planning/phases/01-finish-the-floor/01-09-SUMMARY.md` — the section headed exactly
      `T-INDEX HANDOFF → 01-10 (FLOOR-10)`. It carries both the verbatim diff to apply AND the
      verbatim wiring test to add. Read it first; plan 09 already did the derivation.
    - `.planning/phases/01-finish-the-floor/01-06-SUMMARY.md` and `01-08-SUMMARY.md` — for the FLOOR-09
      gate in the criteria below.
  </read_first>
  <action>
    **Why this task is here.** `src/main/index.ts` is T-INDEX — one owner per wave — and this plan is
    wave 5's owner. Plan 09 (wave 4) minted `hive.budgetForAgent(agentId)` but could not write
    `index.ts`: plan 08 held it in wave 4, and applying a call to `budgetForAgent` in wave 4 would
    have called a method whose definition had not landed yet. Wave 5 is the first wave where it is
    guaranteed to exist.

    Apply plan 09's recorded diff: one property, `budget: hive.budgetForAgent(id)`, in the existing
    `inputs.push({…})` literal inside `runBreakerBeat`. **Re-derive the location by content** —
    `grep -n "const inputs: BreakerInput\[\]" src/main/index.ts` — never by the line number in this
    document; four waves have edited this file above it.

    **Add plan 09's wiring test in the SAME commit as the injection.** Plan 09 deliberately did not
    ship it: an assertion that lands a wave before the line it asserts is red from the moment its wave
    closes, and `npm test` green is a criterion in every wave-5 plan — one coordination gap would have
    failed four unrelated plans. Landing both together removes the red window without weakening
    anything. The test reads the real source and is reproduced verbatim in plan 09's SUMMARY:

    ```js
    const main = fs.readFileSync(path.join(root, 'src/main/index.ts'), 'utf8');
    // MANDATORY slice + comment strip — see below. Never assert against the whole file.
    const anchor = main.indexOf('const inputs: BreakerInput');
    assert.ok(anchor >= 0,
      'runBreakerBeat no longer builds a BreakerInput[] — re-derive the anchor before touching this test');
    const slice = main.slice(anchor, anchor + 2000).replace(/^\s*(\/\/|\*|\/\*).*$/gm, '');
    assert.match(slice, /budget:\s*hive\.budgetForAgent\(/,
      'the breaker beat must populate BreakerInput.budget, or the budget arm is an optional field nothing ever sets');
    ```

    **Slicing to the `const inputs: BreakerInput[]` anchor and stripping comment lines is MANDATORY,
    not conditional.** Plan 09's SUMMARY reproduces the bare `assert.match(main, ...)` form and says
    to slice only "if the bare regex proves loose" — that is the wrong bar and it is overridden here.
    `src/main/index.ts` is ~5,600 lines, so an unsliced, uncomment-stripped match is satisfied by a
    single line reading `// budget: hive.budgetForAgent(id) — pulled in wave 6` anywhere in the file,
    which would also satisfy a bare `grep -c "budgetForAgent" src/main/index.ts` >= 1 criterion.
    Two greps and a test all passing on one comment is not a wiring pin. The sibling FLOOR-09 pin in
    plan 08 task 6 already makes slicing mandatory ("so the regex cannot be satisfied by an unrelated
    mention"); this is the same rule, applied to the same file, for the same reason.
    Follow `test/hive-task-mutation.test.cjs`'s existing source-assertion idiom.
    **Plan 09's copy of this snippet is not this plan's to edit** — if it still carries the
    conditional wording when you read it, apply the mandatory form anyway and note the divergence in
    the SUMMARY.

    **If `01-09-SUMMARY.md` has no such heading**, plan 09 did not complete its handoff. STOP and
    report `FLOOR-10 HANDOFF MISSING FROM 01-09` — do not invent the accessor's name.

    **Also gate FLOOR-09 here, now that wave 4 has closed.** Plan 06 handed its injection to plan 08;
    both are behind us, so the check is deterministic at this point in a way it was not inside wave 4.
  </action>
  <acceptance_criteria>
    - The FLOOR-10 injection landed, **as code inside the `BreakerInput` literal — not as a mention somewhere in a 5,600-line file.** Two commands, both required:
      (a) `grep -c "budgetForAgent" src/main/index.ts` returns at least `1` — baseline today `0`, so this fails against current source. Necessary but **not sufficient**: a comment satisfies it.
      (b) The hit is real code, positioned inside the literal. Run as one line:
      `AN=$(grep -n "const inputs: BreakerInput" src/main/index.ts | head -1 | cut -d: -f1) && echo "anchor=$AN" && grep -vE "^[[:space:]]*(//|\*|/\*)" src/main/index.ts | grep -cE "budget:[[:space:]]*hive\.budgetForAgent\(" && grep -nE "budget:[[:space:]]*hive\.budgetForAgent\(" src/main/index.ts | awk -F: -v a="$AN" '$1 > a && $1 < a + 60'`
      Required: `anchor` is a real line number, the comment-stripped count is at least `1`, and the `awk` filter emits at least one line — i.e. the property sits within 60 lines below the anchor. Paste all three outputs. Baseline today: the anchor exists, both counts are `0`, and the `awk` filter is empty.
    - The wiring test landed in the **same commit** as the injection — asserted over **this task's own recorded SHAs, never over `HEAD`**. `HEAD` is a global pointer and wave 5 runs plans 10, 11 and 12 concurrently in one tree (`use_worktrees: false`), so any wave-mate commit landing between this task's commit and its verification makes `HEAD` someone else's commit and fails this criterion on unrelated work. Use plan 21 task 4's per-commit idiom instead: record each SHA as you make it (`SHAS="$SHAS $(git rev-parse HEAD)"` immediately after each `git commit` **in this task**), then run, with `SHAS` written out **literally, inside the criterion's own command**:
      `SHAS="<sha1> [<sha2> ...]"; for sha in $SHAS; do i=$(git show --name-only --format= "$sha" | grep -cx "src/main/index.ts"); t=$(git show --name-only --format= "$sha" | grep -cx "test/engine-parity.test.cjs"); echo "$sha index=$i test=$t"; done`
      Required: at least one line reads `index=1 test=1`, and **every** line reads either `index=1 test=1` or `index=0 test=0`. A line reading `index=1 test=0` or `index=0 test=1` means the red window this task exists to avoid was reopened. Paste the literal `SHAS="..."` assignment with the real SHAs substituted, the full loop output, and `for sha in $SHAS; do git show --stat "$sha"; done`.
      **Bind `SHAS` in the same shell invocation as the loop.** An unbound `$SHAS` makes the loop body execute zero times and emit nothing, and a criterion whose failure mode is "no output" would then read as PASS — which is the whole reason this is spelled out. The `echo "$sha index=..."` line exists so that a bound-but-empty `SHAS` shows up as an empty table rather than as silence.
    - `grep -c "budgetForAgent" test/engine-parity.test.cjs` returns at least `1` (baseline `0`) and `grep -c "src/main/index.ts" test/engine-parity.test.cjs` returns at least `1`.
    - The wiring test can fail. Demonstrate it: temporarily remove the `budget:` property, run the file, paste the **non-zero** exit, restore, re-run green.
    - **FLOOR-09's injection is present — hard gate, wave 4 has closed.** `grep -c "recordCostSample" src/main/index.ts` returns at least `1`, and `01-08-SUMMARY.md` contains `FLOOR-09 PRODUCTION INJECTION LANDED`. **If either fails, STOP and report `FLOOR-09 INJECTION MISSING — 01-08 DID NOT APPLY 01-06'S HANDOFF`.** Do not apply it yourself and do not proceed: a budget cap enforcing against a spend number that silently excludes every qwen/crush agent is the "worse than no cap" failure this phase exists to remove. Baseline today `0`, so this criterion fails against current source.
    - **Measure this file's pre-edit `# pass` in this task, before touching it**, and label it **B-pass-ep-w5**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/engine-parity.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Re-run the identical command after the edit. Required post-edit: `EXIT=0`, `# fail 0`, **`# skipped 0`**, `# todo 0`, and `# pass` satisfying **both** bounds — at least **B-pass-ep-w5 + 1** (the delta: this task's one wiring test) **and** at least **`25`** in absolute terms. Paste B-pass-ep-w5 beside the post-edit counters so the delta is checkable on the page.
      **Where `25` comes from, and why "one higher than plan 09's recorded count" was wrong.** `test/engine-parity.test.cjs` has `# pass 14` today. CORRECTED 2026-08-21 — an earlier revision of this paragraph read plan 06 task 4 as `B-pass-ep + 3` / absolute `17`; its actual criterion is `B-pass-ep + 5` and absolute `19`, which moves every number downstream. The chain from the live baseline of `14`, each link measured not assumed: plan 06 task 3 `+2`, plan 06 task 4 `+5` with an absolute of **19**, plan 09 `+6` (worst legitimate branch `+5`) giving **24**, plan 10 `+1` giving **25**, plan 13 `+2` giving **27**. Plan 06 (wave 3) raises it to at least `19` (`B-pass-ep + 2` at its task 3, `B-pass-ep + 5` at its task 4). Plan 09 (wave 4) **re-measures** `B-pass-ep` on that post-plan-06 tree and requires `B-pass-ep + 6`, or `B-pass-ep + 5` in its own documented branch where the D-22 row-shape test lands in `test/hive-protocol-v2.test.cjs` instead — so the worst legitimate post-plan-09 value is `17 + 5 = 22`, and this task's `+1` puts the absolute floor at `25`. But plan 09 *labels* only `B-pass-ep`, which is its **PRE**-count. Reading "plan 09's recorded count" as that label yields a floor of `15` against an actual tree of `24`–`25`, which would let this task rewrite the file and drop five or six of plan 09's tests while still passing. Hence: measure it here, and carry an absolute floor as well.
      **Two hard STOP conditions.** If B-pass-ep-w5 measures **below `24`**, plan 09's tests are not all in the tree — STOP and report `ENGINE-PARITY BELOW PLAN 09 FLOOR — <measured> < 24`; do not proceed and do not lower the number. If B-pass-ep-w5 is below the post-run `# pass` recorded in `01-09-SUMMARY.md`, report the discrepancy with both numbers.
    - **Plan 09's and plan 06's tests survived by name, not merely by count.** A count floor alone is satisfied by deleting six tests and adding seven. Assert the content the way plan 13 does: `grep -c "'constrained'" test/engine-parity.test.cjs` returns at least `2` (baseline today `0` — plan 09 raises it; these are its breaker-budget cases), and `grep -c "recordCostSample" test/engine-parity.test.cjs` returns at least `6` (baseline today `6` — the six pre-existing calls plan 06 task 3 is required to leave passing unchanged). Paste both counts. A drop in either is a finding to report, not a number to relax.
    - `grep -cE "\.skip\(|\.todo\(|skip:|todo:" test/engine-parity.test.cjs` returns `0`. **The fake this catches:** closing the wiring test with `{ skip: 'pending' }`, which turns the one blocker the handoff exists to surface into a green gate.
    - `npm run typecheck` and `npm test` exit `0`.
    - `01-10-SUMMARY.md` records `FLOOR-10 BUDGET ARM FED IN PRODUCTION (from 01-09 handoff)`, closing the window plan 09's SUMMARY opened.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck && node --test test/engine-parity.test.cjs</automated>
  </verify>
  <done>The breaker beat feeds the budget arm from the production path, the assertion that proves it landed in the same commit, and FLOOR-09's injection is confirmed present rather than assumed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent-supplied `--wing` scope → memory recall | The scope predicate is supplied by the agent asking. This is a **known, documented** weakness until RECALL-02 enforces it server-side in Phase 5; the control here is honesty, not enforcement. |
| renderer → `app:openLogs` (a path-opening IPC) | An IPC that opens a directory in the OS file manager. The handler takes no caller-supplied path, which is what keeps it safe. |
| migration DDL → a shipped user database | A bad migration can leave a user's store unusable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P10-01 | Information disclosure | one agent recalling another agent's or project's notes | mitigate (partially) + document | The FTS5 schema makes scope a `WHERE agent_id = ?` predicate and the cross-agent negative assertion pins it. The predicate's INPUT is still agent-supplied — RECALL-02 (Phase 5) is the real fix, and D-36 requires the docs and UI to say exactly that rather than claiming more. |
| T-P10-02 | Information disclosure | operator unaware memory defaults to shared | mitigate | `MemoryPanel` renders the `'shared'` default as a plain warning using the language already at `memory.ts:14-16`. |
| T-P10-03 | Tampering | FTS5 injection via unsanitised MATCH input | mitigate | Use bound parameters for both the MATCH term and the scope predicate; never build the query by string concatenation. |
| T-P10-04 | Denial of service | a failed or half-applied migration bricking the store | mitigate | Append-only migration inside the existing single-transaction runner; no `throw` added inside a migration; the corrupt-DB quarantine path at `db.ts:82-96` remains the backstop. |
| T-P10-05 | Tampering | path traversal via a path-taking IPC | accept | `app:openLogs` takes no caller-supplied path — it opens `logsDir()`. No new path-taking IPC is added. If one ever is, copy `skills:reveal`'s `safeJoin`/`isWithinRoots` shape (`index.ts:3983-3991`). |
| T-P10-06 | Repudiation | shipping the FTS5 migration untested because the fake DB reported success | mitigate | `test/db-fts.test.cjs` uses a real handle and queries `sqlite_master`; the acceptance criteria assert `FakeDatabase` appears nowhere in it. |
| T-P10-07 | Repudiation | docs claiming an "Enterprise Knowledge Graph" | mitigate | Renamed in `README.md` and `preload/index.ts:838` to what `kg-core.cjs:3-8` calls itself; pinned by `test/repo-claims.test.cjs`. |
</threat_model>

<verification>
- `npm test` green on all three platforms, including the new `test/db-fts.test.cjs`. That file
  must appear in the run as PASSING, not skipped: an all-skipped file exits `0`, so "green" is only
  meaningful here alongside a `# skipped 0` counter from `--test-reporter=tap`. If it did not execute locally, say so verbatim
  and link the CI run that did execute it — never report an unexecuted file as green.
- `npm run typecheck` green.
- Manual: open Settings → General in the dev app, click `open logs`, and confirm the OS file manager
  opens the log folder. Then confirm the Memory panel shows the shared-scope warning.
</verification>

<success_criteria>
- ROADMAP criterion 5's memory clause is TRUE: memory recall returns only the asking agent's or
  project's notes, backed by a real SQLite FTS index in the already-open `PersistStore` — with the
  `--wing` limitation stated plainly and no doc claiming more.
- ROADMAP criterion 4's FLOOR-05 clause is TRUE: the log folder opens from Settings.
- Issues #13, #16, #31 and #32 close in their PRs per D-43, with the empty `openLogs` and `fts5` greps
  pasted as the evidence those clauses were open.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-10-SUMMARY.md` when done. Record the FTS5
re-verification result after the 13.x bump, whether `CREATE VIRTUAL TABLE IF NOT EXISTS` was accepted,
and the exact new `user_version`.

It MUST also record, in these words or clearer:
- Where `test/db-fts.test.cjs` was executed — `run locally` with the pasted output, or
  `NOT RUN LOCALLY — <the error new Database(':memory:') threw>; gated in CI` **plus a link to the
  green CI run**. Do not add a CI rebuild step to make it run — see task 2.
- The pasted `node --test --test-reporter=tap test/db-fts.test.cjs` counters showing `# skipped 0`
  and `# todo 0` (the default reporter prints `ℹ skipped 0` — either is fine to paste, but say which).
- The runtime FTS5 probe output with its `EXIT` line, not the `require`-only variant.
Anything less and this plan has shipped an untested migration behind a green gate, which is the exact
failure `test/db-fts.test.cjs` exists to prevent.
</output>
