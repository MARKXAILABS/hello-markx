---
phase: 01-finish-the-floor
plan: 31
type: execute
wave: 4
depends_on:
  - "01-24"
  - "01-25"
  - "01-26"
  - "01-27"
  - "01-28"
  - "01-29"
  - "01-30"
gap_closure: true
files_modified:
  - HIVE.md
  - docs/adr/0005-cumulative-cost-ledger.md
  - src/main/config.ts
  - src/renderer/src/store/config.ts
  - resources/skills/capabilities/SKILL.md
  - test/repo-claims.test.cjs
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-finish-the-floor/01-VALIDATION.md
autonomous: true
requirements: [FLOOR-01, FLOOR-04, FLOOR-07, FLOOR-13, FLOOR-17, GATE-01]

must_haves:
  truths:
    - "The six anchors that point at unrelated code are gone, replaced by `<file>.ts <symbol>()` naming, and the four that were already correct were re-checked rather than trusted"
    - "The retired product name survives only in the audit record and in the pin that searches for it — and the pin's corpus provably includes `resources/skills/capabilities/SKILL.md`, `README.md` and every other shipped `.md`"
    - "No shipped doc still states a 1280 window minimum, checked from this plan even though 01-25 owns the file that states it"
    - "The frozen win32 skip ceiling records the count MEASURED on this host, names every member, and says why the ceiling moved"
    - "Every code residual this gap-closure set does not fix has a named owner in one register, and that register is DERIVED by sweeping the seven SUMMARYs this plan loads rather than copied from a fixed list — none is filed as something an operator merely has to look at"
  artifacts:
    - path: "HIVE.md"
      provides: "the drain anchors written symbol-first, with the six stale line numbers gone"
      contains: "hive.ts drainForStop()"
    - path: "docs/adr/0005-cumulative-cost-ledger.md"
      provides: "the cost-ledger writer cited by symbol instead of a line 89 off"
      contains: "index.ts appendCostLedger()"
    - path: "test/repo-claims.test.cjs"
      provides: "a shipped-surface naming pin over a real corpus, a denial table for the six stale anchors, and a cross-file pin that no doc claims a 1280 minimum"
      contains: "STALE_ANCHORS"
    - path: ".planning/REQUIREMENTS.md"
      provides: "rows restated against the landed code, each with the command AND output that verified it"
      contains: "SECRET_SCAN_MAX_BYTES"
    - path: ".planning/phases/01-finish-the-floor/01-VALIDATION.md"
      provides: "the win32 skip set re-derived, enumerated by title, with the ceiling move justified"
      contains: "hook-auth-roundtrip"
  key_links:
    - from: "test/repo-claims.test.cjs"
      to: "every shipped source and doc file"
      via: "an explicit-root walker with an extension allow-list, not the .ts-only renderer walker"
      pattern: "shippedTextFiles"
    - from: "test/repo-claims.test.cjs"
      to: "DESIGN.md's window minimum"
      via: "a doc-corpus assertion that no shipped doc states 1280 × 800, so 01-25's edit cannot fall between the two plans"
      pattern: "1280"
---

<objective>
Close the doc-residual half of this phase: six anchors that point at unrelated code, an ADR citation 89
lines off, the last sites of a retired product name, the requirement rows that still describe the
pre-gap-closure tree, and the code residuals nobody in the set is fixing.

Purpose: `01-VERIFICATION.md`'s SC-5 residual and code-review `c/WR-07`, `c/WR-09`, `c/IN-02`.
ROADMAP criterion 1 is *"grep finds no doc promising a code path that does not run"*.
`01-23-SUMMARY.md` records these anchor corrections as APPLIED; verified at source this session, only
`HIVE.md:90-91` and `:295` were actually updated.

This plan runs LAST so its pins are written against the finished tree. That is also why it — not
01-30, which runs in wave 2 and cannot see 01-29's or 01-31's own additions — owns the authoritative
whole-suite figure.

Output: symbol-first anchors, a naming pin whose corpus is proved rather than asserted, honest
requirement rows, and one register in which every unfixed residual has an owner.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-finish-the-floor/01-VERIFICATION.md
@.planning/phases/01-finish-the-floor/01-REVIEW-c-tests.md
@.planning/phases/01-finish-the-floor/01-GAP-CHECK.md
@.planning/phases/01-finish-the-floor/deferred-items.md
@.planning/phases/01-finish-the-floor/01-24-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-25-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-26-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-27-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-28-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-29-SUMMARY.md
@.planning/phases/01-finish-the-floor/01-30-SUMMARY.md
@HIVE.md
@test/repo-claims.test.cjs

<interfaces>
<!-- Every command below was RUN in this session at HEAD a217018 and its output is pasted. Round 1's
     numbers were carried from 47a48cd; these are re-derived. RE-DERIVE THEM AGAIN before editing:
     01-24 inserts into hooks.ts above :663, 01-25 into index.ts above :545, 01-26 into hive.ts at
     :1336-1339 (above :1365 AND :1375), and 01-27/01-28 both edit delivery.ts. Every number here
     will have moved by wave 4. Write SYMBOLS; that is the whole point of the task. -->

### The anchor sweep, complete — 13 anchors, 6 stale

`grep -noE '(hooks|delivery|index|hive|pty|breaker|telemetry)\.ts:[0-9]+' HIVE.md docs/adr/*.md`, then
`sed -n '<n>p'` on each named file. Run this session:

| doc site | anchor | `sed -n '<n>p'` at HEAD | verdict |
|---|---|---|---|
| `HIVE.md:90` | `hooks.ts:663` | `const drain = this.drainAtStop?.(agentId);` | ✅ correct |
| `HIVE.md:91` | `delivery.ts:604` | `drainAtStop(agentId: string): { block: boolean; reason?: string } {` | ✅ correct |
| `HIVE.md:91` | `index.ts:545` | `(agentId) => delivery.drainAtStop(agentId),` | ✅ correct |
| `HIVE.md:95` | `hooks.ts:646` | `if (p.stop_hook_active) { this.emit(agentId, event, p); return {}; }` | ✅ correct |
| `HIVE.md:116` | `hive.ts:1338` | `this.revokeProxyToken(agentId);` | ❌ **stale** |
| `HIVE.md:138` | `hive.ts:1338` | `this.revokeProxyToken(agentId);` | ❌ **stale** |
| `HIVE.md:139` | `hooks.ts:662` | `// drain keeps working headless — which is the whole point of issue #5.` | ❌ **stale** (a comment; the call is `:663`) |
| `HIVE.md:250` | `hooks.ts:662` | same comment line | ❌ **stale** |
| `HIVE.md:250` | `delivery.ts:262` | `*  was asserted so a dead renderer's veto expires (VETO_TTL_MS). */` | ❌ **stale** |
| `HIVE.md:293` | `hooks.ts:646` | `if (p.stop_hook_active) …` | ✅ correct |
| `HIVE.md:295` | `hive.ts:1375` | `cursor.lastProcessed = fresh[fresh.length - 1].id;` | ✅ correct |
| `docs/adr/0001-…:20` | `delivery.ts:518` | `private async drainQueue(live: LiveAgentPty[], now: number): Promise<void> {` | ✅ correct |
| `docs/adr/0005-…:52` | `index.ts:1524` | `const log = hive.logTail(8).map(…)` — the god-beat prompt builder | ❌ **stale** |

**Six stale anchors across five doc lines.** Round 1 said "four" and asserted `:90-91` / `:295` were
fine *"and not to look"* — they are fine at HEAD, but 01-24, 01-25 and 01-26 all insert above them, so
they will be stale on arrival in wave 4. **Check all thirteen again; trust none of this table.**

The correct targets, located by symbol this session:
```
$ grep -n "drainForStop" src/main/hive.ts
1365:  drainForStop(agentId: string): { block: boolean; reason?: string } {     ← the function
   (its cursor write is the `cursor.lastProcessed = …` line, hive.ts:1375 at HEAD)
$ grep -n "appendCostLedger" src/main/index.ts
1613:    if (sample?.sessionId) hive.appendCostLedger(sample); // ledger covers everyone incl. god
```
`docs/adr/0001-one-gate-for-pty-writes.md` is NOT in this plan's `files_modified` — its one anchor is
correct at HEAD; if wave 1-3 moved `delivery.ts:518`, report it rather than editing an unowned file.

### The retired name — measured over the RIGHT corpus

Round 1 derived its site list from `grep -rn "Enterprise Knowledge Graph" src resources docs
README.md`, which excludes `test/`, which is why its `<done>` gate was unsatisfiable. Full tracked
list, `git grep -n "Enterprise Knowledge Graph"` run this session, `.planning/` rows elided:

```
resources/skills/capabilities/SKILL.md:96     ← agent-facing, shipped into every agent's skills dir
src/main/config.ts:159 / :275 / :493
src/renderer/src/store/config.ts:74 / :142
src/main/hive.ts:1455                         ← plan 01-26's file. VERIFY, do not edit.
docs/floor-inspection.html:710                ← the audit record QUOTING the defect. Excluded.
test/repo-claims.test.cjs:28 / :338 / :346    ← the pin itself. A pin must contain its needle.
.planning/codebase/CONCERNS.md:46             ← see the residual register; out of the pin's scope
```
And the same string, `grep -rn` with the plan's own round-1 exclusions, also returns
`./dist/win-unpacked/resources/app.asar`, `./dist/win-unpacked/resources/skills/capabilities/SKILL.md`
and `./out/main/index.js` — build outputs. **A test walker with no build-output exclusion is
permanently red on any machine that has run `npm run build`.**

What the store actually is, per `src/main/kg-core.cjs:3-8`: keyword scoring over text chunks, term
frequency plus a title boost, no entities and no edges. `INTEGRATIONS.md` already says it is not a
graph. V2-05 (the entity graph) was formally RETIRED, not deferred.

### The walker — do NOT reuse `sourceFiles`

Round 1 said *"reuse the file walker the rest of that file already uses (`:98`)."* Read at source
(`test/repo-claims.test.cjs:66-74`, this session):

```js
function sourceFiles(dir) {
  …
  else if (/\.tsx?$/.test(entry.name)) out.push(…);     // ← .ts / .tsx ONLY
}
```

Built as instructed, the "tree-wide" pin would **never read `SKILL.md`** — its own highest-value site —
and would **drop `README.md`, which the two-file loop it replaces already covers.** It would ship
strictly weaker while reporting wider, and `files.length > 50` would go green over the `.ts` corpus so
the anti-vacuity guard would fire clean. It is also unscoped: `sourceFiles(root)` from the repo root
walks `node_modules`.

A correct walker, **prototyped and run this session**: explicit roots
`['src','resources','docs','test','scripts','e2e']` plus tracked top-level `*.md`, extension allow-list
`/\.(tsx?|cjs|mjs|jsx?|md|html|ya?ml)$/`, directory denies for `node_modules` and `.git`. Measured:

```
corpus size = 312
EKG hits: [ "src/main/config.ts", "src/main/hive.ts", "src/renderer/src/store/config.ts",
            "resources/skills/capabilities/SKILL.md", "docs/floor-inspection.html",
            "test/repo-claims.test.cjs" ]
SKILL.md in corpus: true    README.md in corpus: true    DESIGN.md in corpus: true
```
`dist/`, `out/` and `.planning/` are outside by CONSTRUCTION, not by special-case exclusion. Of the six
hits, three are this plan's to rename, one is 01-26's, and two are excluded by explicit path.

Restricting the doc half of that corpus to `/\.(md|html)$/` gives **43 files**, and the 1280 scan over
it returns exactly one hit — `DESIGN.md`. Both measured this session.

### `test/repo-claims.test.cjs`, other facts read this session

- `:338-352` is the two-file loop (round 1 said `:337-352`; `:337` is blank). Its header explains why
  this one test is deliberately NOT comment-stripped — the claim is made IN prose and IN doc comments.
  **That reasoning survives the widening; do not route the widened scan through `stripComments`
  (`:63`), which has its own unfixed string-truncation defect (`c/WR-13`, out of this gap set).**
- `:98` `files.length > 50` is the corpus floor for `sourceFiles(rendererRoot)` — a RENDERER-tree
  figure. Do not reuse the number for a 312-file corpus.
- `:214-233` `STALE_STOP_DRAIN_DENIALS` is the precedent for a content-located denial table, and it
  works: it survives refactors because it names historical strings, not positions. **Its comment at
  `:231` says *"eleven fails here"* while `:233` asserts `12` and the array holds 12** (counted this
  session). One-word fix, this plan's file.
- `:646-669` is the FLOOR-12 clause-4 pin (`:660` is only the `}` closing the inner `for`; the test
  runs on through the `FullscreenTerminal.tsx` clause to `:669` — re-read this session). Its failure message ALREADY carries the RENDER_SCALE caveat
  verbatim — *"both classes draw inside a container held at RENDER_SCALE = 0.5, so the DESIGNED
  on-screen size is half this number. Raising FONT_SIZE is necessary and not sufficient."* Read it
  before touching it; the ceiling is filed, the arithmetic is not fixed. See the residual register.

### DESIGN.md — 01-25's, cross-checked from here

```
DESIGN.md:169  - Main window minimum: 1280 × 800.
DESIGN.md:677  Min window: 1280 × 800. Right panel collapses below 1024 to bottom drawer.
```
Both become false when 01-25 sets `MIN_WIN.width = 960` (`src/main/index.ts:2516` today).
**`DESIGN.md` is NOT in this plan's `files_modified` — 01-25 owns and edits it.** This plan adds the
criterion that no shipped doc still states 1280, so the gap cannot fall between the two plans: if
01-25 misses it, this plan's pin is RED and says so loudly.

### The win32 skip set

`01-VALIDATION.md:52-55` pins *"the frozen skip baseline for this phase is `# skipped 4`"* and states
the rule this plan must respect while moving it:

> *"`# skipped` **≤ 4** — not `>=`, never `>=` — are the standing floor. A `>=` skip clause permits
> skip growth by construction and is the exact shape that lets a phase close green on skipped work."*

It names **no titles** — round 1 claims it *"names all four by title"*; it does not. (01-30 makes no
such claim: it says only that VALIDATION pins `# skipped 4` and forbids a `>=` clause, which is true.
Round 1's substantive point is wrong; its attribution to 01-30 was also wrong.)
The named list lives in `01-23-SUMMARY.md:181`, which is in no plan's `files_modified` and will keep
its `# skipped 4` figure permanently as a historical record. The four live cases,
`grep -rn "{ skip:" test/*.test.cjs` this session:

```
test/hive-hook-node.test.cjs:153     'a hook fires with NO node on PATH, and its payload reaches HIVE_SOCK'
test/hive-runtime-path.test.cjs:83   '`node` resolves and RUNS with no node on PATH — the whole point'
test/hook-auth-roundtrip.test.cjs:95 'the real shim authenticates to the real hook server'
test/hook-auth-roundtrip.test.cjs:128 'a shim with no token is still rejected'
```

**Do not hardcode the new count.** Round 1 wrote *"the honest count is 6"*. Revised 01-30 settles what
it takes: `test/win-cmd-shim.test.cjs` converts (win32 `# skipped` +1); `test/transcript-project-dir.test.cjs`
SPLITS into a case that RUNS on win32 and skips on POSIX (win32 +0, ubuntu/macOS +1); and
`test/proc-kill.test.cjs` is **explicitly declined**. With 01-24's conversion that is a win32
expectation of 6 — but it is an EXPECTATION, not a figure to publish. **Derive the number from the TAP
counter on this host and reconcile it against 01-30's SUMMARY; on disagreement the measurement wins and
the discrepancy is reported.** Round 1's "7 or 8" hedge is stale and must not be carried forward as a
target.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Anchors that point at what they name</name>
  <files>HIVE.md, docs/adr/0005-cumulative-cost-ledger.md</files>
  <action>
    Correct the six stale anchors. **Locate them by the wrong string, never by the doc line number:**
    `grep -n "hive\.ts:1338\|hooks\.ts:662\|delivery\.ts:262" HIVE.md` returns the four in `HIVE.md`
    (two of them on one line) and `grep -n "index\.ts:1524" docs/adr/0005-cumulative-cost-ledger.md`
    returns the fifth site. Those are the same six strings task 2's `STALE_ANCHORS` table denies, so the
    two halves of this plan cannot drift apart. This task edits `HIVE.md` in four places; a doc line
    number is wrong after the first one.

    **Write the symbol, in a form a grep can check.** Use `<file>.ts <symbol>()` — `hive.ts
    drainForStop()`, `delivery.ts drainAtStop()`, `index.ts appendCostLedger()` — and `hooks.ts`'s Stop
    boundary for the call site. A bare line number is a claim that expires on the next edit, and these
    six expired inside the phase that wrote them. Where a number genuinely helps navigation keep it
    ALONGSIDE the symbol, never instead of it; but do not write a number you have not just re-derived.

    Re-derive against the tree as it stands AFTER 01-24 to 01-30. `hive.ts:1338` moves (01-26 edits
    `:1336-1339`), `hooks.ts:663` moves (01-24 inserts above it), `index.ts:545` and `:1613` move
    (01-25), `delivery.ts` moves twice (01-27, then 01-28). A correction written from this plan's
    `<interfaces>` table without re-checking lands stale on arrival — the exact failure being fixed.

    Then sweep ALL THIRTEEN, including the four this plan's table calls correct and ADR-0001's. Report
    the full list with each `sed -n '<n>p'` result in the SUMMARY — including the ones that were already
    right. `01-23-SUMMARY.md` reported this sweep as done when six of thirteen were not, so the evidence
    must be the check, not the claim.

    Do not add a line-number pin. A pin on `:NNN` is red on every unrelated refactor, which is how it
    ends up disabled; the durable half is in task 2's denial table, which names the six historical wrong
    strings and can never be red for a refactor reason.
  </action>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>
    All thirteen anchors re-checked with the output pasted. Pre-edit measurements, RE-RUN and pasted,
    that were RED at HEAD a217018 and must be GREEN after:
    `grep -c "hive\.ts drainForStop()" HIVE.md` → **0 at HEAD**;
    `grep -c "delivery\.ts drainAtStop()" HIVE.md` → **0 at HEAD**;
    `grep -c "index\.ts appendCostLedger()" docs/adr/0005-cumulative-cost-ledger.md` → **0 at HEAD**;
    `grep -c "hive\.ts:1338\|hooks\.ts:662\|delivery\.ts:262" HIVE.md` → **4 at HEAD, must be 0**;
    `grep -c "index\.ts:1524" docs/adr/0005-cumulative-cost-ledger.md` → **1 at HEAD, must be 0**.
  </done>
</task>

<task type="auto">
  <name>Task 2: The retired name goes, and the pins that hold the line cover what they claim to</name>
  <files>src/main/config.ts, src/renderer/src/store/config.ts, resources/skills/capabilities/SKILL.md, test/repo-claims.test.cjs</files>
  <action>
    **Rename.** Every site `git grep -n "Enterprise Knowledge Graph" -- src resources ':!.planning'`
    returns that is not `src/main/hive.ts`: three in `src/main/config.ts`, two in
    `src/renderer/src/store/config.ts`, one in `resources/skills/capabilities/SKILL.md`. **Locate them by
    the string, not by the line numbers in `<interfaces>`** — 01-26 shifts `config.ts`'s neighbours and
    each rename shifts the next one in the same file. Use `src/main/kg-core.cjs`'s own words: keyword
    scoring over text chunks, term frequency plus a title boost, no entities and no edges. `SKILL.md:96`
    matters most — it ships into every agent's skills directory, so today the agents themselves are told
    a retired capability is available. Verify `src/main/hive.ts:1455` was renamed by 01-26 and do NOT
    edit that file.

    **Widen the pin — the test titled *'the keyword store is not described as an "Enterprise Knowledge
    Graph" (#31, FLOOR-07)'* — with a NEW walker.** Locate it by that title: this task makes four
    separate edits to `test/repo-claims.test.cjs` (the widened pin, `STALE_ANCHORS`, the 1280
    cross-check, the `eleven fails here` comment), so any line range here is wrong after the first one.
    Do not reuse `sourceFiles` — it is `.ts`/`.tsx` only, so it would not read `SKILL.md` and would drop
    `README.md`, which the loop it replaces already covers. Write `shippedTextFiles()`: explicit roots
    `['src','resources','docs','test','scripts','e2e']` plus tracked top-level `*.md`, extension
    allow-list `/\.(tsx?|cjs|mjs|jsx?|md|html|ya?ml)$/`, directory denies for `node_modules` and `.git`.
    `dist/`, `out/` and `.planning/` fall outside by construction — do not add them as exclusions,
    because an exclusion list is a thing to forget and a root list is a thing to read.

    Exclude exactly two paths, each with the reason written into the exclusion:
    `docs/floor-inspection.html` (the audit record quoting the defect — erasing the quotation would
    delete the finding) and `test/repo-claims.test.cjs` itself (a pin must contain its own needle;
    without this the test cannot pass, which is what `01-REDTEAM-5` R-32 demonstrated).

    Keep it NOT comment-stripped — the header's reasoning survives the widening. Assert the corpus
    before asserting it is clean: `files.length > 200` (measured 312 this session), so a broken walker
    and a clean tree stay distinguishable. Rename the test so its title says **shipped surfaces**, not
    "tree-wide": the corpus does not include `.planning/`, and a title that overstates its scope is the
    defect this phase exists to remove.

    **Demonstrate RED against `resources/skills/capabilities/SKILL.md` specifically**, not against a
    convenient `.ts` file. That file's coverage is the exact thing round 1 got wrong, so it is the only
    RED that proves anything. Run the widened pin BEFORE the last rename, paste the failure naming
    `SKILL.md`, then rename and paste the GREEN.

    **Add `STALE_ANCHORS`** — a content-located denial table in the shape of `STALE_STOP_DRAIN_DENIALS`
    (the array and the `assert.equal(STALE_STOP_DRAIN_DENIALS.length, 12, …)` beneath it): the six
    historical wrong anchor strings (`hive.ts:1338` ×2, `hooks.ts:662` ×2,
    `delivery.ts:262`, `index.ts:1524`) asserted absent from `HIVE.md` and `docs/adr/`, with the
    assertion message naming the symbol each should have been. It is RED at HEAD (4 + 1 hits, measured)
    and it can never go red for a refactor reason, because it names strings that were wrong in the past
    rather than positions that are right in the present.

    **Add the 1280 cross-check.** Over the `/\.(md|html)$/` slice of the same corpus (43 files,
    measured), assert no shipped doc states a 1280 window minimum
    (`/1280\s*[×x]\s*800|minimum:?\s*1280/i`). `DESIGN.md:169` and `:677` both match at HEAD and
    **`DESIGN.md` is 01-25's file, not this plan's** — this pin exists so the correction cannot fall
    between the two plans. If 01-25 has not made it by wave 4, this pin is RED and the SUMMARY must
    report it as an unclosed hand-off rather than working around it.

    While in this file, fix the comment reading *"eleven fails here instead of passing there"* — located
    by that text — while the assertion beneath it asserts `12` and the array holds 12 (counted this
    session).
  </action>
  <verify>
    <automated>node --test test/repo-claims.test.cjs</automated>
  </verify>
  <done>
    The widened pin was demonstrated RED **against `SKILL.md`** and is now GREEN, both outputs pasted.
    Pre-edit measurements, RE-RUN and pasted, that were RED at HEAD a217018:
    `grep -c "shippedTextFiles" test/repo-claims.test.cjs` → **0**;
    `grep -c "STALE_ANCHORS" test/repo-claims.test.cjs` → **0**;
    `grep -c "1280" test/repo-claims.test.cjs` → **0**.
    `git grep -l "Enterprise Knowledge Graph" -- src resources docs test scripts e2e '*.md' ':!.planning'`
    names **exactly two files and no others**: `docs/floor-inspection.html` (the audit record quoting
    the defect) and `test/repo-claims.test.cjs` (the pin's own needle). Both are the two exclusions
    written into the walker, so the gate and the pin agree by construction.

    **The gate keys on the FILE SET, not on a line count**, so adding further self-references inside
    `test/repo-claims.test.cjs` — `STALE_ANCHORS`, the widened pin's failure message — cannot falsify
    it. Measured against the tree this revision was written on: that command returns **11 lines across
    6 files** today, of which one is 01-26's `src/main/hive.ts` and six are this task's.

    **Two forms are NOT gates and must not be substituted.** (a) The same command **without**
    `':!.planning'` returns **71 lines**: git's `*.md` pathspec matches at any depth, so it sweeps all
    of `.planning/` — `01-10-SUMMARY.md` ×11, `01-23-SUMMARY.md` ×4, the red-team reports, and this
    plan's own text. Satisfying it would require editing historical records this plan explicitly
    forbids touching. That was round 1's gate and it was unsatisfiable; do not restore it. (b) The raw
    `grep -rn … --exclude-dir=node_modules --exclude-dir=.git --exclude-dir=.planning` form also matches
    `dist/` and `out/` on any machine that has run `npm run build`.
  </done>
</task>

<task type="auto">
  <name>Task 3: The requirement rows, the skip ceiling, and the residuals that have no owner</name>
  <files>.planning/REQUIREMENTS.md, .planning/phases/01-finish-the-floor/01-VALIDATION.md</files>
  <action>
    **Restate the rows — from source, with receipts.** Read every SUMMARY from 01-24 to 01-30, then
    verify each claim AT SOURCE before writing it. For every restated row, paste the **command AND its
    output** in the SUMMARY, not the command alone: a list of commands with no output is
    indistinguishable from a list of commands never run, and SUMMARY-trust is how these rows drifted the
    first time.

    - **FLOOR-04 — READ 01-26's LANDED SUMMARY FIRST. Do NOT restate this row from the sentence below
      without checking it.** An earlier revision of this plan asserted *"the matcher now covers
      underscore prefixes and JSON value position"*. **01-26 revision 4 (`473d961`) WITHDREW the value
      widening**: its objective opens *"This revision does NOT widen the value matcher. That is the
      deliverable"*, and *"The JSON arm is DROPPED."* It ships a **passing** durability assertion that
      `"token": "abcdef123456789"` is **still present** in `git log -p`. Writing the old sentence into
      `.planning/REQUIREMENTS.md` would put a claim in the requirements ledger that 01-26's own green
      test contradicts — ROADMAP criterion 1's exact defect class, manufactured by the plan that closes
      it.

      **What actually shipped in 01-26, and what this row must say:** pattern 5 (the generic
      `key=value` arm) is **byte-frozen and pinned**; only pattern 3's prefix boundaries changed (a
      `\b` on the existing alternatives plus two appended `sk_`/`rk_` arms, which cannot subtract from
      the others). Measured delta: **0 detections lost, 0 new false positives, 5 gained.**

      Restate the ceiling to **all four ACTIVE bypasses 01-26 names**, matching the ceiling text it
      writes into `src/main/hive.ts` so the two cannot contradict each other in one wave:
      **(1)** line-chunking — a key split across two source lines matches nothing, because the commit
      path is line-oriented; **(2)** quoted-key JSON (`"token": "…"`) — the arm was built, measured and
      deliberately rejected; **(3)** `SECRET_SCAN_MAX_LINES` and **(4)** `SECRET_SCAN_MAX_BYTES`, both
      agent-triggerable in one command (`seq 20001 > pad.txt`), which commit unscanned.
      **Cite the two constants by NAME, not by line** — 01-26 edits above them, so round 1's
      `:3196-3226` is stale by wave 4.

      **Re-home the owner.** `.planning/REQUIREMENTS.md:560` currently ends *"Owner: a plan that widens
      the matcher, plus the operator."* **No plan widens it any more**, so that owner resolves to
      nobody — which this register's own preamble forbids. Re-home it to a follow-up plan holding
      `src/main/hive.ts`, and record 01-26's measured finding as the reason the widening was withdrawn:
      the matcher fires on **173 spans across 471 tracked files, none of them credentials**, and would
      have silently unstaged a path in **50 of the last 400 commits (12.5%), across 66 distinct
      paths**. The requirement's own word
      is *"never"*; a best-effort matcher cannot deliver an unconditional never, so the row must say
      what it does deliver. Live clause stays the operator's.
    - **FLOOR-13** — the collapse is now reachable: `MIN_WIN.width` is 960 (01-25), a cross-file pin
      ties it to `SIDEBAR_COLLAPSE_WIDTH` (01-29), and `DESIGN.md` states 960 (01-25). The row records
      the opposite today. State that the resolution was to lower the window floor rather than delete the
      feature, and why. Leave D-22's `{tokens, budgetTokens, pct}` adjudication as it stands.
    - **FLOOR-01** — the AUTO chip reads the operator's own command on the `custom` arm (01-29). Name the
      surviving `opencode` ceiling and the `autoFlag`/`autoModeFlag` field drift.
    - **FLOOR-07** — the naming residual is closed except the audit record and the pin's own
      self-reference, both excluded on purpose. `--wing` scope stays RECALL-02's (Phase 5).
    - **GATE-01** — the second identity channel is closed (01-25); the path gate resolves relative
      targets (01-24). The `/proc/<pid>/environ` sibling-read ceiling is unchanged and still GATE-02's.
    - **FLOOR-06** — the attested subject covers the update feed (01-30). The live
      `gh attestation verify` sample is still unrun and still the operator's.
    - **FLOOR-02 / FLOOR-10 / RECORD-03** — 01-27 and 01-28 changed behaviour behind these (the budget
      band, the queue loader, the blocked-agent guard, the one-shot roster publication). Check whether
      each row's evidence sentence is still literally true and correct it if not.

    **Do not tick any `[ ]` row.** Ticking is the verifier's act after re-verification, and every one of
    these rows also carries an operator clause this set cannot discharge.

    **The win32 skip set.** `01-VALIDATION.md`'s sentence *"The frozen skip baseline for this phase is
    `# skipped 4`"* and its *"`≤ 4` — not `>=`, never `>=`"* rule — located by that text, not by a line
    range, because this task edits the same document — pin the ceiling and, contrary to what round 1
    asserts, name **no titles**. (Round 1 also attributes that claim to 01-30; 01-30 makes it nowhere.) Record the count MEASURED on this host at this point in
    the wave order:
    `node --test --test-reporter=tap test/*.test.cjs | grep -E "^# (tests|pass|fail|skipped|todo) "`.
    Reconcile it against 01-30's SUMMARY; **do not carry a number forward from any plan's prose.** If
    the measured figure and 01-30's disagree, the measurement wins and the discrepancy is reported.
    Enumerate every member by test title — the four listed in this plan's `<interfaces>` plus each new
    one — and state, in `01-VALIDATION.md` itself, that the ceiling moved **because non-runs were
    relabelled as skips, not because work was skipped**, and that the `≤` semantics is re-frozen at the
    new number. That paragraph exists specifically to forbid unannounced ceiling moves; moving it
    without saying why would be the thing it forbids.

    Note that `01-23-SUMMARY.md:181` keeps its `# skipped 4` list permanently — it is a historical
    record and is in no plan's `files_modified`.

    **Do NOT flip `nyquist_compliant` or `status: draft`.** Both are correctly false. Flipping them
    follows a re-run of `/gsd:verify-work`; 01-23 was right to refuse it.

    **Then BUILD the residual register — by SWEEPING, not by copying a list.** The section below is a
    SEED, not the set. Round 1 shipped a fixed eleven-item list and three known residuals fell outside
    it, which made this plan's own truth #5 false as planned. A register that is not closed over the set
    is the same defect as a pin whose corpus excludes its own highest-value site. So:

    1. **Read the seven SUMMARYs this plan `@`-loads** (01-24 through 01-30). For **every** item any of
       them names as a next step, a declined conversion, a deferred fix, a stated ceiling or a residual,
       add a register row. 01-30 in particular is instructed to write two of these under a heading this
       sweep reads (`test/proc-kill.test.cjs` and `test/engine-parity.test.cjs`); 01-25 files a third
       (`recordSession`). Do not stop at those three — the sweep is the instruction, they are only the
       ones already known.
    2. Give each row a **named owner**. "Operator" is legitimate. "A follow-up plan holding `<these
       files>`" is legitimate. An issue number is legitimate. *"An operator has to look at it"* is NOT
       an owner for anything measurable from source — and neither is **a plan that has already landed**.
       01-24 and 01-26 are wave 1 and this plan is wave 4, so nothing may be filed against them from
       here; if a SUMMARY files a residual at an already-landed plan, re-home it to a follow-up and say
       that you did.
    3. **Reconcile the seed against the sweep.** If a seeded row is not corroborated by any SUMMARY,
       re-verify it at source before restating it. If the sweep finds a residual the seed does not
       carry, add it — that is the sweep working, and it is a finding, not an inconvenience.
    4. **Paste the sweep** in the SUMMARY: for each of the seven SUMMARYs, the lines it contributed and
       the rows they became. A register a reader cannot check is closed is a register asserted to be
       closed.

    Copy the finished register into the SUMMARY's residual list too.
  </action>
  <verify>
    <automated>node --test test/repo-claims.test.cjs test/ci-config.test.cjs</automated>
  </verify>
  <done>
    Every restated row has its command AND output pasted; no row was ticked. Pre-edit measurements,
    RE-RUN and pasted, that were RED at HEAD a217018 and must be GREEN after:
    `grep -c "SECRET_SCAN_MAX_BYTES" .planning/REQUIREMENTS.md` → **0**;
    `grep -c "960" .planning/REQUIREMENTS.md` → **0**;
    `grep -c "SIDEBAR_COLLAPSE_WIDTH" .planning/REQUIREMENTS.md` → **0**;
    `grep -c "hook-auth-roundtrip" .planning/phases/01-finish-the-floor/01-VALIDATION.md` → **0**
    (the document names no skip by title today).
    `nyquist_compliant` and `status` are byte-identical to HEAD.
  </done>
</task>

</tasks>

## Residual register — everything this set does not close, with an owner

Replaces the round-1 heading "Out of scope — **operator-blocked**", which had no slot for a code
defect and so dropped two Criticals on the floor. **No plan in this set may report any of these as
done.** Owners are named; "operator" is a legitimate owner and "a plan holding these files" is a
legitimate owner, but *"an operator has to look at it"* is not an owner for something measurable from
source.

**This section is the SEED for task 3's sweep, not its output.** Task 3 derives the shipped register
by reading the seven SUMMARYs this plan loads; these rows are what was already known when the plan was
written, and every one must be re-verified against the landed tree before it is restated. Rows the
sweep adds belong here too.

### Code residuals — measurable now, not fixed by this set

1. **The Pixi bubbles render at 7px on screen.** Measured this session:
   `ThoughtBubble.ts:22 FONT_SIZE = 14`, `:23 RENDER_SCALE = 0.5`, `:76 this.inner.scale.set(RENDER_SCALE)`;
   `ToolBubble.ts:29 FONT_SIZE = 14`, `:71` the same scale. Against `DESIGN.md`'s 14px floor that is a
   **factor of two**, and it is arithmetic, not an observation. It is also not a two-line fix:
   `ThoughtBubble.ts:32 WRAP_WIDTH = MAX_WIDTH / RENDER_SCALE - PADDING_X * 2` shows the inner space is
   deliberately a 2× space, so `PADDING_X = 6` (`:15`), `PADDING_Y = 3` (`:16`) and `CORNER_RADIUS = 5`
   (`:17`) are all half-size on screen too. (Round 1 cited `WRAP_WIDTH` at `:31`; `:31` is a comment
   tail — re-read this session.) Raising `FONT_SIZE` alone changes the geometry, and no automated test
   can judge the result. `test/repo-claims.test.cjs:646-669` reads the LITERAL and passes; its failure
   message already states the caveat verbatim, so the ceiling is filed — the arithmetic is not fixed.
   **Owner: issue #26 (FLOOR-12), a follow-up plan holding `src/renderer/src/scene/office/*.ts`.
   NOT the operator.**
2. **The two source-verified layout clips** — the SkillsTab catalog row and SidebarTabs'
   TERMINAL/MESSAGES labels at the default 420 rail (`b/WR-06`). Same misfiling as (1) in round 1.
   **Owner: a follow-up plan holding those components.**
3. **FLOOR-10's ladder is advisory, not enforced.** `hardStop: false` is the default
   (`src/main/breaker.ts`), capping the ladder at `constrained`, whose entire action is a mail and a
   toast. 01-27 correctly declines to finish it into a kill — D-18 forbids that. **Owner: issue #4.**
4. **`.planning/codebase/CONCERNS.md:46`** still says README calls the store an "Enterprise Knowledge
   Graph"; README has said otherwise since 01-10 (0 hits, re-measured this session). It is outside the
   naming pin's corpus by construction. **Owner: the next codebase-map regeneration.**
5. **`test/net-binding.test.cjs`'s acceptance-case-3 comment cites `hive.ts:1366`**, which is
   `const dir = this.agentDir(agentId);`. That file is 01-24's and this plan's anchor sweep does not
   reach `test/`. **Owner: a follow-up plan holding `test/net-binding.test.cjs`.** Round 1 wrote
   *"01-24 if it is still in flight, otherwise a follow-up"* — 01-24 is wave 1 and this plan is wave 4,
   so that condition is never true and the owner silently resolved to the null branch. 01-24 has no
   such task (`grep -c 1366 01-24-PLAN.md` → 0). A conditional whose condition cannot hold is exactly
   the *"an operator has to look at it"* shape this register's own preamble forbids.
6. **`b/CR-03` is CLOSED by plan 01-28**, not deferred — recorded here so the closure is visible in the
   register that round 1 omitted it from. Its ceiling: a home whose `roster.json` was copied BEFORE
   01-28 landed still carries the frozen slice; those messages were already adopted at the source home,
   so `adoptRendererQueues`' `existsSync` guard is the protection. Verify the claim against 01-28's
   SUMMARY before restating it.
7. **`test/proc-kill.test.cjs` — FIVE cases invisible to the TAP counters on every platform.** A
   module-scope `if (process.platform === 'win32') { … process.exit(0); }` means they never run on
   win32; and because the file is a hand-rolled harness that defines its own `test()` and does not
   `require('node:test')`, they land in **neither `# pass` nor `# skipped` on ANY platform**. This is
   the same defect class as `c/CR-01` — the class the *"the published `531 pass` figure is an honest
   floor"* gap exists to close — and it is five more silent non-runs than that gap enumerated. 01-30
   declines the conversion for a real reason (rewriting onto `node:test` also removes the file from
   `test/repo-claims.test.cjs`'s poisoned-assert loop, a different guarantee, in a file no plan in this
   set owns) and hands it here BY NAME. Verify the arithmetic against 01-30's SUMMARY rather than
   restating this paragraph. **Owner: a follow-up plan holding `test/proc-kill.test.cjs` AND
   `test/repo-claims.test.cjs` — the two halves cannot be done separately. NOT the operator.**
8. **Byte-level drive for the five shims that have only a source-text pin.**
   `test/engine-parity.test.cjs` drives `PROXY_BRIDGE_SHIM` end to end — writes the generated shim,
   runs it, asserts the bytes on the wire — and that is the only assertion shape source text cannot
   satisfy. The other five (`HOOK_SHIM`, `AGY_HOOK_SHIM`, `PI_EXTENSION`, `OPENCODE_PLUGIN`,
   `GROK_HOOK_SHIM`) rest entirely on 01-30's comment-stripped, assignment-shaped pin — a large
   improvement on the bare grep it replaces, and still source text. Round 1's review asked for an owner
   and got a SUMMARY line; 01-30 now hands it here by name. **Owner: a follow-up plan holding
   `test/engine-parity.test.cjs`; it needs five engine harnesses and no plan in this set owns that
   file. NOT the operator.**
9. **`hooks.ts`'s `recordSession` call — RESOLVED IN-SET, verify then delete this row.** This row was
   written when 01-25 filed the stored-state half against *"files owned by 01-24 and 01-26"* — both
   wave 1, both already landed by the time 01-25 runs, so that filing resolved to nobody. **01-25 was
   then revised (commit `bd74777`) to TAKE it**: `src/main/hooks.ts` is now in 01-25's `files_modified`
   with a `shares_files_with` split declared against 01-24, and task 3 gained a fourth sink with its own
   gate (`hooks.ts` contains `SPAWN_SAFE_SESSION_ID`, RED at HEAD).
   **Action for the sweep:** read 01-25's landed SUMMARY. If the fourth sink shipped, DELETE this row and
   paste the command that proved it. If it did not, keep the row with **Owner: a follow-up plan holding
   `src/main/hooks.ts` and `src/main/hive.ts`. NOT the operator.**

10. **Two `quiesce` residuals, ACCEPTED by the orchestrator — record, do not re-open.** Both were
   escalated by the round-3 reviser because fixing either reverses a negative control 01-28 explicitly
   states (*"the once-per-spell arming untouched"*). Accepting them was a scope decision, and an
   accepted residual still needs a named owner. 01-28 is instructed to record both in its SUMMARY under
   `## Quiesce residuals — ACCEPTED, not fixed`, which is how this sweep reaches them.
   **(a)** A false `blocked` landing AFTER the last real Stop is unrecoverable — `delivery.ts:668`
   suppresses the repeat via `this.quiesced.has()`, `:667` resets only on new output, and
   `useHive.ts:533` is the sole clearer of `breakerLevel`.
   **(b)** `delivery.ts:670`'s `setStatus?.(id,'idle')` writes `idle` unconditionally for an agent the
   floor paints `blocked`; the discriminator reaches only the renderer arm.
   **Owner: a follow-up plan holding `src/main/delivery.ts` and `src/renderer/src/hooks/useHive.ts`.
   NOT the operator.** Verify 01-28's SUMMARY carries the heading; if it does not, that is a finding —
   report it rather than inventing the content.

### Operator-blocked

10. **Merging PR #77 and closing the 20 non-epic `floor-inspection` issues.** The phase's headline
   deliverable, 0/20. `origin/main` still pins `"electron": "^32.2.0"` and 158+ phase commits sit behind
   draft PR #77, so closing an issue today records "fixed" against a shipped product that still carries
   the defect. Merge first, then close each against merged source. **Owner: operator.**
11. **D-09.** Launch `dist\win-unpacked\Hello MarkX.exe` on this Windows host; confirm a real PTY echo, a
   persisted setting surviving a relaunch, a clean visual pass; write `01-01-SUMMARY.md` — the only plan
   of 23 with no SUMMARY. `01-VALIDATION.md`'s *"The Nyquist Problem — where sampling is structurally
   insufficient"* section states outright that no number of unit runs can reconstruct an
   Electron-version regression, because `test/load-ts.cjs` stubs `electron` for the whole
   suite. **Owner: operator.**
12. **Cutting a `v*` tag and running `gh attestation verify <artifact> --repo MARKXAILABS/hello-markx`.**
   01-30 makes the attested SUBJECT correct; only a published artifact makes the sample run.
   **Owner: whoever cuts the next tag.**
13. **The remaining `human_verification` items in `01-VERIFICATION.md`** — the log-folder click, the
    Windows blocked-agent toast, the Tasks-board visual after the poller migration, the auto-mode chip
    checkpoint, the ~600 swept FLOOR-12 surfaces, and FLOOR-04's live fake-key drop. The Pixi label
    ARITHMETIC is item (1) above and is NOT part of this bundle. **Owner: operator.**
14. **SC-1's restatement** — "autonomy survives the window" is not observable on win32 while
    `src/main/index.ts:5792` kills every PTY and quits on `window-all-closed`. Running with no window is
    **Phase 2's DAEMON-01**, not FLOOR-02, and it is not restated here. ROADMAP criterion 1's first
    sentence therefore stays untrue on the operator's own platform after this set lands; a one-line
    pointer to Phase 2 belongs in the criterion, and that is the ROADMAP's owner, not this plan's.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs and comments → the next maintainer | An anchor pointing at the wrong function routes the next change into the wrong file. Six of these were reported corrected and were not. |
| shipped skill files → the agents themselves | `resources/skills/capabilities/SKILL.md` is installed into every agent's skills directory; a retired capability named there is a false capability claim consumed by the models. |
| `REQUIREMENTS.md` → every later phase | Phases 2-6 plan against these rows. A row that overstates what landed is a dependency built on a claim. |
| a green pin → everyone downstream of it | A pin whose corpus excludes its own highest-value site reports a guarantee it does not hold. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P31-01 | Repudiation | anchors reported as corrected that were not | mitigate | All thirteen checked against `sed -n '<n>p'` with output pasted; symbols written in a grep-checkable form; `STALE_ANCHORS` denies the six historical strings. |
| T-P31-02 | Repudiation | an agent-facing false capability claim | mitigate | `SKILL.md:96` renamed to `kg-core.cjs:3-8`'s own description, and the pin's corpus is PROVED to contain it (312 files, `SKILL.md in corpus: true`). |
| T-P31-03 | Tampering | the audit record edited to erase its own finding | mitigate | `docs/floor-inspection.html` excluded by explicit path with the reason in the exclusion; `.planning/` is outside the root list by construction. |
| T-P31-04 | Repudiation | a pin that has never been seen failing | mitigate | The widened pin is demonstrated RED **against `SKILL.md` specifically** — the one file whose coverage was in doubt — before the last rename. |
| T-P31-05 | Repudiation | a pin that cannot pass, so it gets weakened until it does | mitigate | `test/repo-claims.test.cjs` excluded from its own scan by explicit path; `dist/` and `out/` outside by construction. |
| T-P31-06 | Repudiation | requirement rows restated from SUMMARY claims | mitigate | Every row verified at source with **command AND output** pasted; three rows carry gate strings that are 0 at HEAD. |
| T-P31-07 | Repudiation | a frozen skip ceiling moved without saying why | mitigate | The count is MEASURED, every member is named by title, and the move is justified in `01-VALIDATION.md` as relabelled non-runs with `≤` re-frozen. |
| T-P31-08 | Repudiation | a code defect filed as an operator observation | mitigate | The residual register separates code residuals from operator-blocked items and gives each a named owner. |
| T-P31-09 | Repudiation | `DESIGN.md`'s 1280 claim falling between two plans | mitigate | 01-25 owns the edit; this plan owns a pin that is RED until it lands. |
| T-P31-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs. `package.json` untouched; edits are docs, comments, one test and two planning documents. |
</threat_model>

<verification>
- `npm test` green; paste the TAP counters. **This plan owns the authoritative whole-suite figure** —
  it runs last and is the only plan that can see every addition. State the arithmetic: the
  `01-VERIFICATION.md` baseline (535 / 531 / 0 / 4) plus the tests added by 01-24 through 01-30 — **and MINUS 2 on `# pass`**, because 01-24 (`net-binding.test.cjs`) and 01-30 (`win-cmd-shim.test.cjs`) each convert a bare `return` — which `node:test` counts as a PASS — into a real `t.skip()`. Both currently-passing cases become skips, so the identity is `pass = 531 - 2 + N_run`, not `531 + N`. A drop of exactly 2 is the change WORKING: this phase's headline gap is that the published figure counted Windows non-runs as passes. Report `# skipped` 4 -> 6 alongside it, with
  the skipped counter up by the measured amount on win32. 01-30's wave-2 figure is a partial by
  construction (01-29 and this plan both add tests after it), so a discrepancy is EXPECTED and must be
  reconciled arithmetically, not narrated away.
- `npx eslint . --max-warnings 0` exit 0; `npm run typecheck` clean (two `config.ts` files are edited,
  even if only in comments).
- The full thirteen-anchor check list, pasted, including the anchors that were already correct.
- The widened pin's RED output naming `SKILL.md`, and its GREEN.
- The command AND output for every restated requirement row.
- The measured TAP skip figure, its per-title enumeration, and the reconciliation against 01-30's.
- The residual-register SWEEP: for each of the seven loaded SUMMARYs, the lines it contributed and the
  rows they became, plus any seeded row the sweep could not corroborate.

**This plan does not close the phase.** Fourteen residuals are seeded above and the sweep may add
more — the count is an output of task 3, not an input to it — and
`01-VALIDATION.md` still carries `nyquist_compliant: false` / `status: draft` — correctly. Re-run
`/gsd:verify-work 1` after this plan lands; the verdict is the verifier's, not this plan's.
</verification>

<success_criteria>
- No anchor in `HIVE.md` or `docs/adr/` names a function it does not point at, and the six historical
  wrong strings can never come back unnoticed.
- The retired name survives only in the audit record and in the pin that searches for it, and the pin's
  corpus provably contains `SKILL.md`, `README.md` and `DESIGN.md`.
- No shipped doc states a 1280 window minimum — enforced from here, edited by 01-25.
- Every restated requirement row was checked at source with its output pasted, and none was ticked.
- The win32 skip ceiling matches a measurement, names its members, and explains its own move.
- Every unfixed residual — code and operator alike — has a named owner in one register, and that
  register was DERIVED by sweeping the seven loaded SUMMARYs rather than copied from a fixed list. No
  row is filed against a plan that has already landed.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-31-SUMMARY.md` when done.
</output>
