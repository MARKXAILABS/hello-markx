---
phase: 01-finish-the-floor
plan: 14
type: execute
wave: 6
depends_on: ["01-12"]
files_modified:
  - src/renderer/src/design/tokens.css
  - DESIGN.md
  - src/renderer/index.html                               # task 2 only - the boot-splash title, 13px Press Start 2P. NO plan declared this file before red-team round 3, and none of M1/M1d/M1x can reach it: M1 scans src/renderer/src with --include=*.tsx --include=*.ts, and this is .html one directory up. Touch ONLY the one font-size.
  - src/renderer/src/components/AgentCard.tsx
  - src/renderer/src/components/AgentStrip.tsx
  - src/renderer/src/components/ThreadsPanel.tsx
  - src/renderer/src/components/FullscreenTerminal.tsx
  - src/renderer/src/scene/office/ThoughtBubble.ts
  - src/renderer/src/scene/office/ToolBubble.ts
autonomous: true
requirements: [FLOOR-12]

must_haves:
  truths:
    - "No --cth-text-* token declares a value below 14px, and --cth-text-display-sm / --cth-lh-display-sm are gone"
    - "The two display-sm consumers convert family and size in the SAME commit as the token deletion"
    - "Every non-literal fontSize in this group evaluates to 14 or above at every input - the three Rule 3 sites, the three rosterScale floors, both note-popover sizes, and AgentCard's decimal 10.5"
    - "DESIGN.md §4.1 no longer contradicts DESIGN.md:706"
    - "The boot-splash title - the first text any user sees, and the one sub-14px site no scan in this phase can reach - is at or above 14px"
    - "No fixed-height container silently clips text after the bump"
  artifacts:
    - path: "src/renderer/src/design/tokens.css"
      provides: "the corrected type scale"
      contains: "--cth-text-display-md"
    - path: "DESIGN.md"
      provides: "§4.1 rows :119, :128 corrected and :129 deleted"
    - path: "src/renderer/index.html"
      provides: "the boot-splash title at or above the floor, in literal px because the splash paints before the token sheet exists"
      contains: "font-size: 14px"
  key_links:
    - from: "src/renderer/src/design/tokens.css"
      to: "AgentCard.tsx:220 and ThreadsPanel.tsx:101"
      via: "Rule 1b — the only two display-sm consumers, converted in the same commit as the token deletion"
      pattern: "cth-text-body-md"
---

<objective>
Land the type-scale correction and sweep the four files that are coupled to it — then hand four
disjoint file groups to plans 15-20.

Purpose: FLOOR-12's size half. `DESIGN.md:706` states *"never go below 14 px for any user-facing
text"* (verified verbatim) while `tokens.css:61-68` declares four values below it and 604 hardcoded
sub-14px `fontSize` **occurrences** across 61 files bypass the token layer entirely. **Occurrences, not
lines, everywhere in this plan and in plans 15-20** — measured today, `grep -o … | wc -l` returns `604`
while the same regex under `grep -c` sums to `603`, because `IntegrationsRegistry.tsx:388` carries two
matches on one physical line. Wave 9 (plan 23) counts occurrences, so every pinned number here is an
occurrence count and every per-file check uses `grep -hoE … | wc -l`, never `grep -cE`. The four-token migration alone
leaves criterion 4 false — that is measured, not estimated.

`01-UI-SPEC.md` § FLOOR-12 is the **binding** contract: it gives mechanically executable sweep rules,
a decidable glyph-exemption predicate, a three-step containment rule and a completeness bar. **Follow
them exactly. Do not re-derive them.**

Output: the token migration, `DESIGN.md` §4.1 made self-consistent, Rule 1b's two sites converted in
the same commit as the token deletion, Rule 3's three non-literal sites, the boot-splash title (Rule
1c), and this plan's four files fully swept.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-finish-the-floor/01-UI-SPEC.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@DESIGN.md

<interfaces>
**Token migration table (UI-SPEC, binding).** `tokens.css:61-68` sizes, `:70-77` line heights.

| Token | Current | New |
|---|---|---|
| `--cth-text-display-sm` | 8px | **DELETE the token** (and `--cth-lh-display-sm`, 12px) |
| `--cth-text-display-md` | 12px | **14px** (`--cth-lh-display-md: 20px` stays — 14/20 is integer px) |
| `--cth-text-body-sm` | 13px | **14px** (`--cth-lh-body-sm: 18px` stays) |
| `--cth-text-mono-sm` | 13px | **14px** — verified **zero** consumers today |
| `--cth-text-display-lg` | 16px | unchanged — zero consumers, already above the floor |

Note the line-height token for mono is `--cth-lh-mono` (single token, `:76`), not `-md`/`-sm` — check
`tokens.css` before pairing.

**`--cth-text-display-sm` has exactly two consumers, and they are this plan's:**
`AgentCard.tsx:220` (the agent NAME on the card) and `ThreadsPanel.tsx:101` (thread title button).
They take **Rule 1b**, not Rule 1: `fontFamily` → `var(--cth-font-ui)`, `fontSize` →
`var(--cth-text-body-md)`, `lineHeight` → `var(--cth-lh-body-md)`. Reason, verified per site: both are
at 8px, the largest jump in the sweep, and both are user-facing *reading* text. Press Start 2P is
monospaced at ~1em/char — 8px→14px is a **75% width increase** inside the 112px agent strip. Inter at
14px averages ~8.4px/char uppercase, roughly flat. **No third site qualifies.**
They must land in the **same commit** as the token deletion, or the renderer resolves `var()` to nothing.

**Sweep rules (UI-SPEC, checked in order, no judgement at the site):**
- **Rule 0** — on the frozen glyph allowlist: size unchanged, element gains `aria-hidden="true"`. Stop.
- **Rule 1** — style object sets `fontFamily: 'var(--cth-font-display)'` AND `fontSize` 7-13:
  `fontSize` → `var(--cth-text-display-md)`, `lineHeight` → `var(--cth-lh-display-md)`.
  **`fontFamily` is UNCHANGED. Press Start 2P stays.** `DESIGN.md:706` is a size rule; converting the
  display face would retire a typeface in a phase whose contract says "no new visual language".
- **Rule 1b** — the two sites named above only.
- **Rule 1c — the boot splash, and it is literal px on purpose.** `src/renderer/index.html:27` declares
  `#cth-splash .title { font-family: "Press Start 2P", monospace; font-size: 13px; }`, rendered at `:37`
  as `<div class="title">HELLO MARKX</div>` — the pixel display face at **13px**, and the first text any
  user of this app ever sees. It is a Rule 1 site by shape (display face, sub-14 size, raise the size and
  keep the family) but it cannot take a token: the splash is inline `<style>` inside `index.html` and
  paints before `main.tsx` mounts, so `tokens.css` does not exist yet and any `var(--cth-text-*)` would
  resolve to nothing on the one screen that has no fallback. Action: `font-size: 13px` → `font-size: 14px`,
  family unchanged. **Verified 2026-08-21, this is the only sub-14px declaration in the file** —
  `grep -n "font-size" src/renderer/index.html` returns three lines and the siblings are already clear:
  `:24` `.mk` at **16px** (also Press Start 2P) and `:28` `.hint` at **15px**. Both are recorded here so
  "the rest of the file was checked" is a stated fact rather than an omission; neither is edited.
  **Why it is here at all:** no plan in this phase declared `src/renderer/index.html`, and no scan the
  phase runs can reach it. M1, M1d and M1x all scan `src/renderer/src` with
  `--include=*.tsx --include=*.ts`; this file is `.html` and one directory above that root, so plan 23's
  wave-9 completeness bar would have signed FLOOR-12 off with the app's first screen at 13px.
- **Rule 2** — `fontSize` 7-13 with no `--cth-font-display` in the same object: → `var(--cth-text-body-md)`
  (or `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`), with the matching `--cth-lh-*`.
- **Rule 3** — the three non-literal sites UI-SPEC names: `ThoughtBubble.ts:22` and `ToolBubble.ts:29`
  → `FONT_SIZE = 14`; `FullscreenTerminal.tsx:694` → `Math.max(14, scale.name - 3)`. **The file has
  drifted — that expression is at `:695` today.** Match on the text, not the number.
- **Rule 3b — the sites M1 structurally cannot see, all of them in this plan's files.** M1's regex
  requires a digit immediately after `fontSize:` and its trailing `[^0-9.]` guard drops decimals, so a
  quoted, decimal, ternary or computed size is invisible to it — and therefore to plan 23's repo-wide
  bar. Verified present today, every one of them user-facing text:

  | Site | Today | Required |
  |---|---|---|
  | `FullscreenTerminal.tsx:49` `name: clamp(zoom * 0.48, 7, 14)` | floors at **7** — consumed at `:664` as the agent NAME in the fullscreen roster; at the default zoom of 12 it renders at **7px** | floor `14`, cap raised so the clamp stays a real range |
  | `FullscreenTerminal.tsx:50` `group: clamp(zoom * 0.45, 7, 13)` | floors at **7** and is hard-capped at **13** — consumed at `:353`, so it can *never* reach 14 | floor `14`, cap `>= 14` |
  | `FullscreenTerminal.tsx:51` `note: clamp(zoom * 0.68, 10, 20)` | floors at **10** — consumed at `:731` and `:746` | floor `14` |
  | `FullscreenTerminal.tsx:566` `noteFontSize = Math.min(useTerminalFontSize(), 14)` | no floor; the shared zoom's own minimum is 8 — consumed at `:809` | floored at `14` |
  | `FullscreenTerminal.tsx:567` `noteLabelSize = Math.max(8, …)` | floors at **8** and, because `noteFontSize <= 14`, evaluates to **8** at every zoom — consumed at `:778` | floor `14` |
  | `AgentCard.tsx:308` `fontSize: 10.5` | a **decimal**, dropped by M1's `[^0-9.]` guard | Rule 2 token |

  Raising only `:695` while leaving `:49`-`:51` alone would also make the subordinate bullet line
  *larger than the agent name it is subordinate to*, which is why this is one edit, not two.
- **The terminal carve-out, stated once and applied consistently.** UI-SPEC's *"terminal sizing is
  user-controlled and is not governed by `DESIGN.md:706`"* covers **xterm's own font and nothing else**:
  `components/terminalFontSize.ts`, `components/terminalPool.ts`, and the `term.options.fontSize`
  assignments in `PtyTerminalView.tsx`. It does **not** cover app chrome that merely *reads* the shared
  zoom. The roster labels and the note popover are ordinary UI that happens to scale with Cmd +/-; an
  operator zooming a terminal has not asked for an agent's name to render at 7px. Applying the carve-out
  to `terminalFontSize.ts` and silently also to its readers is exactly how six sub-14px sites survived
  the bar in this one file.
- **Out of the sweep entirely:** `components/terminalFontSize.ts`, `components/terminalPool.ts` and
  anything xterm — per the carve-out above, and nothing wider.
- **`PixelButton.tsx:102` is an M1x hit that this task fixes without touching the file.** It reads
  `fontSize: size === 'lg' ? 'var(--cth-text-body-md)' : 'var(--cth-text-body-sm)'` — both branches are
  already tokens, so its evaluated minimum is whatever `--cth-text-body-sm` says. Today that is **13px**,
  which is why it is a real sub-14px site; after this task raises the token to 14px it is **14**, with no
  edit to `PixelButton.tsx`. **Record it in the SUMMARY with that evaluated minimum**, because plan 23
  needs the reason on the page: plan 23 requires `PixelButton.tsx` byte-identical to
  `bd286ebf5654a2647c93546dc135f608aeb5d0f0` *and* its M1x criterion admits only the terminal carve-out
  hits, so this hit is the one M1x line that is neither carve-out nor defect. It is this plan's token
  migration that makes it compliant, and this plan is where the evidence for it belongs. Do **not** edit
  `PixelButton.tsx` to silence the scan — that would break plan 23's hash criterion.

**This plan's Rule 0 glyphs:** `AgentCard.tsx` `✎`; `AgentStrip.tsx` `✕`×2; `FullscreenTerminal.tsx`
`✕`, `✎`, `·`×2, `•`; `ThreadsPanel.tsx` `{open ? '▾' : '▸'}`. All 8 are exempt (none is one of the
four non-exempt cases, which all live in plan 17's group).

**Hoisted shared style objects are NOT exempt.** Where a hoisted object serves both a glyph-only
element and any text element, sweep the shared declaration normally and give the glyph element a
**local `fontSize` override**; that override's line is what enters the allowlist.
**The allowlist is in `fontSize`-declaration-line space, not element-line space** — at
`AgentStrip.tsx` the `✕` button's `fontSize: 11` is on line 209 while `>✕</button>` is on line 213.

**Containment (UI-SPEC's three-step rule, in order).** Verified fixed chrome in this group:
`AgentStrip.tsx:257-258` `height: 112, minHeight: 112`; `AgentCard.tsx:111` `const height = 78`;
card chip leading at `AgentCard.tsx:228`, `:262`, `:270`; strip note clamp at `AgentStrip.tsx:303`
`maxHeight: 84`.
1. **Nothing** — the name, info line, cost span and account chip already carry `whiteSpace:'nowrap'` +
   `overflow:'hidden'` + `textOverflow:'ellipsis'`. Horizontal growth truncates by design; this covers
   the majority.
2. **Raise the container's integer by the measured delta and change nothing else.**
3. **Stop and report** with the file:line and the measured overflow.
**Explicitly forbidden:** reflowing a card, moving a field, dropping a field, collapsing a row, or
holding any site below 14px to make room. If the answer looks like "redesign it", it is option 3.

**Accessible names — the rule, not the ratio.** Every `<button>` must have an accessible name.
Visible text content → **already named, do NOT add `aria-label`** (it would override the visible label
and make voice control worse). Icon/glyph/symbol only → `aria-label` required, phrased verb + object,
naming the agent where one is in scope (`FullscreenTerminal.tsx:665` is the house pattern). Decorative
non-control glyphs → `aria-hidden="true"` (Rule 0).
The two `<div role="button">` at `AgentCard.tsx:145` and `FullscreenTerminal.tsx:604` **stay exactly as
they are** — deliberate, documented, recorded as resolved history in `.planning/codebase/CONCERNS.md`.
Converting them regresses a decision. What they owe FLOOR-12 is an accessible name, which they already
have at `:155` and `:622` (and which plan 12 extended with the auto-mode state).

**`DESIGN.md` §4.1 edits, required not optional:** set `:128` `display-md` to 14/20; **delete** the
`:129` `display-sm` row; **amend `:119`** — it reads *"NES-iconic, headings only, 8/12/16 px"*, so
editing `:128`/`:129` without it leaves the doc naming two sizes that no longer exist. Set it to
`14/16 px`. `:132` and `:134` already read 14/18 — `tokens.css` had drifted below the doc. No other
§4.1 row changes. Criterion 1 bans a doc promising what the code does not do, and this phase would
otherwise *create* such a contradiction while closing another.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the pinned measurement commands, paste the numbers, and freeze this group's candidate list</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `01-UI-SPEC.md` § FLOOR-12 in full — measurement commands M1/M2/M3, the sweep rules, the 30-candidate table with its 4 exclusions, the containment rule, and the completeness bar.
    - `src/renderer/src/design/tokens.css:40-80` — the spacing scale, the eight size tokens and the line-height tokens as they stand.
    - `DESIGN.md:115-145` and `:700-712` — §4.1's rows and the 14px floor statement.
    - `src/renderer/src/components/AgentCard.tsx`, `AgentStrip.tsx`, `ThreadsPanel.tsx`, `FullscreenTerminal.tsx` — the four files this plan sweeps, read whole enough to see every hoisted style object.
  </read_first>
  <action>
    Run **M1 exactly as pinned in UI-SPEC** — do not re-derive it with a different regex; the count
    moves ±6 with regex shape and M1 is authoritative:
    `grep -rhoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" src/renderer/src --include=*.tsx --include=*.ts | wc -l`
    and the `-rlE ... | wc -l` file-count variant. Paste both numbers. UI-SPEC recorded 604 sites
    across 61 files; if current source differs (plans 10, 12 and 13 have since edited renderer files),
    record the delta and use the **current** numbers as this phase's baseline.
    **M1's unit is occurrences (`-o`), not lines.** Also paste the `grep -c` sum for the same regex and
    note the difference (measured today: 604 occurrences vs 603 lines). Every per-file number this plan
    and plans 15-20 record is an occurrence count, because that is what plan 23 asserts in wave 9.

    Then run the two **blind-spot scans**, which exist because M1 cannot see a quoted, decimal, ternary
    or computed size and therefore neither can plan 23's bar:
    - **M1d — decimal and quoted sub-14 literals:**
      `grep -rnoE "fontSize *[:=] *\{?['\"]?(1[0-3]|[1-9])\.[0-9]+|fontSize *[:=] *\{?['\"](1[0-3]|[1-9])(\.[0-9]+)?px['\"]" src/renderer/src --include=*.tsx --include=*.ts`
      Repo-wide today: **6** hits. Exactly one is this plan's — `AgentCard.tsx:308` `fontSize: 10.5`.
    - **M1x — expression-valued sizes:**
      `grep -rnE "fontSize *[:=] *\{?[A-Za-z_\$]" src/renderer/src --include=*.tsx --include=*.ts`
      Repo-wide today: **18** hits. Nine are this plan's — `FullscreenTerminal.tsx` `:353`, `:664`,
      `:695`, `:731`, `:746`, `:778`, `:809` plus `ThoughtBubble.ts:84` and `ToolBubble.ts:78`.
    For every M1x hit in this plan's files, record its **evaluated minimum** — the smallest value the
    expression can return over its inputs — not just the expression text. A minimum below 14 is a site
    Rule 3 or Rule 3b must fix.

    Record `git rev-parse HEAD` as this plan's **base sha**. Every "before this task" baseline below is
    read against it, and plan 13 is editing the same working tree in this wave.

    Run M2 (the display-face subset) and M3 (the glyph candidate predicate, with `LC_ALL=C.UTF-8`) and
    paste those too.

    Then produce, for this plan's four files plus the two Pixi files, a per-file table: every `fontSize`
    declaration line, its current value, whether the enclosing style object sets `--cth-font-display` or
    `--cth-font-mono`, and which rule applies (0 / 1 / 1b / 2 / 3). Note per line whether the object is
    inline or hoisted, because a hoisted object serving both a glyph and text needs the local-override
    treatment.

    List this plan's Rule 0 candidates with their `fontSize`-declaration line numbers (not element line
    numbers). That list is what enters the frozen allowlist.

    Finally, record the four containment anchors' current integer values so the delta in task 3 is
    measured rather than guessed.
  </action>
  <acceptance_criteria>
    - Task notes contain the pasted output of M1 (both variants), M2 (all three variants) and M3, with the exact commands shown.
    - Task notes contain the per-file rule table for all six files, with every `fontSize` line classified.
    - Task notes contain this plan's Rule 0 candidate list in `fontSize`-declaration-line space.
    - Task notes record the four containment anchors' current values.
    - **Recorded count baselines** (this plan's later criteria are strict deltas against these, because each token already occurs in its file for unrelated reasons — a bare `at least N` would be satisfied by doing nothing). Run and paste each, verbatim:
      `grep -c "cth-font-ui" src/renderer/src/components/ThreadsPanel.tsx` (expect `5`),
      `grep -c "cth-font-ui" src/renderer/src/components/AgentCard.tsx` (expect `0`) and
      `grep -c "cth-text-display-sm" src/renderer/src/components/ThreadsPanel.tsx` (expect `1`, at `:101`).
    - Task notes contain the pasted M1d and M1x output scoped to this plan's six source files, with the
      **evaluated minimum** written beside every M1x hit. Expected today: one M1d hit (`AgentCard.tsx:308`)
      and nine M1x hits whose evaluated minima are `7` (`:353` `scale.group`), `7` (`:664` `scale.name`),
      `9` (`:695`), `10` and `10` (`:731`, `:746` `scale.note`), `8` and `8` (`:778` `noteLabelSize`,
      `:809` `noteFontSize`), and `12` and `12` (the two Pixi `FONT_SIZE` constants).
    - Task notes record this plan's **base sha** (`git rev-parse HEAD`).
    - `npm test` exit code is `0`, **and the suite's TAP counters are recorded as this plan's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Measured today: `EXIT=0`, `# tests 426`, `# pass 422`, `# fail 0`, `# skipped 4`, `# todo 0`. Exit
      code alone is not a gate — `node --test` counts skipped tests in its total and exits `0` on an
      all-skipped file (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from
      a green one by exit code. Name the TAP reporter explicitly: the default (spec) reporter prints
      `ℹ skipped 4`, so a criterion grepping `^# skipped` against it never matches and silently passes.
      **Write the TAP through `mktemp`, never to a fixed repo-root name.** Plan 13 edits this same
      working tree in wave 6 and the six wave-7 sweeps follow into it, so a shared `sweep.tap` is
      truncated by each concurrent writer while the others are still grepping it — this plan could grade
      a wave-mate's half-written counter block against its own `# pass` / `# skipped` gate. `.gitignore`
      has no `*.tap` entry either, so a stray file would also show up in this plan's own containment
      check and in plan 23's wave-9 sweep.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>The sweep surface is measured with the pinned commands, every line in this group is pre-classified by rule, and the allowlist candidates are frozen before any edit.</done>
</task>

<task type="auto">
  <name>Task 2: The token migration, DESIGN.md §4.1, Rule 1b's two sites, Rule 3's three sites and the boot splash — token deletion and its consumers in one commit</name>
  <files>src/renderer/src/design/tokens.css, DESIGN.md, src/renderer/index.html, src/renderer/src/components/AgentCard.tsx, src/renderer/src/components/ThreadsPanel.tsx, src/renderer/src/scene/office/ThoughtBubble.ts, src/renderer/src/scene/office/ToolBubble.ts, src/renderer/src/components/FullscreenTerminal.tsx</files>
  <read_first>
    - `src/renderer/src/design/tokens.css:55-80` — the exact current block, including the `v0.3.4` comment above the scale.
    - `src/renderer/src/components/AgentCard.tsx:215-240` — the name at `:219-221` and its `nowrap`/`ellipsis` neighbours at `:223-224`.
    - `src/renderer/src/components/ThreadsPanel.tsx:95-110` — the thread title button.
    - `src/renderer/src/scene/office/ThoughtBubble.ts:15-30` and `ToolBubble.ts:22-35` — the `FONT_SIZE` constants and every consumer of them.
    - `src/renderer/src/components/FullscreenTerminal.tsx:685-700` — the computed floor at `:694`.
    - `DESIGN.md:115-145` — §4.1's rows.
    - `src/renderer/index.html:13-31` — the whole inline `<style>` block, all three `font-size` declarations, and `:35-39` where the three elements are rendered. Confirm for yourself that the file imports no stylesheet other than the Google Fonts link before deciding what `.title` may reference.
    - `01-PATTERNS.md` § "FLOOR-01 / FLOOR-12 / FLOOR-13 — the renderer" `:1092-1114` — the exact token block and the warning that the two Rule-1b sites must land with the deletion.
  </read_first>
  <action>
    Apply the token migration table in `<interfaces>` to `tokens.css`: raise `--cth-text-display-md` to
    14px, `--cth-text-body-sm` to 14px, `--cth-text-mono-sm` to 14px; **delete**
    `--cth-text-display-sm` and `--cth-lh-display-sm`. Leave `--cth-text-display-lg` alone. Leave every
    line-height token that pairs with a raised size alone (14/20 and 14/18 are already integer px per
    `DESIGN.md:144-150`). Update the `v0.3.4` comment above the scale so it no longer describes a scale
    that no longer exists. **Do not touch the spacing scale, the colour tokens or the dark-theme mirror.**

    In the **same commit**, convert the two Rule 1b sites — `AgentCard.tsx:220` and
    `ThreadsPanel.tsx:101` — to `var(--cth-font-ui)` / `var(--cth-text-body-md)` /
    `var(--cth-lh-body-md)`. Committing the deletion without them leaves the renderer resolving `var()`
    to nothing. Keep `.toUpperCase()` on the agent name exactly as it is: casing is not one of the six
    requirements and changing it is a visual change nothing asks for.

    Apply Rule 3: `ThoughtBubble.ts:22` and `ToolBubble.ts:29` → `FONT_SIZE = 14`; the
    `Math.max(9, scale.name - 3)` in `FullscreenTerminal.tsx` (UI-SPEC says `:694`; it is at `:695`
    today) → `Math.max(14, scale.name - 3)`. For the two Pixi labels, check whether any layout constant
    is derived from `FONT_SIZE` before changing it, and adjust the derived constant if the bubble would
    clip.

    Apply **Rule 3b** — the six sites M1 cannot see, per the table in `<interfaces>`. Raise the three
    `rosterScale` floors (`:49` `name`, `:50` `group`, `:51` `note`) to 14, raising each clamp's cap
    where the cap would otherwise sit at or below the new floor, so Cmd +/- still moves the roster.
    Floor `noteFontSize` (`:566`) and `noteLabelSize` (`:567`) at 14. Sweep `AgentCard.tsx:308`'s
    decimal `fontSize: 10.5` under Rule 2 like any other literal. Do **not** apply the terminal
    carve-out to any of these: it covers xterm's own font, not chrome that reads the zoom, and
    `<interfaces>` states that once for all siblings.

    Containment applies here too: `:664` sits inside the fullscreen roster row and `:778`/`:809` inside
    the note popover, whose height is computed at `:571` from `noteHeight + noteLabelSize * 2 + 40`.
    Re-check that arithmetic after the bump and apply UI-SPEC's three steps in order.

    Apply **Rule 1c** — `src/renderer/index.html:27`, the boot-splash title: `font-size: 13px` →
    `font-size: 14px`, `font-family` unchanged. Match on the text (`#cth-splash .title`), not the line
    number. **Change nothing else in this file** — not the CSP `<meta>`, not the font `<link>`, not the
    `.mk` badge at `:24` (16px) or the `.hint` at `:28` (15px), both of which already clear the floor and
    are recorded in `<interfaces>` as checked. Do **not** reach for a `var(--cth-text-*)` here: the splash
    paints before `main.tsx` mounts and `tokens.css` is not loaded yet, so the token would resolve to
    nothing and the app's first screen would render at the browser default. A literal `14px` is the
    correct answer, and the reason belongs in the SUMMARY so wave 9 does not read it as a missed sweep.

    Edit `DESIGN.md` §4.1 exactly as specified: `:128` `display-md` → 14/20; delete the `:129`
    `display-sm` row; amend `:119` to `14/16 px`. No other §4.1 row changes. Do not touch the
    known-drift rows listed in UI-SPEC (`:131`, `:137`, `:140-141`, `:130`, `:133`, `:674`) — they are
    real contradictions but they are explicitly scoped out, and chasing them is how this plan grows.

    After this commit, run the app or at minimum `npm run typecheck` and record whether any `var()`
    resolves to nothing.
  </action>
  <acceptance_criteria>
    - `grep -c "cth-text-display-sm\|cth-lh-display-sm" src/renderer/src/design/tokens.css` returns `0`.
    - `grep -rc "cth-text-display-sm\|cth-lh-display-sm" src/renderer/src --include=*.tsx --include=*.ts --include=*.css | awk -F: '{n+=$2} END {print n}'` prints `0` across the whole renderer. **The `awk` sum is the criterion, not the bare `grep -rc`** — `grep -rc` prints one `path:count` line per file and never a number, so "returns `0`" was ungradable: measured 2026-08-21 it emits **148 lines** here, ten of them `.png:` binaries. Plan 23 pipes the identical construct through the same `awk`; the `--include` filters keep binary assets out of the scan. **Baseline today: the sum is `5`** — `AgentCard.tsx:220`, `:221`, `ThreadsPanel.tsx:101`, `tokens.css:63`, `:73`. Paste before and after.
    - Parsing `tokens.css`, no `--cth-text-*` declaration has a px value below 14. Paste the four token lines.
    - `grep -c "cth-font-ui" src/renderer/src/components/AgentCard.tsx` returns at least `1` (`0` today).
    - `grep -c "cth-font-ui" src/renderer/src/components/ThreadsPanel.tsx` returns **strictly greater than `5`**, its recorded baseline — `at least 1` would pass untouched.
    - `grep -c "cth-text-display-sm" src/renderer/src/components/ThreadsPanel.tsx` returns `0` (`1` today, at `:101`) — the Rule 1b conversion is what this criterion actually gates, and it is the same-commit obligation the token deletion depends on.
    - **The token deletion and its only two consumers are in ONE commit, counted rather than eyeballed.** With `SHA` bound to that commit: `SHA=<the sha of this commit>; git show --name-only --format= "$SHA" | sort -u | grep -cE "^(src/renderer/src/design/tokens\.css|src/renderer/src/components/(AgentCard|ThreadsPanel)\.tsx)$"` returns exactly `3`. Paste the command and the `3`. A `git show --stat` read by eye is not a criterion, and splitting the deletion from its consumers across two commits leaves the renderer resolving `var(--cth-text-display-sm)` to nothing at every intermediate commit — which is what this gate exists to prevent.
    - `grep -c "FONT_SIZE = 14" src/renderer/src/scene/office/ThoughtBubble.ts src/renderer/src/scene/office/ToolBubble.ts` returns `1` each.
    - `grep -cE "fontSize: Math\.max\(14, scale\.name - 3\)" src/renderer/src/components/FullscreenTerminal.tsx` returns `1`, and `grep -cE "Math\.max\(9, scale\.name - 3\)" <the same file>` returns `0` (today: `0` and `1`, at `:695`). Matched on the text because the line has drifted off UI-SPEC's `:694`.
    - **Rule 3b, asserted per site — all six are in this plan's own files:**
      `grep -cE "clamp\(zoom \* [0-9.]+, ([0-9]|1[0-3])," src/renderer/src/components/FullscreenTerminal.tsx` returns `0` (today `3` — floors of 7, 7 and 10), and `grep -nE "clamp\(zoom \* [0-9.]+," <the same file>` shows all three `rosterScale` entries with a floor of `14` or higher **and** a cap at or above that floor. Paste all three lines.
      `grep -nE "const note(FontSize|LabelSize) = " src/renderer/src/components/FullscreenTerminal.tsx` shows both floored at 14 — paste both lines with their evaluated minimum. Today `:566` has no floor beneath its cap of 14 (the shared zoom's own minimum is 8) and `:567` evaluates to `8` at every zoom.
      `grep -cE "fontSize: 10\.5" src/renderer/src/components/AgentCard.tsx` returns `0` (today `1`, at `:308`).
    - **Residual blind-spot scan over this plan's six source files:** M1d returns `0` hits, and every M1x hit is pasted with its evaluated minimum, every minimum at or above `14`. This is what makes the Rule 3b table a bar rather than a list — a seventh invisible site added later fails it.
    - `grep -c "display-sm" DESIGN.md` returns `0`, and `grep -n "14/16" DESIGN.md` shows the amended `:119`.
    - **Rule 1c, the boot splash, asserted per site.** `grep -cE "font-size: *(1[0-3]|[0-9])(\.[0-9]+)?px" src/renderer/index.html` returns `0` — **baseline `1` today, measured 2026-08-21, the `.title` rule at `:27`**; paste before and after. The decimal branch is in the pattern deliberately, so `12.5px` cannot be substituted for `13px` and pass. Beside it paste `grep -n "font-size" src/renderer/index.html`, which must still show exactly **three** declarations — `.mk` 16px, `.title` now 14px, `.hint` 15px — proving the sweep raised the one site rather than deleting a rule to clear the grep. And `grep -c 'font-family: "Press Start 2P"' src/renderer/index.html` returns **`2`**, its value today: the family is unchanged at both `.mk` and `.title`, so this is a size fix and not the typeface retirement UI-SPEC's Rule 1 note forbids. Match the `font-family:` prefix, not the bare string — `grep -c "Press Start 2P"` returns **`3`** here, because `:9` is a comment about the typeface, and a literal `2` against the bare string would fail on correct code and invite deleting the comment to make the number agree.
    - `grep -c "cth-text" src/renderer/index.html` returns `0` — the splash still references no design token. It paints before `tokens.css` loads, so a token here resolves to nothing on the one screen with no fallback; this criterion is what stops a well-meaning executor from "finishing the job" with a `var()`.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today, of `426` total). Exit code alone
      is not a gate: `node --test` counts skipped tests in its total and exits `0` on an all-skipped file
      (verified), so a red test converted to `{ skip: '...' }` is indistinguishable from a green one by
      exit code. This task adds no tests, so any rise in `# skipped` is a red test being hidden rather
      than fixed.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The type scale clears the 14px floor, no dangling token reference exists, the three non-literal sites are at 14, and DESIGN.md §4.1 no longer contradicts DESIGN.md:706.</done>
</task>

<task type="auto">
  <name>Task 3: Sweep this plan's four files — Rules 0/1/2, the accessible-name pass, and containment</name>
  <files>src/renderer/src/components/AgentCard.tsx, src/renderer/src/components/AgentStrip.tsx, src/renderer/src/components/ThreadsPanel.tsx, src/renderer/src/components/FullscreenTerminal.tsx</files>
  <read_first>
    - Each of the four files in full — every hoisted style object, every `<button>`, and every fixed-height integer.
    - Task 1's per-file rule table — the classification is already done; this task executes it.
    - `01-UI-SPEC.md` § FLOOR-12's containment section and the accessible-name rule.
    - `src/renderer/src/components/FullscreenTerminal.tsx:660-670` — the house `aria-label` pattern (verb + object naming the agent) at `:665`.
    - `.planning/codebase/CONCERNS.md` — the two `<div role="button">` recorded as resolved history.
  </read_first>
  <action>
    Execute task 1's classification, file by file. For every `fontSize` line: Rule 0 → leave the size,
    add `aria-hidden="true"` to the element; Rule 1 → `var(--cth-text-display-md)` +
    `var(--cth-lh-display-md)`, **family unchanged**; Rule 2 → `var(--cth-text-body-md)` (or
    `var(--cth-text-mono-md)` where the object sets `--cth-font-mono`) plus the matching `--cth-lh-*`.
    Rule 1b's two sites landed in task 2 and must not be re-touched.

    Where a hoisted style object serves both a glyph-only element and any text element, sweep the shared
    declaration normally and give the glyph element a **local `fontSize` override**. Record that
    override's line as the allowlist entry. Allowlisting the shared declaration to protect the glyph
    would leave real text below the floor with the test green — a silent hole in the exact bar meant to
    close criterion 4.

    Accessible-name pass over these four files: every `<button>` whose only content is an icon, glyph or
    symbol gets an `aria-label` phrased verb + object, naming the agent where one is in scope. Every
    `<button>` with visible text content gets **nothing** — adding `aria-label` there overrides the
    visible label. **Do not convert the two `<div role="button">`** at `AgentCard.tsx:145` and
    `FullscreenTerminal.tsx:604`; they are a deliberate documented decision and both already carry an
    accessible name.

    Containment: after the size changes, check each recorded fixed-height anchor. Apply UI-SPEC's
    three-step rule **in order** — do nothing where `nowrap`/`ellipsis` already truncates; otherwise
    raise the container integer by the measured delta and change nothing else; otherwise stop and report
    with the file:line and the measured overflow. **Never** reflow a card, move or drop a field, or hold
    a site below 14px to make room.

    Record this plan's final allowlist as a literal `file:line` array in the SUMMARY. Plan 23 assembles
    all seven sweep plans' arrays (01-14 through 01-20) into `test/repo-claims.test.cjs`; **do not edit that test file here** — it is
    not this wave's to hold.
  </action>
  <acceptance_criteria>
    - For each of the four files, `grep -hoE "fontSize *[:=] *\{?(1[0-3]|[1-9])($|[^0-9.])" <file> | wc -l` returns exactly the number of allowlisted sites recorded for that file — paste both numbers per file. **Occurrences (`-o`), not lines (`-c`)**: plan 23 counts occurrences in wave 9, and the two units differ wherever one physical line carries two matches. Measured today these four files are equal in both units (AgentCard 6, AgentStrip 12, ThreadsPanel 8, FullscreenTerminal 12), so the unit change costs nothing here and keeps the whole phase on one unit.
    - **Residual blind-spot scan over the same four files:** M1d returns `0` hits and every M1x hit is pasted with its evaluated minimum, every minimum at or above `14`. Otherwise sub-14px text M1 cannot see still ships, and plan 23's repo-wide bar cannot see it either.
    - Every allowlisted element carries `aria-hidden="true"`: paste each allowlisted line with the two lines following it.
    - `grep -c "role=\"button\"" src/renderer/src/components/AgentCard.tsx src/renderer/src/components/FullscreenTerminal.tsx` returns **the same value as before this task** for each — paste both numbers. Do **not** assert a literal: each file returns `2` today (the attribute plus the comment above it that explains why it is a `div role="button"` — `AgentCard.tsx:137`/`:145`, `FullscreenTerminal.tsx:598`/`:604`), so a literal `1` fails on correct code and invites deleting the explanatory comment to make the number match.
    - `grep -c "aria-label" src/renderer/src/components/AgentStrip.tsx` is greater than before this task, and no `aria-label` was added to a button with visible text — paste the added labels with their button bodies.
    - Any container integer changed is listed in the notes with its before value, after value and the measured delta.
    - **Containment, path-scoped so it survives the concurrent wave.** Plan 13 runs in wave 6 against
      the same working tree (`use_worktrees: false`), so a bare whole-tree `git diff --stat` shows its
      edits too — **do not use one as a containment check.** Stage and commit with an explicit pathspec
      (`git add -- <this plan's paths>` then `git commit -- <the same paths>`); never `git add -A`,
      `git add .` or `git commit -a`, which sweep a wave-mate's unstaged work into this plan's commit.
      Then assert all three:
      (a) **per commit and filtered.** Bind both variables **inside** the criterion: a criterion is its
      own shell invocation, and an unbound `$SHAS` iterates zero times, so the filter would emit nothing
      and score as PASS having checked nothing. Substitute the literal base sha task 1 recorded for
      `<base sha>`, then run:
      `BASE=<base sha>; SHAS=$(git log --format=%H "$BASE"..HEAD -- <this plan's nine paths>); echo "$SHAS"; for sha in $SHAS; do git show --name-only --format= "$sha"; done | sort -u | grep -vE "^(DESIGN\.md|src/renderer/index\.html|src/renderer/src/design/tokens\.css|src/renderer/src/components/(AgentCard|AgentStrip|ThreadsPanel|FullscreenTerminal)\.tsx|src/renderer/src/scene/office/(ThoughtBubble|ToolBubble)\.ts)?$"`
      The filter must produce **no output**, and `echo "$SHAS" | grep -c .` must be **at least 1** — paste
      both, plus `for sha in $SHAS; do git show --stat --format= "$sha"; done` so the touched set is on
      the page. The trailing `?` in the pattern absorbs the blank line `--format=` emits; the `grep -c .`
      floor is load-bearing, because with no commits made the loop body never runs and the filter is
      empty for the wrong reason. This replaces an unfiltered `git show --stat --format=` adjudicated by
      eye — a diffstat with no pass/fail filter is not a criterion. **Selected by pathspec, so this is
      this plan's own commits:** plan 13 commits into this same tree in this same wave, and a tree-wide
      filter would fail this plan on its work;
      (a2) **positive — the declared set actually moved.**
      `git diff --name-only <base sha>..HEAD -- <this plan's nine paths> | sort -u` lists **all nine**
      paths of `files_modified` — paste the list beside `... | sort -u | grep -c .`, which must print
      exactly `9`. The count is the whole gate and is equivalent to set equality here: a pathspec-scoped
      diff can only ever emit paths from that pathspec, so nine unique lines can only be the nine declared
      paths. Without it, silently skipping a file — `src/renderer/index.html` is the likeliest, being the
      one path outside `src/renderer/src` — leaves every other criterion in this plan still satisfied;
      (b) `git status --porcelain -- <this plan's paths>` is empty, so this plan leaves none of its own
      edits uncommitted for the next wave-mate's `git add` to absorb.
    - `npm run typecheck` exit code is `0`; `npm test` exit code is `0`, **and the TAP counters are
      pasted and compared against task 1's baseline**:
      `TAP=$(mktemp); node --test --test-reporter=tap test/*.test.cjs > "$TAP"; echo "EXIT=$?"; grep -E "^# (tests|pass|fail|skipped|todo) " "$TAP"; rm -f "$TAP"`
      Required: `EXIT=0`, `# fail 0`, `# todo 0`, `# skipped` **no higher** than task 1's baseline
      (`4` today) and `# pass` **no lower** than task 1's (`422` today). This plan adds no tests, so any
      movement in `skipped` is a red test being hidden rather than fixed — which exit code alone cannot
      distinguish, since `node --test` counts skips in its total and exits `0`.
  </acceptance_criteria>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All four files clear the floor except their allowlisted glyphs, every icon-only button is named, and every containment decision is recorded with a measured delta.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| rendered UI → an operator making a safety decision | Text below the legibility floor and unnamed controls are accessibility and misreading risks on a surface where the operator decides whether to let agents keep running. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P14-01 | Information disclosure | text an operator cannot read at 7-13px | mitigate | The token scale clears 14px and every literal in this group is swept to a token; the completeness bar is asserted in plan 23. |
| T-P14-02 | Denial of service | a bumped site clipping inside fixed chrome, hiding information | mitigate | UI-SPEC's three-step containment rule applied in order, with every container change recorded as a measured delta; "stop and report" is the third step, not a redesign. |
| T-P14-03 | Repudiation | a dangling `var(--cth-text-display-sm)` resolving to nothing | mitigate | The token deletion and its only two consumers land in one commit, asserted by `git show --stat` and a repo-wide grep returning `0`. |
| T-P14-04 | Repudiation | allowlisting a hoisted style object and leaving real text below the floor | mitigate | Hoisted objects are swept normally and the glyph gets a local override whose line enters the allowlist — stated in the task and checked by the per-file grep count. |
| T-P14-05 | Information disclosure | a control with no accessible name | mitigate | Icon-only buttons get `aria-label`; text buttons deliberately get none; decorative glyphs get `aria-hidden`. The rule is asserted per element, not as a ratio. |
| T-P14-06 | Repudiation | DESIGN.md naming sizes that no longer exist | mitigate | §4.1 `:119`, `:128` and `:129` are corrected together; the known-drift rows are explicitly out of scope and recorded as such. |
| T-P14-07 | Information disclosure | sub-14px text M1 structurally cannot see — decimals, quoted px, ternaries and clamps — passing plan 23's bar while shipping at 7px | mitigate | Rule 3b names all six sites in this group with per-site criteria, and the M1d/M1x residual scans turn that list into a bar a seventh site would fail. |
| T-P14-08 | Repudiation | the terminal carve-out stretched from xterm's own font to any chrome that reads the zoom | mitigate | The carve-out is stated once in `<interfaces>` with its predicate (`terminalFontSize.ts`, `terminalPool.ts`, `term.options.fontSize`) and applied to every sibling consistently rather than silently to some. |
| T-P14-09 | Tampering | a bare `git add` sweeping plan 13's concurrent edits into this plan's commit | mitigate | Pathspec-scoped staging and commit, per-commit `git show --name-only --format=` containment through an allowlist filter with `$SHAS` bound inside the criterion, and a path-scoped `git status --porcelain` proving nothing of this plan's is left uncommitted. |
| T-P14-10 | Repudiation | a red test hidden as a skip to make the sweep green | mitigate | The TAP counters are baselined in task 1 and re-compared after the sweep; `# skipped` may not rise. `node --test` exits `0` on an all-skipped file, so exit code alone is not the gate. |
</threat_model>

<verification>
- `npm test` and `npm run typecheck` green on all three platforms.
- Manual: launch the dev app and look at the agent strip, an agent card, the fullscreen roster and the
  threads panel. Confirm nothing is clipped, the agent name still truncates rather than wraps, and the
  Pixi thought/tool bubbles still fit their balloons.
</verification>

<success_criteria>
- Completeness-bar clauses 1 and 4 (UI-SPEC) are TRUE: `tokens.css` declares no `--cth-text-*` value
  below 14px with `display-sm` gone, and the three non-literal sites are at or above 14.
- Clause 4 holds **in substance, not only at UI-SPEC's three named sites**: every Rule 3b site in this
  group evaluates to 14 or above at every input, and the M1d/M1x residual scans over this plan's files
  are clean.
- This plan's four files satisfy clauses 2 and 3 for their own lines.
- `DESIGN.md` §4.1 no longer contradicts `DESIGN.md:706`.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-14-SUMMARY.md` when done. It MUST contain the pasted
M1/M2/M3 baselines **in occurrence units**, the M1d/M1x blind-spot scans before and after with every
M1x hit's evaluated minimum, this plan's final Rule 0 allowlist as a literal `file:line` array for plan
23, this plan's base sha with every commit sha and its `git show --stat`, the TAP counters before and
after, and every container integer changed with its measured delta.
</output>
