---
phase: 01-finish-the-floor
plan: 11
type: execute
wave: 5
depends_on: ["01-06", "01-09"]
files_modified:
  - src/main/hive.ts
  - test/hive-durability.test.cjs
autonomous: true
requirements: [FLOOR-04]

must_haves:
  truths:
    - "A secret written into an agent's files is scrubbed before the hive commits it, so it never reaches git history"
    - "The scrub uses the ONE pattern set the project already trusts — no second matcher is introduced"
    - "A scrubbed path is unstaged and logged loudly, not silently dropped"
    - "The single-committer git model and the existing untrackIgnored contract are preserved"
  artifacts:
    - path: "src/main/hive.ts"
      provides: "a redactSecrets-driven check between `git add -A` and `git commit` inside flushCommit"
      contains: "redactSecrets"
    - path: "test/hive-durability.test.cjs"
      provides: "an integration test over a REAL temp git repo asserting the secret never reaches git log -p"
      contains: "git"
  key_links:
    - from: "flushCommit"
      to: "redactSecrets"
      via: "git diff --cached piped through the existing matcher before the commit"
      pattern: "diff --cached"
    - from: "a matching path"
      to: "git restore --staged"
      via: "unstage the offending path and log loudly"
      pattern: "restore"
---

<objective>
Scrub secrets at the single commit choke point, so an agent that writes a key into its workspace does
not put it in the hive's git history.

Purpose: FLOOR-04. `redactSecrets` exists (`hive.ts:324`) and is **mail-only** — applied to
`subject`/`body` in `voiceMessages()` at `:2011-2012` and passed as `sanitize:` at `index.ts:410`. It
is applied nowhere in the git commit path, which is a bare `git add -A` over an agent's workspace with
no content inspection.

Output: one check inside `flushCommit`, reusing the one pattern set the project already trusts, plus
an integration test over a real git repo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-finish-the-floor/01-CONTEXT.md
@.planning/phases/01-finish-the-floor/01-RESEARCH.md
@.planning/phases/01-finish-the-floor/01-PATTERNS.md
@docs/adr/0004-single-committer-git.md

<interfaces>
The commit path: `src/main/hive.ts:2698` `commit(message)` → `scheduleCommit` (debounced) →
`flushCommit` (`:2729`), which does `await this.untrackIgnored(root)`, then
`gitAsync(['add','-A'], root)`, then `gitAsync(['commit', ...])`.

`redactSecrets` — `src/main/hive.ts:324`. The single sanitiser the project trusts.

`UNTRACK_PATHS` — `src/main/hive.ts:288`; the hive repo already excludes `cost-ledger.jsonl`,
`log.jsonl`, `log.jsonl.1` and `backups` from versioning. The comment at `:286-288` explains that a
`.gitignore` line does nothing to an already-tracked file, which is why exclusions go through
`untrackIgnored()` and not through a raw gitignore edit.

**Scope note on `add -A`.** The repo-wide rule *"no `git add .` / `git add -A`, stage specific files
only"* governs commits to THIS repository. `hive.ts`'s `flushCommit` runs `add -A` inside the **hive**
repo, which is a different repository and a deliberate design (ADR-0004, single committer). Do not
"fix" that as part of this plan.

`test/hive-durability.test.cjs` already drives a **real** `git` against a real temp repo — that is
the harness to extend, per the house rule that you never mock the thing under test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the FLOOR-04 clause against current source and inventory what redactSecrets actually matches</name>
  <files>(none — evidence only)</files>
  <read_first>
    - `src/main/hive.ts:280-340` — `UNTRACK_PATHS`, its comment, and `redactSecrets` in full including every pattern in its set.
    - `src/main/hive.ts:1990-2020` — the mail-only application in `voiceMessages()`.
    - `src/main/index.ts:405-415` — the `sanitize:` wiring.
    - `src/main/hive.ts:2690-2760` — `commit`, `scheduleCommit`, `flushCommit`, `untrackIgnored`, and `gitAsync`'s signature and error shape.
    - `test/hive-durability.test.cjs` in full — how it stands up a real temp git repo and cleans up.
    - `docs/adr/0004-single-committer-git.md` — the constraint on the commit path.
  </read_first>
  <action>
    Quote issue #10's FLOOR-04 acceptance text verbatim and run one command per clause with ACTUAL
    output pasted:
    - `grep -n "redactSecrets" src/main/*.ts` — expect the definition plus mail-only call sites, and **no** call in the commit path. That absence is the evidence.
    - `grep -n "add', '-A'\|'add','-A'\|add -A" src/main/hive.ts` — the bare staging call.
    - `grep -n "UNTRACK_PATHS\|untrackIgnored" src/main/hive.ts` — the existing exclusion mechanism.

    Then write down `redactSecrets`'s actual pattern inventory — list every pattern it matches, read
    from source, not assumed. That list is what the commit-path scrub will and will not catch, and it
    must be stated honestly in the SUMMARY: this is a best-effort matcher, not a guarantee, and
    claiming more would be exactly the kind of false self-claim this phase removes.

    State explicitly that **no second pattern set will be built.** Two matchers that disagree is worse
    than one imperfect one, and the project already trusts this one for mail.
  </action>
  <acceptance_criteria>
    - Task notes contain pasted output for all three greps, with the `redactSecrets` grep showing no commit-path caller.
    - Task notes contain the enumerated pattern inventory read from `hive.ts:324`'s implementation.
    - Task notes state the honest ceiling of the matcher in one sentence.
    - `node --test test/hive-durability.test.cjs` exit code is `0` (baseline).
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-durability.test.cjs</automated>
  </verify>
  <done>The clause is confirmed open with pasted evidence and the matcher's real coverage is written down before anything depends on it.</done>
</task>

<task type="auto">
  <name>Task 2: Scrub the staged diff inside flushCommit, reusing the existing matcher</name>
  <files>src/main/hive.ts</files>
  <read_first>
    - `src/main/hive.ts:2690-2760` — `flushCommit` in full; this is the file and function being modified.
    - `src/main/hive.ts:280-340` — `redactSecrets` and `UNTRACK_PATHS`.
    - `01-RESEARCH.md` § "FLOOR-04 — the secret scrub is genuinely absent from the commit path" — the recommended shape and the ADR-0004 / `untrackIgnored` constraints.
    - `.planning/codebase/CONVENTIONS.md` — the discriminated result object, `e instanceof Error ? e.message : String(e)`, never throw across IPC, and the comment convention.
  </read_first>
  <action>
    Inside `flushCommit`, **after** `gitAsync(['add','-A'], root)` and **before** the commit: run
    `git diff --cached` and pass its output through the existing `redactSecrets` matcher. If it
    matches, resolve the offending paths from the diff, unstage them with
    `gitAsync(['restore','--staged', <path>], root)`, and log loudly — the operator must be able to
    see that a commit was altered and why. Then commit what remains.

    Reuse `redactSecrets` exactly. **Do not build a second pattern set.** If a pattern needs widening,
    widen the one matcher so mail and commits stay in agreement, and say so in the SUMMARY.

    Keep this inside `flushCommit`, which is the single choke point ADR-0004 guarantees. Do not add a
    pre-commit hook, a second committer, or a scan anywhere else — the whole value of the single
    committer model is that there is exactly one place to put this.

    If a path must become permanently excluded rather than merely unstaged, route it through
    `untrackIgnored()`, not through a raw `.gitignore` line — the comment at `:286-288` records that a
    gitignore line does nothing to an already-tracked file.

    Handle the failure modes explicitly and never throw out of the debounced flush: a failing
    `git diff --cached`, a failing `git restore --staged`, and a diff so large that scanning it is
    expensive (bound what is scanned and say what the bound is). A scrub that crashes the committer
    would take the hive's whole durability path down with it, which is a worse failure than the one it
    prevents.

    Carry the comment that explains all of this, citing FLOOR-04 and ADR-0004: why the check is here
    and not at a second site, why `redactSecrets` is reused rather than duplicated, and what the
    matcher does not catch.

    **Do not touch `src/main/index.ts`, `src/main/hooks.ts` or `src/preload/index.ts`** — other plans
    hold them this wave.
  </action>
  <acceptance_criteria>
    - `grep -c "redactSecrets" src/main/hive.ts` returns at least `2` (the definition plus the new commit-path use).
    - `grep -c "diff', '--cached'\|diff --cached" src/main/hive.ts` returns at least `1`.
    - `grep -c "restore" src/main/hive.ts` returns at least `1`.
    - `git diff --stat` for this task shows changes ONLY to `src/main/hive.ts`.
    - `npm run typecheck` exit code is `0`.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-durability.test.cjs</automated>
  </verify>
  <done>The staged diff is scanned with the one trusted matcher between staging and commit, offending paths are unstaged and logged loudly, and no failure path can crash the debounced committer.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the secret never reaches git history, over a real temp git repo</name>
  <files>test/hive-durability.test.cjs</files>
  <read_first>
    - `test/hive-durability.test.cjs` in full — the real-git harness, its temp-dir lifecycle, and its existing assertions.
    - `01-PATTERNS.md` § "Test-file skeleton" `:1209-1234` and § "`test/db-fts.test.cjs`" `:294-322` for the `realpathSync`-wrapped temp dir helper.
    - Task 1's pattern inventory — the test's fixture secret must be one the matcher actually catches, and the test must also cover a value it does NOT catch so the ceiling is documented rather than implied.
  </read_first>
  <action>
    Extend `test/hive-durability.test.cjs` — do not create a new file; this one already drives a real
    `git` against a real temp repo, which is the "never mock the thing under test" requirement.

    Tests to add:
    1. **The graded clause.** Write a file containing a secret matching `redactSecrets`'s inventory into
       the agent workspace, trigger the commit path, then run `git log -p` in the temp repo and assert
       the secret string does not appear anywhere in the output. This is criterion 3's clause stated
       literally: *"a secret written into an agent's file does not appear in `git log -p` of the hive."*
    2. **The commit still happens.** The rest of the change is committed — a scrub must not silently
       drop the whole commit. Assert the non-secret file IS in `git log -p`.
    3. **Loud, not silent.** The scrub logs; assert on the injected log fake or on the emitted event, so
       an operator can tell a commit was altered.
    4. **The documented ceiling.** Add one test with a secret-shaped value the matcher does NOT catch,
       asserting current behaviour, with a comment stating this is the honest limit of a best-effort
       matcher — not a bug, and not a licence to claim more in the docs. Name RECALL/GATE follow-ups if
       any apply.

    Every assertion carries a message stating what a failure would MEAN.
  </action>
  <acceptance_criteria>
    - `node --test test/hive-durability.test.cjs` exit code is `0`, with a test count at least 4 higher than the task 1 baseline.
    - The graded test greps real `git log -p` output: `grep -c "log', '-p'\|log -p" test/hive-durability.test.cjs` returns at least `1`.
    - The test file contains no mock or fake of `git` itself — paste the harness lines showing a real `git` invocation.
    - Task notes contain the RED output of test 1 against pre-fix source, then the GREEN output.
    - `npm test` exit code is `0`.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/hive-durability.test.cjs</automated>
  </verify>
  <done>A real-git integration test proves the secret never reaches history, the commit still lands, the scrub is loud, and the matcher's limit is documented by a test rather than by a promise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent workspace files → the hive git repository | An LLM-controlled process writes files that a debounced committer stages with `add -A` and commits without inspection. Git history is durable and hard to redact after the fact. |
| hive git history → anywhere the hive repo is shared or backed up | Once committed, a secret is in every clone and every backup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-P11-01 | Information disclosure | a secret written by an agent landing in git history | mitigate | Scan `git diff --cached` with the existing `redactSecrets` between `add -A` and `commit`; unstage offending paths and log loudly. Single choke point per ADR-0004. |
| T-P11-02 | Information disclosure | a secret the matcher does not recognise | accept + document | Best-effort matching is the honest ceiling. A test pins the current limit and the SUMMARY states it; the docs must not claim more. GATE-02 (a later phase) covers the distinct problem of the child env. |
| T-P11-03 | Tampering | a second, disagreeing pattern set | mitigate | Reuse `redactSecrets` only. Two matchers that disagree is worse than one imperfect one; widen the single matcher if needed so mail and commits stay consistent. |
| T-P11-04 | Denial of service | the scrub throwing and taking down the debounced committer | mitigate | Every git call is handled with the repo's discriminated-result idiom; a failure logs and degrades rather than throwing out of `flushCommit`. |
| T-P11-05 | Denial of service | scanning an enormous staged diff on every commit | mitigate | Bound what is scanned and state the bound in the code comment; the hive already bounds its own files via `LOG_ROTATE_BYTES` and `UNTRACK_PATHS`. |
| T-P11-06 | Repudiation | a commit silently altered without the operator knowing | mitigate | The scrub logs loudly and the behaviour is asserted by a named test. |
</threat_model>

<verification>
- `npm test` green on all three platforms.
- `npm run typecheck` green.
- Manual (optional but recommended): drop a fake key into an agent's workspace in the dev app, let the
  hive commit, and confirm `git log -p` in the hive repo does not contain it and the log records the scrub.
</verification>

<success_criteria>
- ROADMAP criterion 3's secret clause is TRUE: a secret written into an agent's file does not appear
  in `git log -p` of the hive.
- Issue #10's FLOOR-04 clause closes in its PR per D-43, with the pasted evidence that `redactSecrets`
  had no commit-path caller.
</success_criteria>

<output>
Create `.planning/phases/01-finish-the-floor/01-11-SUMMARY.md` when done. It MUST include the
enumerated `redactSecrets` pattern inventory, the honest statement of what the matcher does not catch,
and the diff-scanning bound chosen.
</output>
